---
phase: 17-permissions-elicitation
plan: 05
type: execute
wave: 4
depends_on: ["17-03", "17-04"]
files_modified:
  - docs/permissions-gate.md
  - internal/acpserve/permissions_e2e_test.go
autonomous: false
requirements: [ACP-01, ACP-02]
user_setup:
  - service: zed
    why: "Live native permission-dialog and elicitation-form rendering check (criteria 1+3 manual-only legs)"
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The gate pipeline precedence (hook verdict → permission ask → execute) is documented at ONE named chokepoint — internal/session gateCall — including the Phase-21 hook-join contract (deny-only authority from project scope), the ungated semantics, the automation decline policy, and the permissions.yaml grammar reference (criterion 5)"
    - "No second gate path exists: rule-evaluation calls appear in internal/session only inside gate.go (region-checked by grep over the session package's non-test sources)"
    - "The deterministic Zed-client simulator (16-06) drives full turns green end-to-end: a gated mutating turn (dialog frame → allow_always → rule on disk → execution → no second dialog), an elicitation turn on a capable fake (form → accept → answered tool result), an elicitation turn on a degraded fake (-32601 probe → plain-text fallback), a turn death mid-dialog (cancelled-normal + cascade, no orphan), and an ungated default turn (zero permission frames)"
    - statement: "In a live Zed session, a gated mutating tool call renders Zed's native permission dialog with the four agent-supplied options, and a learning/engine ask renders as a native form — operator-verified in the WINDOWS ledger (A1 stable-channel caveat: confirm the four-option set, not just allow/reject)"
      verification: backstop
  artifacts:
    - path: "docs/permissions-gate.md"
      provides: "the ONE-chokepoint documentation: precedence chain, both-modes semantics, subagent/automation routing, permissions.yaml grammar + examples, deliberate CC divergences (tool-x-project both-direction persistence), Phase-21 hook-join contract"
      contains: "hook verdict"
    - path: "internal/acpserve/permissions_e2e_test.go"
      provides: "TestPermissionsE2E — the five-scenario simulator battery (permission happy path, elicitation capable, elicitation degraded, turn-death cascade, ungated default)"
      contains: "TestPermissionsE2E"
  key_links:
    - from: "docs/permissions-gate.md"
      to: "internal/session/gate.go"
      via: "the doc names gateCall as the single chokepoint Phase 21's PreToolUse hooks join at the head of — the doc and the code must name the same function"
      pattern: "gateCall"
    - from: "internal/acpserve/permissions_e2e_test.go"
      to: "the 16-06 Zed-client simulator"
      via: "E2E scenarios drive the simulator's pipe-level fake client (consume, do not rebuild); assertions observe wire frames + on-disk permissions.yaml + transcript lines"
      pattern: "simulator"
  prohibitions: []
---

<objective>
Close the phase: document the ONE gate pipeline (criterion 5), prove the whole surface end-to-end through the deterministic Zed-client simulator, and hand the two live-rendering criteria (1 and 3) to the operator checkpoint per the 15-07 WINDOWS-ledger pattern.

Purpose: criterion 5 exists so Phase 21's hooks join a documented pipeline instead of bolting a second gate; the E2E battery is the phase's own false-positive guard (unit-green but broken wiring); the live check covers what no deterministic test can (Zed's actual dialog/form rendering).
Output: docs/permissions-gate.md, the simulator E2E battery, operator confirmation recorded.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-permissions-elicitation/17-CONTEXT.md
@.planning/phases/17-permissions-elicitation/17-RESEARCH.md
@.planning/phases/17-permissions-elicitation/17-02-SUMMARY.md
@.planning/phases/17-permissions-elicitation/17-03-SUMMARY.md
@.planning/phases/17-permissions-elicitation/17-04-SUMMARY.md
@.planning/phases/16-acp-wire-foundation/16-06-PLAN.md
@internal/session/gate.go
@internal/acpserve/ask_surface.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `docs/permissions-gate.md` (NEW) — the chokepoint contract document referenced by Phase 21's hook join and by users hand-editing permissions.yaml.
- `internal/acpserve/permissions_e2e_test.go` (NEW) — `TestPermissionsE2E` five-scenario battery built on the 16-06 simulator fixtures.
</artifacts_this_phase_produce>

<tasks>

<task type="auto">
  <name>Task 1: Criterion-5 chokepoint documentation + no-second-gate check</name>
  <files>docs/permissions-gate.md</files>
  <read_first>internal/session/gate.go (the verdict enum, the hook-verdict head comment, the decision tree as shipped), internal/perm/rules.go + the 17-01 grammar tests (the documented grammar's source of truth), .planning/phases/17-permissions-elicitation/17-CONTEXT.md (D-01..D-07 — every documented behavior cites its decision), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (§CC Rule Model — the divergences section: CC's session-only file approvals vs our tool-x-project both-direction persistence; PAR-03 deny-only hook authority vs CC's hook-allow), .planning/ROADMAP.md Phase 21 (the hook-join consumer of this doc)</read_first>
  <action>
Write docs/permissions-gate.md with sections: (1) The pipeline — hook verdict → permission ask → execute, naming internal/session gateCall as the ONE chokepoint invoked per tool call in both runTurn branches, with the Phase-21 join contract at its head (hooks carry deny-only authority from project scope; repo-shipped files never grant allow; a deny stops the call before rule evaluation per D-04). (2) Mode semantics — ungated default evaluates rules and allows without dialogs (deny still denies, D-05); gated suspends ask-class calls (D-06's class derivation from the toolcat mutating/concurrency classes); automation turns decline ask-class loudly in both modes (D-07). (3) permissions.yaml — the three-list rule-string grammar with examples copied from the 17-01 test table, the dialog's narrow write surface (tool-x-project simple entries both directions, D-01/D-03), 0600 atomic discipline, and the hand-edit-only richer rules. (4) Deliberate CC divergences — tool-x-project both-direction persistence vs CC's session-only file approvals; deny-only hook authority vs CC's hook-allow (a project safety decision, not drift). (5) Degradation — -32601 permission fail-safe decline; elicitation plain-text fallback; stickiness per 16-D-18.
  </action>
  <verify>
    <automated>test -f docs/permissions-gate.md && grep -c 'gateCall' docs/permissions-gate.md && ! grep -rn 'Evaluate(' --include='*.go' --exclude='*_test.go' --exclude='gate.go' internal/session/ && go build ./internal/session/</automated>
  </verify>
  <acceptance_criteria>
    - docs/permissions-gate.md exists and names the gate function identically to gate.go (grep count >= 1).
    - The precedence chain appears in order (hook verdict before permission rules, D-04) and the Phase-21 deny-only join contract is stated.
    - The no-second-gate region check passes: zero rule-evaluation call sites across ALL internal/session/*.go non-test sources outside gate.go (the widened package-wide grep in the automated block — not a three-file sample).
    - `go build ./internal/session/` clean (the grep ran against compiling sources, not stale ones).
  </acceptance_criteria>
  <done>Criterion 5 closed: the ONE pipeline is documented where Phase 21's planner will find it, and the codebase provably has no second rule-evaluation path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simulator E2E battery — five scenarios on the 16-06 fake client</name>
  <precondition>16-06's deterministic Zed-client simulator exists and drives initialize/prompt turns over pipes (Phase 16 executed; its fixtures are importable)</precondition>
  <files>internal/acpserve/permissions_e2e_test.go</files>
  <behavior>
    - Permission happy path: gated session (config option set through the wire), a mutating tool call → the fake client receives exactly one session/request_permission frame with the four options, answers allow_always → the project's permissions.yaml on disk gains the allow entry → the tool executes (observable result frame) → a second identical call executes with zero further permission frames.
    - Elicitation capable: an AskUserQuestion turn on a fake advertising elicitation form support → one elicitation/create frame in the v1 form shape → accept → the answered tool result lands (captured-form shape).
    - Elicitation degraded: a fake answering the initialize probe with -32601 → the same ask renders as the plain-text AgentMessageChunk fallback (byte-parity with the v1.1 path) and the turn completes.
    - Turn death mid-dialog: the fake withholds the answer, the client sends session/cancel → the open ask resolves cancelled (one $/cancel_request observed), the transcript carries the cancelled-normal result, no orphaned frames follow.
    - Ungated default: a fresh default session performs a mutating tool call with zero request_permission frames the entire turn.
  </behavior>
  <read_first>.planning/phases/16-acp-wire-foundation/16-06-PLAN.md (the simulator's fixtures/API — consume its fake-client construction, do not rebuild), internal/acpserve/ask_surface.go + config_surface.go (17-02/17-04 output — the surfaces under test), internal/session/gate_test.go + askqueue_test.go (the fakes these scenarios compose at full stack), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (§Validation Architecture crit-1+3 live row — this battery is the deterministic half)</read_first>
  <action>
RED: write permissions_e2e_test.go with the five scenarios failing against the composed server (build the fake client from the 16-06 simulator fixtures; wire a temp project dir for the on-disk yaml assertions). Commit failing — if any scenario is already green, investigate why before proceeding (unexpected-green rule).
GREEN: fix wiring-level gaps the battery exposes (composition only — behavioral fixes belong to 17-02/03/04 and must be routed back there if the unit suites missed them; record any such routing in the SUMMARY). Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestPermissionsE2E' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All five scenarios green under -race in one battery run; the happy-path scenario asserts the on-disk rule entry and the no-second-dialog frame count.
    - The degraded scenario asserts the fallback chunk byte-parity fixture.
    - The turn-death scenario asserts the cascade frame and the cancelled-normal transcript line.
    - `mise ci` green end-to-end (the phase's standing gate — checked at wave merge / phase gate, not re-run inside this per-task verify).
  </acceptance_criteria>
  <done>The phase's wire-level story is proven end-to-end on the deterministic client: dialogs, forms, fallback, death cleanup, and the dialog-free default — all observable at the pipe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live-Zed operator checkpoint — native dialog + form rendering</name>
  <what-built>The full Phase-17 surface: clickable permission asks (allow/reject × once/always with persistence) and structured elicitation forms for the whole ask family, on the ONE documented gate pipeline with the ungated default preserved.</what-built>
  <how-to-verify>
1. In a scratch project, add permissions.mode: gated via Zed's agent settings (or the configOptions UI) and start a fresh ass-guard session.
2. Prompt a mutating tool call (e.g. ask the agent to write a file). EXPECT: Zed's native permission dialog with FOUR options — Allow once / Always allow / Reject once / Always reject (A1 caveat: if only two appear, the stable channel lags main — record the Zed version in the ledger).
3. Click Always allow. EXPECT: the tool runs, .ass-guard/permissions.yaml in the project gains the allow entry (0600), and a second identical call runs with NO dialog.
4. Start an ask (e.g. a question-shaped prompt or a learning-store ask). EXPECT: a native form (select/fields), answerable; on decline the agent proceeds with the non-answer.
5. Cancel a turn while a dialog is open. EXPECT: the dialog closes, no hang, the session stays responsive.
6. Flip permissions.mode back to ungated mid-session. EXPECT: no further dialogs on the running session.
7. Record PASS/FAIL per step in the WINDOWS ledger (15-07 PENDING-OPERATOR-CONFIRMATION pattern); a FAIL on step 2's option count is a ledger note, not a phase blocker (probe/degrade covers it).
  </how-to-verify>
  <resume-signal>Type "approved" if all steps behave as expected (or only step 2 shows a two-option dialog — include the Zed version), or describe the divergence observed per step.</resume-signal>
  <action>
Present the operator with the seven-step live-Zed checklist above and request confirmation of native rendering for criteria 1 and 3 (manual-only per RESEARCH's validation map: requires the live editor; the 16-06 simulator E2E in Task 2 bounds but cannot replace it). Record the disposition in 17-05-SUMMARY.md using exactly one literal marker: OPERATOR-CONFIRMED (the operator approved via the resume-signal — summarize their response beside the marker) or PENDING-OPERATOR-CONFIRMATION (the checkpoint is not yet answered, or workflow.auto_advance bypassed it — tracked in the WINDOWS ledger, 15-07 precedent). A two-option-only dialog on stable Zed is a ledger note (A1 caveat), not a divergence that blocks; anything else unexpected is surfaced, never silently passed.
  </action>
  <verify>
    <automated>S=.planning/phases/17-permissions-elicitation/17-05-SUMMARY.md; test -f "$S" && M=$(grep -c 'OPERATOR-CONFIRMED' "$S"); P=$(grep -c 'PENDING-OPERATOR-CONFIRMATION' "$S"); echo "confirmed=$M pending=$P"; test $((M + P)) -ge 1 && echo "disposition recorded in SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - The native four-option dialog renders and persists an always-allow (steps 2-3).
    - A native form renders and its answer lands as the tool result (step 4).
    - Cancel-mid-ask closes cleanly and the mode flip takes effect live (steps 5-6).
    - The ledger entry exists naming Zed's version and each step's outcome.
  </acceptance_criteria>
  <done>Criteria 1 and 3 confirmed against the real editor surface — the phase's only manual-only legs, closed the same way Phase 15 closed its live-Zed criterion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc ↔ code | the chokepoint doc must not name a pipeline the code does not implement (drift = Phase 21 joins a fiction) |
| E2E harness ↔ surfaces | the battery must exercise the composed server, not unit fakes short-circuiting the wire |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-14 | Repudiation | operator verification record | low | mitigate | The checkpoint records per-step outcomes + Zed version in the WINDOWS ledger and SUMMARY — the audit trail for the two manual-only criteria |
| T-17-15 | Tampering | documentation drift from code | medium | mitigate | The doc task's acceptance ties the named function to gate.go and the no-second-gate grep runs against compiling sources; Phase 21's planner reads this doc as canon |
| T-17-SC | Tampering | package installs | high | accept | No installs (stdlib + existing deps); legitimacy gate not applicable |
</threat_model>

<verification>
- `mise ci` green (the phase's standing gate, now carrying the E2E battery).
- docs/permissions-gate.md present with the no-second-gate check green.
- Operator ledger entry for criteria 1+3 (or the pending-confirmation note if the operator defers).
</verification>

<success_criteria>
Phase 17 closes: the ONE gate pipeline locked and documented (criterion 5), the full ask surface proven end-to-end deterministically, and the native-rendering criteria confirmed live — permissions and elicitation are editor-native with the v1.1 default preserved.
</success_criteria>

<output>
Create `.planning/phases/17-permissions-elicitation/17-05-SUMMARY.md` when done
</output>
