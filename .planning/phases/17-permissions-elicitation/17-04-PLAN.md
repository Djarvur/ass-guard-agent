---
phase: 17-permissions-elicitation
plan: 04
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - internal/acp/types.go
  - internal/acpserve/ask_surface.go
  - internal/acpserve/ask_surface_test.go
  - internal/session/ask.go
  - internal/session/elicitation_reply_test.go
  - internal/runtime/runtime.go
autonomous: true
requirements: [ACP-02]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Learning-store asks, engine asks, and model-initiated AskUserQuestion all render as structured elicitation/create forms on capable clients; on older clients the -32601/probe-degraded surface falls back to today's plain-text AskBroker path verbatim — either way the user can answer and the answer lands as the tool result (D-09, criterion 3)"
    - "Every existing ask kind has a defined form rendering per the D-08 mapping: single-choice → string property with oneOf titled consts; multiSelect → array property with items enum/anyOf; free-text → string property; boolean → boolean property ONLY when the client advertised boolean support, else a two-value string oneOf; headers/labels → property titles; N questions → N properties, all required; enumNames never used (RFD ban)"
    - "An invalid accept payload re-validates against the requested schema, re-asks ONCE with the violation noted in the message, and a second failure routes exactly like the 12-D-01 non-answer; decline and cancel route to the non-answer directly with NO re-ask — decline is an answer-shaped refusal, not a schema violation (D-10)"
    - "A string-valued single-field reply renders byte-identical to today's captured answered form (the zcode-interactive-results.json golden stays green); multi-field replies render deterministically in schema property order as title=value lines joined by newlines, array values comma-joined (Pitfall 6 / A10 — pinned serialization)"
    - "An AskUserQuestion with an empty Options list renders as a free-text property; an accept missing required properties is a D-10 violation (re-ask once, then non-answer); a null or absent reply routes to the non-answer form (edge probe: empty)"
    - "Enum/oneOf matching and length bounds use exact UTF-8 comparison — Go string equality for values, rune count for lengths; no Unicode normalization is applied to replies (edge probe: encoding)"
  artifacts:
    - path: "internal/acp/types.go"
      provides: "ElicitationFormFrame{Message, Mode, RequestedSchema, SessionID, ToolCallID} + ElicitationSchema{Type, Title, Properties, Required} — v1-verbatim camelCase, //nolint:tagliatelle"
      contains: "ElicitationFormFrame"
    - path: "internal/acpserve/ask_surface.go"
      provides: "BuildElicitationForm(questions []session.AskQuestion) frame builder (the D-08 mapping), capability-gated dispatch (sticky 16-D-13/D-18 state) with plain-text fallback routing, accept-content validation, decline/cancel routing"
      contains: "BuildElicitationForm"
    - path: "internal/session/ask.go"
      provides: "ResolveAsk widening: structured-reply seam rendering accept.content maps into the captured answered form; the D-10 re-validate/re-ask-once loop; executor suspension contract UNCHANGED"
      contains: "ResolveAsk"
    - path: "internal/session/elicitation_reply_test.go"
      provides: "TestElicitationRevalidation + reply-rendering goldens incl. byte-identity against zcode-interactive-results.json"
      contains: "TestElicitationRevalidation"
    - path: "internal/runtime/runtime.go"
      provides: "family conversion wiring: the AskBroker onSurface path enqueues through the queue; engine/learning asks (ActionAsk advisory path) surface as forms whose accept writes the learning entry as today"
      contains: "AskQuestion"
  key_links:
    - from: "internal/acpserve/ask_surface.go"
      to: "internal/acp Registry + capability cache (16-03)"
      via: "elicitation/create rides Registry.Call under HUMAN-ASK when the sticky capability is ok; the fallback branch reproduces runtime.go:984-989's plain-text publish verbatim"
      pattern: "elicitation/create"
    - from: "internal/acpserve/ask_surface.go"
      to: "internal/session/ask.go ResolveAsk"
      via: "accept.content maps render through the widened structured-reply seam into RenderAskAnswered's captured form; decline/cancel map to the 12-D-01 non-answer form"
      pattern: "accept"
    - from: "internal/runtime/runtime.go onSurface wiring"
      to: "internal/session/askqueue.go"
      via: "AskUserQuestion surfaces enqueue (class foreground-turn) instead of publishing directly; engine/learning asks enqueue as subagent/engine class (D-11 classes; firing monopoly)"
      pattern: "Enqueue"
  prohibitions:
    - requirement_id: ACP-02
      category: privacy
      statement: "An elicitation form MUST NOT disclose anything the equivalent plain-text ask would not — same question text and option labels only; no extra context (file paths, file contents, config values, environment) may be embedded in messages, property titles, descriptions, or defaults"
      status: unresolved
---

<objective>
Convert the whole ask family to editor-native structured forms: the D-08 typed mapping from AskQuestion shapes to ElicitationSchema properties, capability-gated dispatch with today's plain-text path as verbatim fallback, the widened structured-reply seam preserving the captured answered form, and D-10's bounded re-validation loop — for AskUserQuestion, learning-store, and engine asks alike (D-09).

Purpose: criterion 3 — every ask the agent makes becomes a clickable form on current clients while older clients keep working unchanged; the model's view of answers (the captured form) must not drift (Pitfall 6).
Output: elicitation wire types, surface mapping + dispatch, reply seam widening, family wiring in runtime.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-permissions-elicitation/17-CONTEXT.md
@.planning/phases/17-permissions-elicitation/17-RESEARCH.md
@.planning/phases/17-permissions-elicitation/17-PATTERNS.md
@.planning/phases/17-permissions-elicitation/17-02-SUMMARY.md
@.planning/phases/16-acp-wire-foundation/16-03-PLAN.md
@.planning/milestones/v1.1-phases/12-product-functional-completeness/12-CONTEXT.md
@internal/session/ask.go
@internal/coreexec/ask.go
@internal/runtime/runtime.go
@internal/acp/types.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/acp/types.go` — `ElicitationFormFrame`, `ElicitationSchema`, property-variant raw shapes (`json.RawMessage` per property), accept/decline/cancel action constants.
- `internal/acpserve/ask_surface.go` — `BuildElicitationForm`, `ValidateElicitationContent(schema, content)` (the D-10 validator: type/enum/oneOf/required/min-max length by rune count), the capability-gated dispatcher, decline/cancel routing.
- `internal/session/ask.go` — `ResolveAsk` widened with `RenderStructuredReply(answers)` deterministic serialization; the re-ask-once loop.
- `internal/runtime/runtime.go` — onSurface → queue enqueue wiring; engine/learning ask conversion (accept writes the learning entry; advisory non-answer preserved).
</artifacts_this_phase_produce>

<prohibition_probe_breadcrumbs>
Dropped as canon, not minted: pattern-evaluation resource abuse (ReDoS) — covered by the no-regex validation subset mandated below.
</prohibition_probe_breadcrumbs>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-08 mapping + elicitation frames + capability-gated dispatch</name>
  <precondition>internal/acp Registry + the initialize-time capability cache (16-03) are live: sticky elicitation capability is queryable, and the plain-text fallback path at runtime.go:984-989 still exists verbatim</precondition>
  <files>internal/acp/types.go, internal/acpserve/ask_surface.go, internal/acpserve/ask_surface_test.go</files>
  <behavior>
    - Mapping goldens (TestElicitationMapping): single-choice AskQuestion{Options, !MultiSelect} → property {"type":"string","title":Header,"oneOf":[{"const":Label,"title":Label,"description":Description},...]} — the enum names are the option labels, never enumNames; MultiSelect → {"type":"array","title":Header,"items":{"type":"string","enum":[labels]}} (anyOf-titled when descriptions exist); free-text (no Options) → {"type":"string","title":Header}; boolean ask → {"type":"boolean"} when the injected capability says boolean-advertised, else a two-value string oneOf (D-08 as locked, conservative); the question text lands in the request message; N questions → N properties, every name listed in required.
    - Frame shape: mode "form", requestedSchema.type "object", sessionId scope set, camelCase tags verbatim.
    - Dispatch: sticky capability ok → Registry.Call under HUMAN-ASK; capability degraded → the plain-text fallback reproduces today's AgentMessageChunk publish (RenderAskSurface content, same bus shape) — byte-parity asserted against a fixture built from the current path.
    - 16-03's initialize probe payload stays as-is (valid form; discretion resolved: no replacement needed).
    - No disclosure beyond the ask: builder input is exactly the AskQuestion slice — the builder has no other data source (structural guarantee for the prohibition above).
  </behavior>
  <read_first>internal/session/ask.go (AskQuestion struct :56-63 — the mapping input), internal/coreexec/ask.go (RenderAskSurface :100-133 — the fallback's exact output; the RegisterAsk discipline :70-85 — the executor stays untouched), internal/runtime/runtime.go (:984-989 the fallback wiring verbatim), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (§Wire Shapes elicitation/create verbatim field list; Pattern 4 + the D-08 mapping table with schema bases; RFD quotes — oneOf/anyOf, enumNames ban, validation duty), .planning/phases/16-acp-wire-foundation/16-03-PLAN.md (capability cache + sticky semantics), internal/acp/handlers.go (wire-struct nolint convention)</read_first>
  <action>
RED: ask_surface_test.go with the golden mapping cases (one per row of the D-08 table incl. empty-Options and boolean-both-branches), the frame shape case, and the dispatch/fallback cases with a fake registry + fake capability. Commit failing.
GREEN: add the elicitation frame types to internal/acp/types.go (properties as map[string]json.RawMessage so variant shapes ride verbatim); implement BuildElicitationForm + the dispatcher in ask_surface.go — capability consulted through the injected sticky-state reader (the conservative boolean gate reads the same connection capability family), fallback branch calls the existing RenderAskSurface path unchanged. The message is the question text; property names are stable derived keys (e.g. q1, q2 — deterministic, reused by the reply seam).
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestElicitationMapping|TestAskSurfaceDispatch' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Every D-08 table row has a golden case; the boolean-not-advertised branch yields the two-value string oneOf, the advertised branch the boolean property.
    - The dispatch test proves registry-Call on ok and byte-parity plain-text fallback on degraded.
    - The built frames carry required:"[every property]" and never the banned MCP-legacy enum-name key.
    - `go vet ./internal/acp/ ./internal/acpserve/` clean.
  </acceptance_criteria>
  <done>D-08 and D-09's surface half land: every ask kind has a wire-exact form rendering, dispatched by sticky capability with today's path as the verbatim fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structured-reply seam + D-10 re-validation loop</name>
  <files>internal/session/ask.go, internal/session/elicitation_reply_test.go, internal/acpserve/ask_surface.go, internal/acpserve/ask_surface_test.go</files>
  <behavior>
    - Valid accept with a single string-valued field: the rendered reply string is byte-identical to today's single-answer path — the existing golden (internal/coreexec/testdata/zcode-interactive-results.json) stays green untouched.
    - Valid multi-field accept: the reply renders in schema property order as title=value lines joined by "\n"; array values comma-join their elements; booleans render true/false (pinned serialization, A10).
    - Invalid accept (missing required property / wrong type / value outside the oneOf consts): re-validation fails, exactly ONE re-ask fires with the violation named in the new message ("previous answer invalid: <violation>"); a second invalid accept routes to the 12-D-01 non-answer form — bounded loop, never a third ask.
    - decline and cancel route directly to the non-answer form with zero re-asks (distinct from violations).
    - Validation semantics: exact Go string equality for const/enum membership; rune-count minLength/maxLength; arrays checked item-wise; no regexp and no Unicode normalization anywhere in the validator (encoding edge pinned).
    - A null/absent content object on accept is itself a violation (missing required) → the D-10 path (empty edge pinned).
  </behavior>
  <read_first>internal/session/ask.go (ResolveAsk :350 — the plain-string seam being widened; RenderAskAnswered :281-297 — the captured-form pairing the widening must preserve; resumeAskClaimed result rendering :368-416), .planning/milestones/v1.1-phases/12-product-functional-completeness/12-CONTEXT.md (D-01 the non-answer policy this routes to; D-05 advisory shape), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (Pattern 4 accept mapping; Pitfall 6; RFD "Agents SHOULD also validate received data" defense-in-depth), internal/coreexec/testdata/zcode-interactive-results.json (the golden the byte-identity case runs against)</read_first>
  <action>
RED: elicitation_reply_test.go covering the six behavior families (drive ResolveAsk's widened path with structured payloads; assert the golden file's answered-form fixture still matches for single string replies). Commit failing.
GREEN: widen the reply seam — accept a structured variant beside the plain string (string path untouched); implement the deterministic renderer per the pinned serialization; implement ValidateElicitationContent in ask_surface.go (the closed subset: type/enum/oneOf/required/min-max length, rune-counted, equality-exact); wire the re-ask-once loop at the surface/resolution boundary: first invalid accept → rebuild the form with the violation noted and fire once more; second failure or decline/cancel → hand the resolution to the existing non-answer routing. The re-ask is a NEW queue entry (same turn, same class) — never a bypass of the firing monopoly.
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestElicitationRevalidation|TestStructuredReplyRender' -count=1 && go test ./internal/coreexec/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The single-string byte-identity case passes AND the coreexec golden suite is green (no drift in the model's answered-ask view).
    - The re-validation case asserts exactly two asks total (initial + one re-ask) then the non-answer; decline/cancel cases assert one ask total.
    - The validator has no regexp import and no normalization call (`grep -c 'regexp\|Norm' internal/acpserve/ask_surface.go` over the validator section returns 0 — region-scoped to the validator file-level is acceptable since the whole file owns validation).
    - `mise ci` green.
  </acceptance_criteria>
  <done>D-10 lands as a bounded loop — the user always lands somewhere — and the captured answered-form discipline survives the structured upgrade byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 3: Family conversion wiring — AskUserQuestion, learning-store, engine asks</name>
  <files>internal/runtime/runtime.go, internal/acpserve/ask_surface.go, internal/session/ask.go</files>
  <action>
Wire the whole family through the queue + dispatcher (D-09): (1) replace the AskBroker onSurface callback body (runtime.go:984-989) so a question-shaped pending ask enqueues a foreground-turn-class entry whose fire calls the capability-gated dispatcher from Task 1 — the plain-text publish remains the dispatcher's fallback branch, not the primary; the AskUserQuestion executor in internal/coreexec is NOT modified (ErrSuspended + questions in Output contract unchanged — read coreexec/ask.go:51-85 and leave it alone). (2) Convert the engine/learning ask path (ActionAsk → ErrAskPending → the advisory path at runtime.go:546-548): the ask surfaces as a subagent/engine-class queue entry with an elicitation form; an accept renders through the structured seam and writes the learning-store entry exactly as today's answer path does (A9); decline/cancel render today's advisory non-answer note (12-D-05 shape) — persistence behavior unchanged, only the surface differs. (3) Keep emission subscriber-backed per the 13-03 discipline (17-03 pinned this for notes; the same constraint applies to any post-turn ask surface). Add wiring-level tests in ask_surface_test.go: an AskUserQuestion round-trip (suspend → form → accept → answered tool result) and an engine-ask round-trip (accept → learning entry written; decline → advisory note) using the 17-02 fakes.
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestAskSurfaceDispatch|TestFamilyConversion' -count=1 && go test ./internal/runtime/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The AskUserQuestion round-trip test observes: ErrSuspended executor contract intact, one queue entry (foreground class), a form frame on a capable fake, an accept mapping to the answered tool result via the captured form.
    - The engine-ask round-trip observes: a form frame (subagent/engine class), accept writing the learning entry through the existing store API, decline producing the advisory non-answer.
    - `grep -n 'RenderAskSurface' internal/runtime/runtime.go` shows the direct-publish call only inside the dispatcher fallback path (or its delegation), not as the primary surface.
    - `mise ci` green (the standing v1.1 ask suites stay green — fallback parity).
  </acceptance_criteria>
  <done>D-09 complete: the WHOLE ask family — model questions, learning-store, engine — rides one queue to one dispatcher with a verbatim fallback; consistent editor UX, unchanged executor contracts, unchanged answer persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → accept payload | accept.content is untrusted input claiming to match requestedSchema — validated, never trusted |
| form content → client | the form renders in the user's editor; disclosure must not exceed the plain-text parity bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-11 | Tampering | forged accept payloads | high | mitigate | D-10 agent-side re-validation against requestedSchema (RFD defense-in-depth); violations never execute anything — bounded re-ask then non-answer |
| T-17-12 | Information Disclosure | form messages/titles/defaults | medium | mitigate | Builder consumes exactly the AskQuestion slice (structural disclosure-parity bound); no paths/contents/config enter frames |
| T-17-13 | DoS | schema-pattern resource abuse | medium | mitigate | Validator is the closed subset with no regexp engine; equality and rune-count checks are linear (RFD's bounded-matcher MUST) |
| T-17-SC | Tampering | package installs | high | accept | No installs (stdlib + existing deps); legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/acpserve/ ./internal/session/ -count=1` green; `go test ./internal/coreexec/ ./internal/runtime/ -count=1` green.
- `mise ci` green — including the standing v1.1 ask/eval suites (fallback parity by construction).
</verification>

<success_criteria>
ACP-02 is complete: structured forms for the whole ask family with probe-and-degrade fallback, the D-08 mapping exhaustively pinned, D-10's bounded re-validation, and the captured answered form preserved byte-for-byte — criterion 3 holds at the wire/test level (live rendering is 17-05's operator checkpoint).
</success_criteria>

<output>
Create `.planning/phases/17-permissions-elicitation/17-04-SUMMARY.md` when done
</output>
