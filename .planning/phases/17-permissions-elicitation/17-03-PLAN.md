---
phase: 17-permissions-elicitation
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - internal/session/askqueue.go
  - internal/session/askqueue_test.go
  - internal/acp/handlers.go
  - internal/acp/handlers_test.go
  - internal/acpserve/acp_serve.go
autonomous: true
requirements: [ACP-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One ask is outstanding at a time — never stacked modals; the foreground turn's asks fire before subagent/engine asks, and within a class ask order is preserved FIFO; an already-open dialog is never preempted, priority orders only the waiting queue (D-11)"
    - "Every enqueue that does not fire immediately emits a session/update note reading 'ask queued — N pending' and increments the queue counter — the same counter family as the stall metrics (D-12); entries that fire immediately emit no note (spam killed by construction)"
    - "Turn death resolves the OPEN dialog as cancelled via the registry's ResolveCancelled + $/cancel_request cascade (16-D-19) AND drains that turn's queued-but-unfired asks as cancelled-normal immediately — no orphaned dialogs, no zombie asks firing for a dead turn (D-13)"
    - "While a permission dialog is open the session stays alive and responsive: other prompts queue and run, cancellation works, and a turn death mid-ask delivers cancelled as a NORMAL response — nothing blocks holding the turn mutex (criterion 2, proven under -race)"
    - "The drain runs from all three teardown paths — session/cancel notification, session close, serve shutdown — as one shared function (research Open Question 1), with zero goroutine leaks at shutdown"
    - "Concurrent enqueue/fire/drain operations are safe under go test -race, and no ask method is ever fired outside the queue's fire step (assumption-delta companion invariant, asserted with a counting fake registry)"
  artifacts:
    - path: "internal/session/askqueue.go"
      provides: "AskQueue full semantics: priority classes (foreground-turn before subagent/engine, FIFO within class), enqueue-note emission + counter, turn-id-carrying entries, DrainTurn(turnID) resolving open + queued entries, shutdown drain"
      contains: "DrainTurn"
    - path: "internal/acp/handlers.go"
      provides: "drain hooks: handleSessionCancel and the session-close path call the queue drain before/with cancelTurn (the existing teardown sites)"
      contains: "DrainTurn"
    - path: "internal/session/askqueue_test.go"
      provides: "TestAskQueue family: serialization, priority, notes/counter, turn-death drain, mutex-release proof, leak checks — all -race"
      contains: "TestAskQueue"
  key_links:
    - from: "internal/acp/handlers.go teardown sites"
      to: "internal/session/askqueue.go"
      via: "cancel notification, session close, and serve shutdown all reach the one drain function; the open entry resolves through the registry cascade and queued entries append cancelled-normal results without firing"
      pattern: "DrainTurn"
    - from: "internal/session/askqueue.go enqueue path"
      to: "the in-hand emitter / collector-drain shape"
      via: "queue notes for suspended turns ride a subscriber-backed emission (the 13-03 timing hazard: a bare post-turn bus publish is dropped when no subscriber exists)"
      pattern: "ask queued"
  prohibitions: []
---

<objective>
Expand the 17-02 minimal queue into the full D-11..D-13 discipline: priority classes with one-outstanding firing, visible enqueue notes + counter, and turn-id-carrying drain wired to every teardown path — plus the criterion-2 concurrency proofs (alive-while-asking, cancelled-normal on turn death, no mutex hold).

Purpose: criterion 2 is the phase's liveness contract — a human-scale dialog must never wedge the session or orphan a dialog. The queue is also the promoted identity for every ask surface (assumption-delta decision in 17-02): its firing monopoly is asserted here.
Output: askqueue.go full semantics, teardown wiring in acp handlers + acpserve, -race proof battery.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-permissions-elicitation/17-CONTEXT.md
@.planning/phases/17-permissions-elicitation/17-RESEARCH.md
@.planning/phases/17-permissions-elicitation/17-PATTERNS.md
@.planning/phases/17-permissions-elicitation/17-02-SUMMARY.md
@.planning/phases/16-acp-wire-foundation/16-03-PLAN.md
@internal/session/askqueue.go
@internal/session/ask.go
@internal/acp/handlers.go
@internal/acpserve/acp_serve.go
@internal/runtime/runtime.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/session/askqueue.go` — `askClass` ordering (foreground-turn preempts the waiting queue head; FIFO within class), `(*AskQueue).Enqueue` note+counter emission, `(*AskQueue).DrainTurn(turnID string)`, shutdown drain integration, the queue counter (D-12 family).
- `internal/acp/handlers.go` — drain calls at `handleSessionCancel` and the session-close path.
- `internal/acpserve/acp_serve.go` — serve-shutdown drain composition.
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Queue semantics — priority classes, visible notes, counter</name>
  <files>internal/session/askqueue.go, internal/session/askqueue_test.go</files>
  <behavior>
    - One outstanding: while entry A is fired (unresolved), a second enqueue B is queued, not fired; resolving A fires the head of the waiting queue.
    - Priority: with a subagent/engine-class entry waiting, a later foreground-turn-class enqueue becomes the next to fire (fg preempts the waiting queue head); two same-class entries fire in enqueue order (FIFO); an OPEN entry is never preempted regardless of class (D-11's letter).
    - Notes: an enqueue that fires immediately emits NO note; an enqueue that queues emits one session/update note with content exactly "ask queued — N pending" where N is the pending count including itself; the counter increments on every enqueue (D-12 — pinned discretion: immediate-fire suppression is the dedupe mechanism; wording is the pinned string).
    - Note delivery: notes for suspended turns ride a subscriber-backed emission (in-hand emitter or collector-drain) — a test with no active turn subscriber still observes the note (13-03 hazard pinned).
    - Entries carry turnID + class + seq; the queue exposes the pending count for the counter family snapshot.
    - Concurrency: N concurrent enqueues + resolutions under -race preserve one-outstanding and FIFO-within-class invariants; no double-fire, no lost entry.
  </behavior>
  <read_first>internal/session/askqueue.go (17-02 output — the minimal core being expanded), internal/session/ask.go (AskBroker guard discipline :97-110; claim-race concurrency template :169-224; the settle-channel discipline), internal/runtime/runtime.go (:528-548 the 13-03 timing hazard — subscribe-before/drain-after emission; :984-989 the bus-publish callback shape), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (Pattern 3 the queue sketch; Pitfall 3 stacked modals; D-12 counter family), .planning/phases/17-permissions-elicitation/17-CONTEXT.md (D-11/D-12 verbatim — the discretion items on wording and dedupe are pinned in the behavior block)</read_first>
  <action>
RED: write askqueue_test.go covering the six behavior families with fakes (fire funcs recording order; a note sink; a counter snapshot). Commit failing.
GREEN: extend AskQueue — entries keep class + seq; head selection picks the earliest foreground-class entry else earliest overall; the note emitter is an injected callback (wired at composition to the subscriber-backed path, NOT a bare bus publish); counter is the atomic family consistent with 16-01's stall-metric style. Keep the one-outstanding invariant under the queue's own mutex; never hold it across the fire callback's human-scale wait (fire runs on its own goroutine continuation like the 17-02 tracer).
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestAskQueue' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Serialization, priority, FIFO, no-preemption-of-open, note-suppression-on-immediate-fire, pinned note string, counter increments — each asserted by a dedicated case.
    - The no-subscriber note test proves delivery through the subscriber-backed path.
    - The concurrency case runs >= 50 interleaved operations under -race with invariants held.
    - `go vet ./internal/session/` clean.
  </acceptance_criteria>
  <done>D-11 and D-12 are complete: one dialog at a time with foreground priority, and a queue the user can SEE moving via notes and counters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn-death drain on all teardown paths + criterion-2 proofs</name>
  <precondition>internal/acp/request_registry.go exposes ResolveCancelled with the $/cancel_request cascade (16-03 executed) and 17-02's permission resume path handles the cancelled outcome</precondition>
  <files>internal/session/askqueue.go, internal/session/askqueue_test.go, internal/acp/handlers.go, internal/acp/handlers_test.go, internal/acpserve/acp_serve.go</files>
  <behavior>
    - Turn death with an OPEN ask: the drain resolves it via the registry cascade (ResolveCancelled → $/cancel_request observed on the wire) and the suspended turn's resume appends the cancelled-NORMAL result — no error result, no hang (criterion 2).
    - Turn death with queued-but-unfired entries: each drains immediately as cancelled-normal — appended transcript results, zero fires for the dead turn (D-13's zombie-ask ban).
    - Entries of OTHER turns are untouched by a drain (scoped by turn id).
    - One drain function is reachable from all three teardown paths: the session/cancel notification handler, the session-close path, and serve shutdown — each wired and each tested (research Open Question 1's recommendation).
    - Alive-while-asking (criterion 2, -race): while a permission dialog is open (fake unresolved fire), a second prompt starts and completes; a session/cancel mid-ask delivers cancelled-normal and closes the dialog.
    - Shutdown: serve ctx cancellation drains everything with zero goroutine leaks (wg/wait-group discipline) and no writes to a closed writer.
    - Firing monopoly (assumption-delta companion): with a counting fake registry, the full ask lifecycle (permission suspend + a queued second ask) issues zero ask-method calls outside the queue's fire step.
  </behavior>
  <read_first>internal/acp/handlers.go (handleSessionCancel :153-182 — st.cancelTurn() + closeSessionIfPossible; the s.mu-guarded sessionState lookup), internal/acpserve/acp_serve.go (serve composition + shutdown path; where the queue drain hook attaches), internal/session/ask.go (17-02's permission resume cancelled branch), internal/session/askqueue.go (Task 1 output), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (Pitfall 4 orphaned dialogs; Open Question 1; D-13/16-D-19 cascade contract), .planning/phases/16-acp-wire-foundation/16-03-PLAN.md (ResolveCancelled + cascade + shutdown-drain semantics — consumed, not modified)</read_first>
  <action>
RED: extend askqueue_test.go with the drain/monopoly families and handlers_test.go with the three teardown-path wirings (pipe-backed server fixtures; fake registry counting calls). Commit failing.
GREEN: implement DrainTurn(turnID) in askqueue.go — open entry resolved cancelled through the registry cascade, queued entries cancelled-normal without firing (appending their results through the same loud-append seam the resume path uses); wire the shared drain into handleSessionCancel (before/with cancelTurn), the session-close path, and the serve-shutdown composition in acp_serve.go. The alive-while-asking and leak proofs run at session/serve level under -race. Do NOT modify internal/session/ask.go in this plan — the cancelled resume branch already exists from 17-02.
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestAskQueueDrain|TestGateTurnDeath' -count=1 && go test -race ./internal/acp/ -run 'TestSessionCancelDrain' -count=1 && go test ./internal/acpserve/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The cascade test observes exactly one $/cancel_request for the open ask on turn death and a cancelled-normal appended result.
    - The queued-drain test asserts zero fires and cancelled-normal appends scoped to the dead turn only.
    - All three teardown paths have a wiring test each.
    - The alive-while-asking case (second prompt completes during an open fake dialog) passes under -race.
    - The shutdown test reports zero leaked goroutines (compare runtime.NumGoroutine before/after with settle, or the repo's established leak-check idiom).
    - The monopoly test asserts zero ask-method registry calls outside the queue fire step.
    - `mise ci` green.
  </acceptance_criteria>
  <done>D-13 and criterion 2 are proven: no orphaned dialogs, no zombie asks, no hangs — the session stays alive while a human decides, and every death path cleans up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teardown events → queue drain | cancel/close/shutdown must not race the fire callback or double-resolve an entry |
| queued entries → transcript | drained cancelled-normal appends are agent-visible state; wrong scoping would corrupt live turns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-09 | DoS | orphaned dialogs / leaked entries on turn death | high | mitigate | D-13 drain on all three teardown paths + 16-D-19 cascade + shutdown drain; -race leak tests gate it |
| T-17-10 | DoS | unbounded queue growth (runaway engine asks) | low | accept | Growth is bounded in practice by turn-scoped drain and one-dialog serialization; the pending count is user-visible via D-12 notes/counter, making a runaway observable rather than silent |
| T-17-SC | Tampering | package installs | high | accept | No installs (stdlib only); legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/session/ ./internal/acp/ ./internal/acpserve/ -count=1` green.
- `mise ci` green.
</verification>

<success_criteria>
The ask queue is the phase's complete serialization discipline: D-11's one-at-a-time with foreground priority, D-12's visible queue, D-13's turn-scoped drain — and criterion 2's liveness contract holds under -race from every teardown path.
</success_criteria>

<output>
Create `.planning/phases/17-permissions-elicitation/17-03-SUMMARY.md` when done
</output>
