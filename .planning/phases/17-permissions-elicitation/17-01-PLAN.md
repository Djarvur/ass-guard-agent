---
phase: 17-permissions-elicitation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/perm/rules.go
  - internal/perm/rules_test.go
  - internal/perm/store.go
  - internal/perm/store_test.go
autonomous: true
requirements: [ACP-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Rule evaluation is deny → ask → allow first-match with specificity never reordering: a deny anywhere in the file beats any allow, and a matching ask rule prompts even when a more specific allow also matches (D-02, CC parity)"
    - "The grammar supports bare Tool, Tool(specifier), the trailing wildcard suffix equivalent to a trailing space-star (recognized only at pattern end), compound-command split on && || ; | |& & and newlines with every subcommand matched independently, and mcp__server__tool namespaces (bare mcp__server matches the whole server; unanchored allow globs such as * or B* or mcp__* are skipped with a warning) (D-02)"
    - "permissions.yaml persists dialog-written entries as tool-x-project simple rules in BOTH directions — the allow direction writes an allow entry, the reject direction writes a deny entry (D-01, D-03); richer rules remain hand-edit-only in the file, never written by the dialog"
    - "permissions.yaml is written 0600 atomic (temp+rename): an absent file loads as the empty rule set (zero-config floor), and an interrupted or failed write leaves the prior file byte-intact"
    - "Persisting an already-present rule is an idempotent no-op — repeated always-clicks converge to one entry, and a save/load round-trip never duplicates lines (edge probe: idempotency)"
  artifacts:
    - path: "internal/perm/rules.go"
      provides: "ParseRule + RuleSet with first-match deny→ask→allow evaluation over (toolName, primaryArg) subjects; compound split; prefix-wildcard matcher (no regex engine); MCP namespace helpers MCPName/SplitMCPName"
      contains: "func ParseRule"
    - path: "internal/perm/store.go"
      provides: "Store: Open (zero-config floor), RuleSet snapshot (copy-on-read), AllowTool/ForbidTool dialog-entry writers (both directions, idempotent), mutex-serialized 0600 atomic save"
      contains: "func (s *Store) AllowTool"
    - path: "internal/perm/rules_test.go"
      provides: "CC-parity grammar table tests (ordering, wildcard, compound, mcp namespace, malformed-line skip)"
      contains: "TestRules"
    - path: "internal/perm/store_test.go"
      provides: "persistence round-trip, 0600/atomic/interruption, idempotency, malformed hand-edited line tolerance"
      contains: "TestStore"
  key_links:
    - from: "internal/perm/rules.go"
      to: "internal/session/gate.go (17-02)"
      via: "the gate calls the evaluator per tool call and receives VerdictDeny/VerdictAsk/VerdictAllow/Unmatched — Unmatched defers to the mode+class decision at the chokepoint (D-05/D-06)"
      pattern: "Unmatched"
    - from: "internal/perm/store.go"
      to: "internal/session (17-02 permission resume)"
      via: "an always-click persists the rule through AllowTool/ForbidTool BEFORE the gated call executes (persist-then-execute)"
      pattern: "ForbidTool"
  prohibitions:
    - requirement_id: ACP-01
      category: privacy
      statement: "Permission decisions (dialog choices, written rules, denied tool names and arguments) MUST never leave the local machine — no telemetry, no remote logging, no transmission of rule contents anywhere except the project's local permissions.yaml and the local transcript/stderr; nothing in this package may gain a network path"
      status: unresolved
---

<objective>
Build internal/perm: the CC-parity permission rule grammar (D-02) and the permissions.yaml persistence layer (D-01/D-03) as a pure, dependency-free package the Phase-17 chokepoint (17-02) consumes.

Purpose: the rule grammar is the phase's access-control core — evaluation order bugs are silent privilege escalation (RESEARCH Security Domain), so the grammar gets exhaustive table tests before any consumer exists. The store carries accumulating user trust state, so its write discipline (0600 atomic, idempotent, crash-safe) is pinned by its own test battery.
Output: internal/perm/{rules,store}.go + tests, green under -race.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-permissions-elicitation/17-CONTEXT.md
@.planning/phases/17-permissions-elicitation/17-RESEARCH.md
@.planning/phases/17-permissions-elicitation/17-PATTERNS.md
@internal/learning/store.go
@internal/session/planmode.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/perm` (NEW package) — `ParseRule(string) (Rule, error)`, `Rule{Tool, Pattern, Effect}`, `Effect` enum (EffectDeny/EffectAsk/EffectAllow), `RuleSet` with `Evaluate(toolName, primaryArg string) Verdict` (VerdictDeny/VerdictAsk/VerdictAllow/Unmatched), compound-command splitter, `MCPName(server, tool string) string` / `SplitMCPName(name string) (server, tool string, ok bool)`.
- `internal/perm/store.go` — `Open(path string) (*Store, error)`, `(*Store).Rules() RuleSet`, `(*Store).AllowTool(name string) error`, `(*Store).ForbidTool(name string) error`, constants `filePermOwner = 0o600`, `dirPerm = 0o750`.
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rule grammar — parse + CC-parity first-match evaluation</name>
  <files>internal/perm/rules.go, internal/perm/rules_test.go</files>
  <behavior>
    - Parse "Write" → bare rule matching tool Write with any input; Parse "Bash(git *)" matches Bash with primaryArg "git status"; the trailing ":*" suffix is equivalent to a trailing " *" wildcard and is recognized only at the end of a pattern (a mid-pattern colon-star is literal).
    - Evaluation order is deny → ask → allow, first match wins, specificity never reorders: rule set {deny "Bash(aws *)", allow "Bash(aws s3 ls)"} evaluates "aws s3 rm ..." and "aws s3 ls" both to VerdictDeny; rule set {ask "Write", allow "Write(*.go)"} evaluates any Write call to VerdictAsk.
    - Compound commands: primaryArg "git status && rm -rf /tmp/x" with allow "Bash(git *)" and deny "Bash(rm *)" evaluates to VerdictDeny — each subcommand (split on && || ; | |& & and newlines) is matched independently and the worst matching verdict wins.
    - MCP namespace: rule "mcp__github__get_*" matches tool mcp__github__get_issue; bare "mcp__puppeteer" matches every tool whose name begins "mcp__puppeteer__"; unanchored allow globs ("*", "B*", "mcp__*") are skipped as inert with one structured warning per skipped rule; the same globs in deny/ask lists DO match (CC asymmetry).
    - No rule matches → Unmatched (the caller's mode+class decision, D-05/D-06 — not this package's concern).
    - Malformed rule strings (empty line, unbalanced parentheses, unknown tool characters) are skipped with one structured warning per line; parsing never panics.
    - Matching is prefix-wildcard only (stdlib strings + explicit trailing-star logic); no regexp is compiled anywhere in the package (bounded by construction — RFD pattern-evaluation discipline applied to rule specifiers).
  </behavior>
  <read_first>.planning/phases/17-permissions-elicitation/17-RESEARCH.md (§CC Rule Model — every quoted semantic is the test oracle; §Code Examples CC-parity rule evaluation; Pattern 5 recommended file shape), .planning/phases/17-permissions-elicitation/17-PATTERNS.md (planmode.go stateless-check analog; yaml loading NOT needed here), internal/session/planmode.go (the stateless gate-check style to mirror)</read_first>
  <action>
RED: write rules_test.go as a table-driven battery covering the seven behavior cases; name cases after the CC-parity facts (order-beats-specificity, ask-beats-allow, deny-beats-allow, compound-worst-match, mcp-namespace, unanchored-allow-skip, malformed-skip). Commit failing.
GREEN: implement rules.go — Rule{Tool, Pattern string; Effect Effect} with ParseRule handling bare names, Tool(specifier), and the end-anchored wildcard suffix; RuleSet.Evaluate(toolName, primaryArg) running the three lists in the fixed order; a compound splitter returning subcommand strings; MCPName/SplitMCPName helpers. Doc comment on RuleSet states the evaluation contract verbatim from D-02 and names the caller-owned Unmatched decision (D-05/D-06). No imports beyond stdlib strings/fmt/strings-consts.
  </action>
  <verify>
    <automated>go test -race ./internal/perm/ -run TestRules -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/perm/ -run TestRules -count=1` exits 0 with every table case green.
    - The deny-beats-allow and ask-beats-allow cases assert the verdict even when the competing rule is strictly more specific (order pinned, D-02).
    - The unanchored-allow-skip case asserts both the inert rule and that a structured warning was recorded (observable via the package's warning sink — a settable func or returned diagnostics).
    - `grep -c 'regexp' internal/perm/rules.go` returns 0 (no regex engine in the matcher).
  </acceptance_criteria>
  <done>The CC-parity grammar is complete and table-pinned: parse, first-match order, wildcards, compound split, mcp__ namespace, and malformed-line tolerance — all green under -race before any consumer exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: permissions.yaml store — 0600 atomic, both directions, idempotent</name>
  <reversibility rating="costly">D-02's grammar/file format: permissions.yaml entries accumulate as user trust decisions; reshaping the grammar later means migrating trust state (CONTEXT carries this rating).</reversibility>
  <files>internal/perm/store.go, internal/perm/store_test.go</files>
  <behavior>
    - Open on an absent path creates parent dirs (0750) and an empty-valid file (0600) — the zero-config floor; Open on an existing file loads its deny/ask/allow lists verbatim.
    - File format (pinned discretion item, RESEARCH Pattern 5): three ordered lists of rule strings — deny:, ask:, allow: — parsed by ParseRule from Task 1.
    - AllowTool("Write") appends "Write" to the allow list; ForbidTool("Write") appends "Write" to the deny list — the dialog's only two write shapes, tool-x-project simple entries (D-01, D-03); the store exposes NO API that writes specifier rules.
    - Idempotency: AllowTool/ForbidTool on an already-present identical entry is a no-op — file bytes unchanged, no duplicate line after save/load round-trip.
    - Atomic discipline: save writes path+".tmp" (0600) then renames; a simulated rename failure or a leftover tmp file leaves the prior file byte-intact; concurrent AllowTool calls serialize under the store mutex with -race clean.
    - A hand-edited file containing one malformed line loads with that line skipped plus one structured warning per bad line (tolerant hand-edit surface); valid lines around it load normally.
    - Rules() returns a copy (copy-on-read) — callers cannot mutate store state through the snapshot.
  </behavior>
  <read_first>internal/learning/store.go (Open :46-65 MkdirAll+empty-envelope floor; save :220-241 the atomic temp+rename template; filePermOwner 0o600 :17; mutex-serialized mutations; copy-on-read), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (Pattern 5 — the pinned yaml shape; §Security Domain V14 config row), .planning/phases/17-permissions-elicitation/17-PATTERNS.md (store.go analog section, verbatim save excerpt), go.mod (yaml.v3 already present)</read_first>
  <action>
RED: store_test.go covering the seven behavior cases including a restart-survival case (Open → AllowTool → new Open on same path → rule present, mirroring criterion 1's "persists across subsequent calls" and restarts). Commit failing.
GREEN: implement store.go with gopkg.in/yaml.v3 — a fileEnvelope{Deny, Ask, Allow []string} marshaled with 2-space indent; loading tolerates absent optional keys; parse each line through ParseRule and skip-with-warning malformed lines. AllowTool/ForbidTool compare against the parsed Rule view to decide idempotent no-op before touching disk. Follow learning/store.go's save() verbatim for the atomic write and its Open() for the floor. Keep the warning sink consistent with Task 1's.
  </action>
  <verify>
    <automated>go test -race ./internal/perm/ -count=1 && go vet ./internal/perm/</automated>
  </verify>
  <acceptance_criteria>
    - Full package suite green under -race; store tests cover round-trip, restart survival, both write directions, idempotency (byte-identical file on no-op), atomicity/interruption, malformed-line tolerance.
    - `grep -c '0o600' internal/perm/store.go` is >= 1 and the permission is asserted by a test reading os.Stat mode.
    - The store's exported write surface is exactly two simple-entry methods — `grep -n 'func (s \*Store)' internal/perm/store.go` shows no specifier-writing API (D-01's dialog-never-writes-richer-rules boundary).
    - `mise ci` green.
  </acceptance_criteria>
  <done>The persistence layer lands: tool-x-project trust state in both directions, 0600 atomic and crash-safe, idempotent, hand-edit tolerant — the file 17-02's dialog clicks write into.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-edited permissions.yaml → evaluator | operator-authored rule strings are untrusted input to the parser (malformed lines must degrade, never panic) |
| dialog click → store | the only programmatic writer; must stay scoped to simple entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege | RuleSet evaluation order bug | high | mitigate | First-match deny→ask→allow pinned by exhaustive table tests; specificity-never-reorders and deny-beats-allow cases are explicit rows |
| T-17-02 | Tampering | permissions.yaml | high | mitigate | 0600 perms + atomic temp+rename; dialog writes only tool-x-project simple entries; richer grammar hand-edit-only (D-01) keeps the file operator-owned |
| T-17-03 | DoS | rule-specifier pattern matching | medium | mitigate | Prefix-wildcard matcher with no regexp compilation — bounded by construction; acceptance criterion greps the engine away |
| T-17-SC | Tampering | package installs | high | accept | No installs (stdlib + existing yaml.v3); package-legitimacy gate not applicable |
</threat_model>

<prohibition_probe_breadcrumbs>
Dropped as canon, not minted: raw-PII-at-rest / data-retention — covered by the repo's 0600 artifact-family discipline; ReDoS-class pattern abuse — covered by the no-regex-engine construction above.
</prohibition_probe_breadcrumbs>

<verification>
- `go test -race ./internal/perm/ -count=1` green.
- `mise ci` green (package joins the standing gate).
</verification>

<success_criteria>
The rule grammar and trust-state file exist, table-pinned against the verified CC semantics, with the dialog's write surface deliberately narrower than the file's read surface — ready for 17-02's chokepoint to consume.
</success_criteria>

<output>
Create `.planning/phases/17-permissions-elicitation/17-01-SUMMARY.md` when done
</output>
