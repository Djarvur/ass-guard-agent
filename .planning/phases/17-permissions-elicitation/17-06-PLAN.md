---
phase: 17-permissions-elicitation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/acp/types.go
  - internal/acpserve/ask_surface.go
  - internal/acpserve/ask_surface_test.go
  - internal/acpserve/permissions_e2e_test.go
autonomous: true
gap_closure: true
gap_ids: [G-17-1]
requirements: [ACP-01]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A canonical ACP v1 answer — result.outcome as a NESTED object with inner discriminator selected plus optionId — decodes and maps to AskOutcome.Selected, so the gated call executes and allow_always persists to permissions.yaml (criterion 1, D-01/D-03)"
    - "A canonical cancelled nested answer maps to AskOutcome.Cancelled (cancelled-NORMAL, never an error)"
    - "A nonconformant flat outcome string (the old shape Zed never sent) fails safe: errPermissionOutcomeBad → decline, never an allow"
    - "An unknown inner discriminator fails safe via errPermissionOutcomeUnknown → decline"
    - "The E2E simulator answers the canonical nested shape, so TestPermissionsE2E models the real client (Zed 1.18.0) instead of codifying our bug"
    - "The full existing battery stays green under -race (TestPermissionsE2E plus the internal/acpserve, internal/acp, internal/session sweeps and mise ci)"
  artifacts:
    - "internal/acp/types.go — PermissionOutcomeFrame reshaped to the canonical nested outcome object (new inner struct, v1 field spellings verbatim)"
    - "internal/acpserve/ask_surface.go — Fire's decode + outcome switch reads the nested discriminator and optionId; all fail-safe sentinels preserved"
    - "internal/acpserve/ask_surface_test.go — TestPermissionAskDispatch canned payloads corrected to canonical nested; flat-rejection and unknown-discriminator subtests added"
    - "internal/acpserve/permissions_e2e_test.go — permAnswerSelected answers canonical nested; permAnswerAccept left as-is (verified canonical flat)"
  key_links:
    - "Zed 1.18.0 wire answer → PermissionOutcomeFrame decode → Fire outcome switch → session ask.go persist/execute legs (permissions.yaml non-empty after always clicks — the exact link that was dead in UAT Test 1)"
---

<objective>
Close gap G-17-1 (blocker): session/request_permission responses are decoded from the CANONICAL ACP v1 nested outcome object, not the flat string PermissionOutcomeFrame currently declares.

Live Zed 1.18.0 answers request_permission with result.outcome as a nested object — an inner discriminator ("selected"|"cancelled") plus optionId on selected — but internal/acp/types.go:335-349 declares outcome as a flat string, so json.Unmarshal fails at internal/acpserve/ask_surface.go:155-160 ("cannot unmarshal object into Go struct field PermissionOutcomeFrame.outcome"), every dialog answer takes the fail-safe decline, and nothing persists (permissions.yaml stays empty) — the whole gated permission feature is dead against the real client (D-01/D-03 persistence unreachable, criterion 1 failed). The E2E simulator answers the same wrong flat shape (permissions_e2e_test.go permAnswerSelected) and the unit pins feed it too (ask_surface_test.go TestPermissionAskDispatch), so the battery is green against a nonconformant client.

Canonical shape, verified against https://agentclientprotocol.com/protocol/v1/tool-calls and the live Zed transcript (17-UAT.md G-17-1): selected = {"outcome":{"outcome":"selected","optionId":"…"}}; cancelled = {"outcome":{"outcome":"cancelled"}}. The elicitation/create response was verified on the same spec family (schema CreateElicitationResponse): action IS a flat top-level discriminator with optional content — permAnswerAccept and ElicitationOutcomeFrame are CORRECT and must not be touched.

Purpose: restore the gate pipeline's live-client contract so UAT Test 1 passes and Test 2 (native elicitation form, blocked behind this gap) becomes re-testable at verify-work.
Output: nested decode in production, canonical-answer simulator, RED-proven pins, full -race battery green.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-permissions-elicitation/17-UAT.md

# Gap source (read first): 17-UAT.md §Gaps G-17-1 — root cause fully diagnosed; do NOT re-diagnose.
# Code anchors (all four verified current):
@internal/acp/types.go
@internal/acpserve/ask_surface.go
@internal/acpserve/ask_surface_test.go
@internal/acpserve/permissions_e2e_test.go

# Downstream contract the switch feeds (unchanged by this plan — read to confirm, not to modify):
@internal/session/ask.go

Wire truth (Pitfall-7 discipline — wire shapes come from the canonical schema, never plan prose; re-verify at the spec page during execution):
- https://agentclientprotocol.com/protocol/v1/tool-calls — request_permission response examples (nested outcome)
- https://agentclientprotocol.com/protocol/v1/schema — CreateElicitationResponse (flat action + optional content) — confirms the elicitation side needs NO change
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the canonical nested outcome in both test clients</name>
  <files>internal/acpserve/ask_surface_test.go, internal/acpserve/permissions_e2e_test.go</files>
  <behavior>
    - Test 1 (selected, nested): feeding Result {"outcome":{"outcome":"selected","optionId":"allow_always"}} through PermissionAsk.Fire yields AskOutcome{Selected:"allow_always"} with Cancelled=false and Err=nil — FAILS before the fix (unmarshal of the nested object into the flat string field errors, Fire returns Err).
    - Test 2 (cancelled, nested): feeding Result {"outcome":{"outcome":"cancelled"}} yields AskOutcome{Cancelled:true} with Err=nil — FAILS before the fix.
    - Test 3 (flat nonconformance, NEW subtest): feeding the old flat one-level shape — outcome as a bare string with optionId beside it — yields Err wrapping errPermissionOutcomeBad, with Selected empty and Cancelled false (fail-safe decline, never an allow).
    - Test 4 (unknown inner discriminator, NEW subtest): feeding {"outcome":{"outcome":"banana"}} yields Err wrapping errPermissionOutcomeUnknown.
    - Test 5 (simulator fidelity): permAnswerSelected sends the canonical nested shape on the wire, so TestPermissionsE2E exercises the same decode Zed 1.18.0 exercises.
  </behavior>
  <action>
    Test-side corrections ONLY in this task — production code changes land in Task 2 (RED window).
    1. internal/acpserve/ask_surface_test.go — TestPermissionAskDispatch: replace the two canned Result payloads (currently at ~lines 207 and 219) with the canonical nested shapes from the behavior block (selected carries optionId allow_always INSIDE the nested outcome object; cancelled is the nested object with the bare inner discriminator). Add two sibling subtests in the same style: flat-nonconformance (the pre-fix payload shape: a one-level outcome string with optionId as its sibling key — the exact shape at line 207 before your edit) asserting Err is set, Selected empty, Cancelled false, and errors.Is against the errPermissionOutcomeBad sentinel family as the existing fail-safe subtests do; and unknown-inner-discriminator asserting the errPermissionOutcomeUnknown family. Keep the existing -32800 / -32601 / timeout subtests untouched.
    2. internal/acpserve/permissions_e2e_test.go — permAnswerSelected (~line 377): change the sendf payload so result.outcome is the canonical nested object (inner outcome discriminator "selected" + optionId), and update its doc comment to cite the canonical example (agentclientprotocol.com/protocol/v1/tool-calls) and Zed 1.18.0 as the observed live client. The rawID and simJSONStr(optionID) interpolation style stays.
    3. DO NOT touch permAnswerAccept — verified canonical: schema CreateElicitationResponse is a flat action discriminator with optional content; Zed answers it that way. Add one comment line on permAnswerAccept noting the shape was verified canonical-flat against the schema page during 17-06 so nobody "fixes" it again.
    4. Run the dispatch test and capture the failure output verbatim for the SUMMARY (RED proof, the repo's 17-REVIEW-FIX pin discipline).
    Commit: test(17-06): pin canonical nested request_permission outcome; simulator answers canonical shape (RED)
  </action>
  <verify>
    <automated>go test ./internal/acpserve/ -run 'TestPermissionAskDispatch' -count=1 ; echo "exit=$?"</automated>
    RED GATE: this run MUST FAIL on at least the selected and cancelled subtests (malformed-permission-outcome Err path). A pass here means the pin is not actually exercising the decode — fix the pin before committing. Record the failing output in the SUMMARY.
  </verify>
  <done>Pins and simulator committed; dispatch test demonstrably RED against the flat parse; nested literal count gate: grep -c '"outcome":{"outcome"' internal/acpserve/ask_surface_test.go returns at least 3 and the same grep on internal/acpserve/permissions_e2e_test.go returns at least 1; permAnswerAccept untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — decode the canonical nested outcome in production</name>
  <files>internal/acp/types.go, internal/acpserve/ask_surface.go</files>
  <behavior>
    - Same behaviors as Task 1's tests, now green: nested selected → Selected, nested cancelled → Cancelled, flat/unknown/empty-inner → fail-safe Err.
    - Every pre-existing pin (TestPermissionsE2E full battery, ask drain, registry, session sweeps) stays green under -race.
  </behavior>
  <action>
    1. internal/acp/types.go — reshape the response type (currently lines 335-349): introduce an inner struct (name it PermissionOutcome) carrying the inner discriminator string field outcome plus the optionId string field with the tagliatelle nolint comment per file convention; PermissionOutcomeFrame keeps exactly ONE field — outcome — typed as that inner struct. v1 field spellings verbatim (outcome, optionId; Pitfall-7). Keep the PermissionOutcomeSelected/PermissionOutcomeCancelled discriminator constants unchanged. Rewrite the struct doc comment: the response is result.outcome as a NESTED union object (selected carries optionId, cancelled carries nothing), cite the schema def and the canonical example page, and note Zed 1.18.0 as the live-client confirmation (17-UAT G-17-1).
    2. internal/acpserve/ask_surface.go — Fire's outcome mapping (currently lines 155-173): keep the json.Unmarshal(msg.Result, &of) call and the errPermissionOutcomeBad wrap on failure; change the switch subject to the nested inner discriminator and map Selected from the inner optionId. Preserve verbatim: the errPermissionOutcomeUnsupported/-Bad/-Unknown sentinel family, the ErrRequestCancelled → Cancelled branch, the -32601 sticky degrade branch, the D-14 terminal-fallback Err branch, and every log line's fail-safe wording. Note for the executor: a selected outcome with an empty optionId needs NO new guard — it falls through to the session gate's default unknown-option fail-safe branch (internal/session/ask.go ~960), which is the correct decline.
    3. GREEN run: dispatch test, then the E2E battery under -race, then the three-package sweep, then mise ci (vet + lint + build + test; strict golangci config — mind the nolint conventions).
    Commit: fix(17-06): decode canonical nested request_permission outcome (G-17-1)
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestPermissionAskDispatch|TestPermissionsE2E' -count=1 && go test -race ./internal/acp/ ./internal/acpserve/ ./internal/session/ -count=1 && mise ci</automated>
  </verify>
  <done>All Task 1 pins green; TestPermissionsE2E green under -race (the simulator now models the real client); internal/acp + internal/acpserve + internal/session sweeps green under -race; mise ci green; fail-safe sentinels errPermissionOutcomeBad and errPermissionOutcomeUnknown still referenced by live code paths and exercised by the new subtests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→agent (request_permission response) | The editor's dialog answer crosses into PermissionAsk.Fire — UNTRUSTED wire input parsed by PermissionOutcomeFrame |
| client→agent (elicitation/create response) | Untrusted accept content — NOT modified by this plan (D-10 validator already guards; shape verified canonical flat) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-06-01 | Tampering | PermissionAsk.Fire outcome decode (internal/acpserve/ask_surface.go) | high | mitigate | Strict canonical nested decode: nonconformant shape (flat string, wrong nesting) → errPermissionOutcomeBad; unknown inner discriminator → errPermissionOutcomeUnknown; empty optionId on selected → session default unknown-option branch. Every path lands on fail-safe DECLINE, never an allow — pinned by Task 1's flat-rejection and unknown-discriminator subtests. |
| T-17-06-02 | Tampering | Elicitation accept content (ElicitationOutcomeFrame) | medium | accept | No code change on this path in this plan: response shape verified canonical flat against the ACP v1 schema (CreateElicitationResponse); the D-10 re-validation of untrusted content already ships (T-17-11/12 pins, 17-04). |
</threat_model>

<verification>
1. RED proof recorded in the SUMMARY (Task 1 failing output against the flat parse).
2. go test -race ./internal/acpserve/ -run 'TestPermissionAskDispatch|TestPermissionsE2E' -count=1 — green.
3. go test -race ./internal/acp/ ./internal/acpserve/ ./internal/session/ -count=1 — green (all existing pins hold).
4. mise ci — green (vet + strict lint + build + full -race test).
5. Positive shape gates: grep -c '"outcome":{"outcome"' internal/acpserve/ask_surface_test.go ≥ 3; same grep on internal/acpserve/permissions_e2e_test.go ≥ 1; grep -c 'PermissionOutcome\b' internal/acp/types.go ≥ 2 (frame + inner struct).
6. Manual confirmation of the live-client contract happens at the verify-work re-run (UAT Test 1 legs against live Zed; Test 2 un-blocks) — out of this plan's automated scope, owned by the orchestrator.
</verification>

<success_criteria>
- The canonical nested outcome object from Zed 1.18.0 decodes: selected executes the gated call and allow_always persists to permissions.yaml (D-01); reject_always writes the deny rule (D-03); cancelled resolves cancelled-NORMAL.
- A nonconformant or forged outcome can never produce an allow — only decline or cancelled.
- The E2E battery now models the real client's wire shape, so a decode regression of this class can never again hide behind a green battery.
- Gap G-17-1 is closable: UAT Test 1 re-runnable and Test 2 un-blocked for the verify-work pass.
</success_criteria>

<output>
Create `.planning/phases/17-permissions-elicitation/17-06-SUMMARY.md` when done
</output>
