---
phase: 17-permissions-elicitation
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - internal/session/gate.go
  - internal/session/gate_test.go
  - internal/session/session.go
  - internal/session/ask.go
  - internal/session/askqueue.go
  - internal/acp/types.go
  - internal/acpserve/ask_surface.go
  - internal/acpserve/ask_surface_test.go
  - internal/acpserve/config_surface.go
  - internal/acpserve/config_test.go
  - internal/runtime/runtime.go
autonomous: true
requirements: [ACP-01]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With permissions.mode gated, a mutating tool call pops the native permission dialog with exactly the four options allow_once/allow_always/reject_once/reject_always; allow_always persists the rule BEFORE the gated call executes, the call then executes, the turn resumes, and a subsequent matching call hits the allow rule with no second dialog (criterion 1, D-01)"
    - "The ONE chokepoint gates both modes through one path: ungated (default) evaluates the rules and returns allow without ever opening a dialog while deny rules still deny — zero new dialogs appear under default config versus v1.1 behavior (D-05, criterion 4)"
    - "Automation/cron turns never see a dialog: ask-class calls decline with a client-visible note and a transcript line while deny and allow rules stay enforced — never a dialog nobody answers, never a silent allow, in BOTH modes (D-07)"
    - "Both the batch-eligible branch and the subagent dispatch branch route through the same gate seam — no second permission path exists for subagents or engine turns (D-05; the Task tool is gated per its catalog class under D-06)"
    - "permissions.mode flips live: a set_config_option to gated on the RUNNING session changes the very next tool call's gate behavior without session recreation (criterion 4, Pitfall 8)"
    - "A client that answers session/request_permission with -32601 degrades fail-safe: the gated call declines with a note (never a silent allow), one structured log, sticky for the session (16-D-14/D-18 discipline)"
  artifacts:
    - path: "internal/session/gate.go"
      provides: "gateCall — the ONE per-call chokepoint beside planModeBlocks: hook-verdict head (implicit allow until Phase 21, D-04), rule evaluation via internal/perm, mode+class+human-present decisions returning gateExecute/gateDeny/gateSuspend/gateDeclineAutomation"
      contains: "func (s *Session) gateCall"
    - path: "internal/session/askqueue.go"
      provides: "AskQueue minimal core: Enqueue(entry{turnID, class, seq, fire}), one-outstanding fire loop, FIFO completion chain (priority classes, notes, drain expand in 17-03)"
      contains: "type AskQueue"
    - path: "internal/session/ask.go"
      provides: "PendingAskKindPermission + the permission resume variant mirroring resumeAskClaimed: persist-if-always → execute gated call → appendToolResultLoud → runTurn re-entry → settle close"
      contains: "PendingAskKindPermission"
    - path: "internal/acp/types.go"
      provides: "RequestPermissionFrame{SessionID, ToolCall, Options} + PermissionOption{OptionID, Name, Kind} — v1 verbatim camelCase with //nolint:tagliatelle markers"
      contains: "RequestPermissionFrame"
    - path: "internal/acpserve/ask_surface.go"
      provides: "the permission surface: frame build (ToolCallUpdate shape), registry Call under HUMAN-ASK, outcome mapping selected{optionId}→ask resolution, cancelled→cancelled-normal, -32601→fail-safe decline"
      contains: "PermissionAsk"
    - path: "internal/acpserve/config_surface.go"
      provides: "the real permissions.mode handler behind 16-05's advertised pending slot: typed validation, persist-then-apply via WriteLayerOption, live mode flip through the accessor the gate reads"
      contains: "permissions.mode"
  key_links:
    - from: "internal/session/session.go runTurn tool loop"
      to: "internal/session/gate.go"
      via: "gateCall invoked per call beside s.planModeBlocks(tc.Name) in BOTH the batch-eligible path and the isSubagentTool branch — the only rule-evaluation call sites in the session package"
      pattern: "gateCall"
    - from: "internal/session (suspend)"
      to: "internal/session/askqueue.go"
      via: "gateSuspend ends the turn with the ask marker (stop vocabulary) and enqueues the permission entry; the queue's fire callback is injected from acpserve via runtime wiring — session never imports internal/acp"
      pattern: "Enqueue"
    - from: "internal/acpserve/ask_surface.go"
      to: "internal/acp Registry (16-03)"
      via: "permission asks are id'd Registry.Call(ctx, session/request_permission, frame, HUMAN-ASK) — UUID ids, D-14 ladder, 16-D-19 cascade consumed, never rebuilt"
      pattern: "HUMAN-ASK"
    - from: "internal/acpserve/config_surface.go"
      to: "internal/session/gate.go"
      via: "the set_config_option apply hook flips an atomic mode accessor; gateCall reads it live per call (no boot-time snapshot)"
      pattern: "mode"
  prohibitions:
    - requirement_id: ACP-01
      category: safety
      statement: "An always-allow click MUST never be treated as granted before it is durably recorded — the rule write happens BEFORE the gated call executes, and a write failure downgrades the click to once-only semantics with a loud structured log rather than silently widening trust"
      status: unresolved
    - requirement_id: ACP-01
      category: values
      statement: "Permission dialog option names MUST stay neutral and symmetric (the four CC-parity labels) — never asymmetric-friction, urgency, or consequence-downplaying framing that steers the user toward allow"
      status: unresolved
---

<objective>
Land THE gate chokepoint and the permission-ask tracer: one per-call pipeline (hook verdict → permission ask → execute) in runTurn's tool loop for both modes and both call branches, permission suspension riding the AskBroker discipline through a new minimal ask queue, the request_permission wire surface on 16-03's registry, and the real permissions.mode handler behind 16-05's advertised slot.

Purpose: this is the phase spine — criterion 1 (native dialog + persistence), criterion 4 (ungated default, live mode flip), D-04/D-05 (ONE pipeline), D-06 (grammar-derived dialog set), D-07 (automation fail-safe). Everything else in the phase expands from this slice.
Output: internal/session/{gate,askqueue}.go + ask.go widening, internal/acp wire types, internal/acpserve ask_surface + mode handler, runtime composition.
Scope note (deliberate 11-file footprint): this plan IS the phase tracer — one file per layer the spine touches (session gate/ask/queue + wiring, acp wire types, acpserve ask/config surfaces, runtime composition) plus their test files. Splitting the wire types or surface skeleton into a Wave-1 horizontal plan would break the vertical slice (a types-only plan ships nothing a user can do) and is deliberately not taken; the footprint is accepted as the cost of proving the architecture end-to-end in the earliest commit.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-permissions-elicitation/17-CONTEXT.md
@.planning/phases/17-permissions-elicitation/17-RESEARCH.md
@.planning/phases/17-permissions-elicitation/17-PATTERNS.md
@.planning/phases/16-acp-wire-foundation/16-CONTEXT.md
@.planning/phases/16-acp-wire-foundation/16-03-PLAN.md
@.planning/phases/16-acp-wire-foundation/16-05-PLAN.md
@internal/session/session.go
@internal/session/ask.go
@internal/acp/types.go
@internal/acpserve/acp_serve.go
@internal/runtime/runtime.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/session/gate.go` — `gateAction` enum (gateExecute/gateDeny/gateSuspend/gateDeclineAutomation), `gateVerdict`, `(*Session).gateCall(ctx, turnID, callID, tool string, input json.RawMessage) gateVerdict`, injected gate deps (rule-set provider, mode accessor, human-present signal, ask enqueue).
- `internal/session/ask.go` — `PendingAskKindPermission` const; the permission resume variant (executor names it, e.g. `resumePermissionAsk`) beside `resumeAskClaimed`.
- `internal/session/askqueue.go` — `AskQueue`, `askEntry{turnID, class, seq, fire}`, `Enqueue`, completion chain; `askClass` (foreground-turn vs subagent/engine, D-11).
- `internal/acp/types.go` — `RequestPermissionFrame`, `PermissionOption`, `PermissionOptionKind` constants (allow_once/allow_always/reject_once/reject_always).
- `internal/acpserve/ask_surface.go` — `BuildPermissionAsk(...)` frame builder + the registry-backed dispatcher + outcome mapping.
- `internal/acpserve/config_surface.go` — the real `permissions.mode` handler registration (select ungated|gated, project + `_global/` twin).
- `internal/session/session.go` — gate invocation in both runTurn branches; `internal/runtime/runtime.go` — gate/queue/surface composition (store construction, fire-callback injection, mode accessor default ungated).
</artifacts_this_phase_produce>

<assumption_delta_decision>
Detector fired (`chosen` signal; underlying real transition: the ask model goes singular → plural). The noun now primary: **the ask queue** (one fired head + N queued entries with priority classes) over the AskBroker's single `pending *PendingAsk` slot. Decision: **promote** — the queue becomes the single firing path for every ask surface; the broker's single-slot discipline is demoted to the queue's one-outstanding fired-head invariant. Rationale: D-11 ("never stacked modals") and D-05's ONE-pipeline rule both forbid an alongside second firing path; RESEARCH Pitfall 3 names any ask-method registry Call outside the queue's fire step as the anti-pattern. Companion invariant (17-03): a counting-fake registry test asserts zero ask firings outside the queue.
</assumption_delta_decision>

<flagged_assumptions>
- **ACP-01 edge-probe `unclassified` row (unresolved by protocol — surfaced, not silently dropped):** the deterministic edge probe could not classify an edge dimension for ACP-01 (empty/encoding/idempotency/concurrency probes fired only on ACP-02). Manual review during verification must confirm no unhandled edge class remains on the permission path — in particular permissions.yaml parse failures (handled: skip-line-with-warning, 17-01) and a zero-options/absent-fire-callback configuration (handled: the gate treats an unwirable surface as decline, never silent allow, per the fail-safe family).
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — gated permission ask end-to-end through the ONE chokepoint</name>
  <precondition>internal/acp/request_registry.go exists with Registry.Call/Deliver/ResolveCancelled and the HUMAN-ASK class, and internal/acpserve/config_surface.go exists advertising the permissions.mode pending slot (Phase 16 executed; 16-03/16-05 contracts hold — RESEARCH A4)</precondition>
  <reversibility rating="costly">The chokepoint placement (D-04/D-05): Phase 21's hooks join its head and later phases route every tool path through it — moving it after this lands re-touches every call site.</reversibility>
  <files>internal/session/gate.go, internal/session/session.go, internal/session/ask.go, internal/session/askqueue.go, internal/acp/types.go, internal/acpserve/ask_surface.go, internal/runtime/runtime.go, internal/session/gate_test.go</files>
  <behavior>
    - End-to-end (TestGatePermissionSuspend, fake surface answering allow_always): gated session, mutating catalog tool → the turn suspends with the ask stop marker (no executor call yet), a PendingAsk of the new permission kind carries {turnID, callID, tool name, raw input}, the queue fires the injected surface exactly once, and the frame observed is RequestPermissionFrame{sessionId, toolCall in the ToolCallUpdate shape, four options with kinds allow_once/allow_always/reject_once/reject_always and neutral CC-parity names}.
    - allow_always ordering: the rule write to the perm store is observed BEFORE the gated call executes (fake store records order); execution goes through the deadline-wrapped batch path (toolExecOrStub + boundedToolResult), its result is appended loud, runTurn resumes the SAME turn, and a second identical call executes with zero further surface firings.
    - Ungated default: the same mutating call with no rules executes immediately with zero surface invocations; with a deny rule present it returns a denial result (isErr=true) and never executes — rule evaluation runs in BOTH modes (D-05, Pitfall 5).
    - cancelled outcome → a cancelled-NORMAL tool result is appended (NOT an error result) and the turn resumes (criterion 2's letter).
    - The gate runs per-call in the runTurn loop beside planModeBlocks AND in the isSubagentTool branch — both branches observed gated (subagent Task included per its catalog class, D-05/D-06).
    - sessionTurnMu is NOT held while the ask is outstanding: a second prompt submitted while the (fake) dialog is open starts and completes.
  </behavior>
  <read_first>internal/session/session.go (runTurn tool loop :407-577; planModeBlocks site :464-473 verbatim in PATTERNS; isSubagentTool branch :421-462; batch dispatch :480; ask-suspension exit shape :507-553; appendToolResultLoud + boundedToolResult), internal/session/ask.go (PendingAsk :68-83, kinds :87, AskBroker :97-110, resumeAskClaimed :368-416 incl. the resume tail :403-415, settle discipline :176-181, ResolveAsk :350, askResumeCtx :315-322), .planning/phases/17-permissions-elicitation/17-PATTERNS.md (all verbatim excerpts: gate analog, isAloneInSlot :174-185, resume tail, runtime wiring :984-989, teardown sites), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (Pattern 1 chokepoint, Pattern 2 suspension+resume variant, architecture diagram, Pitfalls 1/2/5/8), .planning/phases/16-acp-wire-foundation/16-03-PLAN.md (Registry contract — Call signature, HUMAN-ASK, -32800), internal/acp/types.go (wire-struct conventions), internal/perm/rules.go (17-01 output — Verdict enum, Unmatched), internal/runtime/runtime.go (askBroker wiring :984-990, RegisterCore :972, mcpHost.Register :950)</read_first>
  <action>
RED: write gate_test.go with the end-to-end battery above (fake surface recording firings + injected answers; fake perm store recording write-vs-execute order; stub executor recording calls). Commit failing.
GREEN, in dependency order: (1) internal/acp/types.go — RequestPermissionFrame/PermissionOption with v1-verbatim camelCase json tags and //nolint:tagliatelle markers per the handlers.go convention; kind constants exactly allow_once/allow_always/reject_once/reject_always. (2) internal/session/gate.go — gateVerdict{action, payload}; gateCall runs the hook-verdict head FIRST (implicit-allow no-op today, one documented seam comment naming Phase 21 and D-04), then rule evaluation through the injected rule-set provider (17-01 RuleSet), then the mode/class/human decision: deny rule or hook deny → gateDeny with the structured-error result form; allow rule → gateExecute; unmatched + read-only/concurrent catalog class → gateExecute; unmatched + ask class (catalog IsMutating() || !IsConcurrencySafe(), mirroring isAloneInSlot against the toolcat API — never importing toolexec's private helper) + ungated → gateExecute; + gated + human turn → gateSuspend; + gated + automation turn → gateDeclineAutomation (wired in Task 2). (3) internal/session/ask.go — PendingAskKindPermission carrying tool+input+ids; the permission resume variant mirroring resumeAskClaimed's shape: outcome selected allow_always → persist via store (failure downgrades to once-semantics + loud structured log) THEN execute the gated call through the batch/deadline path, append boundedToolResult loud, re-enter runTurn on the same turn, close settle; allow_once → execute without persist; selected reject_* → denial result (isErr=true); cancelled → cancelled-normal result; then the same resume tail. (4) internal/session/askqueue.go — minimal AskQueue: mutex-guarded entries, one fired head, Enqueue appends and fires when idle, completion fires the next head FIFO; entries carry turnID + class (foreground-turn vs subagent/engine per D-11) + seq. (5) internal/session/session.go — invoke gateCall in the runTurn loop immediately beside the planModeBlocks check AND inside the subagent branch before DispatchSubagent; gateDeny/decline append their result loud + continue; gateSuspend records the pending ask and returns the ask stop exactly like the existing ask path. (6) internal/acpserve/ask_surface.go — the permission surface: build the ToolCallUpdate-shaped toolCall (toolCallId/title/kind from the call), the four options, dispatch via Registry.Call under HUMAN-ASK, map the response: selected{optionId} → the corresponding outcome; cancelled or -32800 → cancelled; -32601 → fail-safe decline handling (Task 2 completes it). (7) internal/runtime/runtime.go — compose: construct the perm store on the project's .ass-guard path, inject rule-set provider + ungated-default mode accessor + enqueue into the Session gate deps, inject the ask_surface fire callback into the queue (session stays free of internal/acp imports — the onSurface-callback precedent).
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestGatePermissionSuspend|TestGateChokepoint' -count=1 && go test ./internal/session/ ./internal/acpserve/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The end-to-end test observes the full chain — suspend, single fire, verbatim four-option frame, persist-before-execute order, loud result append, same-turn resume, no second dialog on the follow-up call.
    - The ungated cases assert zero surface invocations (no rules) and denial-without-dialog (deny rule) — D-05 in one test family.
    - The cancelled case asserts the appended result is the cancelled-normal form, not an error result.
    - Both runTurn branches are covered by at least one gating test each (batch-eligible and subagent dispatch).
    - The second-prompt-while-dialog-open case passes under -race (no turn-mutex hold).
    - `go vet ./internal/session/ ./internal/acp/ ./internal/acpserve/` clean; `mise ci` green.
  </acceptance_criteria>
  <done>The tracer is committed green: criterion 1's spine works end-to-end in one path — chokepoint, suspension, native-frame ask, persist-then-execute, resume — with the ungated default provably dialog-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Outcome matrix, automation decline, MCP namespace, degraded clients</name>
  <files>internal/session/gate.go, internal/session/session.go, internal/session/gate_test.go, internal/runtime/runtime.go, internal/acpserve/ask_surface.go, internal/acpserve/ask_surface_test.go</files>
  <behavior>
    - reject_once → denial result (isErr=true), no rule written, next call asks again; reject_always → the deny rule is persisted via ForbidTool BEFORE the denial result, and the next matching call denies with no dialog (D-03).
    - Automation turns (turn-origin automation, set by the automation entry path): ask-class call → gateDeclineAutomation — the call does not execute, a decline note lands as a transcript line (loud structured family) plus a structured log, in BOTH modes; deny and allow rules are still enforced on automation turns (D-07, Pitfalls 5/9 — the trail is visible in the next foreground session).
    - Registry error -32601 on the permission ask → the ask resolves to the decline path with a client-visible note, one structured log, and the degradation is sticky for the session (16-D-18) — never a silent allow, never a retry storm.
    - MCP namespace mapping: with a fake registered MCP tool known to the MCP-host seam, the gate's rule subject is the mcp__server__tool name (via 17-01's MCPName against host knowledge) — a rule written in that namespace matches; a table test pins the mapping both directions (Pitfall 7).
    - The human-present signal is per-turn: client Run turns are human; automation firings are not — asserted by the decline test firing only for automation-origin turns.
  </behavior>
  <read_first>internal/session/gate.go (Task 1 output — the verdict enum and decision tree), internal/session/session.go (structured-error result convention :544-551), internal/runtime/runtime.go (runAutomationTurn :123 — the origin marking site; advisory-note timing hazard :528-548 — decline notes must ride a subscriber-backed path, not a bare post-turn publish), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (Pitfalls 7/8/9; Open Question 2 resolution — Task gated with its own specifier; §Security Domain automation silent-allow row), internal/perm/rules.go (MCPName/SplitMCPName)</read_first>
  <action>
RED: extend gate_test.go + ask_surface_test.go with the five behavior families (use the Task 1 fakes; add a fake MCP-host knowledge source returning one registered server/tool pair). Commit failing.
GREEN: implement the reject/decline/namespace/origin logic inside the existing seams — no new dispatch paths: the resume variant gains the reject_* + persist ordering; gateCall gains the automation branch consuming the per-turn origin marker (set where runAutomationTurn enters the session — one line at the runtime entry, session-owned field or turn param, executor's choice within the existing turn-entry shape); the surface maps -32601 to decline-with-note and records stickiness in the session's degraded-state (mirroring 16-03's capability cache discipline, session-scoped); the gate computes rule subjects through the injected namespace resolver built on MCPName. Decline notes ride the same emission discipline as D-12's queue notes (Task 17-03 formalizes the counter; here the transcript line + structured log suffice).
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestGateAutomationDecline|TestGateOutcomeMatrix|TestGateMCPNamespace' -count=1 && go test -race ./internal/acpserve/ -run 'TestPermissionAsk' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The outcome matrix test covers all four option kinds plus cancelled with their persist/no-persist and result-form expectations (D-01/D-03 both directions).
    - The automation-decline test asserts: no executor call, a transcript line exists, deny/allow rules still applied, and the human-turn control case DOES suspend.
    - The -32601 test asserts decline-not-allow plus stickiness (a second gated call declines without a new registry Call).
    - The namespace test proves a rule in mcp__server__tool form matches the fake-registered tool's gate subject both directions.
    - `mise ci` green.
  </acceptance_criteria>
  <done>The chokepoint is complete for ACP-01's decision surface: every option kind, the fail-safe automation policy, degraded-client safety, and the MCP rule namespace — all through the one path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: permissions.mode real handler — persist-then-apply live flip</name>
  <precondition>16-05's ConfigSurface advertisement shape is live: permissions.mode appears as a select (ungated|gated) with a pending-handler no-op slot and a _global/ twin</precondition>
  <files>internal/acpserve/config_surface.go, internal/acpserve/config_test.go, internal/session/session.go, internal/session/gate_test.go</files>
  <behavior>
    - set_config_option permissions.mode=gated on a RUNNING session: the value persists to the project layer (WriteLayerOption, 0600 atomic — 16-D-07) BEFORE the live apply, and the very next mutating tool call in that same session suspends for permission (TestPermissionsModeFlip, Pitfall 8 — no session recreation).
    - Flipping back to ungated: the next mutating call executes with no dialog; deny rules still deny (D-05).
    - Invalid value (anything other than ungated|gated) → typed reject with option key + violation detail; session state untouched (16-D-09).
    - An inbound value equal to the currently-effective value → logged idempotent no-op, no layer write (the 16-05 D-10 re-push discipline — Zed re-pushes stored defaults every connection).
    - _global/permissions.mode routes to the global layer per the _global/ scope convention (16-D-08) with the same live-apply semantics.
    - Boot default: a fresh session with no stored mode reads ungated — zero dialogs (criterion 4's default leg).
  </behavior>
  <read_first>internal/acpserve/config_surface.go (16-05 output — the pending-handler slot, WriteLayerOption routing, effective-value resolution, the apply-hook seam), internal/acpserve/config_test.go (16-05's test fixtures — extend, do not duplicate), .planning/phases/16-acp-wire-foundation/16-05-PLAN.md (must_haves truths for D-07/D-09/D-10/D-11 semantics this handler must match; ApplyTurnModel as the live-apply precedent), internal/session/gate.go (the mode accessor the handler flips — atomic/mutex-guarded, read per call), .planning/phases/17-permissions-elicitation/17-RESEARCH.md (Pitfall 8; Open Question 3 — first-class sibling of tier/model, reuse WriteLayerOption)</read_first>
  <action>
RED: extend config_test.go with the six behavior cases driving the surface through its Set path (fake layer writer + a real Session-side mode accessor to observe the live flip). Commit failing.
GREEN: register the real permissions.mode handler as a first-class sibling of tier/model in the ConfigSurface: typed validation against the two-value enum, persist via WriteLayerOption on the routed layer, then flip the gate's mode accessor (the same accessor injected in Task 1 — shared, atomic). Advertised currentValue resolves through the existing effective chain so the editor never shows stale truth (16-D-11). Remove the pending-handler no-op branch for this id only (compaction-threshold stays pending for Phase 19). The gate_test flip case runs at session level: set option → gated call suspends in the SAME session.
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestPermissionsModeFlip|TestConfigSurface' -count=1 && go test ./internal/acpserve/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The flip test proves same-session effect: set_config_option then a mutating call suspends — no reconstruction, no restart.
    - The ungated-flip-back and boot-default cases assert zero dialogs with deny-still-denies.
    - Typed-reject and idempotent-repush cases assert the 16-05 semantics verbatim (violation detail present; no layer write on no-op).
    - The pending-handler log line for permissions.mode is gone from the surface's behavior (compaction-threshold's remains).
    - `mise ci` green.
  </acceptance_criteria>
  <done>ACP-01's switch is real end-to-end: editor-settable, persisted, and live on the running session's chokepoint — "available, not default" holds by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → permission ask resolution | the dialog answer (optionId/cancelled) is untrusted input selecting an execution decision |
| model tool_calls → chokepoint | the gated input JSON is untrusted content displayed in the dialog's toolCall shape |
| registry errors → gate policy | -32601/-32800 outcomes must map to fail-safe decisions, never to allow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Elevation of Privilege | chokepoint bypass (second path / subagent skip) | high | mitigate | gateCall is the only rule-evaluation site; both runTurn branches route through it; 17-05's doc task greps for stragglers |
| T-17-05 | Elevation of Privilege | automation silent-allow | high | mitigate | D-07 decline branch tested in both modes; deny/allow still enforced; decline is transcripted |
| T-17-06 | Elevation of Privilege | ungated mode widening to allow-all | high | mitigate | Ungated still evaluates rules (deny denies); explicit ungated+deny test; criterion-4 zero-dialog test |
| T-17-07 | Spoofing | dialog request id collision | high | mitigate | Registry UUID v4 ids (16-D-15) consumed as-is; unknown-id responses dropped by 16-03 interception |
| T-17-08 | DoS | client withholds the dialog answer | medium | mitigate | HUMAN-ASK timeout class + 16-D-14 ladder + suspension releases the turn mutex (no wedge) |
| T-17-SC | Tampering | package installs | high | accept | No installs (stdlib + existing deps); legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/session/ ./internal/acpserve/ ./internal/acp/ ./internal/perm/ -count=1` green.
- `mise ci` green.
- Live-Zed verification of the native dialog is 17-05's operator checkpoint (deterministic simulator E2E covers the wire level there).
</verification>

<success_criteria>
Criterion 1 and criterion 4 hold at the wire/test level: the native four-option ask works end-to-end with persist-then-execute trust semantics, the default stays dialog-free with rules still enforced, the mode flips live, and the ONE chokepoint exists in code with both branches and both modes provably routed through it (D-04/D-05/D-06/D-07).
</success_criteria>

<output>
Create `.planning/phases/17-permissions-elicitation/17-02-SUMMARY.md` when done
</output>
