# Phase 17 Deferred Items

Out-of-scope discoveries logged by the executor per the deviation-rule scope boundary (not fixed here; revisit when flagged).

## Watchlist

- **[2026-09-01, from 17-01] Transient `-race` gate flake (unreproduced, package unknown):** twice during 17-01's verification a full-suite run printed a bare `[test] FAIL` (once in `mise ci`, once in a later run); the failing package was lost to output truncation both times. Six dedicated follow-up runs were green (38/38 packages ok, including `mise test` ×3 and `mise ci` back-to-back). Not attributable to 17-01's changes (new leaf package `internal/perm`; zero existing files modified). **Action if it recurs:** run `mise ci 2>&1 | tee /tmp/ci.log` so the `--- FAIL` package line is captured before debugging.
- **[2026-09-01, from 17-02] FLAKE RECURRED AND CAPTURED — same machine-load family, root cause identified and margin-fixed:** during 17-02's first `mise ci`, `internal/acp` `TestRegistryRequestCancelledResponse` failed (`err = ... unanswered after retry (8 ms) — falling back (D-14); want ErrRequestCancelled`) — a pre-existing 16-03 timing-fragile test: it shrank the FAST-CONTROL window to 4ms, and under full-suite parallel load the test goroutine's `Deliver` lost the race to the 8ms D-14 ladder (standalone 10x green; second full-suite `mise ci` flaked a different timing test the same way, confirming load-sensitivity rather than a code regression). **Fix applied at 17-02 (Rule 3 — the red gate blocks the plan's self-check):** window widened to a named `testFastWindow = 25ms` const with the no-retry proof scaled to `3 * testFastWindow`; the test's proof (immediate -32800 resolution, zero retries) is preserved and strengthened on both sides of the D-14 boundary. Full `mise ci` green after the fix (zero FAIL lines, captured at /tmp/17-02-mise-ci-3.log). **Watch:** if load-flakes recur elsewhere, prefer enlarging test windows/margins over retry loops — and always `tee` the log first.
