---
phase: 22-background-execution-sandbox-reality
plan: 08
type: execute
wave: 7
depends_on: ["22-07"]
files_modified:
  - internal/runtime/cron_wiring.go
  - internal/runtime/wake_wiring_test.go
  - internal/runtime/runtime.go
  - internal/tasks/tracker.go
autonomous: true
gap_closure: true
gap_ids: [G-22-2, G-22-4]
requirements: [PAR-07, PAR-08]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low # estimate-calibration has 0 samples for gap-closure tasks (factor 1, not applied); modeled on the pre-split 22-07 projection (60k raw / 5 tasks) x 2 tasks + per-plan context overhead; runtime-package rows are the heavier half (multi-file end-to-end harnesses)

must_haves:
  truths:
    - "G-22-2 (CR-02): the ONE wake-drain chain goes IDLE when the pending queue is empty (no successor spawn, no per-session spin goroutine outliving the chain) and refuses to spawn past serve shutdown — scheduleWakeDrain is a no-op once the serve ctx is done, and the chain's exit defer restarts only when pending is non-empty AND the ctx is live"
    - "G-22-4 (CR-04, OQ5 family): session close fully ends the session's background machinery — CloseSession cancels RUNNING background subagents and deletes the per-session tracker/wakeInFlight/ptyManagers entries; a late completion reaches the drain via a NON-constructing session lookup, drops the batch loudly (one stderr line naming the session and count) without any sessionFor reconstruction, no ghost provider turn, and nothing re-enters r.sessions for the closed id"
  artifacts:
    - "internal/runtime/cron_wiring.go — wakeDrainChain's exit defer restarts ONLY on (ctx.Err()==nil AND tracker non-nil AND PendingPeek non-empty); scheduleWakeDrain refuses to spawn when the serve ctx is done; drainWakeNotifications resolves the session through a NON-constructing r.sessions lookup (sessMu-held) and treats a missing session as terminal: one stderr line + batch consumed via tr.Drain() + return true"
    - "internal/runtime/runtime.go — CloseSession (after eviction + s.Close()): cancel running subagents (OnClose CancelRunning link beside CancelQueued) and delete trackers/wakeInFlight (+ ptyManagers post-Drain) entries"
    - "internal/tasks/tracker.go — new CancelRunning() (invoke+clear every stored subagent cancel, return count — the close-time running-cancel leg; joins 22-07's releaseSubagentSlot in the same file)"
  key_links:
    - "tracker drain callback → scheduleWakeDrain (serve-ctx-guarded) → wakeDrainChain exit defer (gated restart) — the chain idles on empty and dies with the serve ctx (G-22-2)"
    - "CloseSession → s.Close() OnClose (CancelQueued + NEW CancelRunning) → trackers/wakeInFlight/ptyManagers Delete → late Complete → drainWakeNotifications non-constructing lookup → loud drop + chain exit (G-22-4)"

---

<objective>
Close Phase 22 verification gaps G-22-2 and G-22-4 (22-VERIFICATION.md frontmatter `gaps:` 2 and 4, review ids CR-02/CR-04) — the cron_wiring/runtime lifecycle cluster of the split gap-closure chain (22-07 tasks cluster → THIS PLAN → 22-09 coreexec seam). Both gaps are lifecycle paths of the ONE wake-drain infrastructure that the existing green batteries never exercise: the chain's unconditional self-restart (a permanent per-session spin that outlives serve shutdown) and the close-then-complete path that resurrects a closed session through the constructing sessionFor lookup and bills a ghost provider turn.

Purpose: these gaps falsify the phase goal's "ONE lifecycle infrastructure is itself lifecycle-sound" (PAR-07/PAR-08 convergence) and OQ5/PAR-08 close hygiene. The non-constructing lookup also retires IN-05's unbounded retry spam incidentally (its only trigger was construction failure).
Output: five regression rows in internal/runtime (chain idle, no-spawn-past-shutdown, ctx-cancel stop, closed-session terminal drop, close-prunes-state) plus tracker CancelRunning — all red against the current code and green after the fixes; `go test -race ./internal/runtime/ ./internal/tasks/` fully green with the new rows.

Gap mapping: G-22-2=CR-02 wake-chain idle, G-22-4=CR-04 close-then-complete terminal. (G-22-1/G-22-3 are 22-07's; G-22-5 is 22-09's.)

Scope guard: unchanged from 22-07's objective (WR-02..WR-08, IN-01/IN-02/IN-04 stay deferred review debt); IN-03's tracker/wakeInFlight(/ptyManagers) pruning IS in scope here as CR-04's substrate; IN-05 retires incidentally via the non-constructing lookup.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-VERIFICATION.md
@.planning/phases/22-background-execution-sandbox-reality/22-REVIEW.md
@.planning/phases/22-background-execution-sandbox-reality/22-07-SUMMARY.md
@internal/runtime/cron_wiring.go
@internal/tasks/tracker.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (G-22-2): wake-drain chain idles on empty queue and dies with the serve ctx (CR-02)</name>
  <files>internal/runtime/cron_wiring.go, internal/runtime/wake_wiring_test.go</files>
  <behavior>
    - Chain idles on empty: after one delivered batch the chain exits AND no successor spawns — the session's wakeInFlight flag stays false across a >=500ms sampled window (>=25 samples; the buggy unconditional restart re-spawns a successor within microseconds of every exit, so the flag is observably true during the window).
    - Goroutine assertion is a DELTA BAND, never exact process-wide equality: sample runtime.NumGoroutine() immediately before triggering the chain as baseline b, then assert every re-check sample after the settle window lies within [b, b+2] — stray background goroutines from earlier tests make exact counts flaky under the full-package -race run.
    - No spawn past serve shutdown: with a CANCELLED serve ctx, scheduleWakeDrain(sessionID) leaves the wakeInFlight flag false (the LoadOrStore'd entry never flips) and spawns nothing (goroutine count stays within the same [b, b+2] band after a settle window).
    - In-flight chain meets ctx cancel: a chain mid-retry (busy turn slot) stops when the ctx cancels and its exit defer does not restart it — flag false, stays false.
    - Genuine race still recovered: a Complete landing AFTER the chain's empty-queue decision but before the flag store still gets a fresh chain (plant a completion between the peek and the exit using the retry-interval harness; the batch must eventually deliver) — the gating must not strand a racing batch.
    - Flake guard: none of the new TestWakeChain_* tests calls t.Parallel() — the three existing TestWakeTurn batteries DO (wake_wiring_test.go:110/:209/:293) and Go runs parallel tests only alongside other parallel tests, so the serial TestWakeChain rows never overlap them; the mixed serial+parallel set is proven stable by running -run 'TestWakeChain|TestWakeTurn' with -count=3.
    - The three existing wake batteries (TestWakeTurn_BackgroundBashCompletion, TestWakeTurn_BusyClientTurnAccumulates, TestWakeTurn_EmptyPendingNoTurn) pass unmodified.
  </behavior>
  <read_first>internal/runtime/cron_wiring.go :342-432 (scheduleWakeDrain :369-383, wakeDrainChain :393-432 — the defer :393-400, empty-queue exit :412-414, retry select), internal/runtime/wake_wiring_test.go :292-360 (TestWakeTurn_EmptyPendingNoTurn harness — runner construction, tracker priming, wakeRetryInterval injection, provider call counting) and :109-210 for the deliver-path harness, internal/runtime/runtime.go :2389-2400 (serveCtxOrBackground — context.Background() fallback for test runners, whose Err() is always nil)</read_first>
  <action>
RED: add TestWakeChain_IdlesWhenEmpty (flag-window sampling + banded goroutine baseline), TestWakeChain_NoSpawnPastServeShutdown, and TestWakeChain_CtxCancelStopsChain to wake_wiring_test.go — none of the three calls t.Parallel(); commit failing (the idle test fails today: the flag keeps flipping true as successors spawn).
GREEN: (1) wakeDrainChain's exit defer: flag.Store(false) FIRST, then restart only when ctx.Err()==nil AND tr != nil AND len(tr.PendingPeek()) > 0 — re-derive tr inside the defer (the body's tr may be nil on the early tracker-missing return); keep the race comment updated to explain the ordering: a completion landing after the peek fires its own Complete→drain callback→scheduleWakeDrain, whose CAS on the just-cleared flag wins, so a gated restart is only needed for the completion that landed BETWEEN peek and store. (2) scheduleWakeDrain head guard: after the empty-sessionID check, return when r.serveCtxOrBackground().Err() != nil (test runners on the Background fallback are unaffected — Err() is always nil there). No changes to the chain body's loop, retry cadence, or drainWakeNotifications.
  </action>
  <verify>
    <automated>go test -race -count=3 ./internal/runtime/ -run 'TestWakeChain|TestWakeTurn' && go test -race -count=1 ./internal/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=3 ./internal/runtime/ -run 'TestWakeChain|TestWakeTurn'` exits 0 — all three new rows plus the three existing batteries, stable across repeated mixed serial+parallel runs.
    - `awk '/^func TestWakeChain/,/^}$/' internal/runtime/wake_wiring_test.go | grep -c 't.Parallel()'` is 0 (the new rows stay serial).
    - `grep -c 'PendingPeek()) > 0' internal/runtime/cron_wiring.go` is >= 1 (the gated restart) and the scheduleWakeDrain guard references serveCtxOrBackground().Err() (grep -c 'serveCtxOrBackground().Err()' internal/runtime/cron_wiring.go is >= 1).
    - `go vet ./internal/runtime/` clean.
  </acceptance_criteria>
  <done>The ONE wake-drain infrastructure is lifecycle-sound: idle means idle (zero chain goroutines per quiesced session), serve shutdown ends the chains, a racing completion still wakes a fresh chain, and the battery is deterministic under the full-package -race run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (G-22-4): close-then-complete is terminal — cancel running subagents, prune state, never reconstruct (CR-04)</name>
  <files>internal/runtime/runtime.go, internal/runtime/cron_wiring.go, internal/tasks/tracker.go, internal/runtime/wake_wiring_test.go</files>
  <precondition>22-07 landed — `grep -c 'delete(t.subagentCancels' internal/tasks/tracker.go` is 1 (Complete releases slots and retires cancel entries; this task's CancelRunning joins the same file beside it).</precondition>
  <behavior>
    - drainWakeNotifications on a MISSING session is terminal: with a tracker registered and pending planted but the session absent from r.sessions, one call (same package) returns true, consumes the batch (PendingPeek empty afterwards), and writes exactly ONE stderr line naming the session id and the dropped count — no retry loop, and the provider/wake-turn counter stays at zero.
    - No resurrection end-to-end: build a session (TestWakeTurn harness), CloseSession(id), then fire a late completion on the still-held tracker reference — r.sessions never re-contains the id, the fake provider call count stays zero, and after the settle window the chain flag is false (in TODAY's code the drain's constructing sessionFor re-inserts a full session and runs a real wake turn — this row is the CR-04 headline regression).
    - CloseSession cleans the runner's per-session state: after close, r.trackers.Load(id) and r.wakeInFlight.Load(id) are both miss; r.ptyManagers.Load(id) is miss (deleted after OnClose's Drain ran).
    - RUNNING background subagents are cancelled at close: register one on the session's tracker with a channel-signalling cancel; CloseSession closes that channel (the NEW CancelRunning link beside CancelQueued in OnClose), and a counted stderr note fires when the running-cancel count is non-zero.
    - The queued-cancel leg and the whole existing close battery stay green (TestTrackerClose_DropsQueuedNotRunning, TestSessionClosePTY, TestInProcessReloadAfterCloseRebuildsSession — the close-then-RESTORE flow must still reconstruct cleanly through sessionFor on demand).
  </behavior>
  <read_first>internal/runtime/cron_wiring.go :434-448 (drainWakeNotifications head — the constructing r.sessionFor call and the stay-pending retry), internal/runtime/runtime.go :2783-2809 (CloseSession — eviction-before-Close, the place for the deletes), :2346-2362 (OnClose chain — CancelQueued at :2356, ptyMgr.Drain :2351, where CancelRunning joins), :2015-2035 (tracker construction + SetDrain — what a resurrected sessionFor rebuilds), :2364 (r.sessions[sessionID] = s — what reconstruction re-does), internal/tasks/tracker.go CancelTask and CancelQueued (locate by symbol — line numbers shifted after 22-07's releaseSubagentSlot edit; CancelQueued is the shape CancelRunning mirrors), internal/runtime/wake_wiring_test.go :109-207 (the runner/session harness for the end-to-end row)</read_first>
  <action>
RED: add TestWakeChain_ClosedSessionDropsBatch (terminal unit row + end-to-end no-resurrection row) and TestCloseSession_PrunesWakeState (map deletes + running-cancel row) to wake_wiring_test.go — same serialization rule as Task 1 (no t.Parallel()); commit failing (the end-to-end row fails today: sessionFor reconstructs and r.sessions regains the id).
GREEN: (1) tracker.go: new CancelRunning() int — under t.mu, invoke every stored subagentCancels entry, clear the map, return the count (queued waiters stay CancelQueued's job; the two are idempotent together when both run). (2) runtime.go OnClose: beside the CancelQueued block, call tracker.CancelRunning() and note the count on stderr when non-zero (same sentence family as the dropped-queued note). (3) runtime.go CloseSession: AFTER s.Close() returns (so OnClose's cancels and PTY Drain have run while the tracker was still discoverable), delete r.trackers, r.wakeInFlight, and r.ptyManagers entries for the id — unconditional deletes, also on the session-missing path. (4) cron_wiring.go drainWakeNotifications: replace the constructing r.sessionFor(ctx, sessionID) with a non-constructing lookup — r.sessMu.Lock, s, ok := r.sessions[sessionID], r.sessMu.Unlock; when !ok: one stderr line ("wake drain for %s: session closed — dropping %d pending notification(s)") using the PendingPeek count, consume via tr.Drain(), return true (the chain exits; this also retires the IN-05 infinite retry, whose only trigger was construction failure). The found-session path is unchanged through TryLock/deliver.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestWakeChain|TestCloseSession|TestSessionClose|TestInProcessReloadAfterClose' && go test -race -count=1 ./internal/tasks/ && go test -race -count=1 ./internal/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/runtime/ -run 'TestWakeChain_ClosedSessionDropsBatch|TestCloseSession_PrunesWakeState'` exits 0.
    - The end-to-end row asserts r.sessions lacks the closed id after the late completion settles AND the provider call counter is zero.
    - `grep -c 'r.trackers.Delete' internal/runtime/runtime.go` is 1 and `grep -c 'r.wakeInFlight.Delete' internal/runtime/runtime.go` is 1; `grep -c 'func (t \*Tracker) CancelRunning' internal/tasks/tracker.go` is 1.
    - cron_wiring.go no longer calls sessionFor inside drainWakeNotifications (grep -n 'sessionFor' internal/runtime/cron_wiring.go shows only the runAutomationTurn site).
    - `go test -race -count=1 ./internal/runtime/ ./internal/tasks/` exits 0 (existing close/reload batteries unmodified and green, including 22-07's rows); `go vet` on both packages clean.
  </acceptance_criteria>
  <done>Close is final for the background machinery: running subagents die with the session, per-session wake state is pruned, and a late completion drops its batch loudly instead of rebuilding a ghost session and billing a provider turn nobody owns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| late task completions → session lifecycle | Completions arriving after operator close must not cross back into provider/forwarder resources |
| serve ctx → goroutine lifetime | Chain goroutines must not outlive the ctx the editor owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-20 | Denial of Service | cron_wiring.go wakeDrainChain/scheduleWakeDrain | high | mitigate | Task 1: gated restart (pending non-empty AND ctx live) + spawn refusal past serve ctx — kills the permanent per-session spin and the post-shutdown churn |
| T-22-22 | Information Disclosure / cost | runtime.go CloseSession + cron_wiring.go drainWakeNotifications | high | mitigate | Task 2: cancel running subagents at close, prune per-session state, non-constructing lookup with loud terminal drop — ghost provider turns, transcript appends, and spawned subprocesses into closed sessions become impossible |
| T-22-24 | (accepted) | wake-chain stderr drop line | low | accept | The loud drop line names the session id and count only — no task content; stderr is the diagnostics channel by transport discipline |

</threat_model>

<verification>
- `go test -race -count=1 ./internal/runtime/ ./internal/tasks/` green — the new chain/close rows AND every existing 22-01..22-07 row (the fixes must be strictly additive on paths the green batteries already prove).
- `go test -race -count=3 ./internal/runtime/ -run 'TestWakeChain|TestWakeTurn'` exits 0 — the flake guard: serial TestWakeChain rows stay deterministic beside the parallel TestWakeTurn batteries.
- `go vet ./internal/runtime/ ./internal/tasks/` clean.
- `GOOS=linux CGO_ENABLED=0 go build ./...` stays green (no new deps).
- Re-verification mapping: G-22-2→TestWakeChain_IdlesWhenEmpty/NoSpawnPastServeShutdown/CtxCancelStopsChain; G-22-4→TestWakeChain_ClosedSessionDropsBatch + TestCloseSession_PrunesWakeState.
</verification>

<success_criteria>
- Both gap truths in must_haves hold, each pinned by a named test that fails against the pre-fix code.
- No behavior change on any path the existing batteries pin: coalescing, cap notes, PTY close drain, close-then-restore reconstruction all byte-stable.
- The new wake-chain rows are deterministic under the full-package -race run (no t.Parallel() in TestWakeChain_*, banded goroutine assertions).
- Zero scope creep: WR-02..WR-08 and IN-01/IN-02/IN-04 untouched; IN-05 retired incidentally by the non-constructing lookup, noted in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-08-SUMMARY.md` when done
</output>
