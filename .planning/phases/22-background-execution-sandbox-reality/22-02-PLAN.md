---
phase: 22-background-execution-sandbox-reality
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - internal/coreexec/background.go
  - internal/coreexec/background_test.go
  - internal/coreexec/procopts_linux.go
  - internal/coreexec/procopts_other.go
  - internal/acpserve/config_surface.go
  - internal/acpserve/config_test.go
  - internal/acpserve/acp_serve.go
  - internal/acpserve/serve_test.go
  - internal/runtime/runtime.go
autonomous: true
requirements: [PAR-08]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Stopping a background Bash task escalates TERM-before-KILL: the process group receives SIGTERM first, a grace window (~5s discretion) elapses, and only then the existing SIGKILL group-kill + reapGroup runs — wrapper shells' grandchildren get the chance to die cleanly (PAR-08, Pattern 6)"
    - "On Linux, background task children carry Pdeathsig SIGKILL so a killed ass-guard process cannot orphan them — the field is set in a //go:build linux helper with the Start path's threading caveat documented (PAR-08)"
    - "Over-cap background Bash starts QUEUE with a visible note (FIFO, drained as tasks complete) instead of failing with errBgCap — a bounded pathological queue still errors; the cap is operator-tunable via background.bash (D-11, D-12)"
    - "Both caps join the configOptions menu as background.subagents=8 and background.bash=16, advertised-always and applied-as-landed per the Phase 16 rule (D-12)"
    - "At serve start, BEFORE the scheduler starts, a stale-log sweep scans .ass-guard/outputs/ for logs with no live owning task and tombstone-marks them (.stale rename) — it never silently deletes and never removes the directory (PAR-08, OQ4 resolution)"
    - "No background process group outlives agent shutdown: ReapAll cancels running tasks via the escalation ladder and drops queued-but-unstarted ones (OQ5, 18-D-12 family)"
  artifacts:
    - path: "internal/coreexec/procopts_linux.go"
      provides: "//go:build linux setPdeathsig(attr *syscall.SysProcAttr) assigning syscall.SIGKILL, with the go.dev/issue/27505 thread-lifetime caveat in the doc comment"
      contains: "setPdeathsig"
    - path: "internal/coreexec/procopts_other.go"
      provides: "//go:build !linux no-op setPdeathsig — darwin keeps the static-binary claim and compiles clean"
      contains: "setPdeathsig"
    - path: "internal/coreexec/background.go (MOD)"
      provides: "escalation ladder in Stop and the ctx-cancel path (SIGTERM group → grace → SIGKILL group + reapGroup); FIFO waiter queue replacing the errBgCap fail path with the queued-note render form; bgConcurrentCap becomes an injected field (background.bash, default 16); ReapAll drains the waiter queue"
      contains: "terminateGroup"
    - path: "internal/acpserve/config_surface.go (MOD)"
      provides: "optBackgroundSubagents/optBackgroundBash option ids in the optionsLocked() menu with numeric select values, validateSettableLocked/applyLocked/persistLocked cases per the Phase-16 advertise-all apply-as-landed rule"
      contains: "optBackgroundBash"
    - path: "internal/acpserve/acp_serve.go (MOD)"
      provides: "startup stale-log sweep step in Run's pipeline positioned before scheduler start; sweepS logs a tombstone count, never an error up through Run (degrade loudly, never refuse)"
      contains: "sweepStaleTaskLogs"
  key_links:
    - from: "internal/acpserve/config_surface.go Set"
      to: "internal/runtime/runtime.go sessionFor construction"
      via: "the applied background.bash value flows into TaskRegistry construction (injected cap field) and background.subagents into tasks.Tracker construction (22-01's TrackerOpts) — one config surface, two consumers"
      pattern: "injected caps"
    - from: "internal/coreexec/background.go Stop/ReapAll"
      to: "the PAR-08 escalation ladder"
      via: "every termination path (operator Stop, ctx cancel, ReapAll) funnels through ONE terminateGroup helper so TERM-before-KILL cannot drift between call sites"
      pattern: "terminateGroup"
  prohibitions:
    - requirement_id: PAR-08
      category: transparency
      statement: "The startup stale-log sweep MUST never silently delete output logs — a stale log is tombstone-marked (.stale rename) with a counted stderr note, and deletion only happens to already-marked logs past the retention window on a LATER sweep; the outputs directory itself is never removed"
      status: unresolved

---

<objective>
Harden background Bash lifecycle to the PAR-08 letter: TERM-before-KILL escalation with Linux Pdeathsig, the D-11 queue-on-cap reversal with D-12 operator-tunable caps in the configOptions menu, and the startup stale-log sweep with tombstone semantics. Everything lands inside the existing TaskRegistry + serve pipeline — no parallel registry (RESEARCH primary recommendation).

Purpose: PAR-08's process-group lifecycle letter (escalation, Pdeathsig, sweep, no orphans) and D-10/D-11/D-12's bounded, operator-tunable background resource model.
Output: escalation ladder + Pdeathsig helpers, queue-on-cap TaskRegistry, configOptions menu entries, startup sweep — green under -race with the linux legs compile-gated.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-RESEARCH.md
@.planning/phases/22-background-execution-sandbox-reality/22-PATTERNS.md
@internal/coreexec/background.go
@internal/acpserve/config_surface.go
@internal/acpserve/acp_serve.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/coreexec/procopts_linux.go` (NEW, //go:build linux) — `setPdeathsig(attr *syscall.SysProcAttr)`; doc comment cites go.dev/issue/27505 (Pdeathsig fires on thread death; LockOSThread around Start is the mitigation if premature kills appear — RESEARCH Pitfall 3/A4).
- `internal/coreexec/procopts_other.go` (NEW, //go:build !linux) — no-op `setPdeathsig`.
- `internal/coreexec/background.go` (MOD) — `terminateGroup(pid int, done <-chan struct{}, grace time.Duration)` (portable TERM→grace→KILL+reapGroup); `TaskRegistry.cap` injected field replacing the bgConcurrentCap const read; `TaskRegistry.queueBound` documented const; FIFO waiter queue with `queuedNote(position, cap)` prose form; ReapAll cancels running via the ladder and drops waiters.
- `internal/acpserve/config_surface.go` (MOD) — `optBackgroundSubagents = "background.subagents"`, `optBackgroundBash = "background.bash"`; menu rows, validate/apply/persist cases; defaults 8 and 16 (D-12).
- `internal/acpserve/acp_serve.go` (MOD) — `sweepStaleTaskLogs(workDir string) error` tombstone sweep + its pipeline step in Run before the scheduler start.
- Test additions — `internal/coreexec/background_test.go` (ladder + queue batteries), `internal/acpserve/config_test.go` (menu rows), `internal/acpserve/serve_test.go` (sweep ordering).
</artifacts_this_phase_produce>

<flagged_assumptions>
- **PAR-08 edge-probe `empty` row (status: unresolved — surfaced, never auto-resolved):** zero background tasks at sweep time is a no-op (zero renames, zero deletions, count 0, directory untouched) and a background task that produced zero output still notifies with an empty-string tail (22-01's payload contract — all D-02 fields present). Task 3's battery pins the sweep no-op; the row stays flagged until verification.
- **PAR-08 edge-probe `ordering` row (status: unresolved — surfaced, never auto-resolved):** over-cap dispatches drain in FIFO submission order; ties break by submission sequence, never map order. Task 2's battery pins it; flagged until verification.
- **Pdeathsig mitigation efficacy (RESEARCH A4, MEDIUM):** runtime.LockOSThread around cmd.Start() mitigates the thread-lifetime race (go.dev/issue/27505); residual risk (premature child kill) is documented in procopts_linux.go and caught only by a Linux host — darwin cannot exercise it. Compile gate is the darwin-side proof.
- **Grace window value (CONTEXT discretion):** 5s TERM grace in terminateGroup; documented const, operator-facing behavior note in the queued-note prose family.
- **Retention window (OQ4 discretion):** already-.stale logs older than 7 days delete on a later sweep; const in acp_serve.go with the D-20 audit spirit (tombstone first, never silent rm).
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TERM-before-KILL escalation ladder + Linux Pdeathsig</name>
  <files>internal/coreexec/background.go, internal/coreexec/background_test.go, internal/coreexec/procopts_linux.go, internal/coreexec/procopts_other.go</files>
  <behavior>
    - Stop on a `sh -c 'trap "" TERM; sleep 30'` child (TERM-immune trap proves escalation): the group first receives SIGTERM (observed via a TERM-sensitive sibling in the group), then after the grace window the SIGKILL group-kill + reapGroup completes and Start's cmd.Wait returns — the ladder, not SIGKILL-first (PAR-08).
    - Stop on a TERM-respecting child (`sh -c 'trap "exit 7" TERM; sleep 30'`): SIGTERM alone ends it; the exit status records 7/signal-terminated — no SIGKILL ever sent (kill -0 group gone before grace expiry).
    - The ladder is reachable from ALL termination paths: explicit Stop, registry ctx cancel, and ReapAll — one terminateGroup helper, three callers.
    - ESRCH from any kill step is treated as success (group already gone — the bash.go idiom), never an error.
    - Linux build: a background task's cmd.SysProcAttr carries Pdeathsig SIGKILL via setPdeathsig (asserted by compile-gate + a linux-tagged unit test reading the attr); darwin build compiles with the no-op (GOOS=linux CGO_ENABLED=0 build is the gate).
  </behavior>
  <read_first>internal/coreexec/background.go (Start :143+ cmd construction, Stop :278-299 — the SIGKILL-first gap this task closes, Wait goroutine :185-203, reapGroup + reapGroupBudget idiom), internal/coreexec/bash.go :88-138 (killGroupOnCtx/reapGroup — the group-kill idiom to reuse verbatim), 22-RESEARCH.md (Pattern 6 escalation + Pdeathsig doc text, Pitfall 3 thread-lifetime caveat), 22-PATTERNS.md (background.go assignment), 22-CONTEXT.md (PAR-08 letter)</read_first>
  <action>
RED: background_test.go gains TestEscalation_* batteries using real children: the TERM-immune-trap case asserts the child survives past a shortened grace (inject a 150ms grace via a test-visible field, not a const change), receives SIGKILL, and Wait returns; the TERM-respecting case asserts exit-by-TERM with the trapped status and that the process table shows no survivor (reapGroup already bounds the fork-vs-kill race). Commit failing.
GREEN: (1) Add terminateGroup(pid int, done <-chan struct{}, grace time.Duration) to background.go: syscall.Kill(-pid, SIGTERM); select on done or the grace timer; on timeout syscall.Kill(-pid, SIGKILL) + reapGroup(pid); ESRCH-clean at every step (errors.Is syscall.ESRCH → nil). (2) Rewire Stop, the ctx-cancel path, and ReapAll to call terminateGroup — delete the direct SIGKILL-first calls; the doc comment at :278 changes from promising SIGKILL to documenting the ladder (Pitfall 7's doc-comment discipline). (3) Create procopts_linux.go/procopts_other.go with setPdeathsig and call it from Start's SysProcAttr construction; the doc comment names the thread-lifetime caveat and the LockOSThread mitigation posture. (4) Add a //go:build linux unit test asserting the attr field is set through Start's construction path.
Per PAR-08 the escalation is additive to the proven Setpgid idiom; per the AGENTS.md static-binary constraint both new files stay CGO-free stdlib syscall.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/coreexec/ -run 'TestEscalation' && GOOS=linux CGO_ENABLED=0 go build ./... && GOOS=linux CGO_ENABLED=0 go vet ./internal/coreexec/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/coreexec/ -run 'TestEscalation'` exits 0: the TERM-immune case proves SIGKILL only after grace; the TERM-respecting case proves no SIGKILL (exit status comes from the trapped TERM handler).
    - `grep -c 'terminateGroup' internal/coreexec/background.go` is >= 4 (definition + Stop + cancel + ReapAll callers) — one funnel, no drift.
    - `GOOS=linux CGO_ENABLED=0 go build ./...` and `GOOS=linux CGO_ENABLED=0 go vet ./internal/coreexec/` exit 0 (Pdeathsig file compiles cgo-free; the linux-tagged attr test compiles).
    - `go test -race -count=1 ./internal/coreexec/` exits 0 — all six pre-existing TestBackground_* batteries stay green.
    - `grep -c 'Pdeathsig' internal/coreexec/procopts_linux.go` is >= 1 and procopts_other.go contains no Pdeathsig reference (grep count 0 there).
  </acceptance_criteria>
  <done>Every background termination path escalates TERM → grace → KILL on the process group with bounded reap, and Linux children carry Pdeathsig SIGKILL so a dead ass-guard cannot orphan them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Queue-on-cap (D-11) + operator-tunable caps in configOptions (D-12) + ReapAll queued-drop (OQ5)</name>
  <files>internal/coreexec/background.go, internal/coreexec/background_test.go, internal/acpserve/config_surface.go, internal/acpserve/config_test.go, internal/runtime/runtime.go</files>
  <behavior>
    - Start at the bash cap (16 default, injected) → returns a queued form (visible note naming queue position and cap) instead of the errBgCap error; the command starts when a slot frees, in FIFO order (PAR-08 ordering probe: submission order, ties by sequence).
    - A queued Start's id is returned immediately (the exec_ id exists before the process does — the model can Stop/Output it); Output on a queued task reports the queued state; Stop on a queued task removes it from the queue with a stopped note.
    - Pathological queue past the documented bound (64) → the structured cap error path survives (errors naming the cap), so the queue cannot grow unbounded (RESEARCH Pitfall 7).
    - ReapAll cancels running tasks through the ladder and drops waiters without starting them — the OQ5 close semantics for the bash leg (22-01 owns the tracker leg).
    - configOptions menu advertises background.subagents (default 8) and background.bash (default 16) as select options; Set applies-as-landed: the bash value reaches the next constructed TaskRegistry's injected cap, the subagents value reaches tasks.TrackerOpts (runtime.go construction site) — Phase 16's advertise-all rule (D-12).
  </behavior>
  <read_first>internal/coreexec/background.go :140-149 (the errBgCap fail path being reversed — its doc comment promises the old semantics and MUST be rewritten, Pitfall 7), internal/coreexec/background_test.go (existing cap coverage is ZERO — errBgCap has no test references, 22-PATTERNS correction; write NEW cap tests), internal/acpserve/config_surface.go (optModel const block :45-49, optionsLocked() :479-531 build() helper, Set :149, validateSettableLocked :375, applyLocked :435, persistLocked :411 — the Phase-16 menu machinery to extend), internal/runtime/runtime.go :1107-1138 (construction site where injected caps land), 22-CONTEXT.md (D-10, D-11, D-12), 22-RESEARCH.md (Open Question 5, Pitfall 7), internal/tasks/tracker.go (TrackerOpts — the subagents cap consumer from 22-01)</read_first>
  <action>
RED: (1) New TestBackgroundCap_* batteries: over-cap Start returns the queued form with position note; FIFO drain on completion; queued-id Stop/Output semantics; bound-breach error; ReapAll waiter drop. (2) config_test.go rows asserting the two new menu ids render with defaults 8/16 and that Set routes both. Commit failing.
GREEN: (1) background.go: replace the over-cap error branch with FIFO waiter append returning the queued-note form (renderBackgroundStart :435-440 prose family); mint the id before queuing; the Wait goroutine's completion path pops the head waiter and starts it with full registration; add the documented queueBound const and keep errBgCap ONLY for the bound breach (update the :140-141 comment — it currently promises fail semantics). (2) Convert bgConcurrentCap from const to an injected TaskRegistry field (zero value → 16 default) so runtime.go passes the operator value; document the injection in the NewTaskRegistry signature. (3) ReapAll: iterate waiters and drop them (count reported), cancel running via terminateGroup (Task 1). (4) config_surface.go: add optBackgroundSubagents/optBackgroundBash consts beside optModel, menu rows via the existing build() helper with numeric select values (4/8/16/32 for subagents; 8/16/32/64 for bash — advertised-always), validateSettableLocked/applyLocked/persistLocked cases following the optCompactionThresh numeric precedent, apply-as-landed semantics (next session construction picks it up; no mid-session cap mutation). (5) runtime.go: read both applied values at sessionFor and pass them into NewTaskRegistry and tasks.NewTracker.
Per D-11 one discipline two numbers; per D-12 both keys are operator-tunable; per OQ5 queued-but-unstarted tasks cancel silently at close with a noted count.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/coreexec/ -run 'TestBackgroundCap' && go test -race -count=1 ./internal/acpserve/ -run 'TestConfig' && go test -race -count=1 ./internal/coreexec/ ./internal/acpserve/ ./internal/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/coreexec/ -run 'TestBackgroundCap'` exits 0: over-cap Start returns queued form (not an error), FIFO order pinned, queued Stop/Output semantics pinned, bound-breach errors.
    - `grep -c 'errBgCap' internal/coreexec/background.go` is >= 2 (bound-breach path + taxonomy) and the :140 doc comment no longer promises fail-on-cap (it names the queue contract).
    - `go test -race -count=1 ./internal/acpserve/ -run 'TestConfig'` exits 0 with both menu ids present at defaults 8 and 16, and a Set round-trip asserting the applied value lands in the next construction (apply-as-landed, D-12).
    - `go test -race -count=1 ./internal/coreexec/ ./internal/acpserve/ ./internal/runtime/` exits 0 — no regression across the three touched packages.
  </acceptance_criteria>
  <done>Being busy no longer fails: over-cap background Bash queues FIFO with a visible note under operator-tunable caps advertised in the configOptions menu, and close drops never-started work with a count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Startup stale-log sweep — tombstone-mark, never silent rm (OQ4)</name>
  <files>internal/acpserve/acp_serve.go, internal/acpserve/serve_test.go</files>
  <behavior>
    - Serve start with orphaned logs in .ass-guard/outputs/ (no live owning task — at startup NOTHING is live): each is renamed to <id>.log.stale, one counted stderr note reports the tombstone total (PAR-08 sweep letter, OQ4 disposition).
    - Already-.stale logs older than the retention window (7 days, mtime) are DELETED on a later sweep with the deletion count in the note — tombstone first, GC later; fresh .log files (mtime within the current run) are never touched.
    - Zero stale logs → no-op: zero renames, zero deletions, count 0, directory untouched (PAR-08 empty probe).
    - Missing outputs directory → no-op, no error (first run).
    - Sweep failure on one entry (permission) skips that entry with the note continuing — degrade, never refuse serve (the seedACPGuard idiom).
    - Ordering: the sweep runs in Run's pipeline BEFORE the scheduler starts and before any session opens (RESEARCH Pitfall 10 — a late sweep races fresh logs).
  </behavior>
  <read_first>internal/acpserve/acp_serve.go (Run pipeline :121-252 — the step order to extend: after body store/mirror, BEFORE scheduler start; seedACPGuard :75-88 — the degrade-loudly idiom and doc-comment style), internal/coreexec/background.go :120-136 (openTaskLog — the .ass-guard/outputs/<id>.log layout being swept), 22-RESEARCH.md (Pitfall 10 startup order, Open Question 4 recommendation), 22-CONTEXT.md (PAR-08 letter), 22-PATTERNS.md (acp_serve.go assignment)</read_first>
  <action>
RED: serve_test.go gains TestStaleSweep_* batteries driving sweepStaleTaskLogs against a temp workdir: seed .ass-guard/outputs/ with fresh .log, old .log, already-.stale-old, already-.stale-fresh, and a foreign subdirectory; assert rename/delete/untouched per row, the counted note (capture stderr via the Run test harness or make the sweep return its counts for the unit battery), the zero-op and missing-dir rows, and one pipeline-order test asserting the sweep step executes before the scheduler-start step in Run (ordering assertion via a seam or step-recording hook in the test). Commit failing.
GREEN: (1) Implement sweepStaleTaskLogs(workDir string) error in acp_serve.go: readdir .ass-guard/outputs/; for *.log files with no live owner (at serve start there are none — everything present is stale by definition, which is what makes startup timing safe per Pitfall 10) rename to *.stale; for *.stale files with mtime older than the documented retention const, os.Remove; accumulate counts; one fmt.Fprintf(stderr) note with the totals; per-entry errors are skipped into the note. NEVER delete the directory; never touch *.log newer than process start minus a small skew margin. (2) Insert the step in Run's pipeline before scheduler start, error handled degrade-loudly (one stderr line, serve continues — the :28-32 doc-comment contract). (3) Document the OQ4 resolution in the function doc comment: tombstone-mark adopted per the RESEARCH recommendation (D-20 audit spirit).
Per PAR-08 this closes the sweep letter; the never-silent-rm rule is the plan's prohibition and the battery pins it.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ -run 'TestStaleSweep' && go test -race -count=1 ./internal/acpserve/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/acpserve/ -run 'TestStaleSweep'` exits 0: orphaned .log renamed to .stale; .stale past the window deleted; fresh .log untouched; zero-op and missing-dir no-ops; per-entry failure skips without failing the sweep.
    - The pipeline-order assertion proves the sweep step runs before the scheduler-start step (Pitfall 10).
    - `grep -c '.stale' internal/acpserve/acp_serve.go` is >= 2 and the function doc comment names the OQ4 tombstone disposition.
    - `go test -race -count=1 ./internal/acpserve/` exits 0 (whole package green — serve behavior otherwise unchanged).
  </acceptance_criteria>
  <done>Startup sweeps orphaned task logs into counted tombstones with windowed GC on later sweeps — audit-visible, never a silent rm, always before the scheduler can race it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → cap values | configOptions Set values flow into registry construction |
| filesystem (.ass-guard/outputs/) → sweep | Startup sweep renames/deletes files in the operator's workdir |
| signal delivery → process groups | TERM/KILL escalation reaches children ass-guard did not write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Denial of Service | orphaned background groups after kill -9 | high | mitigate | Pdeathsig on Linux (Task 1) + TERM-before-KILL ladder + ReapAll on close + startup stale sweep — the PAR-08 letter is the mitigation set |
| T-22-06 | Denial of Service | fork-bomb via unbounded background starts | medium | mitigate | D-11 caps (default 16) with a bounded FIFO queue (64) and a surviving structured error past the bound (Task 2) |
| T-22-07 | Tampering | sweep deleting operator files outside outputs/ | high | mitigate | sweepStaleTaskLogs operates ONLY on .ass-guard/outputs/ entries matching the <id>.log/.stale shapes it creates; never removes the directory; tombstone-before-delete is the pinned prohibition |
| T-22-08 | Elevation of Privilege | config Set widening caps without authority | low | accept | Caps only bound resource use (lower is safer); the Phase-16 apply-as-landed machinery already authenticates the operator session — widening a cap is an operator's prerogative |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/coreexec/ ./internal/acpserve/ ./internal/runtime/` green (phase quick command subset).
- `GOOS=linux CGO_ENABLED=0 go build ./... && GOOS=linux CGO_ENABLED=0 go vet ./internal/coreexec/` — the linux compile gate for Pdeathsig.
- All six pre-existing TestBackground_* batteries stay green alongside the new TestEscalation_/TestBackgroundCap_ batteries.
</verification>

<success_criteria>
- Background termination escalates TERM → grace → SIGKILL on the group from every path; Linux children carry Pdeathsig (PAR-08).
- Over-cap bash starts queue FIFO with a visible note under operator-tunable caps advertised in configOptions (D-11, D-12).
- Startup tombstone-sweeps orphaned logs before the scheduler starts, never silently deleting (PAR-08, OQ4).
- Probe rows assigned to this plan (PAR-08 empty; PAR-08 ordering) are authored as behaviors/flagged assumptions — zero silent drops.
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-02-SUMMARY.md` when done
</output>
