---
phase: 22-background-execution-sandbox-reality
plan: 06
type: execute
wave: 5
depends_on: ["22-04", "22-05"]
files_modified:
  - cmd/ass-guard/acp_serve.go
  - cmd/ass-guard/main.go
  - internal/acpserve/options.go
  - internal/acpserve/acp_serve.go
  - internal/acpserve/serve_test.go
  - internal/coreexec/bash.go
  - internal/coreexec/bash_test.go
  - internal/coreexec/background.go
  - internal/coreexec/background_test.go
  - internal/coreexec/ptty.go
  - internal/coreexec/ptty_test.go
  - internal/coreexec/register.go
  - internal/runtime/runtime.go
autonomous: true
requirements: [SAND-01]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The sandbox default is OFF and --sandbox=off always escapes — no tool process is ever confined unless the operator asked; invalid flag values fail cobra validation at startup, never mid-serve (SAND-01)"
    - "With --sandbox=on, ALL THREE Bash-class exec sites confine through sandbox.WrapCmd: the foreground Bash exec site, TaskRegistry.Start's background exec construction, and the PTY persistent-shell spawn — a background Bash task under --sandbox=on cannot reach the network (a curl-style child fails its connect, proving confinement through Start's exec construction, not just the foreground path), and a persistent call under --sandbox=on runs in a confined shell (D-09 orthogonality: persistence and confinement compose; D-05: children only)"
    - "The startup probe runs in Run's pipeline BEFORE the scheduler start; enabled-but-unavailable emits ONE loud stderr warning, marks availability false, and EVERY command that runs unconfined because of it gets an individual stderr note + counter — never a silent fail-open (SAND-01, Pitfall 1)"
    - "dangerouslyDisableSandbox=true with the sandbox on runs the command unconfined with a loud stderr note + counter at every exec site that honors it (foreground, background, persistent); with the sandbox off it stays today's documented no-op (OQ2 resolution, all arms pinned)"
    - "Confinement never touches the lifecycle owner: killGroupOnCtx/Setpgid/terminateGroup and the PTY Drain operate on wrapped children exactly as on bare shells — signals are neither FS nor network operations, so group-kill reaches confined children; landlock rulesets do not intercept signals (D-05)"
    - "The re-exec sentinel child never serves ACP: main() calls sandbox.ApplySandboxChildHook() before root-command dispatch — on the sentinel env the process applies the ruleset, execs the target, and never reaches cobra (D-05)"
  artifacts:
    - path: "cmd/ass-guard/acp_serve.go (MOD)"
      provides: "c.Flags().StringVar(&sandboxMode, \"sandbox\", \"off\", ...) validated to {off,on}; the value flows into acpserve.Options.SandboxMode"
      contains: "sandbox"
    - path: "cmd/ass-guard/main.go (MOD)"
      provides: "the unconditional sandbox.ApplySandboxChildHook() call at the top of main() — before newRootCmd(); a normal start never triggers it (no sentinel env), the re-exec'd child never reaches cobra"
      contains: "ApplySandboxChildHook"
    - path: "internal/acpserve/options.go + acp_serve.go (MOD)"
      provides: "Options.SandboxMode string (off|on); resolveSandboxAvailability(opts) sandbox.Availability startup probe step in Run's pipeline (before scheduler start, beside the 22-02 sweep); the resolved sandbox.Handle stored in the composition for coreexec.Config and PTYOpts; one stderr warning on enabled-but-unavailable"
      contains: "SandboxMode"
    - path: "internal/coreexec/bash.go (MOD)"
      provides: "the sandbox wrap at the foreground exec site: enabled+not-disabled → sandbox.WrapCmd before the existing group discipline; dangerouslyDisableSandbox honored with loud note (OQ2); the :57-64 documented-no-op comment retired"
      contains: "sandbox"
    - path: "internal/coreexec/background.go (MOD)"
      provides: "the SAME wrap inside TaskRegistry.Start's exec construction (the config's Handle reaches Start via the registry) — the queued-start path (22-02's FIFO pop) wraps identically, so a background task is confined no matter which start path launched it"
      contains: "sandbox"
    - path: "internal/coreexec/ptty.go (MOD)"
      provides: "PTYOpts.Sandbox *sandbox.Handle; ensureShell routes the shell child through sandbox.WrapCmd before pty.StartWithAttrs — the persistent shell itself is confined for its lifetime; dead-shell lazy restart re-wraps the replacement; sentinel cycle untouched (the sentinel echo is plain text on the master)"
      contains: "Sandbox"
  key_links:
    - from: "cmd/ass-guard/acp_serve.go --sandbox flag"
      to: "internal/acpserve/acp_serve.go startup probe"
      via: "flag → Options.SandboxMode → Run's probe step (before scheduler start, beside the 22-02 sweep) → sandbox.Handle{Policy, Availability} stored once for the process"
      pattern: "startup probe"
    - from: "the three exec sites (bash.go / background.go Start / ptty.go ensureShell)"
      to: "sandbox.WrapCmd (22-05's portable entry)"
      via: "coreexec.Config carries the Handle to the Bash stub and the registry; PTYOpts carries it to the manager; every site calls the ONE WrapCmd — no call site branches on GOOS or re-renders profiles (D-06)"
      pattern: "one entry three callers"
    - from: "cmd/ass-guard/main.go"
      to: "sandbox.ApplySandboxChildHook (22-05's portable hook)"
      via: "unconditional call before root dispatch; linux sentinel env → apply + exec (never returns); every other start path continues untouched"
      pattern: "re-exec sentinel"
  prohibitions:
    - requirement_id: SAND-01
      category: safety
      statement: "A green tool result must never imply confinement that did not happen — with availability false, every unconfined run is individually noted with a counter at EVERY exec site (foreground, background completion prose, persistent result), so the model and operator can never mistake an unconfined run for a confined one"
      status: unresolved
    - requirement_id: SAND-01
      category: safety
      statement: "No exec site may build a tool child that skips the wrap while the sandbox is on — the checker-identified gap class (TaskRegistry.Start and the PTY shell silently unconfined) is closed by routing all three sites through the ONE WrapCmd and pinning each with a proving battery; a fourth exec site added later without WrapCmd is a review-gate violation named in bash.go's wrap comment"
      status: unresolved

---

<objective>
Make the sandbox flag real (SAND-01): --sandbox plumbing (default OFF, =off escape, startup probe-and-degrade loudly before the scheduler), the honored dangerouslyDisableSandbox contract (OQ2), and confinement at ALL THREE Bash-class exec sites — the foreground Bash exec, TaskRegistry.Start's background exec construction, and the PTY persistent-shell spawn — through 22-05's ONE WrapCmd entry, so background and persistent work are confined exactly as foreground work (D-09 orthogonality, D-05 children-only).

Purpose: a sandbox that covers only the foreground exec site is a false boundary — background Bash and the persistent shell would run unconfined under --sandbox=on, silently; the phase prohibition (a green tool result must never imply confinement that did not happen) demands every site wrapped and every wrap proven.
Output: flag + probe pipeline + main-hook + three wrapped exec sites, darwin legs live-tested through the full flag path, linux legs compile-gated, group-kill lifecycle preserved underneath.

Scope rationale (checker r1, warning-class): thirteen declared files is deliberate — the sandbox has exactly THREE exec sites plus one flag surface, and splitting enforcement across plans would scatter the fail-open prohibition's single owner. Beyond the flag quartet (cmd flag, main hook, Options, probe step), every file is a small MOD (a WrapCmd call at an exec site, a Handle field, a PTYOpts field, test batteries); each task stays ≤5 files.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-RESEARCH.md
@.planning/phases/22-background-execution-sandbox-reality/22-PATTERNS.md
@cmd/ass-guard/acp_serve.go
@cmd/ass-guard/main.go
@internal/acpserve/acp_serve.go
@internal/coreexec/bash.go
@internal/coreexec/background.go
@internal/coreexec/ptty.go

The internal/sandbox package surface (WrapCmd, Handle, ApplySandboxChildHook, Availability) is defined by 22-05's artifacts_this_phase_produce — this plan is pure wiring against it.
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/acpserve/options.go` (MOD) — `Options.SandboxMode string`.
- `internal/acpserve/acp_serve.go` (MOD) — `resolveSandboxAvailability(opts) sandbox.Availability` startup probe step in Run's pipeline (before scheduler start; darwin probes sandbox-exec, linux calls ProbeABI, off short-circuits to Availability{Mode:"off", Available:false}); the resolved Handle stored in the composition.
- `cmd/ass-guard/acp_serve.go` (MOD) — `--sandbox` cobra flag (default "off", validated {off,on}).
- `cmd/ass-guard/main.go` (MOD) — the `sandbox.ApplySandboxChildHook()` call at main() entry.
- `internal/coreexec/register.go` (MOD) — Config gains the sandbox Handle plumbed to the Bash stub and to TaskRegistry construction.
- `internal/coreexec/bash.go` (MOD) — the foreground wrap branch; DangerouslyDisableSandbox real semantics (OQ2); the no-op comment retired.
- `internal/coreexec/background.go` (MOD) — the wrap inside Start's exec construction (and the FIFO queued-start path).
- `internal/coreexec/ptty.go` (MOD) — `PTYOpts.Sandbox *sandbox.Handle`; wrapped ensureShell; re-wrap on lazy restart.
- `internal/runtime/runtime.go` (MOD) — Handle flows into Config and PTYOpts at the sessionFor construction site.
- Test additions — serve_test.go (flag + probe pipeline), bash_test.go (TestBashSandbox_*), background_test.go (TestBackgroundSandbox_*), ptty_test.go (TestPTYSandbox_*).
</artifacts_this_phase_produce>

<flagged_assumptions>
- **SAND-01 edge-probe `unclassified` row (status: unresolved — surfaced, never auto-resolved):** this plan's half of the manual review is the FULL-PATH batteries — the Task 1/2/3 live darwin batteries drive curl/touch through the real flag path into each exec site, proving the profile that actually ships denies what the golden claims at every call site; the package-level half is 22-05's. Flagged until the verify phase confirms.
- **OQ2 RESOLVED in-plan (honor the captured contract):** dangerouslyDisableSandbox=true runs the command unconfined with ONE loud stderr note + a counter at every honoring exec site — the captured description promises exactly this and CC parity backs it (RESEARCH Open Question 2 recommendation adopted). The operator enabled the sandbox; the model's explicit per-call escape is visible, never silent.
- **--sandbox vocabulary (discretion):** the flag accepts off|on only (default off); stronger-isolation backends (bubblewrap) stay out of the vocabulary per SAND-01's demotion.
- **Persistent+sandbox composition (D-09 orthogonality — the checker's BLOCKER disposition):** the two switches COMPOSE — the persistent shell is confined at spawn via the same WrapCmd, so persistence never buys an exemption from the sandbox and the sandbox never forbids persistence. Loud refusal was the alternative and is rejected: D-09 locks orthogonality, and refusal would make --sandbox=on silently degrade persistent statefulness for operators who enabled both.
- **Linux runtime proof (22-05's precondition carries here):** the re-exec wrap's runtime behavior is compile-gated on this darwin host; the background/persistent confinement batteries run their live arms on darwin (sandbox-exec) and their linux arms are tagged for a Linux host — enforcement proof lands in the verify phase on an operator/CI Linux host.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --sandbox flag plumbing + startup probe step + sentinel main hook + default OFF</name>
  <files>cmd/ass-guard/acp_serve.go, cmd/ass-guard/main.go, internal/acpserve/options.go, internal/acpserve/acp_serve.go, internal/acpserve/serve_test.go</files>
  <behavior>
    - Default OFF: with no --sandbox flag, SandboxMode is "off" and the probe short-circuits to Availability{Mode:"off", Available:false} — a battery asserts the off path performs no sandbox-exec/ABI probing at all.
    - Flag validation: off/on accepted; any other value ("strict", "yes", "") fails cobra validation at startup with a usage error — never mid-serve.
    - --sandbox=on on this darwin host → the startup probe step runs in Run's pipeline (positioned BEFORE the scheduler-start step, beside the 22-02 sweep) and records Availability once; the ordering row proves probe-before-scheduler.
    - --sandbox=on with a faked probe failure → exactly ONE loud stderr warning at startup, availability marked false, serve continues (degrade loudly, never refuse — the seedACPGuard contract).
    - main() calls sandbox.ApplySandboxChildHook() unconditionally before newRootCmd(); on a normal start it returns false and cobra runs; a unit row asserts the hook is the FIRST call in main (source assertion) and that a sentinel-env process never reaches root dispatch (the hook's own battery in 22-05 covers the apply-and-exec behavior).
  </behavior>
  <read_first>cmd/ass-guard/acp_serve.go :33-115 (newACPServeCmd flags :68-75, runACPServeCmd building acpserve.Options :111), cmd/ass-guard/main.go (main() :37-43 — the hook joins above newRootCmd().Execute()), internal/acpserve/options.go :12-25 (the Options struct gaining SandboxMode), internal/acpserve/acp_serve.go (Run pipeline :121-252 — the probe step joins beside the 22-02 sweep, before scheduler; the degrade doc-comment contract :28-32; seedACPGuard :75-88), 22-05-PLAN.md artifacts_this_phase_produce (Handle, Availability, ApplySandboxChildHook, resolve semantics), 22-RESEARCH.md (Open Question 2, Pitfall 1, State of the Art sandbox-exec row), 22-PATTERNS.md (acp_serve.go assignment)</read_first>
  <action>
RED: serve_test.go gains TestSandboxFlag_* batteries: flag validation rows (off/on ok; "strict" rejected at the cobra layer via cmd/ass-guard's flag validation), pipeline-order row (probe step before scheduler-start step), default-off row (SandboxMode "off", zero probing performed), faked-probe-failure startup row asserting exactly one warning line + availability false + serve continuing. Commit failing.
GREEN: (1) cmd/ass-guard/acp_serve.go: --sandbox StringVar default "off" with cobra validation to {off,on}; pass into Options.SandboxMode. (2) acp_serve.go: resolveSandboxAvailability step in Run's pipeline — off short-circuits to Availability{Mode:"off", Available:false} with no probing; darwin probes sandbox-exec (22-05's first-use probe); linux calls ProbeABI; store the resolved sandbox.Handle{Policy: sandbox.DefaultPolicy(workDir, tmp, guardDir), Availability} in the composition; one stderr warning on enabled-but-unavailable. (3) cmd/ass-guard/main.go: the unconditional ApplySandboxChildHook call as the first statement of main() — before newRootCmd() — with a doc comment naming the D-05 child-only boundary and the never-serves-ACP guarantee. (4) Nothing in this task touches the exec sites — the wrap lands in Tasks 2-3 against this Handle.
Per SAND-01 default OFF; per the plan prohibition the warning is loud and singular at startup (per-run notes are the exec sites' job, Tasks 2-3).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ -run 'TestSandboxFlag|TestStaleSweep' && go build ./... && GOOS=linux CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/acpserve/ -run 'TestSandboxFlag|TestStaleSweep'` exits 0: flag validation (invalid rejected), probe-before-scheduler ordering, default-off zero-probing, one-warning degrade on faked probe failure.
    - `grep -c 'ApplySandboxChildHook' cmd/ass-guard/main.go` is >= 1 and the call precedes newRootCmd in main().
    - `grep -c 'SandboxMode' internal/acpserve/options.go` is >= 1 and `go vet ./internal/acpserve/ ./cmd/ass-guard/` is clean.
    - `go build ./...` and `GOOS=linux CGO_ENABLED=0 go build ./...` exit 0.
  </acceptance_criteria>
  <done>The flag exists and is honest at the composition root: off by default, validated at startup, probed before the scheduler can race it, loud when unsupported, and the sentinel child can never reach the ACP server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Foreground Bash wrap + dangerouslyDisableSandbox honored (OQ2) + escape hatch + group discipline preserved</name>
  <files>internal/coreexec/bash.go, internal/coreexec/bash_test.go, internal/coreexec/register.go</files>
  <behavior>
    - Default OFF: the exec argv for a plain Bash call is byte-identical with the sandbox code present but unavailable (Availability off) — zero behavior change when the operator never asked (battery pins argv identity).
    - --sandbox=on on darwin: a Bash call executes under sandbox-exec with the rendered profile through the FULL flag path (live battery reuses 22-05's curl/touch shape through the executor: network connect fails, outside-write fails, inside-tmp write succeeds).
    - --sandbox=off always escapes even when a Handle with Available:true is present (the escape hatch letter).
    - dangerouslyDisableSandbox=true with sandbox on → the command runs UNCONFINED with one loud stderr note incrementing a counter; with sandbox off it behaves as today's documented no-op (OQ2 resolution, both arms pinned).
    - Availability false (faked) with --sandbox=on → the command runs unconfined WITH a per-run stderr note + counter — never a silent fail-open (the plan prohibition).
    - The wrap substitutes argv BEFORE the existing group discipline: killGroupOnCtx/Setpgid/reapGroup operate on the wrapped child exactly as on the bare sh — a battery asserts a wrapped child still dies via the existing Stop/ctx paths (group semantics preserved).
  </behavior>
  <read_first>internal/coreexec/bash.go :47-71 (bashArgs — DangerouslyDisableSandbox already parses; the :57-64 documented-no-op comment this task retires), internal/coreexec/bash.go :260-310 (the exec path the wrap encloses; killGroupOnCtx discipline; the background branch position), internal/coreexec/register.go :27-67 (Config struct + RegisterCore closure — the Handle joins it), internal/sandbox/policy.go (22-05: WrapCmd substitution-only contract, Handle, Availability), 22-RESEARCH.md (Open Question 2, Pitfall 1), 22-PATTERNS.md (bash.go assignment), 22-CONTEXT.md (D-05, D-09)</read_first>
  <action>
RED: bash_test.go TestBashSandbox_* batteries: default-OFF argv identity (capture the child argv via a seam or sh -c 'printf %s>&2 "$0 $@"' style probe); darwin live wrap through the executor (curl connect deny, outside-touch EPERM, inside-tmp success — through Config with a Handle built from the flag path, not raw 22-05 calls); escape-hatch row; disable-flag arms (on→unconfined+note+counter, off→no-op unchanged); availability-false row (per-run note present); group-discipline row (wrapped child killed by the existing killGroupOnCtx path). Commit failing.
GREEN: (1) register.go: Config gains the sandbox Handle field plumbed to the Bash stub closure and stored for Task 3's registry/manager consumers. (2) bash.go: at the exec site, if Handle.Availability.Available and not a.DangerouslyDisableSandbox → sandbox.WrapCmd(cmd, Handle.Policy) BEFORE killGroupOnCtx; if disabled-while-on → the loud per-run stderr note + counter then run bare; if availability false while on → run bare with the per-run note + counter (the prohibition); --sandbox=off (Availability Mode off) leaves every path untouched; retire the :57-64 no-op comment and document the OQ2 honored semantics; add a wrap comment naming the three-exec-site contract so a future exec site without WrapCmd is a visible review-gate violation.
Per SAND-01 default OFF and the escape hatch; per D-05 only the child is wrapped; per OQ2 the captured contract is honored loudly; per D-06 the site calls WrapCmd — no GOOS branching here.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/coreexec/ -run 'TestBashSandbox' && go test -race -count=1 ./internal/coreexec/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/coreexec/ -run 'TestBashSandbox'` exits 0 on darwin: default-OFF argv identity; live wrapped call denies network/write and allows the rw tmp through the full flag path; escape hatch; both disable-flag arms (note+counter on, silent no-op off); availability-false per-run note; group-kill reaches the wrapped child.
    - `grep -c 'dangerouslyDisableSandbox' internal/coreexec/bash.go` is >= 2 (parse + honored semantics) and the retired no-op comment is gone (the comment no longer states the flag is ignored — grep for the old no-op phrasing returns 0).
    - `grep -c 'WrapCmd' internal/coreexec/bash.go` is >= 1 — the foreground site goes through the ONE entry.
    - `go test -race -count=1 ./internal/coreexec/` exits 0 (all pre-existing batteries, including 22-04's persistent ones, stay green with the sandbox code present but off).
  </acceptance_criteria>
  <done>The foreground path confines honestly: off is byte-identical, on wraps through the one entry, the escape and disable flags are loud, and the group-kill lifecycle is untouched underneath.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Background + persistent confinement — TaskRegistry.Start and the PTY shell wrap through the same entry (checker BLOCKER, D-09 orthogonality)</name>
  <files>internal/coreexec/background.go, internal/coreexec/background_test.go, internal/coreexec/ptty.go, internal/coreexec/ptty_test.go, internal/runtime/runtime.go</files>
  <behavior>
    - Background Bash under --sandbox=on is CONFINED: a backgrounded curl-style command started via TaskRegistry.Start fails its network connect while the task reports running-then-exited — the proving battery drives the background path (Start's own exec construction at the registry, NOT the foreground site) and proves the network deny, closing the checker-identified silent gap.
    - The queued-start path (22-02's FIFO pop starting a waiter on slot-free) wraps identically — a queued task that starts later is confined exactly as an immediately-started one (one battery drives the queue-then-start shape).
    - Background default-OFF: Start's exec construction is byte-identical to today (the six TestBackground_* batteries and 22-02's ladder/queue batteries stay green untouched).
    - A persistent call under --sandbox=on runs in a CONFINED shell: the PTY shell child is wrapped at ensureShell (sandbox-exec -p <rendered> sh), so a persistent `touch <outside rw set>` fails while `touch <inside tmp>` succeeds and the sentinel cycle completes normally (the sentinel echo is plain stdout text — confinement does not affect it).
    - Dead-shell lazy restart under --sandbox=on re-wraps the replacement shell and the D-08 note still fires exactly once — confinement survives restarts.
    - Lifecycle under confinement: terminateGroup's TERM/KILL escalation kills confined background children (signals are neither FS nor network operations) and the PTY Drain kills the confined shell group; a battery drives Stop on a confined background task to completion.
    - runtime.go passes the Handle into coreexec.Config and PTYOpts at the sessionFor construction site — one composition, all three consumers.
  </behavior>
  <read_first>internal/coreexec/background.go :138-206 (Start's exec construction — the site this task wraps; the queued-start FIFO pop from 22-02 — the second start path), internal/coreexec/background.go Stop/terminateGroup (22-02's ladder — the lifecycle owner the wrap must not disturb), internal/coreexec/ptty.go (22-04: PTYOpts, ensureShell's StartWithAttrs child, the dead-shell restart seam), internal/runtime/runtime.go :1107-1138 (sessionFor construction — where Config and PTYOpts gain the Handle), internal/sandbox/policy.go (22-05: WrapCmd, Handle), 22-RESEARCH.md (Pattern 4, Environment Availability), 22-CONTEXT.md (D-05, D-09), 22-05-PLAN.md + 22-06 Task 2 (the wrap comment naming the three-exec-site contract)</read_first>
  <action>
RED: (1) background_test.go TestBackgroundSandbox_* batteries: under a Handle with Availability on (darwin live), Start a backgrounded curl-style command and assert the connect fails while the task lifecycle (running → exited, notification via 22-01's CompletionHook) is unchanged; the queued-then-started variant via a registry at cap 1 with one running filler task; default-OFF argv identity; Stop on a confined task completes via the ladder. (2) ptty_test.go TestPTYSandbox_* batteries: under the Handle, ensureShell's child carries the wrapped argv (live: persistent touch outside fails, inside tmp succeeds, sentinel cycle completes); restart re-wraps with the note once; default-OFF leaves ensureShell byte-identical (22-04's batteries green untouched). Commit failing.
GREEN: (1) background.go: TaskRegistry gains the Handle (injected at construction — register.go/runtime.go pass it); Start's exec construction calls sandbox.WrapCmd(cmd, Handle.Policy) after the SysProcAttr group setup and before Start, gated on Availability.Available; the FIFO queued-start path pops through the SAME construction so both start routes wrap; the disabled-while-on and availability-false arms carry the loud per-run note + counter (OQ2 parity with Task 2). (2) ptty.go: PTYOpts.Sandbox *sandbox.Handle; ensureShell routes the shell child through sandbox.WrapCmd before pty.StartWithAttrs when Available; the lazy-restart path re-wraps (it re-enters ensureShell — one wrap point, both entries); NoteFn ordering unchanged. (3) runtime.go: thread the Handle from the composition into NewTaskRegistry and PTYOpts at sessionFor.
Per D-09 the persistent+sandbox combination COMPOSES (both switches honored — never refused, never exempt); per D-05 only children are wrapped; per the plan prohibition every unconfined run at these sites is individually noted — a green background or persistent result can never imply confinement that did not happen.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/coreexec/ -run 'TestBackgroundSandbox|TestPTYSandbox' && go test -race -count=1 ./internal/coreexec/ ./internal/runtime/ && GOOS=linux CGO_ENABLED=0 go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/coreexec/ -run 'TestBackgroundSandbox|TestPTYSandbox'` exits 0 on darwin: background curl-style child's connect fails under --sandbox=on (confinement proven through Start's exec construction); queued-start wraps identically; persistent touch outside fails / inside tmp succeeds with a completing sentinel cycle; restart re-wraps with one note; default-OFF rows byte-identical.
    - Stop-on-confined row proves terminateGroup's ladder completes on a wrapped child (the lifecycle owner is undisturbed).
    - `grep -c 'WrapCmd' internal/coreexec/background.go internal/coreexec/ptty.go` totals >= 2 — both sites through the ONE entry; `grep -c 'Sandbox' internal/coreexec/ptty.go` is >= 1 (PTYOpts field).
    - `go test -race -count=1 ./internal/coreexec/ ./internal/runtime/` exits 0 — all 22-01/22-02/22-04 batteries stay green (background notifications, ladder, queue, PTY lifecycle with the Handle present but off).
    - `GOOS=linux CGO_ENABLED=0 go build ./...` exits 0.
  </acceptance_criteria>
  <done>Confinement is uniform: background Bash and the persistent shell are confined exactly as foreground work under --sandbox=on — through the same WrapCmd, proven by network-deny batteries at each site — while default-OFF behavior stays byte-identical and the group-kill lifecycle survives underneath.</done>
  <precondition>Linux runtime proof environment: the linux arms of the background/persistent confinement batteries need a Linux kernel ≥5.13 host or CI to RUN (this darwin host runs the sandbox-exec arms only) — same disposition as 22-05 Task 2's precondition; enforcement proof on Linux lands in the verify phase.</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator flag → availability state | A false-negative probe silently disables the safety control |
| model → Bash/background/persistent command → confined child | Three exec sites build children the sandbox must cover uniformly |
| wrapped child → process-group lifecycle | Confinement must not break the TERM/KILL discipline that owns cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-24 | (accepted) | model-authored command executes anything when sandbox OFF (default) | high | accept | T-8-33 posture unchanged — the model's command IS the product; the sandbox is an operator opt-in bounding the blast radius, not a confirmation tier (SAND-01 default OFF is the operator's locked choice) |
| T-22-26 | Elevation of Privilege | enforcement fail-open: an unconfined run without a note | high | mitigate | Per-run stderr note + counter at EVERY exec site on availability-false and on dangerouslyDisableSandbox (Tasks 2-3 batteries); the startup warning is Task 1's; the plan prohibition names the green-result rule |
| T-22-27 | Elevation of Privilege | an exec site builds a tool child that skips the wrap while the sandbox is on (the checker-identified gap: TaskRegistry.Start / PTY shell silently unconfined) | high | mitigate | All three sites route through the ONE sandbox.WrapCmd entry (D-06); Task 3's network-deny batteries prove confinement through Start's construction and the PTY spawn specifically; bash.go's wrap comment names the three-site contract as a review gate for future exec sites |
| T-22-19 | Information Disclosure | curl-style exfil from a confined tool at any of the three sites | high | mitigate | D-04 network deny applied at every site via WrapCmd; the Task 2/3 live batteries re-pin the deny through the full flag path (package-level pin is 22-05's) |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/coreexec/ ./internal/acpserve/ ./internal/sandbox/ ./internal/runtime/` green (phase quick command subset).
- `GOOS=linux CGO_ENABLED=0 go build ./... && GOOS=linux CGO_ENABLED=0 go vet ./internal/sandbox/ ./internal/coreexec/` — the linux compile gate (runtime landlock confinement needs a Linux host; precondition-marked in Task 3).
- The darwin live batteries (curl connect denied, outside-write denied, inside-write allowed) prove the mechanism through the FULL flag path at the foreground, background, and persistent sites.
- Phase gate (per RESEARCH Sampling Rate): full suite green + compile gates before /gsd-verify-work.
</verification>

<success_criteria>
- Children are confined at ALL THREE exec sites through the one WrapCmd entry; background Bash and the persistent shell are never silently unconfined (the checker BLOCKER closed, D-05, D-09).
- Probe-and-degrade is loud with a three-way taxonomy at startup and per-run notes at every site; default is OFF; --sandbox=off always escapes; dangerouslyDisableSandbox is honored with notes (SAND-01, OQ2).
- The group-kill lifecycle (killGroupOnCtx, terminateGroup, PTY Drain) operates on wrapped children unchanged — confinement never breaks cleanup (D-05).
- The sentinel child never serves ACP; main() hooks it before root dispatch.
- Probe rows assigned to this plan (SAND-01 unclassified full-path half) are authored as live-battery flagged assumptions — zero silent drops.
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-06-SUMMARY.md` when done
</output>
