---
phase: 22-background-execution-sandbox-reality
plan: 03
type: execute
wave: 3
depends_on: ["22-01"]
files_modified:
  - internal/tasks/subagent.go
  - internal/tasks/subagent_test.go
  - internal/session/subagent.go
  - internal/session/session.go
  - internal/session/subagent_test.go
  - internal/runtime/runtime.go
autonomous: true
requirements: [PAR-07]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An Agent tool call with run_in_background=true returns IMMEDIATELY with the discriminated result {status: async_launched, task_id, output_file} while the nested subagent loop keeps running under a serve-lifetime context — the dispatching turn ends, the subagent does not (PAR-07, Pattern 2)"
    - "The foreground dispatch path is unchanged: no run_in_background (or false) behaves byte-identically to today, and a foreground dispatch that finishes inline may report status completed — the discrimination is the status field (PAR-07)"
    - "Subagent progress streams to .ass-guard/outputs/<task_id>.log as produced (text chunks, tool results), so a RUNNING task's output is retrievable by Read mid-flight; a cancelled task's file ends with a killed marker line so the D-02 pointer always resolves (PAR-07 output-retrieval letter, Pitfall 9)"
    - "Cancellation rides the tracker's per-task cancel func — TaskStop-style cancellation kills the nested loop cleanly, appends the killed marker, and fires exactly one kind-tagged notification with the killed exit status through the SAME subsystem as bash (PAR-07, 22-01's tracker)"
    - "Over-cap dispatches queue FIFO via the tracker (D-10, 22-01) and the tool result reports the queued state with the visible note"
    - "Model resolution for the background leg flows through the SAME call site the foreground dispatch uses — when Phase 20's resolver lands, the background leg inherits it by construction; a second resolver is never written (Pattern 7 seam, precondition-marked)"
    - "Ask-class tools reached from a background subagent decline with the 17-D-07 note — a background task has no human to ask, and wake-turn provenance already carries the automation class (OQ3 resolution)"
  artifacts:
    - path: "internal/tasks/subagent.go"
      provides: "the async subagent leg: RegisterAndRun on the 22-01 tracker (queued-or-launch, per-task cancel func stored), serve-ctx nested-loop runner, output-file writer (progressive append + killed marker), completion → Tracker.Complete with KindSubagent"
      contains: "RunBackgroundSubagent"
    - path: "internal/session/subagent.go (MOD)"
      provides: "the dispatch-site branch: parse run_in_background from tc.Input (field already in the captured schema, coretools.json:29-32), DispatchSubagent gains the background mode returning the discriminated result; the nested-loop core is extracted so foreground and background share one implementation"
      contains: "async_launched"
    - path: "internal/session/session.go (MOD)"
      provides: "the :432-470 dispatch site's background branch — render the async_launched JSON tool result via the structured-payload idiom and skip the synchronous wait"
      contains: "run_in_background"
  key_links:
    - from: "internal/session/session.go dispatch site"
      to: "internal/tasks/subagent.go RunBackgroundSubagent"
      via: "runtime wiring hands the runner-level tracker into the session's dispatch path (same construction site 22-01 used); background mode registers, returns, detaches"
      pattern: "tracker injection"
    - from: "internal/tasks/subagent.go nested loop"
      to: "the foreground DispatchSubagent loop core"
      via: "one extracted loop implementation, two lifetimes — turn-scoped ctx (foreground) vs serve-lifetime ctx + tracker cancel (background); the goroutine+resCh shape stays, only the select changes (RESEARCH Anti-Pattern: background never selects on the caller's ctx)"
      pattern: "serve-lifetime ctx"
  prohibitions:
    - requirement_id: PAR-07
      category: safety
      statement: "A background subagent MUST never bypass the ask-class decline — with no human present (17-D-07), any ask-class tool the background task reaches is declined with the explanatory note; background work can never become a silent permission escalation"
      status: unresolved

---

<objective>
Complete PAR-07 on the 22-01 spine: background Agent dispatch with the discriminated async_launched result, progressive output-file retrieval for running tasks, tracker-mediated cancellation with killed markers, the queued state for over-cap dispatches, the precondition-marked Phase-20 model-routing seam, and the 17-D-07 ask-decline parity for background work.

Purpose: the model must be able to launch a subagent and keep working, react to its completion via the wake turn, read its output mid-flight, and cancel it — with routing that inherits Phase 20 by construction.
Output: internal/tasks/subagent.go + the session dispatch branch, green under -race, foreground behavior pinned unchanged.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-RESEARCH.md
@.planning/phases/22-background-execution-sandbox-reality/22-PATTERNS.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-CONTEXT.md
@internal/session/subagent.go
@internal/session/session.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/tasks/subagent.go` (NEW) — `RunBackgroundSubagent(dep SubagentDeps, input BackgroundInput) (queued bool, note string, err error)` registering on the tracker (queued-or-launch, D-10), running the nested loop under a serve-lifetime ctx, teeing progress to `.ass-guard/outputs/<id>.log`, firing Tracker.Complete (KindSubagent) on terminal transitions; `SubagentDeps{Run func(...) (string, error), WorkDir string, Tracker *Tracker, ServeCtx func() context.Context}`.
- `internal/session/subagent.go` (MOD) — `run_in_background` parsing beside agentDefFor/extractSubagentPrompt (:299-340); `DispatchSubagent` refactored to extract `runSubagentLoop(ctx, s, turnID, parentTurnID, prompt, restricted, agentDef) (string, error)` — the shared nested-loop core; background mode delegates to tasks.RunBackgroundSubagent with runSubagentLoop as the Run dep.
- `internal/session/session.go` (MOD) — the :432-470 branch: background tool result is the `{"status":"async_launched","task_id":...,"output_file":...}` JSON payload (queued form adds `"queued":true,"note":...`), appended via the existing boundedToolResult/appendToolResultLoud path.
- Test additions — `internal/tasks/subagent_test.go`, `internal/session/subagent_test.go` (TestDispatchBackground_* batteries).
</artifacts_this_phase_produce>

<flagged_assumptions>
- **PAR-07 edge-probe `empty` row (status: unresolved — surfaced, never auto-resolved):** a subagent that produced zero output still notifies with an empty-string tail and ALL D-02 fields present (never a missing field); zero pending notifications never wakes the model (22-01 owns the no-op drain). Task 2's battery pins the zero-output case; flagged until verification.
- **PAR-07 edge-probe `encoding` row (status: unresolved — surfaced, never auto-resolved):** the notification tail is valid UTF-8 — ANSI-safe pass-through plus the 22-01 rune-boundary truncation governs the tail cut; a provider chunk split mid-rune is buffered whole before appending. Task 2 pins the assertion; flagged until verification.
- **async_launched result vocabulary (CONTEXT discretion, RESEARCH A1):** the discriminated result uses status values async_launched/completed per the PAR-07 letter; the block markup inside the wake turn is 22-01's discretion. CC parity is behavioral (wake + re-invoke), not byte-exact (A1).
- **Phase-20 unlanded routing (Pattern 7):** until 20-03 executes, the background leg resolves models through whatever resolution the foreground dispatch site uses TODAY (the pre-20 path). When 20-03 lands, the shared call site inherits the new resolver with zero changes here. The precondition below makes this checkable.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discriminated background dispatch — async_launched immediately, serve-ctx detach, foreground pinned</name>
  <files>internal/session/subagent.go, internal/session/session.go, internal/tasks/subagent.go, internal/session/subagent_test.go, internal/tasks/subagent_test.go, internal/runtime/runtime.go</files>
  <behavior>
    - Agent tool call with run_in_background=true → the tool result is the JSON object with status "async_launched", an exec_-shaped task_id, and an output_file path under .ass-guard/outputs/ — returned before the subagent's first model call completes (PAR-07, Pattern 2 shape).
    - The nested loop runs under the serve-lifetime ctx: the dispatching turn's ctx can be cancelled AFTER the dispatch returns and the background loop keeps running (RESEARCH Anti-Pattern pinned by test — a turn-scoped ctx would kill it instantly).
    - run_in_background absent or false → the foreground path returns the final text byte-identically to today (battery asserts a foreground dispatch before/after the change produces the same tool-result shape).
    - Over-cap dispatch (tracker cap reached) → the result carries queued=true plus the 22-01 queued note, and the task starts when a slot frees.
    - The registered task carries a per-task cancel func in the tracker; completion fires Tracker.Complete with KindSubagent, exit status from the loop outcome ("0" on success, "error" on error, per the D-02 vocabulary).
  </behavior>
  <read_first>internal/session/subagent.go IN FULL (DispatchSubagent :43-123 — the goroutine+outcome/resCh shape to extract; the ctx.Done select :150-156 that background mode MUST NOT take; recover/AppendError discipline; agentDefFor :299-315, extractSubagentPrompt :317-340 — the best-effort parse idiom), internal/session/session.go :425-475 (isSubagentTool branch, appendToolResultLoud + boundedToolResult), internal/runtime/runtime.go :1107-1138 (where the tracker from 22-01 is constructed — the injection point into the session's dispatch deps), internal/tasks/tracker.go (22-01 RegisterSubagent/queued-note contract), 22-PATTERNS.md (tasks/subagent.go assignment + session.go dispatch-site excerpts), 22-RESEARCH.md (Pattern 2 + Anti-Patterns turn-scoped-ctx + blocking-dispatch), internal/toolcat/coretools.json :29-32 (the Agent schema already advertising run_in_background — no schema change)</read_first>
  <action>
RED: session/subagent_test.go gains TestDispatchBackground_* batteries with the fake-provider harness the subagent tests already use: background dispatch returns async_launched immediately (assert BEFORE letting the fake subagent provider finish — a gate channel in the fake); cancelling the dispatching turn's ctx post-return leaves the background loop alive until it completes naturally and the notification fires; foreground parity case; queued case via a tracker constructed at cap 1. Commit failing.
GREEN: (1) Extract the nested-loop body of DispatchSubagent into runSubagentLoop(...) keeping the recover/AppendError discipline inside — foreground DispatchSubagent calls it under the caller's ctx with the existing select; zero behavior change (the parity battery pins this before anything else moves). (2) Create tasks/subagent.go with SubagentDeps + RunBackgroundSubagent: RegisterSubagent (queued → return the queued form without starting; the tracker invokes the start func on a slot freeing), on start spawn the goroutine calling runSubagentLoop under a ctx derived from the serve-lifetime context (the runtime.go writerCtx :1229 discipline — serveCtxOrBackground for test tolerance), store the cancel func in the tracker, tee loop progress to the output file (Task 2 owns the writer — this task creates the file with a header line so the pointer resolves from t=0), and fire Tracker.Complete on every terminal path (success/error/panic-recover). (3) session.go :432-470: parse run_in_background via the anonymous-struct best-effort idiom; background branch builds the async_launched payload (queued variant adds queued+note) and appends it as the tool result, skipping the synchronous wait; wire the tracker into the dispatch deps at the runtime.go construction site. runtime.go is declared in this task's files and the plan runs in Wave 3 — after 22-02, the other runtime.go editor, completes — so construction-site edits never race a same-wave plan.
Per PAR-07 the discrimination is the status field; per D-10 over-cap queues rather than failing; per Pattern 7 the loop-core extraction is what makes the Phase-20 inheritance mechanical (one call site).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run 'TestDispatchBackground' && go test -race -count=1 ./internal/tasks/ ./internal/session/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/session/ -run 'TestDispatchBackground'` exits 0: async_launched returns before subagent completion; post-dispatch turn-ctx cancellation does not kill the background loop (it completes and notifies); the queued case starts on slot-free.
    - The foreground parity case asserts the non-background tool result is unchanged (same final-text shape as the pre-change battery).
    - `grep -c 'async_launched' internal/session/subagent.go internal/session/session.go` totals >= 2 (render + branch).
    - `go test -race -count=1 ./internal/tasks/ ./internal/session/` exits 0 (whole packages; no foreground regression).
  </acceptance_criteria>
  <done>The model can launch a subagent in the background and keep working: immediate discriminated result, detached serve-lifetime execution, queued-when-busy, and completion firing the 22-01 notification spine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Output-file streaming + cancellation with killed markers (PAR-07 retrieval + cancel letters)</name>
  <files>internal/tasks/subagent.go, internal/tasks/subagent_test.go, internal/session/subagent_test.go</files>
  <behavior>
    - While the background subagent is RUNNING, its output file at .ass-guard/outputs/<task_id>.log is non-empty and grows with progress (text chunks and tool-result lines append as produced) — mid-run Read resolves (PAR-07 retrieval letter, Pitfall 9 warning-sign case).
    - Cancellation via the tracker's per-task cancel func: the nested loop's ctx cancels, the loop winds down cleanly (no panic leak), a killed marker line is appended to the output file, and exactly ONE notification with exit status "killed" fires through the subsystem.
    - Cancel racing natural completion → exactly one terminal notification (dedupe owns the race — 22-01) and the file ends with whichever marker reflects the winner, never both.
    - A subagent that produced zero output still leaves a valid file (header line from Task 1) and notifies with tail "" and all D-02 fields present (PAR-07 empty probe).
    - Chunks split mid-UTF-8-rune are buffered to rune boundaries before the file append — the file is always valid UTF-8 (PAR-07 encoding probe; tail truncation stays 22-01's).
  </behavior>
  <read_first>internal/tasks/subagent.go (Task 1 skeleton — the writer seam this task fills), internal/coreexec/background.go :120-136 (openTaskLog + io.MultiWriter tee — the bash leg's log discipline to mirror; the id is hex-minted, not model input), internal/tasks/notify.go (22-01 dedupe — the cancel-race owner), 22-RESEARCH.md (Pitfall 9 output-file lifecycle — the full case list), 22-PATTERNS.md (tasks/subagent.go output-streaming excerpt), internal/session/transcript.go (the Append* signatures the tee mirrors for chunk/tool-result lines)</read_first>
  <action>
RED: tasks/subagent_test.go battery with a controllable fake Run dep: mid-run file non-empty and growing (assert after N progress callbacks); cancel → loop exits, killed marker present, one killed notification; zero-output case (Run returns immediately with "") → file has the header, notification tail is empty with all fields set; a Run dep that emits a chunk split inside a multi-byte rune → the file content around the split is valid UTF-8 (utf8.Valid on the whole file). Commit failing.
GREEN: (1) Implement the output-file writer in tasks/subagent.go: create the file at registration (header: task id, kind, agent name, start timestamp), append progress lines via a mutex-guarded writer, flush per line (the bash tee discipline — no buffering delay so mid-run Read is truthful). (2) Wire cancellation: the tracker cancel func cancels the loop ctx; the loop's terminal handler appends the killed marker line ONLY when the loop ended via cancellation (natural completion appends a completed marker instead — one marker per file, the race winner). (3) Fire Tracker.Complete after the marker write so the tail read from the bounded buffer snapshot includes the marker (D-02 tail correctness). (4) Rune-boundary buffering for chunk text before append. Per PAR-07 the pointer never dangles: every terminal path writes its marker before the notification fires.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/tasks/ -run 'TestBackgroundOutput|TestBackgroundCancel' && go test -race -count=1 ./internal/tasks/ ./internal/session/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/tasks/ -run 'TestBackgroundOutput|TestBackgroundCancel'` exits 0: mid-run retrieval non-empty; killed marker + exactly-one killed notification on cancel; zero-output case tail is empty with all fields present; split-rune case leaves utf8.Valid(file) true.
    - The cancel-race case asserts exactly one terminal notification and exactly one marker line in the file (grep-count of the marker string in the test is 1).
    - `grep -c 'killed' internal/tasks/subagent.go` is >= 1 and `go vet ./internal/tasks/` is clean.
    - `go test -race -count=1 ./internal/tasks/ ./internal/session/` exits 0.
  </acceptance_criteria>
  <done>Output is retrievable for running tasks, cancellation kills cleanly with a resolvable file pointer, and every terminal path notifies exactly once through the shared subsystem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase-20 routing seam (precondition-marked) + 17-D-07 ask-decline parity (OQ3)</name>
  <files>internal/session/subagent.go, internal/session/subagent_test.go</files>
  <behavior>
    - The background leg's model input (the Agent schema's model field) flows into the SAME resolution call the foreground runSubagentLoop path makes — a battery asserts both modes produce identical provider model selection for identical dispatch input (the seam: one resolver, two lifetimes).
    - If the Phase-20 resolver has landed (20-03 executed), precedence frontmatter > dispatch > parent and resolvedModel reporting hold for background dispatches exactly as for foreground ones — the shared call site inherits it with zero background-specific code.
    - An ask-class tool invoked inside a background subagent (or its own nested calls) is declined with the 17-D-07 note — no permission prompt, the decline reason names the background/automation context; the subagent sees the decline as a tool result and continues (decline, not crash).
  </behavior>
  <read_first>.planning/phases/20-built-in-commands-skills-per-agent-model/20-CONTEXT.md (D-13..D-16 — the locked routing contract this seam rides), 22-RESEARCH.md (Pattern 7 — the phase-20 dependency seam; the coretools.json:20-28 model-field note), internal/session/subagent.go (the dispatch-time model input path + the resolution call site), internal/session/ask.go + the 17-D-07 automation-decline machinery (wake turns carry automation-class provenance — 22-01 Task 1), 21-06-PLAN.md (the precondition-mark pattern this task mirrors for a planned-not-executed dependency)</read_first>
  <action>
Implement the seam as a pin, not a feature: (1) Trace where runSubagentLoop resolves the model for its provider calls and add an explicit internal seam comment naming the Phase-20 delegation: background dispatch resolves through this exact call site; when 20-03's resolver lands, both modes inherit it — never fork a second resolver (Pattern 7). No new resolution logic is written in this plan. (2) RED: add a TestDispatchBackgroundRouting battery asserting foreground and background dispatches with the same model input reach the fake provider with the same resolved model (whatever the current path resolves — the assertion is EQUALITY between modes, not a specific model value, so it survives 20-03 landing). (3) RED: add TestBackgroundAskDecline: a fake subagent provider whose tool call targets an ask-class tool inside a background task observes a declined tool result carrying the 17-D-07 note (no pending ask surfaces to any client); GREEN: if the decline does not already fall out of the wake-turn provenance, extend the background task's ask classification to carry the automation class for the duration of the background loop (the same mechanism 17-D-07 uses for automation turns — one classification, not a second gate). (4) If the Phase-20 contract shape has LANDED differently than 20-CONTEXT.md describes (renamed resolver, moved precedence), STOP and report — remedy is a plan revision, never a shim (the 21-06 rule).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run 'TestDispatchBackgroundRouting|TestBackgroundAskDecline'</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/session/ -run 'TestDispatchBackgroundRouting|TestBackgroundAskDecline'` exits 0: mode-equality of resolved model pinned; ask-class call inside a background task yields the declined tool result with the 17-D-07 note and no ask surfaces to the client.
    - `grep -c '20-03\|Phase-20' internal/session/subagent.go` is >= 1 — the delegation-seam comment exists at the resolution call site (Pattern 7 precondition mark).
    - No second resolver is introduced: `grep -c 'resolveSubagentModel\|ResolveModel\|resolveModel' internal/tasks/` is 0 (resolution logic never enters the tasks package).
    - `go test -race -count=1 ./internal/session/` exits 0 (whole package; foreground routing untouched).
  </acceptance_criteria>
  <done>Background dispatches resolve models through the foreground's single call site (Phase-20 inheritance is mechanical, precondition-marked), and background work can never escalate permissions — asks decline with the note, per OQ3's adopted resolution.</done>
  <precondition>Phase-20 seam sanity (Pattern 7): the foreground dispatch path resolves its model through exactly ONE existing call site — assert with grep that internal/session/subagent.go contains the resolution call the background leg will share; if the site is absent or split across two resolvers, HALT and report (plan revision needed), never bolt a parallel resolver.</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagent tool output → output file → model Read | Untrusted nested-agent output lands in a file the parent model later reads |
| tracker registration → task lifecycle | The model names tasks it learned from results; registration ids are minted, not chosen |
| background execution → permission surface | No human is present when a background subagent runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Elevation of Privilege | ask-class tools from background subagents | high | mitigate | 17-D-07 decline-with-note parity (Task 3, OQ3 resolution) — background work has no human; the decline is the permission decision (plan prohibition) |
| T-22-10 | Information Disclosure | output files readable across sessions | medium | mitigate | Files live under the session workdir's .ass-guard/outputs/ with crypto/rand hex ids (22-01 prohibition); scope is the operator's own workdir — same exposure class as the existing bash logs |
| T-22-11 | Denial of Service | unbounded background subagent fan-out | medium | mitigate | D-10 cap (8) + bounded FIFO queue via the 22-01 tracker; queued form keeps the model informed rather than failing |
| T-22-12 | Tampering | turn-scoped lifecycle mistaken for serve-lifetime | high | mitigate | The serve-lifetime ctx discipline (runtime.go writerCtx pattern) is pinned by the Task 1 battery that cancels the dispatching turn post-return and asserts survival — the RESEARCH Anti-Pattern is a regression test |
| T-22-13 | Repudiation | background completion with no audit trail | low | mitigate | Every terminal transition fires Tracker.Complete → the 22-01 wake EngineDecision line; the output file's markers record the terminal mode (killed vs completed) |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/session/ ./internal/tasks/` green (phase quick command subset).
- Foreground dispatch parity battery proves the synchronous path is byte-identical (PAR-07's completed/async_launched discrimination is additive).
- The seam-equality battery survives Phase 20 landing (equality assertion, not a pinned model value).
</verification>

<success_criteria>
- run_in_background=true dispatch returns async_launched immediately, runs under a serve-lifetime ctx, notifies on completion through the 22-01 spine, and cancels cleanly with killed markers (PAR-07).
- Output files are retrievable mid-run and never dangle (Pitfall 9 closed).
- Over-cap dispatches queue with the visible note (D-10); model routing rides the single shared call site (Pattern 7, precondition-marked); asks decline per 17-D-07 (OQ3).
- Probe rows assigned to this plan (PAR-07 empty; PAR-07 encoding) are authored as behaviors/flagged assumptions — zero silent drops.
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-03-SUMMARY.md` when done
</output>
