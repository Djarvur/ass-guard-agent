---
phase: 22-background-execution-sandbox-reality
plan: 09
type: execute
wave: 8
depends_on: ["22-08"]
files_modified:
  - internal/coreexec/background.go
  - internal/coreexec/planmode.go
  - internal/coreexec/background_test.go
  - internal/tasks/tracker.go
  - internal/tasks/tracker_test.go
  - internal/runtime/runtime.go
  - internal/runtime/wake_wiring_test.go
autonomous: true
gap_closure: true
gap_ids: [G-22-5]
requirements: [PAR-07]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low # estimate-calibration has 0 samples for gap-closure tasks (factor 1, not applied); modeled on the pre-split 22-07 projection (60k raw / 5 tasks), this cluster was the single heaviest task and splits into seam + classifier/wiring rows

must_haves:
  truths:
    - "G-22-5 (WR-01, PAR-07 cancellation): TaskStop/TaskOutput address background-subagent task ids — ids unknown to the TaskRegistry fall through to the tracker seam: TaskStop cancels the subagent (Tracker.CancelTask gains production callers), TaskOutput renders the captured not_ready (queued/running) or ready (output-file content) envelope, and a FINISHED subagent classifies as finished (not running) because 22-07's Complete deleted its cancel entry; the coretools.json promise 'Works with all task types' is no longer false for async agents"
  artifacts:
    - "internal/coreexec/planmode.go + background.go — InteractiveConfig gains TaskStopFallback func(id string) bool and TaskOutputFallback func(id string) (output, state string, running, handled bool) (primitive-arg seam, no coreexec→tasks import); TaskStopExecute/TaskOutputExecute fall through to them on unknown-to-registry ids"
    - "internal/tasks/tracker.go — SubagentState(id) queued/running classifier whose running signal is TRUTHFUL for finished ids (22-07's Complete-side subagentCancels delete is what disambiguates finished from running — no delete path existed when the closure was first sketched)"
    - "internal/runtime/runtime.go — sessionFor's RegisterInteractive literal carries the two fallback seams bound to this session's tracker (TaskStopFallback: tracker.CancelTask; TaskOutputFallback: the SubagentState + output-file closure)"
  key_links:
    - "sessionFor tracker → InteractiveConfig.TaskStopFallback/TaskOutputFallback → TaskStopExecute/TaskOutputExecute unknown-id fallthrough → Tracker.CancelTask / SubagentState + output file (G-22-5)"

---

<objective>
Close Phase 22 verification gap G-22-5 (22-VERIFICATION.md PAR-07 row's third cancellation break, review id WR-01) — the coreexec-seam cluster of the split gap-closure chain (22-07 tasks cluster → 22-08 cron_wiring/runtime lifecycle → THIS PLAN). Today TaskStop/TaskOutput consult only the TaskRegistry: background-subagent ids (exec_* minted by the tracker path) are structurally unknown there, so `Tracker.CancelTask` has zero production callers and the model cannot stop or read a background subagent through the tool surface using the id the async_launched payload handed it.

Purpose: PAR-07's model-facing cancellation letter becomes reachable end to end, and TaskOutput's finished leg becomes truthful — the original closure sketch classified finished subagents as running forever because subagentCancels had no delete path; 22-07's Complete-side delete (TestTrackerComplete_RetiresCancelEntry) is the disambiguation substrate this plan's classifier consumes.
Output: coreexec stub rows (ack/queued/running/finished/not-handled), the tracker classifier battery, and two runtime wiring rows — all red against the current code and green after the fixes; `go test -race ./internal/coreexec/ ./internal/tasks/ ./internal/runtime/` fully green (this plan owns the closure round's final three-package gate).

Gap mapping: G-22-5=WR-01 TaskStop/TaskOutput reach subagent ids. (G-22-1/G-22-3 were 22-07's; G-22-2/G-22-4 were 22-08's.)

Scope guard: unchanged from 22-07's objective (WR-02..WR-08, IN-01/IN-02/IN-04 stay deferred review debt).
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-VERIFICATION.md
@.planning/phases/22-background-execution-sandbox-reality/22-REVIEW.md
@.planning/phases/22-background-execution-sandbox-reality/22-07-SUMMARY.md
@.planning/phases/22-background-execution-sandbox-reality/22-08-SUMMARY.md
@internal/tasks/tracker.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (G-22-5, seam): InteractiveConfig fallback fields + TaskStop/TaskOutput unknown-id fallthrough in coreexec</name>
  <files>internal/coreexec/planmode.go, internal/coreexec/background.go, internal/coreexec/background_test.go</files>
  <behavior>
    - TaskStop on a subagent id: with the registry NOT knowing the id and the fallback returning true, the stub renders the captured ack ("Task <id> stopped."); fallback false renders the existing structured unknown-task error unchanged.
    - TaskOutput on a subagent id (registry unknown, fallback handled): queued → the captured not_ready envelope with status queued; running → not_ready with status running; finished → the ready envelope whose output section carries the fallback's output content; not handled anywhere → the existing structured unknown-task error.
    - block/timeout are documented as ignored for fallback ids (render the current state; the schema's promise is that the id is ADDRESSABLE — the current lie is "unknown task").
    - Registry-owned ids are untouched: the fallthrough runs ONLY on the registry's unknown-task error (existing TaskOutput/TaskStop rows stay green byte-identically).
  </behavior>
  <read_first>internal/coreexec/background.go :690-742 (TaskOutputExecute — r.Output err path :713-716 is the fallthrough point; the captured not_ready/ready envelope shapes) and :754-795 (TaskStopExecute — r.Stop err path :783-786), internal/coreexec/planmode.go :100-140 (InteractiveConfig struct + the stubs map binding cfg.Tasks at :131/:136 — where the two new fields join), internal/coreexec/background_test.go (existing TaskStop/TaskOutput rows — the marshalStructured/not_ready assertion helpers)</read_first>
  <action>
RED: add TaskStopExecute/TaskOutputExecute rows to background_test.go with a stub registry (unknown id) plus fake fallbacks covering ack/true, queued, running, finished, and not-handled; commit failing (no fallback parameter exists today — the constructors do not compile with the new args, so write the rows against the TARGET signatures and let the build failure be the RED evidence, or introduce the params first with nil-returning defaults and let the assertions fail — either order keeps one failing commit).
GREEN: (1) planmode.go InteractiveConfig: add TaskStopFallback func(id string) bool and TaskOutputFallback func(id string) (output, state string, running, handled bool) — primitive-arg seam, no coreexec→tasks import (the :2017 CompletionHook precedent); bind them into TaskStopExecute(cfg.Tasks, cfg.TaskStopFallback) and TaskOutputExecute(cfg.Tasks, cfg.TaskOutputFallback) at the stubs map. (2) background.go: extend both constructors with the fallback param (internal callers: planmode.go only); TaskStopExecute — when r.Stop returns the unknown-task error and fallback != nil and fallback(id), render the same captured ack; TaskOutputExecute — when r.Output returns the unknown-task error and the fallback reports handled, render the same not_ready (queued/running) or ready (finished) envelope shapes with the fallback's output. Document the block-ignored choice in the stub comment.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/coreexec/ -run 'TestTaskStop|TestTaskOutput' && go test -race -count=1 ./internal/coreexec/</automated>
  </verify>
  <acceptance_criteria>
    - All new coreexec rows green under the command above; the existing TaskStop/TaskOutput rows stay green byte-identically.
    - `grep -c 'TaskStopFallback' internal/coreexec/planmode.go` is >= 2 (field + binding) and `grep -c 'TaskOutputFallback' internal/coreexec/planmode.go` is >= 2.
    - `grep -n 'internal/tasks' internal/coreexec/background.go internal/coreexec/planmode.go` finds nothing (the seam stays primitive-arg).
    - `go vet ./internal/coreexec/` clean.
  </acceptance_criteria>
  <done>The tool surface can address tracker-owned ids through two primitive-arg fallback seams — no coreexec dependency on internal/tasks, registry behavior untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (G-22-5, wiring): truthful SubagentState classifier + sessionFor binding — finished reads the output file, running stops for real</name>
  <files>internal/tasks/tracker.go, internal/tasks/tracker_test.go, internal/runtime/runtime.go, internal/runtime/wake_wiring_test.go</files>
  <precondition>22-07 landed — `grep -c 'delete(t.subagentCancels' internal/tasks/tracker.go` is 1 (Complete retires cancel entries for KindSubagent; without it the finished rows below cannot pass: a finished subagent would classify as running forever).</precondition>
  <behavior>
    - Tracker classifier, four table rows: SubagentState(id) reports (queued=true) for a waiter; (running=true) for an admitted-and-cancel-registered id; NEITHER for a completed id (the cancel entry was deleted at Complete — 22-07 — this is the disambiguation row); neither for a never-registered id.
    - TaskStop cancels for real through the runtime wiring: a background subagent registered on a session's tracker (blocking Run), the session catalog's TaskStop invoked with its exec_ id → the registered cancel func fires (channel signal).
    - TaskOutput reads a finished subagent end to end: register a subagent on the session's tracker, let it Complete (output file written), then the session catalog's TaskOutput with its id → the ready envelope whose output section carries the output-file content (bounded read — last 64 KiB — documented in the stub comment). This row is the classifier-fix regression: the pre-fix closure rendered not_ready forever because finished classified as running.
    - TaskStop on a finished id renders the structured unknown-task error (CancelTask reports false — the cancel entry was retired at Complete; there is nothing left to stop, and the error is truthful rather than a hang or a fake ack).
    - The fake-provider call count stays zero in both wiring rows (a stop or a read must not wake turns).
  </behavior>
  <read_first>internal/tasks/tracker.go (locate by symbol after 22-07's edits: RegisterSubagent / startNextWaiter populate subagentCancels; releaseSubagentSlot deletes it; CancelTask, Complete, waiters), internal/tasks/subagent.go :54-56 (the output-file path convention .ass-guard/outputs/<id>.log under WorkDir), internal/runtime/runtime.go :2111-2130 (the RegisterInteractive literal inside sessionFor — holds both taskRegistry and tracker in scope; line numbers may have shifted after 22-08's CloseSession/OnClose edits — locate by the RegisterInteractive symbol), internal/coreexec/planmode.go (the two fallback fields Task 1 added), internal/runtime/wake_wiring_test.go :109-207 (the runner/session harness for the wiring rows — same serialization rule as 22-08: no t.Parallel() in the new rows)</read_first>
  <action>
RED: (1) tracker_test.go: TestTrackerSubagentState four rows (queued waiter / running admitted / finished completed / unknown never-registered). (2) wake_wiring_test.go: the two wiring rows (end-to-end stop of a live background subagent by exec_ id; end-to-end finished TaskOutput carrying the output-file content). Commit failing (the classifier's finished row and the finished-output wiring row fail until the delete from 22-07 combines with SubagentState; today neither SubagentState nor the binding exists).
GREEN: (1) tracker.go: SubagentState(id string) (queued, running bool) — waiters scan under t.mu, subagentCancels lookup (truthful for finished ids BECAUSE 22-07's Complete deleted completed entries — cite TestTrackerComplete_RetiresCancelEntry in the doc comment). (2) runtime.go sessionFor RegisterInteractive literal: TaskStopFallback: tracker.CancelTask; TaskOutputFallback: closure classifying via tracker.SubagentState (queued → ("", "queued", true, true); running → ("", "running", true, true)) else reading filepath.Join(dir, ".ass-guard", "outputs", id+".log") with a bounded io.LimitReader-equivalent tail read (found → (content, "finished", false, true); stat-miss → handled=false). Document the block-ignored choice in the stub comment.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/tasks/ -run 'TestTrackerSubagentState' && go test -race -count=1 ./internal/runtime/ -run 'TestTaskStopFallbackWiring|TestTaskOutputFallbackWiring' && go test -race -count=1 ./internal/coreexec/ ./internal/tasks/ ./internal/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - All new rows green under the commands above; the fake-provider call count in both wiring rows stays zero.
    - The finished classifier row asserts SubagentState reports neither queued nor running for a completed id, and the finished-output wiring row asserts the ready envelope carries the output-file content.
    - `grep -c 'func (t \*Tracker) SubagentState' internal/tasks/tracker.go` is 1; `grep -c 'TaskStopFallback: tracker.CancelTask' internal/runtime/runtime.go` is 1.
    - The full three-package battery exits 0 — every 22-01..22-08 row plus this plan's rows green together; `go vet` on all three packages clean.
  </acceptance_criteria>
  <done>The model can stop and read a background subagent through the tool surface using the exact id the async_launched payload handed it — Tracker.CancelTask has production callers, finished means finished (output readable), and PAR-07's cancellation letter is reachable end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → task ids (TaskStop/TaskOutput) | Model-supplied ids resolve to live tasks; unknown ids must stay structured errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-23 | Denial of Service | background.go TaskStop/TaskOutput | medium | mitigate | Tasks 1+2: unknown-id fallthrough to the tracker seam — model-facing cancellation of runaway background subagents becomes reachable; ids the seam does not know keep the structured error (no id-guessing surface added — the ids remain session-local crypto/rand values, and a finished id truthfully errors rather than acking) |

</threat_model>

<verification>
- `go test -race -count=1 ./internal/tasks/ ./internal/runtime/ ./internal/coreexec/` green — the closure round's FINAL three-package gate: the new seam/classifier/wiring rows AND every existing 22-01..22-08 row (the fixes must be strictly additive on paths the green batteries already prove).
- `go vet ./internal/tasks/ ./internal/runtime/ ./internal/coreexec/` clean.
- `GOOS=linux CGO_ENABLED=0 go build ./...` stays green (no new deps).
- Re-verification mapping: G-22-5→TaskStop/TaskOutput fallthrough rows + TestTrackerSubagentState (four rows, finished disambiguated) + the two runtime wiring rows (end-to-end stop; end-to-end finished output read).
</verification>

<success_criteria>
- The G-22-5 gap truth holds, pinned by tests that fail against the pre-fix code — including the finished-disambiguation row that the original single-task sketch could not pass (subagentCancels had no delete path; 22-07's Complete-side delete makes the classifier truthful).
- No behavior change on any path the existing batteries pin: registry-owned TaskStop/TaskOutput rows byte-stable, wake-turn counters untouched by the fallback path.
- Zero scope creep: WR-02..WR-08 and IN-01/IN-02/IN-04 untouched (deferred review debt, see 22-07's scope guard).
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-09-SUMMARY.md` when done
</output>
