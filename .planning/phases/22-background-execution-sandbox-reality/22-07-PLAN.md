---
phase: 22-background-execution-sandbox-reality
plan: 07
type: execute
wave: 6
depends_on: ["22-06"]
files_modified:
  - internal/tasks/tracker.go
  - internal/tasks/tracker_test.go
  - internal/tasks/subagent.go
  - internal/tasks/subagent_test.go
autonomous: true
gap_closure: true
gap_ids: [G-22-1, G-22-3]
requirements: [PAR-07]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low # estimate-calibration has 0 samples for gap-closure tasks (factor 1, not applied); modeled on the pre-split 22-07 projection (60k raw / 5 tasks) x 2 tasks + per-plan context overhead

must_haves:
  truths:
    - "G-22-1 (CR-01, D-10): background-subagent slots are RELEASED on completion — after SubagentCap (default 8) TOTAL starts in a session (sequential or concurrent), RegisterSubagent still admits directly (queued==false); the over-cap FIFO queue drains as slots actually free, so a dispatch told 'it starts when a slot frees' really starts"
    - "G-22-3 (CR-03, D-13 invariant): a panicking background subagent NEVER kills the ass-guard process — the launcher goroutine mirrors the foreground recover() (session/subagent.go:105-126), writes the terminal error marker to the output file, fires exactly ONE error notification (Kind=KindSubagent, ExitStatus error), and releases its slot"
    - "Finished subagents stop looking running: Complete retires the completing subagent's cancel registration (subagentCancels delete) — no delete path exists today (tracker.go:172 and :212 are the only populate sites), and 22-09's TaskOutput classifier depends on this to tell finished from running"
  artifacts:
    - "internal/tasks/tracker.go — Complete handles Kind==KindSubagent via a releaseSubagentSlot(n.TaskID) helper BEFORE startNextWaiter: under t.mu, decrement runningSubagents floored at zero AND delete(t.subagentCancels, n.TaskID)"
    - "internal/tasks/subagent.go — recover() at the launcher goroutine boundary mirroring the foreground D-13 leg: w.finish(\"error\", panic text + stack) + Tracker.Complete with ExitStatus error"
  key_links:
    - "Tracker.Complete (Kind==KindSubagent) → releaseSubagentSlot (floored decrement + cancel-entry delete) → startNextWaiter — a completion frees the slot the waiter reoccupies and retires the cancel entry (G-22-1; the finished-vs-running signal 22-09's SubagentState consumes)"

---

<objective>
Close Phase 22 verification gaps G-22-1 and G-22-3 (22-VERIFICATION.md frontmatter `gaps:` 1 and 3, review ids CR-01/CR-03) — the two `internal/tasks`-cluster gaps of the five-gap closure round. This is plan 1 of the split gap-closure chain: 22-07 (this plan, tasks cluster) → 22-08 (cron_wiring/runtime lifecycle cluster, G-22-2 + G-22-4) → 22-09 (coreexec TaskStop/TaskOutput seam, G-22-5). Both of this plan's gaps are terminal paths of the delivered background-notification machinery that the existing green batteries never exercise: none re-registers after completions (the cap counter is monotonic) and none panics inside the background goroutine.

Purpose: each gap falsifies a delivered must-have — D-10 queue draining (PAR-07) and the D-13 panic invariant the foreground leg already delivers (PAR-07). The cancel-entry delete additionally lays the substrate 22-09's classifier needs (finished ≠ running).
Output: three regression batteries in internal/tasks, all red against the current code and green after the fixes; `go test -race ./internal/tasks/` fully green with the new rows.

Gap mapping: G-22-1=CR-01 slot decrement + cancel-entry retirement, G-22-3=CR-03 panic recovery. (G-22-2/G-22-4 are 22-08's; G-22-5 is 22-09's.)

Scope guard (do NOT expand, applies to the whole closure round 22-07..22-09): WR-02 (recycled-pgid Stop), WR-03 (sentinel swallowed by stdin-readers), WR-04 (sweep tombstones bash-*.log), WR-05 (shared /tmp RW), WR-06 (seatbelt quote escaping), WR-07 (dropped-waiter fd leak), WR-08 (OSC bare backslash), IN-01/IN-02/IN-04 stay deferred review debt. IN-03's tracker/wakeInFlight(/ptyManagers) pruning is IN scope in 22-08 only because CR-04's missing list names it as the gap's substrate. IN-05's unbounded retry retires incidentally in 22-08 (the non-constructing lookup removes the only construction-failure retry path), no separate work.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-VERIFICATION.md
@.planning/phases/22-background-execution-sandbox-reality/22-REVIEW.md
@internal/tasks/tracker.go
@internal/tasks/subagent.go
@internal/session/subagent.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (G-22-1): tracker slot release + cancel-entry retirement — completions actually free D-10 slots (CR-01)</name>
  <files>internal/tasks/tracker.go, internal/tasks/tracker_test.go</files>
  <behavior>
    - Re-register after completions: register SubagentCap (use cap 2 in TrackerOpts) subagents, Complete ALL of them (Kind=KindSubagent), then register once more — queued==false, err==nil (starts immediately). This exact row is the blind spot of TestTrackerQueue_FIFODrainOrder.
    - After all completions with empty waiters, the internal count is zero: same-package assert t.runningSubagents == 0.
    - Sequential-use regression: a loop of 3x-cap register→complete cycles (cap 2) never queues — every registration admits directly.
    - FIFO handoff still correct: cap 2, register 3 (third queued==true with position note), Complete the first → the third's start func runs (observable via a channel) and the count never exceeds cap (same-package assert under 2+ concurrency).
    - Floor safety: a Complete for a KindSubagent arriving with no matching admission (defensive row) leaves runningSubagents at 0, never negative.
    - KindBash completions never touch the subagent count (regression row).
    - Complete retires the completing subagent's cancel registration: after Complete(id, KindSubagent), a same-package lookup of t.subagentCancels lacks the id — no delete path exists today (the map is populated only at RegisterSubagent :172 and startNextWaiter :212), so finished subagents currently look running forever.
    - CancelTask on a completed id reports false (the stored cancel is gone — idempotent no-op; the truthful not-running signal 22-09's TaskStop/TaskOutput seam consumes).
  </behavior>
  <read_first>internal/tasks/tracker.go IN FULL (Complete :115-133, RegisterSubagent :160-189 admission check :163, startNextWaiter :194-214 with its :205 increment — the counter's ONLY movements today are the two ++ sites; subagentCancels populate sites :172/:212, CancelTask :218-228), internal/tasks/tracker_test.go :59-160 (TestTrackerCap_UnderCapStartsImmediately, TestTrackerQueue_FIFODrainOrder — the harness helpers: start funcs returning channel-signalling cancels, same-package field access), 22-REVIEW.md CR-01 (the suggested releaseSubagentSlot shape)</read_first>
  <action>
RED: add TestTrackerCap_SlotsFreeAfterCompletion, TestTrackerCap_SequentialReuseNeverQueues, and TestTrackerComplete_RetiresCancelEntry to tracker_test.go with the rows above; commit failing (the re-register rows fail today: after 2 total starts the admission check is permanently false and the re-register queues forever — use a bounded WaitTimeout on the start channel so the failure is an assertion, not a hang; the cancel-entry rows fail today: the map still holds the id after Complete).
GREEN: in Complete, for n.Kind == KindSubagent call a new releaseSubagentSlot(n.TaskID) BEFORE startNextWaiter(): under t.mu, decrement runningSubagents floored at zero AND delete(t.subagentCancels, n.TaskID); outside the lock, startNextWaiter() (its own lock; it re-increments when a waiter is admitted — a completing task frees the slot, the waiter reoccupies it, and the completed id's cancel entry is retired in the same critical section). Update Complete's D-10 comment to state the release-then-admit ordering and the cancel-entry retirement. No changes to RegisterSubagent, QueuedNote, or CancelQueued semantics.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/tasks/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/tasks/ -run 'TestTrackerCap_SlotsFreeAfterCompletion|TestTrackerCap_SequentialReuseNeverQueues|TestTrackerComplete_RetiresCancelEntry'` exits 0.
    - The pre-existing tracker battery stays green: `go test -race -count=1 ./internal/tasks/ -run 'TestTracker'` exits 0.
    - `grep -c 'runningSubagents--' internal/tasks/tracker.go` is 1 and it sits inside the floored helper (grep -c 'runningSubagents > 0' internal/tasks/tracker.go is 1).
    - `grep -c 'delete(t.subagentCancels' internal/tasks/tracker.go` is 1 (inside the same helper).
    - `go vet ./internal/tasks/` clean.
  </acceptance_criteria>
  <done>After any number of total background-subagent starts in a session, completions free their slots AND retire their cancel registrations: the D-10 queue drains as promised, the 22-01 T5 / 22-03 T5 truths hold again, and a completed subagent no longer classifies as running (the substrate 22-09 needs).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (G-22-3): panic recovery in the background-subagent goroutine — the D-13 invariant, background leg (CR-03)</name>
  <files>internal/tasks/subagent.go, internal/tasks/subagent_test.go</files>
  <behavior>
    - A dep.Run that panics (panic("boom: probe")) does NOT crash the test process (the test completing is the proof — mirror TestSubagentPanicRecovery's shape, session/subagent_test.go).
    - Exactly ONE notification fires: Kind=KindSubagent, ExitStatus "error" (collect via SetDrain/PendingPeek; assert count==1 — the normal path and the recover path are mutually exclusive because the panic unwinds past the normal Complete).
    - The output file is not left dangling: it is CLOSED and carries the terminal marker with the panic text ("subagent panic: boom: probe") plus the stack (debug.Stack, the foreground leg's investigate-ready posture) — read the file back and assert the marker line and a non-empty stack tail.
    - The slot is released: with Task 1's fix in (same plan, landed first), the same-package tracker count returns to 0 after the panic notification AND the panic-path Complete retires the cancel entry (same-package map lookup misses the id) — the panic path no longer wedges slot accounting or leave a stale running classification.
    - A panicking subagent in a QueuedNote'd... not applicable — recovery is at the goroutine boundary only; the queued path is untouched (existing TestDispatchBackground_QueuedOverCap stays green in internal/session).
  </behavior>
  <read_first>internal/tasks/subagent.go :77-100 (the launcher goroutine — defer cancelFn() at :78, dep.Run at :80, the terminal switch and Complete at :82-99), internal/session/subagent.go :105-126 (the FOREGROUND recover: r := recover(), debug.Stack(), error line to transcript, exactly-one outcome — the shape to mirror), internal/tasks/subagent_test.go :42-110 (TestBackgroundOutput_MidRunRetrieval harness — SubagentDeps construction, ServeCtx, output-file location, notification collection), 22-REVIEW.md CR-03 fix sketch</read_first>
  <action>
RED: add TestBackgroundSubagent_PanicRecovered to subagent_test.go with the rows above; commit failing (today it kills the test binary — run it with a subprocess guard if the harness needs one, or simply assert via the recover-free failure mode: the test fails on process crash either way).
GREEN: inside the launcher goroutine, directly after `defer cancelFn()`, add a second deferred func: if p := recover(); p != nil — w.finish("error", fmt.Sprintf("subagent panic: %v\n%s", p, debug.Stack())) then dep.Tracker.Complete(Notification{TaskID: id, Kind: KindSubagent, ExitStatus: "error", Duration: time.Since(started), Tail: w.tail(), OutputFile: logPath}). Defer order note: LIFO runs the recover defer first during unwind, cancelFn still runs after. Import runtime/debug. The status/marker vocabulary reuses the existing "error" terminal class (Notification doc: "0" | nonzero | "killed" | "error"). The Complete call lands in Task 1's releaseSubagentSlot path automatically — no extra slot/cancel bookkeeping here.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/tasks/ -run 'TestBackgroundSubagent_Panic' && go test -race -count=1 ./internal/tasks/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/tasks/ -run 'TestBackgroundSubagent_Panic'` exits 0 with the process alive (the test itself is the liveness proof).
    - Exactly one notification asserted (count==1) with ExitStatus "error"; the output file contains the panic marker text and a stack tail.
    - `grep -c 'recover()' internal/tasks/subagent.go` is 1.
    - `go test -race -count=1 ./internal/tasks/` and `go test -race -count=1 ./internal/session/ -run 'TestDispatchBackground|TestSubagentPanicRecovery'` exit 0.
    - `go vet ./internal/tasks/` clean.
  </acceptance_criteria>
  <done>A panicking background subagent is a contained, notified, slot-releasing error — the process (ACP server, every session, every PTY) survives, matching the foreground D-13 invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagent nested loop → process | Panics in provider streaming / restricted tool execution cross into the agent process (background goroutine boundary) |
| tracker accounting → resource admission | A monotonic cap counter converts long sessions into permanent queue starvation (resource-exhaustion vector) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-19 | Denial of Service | tasks/subagent.go launcher goroutine | critical | mitigate | Task 2: recover() at the goroutine boundary (D-13 invariant) — one panicking subagent becomes one error notification instead of killing the ACP server, every session, every PTY |
| T-22-21 | Denial of Service | tracker.go runningSubagents accounting | high | mitigate | Task 1: floored decrement in Complete — the cap queue can no longer be starved by a monotonic counter |

</threat_model>

<verification>
- `go test -race -count=1 ./internal/tasks/` green — the new regression batteries (slot release, sequential reuse, cancel-entry retirement, panic recovery) AND every existing 22-01..22-06 row in the package (the fixes must be strictly additive on paths the green batteries already prove).
- `go vet ./internal/tasks/` clean.
- `GOOS=linux CGO_ENABLED=0 go build ./...` stays green (no new deps; runtime/debug is stdlib).
- Re-verification mapping: G-22-1→TestTrackerCap_SlotsFreeAfterCompletion/SequentialReuseNeverQueues + TestTrackerComplete_RetiresCancelEntry; G-22-3→TestBackgroundSubagent_PanicRecovered.
</verification>

<success_criteria>
- Both gap truths in must_haves hold, each pinned by a named test that fails against the pre-fix code (the gaps were precisely "green batteries never exercise these paths" — the new batteries exercise exactly those paths).
- No behavior change on any path the existing batteries pin: FIFO order, cap notes, cancel-by-id semantics, queued-over-cap dispatch all byte-stable.
- Zero scope creep: WR-02..WR-08 and IN-01/IN-02/IN-04 untouched (deferred review debt, see the objective's scope guard).
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-07-SUMMARY.md` when done
</output>
