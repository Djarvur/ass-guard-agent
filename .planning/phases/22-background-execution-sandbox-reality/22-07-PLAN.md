---
phase: 22-background-execution-sandbox-reality
plan: 07
type: execute
wave: 6
depends_on: ["22-06"]
files_modified:
  - internal/tasks/tracker.go
  - internal/tasks/tracker_test.go
  - internal/tasks/subagent.go
  - internal/tasks/subagent_test.go
  - internal/runtime/cron_wiring.go
  - internal/runtime/wake_wiring_test.go
  - internal/runtime/runtime.go
  - internal/coreexec/background.go
  - internal/coreexec/planmode.go
  - internal/coreexec/background_test.go
autonomous: true
gap_closure: true
gap_ids: [G-22-1, G-22-2, G-22-3, G-22-4, G-22-5]
requirements: [PAR-07, PAR-08]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 5
  confidence: low # estimate-calibration has 0 samples for gap-closure tasks (factor 1, not applied); modeled on Phase 22 per-plan metrics (P04 35 min / P06 32 min, 3 tasks) x 5 focused RED/GREEN pairs

must_haves:
  truths:
    - "G-22-1 (CR-01, D-10): background-subagent slots are RELEASED on completion — after SubagentCap (default 8) TOTAL starts in a session (sequential or concurrent), RegisterSubagent still admits directly (queued==false); the over-cap FIFO queue drains as slots actually free, so a dispatch told 'it starts when a slot frees' really starts"
    - "G-22-2 (CR-02): the ONE wake-drain chain goes IDLE when the pending queue is empty (no successor spawn, no per-session spin goroutine outliving the chain) and refuses to spawn past serve shutdown — scheduleWakeDrain is a no-op once the serve ctx is done, and the chain's exit defer restarts only when pending is non-empty AND the ctx is live"
    - "G-22-3 (CR-03, D-13 invariant): a panicking background subagent NEVER kills the ass-guard process — the launcher goroutine mirrors the foreground recover() (session/subagent.go:105-126), writes the terminal error marker to the output file, fires exactly ONE error notification (Kind=KindSubagent, ExitStatus error), and releases its slot"
    - "G-22-4 (CR-04, OQ5 family): session close fully ends the session's background machinery — CloseSession cancels RUNNING background subagents and deletes the per-session tracker/wakeInFlight/ptyManagers entries; a late completion reaches the drain via a NON-constructing session lookup, drops the batch loudly (one stderr line naming the session and count) without any sessionFor reconstruction, no ghost provider turn, and nothing re-enters r.sessions for the closed id"
    - "G-22-5 (WR-01, PAR-07 cancellation): TaskStop/TaskOutput address background-subagent task ids — ids unknown to the TaskRegistry fall through to the tracker seam: TaskStop cancels the subagent (Tracker.CancelTask gains production callers), TaskOutput renders the captured not_ready (queued/running) or ready (output-file content) envelope; the coretools.json promise 'Works with all task types' is no longer false for async agents"
  artifacts:
    - "internal/tasks/tracker.go — Complete decrements runningSubagents (floored) for Kind==KindSubagent via a releaseSubagentSlot helper BEFORE startNextWaiter; new CancelRunning() (invoke+clear every stored subagent cancel, return count — the close-time running-cancel leg); new SubagentState(id) queued/running classifier for the TaskOutput seam"
    - "internal/tasks/subagent.go — recover() at the launcher goroutine boundary mirroring the foreground D-13 leg: w.finish(\"error\", panic text + stack) + Tracker.Complete with ExitStatus error"
    - "internal/runtime/cron_wiring.go — wakeDrainChain's exit defer restarts ONLY on (ctx.Err()==nil AND tracker non-nil AND PendingPeek non-empty); scheduleWakeDrain refuses to spawn when the serve ctx is done; drainWakeNotifications resolves the session through a NON-constructing r.sessions lookup (sessMu-held) and treats a missing session as terminal: one stderr line + batch consumed via tr.Drain() + return true"
    - "internal/runtime/runtime.go — CloseSession (after eviction + s.Close()): cancel running subagents (OnClose CancelRunning link beside CancelQueued) and delete trackers/wakeInFlight (+ ptyManagers post-Drain) entries; sessionFor's RegisterInteractive literal (runtime.go:2116) carries the two new fallback seams bound to this session's tracker"
    - "internal/coreexec/planmode.go + background.go — InteractiveConfig gains TaskStopFallback func(id string) bool and TaskOutputFallback func(id string) (output, state string, running, handled bool) (primitive-arg seam, no coreexec→tasks import); TaskStopExecute/TaskOutputExecute fall through to them on unknown-to-registry ids"
  key_links:
    - "Tracker.Complete (Kind==KindSubagent) → releaseSubagentSlot → startNextWaiter — a completion actually frees the slot the waiter reoccupies (G-22-1)"
    - "tracker drain callback → scheduleWakeDrain (serve-ctx-guarded) → wakeDrainChain exit defer (gated restart) — the chain idles on empty and dies with the serve ctx (G-22-2)"
    - "CloseSession → s.Close() OnClose (CancelQueued + NEW CancelRunning) → trackers/wakeInFlight/ptyManagers Delete → late Complete → drainWakeNotifications non-constructing lookup → loud drop + chain exit (G-22-4)"
    - "sessionFor tracker → InteractiveConfig.TaskStopFallback/TaskOutputFallback → TaskStopExecute/TaskOutputExecute unknown-id fallthrough → Tracker.CancelTask / SubagentState + output file (G-22-5)"

---

<objective>
Close the four Phase 22 verification gaps (22-VERIFICATION.md frontmatter `gaps:` 1-4, review ids CR-01..CR-04) plus the same-class WR-01 cancellation break (G-22-5): all five are terminal/lifecycle paths of the delivered background-notification machinery that the existing green batteries never exercise. One plan, five RED/GREEN pairs, because the fixes share files (tracker.go, cron_wiring.go, runtime.go) and one root cause — every battery proves happy paths (launch, notify, coalesce, cancel-by-id) and none re-register after completions, panic, or close-then-complete.

Purpose: each gap falsifies a delivered must-have — D-10 queue draining (PAR-07), the phase goal's "ONE lifecycle infrastructure is itself lifecycle-sound" (PAR-07/PAR-08), the D-13 panic invariant the foreground leg already delivers (PAR-07), OQ5/PAR-08 close hygiene, and PAR-07's model-facing cancellation letter (Tracker.CancelTask is dead code today).
Output: the five regression batteries named below, all red against the current code and green after the fixes; `go test -race ./internal/tasks/ ./internal/runtime/ ./internal/coreexec/` fully green with the new rows.

Gap mapping: G-22-1=CR-01 slot decrement, G-22-2=CR-02 wake-chain idle, G-22-3=CR-03 panic recovery, G-22-4=CR-04 close-then-complete terminal, G-22-5=WR-01 TaskStop/TaskOutput reach subagent ids (verification's PAR-07 row names it the third cancellation break).

Scope guard (do NOT expand): WR-02 (recycled-pgid Stop), WR-03 (sentinel swallowed by stdin-readers), WR-04 (sweep tombstones bash-*.log), WR-05 (shared /tmp RW), WR-06 (seatbelt quote escaping), WR-07 (dropped-waiter fd leak), WR-08 (OSC bare backslash), IN-01/IN-02/IN-04 stay deferred review debt. IN-03's tracker/wakeInFlight(/ptyManagers) pruning is IN scope only because CR-04's missing list names it as the gap's substrate; turnMus/turnActive stay (IN-03's own caveat: in-flight holders may still reference the mutex). IN-05's unbounded retry retires incidentally — the non-constructing lookup removes the only construction-failure retry path, no separate work.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-VERIFICATION.md
@.planning/phases/22-background-execution-sandbox-reality/22-REVIEW.md
@internal/tasks/tracker.go
@internal/tasks/subagent.go
@internal/runtime/cron_wiring.go
@internal/session/subagent.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (G-22-1): tracker slot release — completions actually free D-10 slots (CR-01)</name>
  <files>internal/tasks/tracker.go, internal/tasks/tracker_test.go</files>
  <behavior>
    - Re-register after completions: register SubagentCap (use cap 2 in TrackerOpts) subagents, Complete ALL of them (Kind=KindSubagent), then register once more — queued==false, err==nil (starts immediately). This exact row is the blind spot of TestTrackerQueue_FIFODrainOrder.
    - After all completions with empty waiters, the internal count is zero: same-package assert t.runningSubagents == 0.
    - Sequential-use regression: a loop of 3x-cap register→complete cycles (cap 2) never queues — every registration admits directly.
    - FIFO handoff still correct: cap 2, register 3 (third queued==true with position note), Complete the first → the third's start func runs (observable via a channel) and the count never exceeds cap (same-package assert under 2+ concurrency).
    - Floor safety: a Complete for a KindSubagent arriving with no matching admission (defensive row) leaves runningSubagents at 0, never negative.
    - KindBash completions never touch the subagent count (regression row).
  </behavior>
  <read_first>internal/tasks/tracker.go IN FULL (Complete :115-133, RegisterSubagent :160-189 admission check :163, startNextWaiter :194-214 with its :205 increment — the counter's ONLY movements today are the two ++ sites), internal/tasks/tracker_test.go :59-160 (TestTrackerCap_UnderCapStartsImmediately, TestTrackerQueue_FIFODrainOrder — the harness helpers: start funcs returning channel-signalling cancels, same-package field access), 22-REVIEW.md CR-01 (the suggested releaseSubagentSlot shape)</read_first>
  <action>
RED: add TestTrackerCap_SlotsFreeAfterCompletion and TestTrackerCap_SequentialReuseNeverQueues to tracker_test.go with the rows above; commit failing (they fail today: after 2 total starts the admission check is permanently false and the re-register queues forever — use a bounded WaitTimeout on the start channel so the failure is an assertion, not a hang).
GREEN: in Complete, for n.Kind == KindSubagent call a new releaseSubagentSlot() BEFORE startNextWaiter(): under t.mu, decrement runningSubagents floored at zero; outside the lock, startNextWaiter() (its own lock; it re-increments when a waiter is admitted — a completing task frees the slot, the waiter reoccupies it). Update Complete's D-10 comment to state the release-then-admit ordering. No changes to RegisterSubagent, QueuedNote, or CancelQueued semantics.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/tasks/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/tasks/ -run 'TestTrackerCap_SlotsFreeAfterCompletion|TestTrackerCap_SequentialReuseNeverQueues'` exits 0.
    - The pre-existing tracker battery stays green: `go test -race -count=1 ./internal/tasks/ -run 'TestTracker'` exits 0.
    - `grep -c 'runningSubagents--' internal/tasks/tracker.go` is 1 and it sits inside the floored helper (grep -c 'runningSubagents > 0' is 1).
    - `go vet ./internal/tasks/` clean.
  </acceptance_criteria>
  <done>After any number of total background-subagent starts in a session, completions free their slots: the D-10 queue drains as promised and the 22-01 T5 / 22-03 T5 truths hold again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (G-22-2): wake-drain chain idles on empty queue and dies with the serve ctx (CR-02)</name>
  <files>internal/runtime/cron_wiring.go, internal/runtime/wake_wiring_test.go</files>
  <behavior>
    - Chain idles on empty: after one delivered batch the chain exits AND no successor spawns — the session's wakeInFlight flag stays false across a >=500ms sampled window (>=25 samples; the buggy unconditional restart re-spawns a successor within microseconds of every exit, so the flag is observably true during the window) and runtime.NumGoroutine() returns to its pre-trigger baseline and stays there across re-checks.
    - No spawn past serve shutdown: with a CANCELLED serve ctx, scheduleWakeDrain(sessionID) leaves the wakeInFlight flag false (the LoadOrStore'd entry never flips) and spawns nothing (goroutine count unchanged after a settle window).
    - In-flight chain meets ctx cancel: a chain mid-retry (busy turn slot) stops when the ctx cancels and its exit defer does not restart it — flag false, stays false.
    - Genuine race still recovered: a Complete landing AFTER the chain's empty-queue decision but before the flag store still gets a fresh chain (plant a completion between the peek and the exit using the retry-interval harness; the batch must eventually deliver) — the gating must not strand a racing batch.
    - The three existing wake batteries (TestWakeTurn_BackgroundBashCompletion, TestWakeTurn_BusyClientTurnAccumulates, TestWakeTurn_EmptyPendingNoTurn) pass unmodified.
  </behavior>
  <read_first>internal/runtime/cron_wiring.go :342-432 (scheduleWakeDrain :369-383, wakeDrainChain :393-432 — the defer :393-400, empty-queue exit :412-414, retry select), internal/runtime/wake_wiring_test.go :292-360 (TestWakeTurn_EmptyPendingNoTurn harness — runner construction, tracker priming, wakeRetryInterval injection, provider call counting) and :109-210 for the deliver-path harness, internal/runtime/runtime.go :2389-2400 (serveCtxOrBackground — context.Background() fallback for test runners, whose Err() is always nil)</read_first>
  <action>
RED: add TestWakeChain_IdlesWhenEmpty (flag-window sampling + goroutine baseline), TestWakeChain_NoSpawnPastServeShutdown, and TestWakeChain_CtxCancelStopsChain to wake_wiring_test.go; commit failing (the idle test fails today: the flag keeps flipping true as successors spawn).
GREEN: (1) wakeDrainChain's exit defer: flag.Store(false) FIRST, then restart only when ctx.Err()==nil AND tr != nil AND len(tr.PendingPeek()) > 0 — re-derive tr inside the defer (the body's tr may be nil on the early tracker-missing return); keep the race comment updated to explain the ordering: a completion landing after the peek fires its own Complete→drain callback→scheduleWakeDrain, whose CAS on the just-cleared flag wins, so a gated restart is only needed for the completion that landed BETWEEN peek and store. (2) scheduleWakeDrain head guard: after the empty-sessionID check, return when r.serveCtxOrBackground().Err() != nil (test runners on the Background fallback are unaffected — Err() is always nil there). No changes to the chain body's loop, retry cadence, or drainWakeNotifications.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestWakeChain|TestWakeTurn' && go test -race -count=1 ./internal/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/runtime/ -run 'TestWakeChain'` exits 0 (all three new rows).
    - `go test -race -count=1 ./internal/runtime/ -run 'TestWakeTurn'` exits 0 — the three existing batteries pass unmodified.
    - `grep -c 'PendingPeek()) > 0' internal/runtime/cron_wiring.go` is >= 1 (the gated restart) and the scheduleWakeDrain guard references serveCtxOrBackground().Err() (grep -c 'serveCtxOrBackground().Err()' internal/runtime/cron_wiring.go is >= 1).
    - `go vet ./internal/runtime/` clean.
  </acceptance_criteria>
  <done>The ONE wake-drain infrastructure is lifecycle-sound: idle means idle (zero chain goroutines per quiesced session), serve shutdown ends the chains, and a racing completion still wakes a fresh chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (G-22-3): panic recovery in the background-subagent goroutine — the D-13 invariant, background leg (CR-03)</name>
  <files>internal/tasks/subagent.go, internal/tasks/subagent_test.go</files>
  <behavior>
    - A dep.Run that panics (panic("boom: probe")) does NOT crash the test process (the test completing is the proof — mirror TestSubagentPanicRecovery's shape, session/subagent_test.go).
    - Exactly ONE notification fires: Kind=KindSubagent, ExitStatus "error" (collect via SetDrain/PendingPeek; assert count==1 — the normal path and the recover path are mutually exclusive because the panic unwinds past the normal Complete).
    - The output file is not left dangling: it is CLOSED and carries the terminal marker with the panic text ("subagent panic: boom: probe") plus the stack (debug.Stack, the foreground leg's investigate-ready posture) — read the file back and assert the marker line and a non-empty stack tail.
    - The slot is released: with Task 1's fix in, the same-package tracker count returns to 0 after the panic notification (the panic path no longer wedges slot accounting).
    - A panicking subagent in a QueuedNote'd... not applicable — recovery is at the goroutine boundary only; the queued path is untouched (existing TestDispatchBackground_QueuedOverCap stays green in internal/session).
  </behavior>
  <read_first>internal/tasks/subagent.go :77-100 (the launcher goroutine — defer cancelFn() at :78, dep.Run at :80, the terminal switch and Complete at :82-99), internal/session/subagent.go :105-126 (the FOREGROUND recover: r := recover(), debug.Stack(), error line to transcript, exactly-one outcome — the shape to mirror), internal/tasks/subagent_test.go :42-110 (TestBackgroundOutput_MidRunRetrieval harness — SubagentDeps construction, ServeCtx, output-file location, notification collection), 22-REVIEW.md CR-03 fix sketch</read_first>
  <action>
RED: add TestBackgroundSubagent_PanicRecovered to subagent_test.go with the rows above; commit failing (today it kills the test binary — run it with a subprocess guard if the harness needs one, or simply assert via the recover-free failure mode: the test fails on process crash either way).
GREEN: inside the launcher goroutine, directly after `defer cancelFn()`, add a second deferred func: if p := recover(); p != nil — w.finish("error", fmt.Sprintf("subagent panic: %v\n%s", p, debug.Stack())) then dep.Tracker.Complete(Notification{TaskID: id, Kind: KindSubagent, ExitStatus: "error", Duration: time.Since(started), Tail: w.tail(), OutputFile: logPath}). Defer order note: LIFO runs the recover defer first during unwind, cancelFn still runs after. Import runtime/debug. The status/marker vocabulary reuses the existing "error" terminal class (Notification doc: "0" | nonzero | "killed" | "error").
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/tasks/ -run 'TestBackgroundSubagent_Panic' && go test -race -count=1 ./internal/tasks/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/tasks/ -run 'TestBackgroundSubagent_Panic'` exits 0 with the process alive (the test itself is the liveness proof).
    - Exactly one notification asserted (count==1) with ExitStatus "error"; the output file contains the panic marker text and a stack tail.
    - `grep -c 'recover()' internal/tasks/subagent.go` is 1.
    - `go test -race -count=1 ./internal/tasks/` and `go test -race -count=1 ./internal/session/ -run 'TestDispatchBackground|TestSubagentPanicRecovery'` exit 0.
    - `go vet ./internal/tasks/` clean.
  </acceptance_criteria>
  <done>A panicking background subagent is a contained, notified, slot-releasing error — the process (ACP server, every session, every PTY) survives, matching the foreground D-13 invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (G-22-4): close-then-complete is terminal — cancel running subagents, prune state, never reconstruct (CR-04)</name>
  <files>internal/runtime/runtime.go, internal/runtime/cron_wiring.go, internal/tasks/tracker.go, internal/runtime/wake_wiring_test.go</files>
  <behavior>
    - drainWakeNotifications on a MISSING session is terminal: with a tracker registered and pending planted but the session absent from r.sessions, one call (same package) returns true, consumes the batch (PendingPeek empty afterwards), and writes exactly ONE stderr line naming the session id and the dropped count — no retry loop, and the provider/wake-turn counter stays at zero.
    - No resurrection end-to-end: build a session (TestWakeTurn harness), CloseSession(id), then fire a late completion on the still-held tracker reference — r.sessions never re-contains the id, the fake provider call count stays zero, and after the settle window the chain flag is false (in TODAY's code the drain's constructing sessionFor re-inserts a full session and runs a real wake turn — this row is the CR-04 headline regression).
    - CloseSession cleans the runner's per-session state: after close, r.trackers.Load(id) and r.wakeInFlight.Load(id) are both miss; r.ptyManagers.Load(id) is miss (deleted after OnClose's Drain ran).
    - RUNNING background subagents are cancelled at close: register one on the session's tracker with a channel-signalling cancel; CloseSession closes that channel (the NEW CancelRunning link beside CancelQueued in OnClose), and a counted stderr note fires when the running-cancel count is non-zero.
    - The queued-cancel leg and the whole existing close battery stay green (TestTrackerClose_DropsQueuedNotRunning, TestSessionClosePTY, TestInProcessReloadAfterCloseRebuildsSession — the close-then-RESTORE flow must still reconstruct cleanly through sessionFor on demand).
  </behavior>
  <read_first>internal/runtime/cron_wiring.go :434-448 (drainWakeNotifications head — the constructing r.sessionFor call and the stay-pending retry), internal/runtime/runtime.go :2783-2809 (CloseSession — eviction-before-Close, the place for the deletes), :2346-2362 (OnClose chain — CancelQueued at :2356, ptyMgr.Drain :2351, where CancelRunning joins), :2015-2035 (tracker construction + SetDrain — what a resurrected sessionFor rebuilds), :2364 (r.sessions[sessionID] = s — what reconstruction re-does), internal/tasks/tracker.go :216-228 (CancelTask) and :260-268 (CancelQueued — the shape CancelRunning mirrors), internal/runtime/wake_wiring_test.go :109-207 (the runner/session harness for the end-to-end row)</read_first>
  <action>
RED: add TestWakeChain_ClosedSessionDropsBatch (terminal unit row + end-to-end no-resurrection row) and TestCloseSession_PrunesWakeState (map deletes + running-cancel row) to wake_wiring_test.go; commit failing (the end-to-end row fails today: sessionFor reconstructs and r.sessions regains the id).
GREEN: (1) tracker.go: new CancelRunning() int — under t.mu, invoke every stored subagentCancels entry, clear the map, return the count (queued waiters stay CancelQueued's job; the two are idempotent together when both run). (2) runtime.go OnClose: beside the CancelQueued block, call tracker.CancelRunning() and note the count on stderr when non-zero (same sentence family as the dropped-queued note). (3) runtime.go CloseSession: AFTER s.Close() returns (so OnClose's cancels and PTY Drain have run while the tracker was still discoverable), delete r.trackers, r.wakeInFlight, and r.ptyManagers entries for the id — unconditional deletes, also on the session-missing path. (4) cron_wiring.go drainWakeNotifications: replace the constructing r.sessionFor(ctx, sessionID) with a non-constructing lookup — r.sessMu.Lock, s, ok := r.sessions[sessionID], r.sessMu.Unlock; when !ok: one stderr line ("wake drain for %s: session closed — dropping %d pending notification(s)") using the PendingPeek count, consume via tr.Drain(), return true (the chain exits; this also retires the IN-05 infinite retry, whose only trigger was construction failure). The found-session path is unchanged through TryLock/deliver.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestWakeChain|TestCloseSession|TestSessionClose|TestInProcessReloadAfterClose' && go test -race -count=1 ./internal/tasks/ && go test -race -count=1 ./internal/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/runtime/ -run 'TestWakeChain_ClosedSessionDropsBatch|TestCloseSession_PrunesWakeState'` exits 0.
    - The end-to-end row asserts r.sessions lacks the closed id after the late completion settles AND the provider call counter is zero.
    - `grep -c 'r.trackers.Delete' internal/runtime/runtime.go` is 1 and `grep -c 'r.wakeInFlight.Delete' internal/runtime/runtime.go` is 1; `grep -c 'func (t \*Tracker) CancelRunning' internal/tasks/tracker.go` is 1.
    - cron_wiring.go no longer calls sessionFor inside drainWakeNotifications (grep -n 'sessionFor' internal/runtime/cron_wiring.go shows only the runAutomationTurn site).
    - `go test -race -count=1 ./internal/runtime/ ./internal/tasks/` exits 0 (existing close/reload batteries unmodified and green); `go vet` on both packages clean.
  </acceptance_criteria>
  <done>Close is final for the background machinery: running subagents die with the session, per-session wake state is pruned, and a late completion drops its batch loudly instead of rebuilding a ghost session and billing a provider turn nobody owns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5 (G-22-5): TaskStop/TaskOutput reach background-subagent ids — the tracker fallthrough seam (WR-01)</name>
  <files>internal/coreexec/background.go, internal/coreexec/planmode.go, internal/coreexec/background_test.go, internal/tasks/tracker.go, internal/tasks/tracker_test.go, internal/runtime/runtime.go, internal/runtime/wake_wiring_test.go</files>
  <behavior>
    - TaskStop on a subagent id: with the registry NOT knowing the id and the fallback returning true, the stub renders the captured ack ("Task <id> stopped."); fallback false renders the existing structured unknown-task error unchanged.
    - TaskStop cancels for real through the runtime wiring: a background subagent registered on a session's tracker (blocking Run), the session catalog's TaskStop invoked with its exec_ id → the registered cancel func fires (channel signal).
    - TaskOutput on a subagent id (registry unknown, fallback handled): queued → the captured not_ready envelope with status queued; running → not_ready with status running; finished → the ready envelope whose output section carries the output-file content (bounded read — last 64 KiB — documented in the stub comment); not handled anywhere → the existing structured unknown-task error.
    - block/timeout are documented as ignored for fallback ids (render the current state; the schema's promise is that the id is ADDRESSABLE — the current lie is "unknown task").
    - Registry-owned ids are untouched: the fallthrough runs ONLY on the registry's unknown-task error (existing TaskOutput/TaskStop rows stay green byte-identically).
    - Tracker classifier: SubagentState(id) reports queued for a waiter, running for an admitted-and-cancel-registered id, unknown otherwise (three table rows).
  </behavior>
  <read_first>internal/coreexec/background.go :690-742 (TaskOutputExecute — r.Output err path :713-716 is the fallthrough point; the captured not_ready/ready envelope shapes) and :754-795 (TaskStopExecute — r.Stop err path :783-786), internal/coreexec/planmode.go :100-140 (InteractiveConfig struct + the stubs map binding cfg.Tasks at :131/:136 — where the two new fields join), internal/runtime/runtime.go :2111-2130 (the RegisterInteractive literal inside sessionFor — holds both taskRegistry and tracker in scope), internal/tasks/tracker.go :216-228 (CancelTask) and :154-214 (waiters vs subagentCancels — the classifier's two sources), internal/tasks/subagent.go :54-56 (the output-file path convention .ass-guard/outputs/<id>.log under WorkDir), internal/coreexec/background_test.go (existing TaskStop/TaskOutput rows — the marshalStructured/not_ready assertion helpers)</read_first>
  <action>
RED: (1) coreexec rows in background_test.go: TaskStopExecute/TaskOutputExecute with a stub registry (unknown id) plus fake fallbacks covering ack/true, queued, running, finished, and not-handled. (2) tracker_test.go: TestTrackerSubagentState three rows. (3) runtime row in wake_wiring_test.go: the sessionFor-wired catalog stops a live background subagent by id. Commit failing.
GREEN: (1) planmode.go InteractiveConfig: add TaskStopFallback func(id string) bool and TaskOutputFallback func(id string) (output, state string, running, handled bool) — primitive-arg seam, no coreexec→tasks import (the :2017 CompletionHook precedent); bind them into TaskStopExecute(cfg.Tasks, cfg.TaskStopFallback) and TaskOutputExecute(cfg.Tasks, cfg.TaskOutputFallback) at the stubs map. (2) background.go: extend both constructors with the fallback param (internal callers: planmode.go only); TaskStopExecute — when r.Stop returns the unknown-task error and fallback != nil and fallback(id), render the same captured ack; TaskOutputExecute — when r.Output returns the unknown-task error and the fallback reports handled, render the same not_ready (queued/running) or ready (finished) envelope shapes with the fallback's output. (3) tracker.go: SubagentState(id string) (queued, running bool) — waiters scan under t.mu, subagentCancels lookup. (4) runtime.go sessionFor RegisterInteractive literal: TaskStopFallback: tracker.CancelTask; TaskOutputFallback: closure classifying via tracker.SubagentState (queued → ("", "queued", true, true); running → ("", "running", true, true)) else reading filepath.Join(dir, ".ass-guard", "outputs", id+".log") with a bounded io.LimitReader-equivalent tail read (found → (content, "finished", false, true); stat-miss → handled=false). Document the block-ignored choice in the stub comment.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/coreexec/ -run 'TestTaskStop|TestTaskOutput' && go test -race -count=1 ./internal/tasks/ && go test -race -count=1 ./internal/runtime/ -run 'TestTaskStopFallbackWiring' && go test -race -count=1 ./internal/coreexec/ ./internal/tasks/ ./internal/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - All new rows green under the commands above; the fake-provider call count in the wiring row stays zero (a stop must not wake turns).
    - `grep -c 'TaskStopFallback' internal/coreexec/planmode.go` is >= 2 (field + binding) and `grep -c 'TaskOutputFallback' internal/coreexec/planmode.go` is >= 2.
    - `grep -rn 'import' internal/coreexec/background.go` shows no internal/tasks import (the seam stays primitive-arg).
    - The full three-package battery exits 0 — every 22-01..22-06 row plus Tasks 1-4 rows green together; `go vet` on all three packages clean.
  </acceptance_criteria>
  <done>The model can stop and read a background subagent through the tool surface using the exact id the async_launched payload handed it — Tracker.CancelTask has production callers and PAR-07's cancellation letter is reachable end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagent nested loop → process | Panics in provider streaming / restricted tool execution cross into the agent process (background goroutine boundary) |
| late task completions → session lifecycle | Completions arriving after operator close must not cross back into provider/forwarder resources |
| model → task ids (TaskStop/TaskOutput) | Model-supplied ids resolve to live tasks; unknown ids must stay structured errors |
| serve ctx → goroutine lifetime | Chain goroutines must not outlive the ctx the editor owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-19 | Denial of Service | tasks/subagent.go launcher goroutine | critical | mitigate | Task 3: recover() at the goroutine boundary (D-13 invariant) — one panicking subagent becomes one error notification instead of killing the ACP server, every session, every PTY |
| T-22-20 | Denial of Service | cron_wiring.go wakeDrainChain/scheduleWakeDrain | high | mitigate | Task 2: gated restart (pending non-empty AND ctx live) + spawn refusal past serve ctx — kills the permanent per-session spin and the post-shutdown churn |
| T-22-21 | Denial of Service | tracker.go runningSubagents accounting | high | mitigate | Task 1: floored decrement in Complete — the cap queue can no longer be starved by a monotonic counter |
| T-22-22 | Information Disclosure / cost | runtime.go CloseSession + cron_wiring.go drainWakeNotifications | high | mitigate | Task 4: cancel running subagents at close, prune per-session state, non-constructing lookup with loud terminal drop — ghost provider turns, transcript appends, and spawned subprocesses into closed sessions become impossible |
| T-22-23 | Denial of Service | background.go TaskStop/TaskOutput | medium | mitigate | Task 5: unknown-id fallthrough to the tracker seam — model-facing cancellation of runaway background subagents becomes reachable; ids the seam does not know keep the structured error (no id-guessing surface added — the ids remain session-local crypto/rand values) |
| T-22-24 | (accepted) | wake-chain stderr drop line | low | accept | The loud drop line names the session id and count only — no task content; stderr is the diagnostics channel by transport discipline |

</threat_model>

<verification>
- `go test -race -count=1 ./internal/tasks/ ./internal/runtime/ ./internal/coreexec/` green — the five new regression batteries AND every existing 22-01..22-06 row (the fixes must be strictly additive on paths the green batteries already prove).
- `go vet ./internal/tasks/ ./internal/runtime/ ./internal/coreexec/` clean.
- `GOOS=linux CGO_ENABLED=0 go build ./...` stays green (no new deps; debug.Stack is stdlib).
- Re-verification mapping: each VERIFICATION.md gap truth maps 1:1 to a task battery — G-22-1→TestTrackerCap_SlotsFreeAfterCompletion/SequentialReuseNeverQueues, G-22-2→TestWakeChain_IdlesWhenEmpty/NoSpawnPastServeShutdown/CtxCancelStopsChain, G-22-3→TestBackgroundSubagent_PanicRecovered, G-22-4→TestWakeChain_ClosedSessionDropsBatch + TestCloseSession_PrunesWakeState, G-22-5→TaskStop/TaskOutput fallthrough rows + TestTrackerSubagentState + the runtime wiring row.
</verification>

<success_criteria>
- All five gap truths in must_haves hold, each pinned by a named test that fails against the pre-fix code (the four gaps were precisely "green batteries never exercise these paths" — the new batteries exercise exactly those paths).
- No behavior change on any path the existing batteries pin: FIFO order, coalescing, cap notes, cancel-by-id semantics, PTY close drain, sandbox legs, close-then-restore reconstruction all byte-stable.
- Zero scope creep: WR-02..WR-08 and IN-01/IN-02/IN-04 untouched (deferred review debt); IN-05 retired incidentally by the non-constructing lookup, noted in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-07-SUMMARY.md` when done
</output>
