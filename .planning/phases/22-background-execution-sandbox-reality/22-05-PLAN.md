---
phase: 22-background-execution-sandbox-reality
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/sandbox/policy.go
  - internal/sandbox/policy_test.go
  - internal/sandbox/landlock_linux.go
  - internal/sandbox/landlock_linux_test.go
  - internal/sandbox/child_linux.go
  - internal/sandbox/seatbelt_darwin.go
  - internal/sandbox/seatbelt_darwin_test.go
  - internal/sandbox/doc.go
  - go.mod
  - go.sum
autonomous: true
requirements: [SAND-01]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ONE policy struct feeds BOTH sandbox backends: the same Policy renders a landlock ruleset description and a seatbelt profile whose deny sets are symmetric — drift between OSes is impossible by construction (D-06, SAND-01)"
    - "ONE portable enforcement entry exists — sandbox.WrapCmd(cmd, Policy) — so every in-phase exec site (22-06 wires the foreground Bash exec, TaskRegistry.Start's background exec, and the PTY persistent-shell spawn) confines through the same code path; no call site re-implements argv substitution (D-06, D-05)"
    - "The probe taxonomy is strict and loud: ProbeABI distinguishes ENOSYS (kernel lacks Landlock) / EOPNOTSUPP (disabled) / ABI<4, and the seatbelt first-use probe detects a missing sandbox-exec — each maps to a distinct Availability reason; BestEffort is never the enforcement path (SAND-01, Pitfall 1)"
    - "Confinement applies to SPAWNED tool processes only; ass-guard itself, in-process tools, and MCP host connections stay unconfined — the boundary is documented in the package doc as the D-05 honesty note, with the MPTCP caveat"
    - "macOS confinement rides an embedded .sb template rendered IN MEMORY (sandbox-exec -p) with targeted denies over (allow default) — never deny-default, never a runtime profile artifact on disk; the live darwin battery proves curl-connect deny and outside-write deny on this host (D-06, D-04, SAND-01)"
  artifacts:
    - path: "internal/sandbox/policy.go"
      provides: "Policy{RWPaths, ROSysPaths []string, DenyNetwork bool}; DefaultPolicy(workDir, tmpDir, guardDir) with the D-04 path set; LandlockRules() []Rule (V4-shape description consumed by the linux backend); SeatbeltProfile() string (in-memory template render); Handle{Policy, Availability} — the resolved immutable pair executors read; WrapCmd(cmd *exec.Cmd, p Policy) error — the ONE portable enforcement entry (darwin: sandbox-exec -p argv prefix; linux: self re-exec with sentinel + JSON policy env); IsSandboxChild/ApplySandboxChildHook portable main hook; the D-05 confinement-boundary doc"
      contains: "func DefaultPolicy"
    - path: "internal/sandbox/landlock_linux.go"
      provides: "//go:build linux ProbeABI() (int, error) — strict-mode probe with the ENOSYS/EOPNOTSUPP taxonomy (never BestEffort enforcement); ApplyChildRuleset(Policy) building RestrictPaths+RestrictNet V4 inside the re-exec child"
      contains: "ProbeABI"
    - path: "internal/sandbox/child_linux.go"
      provides: "//go:build linux the re-exec sentinel entrypoint: __ASS_GUARD_SANDBOX_CHILD=1 + __ASS_GUARD_SANDBOX_POLICY (JSON Policy) → apply ruleset → syscall.Exec the real target — ass-guard's own binary is the sandbox loader (Pattern 4); the linux leg of ApplySandboxChildHook"
      contains: "RunSandboxChild"
    - path: "internal/sandbox/seatbelt_darwin.go"
      provides: "//go:build darwin embedded .sb template + RenderProfile(Policy) + the WrapCmd darwin leg via sandbox-exec -p; first-use probe with loud degrade"
      contains: "WrapCommand"
    - path: "internal/sandbox/doc.go"
      provides: "the package doc: what IS confined (spawned tool processes), what is NOT (ass-guard itself, in-process tools, MCP host connections), the MPTCP caveat — the D-05 honesty note verbatim-in-intent"
      contains: "Confinement"
  key_links:
    - from: "internal/sandbox/policy.go Policy"
      to: "LandlockRules() + SeatbeltProfile()"
      via: "both renderings derive from the same Policy source; the symmetry golden pins rw={workdir, tmp, .ass-guard} + DenyNetwork against BOTH renderings in one test"
      pattern: "one policy two backends"
    - from: "sandbox.WrapCmd (portable)"
      to: "the GOOS backends"
      via: "darwin leg prepends sandbox-exec -p <rendered>; linux leg re-execs ass-guard's own binary with the sentinel env + policy env (child applies ruleset then syscall.Exec's the original target) — callers (22-06) never branch on GOOS themselves"
      pattern: "single enforcement entry"
  prohibitions:
    - requirement_id: SAND-01
      category: safety
      statement: "The sandbox MUST never fail open silently — the probe machinery distinguishes ENOSYS/EOPNOTSUPP/missing sandbox-exec into distinct Availability reasons, strict-mode only (never BestEffort); 22-06 owns the startup warning + per-run unconfined notes that consume these reasons"
      status: unresolved
    - requirement_id: SAND-01
      category: safety
      statement: "Confinement MUST never be applied to the ass-guard process itself — landlock is process-wide and would sever provider/MCP networking; WrapCmd substitutes argv for the CHILD only; the main-path hook (ApplySandboxChildHook) fires exclusively on the sentinel env, never on a normal serve start"
      status: unresolved

---

<objective>
Build the internal/sandbox package core (SAND-01's machinery): ONE policy struct driving symmetric landlock (Linux ≥5.13, go-landlock v0.10.0, child-applied via the re-exec sentinel) and sandbox-exec (macOS, embedded in-memory .sb template, targeted denies never deny-default) backends, behind ONE portable enforcement entry (WrapCmd) with the strict probe taxonomy and the D-05 honesty doc. Enforcement WIRING (the --sandbox flag, the startup probe step in Run, the three exec-site wraps, dangerouslyDisableSandbox) is 22-06 (wave 5) — this plan proves the package standalone: darwin legs live-tested on this host, linux legs compile-gated.

Purpose: D-04/D-05/D-06 lock the deny set, the child-only boundary, and the template symmetry; the package must exist and be proven BEFORE the exec sites consume it, so 22-06 is pure wiring.
Output: internal/sandbox package (policy + both backends + child sentinel + doc) + go-landlock v0.10.0 in go.mod, green under -race on darwin, GOOS=linux compile gate green.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-RESEARCH.md
@.planning/phases/22-background-execution-sandbox-reality/22-PATTERNS.md
@internal/acpserve/acp_serve.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/sandbox` (NEW package) — Policy/DefaultPolicy/LandlockRules/SeatbeltProfile (policy.go, portable); Handle{Policy, Availability}; WrapCmd (policy.go, portable entry over the GOOS legs); IsSandboxChild + ApplySandboxChildHook (portable signatures; linux leg in child_linux.go); ProbeABI/ApplyChildRuleset (landlock_linux.go, //go:build linux); RunSandboxChild (child_linux.go, //go:build linux); RenderProfile/WrapCommand (seatbelt_darwin.go, //go:build darwin); Availability type {Mode string, Available bool, Reason string}; doc.go (D-05 honesty note).
- go.mod / go.sum — `github.com/landlock-lsm/go-landlock v0.10.0` (import lives ONLY in //go:build linux files).
- Tests — policy_test.go (symmetry golden), seatbelt_darwin_test.go (live probe, //go:build darwin), landlock_linux_test.go (degrade taxonomy, //go:build linux).
- NOT here (22-06): Options.SandboxMode, the --sandbox cobra flag, resolveSandboxAvailability in Run's pipeline, the bash.go / background.go / ptty.go wraps, the main.go hook call.
</artifacts_this_phase_produce>

<flagged_assumptions>
- **SAND-01 edge-probe `unclassified` row (status: unresolved — surfaced, never auto-resolved):** the probe row is "review manually" — the manual review is this plan-set: seatbelt rule semantics are undocumented by Apple (RESEARCH A7), so the shipped profile's actual denies are pinned by the LIVE probe test on this darwin host (network deny breaks a real curl; write deny breaks a real touch), and the landlock ruleset's enforcement is pinned on a Linux host by the same battery shape (skipped-by-tag here). The ro-system path set is per-distro discretion (A8) — DefaultPolicy ships a documented macOS/Linux set and the loud-degrade contract covers gaps. Flagged until the verify phase confirms both batteries.
- **Re-exec sentinel pattern (RESEARCH A2, MEDIUM):** os/exec has no pre-exec child hook, so the Linux child is ass-guard's own binary re-invoked with __ASS_GUARD_SANDBOX_CHILD=1; if review rejects the pattern the fallback is a tiny internal helper binary — same semantics, more surface. Documented in child_linux.go's doc comment.
- **MPTCP blind spot (Pitfall 2):** a confined Go ≥1.24 child could bind via MPTCP outside the TCP rules — documented in the D-05 honesty note; non-Go children (the normal case) are unaffected on connect.
- **Package-core scope (checker r1 split):** the original single sandbox plan carried the flag plumbing and exec-site wraps too; the split keeps THIS plan a standalone package proof (10 files, 2 tasks) and gives 22-06 sole ownership of every enforcement call site — the fail-open prohibition then has exactly one wiring owner.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Policy struct + symmetry golden + portable WrapCmd entry + darwin seatbelt backend (live-probed)</name>
  <files>internal/sandbox/policy.go, internal/sandbox/policy_test.go, internal/sandbox/seatbelt_darwin.go, internal/sandbox/seatbelt_darwin_test.go, internal/sandbox/doc.go</files>
  <behavior>
    - Policy symmetry golden: the same DefaultPolicy(workDir, tmp, guard) renders a LandlockRules() list whose rw set is exactly {workdir, tmp, .ass-guard} with DenyNetwork true, AND a SeatbeltProfile() string containing (allow default), (deny network*), and write-denies OUTSIDE the rw set — one policy, two symmetric renderings (D-06).
    - SeatbeltProfile never contains deny-default: the profile's first rule is (allow default) and denies are targeted subpath/network forms (SAND-01 letter, pinned by golden assertion).
    - WrapCmd substitution contract (portable, unit-tested on darwin against the darwin leg): given a cmd whose Path/Args target the real command, WrapCmd(cmd, policy) replaces Path/Args so the CHILD runs confined — darwin: argv becomes sandbox-exec -p <rendered> <original argv...>; the original argv is recoverable as the tail; nothing else about cmd mutates (Dir, Env, SysProcAttr untouched — lifecycle stays the caller's).
    - RenderProfile substitutes the session's real paths into the embedded template IN MEMORY; no temp file is created (D-06 no-artifacts rule — battery asserts no file writes).
    - LIVE darwin probe test (//go:build darwin, runs on this host): a child under WrapCommand with the rendered profile fails a curl connect (network deny) and fails a touch outside the rw set (write deny), while a touch INSIDE the tmp rw path succeeds — the RESEARCH-verified mechanism pinned as a test.
    - First-use probe: WrapCommand probes sandbox-exec presence/-p acceptance; absence or failure → Availability{Available:false, Reason} + one stderr note, command runs unconfined (degrade loudly, never refuse).
  </behavior>
  <read_first>22-RESEARCH.md IN FULL for this task (Pattern 4 Policy struct + the live-verified .sb profile text + Code Examples darwin render/invoke, Pitfall 1 BestEffort, the Package Legitimacy rows), 22-PATTERNS.md (seatbelt_darwin.go assignment — degrade discipline from acp_serve.go seedACPGuard :75-88; policy.go role-match to bashArgs documented-const style), 22-CONTEXT.md (D-04, D-05, D-06), internal/acpserve/acp_serve.go :75-88 (the degrade idiom to copy)</read_first>
  <action>
RED: (1) policy_test.go golden: DefaultPolicy → LandlockRules() row set assertions (rw/ro/deny-network) AND SeatbeltProfile() content assertions (allow-default first, deny network*, targeted write denies, no deny-default token) — the symmetry contract is one test over both renderings. (2) WrapCmd unit rows: a dummy cmd through WrapCmd carries the rendered profile prefix with the original command as the tail, and Dir/Env/SysProcAttr are byte-identical before/after (the substitution-only contract the three 22-06 call sites rely on). (3) seatbelt_darwin_test.go live battery: render a profile against a temp workdir, exec `sandbox-exec -p <rendered> sh -c 'curl -m 2 -sS <loopback>:1'` expecting connect failure, `touch <outside>` expecting EPERM, `touch <inside tmp>` expecting success — plus the no-temp-file assertion and the degrade path (PATH-scrubbed probe). Commit failing.
GREEN: (1) policy.go: Policy struct + DefaultPolicy (D-04: rw = workdir, os.TempDir-session tmp, workdir/.ass-guard; ro = the documented system set; DenyNetwork always true v1) + LandlockRules() + SeatbeltProfile() rendering the embedded template with path substitution + Handle{Policy, Availability} + WrapCmd(cmd, p) dispatching to the GOOS leg (darwin leg lives in seatbelt_darwin.go; linux leg's argv/env substitution in policy.go — the sentinel env names live here so both legs share constants). (2) seatbelt_darwin.go: embedded template const, RenderProfile, WrapCommand building `sandbox-exec -p <rendered> sh -c <command>` argv, first-use probe + Availability reporting with the one-loud-note degrade. (3) doc.go: the D-05 honesty note verbatim-in-intent — what IS confined (spawned tool processes), what is NOT (ass-guard, in-process tools, MCP host connections), and the MPTCP caveat. Per D-06 no runtime profile artifacts on disk; per SAND-01 targeted denies never deny-default.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/sandbox/ -run 'TestPolicySymmetry|TestSeatbelt' && go vet ./internal/sandbox/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/sandbox/ -run 'TestPolicySymmetry|TestSeatbelt'` exits 0 on the darwin host: golden symmetry rows green; the live battery proves curl connect fails, outside-touch fails with permission denied, inside-touch succeeds.
    - The golden asserts SeatbeltProfile contains allow default and deny network* and contains NO deny-default form (grep-level assertion inside the test).
    - The WrapCmd rows assert the original argv survives as the tail and Dir/Env/SysProcAttr are unmutated — `grep -c 'func WrapCmd' internal/sandbox/policy.go` is 1 (one portable entry).
    - The no-artifacts case asserts zero new files in the workdir/tmp after RenderProfile+WrapCommand.
    - `go vet ./internal/sandbox/` clean and the package builds on darwin without landlock imports (`grep -rc 'go-landlock' internal/sandbox/*_darwin.go internal/sandbox/policy.go` totals 0).
  </acceptance_criteria>
  <done>One policy struct renders symmetric deny sets for both backends behind one portable WrapCmd entry, the macOS profile's denies are proven live on this host, and the D-05 boundary is documented — the D-06/D-04 core of SAND-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linux landlock backend — strict probe, child re-exec sentinel, portable main hook, loud degrade taxonomy</name>
  <files>internal/sandbox/landlock_linux.go, internal/sandbox/landlock_linux_test.go, internal/sandbox/child_linux.go, go.mod, go.sum</files>
  <behavior>
    - ProbeABI taxonomy (unit-testable with a fake prober seam): success returns the negotiated ABI; ENOSYS → Availability{Available:false, Reason:"kernel lacks Landlock"}; EOPNOTSUPP → Reason:"Landlock disabled"; ABI < 4 → Reason:"network restrictions need ABI >= 4" — strict-mode errors only, BestEffort is never the enforcement path (Pitfall 1).
    - ApplyChildRuleset(Policy): builds RestrictPaths (RWDirs rw set, RODirs ro set) + RestrictNet (V4 TCP deny) from the Policy — the same LandlockRules() rows the golden pinned (symmetry holds on linux too).
    - RunSandboxChild: when __ASS_GUARD_SANDBOX_CHILD=1, the process reads __ASS_GUARD_SANDBOX_POLICY (JSON Policy) + the target argv, applies the ruleset, then syscall.Execs the target — the child never returns to ass-guard's normal main path.
    - ApplySandboxChildHook portable contract: returns true only when the sentinel env fired (the linux leg applied-and-exec'd, never returning); on every other GOOS it returns false immediately with zero side effects — main() (22-06 wires the call) can invoke it unconditionally before root-command dispatch.
    - Compile gate: `GOOS=linux CGO_ENABLED=0 go build ./...` passes with the go-landlock dependency (verified cgo-free in RESEARCH) and go.sum carries it; darwin builds never import it.
    - The child's applied domain is inherited by ITS children (kernel clone-inheritance) — the doc comment cites the kernel contract so the group-kill discipline in bash.go/background.go stays the lifecycle owner.
  </behavior>
  <read_first>22-RESEARCH.md (Pattern 4 re-exec + kernel inheritance citations, Code Examples probe idiom + V4 ruleset, Pitfall 1 BestEffort, Pitfall 2 MPTCP, Environment Availability table — no Linux host here, compile gate IS the local proof), pkg.go.dev go-landlock v0.10.0 API notes in RESEARCH Sources, internal/sandbox/policy.go (Task 1 — LandlockRules rows + WrapCmd sentinel constants), 22-PATTERNS.md (landlock_linux.go + child_linux.go — NO in-repo analog; RESEARCH Code Examples are the source), go.mod (module github.com/Djarvur/ass-guard-agent — the re-exec target)</read_first>
  <action>
RED: landlock_linux_test.go (//go:build linux) with a prober seam: table over injected errno outcomes asserting the three-way taxonomy + ABI-floor reason; a ruleset-shape test asserting ApplyChildRuleset derives its rules from LandlockRules() (the same source the darwin golden used — symmetry by shared code, D-06). A child-entry test asserts RunSandboxChild with the sentinel env applies-then-execs (exec replaced via a seam). Commit failing (files are linux-tagged — the RED state is proven by GOOS=linux go test compilation, the darwin host cannot run them).
GREEN: (1) go get github.com/landlock-lsm/go-landlock@v0.10.0 (the RESEARCH-pinned version; legitimacy audited OK — official landlock-lsm org). (2) landlock_linux.go: ProbeABI via the strict-mode version probe with errors.Is errno classification; ApplyChildRuleset with landlock.V4 RestrictPaths/RestrictNet built from Policy — NO BestEffort call anywhere (the prohibition's grep gate lives in the battery); WithRefer stays OFF (RESEARCH: cross-dir rename deny-by-default, symlink-escape mitigation). (3) child_linux.go: RunSandboxChild parsing the policy env (operator/session-controlled — the model never supplies it), apply, syscall.Exec; the linux leg of ApplySandboxChildHook checks the sentinel env and drives RunSandboxChild; the portable false-returning stub for other GOOS lives in policy.go beside WrapCmd. (4) Package doc cross-links the D-05 note. 22-06 wires the main() call and the three exec-site invocations — nothing here touches coreexec/acpserve/cmd.
  </action>
  <verify>
    <automated>GOOS=linux CGO_ENABLED=0 go build ./... && GOOS=linux CGO_ENABLED=0 go vet ./internal/sandbox/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `GOOS=linux CGO_ENABLED=0 go build ./...` exits 0 with go-landlock v0.10.0 in go.mod (`grep -c 'landlock-lsm/go-landlock v0.10.0' go.mod` is 1) — the RESEARCH compile-verified cgo-free claim reproduced.
    - `GOOS=linux CGO_ENABLED=0 go vet ./internal/sandbox/` exits 0 (the linux-tagged files vet clean including tests).
    - `grep -rc 'BestEffort' internal/sandbox/` totals 0 — the silent fail-open API is never used (prohibition grep gate).
    - The taxonomy table pins ENOSYS/EOPNOTSUPP/ABI<4 to distinct Availability reasons, and the ruleset test derives from LandlockRules() (shared source with the darwin golden).
    - On darwin: `go build ./...` exits 0 with zero landlock imports outside linux-tagged files.
  </acceptance_criteria>
  <done>Linux confinement compiles cgo-free against go-landlock v0.10.0, probes strictly with a loud three-way taxonomy, applies child-only rulesets derived from the ONE policy struct, and the re-exec sentinel + portable main hook make ass-guard's own binary the loader — the package is ready for 22-06 to wire.</done>
  <precondition>Linux runtime verification environment: the landlock enforcement battery needs a Linux kernel ≥5.13 host or CI to RUN (this darwin dev host cannot). Assert before relying on runtime results: the compile gate passes locally AND the runtime battery is tagged for linux execution — enforcement proof otherwise lands in the verify phase on an operator/CI Linux host (RESEARCH Environment Availability).</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → Bash command → confined child | Model-authored commands will execute under the operator-enabled sandbox (wiring in 22-06) |
| sandbox profile → child syscalls | The ruleset boundary is the last control before arbitrary execution |
| probe result → availability state | A false-negative probe silently disables the safety control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-19 | Information Disclosure | curl-style exfil from a confined tool | high | mitigate | D-04 network deny both backends (landlock RestrictNet V4 / sandbox-exec deny network*) — live-verified in RESEARCH and re-pinned by Task 1's live battery |
| T-22-20 | Elevation of Privilege | silent fail-open on unsupported kernels | high | mitigate | Strict-mode probe (never BestEffort) with the three-way taxonomy and distinct Availability reasons (Task 2 battery); 22-06 owns the startup warning + per-run unconfined notes consuming these reasons |
| T-22-21 | Elevation of Privilege | privilege regain via SUID inside the sandbox | medium | mitigate | go-landlock sets PR_SET_NO_NEW_PRIVS with every Restrict* call (RESEARCH security table); seatbelt profile rides sandbox-exec's own no-suid semantics — documented in policy.go |
| T-22-22 | Tampering | symlink/rename escape inside the workdir | medium | mitigate | WithRefer stays OFF (ABI≥2 cross-dir rename denied by default, RESEARCH security table); rw set is the narrow D-04 triple |
| T-22-23 | Spoofing | policy env tampering by the child itself | low | mitigate | The sentinel + policy env are set by ass-guard at spawn; the child is already confined when it could read them — post-apply reads are moot (landlock is irreversible per kernel contract) |
| T-22-25 | Denial of Service | sandbox-exec disappears from a future macOS | low | mitigate | First-use probe + loud degrade is the designed behavior (State of the Art row); feature stays runnable unconfined with notes |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/sandbox/` green — the package batteries on the darwin host (symmetry golden + live seatbelt).
- `GOOS=linux CGO_ENABLED=0 go build ./... && GOOS=linux CGO_ENABLED=0 go vet ./internal/sandbox/` — the linux compile gate (runtime landlock enforcement needs a Linux host; precondition-marked in Task 2).
- `go build ./...` on darwin exits 0 with zero landlock imports outside linux-tagged files.
</verification>

<success_criteria>
- ONE Policy struct renders symmetric deny sets to both backends; D-04's rw/ro/deny-network set is the golden (D-06).
- ONE portable WrapCmd entry exists for every exec site 22-06 wires; substitution-only contract pinned (D-05, D-06).
- The probe taxonomy is strict and loud with distinct Availability reasons; never BestEffort (SAND-01).
- Children are confined, ass-guard is not, and the boundary is documented honestly in doc.go (D-05).
- Probe row assigned to this plan (SAND-01 unclassified) is authored as the manual-review flagged assumption with live/compile battery dispositions — zero silent drops.
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-05-SUMMARY.md` when done
</output>
