---
phase: 22-background-execution-sandbox-reality
plan: 05
type: execute
wave: 4
depends_on: ["22-04"]
files_modified:
  - internal/sandbox/policy.go
  - internal/sandbox/policy_test.go
  - internal/sandbox/landlock_linux.go
  - internal/sandbox/landlock_linux_test.go
  - internal/sandbox/child_linux.go
  - internal/sandbox/seatbelt_darwin.go
  - internal/sandbox/seatbelt_darwin_test.go
  - internal/coreexec/bash.go
  - internal/coreexec/bash_test.go
  - internal/coreexec/register.go
  - internal/acpserve/options.go
  - internal/acpserve/acp_serve.go
  - internal/acpserve/serve_test.go
  - cmd/ass-guard/acp_serve.go
autonomous: true
requirements: [SAND-01]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ONE policy struct feeds BOTH sandbox backends: the same Policy renders a landlock ruleset description and a seatbelt profile whose deny sets are symmetric — drift between OSes is impossible by construction (D-06, SAND-01)"
    - "With --sandbox=on, Bash tool processes run confined: rw on workdir + tmp + .ass-guard, ro on the system path set, network denied — a curl-style exfil from a confined tool fails (D-04)"
    - "Confinement applies to SPAWNED tool processes only; ass-guard itself, in-process tools, and MCP host connections stay unconfined — the boundary is documented in the package doc as the D-05 honesty note"
    - "An unsupported environment degrades LOUDLY: the startup probe distinguishes ENOSYS (kernel lacks Landlock) / EOPNOTSUPP (disabled) / missing sandbox-exec, emits ONE stderr warning + counter, marks the sandbox unavailable, and every unconfined run is noted — never a silent fail-open (SAND-01, Pitfall 1)"
    - "The sandbox default is OFF and --sandbox=off always escapes — no tool process is ever confined unless the operator asked (SAND-01)"
    - "dangerouslyDisableSandbox=true with the sandbox on runs the command unconfined with a loud stderr note + counter — the captured contract acquires real meaning (OQ2 resolution)"
    - "macOS confinement rides an embedded .sb template rendered IN MEMORY (sandbox-exec -p) with targeted denies over (allow default) — never deny-default, never a runtime profile artifact on disk (D-06, SAND-01)"
  artifacts:
    - path: "internal/sandbox/policy.go"
      provides: "Policy{RWPaths, ROSysPaths []string, DenyNetwork bool}; DefaultPolicy(workDir, tmpDir, guardDir) with the D-04 path set; LandlockRules() []Rule (V4-shape description consumed by the linux backend); SeatbeltProfile() string (in-memory template render); the D-05 confinement-boundary doc"
      contains: "func DefaultPolicy"
    - path: "internal/sandbox/landlock_linux.go"
      provides: "//go:build linux ProbeABI() (int, error) — strict-mode probe with the ENOSYS/EOPNOTSUPP taxonomy (never BestEffort enforcement); ApplyChildRuleset(Policy) building RestrictPaths+RestrictNet V4 inside the re-exec child"
      contains: "ProbeABI"
    - path: "internal/sandbox/child_linux.go"
      provides: "//go:build linux the re-exec sentinel entrypoint: __ASS_GUARD_SANDBOX_CHILD=1 + __ASS_GUARD_SANDBOX_POLICY (JSON Policy) → apply ruleset → syscall.Exec the real target — ass-guard's own binary is the sandbox loader (Pattern 4)"
      contains: "RunSandboxChild"
    - path: "internal/sandbox/seatbelt_darwin.go"
      provides: "//go:build darwin embedded .sb template + RenderProfile(Policy) + WrapCommand(cmd, policy, command) via sandbox-exec -p; first-use probe with loud degrade"
      contains: "WrapCommand"
    - path: "internal/coreexec/bash.go (MOD)"
      provides: "the sandbox wrap at the exec site: enabled+not-disabled → backend argv substitution (linux re-exec self / darwin sandbox-exec -p); dangerouslyDisableSandbox honored with loud note (OQ2); Config gains the resolved Sandbox policy handle"
      contains: "sandbox"
    - path: "internal/acpserve/options.go + acp_serve.go + cmd/ass-guard/acp_serve.go (MOD)"
      provides: "Options.SandboxMode string (off|on); the --sandbox cobra flag defaulting to off; the startup probe step in Run's pipeline storing availability for the executors"
      contains: "SandboxMode"
  key_links:
    - from: "cmd/ass-guard/acp_serve.go --sandbox flag"
      to: "internal/acpserve/acp_serve.go startup probe"
      via: "flag → Options.SandboxMode → Run's probe step (before scheduler start, beside the 22-02 sweep) → availability recorded once for the process"
      pattern: "startup probe"
    - from: "internal/coreexec/bash.go exec site"
      to: "internal/sandbox backends"
      via: "coreexec.Config carries the resolved Policy + availability; the wrap substitutes argv before the existing killGroupOnCtx/group discipline — confinement never touches the group-kill idiom"
      pattern: "argv substitution"
  prohibitions:
    - requirement_id: SAND-01
      category: safety
      statement: "The sandbox MUST never fail open silently — a probe failure (ENOSYS/EOPNOTSUPP/missing sandbox-exec) is announced once, loudly, at startup with a stderr warning + counter, and every command that runs unconfined because of it is individually noted; a green tool result must never imply confinement that did not happen"
      status: unresolved
    - requirement_id: SAND-01
      category: safety
      statement: "Confinement MUST never be applied to the ass-guard process itself — landlock is process-wide and would sever provider/MCP networking; rulesets apply in the child before exec, children only (D-05)"
      status: unresolved

---

<objective>
Make the sandbox flag real (SAND-01): ONE policy struct driving symmetric landlock (Linux ≥5.13, go-landlock v0.10.0, child-applied via the re-exec sentinel) and sandbox-exec (macOS, embedded in-memory .sb template, targeted denies never deny-default) backends, probe-and-degrade loudly at startup, default OFF, --sandbox=off escape, and the captured dangerouslyDisableSandbox contract honored (OQ2).

Purpose: D-04/D-05/D-06 lock the deny set, the child-only boundary, and the template symmetry; bubbleware stays demoted (opt-in stronger isolation is out of this phase's scope).
Output: internal/sandbox package + bash.go wrap + flag plumbing, darwin legs live-tested on this host, linux legs compile-gated.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/22-background-execution-sandbox-reality/22-RESEARCH.md
@.planning/phases/22-background-execution-sandbox-reality/22-PATTERNS.md
@internal/coreexec/bash.go
@internal/acpserve/acp_serve.go
@cmd/ass-guard/acp_serve.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/sandbox` (NEW package) — Policy/DefaultPolicy/LandlockRules/SeatbeltProfile (policy.go, portable); ProbeABI/ApplyChildRuleset (landlock_linux.go, //go:build linux); RunSandboxChild (child_linux.go, //go:build linux); RenderProfile/WrapCommand (seatbelt_darwin.go, //go:build darwin); Availability type {Mode string, Available bool, Reason string}.
- `internal/coreexec/bash.go` (MOD) — the sandbox wrap branch at the exec site; `bashArgs.DangerouslyDisableSandbox` acquires real semantics (OQ2).
- `internal/coreexec/register.go` (MOD) — Config gains the sandbox handle (policy + availability) plumbed to the Bash stub.
- `internal/acpserve/options.go` (MOD) — `Options.SandboxMode string`.
- `internal/acpserve/acp_serve.go` (MOD) — `resolveSandboxAvailability(opts) sandbox.Availability` startup probe step in Run's pipeline (before scheduler start).
- `cmd/ass-guard/acp_serve.go` (MOD) — `c.Flags().StringVar(&sandboxMode, "sandbox", "off", ...)` validated to {off,on}.
- go.mod — `github.com/landlock-lsm/go-landlock v0.10.0` (import lives ONLY in //go:build linux files).
- Tests — policy_test.go (symmetry golden), seatbelt_darwin_test.go (live probe, //go:build darwin), landlock_linux_test.go (degrade taxonomy, //go:build linux), bash_test.go (wrap + escape + disable-flag), serve_test.go (flag + probe pipeline).
</artifacts_this_phase_produce>

<flagged_assumptions>
- **SAND-01 edge-probe `unclassified` row (status: unresolved — surfaced, never auto-resolved):** the probe row is "review manually" — the manual review is this plan: seatbelt rule semantics are undocumented by Apple (RESEARCH A7), so the shipped profile's actual denies are pinned by the LIVE probe test on this darwin host (network deny breaks a real curl; write deny breaks a real touch), and the landlock ruleset's enforcement is pinned on a Linux host by the same battery shape (skipped-by-tag here). The ro-system path set is per-distro discretion (A8) — DefaultPolicy ships a documented macOS/Linux set and the loud-degrade contract covers gaps. Flagged until the verify phase confirms both batteries.
- **Re-exec sentinel pattern (RESEARCH A2, MEDIUM):** os/exec has no pre-exec child hook, so the Linux child is ass-guard's own binary re-invoked with __ASS_GUARD_SANDBOX_CHILD=1; if review rejects the pattern the fallback is a tiny internal helper binary — same semantics, more surface. Documented in child_linux.go's doc comment.
- **MPTCP blind spot (Pitfall 2):** a confined Go ≥1.24 child could bind via MPTCP outside the TCP rules — documented in the D-05 honesty note; non-Go children (the normal case) are unaffected on connect.
- **OQ2 RESOLVED in-plan (honor the captured contract):** dangerouslyDisableSandbox=true runs the command unconfined with ONE loud stderr note + a counter — the captured description promises exactly this and CC parity backs it (RESEARCH Open Question 2 recommendation adopted). The operator enabled the sandbox; the model's explicit per-call escape is visible, never silent.
- **--sandbox vocabulary (discretion):** the flag accepts off|on only (default off); stronger-isolation backends (bubblewrap) stay out of the vocabulary per SAND-01's demotion.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Policy struct + symmetry golden + darwin seatbelt backend (live-probed)</name>
  <files>internal/sandbox/policy.go, internal/sandbox/policy_test.go, internal/sandbox/seatbelt_darwin.go, internal/sandbox/seatbelt_darwin_test.go, internal/sandbox/doc.go</files>
  <behavior>
    - Policy symmetry golden: the same DefaultPolicy(workDir, tmp, guard) renders a LandlockRules() list whose rw set is exactly {workdir, tmp, .ass-guard} with DenyNetwork true, AND a SeatbeltProfile() string containing (allow default), (deny network*), and write-denies OUTSIDE the rw set — one policy, two symmetric renderings (D-06).
    - SeatbeltProfile never contains deny-default: the profile's first rule is (allow default) and denies are targeted subpath/network forms (SAND-01 letter, pinned by golden assertion).
    - RenderProfile substitutes the session's real paths into the embedded template IN MEMORY; no temp file is created (D-06 no-artifacts rule — battery asserts no file writes).
    - LIVE darwin probe test (//go:build darwin, runs on this host): a child under WrapCommand with the rendered profile fails a curl connect (network deny) and fails a touch outside the rw set (write deny), while a touch INSIDE the tmp rw path succeeds — the RESEARCH-verified mechanism pinned as a test.
    - First-use probe: WrapCommand probes sandbox-exec presence/-p acceptance; absence or failure → Availability{Available:false, Reason} + one stderr note, command runs unconfined (degrade loudly, never refuse).
  </behavior>
  <read_first>22-RESEARCH.md IN FULL for this task (Pattern 4 Policy struct + the live-verified .sb profile text + Code Examples darwin render/invoke, Pitfall 1 BestEffort, the Package Legitimacy rows), 22-PATTERNS.md (seatbelt_darwin.go assignment — degrade discipline from acp_serve.go seedACPGuard :75-88; policy.go role-match to bashArgs documented-const style), 22-CONTEXT.md (D-04, D-05, D-06), internal/acpserve/acp_serve.go :75-88 (the degrade idiom to copy)</read_first>
  <action>
RED: (1) policy_test.go golden: DefaultPolicy → LandlockRules() row set assertions (rw/ro/deny-network) AND SeatbeltProfile() content assertions (allow-default first, deny network*, targeted write denies, no deny-default token) — the symmetry contract is one test over both renderings. (2) seatbelt_darwin_test.go live battery: render a profile against a temp workdir, exec `sandbox-exec -p <rendered> sh -c 'curl -m 2 -sS <loopback>:1'` expecting connect failure, `touch <outside>` expecting EPERM, `touch <inside tmp>` expecting success — plus the no-temp-file assertion and the degrade path (PATH-scrubbed probe). Commit failing.
GREEN: (1) policy.go: Policy struct + DefaultPolicy (D-04: rw = workdir, os.TempDir-session tmp, workdir/.ass-guard; ro = the documented system set; DenyNetwork always true v1) + LandlockRules() + SeatbeltProfile() rendering the embedded template with path substitution; package doc (doc.go) carries the D-05 honesty note verbatim-in-intent: what IS confined (spawned tool processes), what is NOT (ass-guard, in-process tools, MCP host connections), and the MPTCP caveat. (2) seatbelt_darwin.go: embedded template const, RenderProfile, WrapCommand building `sandbox-exec -p <rendered> sh -c <command>` argv, first-use probe + Availability reporting with the one-loud-note degrade. Per D-06 no runtime profile artifacts on disk; per SAND-01 targeted denies never deny-default.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/sandbox/ -run 'TestPolicySymmetry|TestSeatbelt'</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/sandbox/ -run 'TestPolicySymmetry|TestSeatbelt'` exits 0 on the darwin host: golden symmetry rows green; the live battery proves curl connect fails, outside-touch fails with permission denied, inside-touch succeeds.
    - The golden asserts SeatbeltProfile contains allow default and deny network* and contains NO deny-default form (grep-level assertion inside the test).
    - The no-artifacts case asserts zero new files in the workdir/tmp after RenderProfile+WrapCommand.
    - `go vet ./internal/sandbox/` clean and the package builds on darwin without landlock imports (`grep -rc 'go-landlock' internal/sandbox/*_darwin.go internal/sandbox/policy.go` totals 0).
  </acceptance_criteria>
  <done>One policy struct renders symmetric deny sets for both backends, the macOS profile's denies are proven live on this host, and degradation is loud — the D-06/D-04 core of SAND-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linux landlock backend — strict probe, child re-exec sentinel, loud degrade taxonomy</name>
  <files>internal/sandbox/landlock_linux.go, internal/sandbox/landlock_linux_test.go, internal/sandbox/child_linux.go, go.mod, go.sum</files>
  <behavior>
    - ProbeABI taxonomy (unit-testable with a fake prober seam): success returns the negotiated ABI; ENOSYS → Availability{Available:false, Reason:"kernel lacks Landlock"}; EOPNOTSUPP → Reason:"Landlock disabled"; ABI < 4 → Reason:"network restrictions need ABI >= 4" — strict-mode errors only, BestEffort is never the enforcement path (Pitfall 1).
    - ApplyChildRuleset(Policy): builds RestrictPaths (RWDirs rw set, RODirs ro set) + RestrictNet (V4 TCP deny) from the Policy — the same LandlockRules() rows the golden pinned (symmetry holds on linux too).
    - RunSandboxChild: when __ASS_GUARD_SANDBOX_CHILD=1, the process reads __ASS_GUARD_SANDBOX_POLICY (JSON Policy) + the target argv, applies the ruleset, then syscall.Execs the target — the child never returns to ass-guard's normal main path.
    - Compile gate: `GOOS=linux CGO_ENABLED=0 go build ./...` passes with the go-landlock v1.62-era v0.10.0 dependency (verified cgo-free in RESEARCH) and go.sum carries it; darwin builds never import it.
    - The child's applied domain is inherited by ITS children (kernel clone-inheritance) — the doc comment cites the kernel contract so the group-kill discipline in bash.go stays the lifecycle owner.
  </behavior>
  <read_first>22-RESEARCH.md (Pattern 4 re-exec + kernel inheritance citations, Code Examples probe idiom + V4 ruleset, Pitfall 1 BestEffort, Pitfall 2 MPTCP, Environment Availability table — no Linux host here, compile gate IS the local proof), pkg.go.dev go-landlock v0.10.0 API notes in RESEARCH Sources, internal/sandbox/policy.go (Task 1 — LandlockRules rows), 22-PATTERNS.md (landlock_linux.go + child_linux.go — NO in-repo analog; RESEARCH Code Examples are the source), go.mod (module github.com/Djarvur/ass-guard-agent — the re-exec target)</read_first>
  <action>
RED: landlock_linux_test.go (//go:build linux) with a prober seam: table over injected errno outcomes asserting the three-way taxonomy + ABI-floor reason; a ruleset-shape test asserting ApplyChildRuleset derives its rules from LandlockRules() (the same source the darwin golden used — symmetry by shared code, D-06). A child-entry test asserts RunSandboxChild with the sentinel env applies-then-execs (exec replaced via a seam). Commit failing (files are linux-tagged — the RED state is proven by GOOS=linux go test compilation, the darwin host cannot run them).
GREEN: (1) go get github.com/landlock-lsm/go-landlock@v0.10.0 (the RESEARCH-pinned version; legitimacy audited OK — official landlock-lsm org). (2) landlock_linux.go: ProbeABI via the strict-mode version probe with errors.Is errno classification; ApplyChildRuleset with landlock.V4 RestrictPaths/RestrictNet built from Policy — NO BestEffort call anywhere (the prohibition's grep gate lives in the battery); WithRefer stays OFF (RESEARCH: cross-dir rename deny-by-default, symlink-escape mitigation). (3) child_linux.go: RunSandboxChild parsing the policy env (operator/session-controlled — the model never supplies it), apply, syscall.Exec; the main-path hook that detects the sentinel env joins cmd's re-exec wrap (Task 3 wires the invocation). (4) Package doc cross-links the D-05 note.
  </action>
  <verify>
    <automated>GOOS=linux CGO_ENABLED=0 go build ./... && GOOS=linux CGO_ENABLED=0 go vet ./internal/sandbox/ && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `GOOS=linux CGO_ENABLED=0 go build ./...` exits 0 with go-landlock v0.10.0 in go.mod (`grep -c 'landlock-lsm/go-landlock v0.10.0' go.mod` is 1) — the RESEARCH compile-verified cgo-free claim reproduced.
    - `GOOS=linux CGO_ENABLED=0 go vet ./internal/sandbox/` exits 0 (the linux-tagged files vet clean including tests).
    - `grep -rc 'BestEffort' internal/sandbox/` totals 0 — the silent fail-open API is never used (prohibition grep gate).
    - The taxonomy table pins ENOSYS/EOPNOTSUPP/ABI<4 to distinct Availability reasons, and the ruleset test derives from LandlockRules() (shared source with the darwin golden).
    - On darwin: `go build ./...` exits 0 with zero landlock imports outside linux-tagged files.
  </acceptance_criteria>
  <done>Linux confinement compiles cgo-free against go-landlock v0.10.0, probes strictly with a loud three-way taxonomy, applies child-only rulesets derived from the ONE policy struct, and the re-exec sentinel makes ass-guard's own binary the loader.</done>
  <precondition>Linux runtime verification environment: the landlock enforcement battery needs a Linux kernel ≥5.13 host or CI to RUN (this darwin dev host cannot). Assert before relying on runtime results: the compile gate passes locally AND the runtime battery is tagged for linux execution — enforcement proof otherwise lands in the verify phase on an operator/CI Linux host (RESEARCH Environment Availability).</precondition>
</task>

<task type="auto" tdd="true">
  <name>Task 3: --sandbox flag plumbing + bash.go wrap + dangerouslyDisableSandbox honored (OQ2) + default OFF</name>
  <files>cmd/ass-guard/acp_serve.go, internal/acpserve/options.go, internal/acpserve/acp_serve.go, internal/acpserve/serve_test.go, internal/coreexec/bash.go, internal/coreexec/bash_test.go, internal/coreexec/register.go</files>
  <behavior>
    - Default OFF: with no --sandbox flag, zero tool processes are confined — a battery asserts the exec argv for a plain Bash call is byte-identical with the sandbox code present but unavailable (Availability off).
    - --sandbox=on → the startup probe step runs in Run's pipeline (before scheduler start, beside the 22-02 sweep) and records Availability once; on darwin the wrapped Bash call executes under sandbox-exec with the rendered profile (live battery reuses Task 1's probe shape through the full flag path).
    - --sandbox=on on a probe-failed environment (faked) → ONE loud stderr warning at startup, availability marked false, and every Bash call runs unconfined with a per-run stderr note + counter (never a silent fail-open — the plan prohibition).
    - --sandbox=off always escapes even when a policy is present (the escape hatch letter).
    - dangerouslyDisableSandbox=true with sandbox on → the command runs UNCONFINED with one loud stderr note incrementing a counter; with sandbox off it behaves as today's documented no-op (OQ2 resolution, both arms pinned).
    - The wrap substitutes argv BEFORE the existing group discipline: killGroupOnCtx/Setpgid/reapGroup operate on the wrapped child exactly as on the bare sh — group semantics preserved (D-05: children confined; the group-kill still reaches them).
    - Invalid --sandbox values (not off|on) fail the cobra flag validation at startup, never mid-serve.
  </behavior>
  <read_first>cmd/ass-guard/acp_serve.go :33-115 (newACPServeCmd flags :68-75, runACPServeCmd building acpserve.Options :111), internal/acpserve/options.go :12-25 (the Options struct gaining SandboxMode), internal/acpserve/acp_serve.go (Run pipeline :121-252 — the probe step joins beside the 22-02 sweep, before scheduler; the degrade doc-comment contract :28-32), internal/coreexec/bash.go :47-71 (bashArgs — DangerouslyDisableSandbox already parses; the :57-64 documented-no-op comment this task retires), internal/coreexec/bash.go :278-304 (the exec path the wrap encloses; killGroupOnCtx discipline), internal/coreexec/register.go (Config plumbing), 22-RESEARCH.md (Open Question 2, Pitfall 1, State of the Art sandbox-exec row), 22-PATTERNS.md (bash.go + acp_serve.go assignments)</read_first>
  <action>
RED: (1) serve_test.go: flag validation rows (off/on ok; "strict" rejected), pipeline-order row (probe before scheduler start), faked-probe-failure startup row asserting exactly one warning line + availability false. (2) bash_test.go: default-OFF argv identity; sandbox-on darwin live wrap (reuses Task 1's curl/touch shape through the executor); --sandbox=off escape; disable-flag arms (on→unconfined+note, off→no-op unchanged); group-discipline case asserting the wrapped child still dies via the existing Stop path. Commit failing.
GREEN: (1) cmd/ass-guard/acp_serve.go: --sandbox StringVar default "off" with cobra validation to {off,on}; pass into Options.SandboxMode. (2) acp_serve.go: resolveSandboxAvailability step in Run's pipeline — darwin probes sandbox-exec (Task 1's probe), linux calls ProbeABI (Task 2), off short-circuits to Availability{Mode:"off", Available:false}; store the Availability in the composition for coreexec.Config; one stderr warning on enabled-but-unavailable. (3) bash.go: at the exec site, if Availability.Available and not a.DangerouslyDisableSandbox → substitute argv per GOOS (linux: os.Executable() with the sentinel + policy env; darwin: sandbox-exec -p rendered); if disabled-while-on → the loud per-run note + counter; retire the :57-64 no-op comment and document the OQ2 honored semantics; --sandbox=off leaves every path untouched. (4) register.go Config plumbing for the policy + availability.
Per SAND-01 default OFF and the escape hatch; per D-05 only children are wrapped; per OQ2 the captured contract is honored loudly.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ -run 'TestSandboxFlag|TestStaleSweep' && go test -race -count=1 ./internal/coreexec/ -run 'TestBashSandbox' && go test -race -count=1 ./internal/coreexec/ ./internal/acpserve/ ./internal/sandbox/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/acpserve/ -run 'TestSandboxFlag|TestStaleSweep'` exits 0: flag validation, probe-before-scheduler ordering, one-warning degrade on faked probe failure.
    - `go test -race -count=1 ./internal/coreexec/ -run 'TestBashSandbox'` exits 0 on darwin: default-OFF argv identity; live wrapped call denies network/write and allows the rw tmp; escape hatch; both disable-flag arms; group-kill reaches the wrapped child.
    - `grep -c 'dangerouslyDisableSandbox' internal/coreexec/bash.go` is >= 2 (parse + honored semantics) and the retired no-op comment is gone (the comment no longer states the flag is ignored — grep for the old no-op phrasing returns 0).
    - `go test -race -count=1 ./internal/coreexec/ ./internal/acpserve/ ./internal/sandbox/` exits 0 and `GOOS=linux CGO_ENABLED=0 go build ./...` stays green.
  </acceptance_criteria>
  <done>The sandbox is real and honest: off by default, loud when unsupported, per-call escapable by the captured flag, confining only the children — with the group-kill lifecycle fully preserved underneath.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → Bash command → confined child | Model-authored commands execute under the operator-enabled sandbox |
| sandbox profile → child syscalls | The ruleset boundary is the last control before arbitrary execution |
| probe result → availability state | A false-negative probe silently disables the safety control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-19 | Information Disclosure | curl-style exfil from a confined tool | high | mitigate | D-04 network deny both backends (landlock RestrictNet V4 / sandbox-exec deny network*) — live-verified in RESEARCH and re-pinned by Task 1's live battery |
| T-22-20 | Elevation of Privilege | silent fail-open on unsupported kernels | high | mitigate | Strict-mode probe (never BestEffort), three-way taxonomy, ONE loud startup warning + per-run unconfined notes + counter (Task 2/3 batteries; the plan prohibition) |
| T-22-21 | Elevation of Privilege | privilege regain via SUID inside the sandbox | medium | mitigate | go-landlock sets PR_SET_NO_NEW_PRIVS with every Restrict* call (RESEARCH security table); seatbelt profile rides sandbox-exec's own no-suid semantics — documented in policy.go |
| T-22-22 | Tampering | symlink/rename escape inside the workdir | medium | mitigate | WithRefer stays OFF (ABI≥2 cross-dir rename denied by default, RESEARCH security table); rw set is the narrow D-04 triple |
| T-22-23 | Spoofing | policy env tampering by the child itself | low | mitigate | The sentinel + policy env are set by ass-guard at spawn; the child is already confined when it could read them — post-apply reads are moot (landlock is irreversible per kernel contract) |
| T-22-24 | (accepted) | model-authored command executes anything when sandbox OFF (default) | high | accept | T-8-33 posture unchanged — the model's command IS the product; the sandbox is an operator opt-in bounding the blast radius, not a confirmation tier (SAND-01 default OFF is the operator's locked choice) |
| T-22-25 | Denial of Service | sandbox-exec disappears from a future macOS | low | mitigate | First-use probe + loud degrade is the designed behavior (State of the Art row); feature stays runnable unconfined with notes |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/coreexec/ ./internal/acpserve/ ./internal/sandbox/` green (phase quick command subset).
- `GOOS=linux CGO_ENABLED=0 go build ./... && GOOS=linux CGO_ENABLED=0 go vet ./internal/sandbox/` — the linux compile gate (runtime landlock enforcement needs a Linux host; precondition-marked in Task 2).
- The darwin live battery (curl connect denied, outside-write denied, inside-write allowed) proves the seatbelt mechanism on this host.
- Phase gate (per RESEARCH Sampling Rate): full suite green + compile gate before /gsd-verify-work.
</verification>

<success_criteria>
- ONE Policy struct renders symmetric deny sets to both backends; D-04's rw/ro/deny-network set is the golden (D-06).
- Children are confined, ass-guard is not, and the boundary is documented honestly (D-05).
- Probe-and-degrade is loud with a three-way taxonomy; default is OFF; --sandbox=off always escapes; dangerouslyDisableSandbox is honored with notes (SAND-01, OQ2).
- Probe row assigned to this plan (SAND-01 unclassified) is authored as the manual-review flagged assumption with live/compile battery dispositions — zero silent drops.
</success_criteria>

<output>
Create `.planning/phases/22-background-execution-sandbox-reality/22-05-SUMMARY.md` when done
</output>
