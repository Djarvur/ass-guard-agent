---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 07
type: execute
wave: 7
depends_on: ["25-06"]
files_modified:
  - "kit/runtime/toolkit.go"
  - "kit/runtime/runtime.go"
  - "internal/acpserve/toolkit.go"
  - "internal/acpserve/acp_serve.go"
autonomous: true
requirements: [KIT-01, KIT-02, KIT-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The kit obtains per-session core tool executors ONLY through the SessionToolkit seam — one coarse Attach method receiving the per-session catalog plus a ToolkitEnv carrying exactly the inputs sessionFor passes today (dir, session id, transcript path, kit Hooks, plan-mode state, ask broker, Scheduler port) and returning a Reaper handle (OQ1 resolution: coarse, not per-family)"
    - "kit/runtime imports NO app package — all six runtime edges severed (acp in 25-04; sched/learning/openspec in 25-05; ecosys in 25-06; coreexec here); the D-19 gate in 25-09 becomes enforceable"
    - "coreexec construction (task registry, RegisterCore, RegisterAsk, mailbox, session reader, RegisterInteractive) lives in internal/acpserve/toolkit.go verbatim-in-role; coreexec itself stays app-side untouched (KIT-03)"
    - "nil Toolkit degrades to the documented stub-executor path (enginebridge.NewStubCatalogExec) — the exact existing degraded behavior, never a panic; the 25-09 hostproof test rides this path"
    - "OnClose composition preserved: writer cancel, toolkit Reaper, MCP host close — the 12-06 invariant (no background group outlives the session) holds through the Reaper handle"
  artifacts:
    - path: "kit/runtime/toolkit.go"
      provides: "SessionToolkit interface + ToolkitEnv + Reaper — the coreexec injection seam (OQ1 coarse shape)"
    - path: "internal/acpserve/toolkit.go"
      provides: "The app-side toolkit: coreexec registration family + AskSurface renderer + Reaper over the task registry"
  key_links:
    - from: "kit/runtime"
      to: "internal/coreexec"
      via: "NONE after this plan — sessionFor calls the injected toolkit's Attach; the stub-exec nil path remains the degraded arm"
      pattern: "SessionToolkit|Attach"
    - from: "internal/acpserve/toolkit.go"
      to: "kit/runtime/toolkit.go"
      via: "the app toolkit implements Attach and returns the Reaper that OnClose invokes"
      pattern: "Reaper|ReapAll|Reap"
---

<objective>
Sever the last kit→app edge — runtime→coreexec — via the SessionToolkit injection seam (the coarse Attach shape resolved in OQ1), completing the pass-2 seam family: Emitter, Requester, Scheduler, LearnedStore, CommandCatalog, SessionToolkit.

Purpose: core tool executors (Bash/Read/Write/Edit/Todo/Ask/Interactive) are app domain (KIT-03); the kit needs only "register per-session tools and give me a reaper". After this plan the kit tree imports nothing from internal/ or cmd/ — the state D-19's gate enforces in 25-09.

Output: kit/runtime/toolkit.go; internal/acpserve/toolkit.go; zero app imports under kit/ (non-test); mise ci green with the coreexec wiring batteries passing.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-06-SUMMARY.md
@kit/runtime/runtime.go
@internal/coreexec/
</context>

<tasks>

<task type="auto">
  <name>Task 1: SessionToolkit seam kit-side — coarse Attach + ToolkitEnv + Reaper (OQ1)</name>
  <files>kit/runtime/toolkit.go (new), kit/runtime/runtime.go</files>
  <read_first>kit/runtime/runtime.go:1107-1140 (the per-session toolkit construction site — the exact env inputs: dir, sessionID, mgr.Path() transcript path, hook runner, task registry, ask broker, plan mode, mailbox, session reader, schedule) and :1218-1223 (the engine-off stub-executor degraded path) and :1238-1243 (OnClose composing taskRegistry.ReapAll), kit/session/ask.go (the broker the env carries — kit type), 25-RESEARCH.md Open Question 1 + §Pattern 4 SessionToolkit row + the hostproof reliance on the nil path, 25-CONTEXT.md D-04/D-10/D-12, 25-06-SUMMARY.md (the kit Hooks/AgentDef types the env carries)</read_first>
  <action>
    1. OQ1 resolution — COARSE shape: define SessionToolkit in kit/runtime/toolkit.go as ONE method, Attach, receiving the per-session tool catalog (kit/toolcat type) and a ToolkitEnv struct, returning a Reaper and error. Do NOT split into per-family methods (core/ask/interactive): the kit's only need is register-and-reap; finer methods would freeze coreexec's internal family structure into the public API against D-12. Record the split-later trigger in the doc comment: only a kit-only consumer needing PARTIAL toolkits justifies per-family methods.
    2. ToolkitEnv fields mirror today's arguments exactly, kit-typed only: work dir, session id, transcript path, the kit Hooks interface (from 25-06), plan-mode state (kit/session type), the ask broker (kit/session type — the broker stays kit-side; the env passes it so the toolkit's RegisterAsk wiring binds the same broker), and the Scheduler port (25-05's interface — the env carries the port; the app toolkit holds its own concrete store reference for coreexec's InteractiveConfig). The ask-surface RENDERER (what surfaces a pending ask's questions) is NOT in the env: it is a RunnerConfig func-field (mirroring the MakeProvider precedent, D-10) that sessionFor uses when constructing the broker's onSurface callback — nil renderer degrades to no surface chunk (documented; asks still function).
    3. Reaper: a minimal interface with one method (reap all session task groups); doc comment pins the 12-06 invariant (no background group outlives the session).
    4. Rewire sessionFor: when the toolkit is set, call Attach and wire OnClose to include the Reaper; when nil, take the EXISTING stub-executor degraded path unchanged. Delete the direct coreexec construction from sessionFor (it moves to Task 2). Keep the writer-cancel and MCP-host-close OnClose legs exactly as they are.
  </action>
  <verify>
    <automated>go build ./kit/... && go test ./kit/runtime/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - kit/runtime/toolkit.go defines SessionToolkit (one Attach method), ToolkitEnv (kit-typed fields mirroring today's inputs), Reaper (one method), all with contract doc comments naming implementor and nil degradation
    - No app type appears in SessionToolkit/ToolkitEnv/Reaper signatures (Pitfall 3)
    - sessionFor uses the seam; nil toolkit hits the existing stub-exec path; OnClose gains the Reaper leg without losing cancel/close legs
    - The ask-surface renderer is a RunnerConfig func-field with documented nil degradation
  </acceptance_criteria>
  <done>The coreexec seam exists kit-side in its coarse OQ1 shape; the kit defines the contract, the app will fill it.</done>
  <tdd_eligibility>Interface extraction over behavior pinned by relocated wiring tests. TDD-ineligible with rationale.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 2: App-side toolkit — coreexec registration relocated, last edge severed</name>
  <files>internal/acpserve/toolkit.go (new), internal/acpserve/acp_serve.go, kit/runtime/runtime.go</files>
  <read_first>kit/runtime/runtime.go:1107-1140 (the construction sequence being relocated: NewTaskRegistry, RegisterCore with Config, RegisterAsk with the broker, NewAgentMailbox, NewSessionReader, RegisterInteractive with InteractiveConfig), :1240 (ReapAll in OnClose), internal/coreexec/ package surface (the Register family + Config/InteractiveConfig structs — unchanged), .planning/phases/21-context-policy-parity-closures/21-06-PLAN.md and .planning/phases/22-background-execution-sandbox-reality/22-04-PLAN.md + 22-06-PLAN.md (locked contracts: gate-join at the executor chokepoint, coretools.json, three sandbox exec sites — precondition marks), 25-CONTEXT.md D-05 (coreexec stays app-side)/D-09/D-10, 25-06-SUMMARY.md</read_first>
  <action>
    1. Precondition marks (phases 20-24 interactions): the coreexec state this toolkit wraps includes the LOCKED 21-06 gate-join (hook verdicts at the executor chokepoint), 22-04 (coretools.json schema), and 22-06 (sandbox wrap at all three exec sites). This plan does NOT build any of their internals — the toolkit relocates the REGISTRATION sequence verbatim-in-role against whatever landed state exists.
    2. Create internal/acpserve/toolkit.go implementing kit SessionToolkit: Attach performs the registration family in today's order (task registry, RegisterCore, RegisterAsk binding the env's broker, mailbox, session reader, RegisterInteractive with the concrete schedule store the toolkit struct holds), and returns the Reaper whose single method calls the task registry's reap-all. The struct carries the concrete app values the composition root injects.
    3. The AskSurface renderer func-field (Task 1) is populated at the composition root from coreexec's ask-surface renderer — the same renderer the broker's onSurface used before the seam.
    4. acpserve.Run: construct the toolkit before the runner (or inject via the flat config at the mirrored field); the composition statement order is otherwise untouched.
    5. Verify the severance is total: enumerate kit/ packages' imports — non-test files across ALL of kit/ must now show zero app-package imports. Record the final edge inventory in the SUMMARY (this is the state 25-09's gate enforces). The relocated coreexec_wiring and ask_wiring tests pass with assertion-preserving retargets to the app-side toolkit.
  </action>
  <verify>
    <automated>mise ci && go test ./kit/runtime/ ./internal/acpserve/ -count=1 && go list -deps ./kit/runtime | grep -c "ass-guard-agent/internal/coreexec$"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - internal/acpserve/toolkit.go implements Attach with the registration family in today's order; the Reaper wraps reap-all; OnClose's three legs (cancel, reap, MCP close) all fire
    - kit/runtime's dependency set contains no internal/coreexec — and the SUMMARY's final inventory shows ZERO app imports across all kit/ non-test files (all six runtime edges + session edge severed)
    - The relocated coreexec/ask wiring batteries pass with assertion-preserving retargets; the stub-exec nil path still pins its test
    - mise ci green
  </acceptance_criteria>
  <done>The last kit→app edge is severed; the kit is import-clean; coreexec stays app-side behind the toolkit seam.</done>
  <tdd_eligibility>Registration relocation over behavior pinned by relocated wiring batteries. TDD-ineligible with rationale.</tdd_eligibility>
</task>

</tasks>

Artifacts this phase produces (plan 07's share): kit/runtime/toolkit.go (SessionToolkit, ToolkitEnv, Reaper), internal/acpserve/toolkit.go (coreexec registration + Reaper impl), the severed runtime→coreexec edge — completing an import-clean kit/. Phase-wide artifact list: see 25-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toolkit Attach → per-session tool executors | Tool registration crosses into the kit catalog — executor semantics (incl. the Phase-17 gate join and 22-06 sandbox wraps) stay app-side |
| env ask broker → kit session | The broker is the suspension mechanism; the seam must not let the app bypass or double-bind it |
| Reaper → session teardown | A missed reap leaks background process groups past session lifetime (12-06 invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-30 | Elevation | toolkit registration bypassing the gate pipeline | high | mitigate | Precondition mark: the locked 21-06 gate-join state is relocated verbatim-in-role; the executor chokepoint stays inside coreexec (app-side); kit sees only registered tools |
| T-25-31 | Denial of Service | Reaper skipped on a teardown path | high | mitigate | OnClose composes all three legs with the Reaper explicit; relocated checkpoint/wiring tests pin session teardown; the 12-06 invariant is in the Reaper doc comment |
| T-25-32 | Tampering | sandbox wrap dropped during relocation | high | mitigate | Precondition mark: 22-06's three exec sites are inside coreexec/app code this plan does not rewrite; relocation is registration-sequence only; sandbox tests remain green under mise ci |
| T-25-33 | Denial of Service | double-bound ask broker (env + renderer wiring) | medium | mitigate | The broker is constructed once in sessionFor; the toolkit only registers it; relocated ask batteries pin single-surface behavior |
| T-25-34 | Information Disclosure | residual app import left in a kit test file | low | accept | Non-test files are import-clean after this plan; test files are the explicit 25-08 concern with the recorded gate-scope decision (strict non-test, test-exempt) |
</threat_model>

<verification>
- SessionToolkit seam in its coarse OQ1 shape; env carries exactly today's inputs, kit-typed
- internal/acpserve/toolkit.go relocates the registration family; Reaper honored in OnClose
- kit/ non-test imports contain zero app packages — the final inventory is recorded in the SUMMARY
- mise ci green
</verification>

<success_criteria>
- All eight kit→app edges severed (six runtime + session→ecosys + enginebridge→learning)
- coreexec remains app-side behind the coarse toolkit seam; gate-join and sandbox contracts ride as preconditions, unbuilt here
- The nil-toolkit degraded path is documented and pinned
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-07-SUMMARY.md` when done
</output>
