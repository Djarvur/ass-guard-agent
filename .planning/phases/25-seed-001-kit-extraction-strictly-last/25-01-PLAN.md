---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "kit/event/"
  - "kit/redact/"
  - "kit/checkpoint/"
  - "kit/profile/"
  - "scripts/eval-change-class.sh"
  - ".planning/phases/25-seed-001-kit-extraction-strictly-last/test-ledger.txt"
  - "internal/event/ (deleted by move)"
  - "internal/redact/ (deleted by move)"
  - "internal/checkpoint/ (deleted by move)"
  - "internal/profile/ (deleted by move)"
autonomous: false
requirements: [KIT-01]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[FLAGGED ASSUMPTION — probe KIT-01, category concurrency, status unresolved — surfaced, not silently dropped] If a move step is interrupted or tasks run in parallel, the tree is still safe: every move step is ONE atomic commit ending mise-ci-green, and all move waves are strictly sequential (files_modified exclusivity forbids parallel moves). An interrupted run resumes from a compiling boundary, never from a mid-move tree. The executor asserts this by confirming each move commit builds and tests green before starting the next."
    - "Rank-0 packages (event, redact, checkpoint, profile) live under kit/ with byte-identical bodies — proven by git diff --color-moved=dimmed-zebra review and the full mise ci gate green at the tracer commit"
    - "The eval change-class detector recognizes the kit layout: selftest proves a kit/engine path fires turn-behavior and a docs path stays clean"
    - "A pre-move test-function ledger exists in the phase dir with the whole-repo total recorded, so post-extraction sums are provably equal (D-20)"
  artifacts:
    - path: "kit/event/"
      provides: "Bus + neutral event vocabulary (moved verbatim from internal/event)"
    - path: "kit/redact/"
      provides: "Redaction engine (moved verbatim)"
    - path: "kit/checkpoint/"
      provides: "Checkpoint store (moved verbatim)"
    - path: "kit/profile/"
      provides: "Profile model (moved verbatim)"
    - path: ".planning/phases/25-seed-001-kit-extraction-strictly-last/test-ledger.txt"
      provides: "Pre-move test-function baseline (D-20 instrument, Phase-15 precedent)"
    - path: "scripts/eval-change-class.sh"
      provides: "CLASSES extended with kit/ equivalents (Pitfall 1 silent-gate fix) — legacy paths retained"
  key_links:
    - from: "go.mod"
      to: "kit/*"
      via: "unchanged module path github.com/Djarvur/ass-guard-agent — kit imports read ass-guard-agent/kit/<pkg> (D-01/D-07)"
      pattern: "ass-guard-agent/kit"
    - from: "scripts/eval-change-class.sh"
      to: "kit/*"
      via: "CLASSES entries naming kit/ paths keep the behavioral eval gate firing after extraction (D-20)"
      pattern: "turn-behavior:kit/"
  prohibitions:
    - requirement_id: KIT-01
      category: safety
      statement: "MUST NOT alter any persisted data shape during re-homing — transcript JSONL line kinds, audit event JSON, checkpoint objects, and schedule-store YAML marshal byte-identically after every move (verbatim bodies, no type renames)"
    - requirement_id: KIT-01
      category: safety
      statement: "MUST NOT write diagnostics to stdout from kit code — stdout is exclusively the ACP JSON-RPC transport; kit logging stays on injected io.Writer / stderr"
---

<objective>
Wave 0 + the tracer: confirm the one-way kit/ boundary commitment, arm the equivalence instruments (test-ledger baseline, eval change-class extension), and execute the FIRST verbatim package move (rank-0: event, redact, checkpoint, profile) as the production-quality tracer proving the whole move discipline end-to-end.

Purpose: D-03 two-pass discipline starts with instruments + a proven slice. The rank-0 move is the phase's thinnest path through every layer it touches (git mv -> import rewrite -> goimports -> mise ci -> color-moved review); if the discipline is broken, it breaks here on four leaf packages, not on runtime. D-01 is rated costly (module stays single — no re-litigation, but noted); D-02 is rated ONE-WAY and earns its blocking decision checkpoint before the first permanent import path is created.

Output: kit/ tree seeded with 4 leaf packages; test-ledger baseline; eval-change-class.sh recognizing kit paths; mise ci green.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/15-internal-runtime-carve-step-0/15-VALIDATION.md
@.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt
@scripts/eval-change-class.sh
@.mise.toml
@go.mod
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way kit/ library-boundary commitment (D-02/D-07 reversibility gate)</name>
  <decision>Create the first permanent public kit/ import paths now (the D-02 one-way commitment takes effect at the tracer commit)</decision>
  <context>CONTEXT.md rates D-02 one-way: import paths under ass-guard-agent/kit/ become the library's public contract; renaming after external consumers exist breaks published paths. This checkpoint is the reversibility gate REVERSIBILITY_GATES=true requires before the task implementing the one-way decision. D-01 (single module, rated costly) and D-07 (module path unchanged) are locked and carried; only the one-way moment needs explicit confirmation. Phases 17-24 are planned-not-executed as of planning time; this phase executes strictly after them per ROADMAP ordering.</context>
  <options>
    <option id="option-a">
      <name>Proceed — kit/ tree inside the single module (per D-01/D-02/D-07, locked)</name>
      <pros>Honors the operator's locked decisions; atomic cross-cutting commits preserved; mise/goreleaser untouched</pros>
      <cons>Import paths become permanent public contract from the first move commit (the accepted one-way cost)</pros>
    </option>
    <option id="option-b">
      <name>Halt — return to orchestrator before any import path changes</name>
      <pros>No permanent paths created until the operator re-confirms ordering</pros>
      <cons>Stalls the milestone's final phase; contradicts the "strictly last, now settle it" plan</cons>
    </option>
  </options>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Wave-0 instruments — test-ledger baseline + eval change-class kit/ extension</name>
  <files>.planning/phases/25-seed-001-kit-extraction-strictly-last/test-ledger.txt, scripts/eval-change-class.sh</files>
  <read_first>scripts/eval-change-class.sh (CLASSES block at lines 27-40, selftest fixtures at 42-69, match_class prefix semantics at 71-81), .planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt (ledger format + post-carve rule precedent), .planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md (§Validation Architecture Wave 0 Gaps, Pitfall 1)</read_first>
  <action>
    Part A — ledger baseline (D-20 instrument). Capture the CURRENT pre-move tree: for every package under kit/ (none yet), internal/, and cmd/, list per-file test-function declarations (lines matching the Go test-func declaration prefix) with per-file counts and a header recording the capture date, HEAD sha, and the post-extraction rule: the sum of test-function declarations across kit/... plus all remaining internal/... and cmd/... scopes must equal the baseline total after every plan of this phase. Mirror the Phase-15 ledger format. Record the whole-repo total prominently (the executor of later plans asserts sum equality).

    Part B — eval detector extension (D-20, Pitfall 1). In scripts/eval-change-class.sh CLASSES, ADD kit/ equivalents while KEEPING every legacy path: add model entries for kit/provider and kit/shaper; add turn-behavior entries for kit/engine, kit/session, and kit/runtime (the runtime-equivalent wiring named in the header comment). Do not remove model:internal/scheduler even though no package currently exists at that path — it is defensive. Extend the selftest check() fixtures with at least one kit-path hit (a hypothetical kit/engine file path) and keep the existing clean-path assertions (docs/README.md, .planning/STATE.md, the script itself) passing unchanged. The comment header gains one line noting the 25-… extension per D-20. Rationale: after extraction, edits to kit/engine and kit/session must still fire the behavioral eval gate — without this the gate silently stops firing exactly when the riskiest moves land (RESEARCH Pitfall 1).
  </action>
  <verify>
    <automated>./scripts/eval-change-class.sh --selftest && test -f .planning/phases/25-seed-001-kit-extraction-strictly-last/test-ledger.txt && grep -c "kit/" scripts/eval-change-class.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/eval-change-class.sh --selftest exits 0 and prints "selftest ok" with hits increased by at least 1 versus its prior count
    - CLASSES contains all five new kit/ entries (model:kit/provider, model:kit/shaper, turn-behavior:kit/engine, turn-behavior:kit/session, turn-behavior:kit/runtime) AND retains every original entry (model:internal/provider, model:internal/shaper, model:internal/scheduler, turn-behavior:internal/engine, turn-behavior:internal/session, turn-behavior:internal/coreexec, turn-behavior:cmd/ass-guard, turn-behavior:internal/evalharness, turn-behavior:internal/evalsuite, profile:profiles/)
    - test-ledger.txt exists in the phase dir, records the HEAD sha, and states a numeric whole-repo test-function total
    - No legacy CLASSES line was deleted (diff shows additions only within CLASSES plus the header comment)
  </acceptance_criteria>
  <done>Ledger baseline captured pre-move; eval detector proven to fire on kit paths and stay clean on docs paths; all legacy classes retained.</done>
  <tdd_eligibility>Not a behavior task — instruments/config only (test-ledger capture + shell script extension). TDD red-green does not apply; verified by the script's own selftest.</tdd_eligibility>
</task>

<task type="tracer">
  <name>Task 3: TRACER — rank-0 verbatim moves (event, redact, checkpoint, profile to kit/) with mise ci green</name>
  <files>kit/event/, kit/redact/, kit/checkpoint/, kit/profile/ (destinations; sources internal/event, internal/redact, internal/checkpoint, internal/profile deleted), plus all importer files rewritten by goimports (compiler-driven discovery — every file importing the four moved packages)</files>
  <read_first>25-CONTEXT.md D-01/D-02/D-03/D-07 (module/naming/move discipline), 25-RESEARCH.md §Move Order step 1 and §Current-State Inventory rank 0 (all four packages verified import-clean of app code), 25-PATTERNS.md §Verbatim-move hygiene (goimports local-prefix .golangci.yml:127-129; in-file decision-record precedent provider_factory_helpers_test.go:11-17), go.mod (module github.com/Djarvur/ass-guard-agent, go 1.26)</read_first>
  <precondition>Phases 16-24 executed and their phase gates closed (ROADMAP ordering: 25 is strictly last) — the executor asserts the working tree contains their landed surfaces (e.g. the internal/perm, internal/tasks, internal/sandbox packages from the locked 17-01/22-01/22-05 plans); if absent, halt: extraction ordering is violated and the move inventory would be wrong.</precondition>
  <action>
    This is the production-quality tracer for the entire phase: ONE path through every layer the phase touches, executed for keeps.

    1. Move each package DIRECTORY wholesale with git mv (never file picks — embed payloads are file-adjacent and must travel with their package): internal/event to kit/event, internal/redact to kit/redact, internal/checkpoint to kit/checkpoint, internal/profile to kit/profile. Rank-0 packages have zero kit-internal or app dependencies (verified by go list this session), so they move first with no ordering hazard.
    2. Rewrite every importer's import path from the old prefix to the new kit prefix across the whole repo using goimports -w (the local-prefix formatter config already groups module-internal imports correctly). Let the compiler find every straggler: go build ./... must pass with zero manual body edits.
    3. Zero body edits, zero renames, zero "improvements" — D-03 sanctions no de-cobra-style edits in this phase at all (Phase 15 sanctioned two; Phase 25 pass 1 sanctions zero). Any edit discovered as necessary is a planning bug: stop and record it in the SUMMARY.
    4. Review the move as a pure relocation: git diff --color-moved=dimmed-zebra --color-moved-ws=allow-indentation-change against the pre-move commit (dimmed-zebra per RESEARCH §State of the Art — block modes avoid the false single-line matches plain produces at tree scale).
    5. Run the full gate: mise ci. Then go test ./kit/... -count=1 specifically.
    6. Commit as one atomic commit (the interrupt-safety boundary the flagged assumption above guarantees).
  </action>
  <verify>
    <automated>mise ci && go test ./kit/... -count=1 && test -d kit/event && test -d kit/profile && ! test -d internal/event</automated>
  </verify>
  <acceptance_criteria>
    - kit/event, kit/redact, kit/checkpoint, kit/profile exist; the corresponding internal/ directories are gone
    - go build ./... and go vet ./... pass; mise ci (vet + lint + CGO_ENABLED=0 build + go test -race) exits 0
    - The tracer commit's diff shows the four package bodies as moved blocks under git diff --color-moved=dimmed-zebra (reviewer sees relocations, not rewrites); no non-import line of any moved file differs from its source
    - Every importer file compiles against the new import prefix; grep over the repo for the old module-internal import paths of these four packages returns nothing outside .planning/ and git history
    - go test ./kit/... -count=1 passes (the moved packages' own suites run at their new home)
  </acceptance_criteria>
  <done>The four rank-0 packages live under kit/ verbatim, the whole repo builds and tests green, and the move is reviewable as a pure relocation — the pass-1 discipline is proven end-to-end on real packages.</done>
  <tdd_eligibility>Not a behavior task — pure verbatim relocation (D-03); the relocated suites ARE the behavior proof. No new production behavior exists to drive a red-green cycle.</tdd_eligibility>
</task>

</tasks>

Artifacts this phase produces (plan 01's share): the kit/ tree's first four packages (kit/event, kit/redact, kit/checkpoint, kit/profile — verbatim bodies at new import paths), the phase test-ledger baseline, and the eval change-class detector's kit/ class extensions. Later plans add the remaining 11 moved packages (kit/audit, kit/hookdag, kit/shaper, kit/toolcat, kit/provider, kit/mcp, kit/toolexec, kit/modelrouting, kit/session, kit/engine, kit/runtime + kit/internal/enginebridge), the six seam interfaces (Emitter, Requester, Scheduler, LearnedStore, CommandCatalog, SessionToolkit) with their kit-neutral value types, the internal/acpserve adapter family (kit_adapter.go, catalog_adapter.go, toolkit.go, app-side discovery helper), kit/runtime/hostproof_test.go, kit/README.md, and the D-19 gate edits (.mise.toml kit-boundary task, .golangci.yml KitBoundary depguard rule).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit/* → importers | The moved packages' import paths become the library's public contract (one-way, D-02); anything importing them crosses the library boundary |
| detector → merge gate | eval-change-class.sh decides whether the behavioral eval gate fires before merge — a stale detector is a silent gate bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | scripts/eval-change-class.sh CLASSES | high | mitigate | Task 2 extends CLASSES with kit/ equivalents before any risky move lands, keeps legacy paths through the transition, and proves both directions via --selftest hit + clean-path fixtures (RESEARCH Pitfall 1) |
| T-25-02 | Information Disclosure | moved logging/redact code | medium | mitigate | Verbatim bodies guarantee redaction and stderr discipline are unchanged; the relocated suites (redaction-carry, audit tests) run green under mise ci at the tracer commit |
| T-25-03 | Tampering | persisted data shapes during move | medium | mitigate | Prohibition authored in must_haves: no marshaling-shape change; verbatim-move review (color-moved) makes any body edit visible in review |
| T-25-04 | Elevation | kit/ boundary creation (D-02) | low | accept | One-way per operator's own rating; confirmed at the Task 1 blocking decision checkpoint before any path exists |
</threat_model>

<verification>
- mise ci green at every commit in this plan (the D-20 equivalence proof begins here)
- Ledger baseline exists with a recorded whole-repo total; later plans assert sum equality against it
- eval-change-class.sh --selftest green with kit/ fixtures
- The four rank-0 packages compile, test, and lint at their kit/ homes with zero body edits
</verification>

<success_criteria>
- kit/{event,redact,checkpoint,profile} exist and their internal/ originals are gone; all importers rewritten mechanically
- The D-02 one-way checkpoint was answered option-a before the first path was created
- The behavioral eval detector recognizes kit/ paths (selftest-proven) with legacy classes intact
- The pre-move ledger baseline is committed to the phase dir
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-01-SUMMARY.md` when done
</output>
