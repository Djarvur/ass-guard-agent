---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 09
type: execute
wave: 9
depends_on: ["25-08"]
files_modified:
  - ".mise.toml"
  - ".golangci.yml"
  - "kit/runtime/hostproof_test.go"
  - "kit/README.md"
autonomous: false
requirements: [KIT-01, KIT-02, KIT-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[FLAGGED ASSUMPTION — probe KIT-03, category unclassified (manual review), status unresolved — surfaced, not silently dropped] Criterion #3's live-Zed equivalence is verified by the OPERATOR checkpoint at the end of this plan; the automated bounds (zeroconfig handshake smoke, simulator E2E over pipes, behavioral eval suites, ledger + CLI golden) approximate it but do not replace it. The phase is not closed on automated evidence alone."
    - "[FLAGGED ASSUMPTION — probe KIT-02, category unclassified — surfaced] The hostproof test proves a non-ACP frontend hosts the kit at the compile+behavior level (zero app imports in the test's import graph); it does not prove a SHIPPED second frontend exists. KIT-02's letter — design-level proof — is exactly what the test delivers."
    - "The D-19 import-direction gate is live in BOTH layers — the mise kit-boundary task wired into the ci gate's dependencies, and the depguard KitBoundary rule (non-test scoped) — and fails the build on any kit→internal or kit→cmd import (D-19: enforced, not conventional)"
    - "The full D-20 equivalence bar is green at the extracted layout: mise ci, relocated-ledger sum match, CLI-contract golden, zeroconfig stdout-clean smoke, AND the behavioral eval suites via the change-class gate"
    - "SEED-002 + SEED-003 remain in .planning/seeds/ as reading material with zero runtime dependencies — kit/README.md canonical-refs them (D-08); no kit code reads .planning/"
  artifacts:
    - path: ".mise.toml"
      provides: "kit-boundary task (D-19 grep layer, non-test files) wired into the ci gate's dependency list"
    - path: ".golangci.yml"
      provides: "depguard KitBoundary rule (files-glob scoped to kit, tests excluded, strict list-mode) — the lint layer of D-19"
    - path: "kit/runtime/hostproof_test.go"
      provides: "TestKitHostsNonACPFrontend — KIT-02's automated criterion: a full turn driven by fake Emitter/Requester/Provider with nil Catalog/Toolkit on the documented degraded paths, zero app imports"
    - path: "kit/README.md"
      provides: "The kit's front door: what it is, composition-root pointer, seam list, reference-app statement, SEED reading-material refs (D-08), nil-degradation table"
  key_links:
    - from: ".mise.toml [tasks.ci]"
      to: "kit-boundary"
      via: "the D-19 grep gate joins the ci gate's dependency list — fails the build, not a review note"
      pattern: "kit-boundary"
    - from: "kit/runtime/hostproof_test.go"
      to: "kit/runtime (Runner + seams)"
      via: "drives a real turn through NewRunner + SetEmitter + Run with kit fakes only"
      pattern: "TestKitHostsNonACPFrontend"
    - from: "kit/README.md"
      to: ".planning/seeds/SEED-002-*.md + SEED-003-*.md"
      via: "canonical references as design prior art — reading material, zero runtime deps (D-08/KIT-03)"
      pattern: "SEED-00"
---

<objective>
Land the enforcement and the proof: the D-19 import-direction gate (mise + depguard, enabled only now that the last edge is severed — gate-before-seams is the anti-pattern), KIT-02's hostproof test, the kit's README (D-08 reading-material refs), and the full D-20 equivalence battery closing with the live-Zed operator checkpoint (criterion #3).

Purpose: the extraction is only done when the boundary is mechanically enforced and the reference app is provably identical. This plan converts the pass-2 state into permanent gates and closes the phase with the operator's confirmation.

Output: enforced kit boundary; green hostproof; README; full battery green; operator approval recorded.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-08-SUMMARY.md
@.mise.toml
@.golangci.yml
@.planning/seeds/SEED-001-agent-creation-kit-library-with-ass-guard-as-first-app.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-19 gates + hostproof test + kit README (D-08/KIT-03)</name>
  <files>.mise.toml, .golangci.yml, kit/runtime/hostproof_test.go (new), kit/README.md (new)</files>
  <read_first>.mise.toml [tasks.ci] depends list (:25-27) and existing task shapes, .golangci.yml depguard rules block (:16-22) and formatter local-prefixes (:127-129), 25-RESEARCH.md §Code Examples (the D-19 two-layer gate: mise grep task with the non-test exclude flag; depguard KitBoundary rule with files globs and the strict list-mode) and §Non-ACP host proof (the test sketch: recordingEmitter, immediateRequester, scripted provider, nil Catalog/Toolkit degraded paths), 25-CONTEXT.md D-19/D-08/D-12, kit/runtime seams (Emitter/Requester — the fakes implement these), 25-08-SUMMARY.md (the fakes the hostproof reuses or extends)</read_first>
  <action>
    1. The mise layer: add a kit-boundary task running the recursive search over kit/ for app-module import paths in Go files, EXCLUDING test files (the D-19 decision: strict non-test, test-exempt per 25-08's recorded decision), and wire it into [tasks.ci]'s dependency list alongside vet/lint/build/test. Rationale comment cites D-19 and the gate-after-seams ordering (this plan runs only because 25-07 severed the last edge — enabling it earlier would have failed pass 1 by construction, D-03).
    2. The depguard layer: add the KitBoundary rule to .golangci.yml's depguard rules — files-glob scoped to the kit tree excluding test files, strict list-mode, denying both app-internal and cmd module paths, with descs citing D-19. Belt-and-suspenders per the research alternative table (grep = ci layer, depguard = lint layer).
    3. Prove the gate has teeth BOTH ways: it passes on the current tree, and a deliberate violation in a scratch commit (or a temp file, added and removed within the task) flips both layers red. Record the negative proof in the SUMMARY.
    4. kit/runtime/hostproof_test.go: TestKitHostsNonACPFrontend — construct NewRunner with the flat config (bus, test profile, temp work dir, a scripted provider over the kit Provider interface), SetEmitter with a recording Emitter factory, a canned Requester, nil Catalog/Toolkit riding the DOCUMENTED degraded paths (stub exec; structured no-store schedule errors), drive one real turn with a plain text prompt, assert turn completion + recorded events. The test file's import graph contains zero app packages — assert THAT too (a meta-check reading the file's own imports, or verified by the gate this task just enabled).
    5. kit/README.md (short, D-12 honest — not a curated godoc-first API): what the kit is (one paragraph from SEED-001's value proposition), the composition-root statement (the reference app's acpserve.Run composes; no mega-constructor per D-09), the six seams with one-line contracts, the nil-degradation table, the app→kit direction rule (point at the gates), and canonical references to the SEED-002 and SEED-003 documents in .planning/seeds/ as design prior art (D-08 reading material; zero runtime deps — the README references paths, no code reads them).
  </action>
  <verify>
    <automated>mise kit-boundary && go test ./kit/runtime/ -run TestKitHostsNonACPFrontend -count=1 -v && golangci-lint run ./kit/... && test -f kit/README.md && test -f .planning/seeds/SEED-002-mine-charmbracelet-fantasy-for-patterns-and-prior-art.md && test -f .planning/seeds/SEED-003-go-agent-reference-landscape-frameworks-and-coding-agents.md</automated>
  </verify>
  <acceptance_criteria>
    - mise ci now includes the kit-boundary gate (visible in [tasks.ci] deps); the gate passes on the clean tree AND was proven red on a deliberate violation (negative proof in SUMMARY)
    - .golangci.yml carries the KitBoundary rule with the test-exempt files glob; golangci-lint run passes
    - TestKitHostsNonACPFrontend passes, drives a real turn through the degraded nil paths, and imports zero app packages
    - kit/README.md exists with the seams list, nil-degradation table, composition-root statement, and the two SEED canonical refs; no kit code reads .planning/
  </acceptance_criteria>
  <done>The kit boundary is mechanically enforced, the non-ACP host proof is green, and the library has an honest front door.</done>
  <tdd_eligibility>The hostproof test is a NEW test but characterizes EXISTING behavior (degraded paths, turn loop) — it is an architectural proof artifact, not a red-green driver for new production code. Gate edits are config. TDD-ineligible with rationale.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 2: Full D-20 equivalence battery at the extracted layout</name>
  <files>.planning/phases/25-seed-001-kit-extraction-strictly-last/25-09-SUMMARY.md (battery record)</files>
  <read_first>25-CONTEXT.md D-20 (the full gate family verbatim), 25-RESEARCH.md §Validation Architecture (commands + sampling), .planning/phases/25-seed-001-kit-extraction-strictly-last/test-ledger.txt (baseline total), cmd/ass-guard/cli_contract_test.go (TestCLIBinaryContractRootAndACP, TestCLIBinaryContractSupportCommands, TestCLIBinaryContractProfile), cmd/ass-guard/zeroconfig_test.go (TestZeroConfigFirstRun), .mise.toml eval-gate/eval-check-changed tasks</read_first>
  <action>
    Run and record the complete battery, each with its outcome in the SUMMARY:
    1. mise ci (vet + lint incl. the new KitBoundary rule + CGO_ENABLED=0 build + go test -race) — green.
    2. Ledger proof: whole-repo test-function sum equals the 25-01 baseline total — record both numbers.
    3. CLI-contract golden: the three TestCLIBinaryContract functions — green (the CLI contract is unchanged; criterion #3's command surface).
    4. Zeroconfig stdout-clean smoke: TestZeroConfigFirstRun — green (stdout carried only protocol frames through the entire extraction).
    5. Behavioral eval: mise eval-check-changed against the phase's merge base (it fires — kit/ classes are in CLASSES since 25-01), then mise eval-gate (~8-10 min, real binary + real model, env-gated) — green. This is criterion #3's "all behavioral eval suites still pass against the extracted layout" executed literally.
    6. git diff --color-moved=dimmed-zebra master...HEAD as the pure-move review record for pass 1 (pass 2's design diffs reviewed normally); note the review mode in the SUMMARY.
    If ANY leg fails: fix forward within this plan (the failure is in-scope; the phase does not close red), re-running the failed leg plus mise ci after the fix.
  </action>
  <verify>
    <automated>mise ci && go test ./cmd/ass-guard/ -run 'TestCLIBinaryContract|TestZeroConfigFirstRun' -count=1 && MERGE_BASE=$(git merge-base master HEAD) mise eval-check-changed; test $? -eq 3 && mise eval-gate</automated>
  </verify>
  <acceptance_criteria>
    - All five battery legs green with outcomes recorded in the SUMMARY (ci, ledger sum, CLI golden, zeroconfig, eval-gate)
    - The eval detector fired (exit 3) proving the kit/ classes route the diff into the behavioral gate
    - The color-moved review record notes pass 1 as pure relocation
  </acceptance_criteria>
  <done>The full D-20 equivalence bar is green at the extracted layout — the reference app provably behaves identically by every automated instrument.</done>
  <tdd_eligibility>Verification battery — no production change. TDD-ineligible.</tdd_eligibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-Zed operator checkpoint — criterion #3's manual leg (15-07 precedent)</name>
  <what-built>The completed kit extraction: all agent machinery under kit/ behind the composition-root API, the Emitter/Requester frontend seam, the enforced D-19 boundary, and the full automated equivalence battery green (Task 2). ass-guard is the kit's reference app with unchanged behavior by every automated instrument.</what-built>
  <how-to-verify>
    1. Launch a live Zed session against the freshly built binary (the operator's daily-use surface, exactly as at v1.1/Phase-15/16 closes): a session streams tokens in real time, executes tools with native diffs, plan/todo cards render, asks surface as plain-text surfaces as before.
    2. Run one slash-command invocation and one engine-driven behavior if available (cron/automation or learning ask) — the surfaces are unchanged.
    3. Optional deep check: restart mid-session and confirm replay/resume behaves as at the pre-extraction close.
    4. Compare against the recorded 15-07/16-06 operator criteria — nothing should differ.
    The automated bounds already green: zeroconfig handshake smoke, simulator E2E over pipes, behavioral eval suites, CLI golden, ledger match.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

Artifacts this phase produces (plan 09's share — phase-closing): the D-19 gate pair (.mise.toml kit-boundary task wired into ci; .golangci.yml KitBoundary depguard rule), kit/runtime/hostproof_test.go (TestKitHostsNonACPFrontend — KIT-02's automated criterion), kit/README.md (D-08 canonical refs), the recorded D-20 battery, and the operator approval record. Phase-wide artifact list: see 25-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit/ → all future importers | The gate pair is the permanent enforcement of the library boundary — a bypass re-opens dependency-direction erosion |
| hostproof test → KIT-02 claim | A vacuous proof (accidental app import) would claim the seam property falsely |
| eval gate → merge decision | The detector's kit/ classes are what route behavioral risk into the eval suite |
| operator checkpoint → phase close | Criterion #3's live-Zed leg is human-verified, not automatable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-39 | Tampering | behavioral-gate coverage post-extraction | high | mitigate | The battery PROVES the detector fires on this diff (exit 3) and eval-gate runs green; 25-01's CLASSES extension + selftest made the fix permanent; prohibition below pins the must-NOT |
| T-25-40 | Elevation | dependency-direction erosion over time | high | mitigate | Two enforcement layers (mise task in ci deps + depguard strict rule), both proven red on a deliberate violation (negative proof recorded) |
| T-25-41 | Spoofing | vacuous hostproof (accidental app import) | high | mitigate | The test asserts its own app-import-free property; the D-19 grep gate (non-test) plus a deliberate check on the test file in the task verify |
| T-25-42 | Information Disclosure | stdout discipline through the final battery | high | mitigate | TestZeroConfigFirstRun + simulator E2E over pipes green in the battery; the transport boundary survived the whole extraction |
| T-25-43 | Repudiation | phase closed without the manual leg | medium | mitigate | The live-Zed checkpoint is BLOCKING; the flagged KIT-03 assumption in must_haves records that automated bounds do not replace it |
| T-25-44 | Tampering | kit/README overclaiming the API (D-12 creep) | low | accept | README is doc-only, seams-listed from the actual code; D-12's demand-grown policy governs future growth; review checks claims against seams.go/catalog.go/toolkit.go |
</threat_model>

<verification>
- Both D-19 layers live, proven red-on-violation and green-on-clean
- TestKitHostsNonACPFrontend green with zero app imports
- Full D-20 battery green and recorded; eval detector fires and eval-gate passes
- Operator approval recorded (blocking checkpoint)
</verification>

<success_criteria>
- The kit boundary is enforced by the build, not by convention (D-19)
- KIT-02's automated criterion (non-ACP host proof) is green
- Criterion #3 is satisfied: identical behavior by automated instruments PLUS operator confirmation of the live session
- KIT-03's reading-material clause is satisfied with zero runtime dependencies (D-08)
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-09-SUMMARY.md` when done
</output>
