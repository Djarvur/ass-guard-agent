---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - "kit/runtime/runtime.go"
  - "kit/runtime/seams.go"
  - "kit/session/ask.go"
  - "internal/acpserve/kit_adapter.go"
  - "internal/acpserve/acp_serve.go"
autonomous: true
requirements: [KIT-02]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[FLAGGED ASSUMPTION — probe KIT-02, category unclassified (manual review), status unresolved — surfaced, not silently dropped] The minimal-pair seam (one Emit method + one ctx-blocking Request method) is SUFFICIENT to host a non-ACP frontend. This is proven at DESIGN level only — by the 25-09 hostproof test and the adapter's existence — not by a real second frontend (Telegram is v1.3, deferred). If v1.3 needs more, adding event kinds is additive (D-14 single-method rule holds); widening the METHOD SET later is a breaking change to a public interface."
    - "kit/runtime holds ZERO acp vocabulary after this plan: no internal/acp import, no frame construction, no tool-kind table, no stop-reason wire mapping — Phase-15 D-20 holds by construction (KIT-02's whole point)"
    - "The kit emits its own neutral event vocabulary through ONE method (Emit(ctx, event.Event) error) and asks through ONE method (Request(ctx, Ask) (Answer, error)) — no typed method-set interface, no opaque pass-through (D-14)"
    - "The adapter satisfies acp.TurnRunner with its signature UNCHANGED — the Phase 16-20 frozen wire surface (server.go:31-33)"
    - "acpserve.Run statement order is preserved exactly; SetEmitter injection stays strictly between server construction and scheduler start (WINDOWS #3 behavioral contract)"
    - "Stop-reason ownership: the kit returns its raw marker; ONLY the adapter maps it to the wire value; cron automation audit lines continue to record the kit-raw stop (OQ3 resolution)"
  artifacts:
    - path: "kit/runtime/seams.go"
      provides: "Emitter + Requester interface definitions with contract doc comments (acp/server.go:16-66 pattern family), plus Ask/Answer kit-neutral structs"
    - path: "internal/acpserve/kit_adapter.go"
      provides: "kitTurnAdapter (implements frozen acp.TurnRunner), the kit-Emitter impl translating events to frames, and the relocated translations: toolKindFor table, ToolCallFrame/UpdateFrame construction, mapAskStop, acp↔session ContentBlock conversion"
  key_links:
    - from: "internal/acpserve/kit_adapter.go"
      to: "kit/runtime"
      via: "adapter drives kit Runner.Run and wraps srv emitters as kit Emitters; composition root injects via SetEmitter"
      pattern: "SetEmitter|kitTurnAdapter"
    - from: "kit/runtime"
      to: "kit/event"
      via: "forwarder loop publishes kit events through the Emitter seam instead of building transport frames"
      pattern: "Emit\\(ctx"
---

<objective>
KIT-02, first half: define the Emitter + Requester seams kit-side (the milestone's one genuinely new design act) and sever the runtime→acp edge — the kit stops building transport frames; a new app-side adapter in internal/acpserve does the translation that today lives in kit/runtime.

Purpose: the frontend seam is mostly a FORMALIZATION of existing wires — the kit bus already carries neutral event kinds (AgentMessageChunk, ToolCall, ToolCallUpdate, plus the additive union), and the ask machinery (AskBroker) is already kit-side. This plan stops the translation at the kit boundary and lands it in the adapter, so a Telegram frontend could translate the same events (D-14).

Open-Question resolutions recorded here: OQ2 — the adapter home is internal/acpserve (the composition root per D-09; 17-02's locked ask_surface.go already lands there; internal/acp stays wire-only per its ConfigSurface precedent). OQ3 — stop-reason: kit-raw marker stays kit-side, adapter maps for the wire, cron audit lines keep the raw value.

Output: kit/runtime with zero acp imports; kitTurnAdapter + emitter/asker impls in acpserve; mise ci green; relocated emitter/forwarder tests pass.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-03-SUMMARY.md
@kit/runtime/runtime.go
@kit/session/ask.go
@internal/acp/server.go
@internal/acpserve/acp_serve.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kit-side seams — Emitter + Requester interfaces, forwarder rewiring, frame code out</name>
  <files>kit/runtime/seams.go (new), kit/runtime/runtime.go, kit/session/ask.go</files>
  <read_first>kit/runtime/runtime.go (emitFor field :190, SetEmitter :1500-1502, Run signature :479-482, startChunkForwarder :591-645, routeBusEvent :651-664, forwardToolCall :666-682, forwardToolCallUpdate :687-703, toolKindFor :707-739, mapAskStop :573-583, toContentBlocks :1631-1639), kit/event/events.go (Kind() discriminator :41-155 — the neutral vocabulary the Emitter carries), kit/session/ask.go (NewAskBroker :112-122, kind constants :85-91, PendingAsk), internal/acp/server.go:16-66 (the interface-definition pattern family: contract doc comments, minimal method sets, separate optional-capability interfaces), 25-CONTEXT.md D-13/D-14/D-15, 25-PATTERNS.md §kit/runtime seam definitions and §Emitter injection precedent</read_first>
  <action>
    1. Create kit/runtime/seams.go defining, at the point of use (accept-interfaces rule), the two KIT-02 seams with contract doc comments in the acp/server.go family style:
       - Emitter: exactly one method, Emit taking a context and a kit/event event, returning error (D-14 single method). Doc comment states: implemented by the frontend adapter; nil means the documented degraded state (existing nil-emitter behavior preserved).
       - Requester: exactly one method, Request taking a context and an Ask, returning Answer and error — ctx-BLOCKING per D-15. Doc comment states: implemented by the frontend adapter; timeout policy is kit-side config mirroring the Phase-12 D-01 ask timeout (configurable wait, then a capture-shaped non-answer; 0 blocks forever); cancelled resolves as a normal non-answer, never a wedge.
       - The kit-neutral Ask and Answer value structs next to the interface, carrying exactly what today's PendingAsk/answer flow transports (questions, ask kind, answers, cancelled flag) — no wire vocabulary.
    2. Retype the Runner seam: the emitFor field and SetEmitter setter carry a per-session Emitter factory instead of the acp chunk-emitter type; Run's signature becomes kit-neutral — context, session id, an Emitter, and a slice of session ContentBlock (the session package is kit — this is a kit type, allowed).
    3. Rewire the forwarder loop: startChunkForwarder's route closure calls the injected Emitter's single method with the raw bus event instead of routeBusEvent. DELETE from kit/runtime: routeBusEvent, forwardToolCall, forwardToolCallUpdate, toolKindFor, toContentBlocks, and the ask→wire stop mapping (mapAskStop) — these are transport translations and move to the adapter in Task 2. Stop reasons: Run returns the kit-raw marker; do NOT map it kit-side (OQ3).
    4. kit/session/ask.go: the AskBroker stays kit-side and untouched in its timeout/suspend semantics; add only what formalizes the ask ORIGIN as the Requester seam (the adapter will bridge wire asks into broker resolutions). Zero behavior change to timeout/suspend/resume — the relocated ask batteries are the proof.
    5. Staging rule (no deletion before the replacement lands): the serve path's legacy behavior stays LIVE until Task 2's replacement commit. The translation logic this task removes from kit/runtime (routeBusEvent family, toolKindFor, toContentBlocks, mapAskStop) lands immediately as temporary app-side glue in internal/acpserve mirroring it verbatim-in-role, so the serve path is never degraded between commits; Task 2 deletes that temporary glue in the SAME commit that lands the real adapter (no commit leaves glue and adapter both wired). The tree compiles whole at every commit — `go build ./...` is part of this task's verify — and mise ci stays green.
  </action>
  <verify>
    <automated>go build ./... && go test ./kit/runtime/ ./kit/session/ -count=1 && go list -deps ./kit/runtime | grep -c "ass-guard-agent/internal/acp$"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - kit/runtime/seams.go exists exporting Emitter and Requester, each with EXACTLY one method, plus kit-neutral Ask/Answer structs, all with contract doc comments naming implementor and nil-degradation
    - kit/runtime's dependency set contains no app packages (the acp import is gone — the first of the six edges severed)
    - routeBusEvent, forwardToolCall, forwardToolCallUpdate, toolKindFor, toContentBlocks no longer exist anywhere under kit/
    - The forwarder loop emits raw kit events through the single Emitter method
    - kit compiles and its relocated runtime/session suites pass
  </acceptance_criteria>
  <done>The kit emits neutral events and speaks the Requester contract; zero transport vocabulary remains kit-side.</done>
  <tdd_eligibility>Interface declarations + rewiring of existing pinned behavior (relocated suites already pin forwarder semantics). No new business logic to drive red-green; the existing tests are the contract. Recorded as TDD-ineligible with rationale.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 2: App-side adapter — kitTurnAdapter, emitter translation, stop-reason mapping (OQ2/OQ3)</name>
  <files>internal/acpserve/kit_adapter.go (new), internal/acpserve/acp_serve.go</files>
  <read_first>internal/acp/server.go:31-33 (FROZEN TurnRunner signature — do not change), internal/acpserve/acp_serve.go:164-251 (composition order — preserve exactly; SetEmitter injection point WINDOWS #3 at :239-240), kit/runtime/runtime.go (Run's new kit-neutral signature from Task 1), 25-RESEARCH.md §Pattern 2 (Emitter seam) and §acp adapter code example, internal/runtime/cron_wiring.go:192-199 now at kit/runtime/cron_wiring.go (audit lines reading the kit-raw stop — verify before finalizing OQ3), 25-CONTEXT.md D-09/D-14</read_first>
  <action>
    1. Create internal/acpserve/kit_adapter.go — the OQ2 resolution: ALL acp↔kit adapters live in internal/acpserve (composition root; internal/acp stays wire-only). The adapter:
       - implements the FROZEN acp.TurnRunner interface (context, session id, ChunkEmitter, slice of acp ContentBlock → stop-reason string, error) by converting wire blocks into session ContentBlock, driving the kit Runner, and mapping the kit-raw stop marker to the wire value at THIS layer only (OQ3: adapter maps; kit raw). The mapping table is exactly the relocated mapAskStop logic.
       - hosts the relocated translations verbatim-in-role: the tool-name→tool-kind table (was toolKindFor), the ToolCallFrame/ToolCallUpdateFrame construction (was forwardToolCall/forwardToolCallUpdate), and the acp↔session ContentBlock conversion (was toContentBlocks).
       - provides the kit Emitter implementation: wraps the server's per-session emitter handle and translates kit events to frames — AgentMessageChunk events to chunk emissions, ToolCall/ToolCallUpdate events through the relocated frame builders. One Emit method, switch on the event Kind.
    2. Rewire acpserve.Run WITHOUT reordering any statement: the Runner is constructed (or its adapter wrapped) such that the adapter satisfies acp.WithTurnRunner as today; SetEmitter injection happens at the exact same statement position (strictly between server construction and scheduler start — WINDOWS #3). The startup step sequence bus → profile → providerfactory → warnings → BodyStore → audit mirror → runner → LoadCommandRegistry (still kit-side until 25-06) → SetupEngine → server → ConfigSurface → sched.Open → SetSchedule → SetEmitter → StartScheduler → reap goroutine → Serve is preserved statement-for-statement.
    3. Verify OQ3 against the landed cron code: read kit/runtime/cron_wiring.go's automation-turn audit line — confirm it records the stop marker the kit returns (raw), so adapter-side mapping does not change audit content. If the audit line consumed the MAPPED value, keep the raw value in audit by mapping only at the wire boundary and note the finding.
    4. Full gate + the serve-path and emitter suites: the relocated emitter_e2e, acp_engine_e2e, and acpserve serve tests must pass UNCHANGED in their assertions (they pin behavior, not homes; if a test references the runner as the TurnRunner directly, retarget it to the adapter — an assertion-preserving retarget, recorded in the SUMMARY).
  </action>
  <verify>
    <automated>mise ci && go test ./internal/acpserve/... ./kit/runtime/ ./cmd/ass-guard/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/acpserve/kit_adapter.go exists; the adapter satisfies acp.TurnRunner whose declaration is byte-unchanged (git diff on internal/acp/server.go shows no signature change)
    - The stop marker mapping exists ONLY in the adapter; cron audit lines still record the kit-raw marker (cron wiring test asserts the audit content unchanged)
    - acpserve.Run's statement order is unchanged (the serve tests that pin construction/degradation order pass without edits to their assertions)
    - Full mise ci green including the simulator/zeroconfig smoke at cmd/ass-guard
  </acceptance_criteria>
  <done>The wire↔kit translation lives entirely app-side; the composition root wires it at the same statement positions; behavior pinned identical by relocated suites.</done>
  <tdd_eligibility>Relocated translation logic already pinned by existing tests (emitter e2e, serve tests). The adapter is glue over moved code — TDD-ineligible with rationale; the assertion-preserving test retarget is the proof.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 3: Requester wire bridge — acp ask surface implements the kit Requester (D-15)</name>
  <files>internal/acpserve/kit_adapter.go, internal/acpserve/acp_serve.go, kit/session/ask.go</files>
  <read_first>internal/acpserve/ask_surface.go (if landed by 17-02/17-04 — the locked home of the wire ask surface), kit/session/ask.go (NewAskBroker timeout contract :112-122 — NEGATIVE normalizes to DefaultAskTimeout, 0 = block forever; onSurface fires once per surfaced ask BEFORE the suspending turn's response), .planning/phases/17-permissions-elicitation/17-02-PLAN.md (locked contract this bridge rides — precondition), 25-CONTEXT.md D-15, .planning/milestones/v1.1-phases/12-product-functional-completeness/12-CONTEXT.md D-01 (timeout semantics mirrored)</read_first>
  <precondition>The locked 17-02/17-04 contract has landed: internal/acpserve/ask_surface.go exists with the structured permission/elicitation ask surface. This plan implements the kit Requester ON TOP of that surface and never redesigns its wire shapes.</precondition>
  <action>
    1. Precondition mark (phases 20-24 interaction): this task rides the LOCKED 17-02/17-04 contract — internal/acpserve/ask_surface.go exists at execution time with the structured permission/elicitation ask surface. Do NOT redesign its wire shapes; implement the kit Requester ON TOP of it.
    2. Implement kit Requester in acpserve: the implementation's Request blocks the caller's context until the ask resolves, bridging to the landed ask-surface machinery; cancelled/timeout resolves to the capture-shaped non-answer (12-D-01 semantics: configurable interval, then non-answer; 0 = block forever) — the SAME semantics the kit-side broker timeout already implements, so the bridge must not double-time-out: the kit owns timeout policy (D-15), the adapter owns transport. Record which side arms the timer based on the landed 17-02 code, and make the other side defer.
    3. Wire the implementation into the composition root at the established injection point; the suspended-turn semantics are byte-preserved: turn suspends on ask, reply lands as the tool result, turn death mid-ask delivers cancelled as a NORMAL outcome (the 17-03 locked behavior, proven by its relocated tests).
    4. A Telegram-shaped consumer could implement the same interface — assert this in the seams.go doc comment (one sentence; no Telegram code).
  </action>
  <verify>
    <automated>mise ci && go test ./kit/session/ ./internal/acpserve/... -count=1 -run 'Ask|ask'</automated>
  </verify>
  <acceptance_criteria>
    - The kit Requester has a working acp-side implementation bridging the locked ask surface; kit/session's AskBroker timeout/suspend tests pass unchanged
    - Ask timeout semantics unchanged: negative config normalizes to the default, 0 blocks forever, timeout yields the capture-shaped non-answer (relocated 12-01/17-03 batteries green)
    - No double timeout: exactly one layer arms the timer (recorded in code comment + SUMMARY)
    - mise ci green
  </acceptance_criteria>
  <done>The Requester seam is implemented acp-side over the locked ask surface; suspended-turn semantics preserved; KIT-02's minimal pair is complete and wired.</done>
  <tdd_eligibility>Bridge/wiring over behavior pinned by relocated 12-01/17-03 ask batteries. TDD-ineligible with rationale.</tdd_eligibility>
</task>

</tasks>

Artifacts this phase produces (plan 04's share): kit/runtime/seams.go (Emitter, Requester, Ask, Answer — the KIT-02 interfaces), internal/acpserve/kit_adapter.go (kitTurnAdapter + event→frame translations + stop mapping + ContentBlock conversion + Emitter/Requester impls), the severed runtime→acp edge. Phase-wide artifact list: see 25-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit events → ACP wire | The adapter is the ONLY place kit vocabulary becomes wire frames — a translation bug corrupts the client stream |
| client wire → kit asks | Requester bridges untrusted client replies into the kit's ask resolution |
| acpserve.Run composition order | Degradation arms depend on statement order; reordering silently flips startup behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-14 | Information Disclosure | kit/runtime after seam extraction | high | mitigate | Acceptance criterion: kit/runtime's dependency set contains no app packages; the transport-discipline suites (stdout-clean smoke, simulator E2E) run green; no ACP vocabulary remains kit-side |
| T-25-15 | Tampering | acp.TurnRunner frozen signature | high | mitigate | Acceptance criterion: git diff on internal/acp/server.go shows no interface change; Phases 16-20 wire surfaces untouched |
| T-25-16 | Denial of Service | adapter translation dropping/misordering events | high | mitigate | The relocated emitter_e2e and ordered-forwarder tests pin frame order and completeness; mise ci runs them per commit; the backpressure policy stays in the kit drain, unchanged |
| T-25-17 | Denial of Service | double ask-timeout (kit + adapter both arming timers) | medium | mitigate | Task 3 pins exactly one timer owner (kit per D-15) and the ask batteries prove the capture-shaped non-answer semantics |
| T-25-18 | Tampering | composition-order drift in acpserve.Run | medium | mitigate | Statement-order preservation is an explicit acceptance criterion; serve tests pin degradation arms |
| T-25-19 | Repudiation | cron audit stop-marker change (OQ3) | low | mitigate | OQ3 verified against kit/runtime/cron_wiring.go before finalizing: audit keeps kit-raw stop; cron wiring test asserts content unchanged |
</threat_model>

<verification>
- kit/runtime imports zero app packages (first edge severed); seams.go exports the one-method pair
- The adapter satisfies the FROZEN acp.TurnRunner; all translations relocated and pinned by unchanged assertions
- acpserve.Run statement order preserved; SetEmitter injection point unchanged
- Full mise ci green
</verification>

<success_criteria>
- KIT-02's minimal pair exists kit-side with acp-side implementations over the locked ask surface
- Zero transport vocabulary in kit/; behavior pinned identical by relocated suites
- OQ2 and OQ3 resolved and recorded (adapter home = internal/acpserve; stop-reason = kit-raw, adapter maps)
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-04-SUMMARY.md` when done
</output>
