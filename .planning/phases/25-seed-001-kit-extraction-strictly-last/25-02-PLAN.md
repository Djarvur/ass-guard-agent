---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - "kit/audit/"
  - "kit/hookdag/"
  - "kit/shaper/"
  - "kit/toolcat/"
  - "kit/provider/"
  - "kit/mcp/"
  - "kit/toolexec/"
  - "kit/modelrouting/"
  - "internal/audit/ (deleted by move)"
  - "internal/hookdag/ (deleted by move)"
  - "internal/shaper/ (deleted by move)"
  - "internal/toolcat/ (deleted by move)"
  - "internal/provider/ (deleted by move)"
  - "internal/mcp/ (deleted by move)"
  - "internal/toolexec/ (deleted by move)"
  - "internal/modelrouting/ (deleted by move)"
autonomous: true
requirements: [KIT-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Eight rank-1..3 packages live under kit/ verbatim — audit, hookdag, shaper, toolcat (rank 1), provider, mcp (rank 2), toolexec, modelrouting (rank 3) — with mise ci green after EACH of the two move commits"
    - "All three go:embed payloads travel with their packages: kit/hookdag/seeded.yaml, kit/toolcat/coretools.json, kit/modelrouting/defaults/config.yaml — defaults load identically at runtime (Pitfall 4)"
    - "The move inventory was re-taken at execution time: no NEW kit-to-app edge appeared from the phases-17-24 landed code without being recorded in the SUMMARY (D-19 gate comes later; the ledger here is the record)"
  artifacts:
    - path: "kit/audit/"
      provides: "Audit sink/mirror family (moved verbatim)"
    - path: "kit/hookdag/"
      provides: "Hook-DAG executor + seeded.yaml embed (moved verbatim)"
    - path: "kit/shaper/"
      provides: "Request shaper (moved verbatim)"
    - path: "kit/toolcat/"
      provides: "Tool catalog + coretools.json embed (moved verbatim)"
    - path: "kit/provider/"
      provides: "Provider interface + Anthropic/OpenAI adapters (moved verbatim)"
    - path: "kit/mcp/"
      provides: "MCP host (moved verbatim)"
    - path: "kit/toolexec/"
      provides: "Tool executors (moved verbatim)"
    - path: "kit/modelrouting/"
      provides: "Model routing resolver + defaults/config.yaml embed (moved verbatim)"
  key_links:
    - from: "kit/provider"
      to: "kit/profile, kit/redact, kit/shaper"
      via: "kit-internal import edges rewritten to the kit prefix (rank-2 depends on rank-0/1 homes)"
      pattern: "ass-guard-agent/kit/(profile|redact|shaper)"
    - from: "kit/modelrouting"
      to: "kit/modelrouting/defaults/config.yaml"
      via: "go:embed file-adjacent payload moved with the package directory (Pitfall 4)"
      pattern: "go:embed defaults/config.yaml"
  key_links_note: "kit→kit imports are legal by construction (D-19 forbids only kit→internal and kit→cmd); during pass 1 the moved packages may still import app packages where the research found none at rank<=3 — none exist (verified), so this plan severs nothing by design"
---

<objective>
Pass-1 continuation: move ranks 1-3 (eight packages) leaf-first as pure verbatim relocations, one mise-ci-green atomic commit per task — audit/hookdag/shaper/toolcat (rank 1), provider/mcp (rank 2), toolexec/modelrouting (rank 3).

Purpose: these are the packages the KIT-01 letter names that have zero app-code edges (verified by go list this session and in 25-RESEARCH.md §Current-State Inventory); moving them in import rank order keeps every intermediate tree compiling. The three go:embed payloads force whole-directory moves (Pitfall 4).

Output: kit/ holds 12 of the 15 promoted packages; every intermediate state proven by the full gate.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rank-1 moves — audit, hookdag, shaper, toolcat to kit/</name>
  <files>kit/audit/, kit/hookdag/, kit/shaper/, kit/toolcat/ (destinations; internal/ counterparts deleted), plus importer files rewritten by goimports</files>
  <read_first>25-RESEARCH.md §Move Order step 2 and §Current-State Inventory rank 1 (audit deps event+redact; hookdag deps event; shaper/toolcat deps profile — all kit-side after 25-01), 25-PATTERNS.md §The 15 promoted packages (hookdag seeded.yaml embed at config.go:79; toolcat coretools.json embed at catalog.go:11), 25-CONTEXT.md D-03/D-05, 25-01-SUMMARY.md (tracer-proven move procedure)</read_first>
  <action>
    Re-take the import audit for THIS plan's packages at execution time first: run the go-list import enumeration over internal/audit, internal/hookdag, internal/shaper, internal/toolcat. The planning-time graph shows zero app-package imports at these ranks; phases 17-24 landed code since the audit — if any of the four now imports an app package, STOP that package's move, record the new edge in the SUMMARY, and move only the clean packages (the edge belongs to the pass-2 seam plans, not a verbatim move).

    For each clean package: git mv the whole directory (internal/X to kit/X) — hookdag and toolcat MUST move as directories because their go:embed payloads (seeded.yaml; coretools.json) are file-adjacent. Then goimports -w the repo, go build ./... to prove zero stragglers, run mise ci, and commit atomically. Repeat per package or per safe batch — the commit boundary is the interrupt-safety guarantee (flagged assumption in 25-01). Zero body edits (D-03 pass-1 discipline; zero sanctioned edits in this phase). Spot-check after the move: go test ./kit/hookdag/ ./kit/toolcat/ -count=1 exercises DefaultHooks and the catalog so a missing embed fails loudly here, not at runtime.
  </action>
  <verify>
    <automated>mise ci && go test ./kit/hookdag/ ./kit/toolcat/ ./kit/audit/ ./kit/shaper/ -count=1 && ! test -d internal/hookdag</automated>
  </verify>
  <acceptance_criteria>
    - kit/audit, kit/hookdag, kit/shaper, kit/toolcat exist; internal/ counterparts are gone
    - kit/hookdag/seeded.yaml and kit/toolcat/coretools.json exist beside their consuming .go files; hookdag DefaultHooks and toolcat catalog tests pass (embed resolved at the new path)
    - mise ci exits 0 at the task's final commit
    - The color-moved diff review shows relocations, not rewrites; no non-import line changed
  </acceptance_criteria>
  <done>Rank-1 packages re-homed verbatim with embed payloads intact, gate green.</done>
  <tdd_eligibility>Not a behavior task — pure verbatim relocation (D-03); relocated suites are the proof.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 2: Rank-2+3 moves — provider, mcp, toolexec, modelrouting to kit/</name>
  <files>kit/provider/, kit/mcp/, kit/toolexec/, kit/modelrouting/ (destinations; internal/ counterparts deleted), plus importer files rewritten by goimports</files>
  <read_first>25-RESEARCH.md §Move Order steps 3-4 and §Current-State Inventory ranks 2-3 (provider deps profile+redact+shaper; mcp deps toolcat; toolexec deps provider+toolcat; modelrouting deps event+profile+provider+shaper — all kit-side after Task 1), 25-PATTERNS.md §The 15 promoted packages (modelrouting defaults/config.yaml embed at load.go:18), 25-CONTEXT.md D-03/D-05, 25-01-SUMMARY.md</read_first>
  <action>
    Same procedure as Task 1, rank-ordered. Re-take the execution-time import audit for these four packages first (phases 17-24 may have added edges — 24-01/24-02 land modelrouting/outcomes.go and dispatch.go INSIDE modelrouting, which is kit-bound by their locked plans and imports only kit/event + kit/modelrouting-local types; if anything else surfaced, stop that package and record). git mv each whole directory — modelrouting MUST move as a directory for the defaults/config.yaml embed. goimports -w, go build, mise ci, atomic commit. Then go test ./kit/modelrouting/ ./kit/provider/ -count=1 to prove the embedded default config loads at the new home.

    After Task 2 completes, run the same go-list import enumeration across ALL twelve moved packages and record the result in the SUMMARY as the pass-1 mid-point inventory — this is the record later plans and the D-19 gate check against drift.
  </action>
  <verify>
    <automated>mise ci && go test ./kit/modelrouting/ ./kit/provider/ ./kit/mcp/ ./kit/toolexec/ -count=1 && ! test -d internal/modelrouting && test -f kit/modelrouting/defaults/config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - kit/provider, kit/mcp, kit/toolexec, kit/modelrouting exist; internal/ counterparts gone
    - kit/modelrouting/defaults/config.yaml exists and modelrouting's defaults/loading tests pass at the new home
    - mise ci exits 0; the SUMMARY records the twelve-package import inventory (kit-internal edges only, zero app edges at ranks 0-3)
    - Twelve of the fifteen KIT-01 packages now live under kit/ (with 25-01's four)
  </acceptance_criteria>
  <done>Ranks 1-3 fully re-homed; pass-1 mid-point inventory recorded; gate green at every commit.</done>
  <tdd_eligibility>Not a behavior task — pure verbatim relocation (D-03).</tdd_eligibility>
</task>

</tasks>

Artifacts this phase produces (plan 02's share): eight moved packages under kit/ (audit, hookdag, shaper, toolcat, provider, mcp, toolexec, modelrouting) with all three go:embed payloads intact, plus the pass-1 mid-point import inventory recorded in the SUMMARY. Phase-wide artifact list: see 25-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit/provider → model providers | Outbound HTTP to model providers (surface unchanged; moved verbatim) |
| kit/mcp → MCP server subprocesses | stdio JSON-RPC to child processes (surface unchanged) |
| detector → merge gate | eval-change-class.sh must keep firing on moved model surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Tampering | kit/hookdag seeded.yaml, kit/toolcat coretools.json, kit/modelrouting defaults/config.yaml | high | mitigate | Whole-directory git mv per package (embeds are file-adjacent); post-move package tests exercise DefaultHooks/catalog/defaults so a stranded payload fails the task, not production (Pitfall 4) |
| T-25-06 | Information Disclosure | kit/redact consumers (audit, provider request capture) | medium | mitigate | Verbatim bodies; the relocated redaction-carry and audit suites run under mise ci; no marshaling shape touched (prohibition in 25-01 must_haves) |
| T-25-07 | Tampering | eval detector coverage of moved model surfaces | medium | mitigate | 25-01 already extended CLASSES with model:kit/provider and model:kit/shaper before these moves land; legacy paths retained |
| T-25-08 | Denial of Service | silent new kit→app edge from landed 17-24 code | medium | mitigate | Execution-time go-list re-audit is the first action of each task; any new edge halts that package's move and routes it to pass-2 seam plans; the D-19 gate (25-09) is the permanent enforcement |
</threat_model>

<verification>
- mise ci green at both atomic move commits
- All three go:embed payloads resolve at kit/ paths (package tests prove defaults load)
- Execution-time import audits show zero NEW app edges at ranks 0-3; any discovered edge is recorded and deferred to pass-2 plans
- Twelve of fifteen promotion-set packages live under kit/
</verification>

<success_criteria>
- kit/{audit,hookdag,shaper,toolcat,provider,mcp,toolexec,modelrouting} exist; internal/ counterparts gone
- Every intermediate tree compiled and passed the full gate before the next move started
- The mid-point inventory in the SUMMARY matches the planning-time graph (or records every drift with its disposition)
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-02-SUMMARY.md` when done
</output>
