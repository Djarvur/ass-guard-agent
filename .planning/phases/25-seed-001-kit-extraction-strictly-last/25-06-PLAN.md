---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 06
type: execute
wave: 6
depends_on: ["25-05"]
files_modified:
  - "kit/session/types.go"
  - "kit/session/session.go"
  - "kit/session/subagent.go"
  - "kit/runtime/catalog.go"
  - "kit/runtime/runtime.go"
  - "internal/acpserve/catalog_adapter.go"
  - "internal/acpserve/discover.go"
  - "internal/acpserve/acp_serve.go"
autonomous: true
requirements: [KIT-01, KIT-02, KIT-03]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "kit/session holds zero ecosys vocabulary: SubagentTypes is a map of a kit-defined agent-definition type, Hooks is a kit-defined hooks interface (lifecycle Fire plus the PreToolUse/PostToolUse pair the coreexec ToolHooks contract uses), and hook outcomes flow through a kit-defined outcome struct (severs session→ecosys, edge 7 of 8)"
    - "The kit consumes the command/skill/plugin world ONLY through its CommandCatalog interface — ParseInvocation, Lookup (Expand/Path), Mutability, SkillListing, AgentListing, Agents, Hooks, SkillExecute, MCPLayers — everything LoadCommandRegistry, expandUserBlocks, and sessionFor consume from ecosys+openspec today (D-17; severs runtime→ecosys and the LoadCommandRegistry openspec residual)"
    - ".claude/ discovery, precedence merging, and OpenSpec hosting live in internal/acpserve (discover.go + the catalog adapter) — the graceful-degradation arm registry-load-failure → expansion-off moves app-side verbatim; kit consumes an injected catalog, never discovery (D-17/SEED-001 inversion by injection, not promotion)"
    - "ecosys.Agent and HookOutcome values are mapped to their kit mirrors ONCE, in the app-side adapter — the kit never names an app type (Pitfall 3)"
    - "nil Catalog has a documented degradation (the existing engine-off/stub-exec path family), never a panic — the hostproof test will ride it"
  artifacts:
    - path: "kit/session/types.go"
      provides: "Kit-neutral AgentDef, Hooks, HookOutcome types (the session struct's vocabulary)"
    - path: "kit/runtime/catalog.go"
      provides: "CommandCatalog interface + kit Command record — the D-17 injection seam"
    - path: "internal/acpserve/catalog_adapter.go"
      provides: "ecosys.Registry + openspec mutability-backed CommandCatalog implementation; the ONE place app agent/hook values map to kit mirrors"
    - path: "internal/acpserve/discover.go"
      provides: "App-side discovery startup step (the relocated LoadCommandRegistry body: ecosys discovery + precedence + degradation arms)"
  key_links:
    - from: "kit/runtime"
      to: "internal/ecosys"
      via: "NONE after this plan — catalog injection replaces every Discover/Registry/ParseInvocation/SkillListing/AgentListing/SkillExecute/NewHookRunner/reg.Agents/mergeMCPServers use"
      pattern: "CommandCatalog"
    - from: "internal/acpserve/catalog_adapter.go"
      to: "kit/session/types.go"
      via: "adapter maps ecosys.Agent/HookRunner values to kit AgentDef/Hooks for the catalog's Agents()/Hooks() accessors"
      pattern: "AgentDef|Hooks"
  prohibitions:
    - requirement_id: KIT-01
      category: safety
      statement: "MUST NOT grant the kit enforcement or authority capabilities through the new seams — no policy-toggle structs, no allow-granting hook or catalog methods; the reference app remains the only enforcement point (D-18 by construction; the gate pipeline's authority stays app-side)"
---

<objective>
Sever the ecosystem edges: neutralize kit/session's ecosys-typed fields with kit-defined types, then invert the discovery dependency — the kit consumes an injected CommandCatalog, and .claude/ discovery + OpenSpec hosting become app-side startup steps in internal/acpserve (D-17).

Purpose: KIT-03's letter keeps ecosys/openspec app-side; D-17 satisfies SEED-001's "invert .claude/ layout" by injection, not promotion. This is the last plan touching session types before the toolkit plan builds on them.

Output: kit/session and kit/runtime with zero ecosys/openspec imports; catalog adapter + discovery helper in acpserve; mise ci green with subagent/session batteries passing.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-05-SUMMARY.md
@kit/session/session.go
@kit/session/subagent.go
@kit/runtime/runtime.go
@internal/ecosys/
</context>

<tasks>

<task type="auto">
  <name>Task 1: kit/session neutral types — AgentDef, Hooks, HookOutcome (severs session→ecosys)</name>
  <files>kit/session/types.go (new), kit/session/session.go, kit/session/subagent.go</files>
  <read_first>kit/session/session.go:57-70 (SubagentTypes map[string]ecosys.Agent; Hooks *ecosys.HookRunner) and :331 (injectHookContext(out ecosys.HookOutcome)), kit/session/subagent.go:30,44,133,273,299 (agentDef *ecosys.Agent params), the session Fire call sites :220-223,315-316,573-574 (SessionStart/UserPromptSubmit/Stop/SessionEnd lifecycle events) plus the PreToolUse/PostToolUse pair consumed via coreexec's ToolHooks interface (register.go:16), internal/ecosys/ (Agent struct fields the kit actually consumes; HookRunner.Fire signature; HookOutcome shape), 25-RESEARCH.md §The eight kit→app edges item 7 and §Pattern 4 (Agent/Hooks mirror-or-promote row), 25-CONTEXT.md D-05 (per-case promotion granted) / D-12 (minimal exports)</read_first>
  <action>
    1. Create kit/session/types.go with the kit-neutral vocabulary: an AgentDef struct carrying the agent fields the kit consumes (name/description/prompt/model/foreground-or-background flags — enumerate the exact consumed fields from ecosys.Agent's uses in subagent.go at execution time; carry only what is read, keeping mirrors minimal per D-12); a Hooks interface joining the lifecycle Fire method (same event/fields signature HookRunner.Fire exposes) with the PreToolUse/PostToolUse pair matching the ToolHooks contract coreexec's register.go defines; and a HookOutcome struct mirroring the fields injectHookContext reads. Choose mirror-over-promotion (behavioral types stay app-side; pure-data may promote — justify the choice per type in the file's doc comment).
    2. Retype session.go's SubagentTypes and Hooks fields, injectHookContext's parameter, and subagent.go's agentDef parameters to the kit types. Every session Fire/PreToolUse/PostToolUse call site compiles unchanged against the kit interface.
    3. In kit/runtime's sessionFor (temporary until Task 2 lands the catalog): map ecosys.Agent values to AgentDef and the HookRunner to the kit Hooks interface where the session config is built — a small mapping func local to runtime.go, deleted in Task 2 when the catalog's Agents()/Hooks() accessors supply kit-typed values directly.
    4. Delete the ecosys import from kit/session; the relocated subagent/hooks batteries (hooks_seam, subagent, subagent_types test files) pass with retargeted fixture types only — assertion-preserving.
  </action>
  <verify>
    <automated>go build ./kit/... && go test ./kit/session/ ./kit/runtime/ -count=1 && go list -deps ./kit/session | grep -c "ass-guard-agent/internal/ecosys$"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - kit/session/types.go defines AgentDef, Hooks (lifecycle Fire + PreToolUse/PostToolUse), HookOutcome with doc comments stating the mirror-or-promote choice per type
    - kit/session's dependency set contains no internal/ecosys (edge 7 severed)
    - All session Fire/hook call sites compile against the kit interface; the relocated subagent + hooks batteries pass with assertion-preserving retargets
  </acceptance_criteria>
  <done>Session speaks only kit vocabulary; the ecosys types it once named are mirrored kit-side and mapped app-side.</done>
  <tdd_eligibility>Type neutralization over behavior pinned by relocated session batteries. TDD-ineligible with rationale.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 2: CommandCatalog injection (D-17) — kit interface, app-side discovery, adapter</name>
  <files>kit/runtime/catalog.go (new), kit/runtime/runtime.go, internal/acpserve/catalog_adapter.go (new), internal/acpserve/discover.go (new), internal/acpserve/acp_serve.go</files>
  <read_first>kit/runtime/runtime.go consumption sites — :162-169,357-373 (ecosys.Discover + Registry/ServerConfig), :403,466 (ecosys.ParseInvocation), :423 (openspec mutability comparison — the residual from 25-05), :1056,1072 (SkillListing/AgentListing profile merges), :1092 (ecosys.SkillExecute override), :1106 (ecosys.NewHookRunner), :1190 (reg.Agents → SubagentTypes), :1368 (mergeMCPServers project-vs-lower layers), :375-382 (LoadCommandRegistry reading openspec CommandMutability + the graceful-degradation arm), 25-RESEARCH.md §Pattern 4 (the evidence-grounded CommandCatalog method table), 25-CONTEXT.md D-17, 25-PATTERNS.md §Interface-at-consumer (acp/server.go doc-comment family), 25-05-SUMMARY.md (the recorded openspec residual)</read_first>
  <action>
    1. Create kit/runtime/catalog.go: the CommandCatalog interface with exactly the consumption-derived method set — invocation parsing, key lookup returning a kit Command record (Expand plus Path), mutability query, skill/agent listing strings, the Agents accessor returning kit AgentDef values (Task 1's types), a Hooks accessor producing the kit Hooks interface for a session (session id, work dir, transcript path arguments — what NewHookRunner consumes today), the SkillExecute closure accessor, and the MCP lower-layers accessor feeding mergeMCPServers. Every method gets the contract doc comment (who implements, what degrades when nil). Define the kit Command record here, not in session.
    2. Retype the Runner: a Catalog field on the flat config struct (D-10 mirrored name) or a SetCatalog step — choose the form that preserves the composition statement order (LoadCommandRegistry's step is REPLACED by the app-side discovery step at the same position). Rewire expandUserBlocks, the Skill tool override, the profile merges, the hook-runner construction, SubagentTypes sourcing, and the MCP layer merge to the catalog; DELETE kit/runtime's LoadCommandRegistry body and the ecosys import; the Task-1 temporary mapping funcs die here (the catalog supplies kit-typed values directly). Delete the openspec import — the mutability residual closes via the catalog's Mutability method.
    3. internal/acpserve/discover.go: the relocated LoadCommandRegistry logic as an app-side startup step — ecosys discovery, precedence merging, openspec mutability table load, and the graceful-degradation arms VERBATIM (registry-load failure → expansion off, never stale, turns proceed on plain text). internal/acpserve/catalog_adapter.go: implements the kit interface over the discovered registry + openspec mutability + SkillExecute + the Agents()/Hooks() mappings from Task 1 (the ONE place app values become kit values).
    4. acpserve.Run: replace the runner.LoadCommandRegistry() call with the app-side discovery step + catalog injection at the SAME statement position; no other reorder. nil-Catalog degradation documented (the stub-exec/engine-off family).
    5. Relocated wiring tests (expansion, advisory, planmode, coreexec wiring) pass with assertion-preserving retargets to the adapter-built fixtures.
  </action>
  <verify>
    <automated>mise ci && go test ./kit/runtime/ ./internal/acpserve/ -count=1 && go list -deps ./kit/runtime | grep -cE "ass-guard-agent/internal/(ecosys|openspec)$"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - kit/runtime/catalog.go defines CommandCatalog with the consumption-derived method set and the kit Command record, all with contract doc comments
    - kit/runtime's dependency set contains NO internal/ecosys and NO internal/openspec (edges 2, 3, and the 25-05 residual severed — five of six runtime edges now gone)
    - The degradation arm (discovery failure → expansion off, plain-text turns) is preserved in discover.go and pinned by a relocated test
    - The catalog adapter is the only place ecosys values map to kit types; no kit signature names an app type
    - acpserve.Run statement order preserved (discovery step at LoadCommandRegistry's old position); mise ci green
  </acceptance_criteria>
  <done>The kit is ecosystem-blind: it consumes an injected catalog; discovery, precedence, and OpenSpec hosting are app-side startup steps.</done>
  <tdd_eligibility>Dependency inversion over behavior pinned by relocated wiring batteries (expansion, degradation arms). TDD-ineligible with rationale.</tdd_eligibility>
</task>

</tasks>

Artifacts this phase produces (plan 06's share): kit/session/types.go (AgentDef, Hooks, HookOutcome), kit/runtime/catalog.go (CommandCatalog + Command), internal/acpserve/catalog_adapter.go, internal/acpserve/discover.go — the D-17 injection seam and its app-side implementation. Phase-wide artifact list: see 25-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo .claude/ files → discovery → catalog adapter | Untrusted repo-shipped command/skill/agent definitions enter through the app-side adapter — validation and precedence stay app-side (D-17), the kit sees only post-validation values |
| catalog adapter → kit session hooks | Hook outcomes flow from repo/user hook scripts through the kit interface — deny-only authority stays app-side (the Phase-17 locked pipeline) |
| kit/session types → subagent dispatch | AgentDef carries agent definitions into dispatch — field minimality limits what a definition can influence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-25 | Elevation | kit Hooks interface widening hook authority | high | mitigate | The kit Hooks interface is deny/lifecycle-shaped exactly as the locked Phase-17 pipeline defines; no allow-granting method exists kit-side; the authority-creep prohibition in this plan's must_haves pins it |
| T-25-26 | Tampering | discovery validation lost in the inversion | high | mitigate | discover.go relocates the LoadCommandRegistry body VERBATIM incl. validation/precedence/degradation; relocated expansion + advisory batteries pin behavior |
| T-25-27 | Information Disclosure | AgentDef carrying more fields than the kit consumes | medium | mitigate | Field-minimality rule in Task 1 (carry only what subagent.go reads); doc comment records the consumed-field enumeration |
| T-25-28 | Denial of Service | nil catalog panic | medium | mitigate | Documented nil degradation (stub-exec family); doc comment states it; the 25-09 hostproof test rides the nil path |
| T-25-29 | Spoofing | mutability vocabulary drift (openspec values hardcoded kit-side) | low | mitigate | Mutability is queried from the catalog (app feeds openspec's table); no vocabulary literal in kit code — the relocated mutability boundary tests pin the behavior |
</threat_model>

<verification>
- kit/session and kit/runtime import neither ecosys nor openspec (edges 2, 3, 7 severed + the residual closed)
- The CommandCatalog method set covers every documented consumption site; nothing more (D-12)
- Discovery degradation arms preserved app-side and pinned by tests
- mise ci green; composition statement order preserved
</verification>

<success_criteria>
- The kit is blind to .claude/, OpenSpec, and ecosys internals — injection only (D-17)
- Kit-defined AgentDef/Hooks/HookOutcome are the session vocabulary; app mapping happens once, in the adapter
- Five of the six runtime edges are gone (only coreexec remains, for 25-07)
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-06-SUMMARY.md` when done
</output>
