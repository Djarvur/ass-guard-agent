---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 08
type: execute
wave: 8
depends_on: ["25-07"]
files_modified:
  - "internal/acpserve/e2e_opsx_test.go (moved subject)"
  - "internal/acpserve/e2e_opsx_matrix_test.go (moved subject)"
  - "internal/acpserve/acp_engine_e2e_test.go (moved subject)"
  - "kit/runtime/advisory_wiring_test.go"
  - "kit/runtime/apply_model_test.go"
  - "kit/runtime/ask_wiring_test.go"
  - "kit/runtime/planmode_wiring_test.go"
  - "kit/runtime/cron_wiring_test.go"
  - "kit/runtime/checkpoint_session_test.go"
  - "kit/runtime/emitter_e2e_test.go"
  - "kit/runtime/coreexec_wiring_test.go"
  - "kit/runtime/integration_test.go"
  - "kit/runtime/runner_battery_test.go"
  - "kit/runtime/fakes_test.go"
  - "kit/session/hooks_seam_test.go"
  - "kit/session/subagent_test.go"
  - "kit/session/subagent_types_test.go"
  - "kit/session/truncate_test.go"
autonomous: true
requirements: [KIT-01, KIT-03]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The gate-scope decision is made and recorded: the D-19 import gate is STRICT on non-test kit files and EXEMPT kit test files (grep excludes *_test.go; depguard files glob excludes the test matcher) — chosen because module zips ship test files, so out-of-module go test of kit would fail on app imports (research assumption A1, deferred by D-01 single-module); the decision is recorded in the 25-09 gate files and here"
    - "Every kit test file whose SUBJECT is the app adapter/composition/wire integration lives in internal/acpserve (subject-split, Phase-15 D-02 precedent); every kit test file whose subject is kit-core behavior stays kit-side with app-typed fixtures replaced by kit fakes (provider.Provider is an interface — kit fakes are constructible with zero app imports)"
    - "The test-function count is conserved: split + rework moves/retargets functions but deletes none — the ledger sum rule from 25-01 still holds at phase close"
  artifacts:
    - path: "internal/acpserve/ (app-E2E-subject test homes)"
      provides: "The moved e2e/integration test files whose subject is the adapter + wire, retargeted to the 25-04..25-07 adapters"
    - path: "kit/runtime/fakes_test.go"
      provides: "In-kit test doubles: scripted provider, recording emitter, immediate requester, fake catalog/toolkit — zero app imports"
  key_links:
    - from: "kit/runtime/*_test.go"
      to: "kit/runtime/fakes_test.go"
      via: "white-box wiring tests construct kit fakes instead of app packages"
      pattern: "fakes_test"
    - from: "internal/acpserve/*_test.go"
      to: "internal/acpserve/kit_adapter.go"
      via: "moved app-E2E subjects exercise the adapter directly (their natural home per subject-split)"
      pattern: "kitTurnAdapter"
---

<objective>
Resolve the white-box test collateral from pass 1: re-inventory the kit test files that import app packages (17 at planning time; phases 17-24 added more), split true app-E2E subjects into internal/acpserve, and rework the remaining in-kit wiring tests onto kit fakes — making the kit tree honest at the test layer before the D-19 gate lands in 25-09.

Purpose: the gate-scope decision (strict non-test, test-exempt) only stays honest if kit test files genuinely belong kit-side — either because their subject is kit-core (fake-able) or because they moved to their subject's home. Leaving app-importing tests in kit/ silently would make the "kit imports nothing app" claim true only of non-test code.

Output: app-E2E subjects in acpserve; kit wiring tests on kit fakes; every test function conserved; mise ci green.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-07-SUMMARY.md
@.planning/phases/15-internal-runtime-carve-step-0/15-CONTEXT.md
@kit/runtime/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-inventory + subject-split — app-E2E-subject tests move to internal/acpserve</name>
  <files>internal/acpserve/e2e_opsx_test.go, internal/acpserve/e2e_opsx_matrix_test.go, internal/acpserve/acp_engine_e2e_test.go (destinations for the planning-time e2e subjects; execution-time inventory may add files), kit/runtime/ (sources deleted)</files>
  <read_first>25-RESEARCH.md §Test files importing app packages (the 13 runtime + 4 session planning-time inventory) and its disposition recommendation, 25-07-SUMMARY.md (the final seam shapes the tests retarget to), 15-CONTEXT.md D-02 (the subject-split rule: tests follow their subject), the current kit/runtime/*_test.go import lists (execution-time re-inventory — phases 17-24 added test files)</read_first>
  <action>
    1. Re-inventory at execution time: list every kit/**/*_test.go that imports an app package (internal/... or cmd/...). Planning-time count: 17; the landed 17-24 phases will have added more. Record the list in the SUMMARY.
    2. Apply the subject rule to each: if the behavior under test is the app adapter/composition/wire integration — the planning-time e2e_opsx, e2e_opsx_matrix, and acp_engine_e2e files, plus any execution-time file whose assertions are about the serve path, wire frames, or the 25-04..25-07 adapters — the file MOVES to internal/acpserve (git mv + package rename to the acpserve test package + retarget to the adapter types from the 25-04..25-07 SUMMARYs). If the subject is kit-core behavior (runner battery, cron wiring/claim semantics, emitter forwarding, checkpoint session lifecycle, apply-model, ask wiring, plan-mode wiring, integration-over-interfaces), it STAYS and Task 2/3 rework its fixtures.
    3. Moved files keep every test function and assertion target (retarget construction sites to the adapters; never weaken an assertion). Record per-file dispositions (moved-vs-faked + one-line subject rationale) in the SUMMARY — this record is what makes the test-exempt gate scope auditable.
  </action>
  <verify>
    <automated>go test ./internal/acpserve/... -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - The execution-time inventory exists in the SUMMARY with per-file dispositions
    - Every moved file compiles in internal/acpserve and its tests pass against the adapters; no test function was deleted in a move
    - The remaining kit-side test files are exactly the fake-able kit-core subjects (Task 2/3 inputs)
  </acceptance_criteria>
  <done>App-E2E subjects live at their subject's home; the split record is written.</done>
  <tdd_eligibility>Test relocation/retargeting — no production behavior. TDD-ineligible.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 2: Kit-runtime white-box tests onto kit fakes</name>
  <files>kit/runtime/fakes_test.go (new), kit/runtime/advisory_wiring_test.go, kit/runtime/apply_model_test.go, kit/runtime/ask_wiring_test.go, kit/runtime/planmode_wiring_test.go, kit/runtime/cron_wiring_test.go, kit/runtime/checkpoint_session_test.go, kit/runtime/emitter_e2e_test.go, kit/runtime/coreexec_wiring_test.go, kit/runtime/integration_test.go, kit/runtime/runner_battery_test.go (execution-time inventory may adjust the file set)</files>
  <read_first>kit/runtime/fakes candidates from existing precedents — internal (now kit)/provider/provider.go:19-30 (Provider is an interface; scripted fakes constructible), the stub-exec path (kit/internal/enginebridge NewStubCatalogExec), 25-04..25-07 seam definitions in kit/runtime (Emitter, Requester, Scheduler, CommandCatalog, SessionToolkit — the fakes implement THESE), 25-07-SUMMARY.md (nil-degradation paths the fakes can ride), 25-RESEARCH.md §Code Examples hostproof sketch (recordingEmitter/immediateRequester shapes), 15-CONTEXT.md D-02 (subject-split rationale)</read_first>
  <action>
    1. Create kit/runtime/fakes_test.go with the kit test-double family, ALL in-package test files (package runtime), zero app imports: a scripted provider implementing the kit Provider interface; a recording Emitter capturing emitted events; an immediate/canned Requester; a fake Scheduler over in-memory automations; a fake CommandCatalog serving canned commands/listings; a fake SessionToolkit registering nothing and returning a no-op Reaper (or nil where the nil-degradation path is the assertion).
    2. For each staying test file, replace app-typed fixture construction (ecosys registries, coreexec toolkits, acp emitters, sched stores) with the fakes; keep every test function and its assertions — the fixture swap changes HOW the world is built, never WHAT is asserted. Where a test asserted app-interaction details (e.g. the exact coreexec registration), the assertion retargets to the seam contract (Attach called, Reaper returned) with the app-side detail test living in internal/acpserve (Task 1's homes) — record each such retarget in the SUMMARY.
    3. Delete every app import from the staying kit/runtime test files; go test ./kit/runtime/ green; the conserved test-function count is recorded (sum rule intact).
  </action>
  <verify>
    <automated>go test ./kit/runtime/ -count=1 && go vet ./kit/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - kit/runtime/fakes_test.go provides the five fake families; zero app imports in ANY kit/runtime test file
    - No test function deleted from kit/runtime (move-or-fake, never delete); assertions preserved or seam-contract-retargeted with SUMMARY records
    - go test ./kit/runtime/ -count=1 green including -race in the full gate
  </acceptance_criteria>
  <done>Kit-runtime tests are self-contained: kit subjects, kit fakes, zero app imports.</done>
  <tdd_eligibility>Test-infrastructure work (fakes) + fixture swaps — no production behavior. TDD-ineligible.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 3: Kit-session test files + final in-kit import sweep</name>
  <files>kit/session/hooks_seam_test.go, kit/session/subagent_test.go, kit/session/subagent_types_test.go, kit/session/truncate_test.go (execution-time inventory governs; 25-06 already retargeted the ecosys fixtures)</files>
  <read_first>kit/session test files' current import lists (execution-time), 25-06-SUMMARY.md (the kit AgentDef/Hooks/HookOutcome retargets already applied to these files' fixtures), 25-RESEARCH.md §Test files importing app packages (the 4 planning-time session files: hooks_seam, subagent, subagent_types, truncate)</read_first>
  <action>
    1. For each kit/session test file still importing an app package after 25-06's retargets: apply the same subject rule — app-subject files move to their subject's app home (most likely none; hooks_seam/subagent/subagent_types should already be kit-typed from 25-06), kit-subject files swap fixtures to kit fakes (a fake Hooks recording Fire calls, canned AgentDef maps).
    2. Final sweep: enumerate ALL kit/**/*_test.go app imports and drive the count to the recorded decision state — zero is the goal where subjects allow; any file that genuinely cannot sever (an app-E2E subject that Task 1 should have caught) moves instead. Record the final per-file state in the SUMMARY.
    3. Verify the conserved ledger: total test functions across kit/ + internal/ + cmd/ equal the 25-01 baseline (moves and fakes conserve functions).
  </action>
  <verify>
    <automated>go test ./kit/... -count=1 && go list -deps ./kit/session | grep -c "ass-guard-agent/internal/"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - kit/session test files import no app package (or the moved exceptions are recorded with subject rationale)
    - go test ./kit/... green; the whole-repo test-function sum equals the 25-01 baseline total
    - The SUMMARY's final test-disposition table is complete: every kit test file classified (clean / faked / moved) with zero unclassified
  </acceptance_criteria>
  <done>The kit tree — production AND test files — is app-import-free or honestly dispositioned; the ledger proves conservation.</done>
  <tdd_eligibility>Test-infrastructure work. TDD-ineligible.</tdd_eligibility>
</task>

</tasks>

Artifacts this phase produces (plan 08's share): the app-E2E-subject test homes in internal/acpserve, kit/runtime/fakes_test.go, the completed test-disposition record, and a kit tree whose test layer matches its production import cleanliness. Phase-wide artifact list: see 25-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit test files → gate scope | The test-exempt carve-out is a trust decision: an app import hiding in a kit test silently narrows the D-19 claim |
| fakes → seam contracts | A fake that drifts from the real adapter's behavior makes kit tests pass while the app breaks — mitigated by the moved app-E2E subjects testing the real adapters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-35 | Tampering | silent test deletion during split/rework | high | mitigate | Ledger sum rule (25-01 baseline) asserted in Task 3; move-or-fake, never delete, is the task rule; SUMMARY records per-file dispositions |
| T-25-36 | Spoofing | fakes diverging from real adapter behavior | medium | mitigate | The moved app-E2E subjects (Task 1) exercise the REAL adapters; in-kit fakes only cover kit-core subjects — the two layers complement, not substitute |
| T-25-37 | Repudiation | unevidenced gate-scope exemption | medium | mitigate | The decision (strict non-test, test-exempt) is recorded here and re-recorded in the 25-09 gate files with the A1 rationale; the disposition table makes every exempt file auditable |
| T-25-38 | Denial of Service | weakened assertion during retarget | medium | mitigate | Assertion-preserving rule with SUMMARY record per retarget; review compares assertion sets pre/post |
</threat_model>

<verification>
- The execution-time inventory + per-file dispositions recorded (moved / faked / clean)
- Zero app imports in kit/ test files (or recorded subject-driven moves)
- Test-function sum equals the 25-01 baseline; mise ci green
</verification>

<success_criteria>
- App-E2E subjects test the real adapters in internal/acpserve; kit-core subjects test with kit fakes in kit/
- The gate-scope decision (strict non-test, test-exempt) is honest: every exempt file is kit-subject by construction
- No test function lost anywhere in the phase
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-08-SUMMARY.md` when done
</output>
