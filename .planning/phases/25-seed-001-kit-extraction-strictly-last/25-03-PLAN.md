---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - "kit/session/"
  - "kit/engine/"
  - "kit/internal/enginebridge/"
  - "kit/runtime/"
  - "internal/session/ (deleted by move)"
  - "internal/engine/ (deleted by move)"
  - "internal/runtime/ (deleted by move)"
  - "go.mod (tidy only)"
autonomous: true
requirements: [KIT-01, KIT-03]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All fifteen promotion-set packages live under kit/: the 25-01/25-02 twelve plus kit/session, kit/engine, kit/runtime and kit/internal/enginebridge — moved verbatim WITH their expected app edges still present (kit/session→internal/ecosys; kit/runtime→internal/{acp,coreexec,ecosys,learning,openspec,sched}; kit/internal/enginebridge→internal/learning), because D-19 does not exist yet and D-03 forbids fixing while moving"
    - "kit/runtime's package doc kit-ambition paragraph is now simply true — the doc comment moved verbatim with the package"
    - "internal/ contains only app-only packages after this plan (D-06): acpserve, providerfactory, checkpointcmd, learningcmd, modelroutingcmd, profilecheckcmd, paritycli, acp, ecosys, openspec, coreexec, learning, firstrun, evalsuite, parity, plus the six unmapped-by-D-06 packages dispositioned app-side per D-05 default (defaults, drift, evalharness, loop, sched, version)"
    - "The whole-repo test-function sum equals the 25-01 ledger baseline total (D-20 relocated-ledger match)"
  artifacts:
    - path: "kit/session/"
      provides: "Session, Manager, Projector, AskBroker, transcript kinds (moved verbatim)"
    - path: "kit/engine/"
      provides: "Turn engine (moved verbatim)"
    - path: "kit/internal/enginebridge/"
      provides: "Engine↔ACP adapters (moved verbatim; kit-PRIVATE per D-04 — sole importer is kit/runtime, verified by go list this session)"
    - path: "kit/runtime/"
      provides: "Runner, RunnerConfig, sessionFor, cron wiring, park — the SEED-001 kit core (moved verbatim)"
  key_links:
    - from: "kit/runtime"
      to: "kit/internal/enginebridge"
      via: "the only enginebridge importer (go list verified) keeps its kit-internal edge"
      pattern: "ass-guard-agent/kit/internal/enginebridge"
    - from: "kit/runtime"
      to: "internal/* (EXPECTED, temporary)"
      via: "pass-1 verbatim state: the six app edges ride along unfixed (D-03); plans 25-04..25-07 sever them; the 25-09 D-19 gate enforces the end state"
      pattern: "ass-guard-agent/internal/"
---

<objective>
Pass-1 completion: move the remaining three top-level packages (session rank 4, engine rank 5, runtime rank 7) plus enginebridge into kit/ — enginebridge to kit/internal/enginebridge per D-04 — then close out pass 1 with the ledger sum proof, the D-06 app-side disposition record, and the D-20 gate family.

Purpose: the kit core (runtime) lands verbatim WITH its six known app edges intact — equivalence first, design second (D-03). Enginebridge goes to kit/internal because exactly one package imports it (runtime; go list verified this session) and D-04 seeds kit-private space organically, never preemptively public.

Output: kit/ complete (15 packages); internal/ app-only; pass-1 closed with ledger + inventory records.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-01-SUMMARY.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-02-SUMMARY.md
@.planning/phases/15-internal-runtime-carve-step-0/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rank-4+5 moves — session and engine to kit/</name>
  <files>kit/session/, kit/engine/ (destinations; internal/session, internal/engine deleted), plus importer files rewritten by goimports</files>
  <read_first>25-RESEARCH.md §Current-State Inventory ranks 4-5 (session: six kit deps + ecosys types-only edge at session.go:63,70 — SubagentTypes map and Hooks field; engine: event+session only), 25-RESEARCH.md §The eight kit→app edges item 7 (the session→ecosys edge detail), 25-CONTEXT.md D-03/D-05, 25-02-SUMMARY.md</read_first>
  <action>
    Re-take the execution-time import audit for session and engine (phases 17-24 landed gate.go/askqueue.go/reconcile.go/list.go/tombstone.go/seed.go/compaction.go/steerqueue.go/thinking-passthrough INSIDE internal/session and subagent/task integration per their locked plans — these files move WITH the package; verify their imports are still only ecosys on the app side, which their locked contracts keep). git mv internal/session to kit/session and internal/engine to kit/engine as whole directories; goimports -w; go build; mise ci; atomic commit.

    The session→ecosys edge moves INTO kit/ verbatim — that is the expected pass-1 state (kit may import internal until pass 2; D-03 forbids fixing it here; the D-19 gate does not exist yet). Do NOT neutralize the SubagentTypes/Hooks field types in this plan — that is 25-06's design work.

    Note the methods-without-receiver rule: session and engine move as whole packages precisely because Go forbids declaring methods outside the receiver's package (RESEARCH Anti-Patterns).
  </action>
  <verify>
    <automated>mise ci && go test ./kit/session/ ./kit/engine/ -count=1 && ! test -d internal/session && ! test -d internal/engine</automated>
  </verify>
  <acceptance_criteria>
    - kit/session and kit/engine exist; internal/ counterparts gone; mise ci exits 0
    - kit/session still compiles against internal/ecosys (the verbatim edge — expected temporary state, asserted present, not absent)
    - The relocated session suites (projector, boundary, ask, subagent batteries) pass at the new home
    - Color-moved review shows pure relocation
  </acceptance_criteria>
  <done>Ranks 4-5 re-homed verbatim with the ecosys edge intact and recorded; gate green.</done>
  <tdd_eligibility>Not a behavior task — pure verbatim relocation (D-03).</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 2: Rank-6+7 moves — enginebridge to kit/internal/enginebridge (D-04) and runtime to kit/runtime</name>
  <files>kit/internal/enginebridge/, kit/runtime/ (destinations; internal/runtime and internal/runtime/enginebridge deleted), plus importer files rewritten by goimports</files>
  <read_first>25-RESEARCH.md §Current-State Inventory ranks 6-7 (enginebridge deps engine+event+hookdag+session + one learning call at enginebridge.go:423; runtime deps all + six app edges listed), 25-CONTEXT.md D-04 (organic kit/internal seeding) and D-05 (runtime promotion: the Phase-15 carve whose package doc declares kit ambition), 25-RESEARCH.md §Anti-Patterns (methods move with receivers), 25-02-SUMMARY.md</read_first>
  <action>
    Re-take the execution-time audit for enginebridge and runtime. enginebridge: confirm its sole importer is still runtime (planning-time go list says yes; 22-01/22-03 land task-notification producers in internal/tasks — an APP package — which may import enginebridge types; if a non-runtime importer appeared, escalate in the SUMMARY and keep the destination kit/internal/enginebridge only if runtime remains the sole PRODUCTION importer, else fall back to kit/runtime/enginebridge per D-04 discretion and record the choice). git mv internal/runtime/enginebridge to kit/internal/enginebridge, then git mv internal/runtime to kit/runtime as whole directories (runtime's 17 test files and all wiring move together). goimports -w; go build; mise ci; atomic commit.

    kit/runtime lands with its six app edges intact (acp, coreexec, ecosys, learning, openspec, sched) — the pass-1 verbatim state. The 13 runtime test files that import app packages ride along (they compile in-module; the gate-scope decision is 25-08/25-09's). The package doc's SEED-001 kit-ambition paragraph moves verbatim and becomes true.

    After both moves: go test ./kit/runtime/ ./kit/internal/enginebridge/ -count=1 (the full runtime test set runs at home — the count comes from the execution-time ledger baseline for kit/runtime recorded in the SUMMARY; it was 87 at planning time and phases 17-24 grow it before execution).
  </action>
  <verify>
    <automated>mise ci && go test ./kit/runtime/... -count=1 && test -d kit/internal/enginebridge && ! test -d internal/runtime</automated>
  </verify>
  <acceptance_criteria>
    - kit/runtime and kit/internal/enginebridge exist; internal/runtime tree gone; mise ci exits 0
    - All 15 promotion-set packages now live under kit/ (12 from 25-01/25-02 + session, engine, runtime, enginebridge)
    - kit/runtime's import list contains exactly the six expected app packages (acp, coreexec, ecosys, learning, openspec, sched) — no MORE, no fewer, than the planning-time graph plus any recorded drift
    - The relocated runtime suites pass at the new home — the set defined by the execution-time ledger baseline for kit/runtime (runner battery, cron wiring, emitter e2e among them; the planning-time count was 87 and phases 17-24 grow it)
  </acceptance_criteria>
  <done>The kit core is complete under kit/ verbatim; pass 1 has moved everything it moves.</done>
  <tdd_eligibility>Not a behavior task — pure verbatim relocation (D-03).</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 3: Pass-1 close-out — ledger sum proof, D-06 disposition record, D-20 gate family</name>
  <files>.planning/phases/25-seed-001-kit-extraction-strictly-last/25-03-SUMMARY.md (disposition record section), go.mod (tidy only)</files>
  <read_first>.planning/phases/25-seed-001-kit-extraction-strictly-last/test-ledger.txt (baseline total from 25-01), 25-RESEARCH.md §Reference-app disposition (the six unmapped packages: defaults, drift, evalharness, loop, sched, version — each verified import-clean, zero further decisions needed), 25-CONTEXT.md D-06 (internal = app-only list), D-20 (equivalence bar), 25-01-SUMMARY.md</read_first>
  <action>
    1. Ledger proof (D-20): recompute the whole-repo test-function sum over kit/... plus all remaining internal/... and cmd/... scopes and assert it equals the 25-01 baseline total recorded in the ledger header. Record both numbers in the SUMMARY. A mismatch means a test function was added or dropped during pass 1 — pass 1 moves must not do that; investigate before proceeding.
    2. go mod tidy (the only module-file action; external deps untouched per RESEARCH §Package Legitimacy Audit).
    3. D-06 disposition record: in the SUMMARY, list internal/'s final package set — the fifteen D-06 app-only packages plus the six unmapped (defaults: go:embed seed consumed by firstrun; drift: imports profile/kit only; evalharness: imports session/kit only; loop: parity helper; sched: implements the Scheduler port app-side per D-16; version: imports nothing) — each marked stays-app-side per D-05 default.
    4. Run the full D-20 gate family as pass 1's exit proof: mise ci green; the CLI-contract golden tests in cmd/ass-guard/cli_contract_test.go (TestCLIBinaryContractRootAndACP, TestCLIBinaryContractSupportCommands, TestCLIBinaryContractProfile) green; TestZeroConfigFirstRun green; mise eval-check-changed dry-run — it must now FIRE (exit 3) on this branch's diff because 25-01 extended CLASSES with kit/ paths, proving the behavioral detector sees the extraction.
    5. Record in the SUMMARY: the pass-1 diff reviewed under git diff --color-moved=dimmed-zebra is a pure relocation; zero body edits were made in all of pass 1 (D-03 sanctions zero).
  </action>
  <verify>
    <automated>mise ci && go test ./cmd/ass-guard/ -run 'TestCLIBinaryContract|TestZeroConfigFirstRun' -count=1 && mise eval-check-changed; test $? -eq 3</automated>
  </verify>
  <acceptance_criteria>
    - Whole-repo test-function sum equals the 25-01 baseline total (both numbers recorded in the SUMMARY)
    - CLI golden + zeroconfig smoke green at the extracted layout
    - mise eval-check-changed exits 3 on the pass-1 diff (the kit/ classes fire — the silent-gate fix is proven live)
    - The SUMMARY contains the complete D-06 app-side disposition (15 + 6 packages) and the zero-body-edits pass-1 record
  </acceptance_criteria>
  <done>Pass 1 closed: kit/ complete, internal/ app-only, equivalence instruments all green, records written.</done>
  <tdd_eligibility>Not a behavior task — verification and record-keeping.</tdd_eligibility>
</task>

</tasks>

Artifacts this phase produces (plan 03's share): kit/session, kit/engine, kit/runtime, kit/internal/enginebridge (verbatim moves — the kit core), the pass-1 ledger proof and D-06 disposition record. Phase-wide artifact list: see 25-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit/runtime → ACP transport | The turn loop touches the stdout JSON-RPC boundary via internal/acp (edge severed properly in 25-04; here it moves verbatim) |
| kit/session → transcript/checkpoint stores | Persisted data shapes (JSONL lines, checkpoint objects) must marshal identically |
| kit/internal/ → external importers | kit/internal is kit-PRIVATE (D-04); nothing outside kit/ may import it once the D-19 gate lands |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-09 | Information Disclosure | kit/runtime forwarders (transport discipline) | high | mitigate | Verbatim move; the relocated stdout-clean suite (TestACPServeWiresStdoutClean family, zeroconfig smoke) runs green at the new home in Task 3's battery; 25-04 then properly extracts the translation |
| T-25-10 | Repudiation | audit mirror wiring during runtime move | medium | mitigate | Construction order in acpserve.Run untouched in pass 1 (composition not yet redesigned); audit suites relocated and green |
| T-25-11 | Tampering | transcript/checkpoint persisted shapes | medium | mitigate | 25-01 prohibition carries: verbatim bodies, no type renames; projector/reconstruction suites green at kit/session |
| T-25-12 | Denial of Service | ledger drift (test function silently lost in a move) | medium | mitigate | Task 3's sum-equality proof against the 25-01 baseline makes any loss a hard failure before pass 2 starts |
| T-25-13 | Elevation | kit/internal/enginebridge visibility | low | accept | kit/internal is compiler-enforced private to kit/ (Go internal rule); sole importer verified runtime; D-19 gate will enforce no-external-imports in 25-09 |
</threat_model>

<verification>
- All fifteen promotion-set packages under kit/; internal/ app-only per D-06
- mise ci green at every commit; ledger sum equals baseline
- The six expected kit→app edges are the ONLY app edges in kit/ (execution-time audit recorded in SUMMARY)
- eval detector fires on the pass-1 diff (exit 3)
</verification>

<success_criteria>
- kit/ holds the complete KIT-01 set + runtime + enginebridge, all verbatim
- Pass 1 committed as pure relocations with zero body edits; ledger and disposition records written
- The behavioral eval gate provably still fires at the extracted layout
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-03-SUMMARY.md` when done
</output>
