---
phase: 25-seed-001-kit-extraction-strictly-last
plan: 05
type: execute
wave: 5
depends_on: ["25-04"]
files_modified:
  - "kit/runtime/scheduler.go"
  - "kit/runtime/runtime.go"
  - "kit/runtime/cron_wiring.go"
  - "kit/internal/enginebridge/enginebridge.go"
  - "internal/sched/sched.go"
  - "internal/acpserve/kit_setup.go"
  - "internal/acpserve/acp_serve.go"
autonomous: true
requirements: [KIT-01, KIT-02, KIT-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The runner core talks to scheduling through the kit-defined Scheduler port (exactly four methods: Now, Due, ClaimForFire, CatchUp — the entire schedule surface the core consumes at cron_wiring.go:116-140,216); internal/sched's behavioral store stays app-side and satisfies the port structurally (D-16)"
    - "Automation and FireEvent promote to kit/ as pure-data structs with their json tags verbatim — the persisted schedule-store YAML shape is unchanged; internal/sched keeps compiling via type aliases (D-05 per-case promotion, research-recommended)"
    - "The enginebridge learning dependency is a one-method kit port (Lookup situation → answer, ok); learning.Entry and the store stay app-side; a one-method adapter at the wiring site bridges them (severs enginebridge→learning AND runtime→learning)"
    - "SetupEngine keeps its name (the acpserve compile surface from 15-06) and takes an inputs parameter carrying exactly the kit-neutral values it consumes — openspec config, tool registration, and pattern-table derivation move app-side (OQ4 resolution); hookdag.DefaultHooks stays kit self-config (kit/hookdag is kit-owned machinery, mirroring modelrouting's embedded defaults)"
    - "No port signature mentions an app type (Pitfall 3): every type in Scheduler/LearnedStore/EngineSetup signatures is kit-defined"
  artifacts:
    - path: "kit/runtime/scheduler.go"
      provides: "Scheduler port interface + promoted Automation/FireEvent value structs (json tags verbatim)"
    - path: "internal/acpserve/kit_setup.go"
      provides: "App-side setup loader: openspec config + tool registration + pattern-table derivation + learning.Open + the one-method LearnedStore adapter"
    - path: "internal/sched/sched.go"
      provides: "Behavioral store (app-side) + type aliases to the promoted kit value structs — persisted YAML untouched"
  key_links:
    - from: "kit/runtime"
      to: "internal/sched"
      via: "NONE after this plan — the port is satisfied structurally at the composition root (SetSchedule receives the store)"
      pattern: "SetSchedule"
    - from: "internal/acpserve/kit_setup.go"
      to: "kit/internal/enginebridge"
      via: "LearnedStore adapter feeds the bridge's Lookup calls"
      pattern: "Learned|Lookup"
---

<objective>
Sever three of the six kit→app edges: runtime→sched (D-16 Scheduler port — the Phase-15 D-13 amendment's named successor, no second deferral), runtime→learning + enginebridge→learning (one-method LearnedStore port), and the openspec half of runtime→openspec (SetupEngine parameterization per OQ4).

Purpose: the cron seam is the bidirectional core↔cron call graph (4 of 9 cron methods called from core; runAutomationTurn → runOneTurn) turned into interface calls; the learning seam is the trivially port-shaped single Lookup call; SetupEngine stops self-loading app-hosted config so D-11's explicit-steps discipline holds with the app owning ecosystem hosting (D-17).

Output: kit/runtime with Scheduler port + promoted value structs; enginebridge with LearnedStore port; app-side setup loader; aliases keeping sched's persisted API intact; mise ci green with cron batteries passing unchanged.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-CONTEXT.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-RESEARCH.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-PATTERNS.md
@.planning/phases/25-seed-001-kit-extraction-strictly-last/25-04-SUMMARY.md
@kit/runtime/cron_wiring.go
@kit/runtime/runtime.go
@kit/internal/enginebridge/enginebridge.go
@internal/sched/sched.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scheduler port (D-16) — interface, value-struct promotion, structural satisfaction</name>
  <files>kit/runtime/scheduler.go (new), kit/runtime/runtime.go, kit/runtime/cron_wiring.go, internal/sched/sched.go, internal/acpserve/acp_serve.go</files>
  <read_first>kit/runtime/cron_wiring.go:116-140,216 (the four store calls — the port's exact method set), kit/runtime/runtime.go:1459-1461 (SetSchedule concrete-typed setter to invert) and the schedule field :186, internal/sched/sched.go (Automation :56-73 and FireEvent :94-97 value structs with json tags; the behavioral store's full method set), 25-RESEARCH.md §Pattern 1 (seam inversion example), 25-CONTEXT.md D-16, 15-CONTEXT.md D-13 amendment (the deferred design this lands)</read_first>
  <action>
    1. Create kit/runtime/scheduler.go: the Scheduler interface with EXACTLY the four methods the runner core calls (returning time, due automations, claim-by-id, catch-up events), each with a one-line contract doc comment; plus the Automation and FireEvent structs moved verbatim from internal/sched with their json tags byte-identical (pure data, zero app deps — the D-05 per-case promotion the research recommends; the json tags are the persisted shape and must not change).
    2. In kit/runtime: retype the schedule field and SetSchedule parameter to the port; cron_wiring's four call sites compile unchanged against the interface. Add the documented nil degradation to the port's doc comment (nil schedule → the existing structured no-store errors — runtime.go's current nil behavior at the InteractiveConfig comment :1139 carries forward).
    3. In internal/sched: replace the moved struct definitions with type aliases to the kit structs (the store's methods, YAML marshaling, and public API compile untouched; persisted files round-trip identically). The store then satisfies the kit Scheduler interface structurally — verify the method sets match exactly (rename nothing; if a store method name differs from the port, adapt at the composition root with a one-method adapter at the wiring site instead of renaming store methods).
    4. acpserve.Run: the SetSchedule(scheduleStore) call site is unchanged in position and argument (structural satisfaction); assert in the SUMMARY that no adapter was needed (or record the adapter if method names forced one).
    5. The cron batteries (cron_wiring tests, automation-turn tests) pass UNCHANGED in assertions — they pin firing/claim/catch-up behavior.
  </action>
  <verify>
    <automated>mise ci && go test ./kit/runtime/ ./internal/sched/ -count=1 && go list -deps ./kit/runtime | grep -c "ass-guard-agent/internal/sched$"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - kit/runtime/scheduler.go exports the four-method Scheduler port and the two promoted value structs with byte-identical json tags
    - kit/runtime's dependency set no longer contains internal/sched (edge severed)
    - internal/sched compiles via aliases; its YAML round-trip tests pass unchanged (persisted shape proof)
    - The store satisfies the port (compile-level: SetSchedule accepts it at the unchanged call site)
    - Cron batteries green with unchanged assertions
  </acceptance_criteria>
  <done>The kit core speaks scheduling through its own port; the store stays app-side; persisted schedule data untouched.</done>
  <tdd_eligibility>Interface port over behavior pinned by existing cron batteries. TDD-ineligible with rationale.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 2: LearnedStore port — one-method seam in enginebridge, adapter at the wiring site</name>
  <files>kit/internal/enginebridge/enginebridge.go, internal/acpserve/kit_setup.go (new), internal/acpserve/acp_serve.go</files>
  <read_first>kit/internal/enginebridge/enginebridge.go:35 (BridgeConfig.Learned concrete-typed field) and :418-428 (the ONLY call — d.learned.Lookup(situation) consuming the answer string and ok flag), kit (was internal)/learning/store.go:70 (Lookup returns Entry — the app value type), 25-RESEARCH.md §The eight kit→app edges items 5 and 8, 25-PATTERNS.md §Learned one-method port and §One-method adapter at the wiring site (checkpointerAdapter precedent runtime.go:256-268), 25-CONTEXT.md D-12/D-17</read_first>
  <action>
    1. Define the kit port at the point of use in kit/internal/enginebridge: a one-method interface, Lookup taking the situation string and returning the answer string plus a found bool — the (string, bool) shape the call site consumes, NOT the app Entry type (D-12 minimal; Entry stays app-side).
    2. Retype BridgeConfig.Learned to the port; the Ask method's nil-check + ErrAskPending semantics are preserved exactly (the relocated wiring tests pin them).
    3. Create internal/acpserve/kit_setup.go and put the one-method adapter there (the wiring site, per the checkpointerAdapter precedent): it wraps the app learning store, returning the entry's Answer field and ok flag. Delete the learning import from kit/internal/enginebridge AND from kit/runtime (runtime's learning.Open call moves app-side in Task 3).
    4. The adapter is constructed in acpserve.Run where the store opens today; nil store → the port's nil (ErrAskPending path) — document the nil degradation in the interface comment.
  </action>
  <verify>
    <automated>go build ./... && go test ./kit/internal/enginebridge/ ./internal/acpserve/ -count=1 && go list -deps ./kit/internal/enginebridge | grep -c "ass-guard-agent/internal/learning$"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - The LearnedStore port exists in kit/internal/enginebridge with the (string, bool) Lookup signature; no app type appears in its signature (Pitfall 3)
    - kit/internal/enginebridge and kit/runtime dependency sets contain no internal/learning (both edges severed)
    - The ErrAskPending nil-store path is preserved and pinned by relocated tests
    - The adapter lives in internal/acpserve/kit_setup.go, not in any kit package
  </acceptance_criteria>
  <done>The learning dependency is a one-method kit port satisfied by an app-side adapter; Entry never crosses the boundary.</done>
  <tdd_eligibility>One-method port over behavior pinned by relocated enginebridge tests. TDD-ineligible with rationale.</tdd_eligibility>
</task>

<task type="auto">
  <name>Task 3: SetupEngine parameterization (OQ4) — openspec hosting moves app-side</name>
  <files>kit/runtime/runtime.go, internal/acpserve/kit_setup.go, internal/acpserve/acp_serve.go</files>
  <read_first>kit/runtime/runtime.go:275-337 (SetupEngine's self-loads: openspec.DefaultConfig, openspec.RegisterTools into the catalog, openspec.FromConfig → pattern table, hookdag.DefaultHooks, learning.Open) and :269-273 (engine-off degradation arm), internal/acpserve/acp_serve.go:267-272 (the SetupEngine call site inside the engine-enabled arm), 25-CONTEXT.md D-11 (thin constructor, explicit steps), D-17 (openspec hosting app-side), D-10 (flat mirrored config), 25-RESEARCH.md Open Question 4 + §Pattern 4 table row (OpenSpec tools + pattern table), internal/openspec/ package surface (FromConfig return type — enumerate what the kit actually consumes)</read_first>
  <action>
    1. KEEP the name SetupEngine (the acpserve compile surface from 15-06). Change its signature to accept an inputs parameter — a flat struct with mirrored names per D-10 — carrying exactly the kit-neutral values SetupEngine consumes: the derived pattern-table data and the LearnedStore port from Task 2. NEVER an openspec-typed value in the signature (Pitfall 3): enumerate at execution time which fields FromConfig's result actually feeds onward into enginebridge/hookdag, and carry THOSE as plain kit-side types.
    2. Move the app-hosted loads to internal/acpserve/kit_setup.go as an explicit startup helper the composition root calls BEFORE SetupEngine, preserving today's order and degradation semantics: openspec.DefaultConfig + RegisterTools into the shared kit catalog (D-17: the app registers tools; the kit owns the catalog type), the FromConfig-derived values, and learning.Open feeding the Task-2 adapter. hookdag.DefaultHooks STAYS inside SetupEngine — kit/hookdag is kit-owned machinery self-loading its embedded defaults (same legitimacy as kit/modelrouting's embedded config); record this OQ4 sub-decision in the SUMMARY.
    3. Preserve the engine-off arm and the degrade-never-crash semantics (15-D-04): a setup-helper failure degrades exactly as SetupEngine's failures do today (engine off, serve continues). The composition root's statement order grows the helper call immediately before SetupEngine — no other reorder.
    4. The relocated engine wiring tests pin the semantics: engine-on path identical, engine-off identical, learning lookups identical through the adapter.
  </action>
  <verify>
    <automated>mise ci && go test ./kit/runtime/ ./internal/acpserve/ ./cmd/ass-guard/ -count=1 && go list -deps ./kit/runtime | grep -c "ass-guard-agent/internal/openspec$"; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - SetupEngine's signature takes the kit-neutral inputs struct; no openspec-typed value appears in any kit signature
    - kit/runtime's dependency set no longer contains internal/openspec or internal/learning (openspec FULLY severed only if LoadCommandRegistry's mutability read was also relocated — if that read remains, record it as the residual edge handed to 25-06's CommandCatalog.Mutability and assert the count reflects exactly that residual)
    - The engine-on/engine-off degradation arms behave identically (relocated wiring tests green with unchanged assertions)
    - acpserve.Run statement order preserved with the helper call adjacent to SetupEngine
  </acceptance_criteria>
  <done>SetupEngine is parameterized with kit-neutral inputs; the app hosts openspec and learning; the kit consumes injected values per D-11/D-17.</done>
  <tdd_eligibility>Parameterization of behavior pinned by relocated engine wiring tests. TDD-ineligible with rationale.</tdd_eligibility>
</task>

</tasks>

Artifacts this phase produces (plan 05's share): kit/runtime/scheduler.go (Scheduler port + promoted Automation/FireEvent), the LearnedStore port in kit/internal/enginebridge, the EngineSetup inputs parameter on SetupEngine, internal/acpserve/kit_setup.go (app-side openspec/learning loader + LearnedStore adapter), internal/sched aliases. Phase-wide artifact list: see 25-01-PLAN.md.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit Scheduler port → sched store | The store's claim/catch-up semantics guard against double-firing automations; the port must not widen or weaken them |
| app setup loader → kit engine | The app feeds pattern tables and learning answers the engine consumes — injected data crosses into the kit |
| persisted schedule YAML | Value-struct promotion must not move the persisted shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-20 | Tampering | Automation/FireEvent json tags during promotion | high | mitigate | Tags moved byte-identical; sched's YAML round-trip tests pass unchanged (acceptance criterion) |
| T-25-21 | Denial of Service | Scheduler port mismatch (claim/catch-up semantics drift) | high | mitigate | Port method set is EXACTLY the four consumed calls; cron batteries run green with unchanged assertions; structural satisfaction means no translation layer can drop semantics |
| T-25-22 | Elevation | port signatures leaking app types (Pitfall 3) | medium | mitigate | Explicit acceptance criterion: every type in Scheduler/LearnedStore/EngineSetup signatures is kit-defined; go-list check proves the severed imports |
| T-25-23 | Tampering | SetupEngine degradation arms | medium | mitigate | Relocated engine wiring tests pin engine-on/engine-off arms with unchanged assertions; helper failure degrades identically |
| T-25-24 | Repudiation | learning answer provenance through the adapter | low | accept | The adapter forwards Entry.Answer verbatim; audit paths unaffected (learning lookups are read-only; no audit-shape change) |
</threat_model>

<verification>
- Three edges severed: kit/runtime→sched, kit/runtime→learning, kit/internal/enginebridge→learning; openspec residual recorded precisely (zero or the named LoadCommandRegistry remainder)
- The four-method Scheduler port + promoted value structs with byte-identical json tags
- SetupEngine parameterized per OQ4 with the hookdag self-config sub-decision recorded
- mise ci green; cron/engine batteries unchanged
</verification>

<success_criteria>
- Scheduling and learning reach the kit only through kit-defined ports satisfied structurally or by wiring-site adapters
- No port signature mentions an app type
- Persisted schedule YAML and engine degradation semantics provably unchanged
</success_criteria>

<output>
Create `.planning/phases/25-seed-001-kit-extraction-strictly-last/25-05-SUMMARY.md` when done
</output>
