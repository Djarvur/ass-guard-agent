---
phase: 20-built-in-commands-skills-per-agent-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/runtime/commands.go
  - internal/runtime/runtime.go
  - internal/acp/types.go
  - internal/acp/emitter.go
  - internal/acp/server.go
  - internal/acpserve/acp_serve.go
  - internal/runtime/commands_test.go
  - internal/acp/available_commands_test.go
autonomous: true
requirements: [ACP-04, CMDS-01, CMDS-02]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Typing /status in a session returns the live snapshot with ZERO provider calls (fake provider Stream count == 0) and the turn ends with stopReason end_turn (CMDS-02, D-05)
    - The class-B echo reaches the client as a user_message_chunk ContentChunk whose messageId differs from the messageId on the following agent_message_chunk output chunks, so the client renders two messages (D-05)
    - All thirteen reserved builtin names (help status cost mcp memory permissions doctor config model clear resume compact init) are pre-seated in the chain — a discovered file, skill, or agent sharing a builtin name never fires via slash, and the loser triggers the D-01 shadow-check warning (D-01)
    - Chain collisions among skills/agents/file commands break by chain order builtins → skills → agents → file, silently and deterministically, first-writer-wins (D-02, CMDS-01)
    - available_commands_update at session start lists exactly the chain winners — full replacement set, sorted deterministically, shadowed entries absent not annotated (D-04, ACP-04)
    - The local_command transcript line records the full 16-D-22 shape (key, args, source, outcome) for every class-B invocation, appended via the REDACTED path
  artifacts:
    - internal/runtime/commands.go — commandChain type, chainEntry, buildChain, commandChain.resolve, builtinCommand table, RESERVED builtin name set, advertisement builder
    - internal/acp/types.go — KindAvailableCommandsUpdate const, AvailableCommandFrame, AvailableCommandInputFrame
    - internal/acp/emitter.go — updKindUserMessageChunk const, EmitterHandle.UserMessageChunk(messageID, text)
    - internal/acp/server.go — Server.NotifyAvailableCommands(sessionID, cmds)
    - internal/runtime/runtime.go — class-B intercept in Runner.Run + chain accessor (atomic.Pointer)
    - internal/acpserve/acp_serve.go — session-start advertisement wiring + rescan-callback seam
    - internal/runtime/commands_test.go, internal/acp/available_commands_test.go — Wave 0 test scaffolds
  key_links:
    - Class-B intercept placement in Runner.Run: AFTER routeAskReply (runtime.go:1611 call) and AFTER turnMu + forwarder subscriptions, BEFORE the engine branch (runtime.go:539) — placement after the engine branch is the research Anti-Pattern 3 (divergent engine-on/off behavior)
    - The advertisement builder reads the SAME chain the resolver resolves — one source of truth, autocomplete cannot lie (D-04)
    - Echo + output chunks ride the turn's in-hand emitter handle (foreground lane), NOT a side channel
  assumptions:
    - "PROBE-UNRESOLVED ACP-04 (unclassified): deterministic probe returned 'unclassified — review manually'; row stays flagged, never auto-resolved. Goal-backward coverage: schema-derived frame golden test + session-start emission test assert the observable behavior."
    - "PROBE-UNRESOLVED CMDS-01 (concurrency, probe: 'If interrupted or run in parallel, what is guaranteed?'): the guarantee is by design — the chain is immutable once built and read through one atomic pointer; the concurrent rescan x turn -race proof lands in plan 20-05. Probe row remains flagged unresolved."
    - "PROBE-UNRESOLVED CMDS-02 (unclassified): probe returned 'unclassified — review manually'; row stays flagged. Goal-backward coverage: per-command battery (this plan + 20-02) asserts zero-model-turn + local_command line per command."
---

<objective>
Prove the phase skeleton end-to-end with ONE class-B command and harden the chain semantics it rides: an immutable command chain (builtins → skills → agents → file, winners-only) behind an atomic pointer on the Runner, the class-B intercept in `Runner.Run`, the `available_commands_update` wire frame emitted at session start, and the D-05 echo/output shape (per CMDS-01, CMDS-02 partial, ACP-04; decisions D-01..D-05).

Purpose: every later plan (class-B family, skills/agents, rescan) builds on this exact slice — the intercept placement, the chain accessor, and the two new wire frames are the load-bearing architecture; proving them first catches a dead-end after one commit instead of six plans.
Output: working /status command end-to-end in a runner test, chain resolver with collision + reservation semantics, `available_commands_update` + `user_message_chunk` frames with golden-JSON contract tests, session-start advertisement wired in acpserve.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-CONTEXT.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-RESEARCH.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-PATTERNS.md

Phase 16 executed: TurnEmitter + EmitterHandle (internal/acp/emitter.go), AppendLocalCommand +
local_command kind (internal/session/manager.go:362, transcript.go additive family). Phase 15 carved
internal/runtime. expandUserBlocks/invocationFor (runtime.go:395–476) is the seam the chain
generalizes.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — /status end-to-end: chain → class-B intercept → echo/chunks/end_turn → local_command line → session-start advertisement</name>
  <files>internal/runtime/commands.go, internal/runtime/runtime.go, internal/acp/types.go, internal/acp/emitter.go, internal/acp/server.go, internal/acpserve/acp_serve.go, internal/runtime/commands_test.go, internal/acp/available_commands_test.go</files>
  <read_first>internal/runtime/runtime.go:357–476 (LoadCommandRegistry, expandUserBlocks, invocationFor — the seam being generalized), runtime.go:479–566 (Run ordering: sessionFor → turnMu → markClientTurn → bus subscriptions → toContentBlocks → routeAskReply → engine branch at :539), runtime.go:1582–1603 (advisoryNoteDue), internal/acp/server.go:299–317 (NotifyConfigOptions — the Notify template to mirror), internal/acp/emitter.go:17–24 (updKind family) and :427–441 (AgentMessageChunk(messageID, text) — the UserMessageChunk template), internal/acp/types.go:176–234 (ThoughtChunkFrame + KindConfigOptionUpdate/ConfigOptionFrame — frame + const templates), internal/session/manager.go:362–367 (AppendLocalCommand signature), internal/ecosys/expand.go:14–33 (ParseInvocation regex), 20-RESEARCH.md §Wire Vocabulary + §Pattern 1/2/3, 20-PATTERNS.md (all excerpts)</read_first>
  <action>Start RED: write internal/runtime/commands_test.go TestTracerStatusClassB first — a runner-level test using the existing fake-provider fixture style (runner_battery_test.go) that drives Runner.Run with prompt text "/status", a fake provider that COUNTS Stream calls, and an in-memory frame sink; assert Stream count == 0, an emitted user_message_chunk frame, >=1 agent_message_chunk frame with a DIFFERENT messageId, stopReason end_turn, and a local_command transcript line whose Name is "status". Run it — it must fail to compile (nothing exists). Then implement:

1. internal/runtime/commands.go: type chainEntry {name, kind (builtin|skill|agent|file), key, desc, hint string; handler for builtins}; type commandChain holding an ordered winners map + the ecosys.Registry it was built from; buildChain(reg ecosys.Registry, reserved map[string]struct{}) that pre-seats the builtin table FIRST (D-01), then overlays reg.Skills, reg.Agents, reg.Commands in that order with first-writer-wins (D-02) — a discovered entry colliding with a RESERVED name is dropped and emits exactly one structured stderr warning naming the file and the reserved name (D-01 shadow-check, via r.stderrOrDefault()); commandChain.resolve(name) returning the winner. Runner gains an atomic.Pointer[commandChain] (or an equivalent single-guard accessor — research sanctions either; document the choice in code) plus a method to rebuild the chain from r.reg. Keep 15-D-20 layering: no ACP vocabulary in internal/session.
2. Builtin table in commands.go: the RESERVED set is exactly the thirteen names — help, status, cost, mcp, memory, permissions, doctor, config, model, clear, resume, compact (CMDS-02 class-B list) plus init (class-A, CMDS-03). In THIS task only /status gets a live handler: a real snapshot formatter reading the session's resolved model + tier, provider name, session id, turn count, context-usage estimate, and degraded-capability flags from the existing counters/advisory family — every line derived from live state, zero network. /init is registered as a class-A entry carrying a synthesized ecosys.Command (Name "init", Path "builtin:init") ONLY as a reservation in this task — its prompt body and expansion behavior are plan 20-04's deliverable, so in this task /init falls through to the existing expansion path (registry miss = plain text, runtime.go:395–438 semantics) exactly as today. The remaining eleven reserved names are NOT chain entries yet — they fall through as plain text (current behavior) and become live entries in their own plan-20-02 tasks; the RESERVED name set still blocks discovery from ever claiming them from this commit onward.
3. internal/acp/types.go: KindAvailableCommandsUpdate = "available_commands_update" (place beside KindConfigOptionUpdate :206, same //nolint:tagliatelle discipline and "field names pinned VERBATIM against schema/v1" header style); AvailableCommandFrame {Name string `json:"name"`, Description string `json:"description"`, Input *AvailableCommandInputFrame `json:"input,omitempty"`}; AvailableCommandInputFrame {Hint string `json:"hint"`} — v1 has exactly one input variant, unstructured, key "hint". Also the user-echo frame kind: updKindUserMessageChunk = "user_message_chunk" in emitter.go's kind family (:19–24) and EmitterHandle.UserMessageChunk(messageID, text string) error mirroring AgentMessageChunk :427–441 line-for-line (ContentChunk shape: content + shared messageId). The echo kind constant must be the chunk kind; the bare word that is a prefix of it is NOT a v1 SessionUpdate kind and must never appear as a sessionUpdate value (RESEARCH Pitfall 5).
4. internal/acp/server.go: NotifyAvailableCommands(sessionID string, cmds []AvailableCommandFrame) error — line-for-line mirror of NotifyConfigOptions :299–317 with payload key "availableCommands"; nil slice normalizes to empty slice (full replacement semantics); FOREGROUND lane; best-effort error return.
5. internal/runtime/runtime.go: the class-B intercept in Run — after routeAskReply returns not-handled and BEFORE the engine branch :539: ParseInvocation ONCE on the first text block (single-parse discipline, invocationFor pattern), resolve through the live chain; if the winner is a builtin with a live handler: emit echo via the in-hand emit handle (UserMessageChunk with its own messageId, e.g. derived from the turn id + ":echo"), run the handler to produce output text, stream it as AgentMessageChunk(s) with the turn's normal message id, write the durable record via sess.Manager.AppendLocalCommand(turnID, key, args, outcome, sourceChain) where sourceChain names "builtin", then return end_turn — zero provider calls, zero engine involvement. If the winner is class-A or non-builtin: fall through to the existing flow unchanged.
6. internal/acpserve/acp_serve.go: after session/new completes, build the advertisement list from the runner's live chain (sorted winners, D-04) and call srv.NotifyAvailableCommands — mirror the surface.SetNotify closure wiring at :219–225 including the log-and-continue error posture. Expose a re-fire seam: a SetCommandsNotify-style callback field on the Runner (or acpserve-side closure) that plan 20-05's rescan will invoke — in this task it is called exactly once at session start.
Real error handling on the single path: a handler panic is recovered and recorded as a failed local_command outcome with a loud stderr warning; the turn still ends end_turn (a control-plane command never fails the session).
Then GREEN: make TestTracerStatusClassB pass; add the golden frame assertions from Task 3's file as stubs-only if needed to compile.</action>
  <behavior>
    - Runner.Run("/status") → fake provider Stream call count == 0; response stopReason == end_turn
    - emitted frames contain exactly one user_message_chunk whose messageId != the agent_message_chunk messageId
    - transcript contains a local_command line with Name "status", Args as typed, SourceChain ["builtin"]
    - session/new flow emits available_commands_update listing the sorted winner set (at minimum "status"; plus any discovered entries in the fixture)
  </behavior>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestTracerStatusClassB' -x && go test -count=1 ./internal/acp/ -run 'TestAvailableCommandsFrame|TestUserMessageChunkEcho' -x</automated>
  </verify>
  <acceptance_criteria>
    - go test -race -count=1 ./internal/runtime/ -run TestTracerStatusClassB passes with the fake provider Stream counter asserted == 0
    - internal/acp/types.go contains const KindAvailableCommandsUpdate = "available_commands_update" and both new frame structs with json tags name/description/input/hint
    - internal/acp/emitter.go contains updKindUserMessageChunk and EmitterHandle.UserMessageChunk; no sessionUpdate value in the package emits the invalid bare kind (asserted by TestUserMessageChunkEcho golden)
    - internal/runtime/commands.go contains buildChain, commandChain.resolve, and the thirteen-name RESERVED set
    - the class-B path parses the invocation exactly once: the tracer test's instrumentation asserts ParseInvocation semantics hold with a single resolution call per Run (single-parse discipline proven behaviorally, not by grep)
    - the golden test asserts the serialized update map key is "availableCommands" and the echo kind value is "user_message_chunk"
  </acceptance_criteria>
  <done>A typed /status produces echo + output + end_turn with zero provider calls and a durable local_command line; session start advertises the winner set over the real emitter; the single path is race-clean and committed.</done>
  <reversibility rating="costly">D-01 makes the thirteen reserved names a published contract — flagged per CONTEXT; growth of the list needs the shadow-check warning this task ships.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chain semantics battery — D-01 reservation, D-02 collision order, D-04 winners-only view</name>
  <files>internal/runtime/commands.go, internal/runtime/commands_test.go</files>
  <read_first>internal/runtime/commands.go (from Task 1), internal/ecosys/loader.go:1178–1222 (AllAgents/AllSkills/AllCommands deterministic sorted accessors), internal/ecosys/types.go:60–83 (Registry + Agent structs), 20-RESEARCH.md §Pattern 1 + §Pitfall 7, 20-PATTERNS.md §Shared Patterns (deterministic ordering from the registry)</read_first>
  <behavior>
    - builtin beats skill/agent/file with the same name; the shadowed discovered entry is absent from the chain and one stderr warning names it (D-01)
    - skill beats agent beats file-command on the same name; loser stays reachable through its native surface (registry maps unchanged — chain is a view) (D-02)
    - resolve on an unknown name returns not-found and the caller falls through as plain text, never an error
    - advertisement list is sorted by name, contains each winning name exactly once, and omits every shadowed entry (D-04)
    - a builtin name outside the invocation grammar would be unresolvable — all thirteen reserved names match ^[a-z0-9][a-z0-9_:-]{0,63}$ (Pitfall 7, asserted by a table test)
  </behavior>
  <action>Write the failing table tests in commands_test.go FIRST (fixtures build ecosys.Registry values in code — skills/agents/commands maps with colliding names; no temp dirs needed), then harden buildChain/resolve to satisfy: deterministic first-writer-wins in chain order; reserved-name shadow-check warning emitted exactly once per shadowed entry per chain build (dedupe by file path — the logPluginSkipf pattern, loader.go:801–803); the advertisement builder returns a fresh sorted slice of AvailableCommandFrame values (description from the entry's desc, input.hint only when non-empty — omit the Input field entirely when the hint is empty); the chain never mutates the underlying ecosys.Registry (it is a VIEW — the loser's native surface keeps working; assert reg maps unchanged after build). No r.reg in-place mutation anywhere (Pitfall 1).</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestCommandChain' -x</automated>
  </verify>
  <acceptance_criteria>
    - go test -race -count=1 ./internal/runtime/ -run TestCommandChain passes with table cases covering: builtin-vs-skill, skill-vs-agent, agent-vs-file, three-way collision, unknown-name miss, empty-registry build
    - the shadow-check warning is asserted to fire exactly once per shadowed file per build (count captured via a stderr buffer on the Runner)
    - the advertisement builder test asserts sorted order, absence of shadowed names, and omission of the input object when hint is empty
    - grep -c "first-writer-wins\|firstWriterWins\|first writer wins" is NOT the gate — the behavior is proven by the collision table tests, not comments
  </acceptance_criteria>
  <done>Chain collision, reservation, and winners-only semantics are pinned by table tests; resolve is single-parse and side-effect-free on the registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire contract battery — available_commands_update + user_message_chunk golden JSON, full-replacement re-fire seam</name>
  <files>internal/acp/available_commands_test.go, internal/acp/server_test.go, internal/acp/emitter_test.go, internal/acpserve/acp_serve.go</files>
  <read_first>internal/acp/types.go + server.go + emitter.go (Task 1 additions), internal/acp/server_test.go + emitter_test.go (golden-JSON assertion style), internal/acpserve/acp_serve.go:219–245 (surface.SetNotify closure + StartScheduler ordering), 20-RESEARCH.md §Wire Vocabulary (AvailableCommand def, full-replacement quote, ContentChunk/messageId semantics), 20-PATTERNS.md (frame templates)</read_first>
  <behavior>
    - NotifyAvailableCommands serializes params {sessionId, update:{sessionUpdate:"available_commands_update", availableCommands:[...]}} — golden JSON compared byte-for-byte against a schema-derived fixture
    - nil/empty cmds serializes as an empty availableCommands array (full replacement — the client holds no merge state)
    - UserMessageChunk emits a ContentChunk with content + messageId; two successive UserMessageChunk calls with different messageIds serialize two distinct messages
    - the rescan-callback seam (SetCommandsNotify-style) fires the FULL winner set every invocation — asserted by calling it twice and asserting both frames carry the complete list
  </behavior>
  <action>Write golden-JSON tests FIRST in available_commands_test.go pinning the exact wire spellings from the canonical v1 schema (RESEARCH §Wire Vocabulary): the update kind string, the availableCommands array key, name/description always present, input omitted-when-empty. Add the full-replacement test (send twice, assert no client-side merge is assumed — both frames complete). Then wire the re-fire seam in acpserve: the callback registered in Task 1 becomes a named field/closure the rescan plan (20-05) will call after every chain swap — verify the wiring ordering mirrors SetEmitter/StartScheduler (acp_serve.go:239–240) so the emitter exists before any advertisement can fire. Keep errors best-effort with the log-and-continue posture (:231–235 pattern) — a failed advertisement never kills the session.</action>
  <verify>
    <automated>go test -count=1 ./internal/acp/ -run 'TestAvailableCommands|TestUserMessageChunk' -x && go test -race -count=1 ./internal/acpserve/ -run 'TestCommandsNotifySeam' -x</automated>
  </verify>
  <acceptance_criteria>
    - golden JSON in available_commands_test.go matches the schema-derived fixture exactly (kind string, array key, field spellings)
    - the empty-set frame serializes availableCommands as [] (not null)
    - TestCommandsNotifySeam proves two consecutive fires each carry the complete winner list
    - all new-frame tests fail if the echo kind constant is changed to the invalid bare kind (mutation check: flip the constant in a scratch run and the golden test must go red — assert this once manually, do not commit the flip)
  </acceptance_criteria>
  <done>The two new wire frames are pinned against the canonical schema; the re-fire seam exists and is proven full-replacement; autocomplete wire truth cannot drift silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo/user filesystem → resolver chain | Discovered command/skill/agent files are UNTRUSTED content entering the chain (names, descriptions, hints reach the wire; bodies reach prompts in later plans) |
| client (editor) → Runner.Run | Slash text typed in the editor crosses into the class-B intercept as ordinary prompt content |
| chain → wire | The advertisement builder's output is rendered in the editor UI (autocomplete) — a lying list misleads the operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering/Elevation | buildChain (discovered files) | medium | accept | Discovered-file NAMES/DESC enter the chain but bodies never execute (expansion is pure text substitution, ecosys/expand.go:38); Tools/AllowedTools frontmatter stays advisory per the existing D-09 posture; the bespoke prohibition (discovered files never gain execution/permission authority) is hosted in plan 20-04's must_haves |
| T-20-02 | Denial of Service | class-B intercept (turn mutex held) | medium | mitigate | /status handler is pure-local (no network); the FAST-CONTROL budget discipline for network-bound handlers is enforced in plan 20-02 (Pitfall 8); handler panic recovered → failed outcome, never a wedged session |
| T-20-03 | Information Disclosure | /status output builder | medium | mitigate | Snapshot builder reads model/tier/provider NAME/id/flags only — never credential values; keys resolve only via ResolveCredential and are never interpolated into output (RESEARCH V8; threat register row repeated in 20-02 where the output family lives) |
| T-20-04 | Repudiation | class-B record | low | mitigate | Every class-B invocation writes the full 16-D-22 local_command record via the REDACTED append path (manager.go:362) — transcript-as-truth survives resume |
| T-20-SC | Tampering | go.mod dependency adds | high | mitigate | Only new package this phase is fsnotify v1.10.1 (plan 20-05) — RESEARCH §Package Legitimacy Audit verdict OK/Approved; no install occurs in this plan |

Security posture: ASVS L1; block_on=high — no high-severity unresolved threat in this plan.
</threat_model>

<verification>
- `go test -race -count=1 ./internal/runtime/ ./internal/acp/ ./internal/acpserve/` green
- The tracer test proves the full single path: slash text → chain → handler → echo/chunks/end_turn → local_command line → advertisement
- Golden JSON pins both new frames against the canonical v1 schema spellings
- Reserved-name shadowing is proven blocked by table tests
</verification>

<success_criteria>
1. /status executes with zero provider calls and the D-05 wire shape, recorded as a local_command line (CMDS-02 partial, D-05).
2. The chain resolves collisions builtins → skills → agents → file with D-01 reservation + shadow warning and D-04 winners-only advertisement (CMDS-01, D-01, D-02, D-04).
3. available_commands_update is emitted at session start with the full sorted winner set and its golden shape is schema-pinned (ACP-04).
4. The re-fire seam exists for plan 20-05's rescan (full-replacement semantics proven).
</success_criteria>

## Artifacts this phase produces

- internal/runtime/commands.go (NEW): commandChain, chainEntry, buildChain, commandChain.resolve, builtin command table + RESERVED thirteen-name set, chain advertisement builder, Runner chain accessor (atomic pointer)
- internal/runtime/runtime.go (MOD): class-B intercept in Runner.Run (after routeAskReply, before engine branch), SetCommandsNotify-style re-fire seam field
- internal/acp/types.go (MOD): KindAvailableCommandsUpdate = "available_commands_update", AvailableCommandFrame, AvailableCommandInputFrame
- internal/acp/emitter.go (MOD): updKindUserMessageChunk = "user_message_chunk", EmitterHandle.UserMessageChunk(messageID, text string) error
- internal/acp/server.go (MOD): Server.NotifyAvailableCommands(sessionID string, cmds []AvailableCommandFrame) error
- internal/acpserve/acp_serve.go (MOD): session-start advertisement wiring, rescan-callback seam
- Wire frames: available_commands_update (session/update kind), user_message_chunk echo
- Tests: internal/runtime/commands_test.go, internal/acp/available_commands_test.go (both NEW)
- No CLI flags, no env vars, no user setup

<output>
Create `.planning/phases/20-built-in-commands-skills-per-agent-model/20-01-SUMMARY.md` when done
</output>
