---
phase: 20-built-in-commands-skills-per-agent-model
plan: 05
type: execute
wave: 4
depends_on: ["20-01", "20-03", "20-04"]
files_modified:
  - go.mod
  - go.sum
  - internal/runtime/rescan.go
  - internal/runtime/runtime.go
  - internal/runtime/commands.go
  - internal/acpserve/acp_serve.go
  - internal/runtime/rescan_test.go
autonomous: true
requirements: [CMDS-04, ACP-04]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Creating, modifying, or removing a command/skill/agent file under a discovery root updates the resolver chain and re-fires available_commands_update WITHOUT restarting the session (CMDS-04, criterion 2)
    - The editor autocomplete reflects the change immediately — the re-fired frame is the FULL winner set (full-replacement semantics, D-04)
    - Every slash resolution passes an invoke-time freshness re-check — a change the watcher missed (watch degrade, editor quirk, race) is still picked up at the next invocation (D-10 backstop)
    - Watcher failure (unsupported filesystem, resource limits) degrades ONCE with a loud warning and falls back to invoke-time-only rescan; sessions never die over discovery (D-12)
    - A malformed discovered file is skipped with ONE structured warning naming file + reason; the rest of the registry loads (D-11)
    - Concurrent rescan × running turns is -race clean; an agent file added mid-session is dispatchable from that session (live-chain lookup from 20-03)
  artifacts:
    - go.mod/go.sum — github.com/fsnotify/fsnotify v1.10.1 (RESEARCH §Package Legitimacy Audit: Approved)
    - internal/runtime/rescan.go (NEW) — watch set construction, fsnotify loop, debounce, Discover re-run, chain rebuild + atomic swap, rescan-complete callback, D-12 degrade
    - internal/runtime/runtime.go — LoadCommandRegistry reworked to build+install the chain; rescan lifecycle on the serve context
    - internal/runtime/commands.go — invoke-time freshness probe on the resolution path
    - internal/acpserve/acp_serve.go — watcher start/teardown on serve ctx; rescan-complete → NotifyAvailableCommands re-fire
    - internal/runtime/rescan_test.go (NEW) — temp-dir watch fixtures + concurrent-swap race test
  key_links:
    - rescan loop → ecosys.Discover (loader.go:1275–1296) — the one-call rescan primitive, re-run verbatim
    - chain swap → the 20-01 atomic-pointer accessor + the 20-01 re-fire seam — one swap, one full advertisement
    - freshness probe → resolution path (commands.go) — bounded stat-check of discovery roots, never a full Discover per keystroke
    - watcher lifecycle → serve ctx (acp_serve.go:245–249 ctx-done goroutine shape)
  assumptions:
    - "PROBE-UNRESOLVED CMDS-01 (concurrency, shared with 20-01): the rescan swap is the concurrency-critical leg — immutable chain built off-thread + atomic swap + -race proof here; the probe row remains flagged unresolved (never auto-resolved)."
    - "PROBE-UNRESOLVED CMDS-04 (unclassified): probe returned 'unclassified — review manually'; row stays flagged. Goal-backward coverage: create/remove/re-fire + watch-miss backstop + degrade tests assert the observable behavior."
    - "PLATFORM FLAG (RESEARCH Pitfall 4): fsnotify has NO polling watcher — NFS/SMB mounts get no events; D-12's locked fallback is invoke-time-only, never a polling ticker."
---

<objective>
Make discovery live: fsnotify directory watches (debounced) on the discovery roots driving Discover re-runs, atomic chain swaps, and available_commands_update re-fires — with the invoke-time freshness backstop guaranteeing resolution never runs against a stale chain, and the D-11/D-12 graceful-degradation posture — per CMDS-04 and decisions D-10, D-11, D-12 (ACP-04's "on discovery change" leg).

Purpose: criterion 2 — newly created/installed (or removed) commands, skills, and agents are picked up without restarting the session. This is the plan that closes ACP-04's second emission point.
Output: internal/runtime/rescan.go with the watch/swap machinery, the freshness probe, acpserve wiring, and the race-clean test battery.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-CONTEXT.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-RESEARCH.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-PATTERNS.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-01-SUMMARY.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-03-SUMMARY.md

20-01 delivered the chain + advertisement + re-fire seam; 20-03 rerouted agent lookup through the
live chain (the stale-alias fix rescan depends on); 20-04 added the skill/agent/init entries the
rescan must rebuild. fsnotify is ABSENT from go.mod today (verified) — this plan adds it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: fsnotify watcher — watch set, debounce, Discover re-run, atomic swap, D-11/D-12 posture</name>
  <files>go.mod, go.sum, internal/runtime/rescan.go, internal/runtime/runtime.go, internal/runtime/rescan_test.go</files>
  <precondition>Module proxy reachability for `go get github.com/fsnotify/fsnotify@v1.10.1`; if the proxy is unreachable, halt and report — D-12's fallback covers runtime watch failure, not build-time dependency absence.</precondition>
  <read_first>20-RESEARCH.md §Code Examples (fsnotify watch + debounce skeleton — copy verbatim as the base), §Pitfalls 2/3/4 (watch roots must exist; non-recursive; parent+leaf watch set; editor temp+rename bursts; Chmod ignore; NO polling fallback), 20-CONTEXT.md D-10/D-11/D-12, internal/runtime/runtime.go:357–383 (LoadCommandRegistry — the startup one-shot being reworked into build+install), internal/ecosys/loader.go:1275–1296 (Discover — the re-run target), loader.go:801–803 (logPluginSkipf — the D-11 one-structured-warning seam; verify skills/commands discovery paths have equivalent skip warnings or add them), 20-PATTERNS.md §No Analog Found (watcher + swap discipline), 20-01-SUMMARY.md (chain build/swap accessor + re-fire seam)</read_first>
  <action>Run `go get github.com/fsnotify/fsnotify@v1.10.1` (pinned — RESEARCH audit Approved; no other new deps). Create internal/runtime/rescan.go implementing the RESEARCH skeleton: (1) watch set = the stable PARENTS (.claude/ at project + user roots, .ass-guard/ where present, plugin cache roots) plus each EXISTING discovery leaf (skills/, commands/, agents/); literal paths only — no symlink resolution on the watch set (T-20-19); on a Create event landing ON a watched-set directory, Add it dynamically (Pitfall 2's first-ever-install gap, bounded by the invoke-time backstop for anything the watch set still misses). (2) Debounce 300ms (discretion midpoint of the locked 100–500ms window — document the constant), coalescing pending events into ONE rescan; ignore Chmod. (3) Rescan body: ecosys.Discover(workDir) off the turn goroutines → buildChain over the fresh registry (20-01 builder) → atomic swap through the 20-01 accessor → invoke the rescan-complete callback (the 20-01 seam → NotifyAvailableCommands full set). NEVER mutate r.reg maps in place; the fresh Registry replaces the pointer wholesale (Pitfall 1). (4) D-11: verify every discovery path (skills, commands, agents — agents have logPluginSkipf already) skips malformed files with exactly one structured warning naming file + reason; add the warning where missing, in the loader's existing style. (5) D-12: watcher construction failure or per-root Add failure or a terminal watch error → ONE loud degrade warning (dedupe per root) and the invoke-time-only fallback; the rescan goroutine exits cleanly on serve ctx done (acp_serve.go:245–249 shape). Rework LoadCommandRegistry to route through the same build+install path so startup and rescan share one code path. Start/stop the watcher from acpserve on the serve ctx.</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestRescan' -x && go vet ./internal/runtime/</automated>
  </verify>
  <acceptance_criteria>
    - TestRescanWatcher (temp-dir fixture): create a commands/*.md file under the watched root → within debounce + epsilon the chain resolves it and the rescan callback fired; remove the file → it stops resolving; assertions poll with a deadline, never sleep-fixed
    - debounce test: five rapid writes produce exactly ONE rescan callback invocation (Pitfall 3)
    - D-12 test: a watch root that cannot be added (delete the dir between construction and Add, or an unsupported-path fixture) yields exactly ONE loud warning and the loop continues on remaining roots
    - D-11 test: a malformed skill file (broken frontmatter) is skipped; the healthy sibling loads; exactly one warning names the bad file
    - `-race` clean with a concurrent turn running during the swap (full race matrix lands in Task 3)
  </acceptance_criteria>
  <done>Watched discovery changes rebuild and swap the chain off-thread and re-fire the advertisement; watcher loss degrades once to invoke-time-only; malformed files skip with one warning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invoke-time freshness backstop (D-10's second layer)</name>
  <files>internal/runtime/commands.go, internal/runtime/rescan_test.go</files>
  <read_first>20-CONTEXT.md D-10 (locked: watch gives immediacy, invoke-time guarantees resolution never stale), internal/runtime/commands.go (resolve path from 20-01 + skill body re-read from 20-04), 20-RESEARCH.md §Pitfall 2 (what the watch set cannot see), internal/ecosys/loader.go (Discover cost shape — why the backstop must be stat-cheap, not a full re-Discover per keystroke)</read_first>
  <behavior>
    - a file change the watcher NEVER saw (simulated: watcher disabled via the D-12 degrade path, then a file added) is picked up at the next slash invocation — resolution reflects the new chain
    - the freshness probe is bounded: a stat-level check of the discovery roots (existence + mtime of the root set), not a recursive walk, and not a full Discover — a benchmark/smoke assertion keeps per-invocation overhead trivial
    - when the probe detects drift: one synchronous re-scan + swap happens BEFORE the invocation resolves (the invocation must not run against the stale chain)
    - probe failure (stat errors) degrades to using the current chain with at most a throttled warning — never blocks resolution
  </behavior>
  <action>Write the failing backstop test FIRST (disable the watcher via the D-12 path, add a file, invoke /name, assert it resolves). Implement the probe in commands.go on the resolution path: maintain the chain-build timestamp + a lightweight signature of the discovery roots (stat each root in the watch set: exists + ModTime); on invocation, compare; drift → synchronous rescanAndSwap (the Task 1 body) before resolving. Bound the probe: only the root set from the watch set construction (a handful of stats), no recursion. Throttle probe-failure warnings (advisoryNoteDue-style per-session dedupe or a simple interval gate). Keep the single-parse discipline: probe → (maybe rescan) → ParseInvocation → resolve, still one parse of the text.</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestFreshnessBackstop' -x</automated>
  </verify>
  <acceptance_criteria>
    - watcher-disabled + file-added test: the NEXT invocation resolves the new file (the watch-miss scenario is covered without real filesystem-event flakiness)
    - probe-overhead assertion: the probe performs only O(watch-set) stat calls (count via a stat-injection seam or tmpfs timing bound — pick one, assert it)
    - probe-error test: unreadable root → resolution still succeeds on the current chain, warning throttled to one per interval
    - drift-then-resolve ordering: the invocation's resolution used the POST-scan chain (assert the resolved entry carries the new file's content/identity)
  </acceptance_criteria>
  <done>Resolution can never run against a stale chain: watch misses are caught by the bounded invoke-time probe and resolved synchronously before the invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: acpserve wiring + advertisement re-fire + concurrency proof</name>
  <files>internal/acpserve/acp_serve.go, internal/runtime/rescan_test.go, internal/runtime/runtime.go</files>
  <read_first>internal/acpserve/acp_serve.go:164–249 (Run composition: LoadCommandRegistry call site :189, SetEmitter/StartScheduler ordering :239–240, ctx-done goroutine :245–249, surface.SetNotify closure :219–225), 20-01-SUMMARY.md (re-fire seam contract), 20-03-SUMMARY.md (live-chain agent lookup), 20-RESEARCH.md §Pitfall 1 (the stale-alias hazard this wiring must prove dead), 20-CONTEXT.md D-10 (both layers firing order)</read_first>
  <behavior>
    - rescan completion fires exactly ONE available_commands_update per debounced change, carrying the FULL new winner set (asserted twice for two different changes)
    - an agent file created mid-session is dispatchable from the PRE-EXISTING session immediately after the swap (the 20-03 live-lookup integration proven at the wiring level)
    - concurrent rescan × active turns × concurrent resolutions is -race clean over a sustained loop (the Pitfall 1 proof)
    - serve shutdown (ctx done) stops the watcher and the rescan goroutine without leaking (goroutine count returns to baseline after shutdown)
  </behavior>
  <action>Wire the lifecycle in acpserve Run: after LoadCommandRegistry, start the watcher with the serve ctx; bind the rescan-complete callback to srv.NotifyAvailableCommands via the 20-01 seam (log-and-continue posture, :231–235 style). Order the startup so the emitter exists before the first advertisement (SetEmitter before watcher start). Then the concurrency proof in rescan_test.go: a stress test running N turns (fake providers, mixed slash + normal prompts) while a goroutine creates/deletes discovery files in a loop through the debounce path — assert -race clean, no lost advertisement (final state's winner set == last fired frame), and the mid-session agent dispatch succeeds. Shutdown test: cancel the serve ctx, assert the watcher goroutine exits (poll goroutine count or a done channel exposed for the test).</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ ./internal/acpserve/ -run 'TestRescanConcurrency|TestRescanRefire|TestRescanShutdown' -x</automated>
  </verify>
  <acceptance_criteria>
    - TestRescanRefire: two successive changes produce two frames, each the complete winner set for its moment (full replacement, D-04)
    - TestRescanConcurrency: -race clean under sustained concurrent rescan × turns × resolutions; the pre-existing session dispatches the mid-session-added agent
    - TestRescanShutdown: ctx cancellation stops the watch loop (done channel or goroutine-count assertion) with no panic
    - mise-ci-compatible: go vet + golangci-lint clean on the new files (stderr-only logging discipline — no stdout writes in rescan.go)
  </acceptance_criteria>
  <done>Discovery is live end-to-end: change → debounced rescan → atomic swap → full advertisement re-fire, race-proven, with clean serve-lifecycle teardown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem events → resolver chain | Anything writable to the discovery roots (repo collaborators, installers, malware in the repo) reshapes the command surface live — mid-session, without restart |
| watch roots → fsnotify | Symlinked or pathological paths could point watches outside the intended roots |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-18 | Denial of Service | watch event storm (editor temp+rename bursts, runaway writers) | high | mitigate | 300ms debounce coalescing all pending events into one rescan; Chmod ignored; rescan runs off the turn goroutines; D-12 degrade caps the damage if the watcher itself fails; battery asserts one-rescan-per-burst |
| T-20-19 | Tampering | symlinked discovery dirs escaping watch roots | medium | mitigate | Watch LITERAL paths only — no EvalSymlinks resolution on the watch set; discovery content is still bounded by the loader's read-caps; the invoke-time probe stats literal roots |
| T-20-20 | Denial of Service | rescan wedged by a huge/hanging filesystem | low | accept | Discover is the same call startup already makes (accepted startup-risk posture); the swap is off-thread so a slow scan delays freshness, never the session; the backstop makes the delay visible on next invocation |
| T-20-21 | Tampering | live reshaping of the slash surface | medium | accept | This is the FEATURE (criterion 2) — the content-level trust posture is unchanged from startup discovery (T-20-01 in 20-01: bodies never execute, frontmatter advisory, P-20-01); the operator sees changes via autocomplete within the debounce window |
| T-20-SC | Tampering | go.mod dependency add (fsnotify v1.10.1) | high | mitigate | RESEARCH §Package Legitimacy Audit: verdict OK, Approved, v1.10.1 pinned, provenance via Go module proxy + official repo; no [ASSUMED]/[SUS] packages remain — no human checkpoint required by the gate |

Security posture: ASVS L1; block_on=high — T-20-18 mitigated by enforced debounce assertions; T-20-SC closed by the audit table; no unmitigated high remains.
</threat_model>

<verification>
- TestRescan* battery green under -race (watcher, debounce, degrade, D-11 skip, backstop, re-fire, concurrency, shutdown)
- The stale-alias hazard (Pitfall 1) is proven dead at the wiring level: mid-session agent dispatchable
- `go test -race -count=1 ./...` and `mise ci` green at wave merge
- No stdout writes in the new code (stderr discipline)
</verification>

<success_criteria>
1. Created/removed command, skill, or agent files are discovered without restart; autocomplete re-fires the full winner set (CMDS-04, criterion 2, D-04, ACP-04's discovery-change leg).
2. The invoke-time backstop catches watch misses before any resolution (D-10's both layers).
3. Watcher failure degrades once, loudly, to invoke-time-only; malformed files skip with one warning; sessions never die over discovery (D-11, D-12).
4. Concurrent rescan × turns is race-clean with live-chain dispatch (Pitfall 1 closed).
</success_criteria>

## Artifacts this phase produces

- go.mod/go.sum (MOD): github.com/fsnotify/fsnotify v1.10.1
- internal/runtime/rescan.go (NEW): watcher construction, watch-set builder (parents + existing leaves), debounced event loop, rescanAndSwap (Discover → buildChain → atomic swap → callback), D-12 degrade path, ctx teardown
- internal/runtime/runtime.go (MOD): LoadCommandRegistry routed through the shared build+install path; watcher start/stop hooks
- internal/runtime/commands.go (MOD): invoke-time freshness probe on the resolution path (stat-level root signature)
- internal/acpserve/acp_serve.go (MOD): watcher lifecycle on serve ctx; rescan-complete → NotifyAvailableCommands re-fire
- internal/runtime/rescan_test.go (NEW): TestRescanWatcher, TestRescanDebounce, TestRescanDegrade, TestFreshnessBackstop, TestRescanRefire, TestRescanConcurrency, TestRescanShutdown
- No new wire frames, no CLI flags, no env vars, no user setup

<output>
Create `.planning/phases/20-built-in-commands-skills-per-agent-model/20-05-SUMMARY.md` when done
</output>
