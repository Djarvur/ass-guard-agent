---
phase: 20-built-in-commands-skills-per-agent-model
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-03"]
files_modified:
  - internal/runtime/commands.go
  - internal/runtime/runtime.go
  - internal/runtime/commands_test.go
autonomous: true
requirements: [SKLS-01, SKLS-02, CMDS-03]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Typing /<skill-name> expands the SKILL.md body as the prompt with args appended per the locked Expand semantics ($ARGUMENTS, $1..$9, append-under-heading) — riding the existing expansion path with provenance (SKLS-01)
    - Typing /<agent-name> DISPATCHES the subagent: the invocation body becomes the subagent's prompt, the discovered agent's Prompt/Tools apply through the existing PARA machinery, and the result streams into the current turn — it is NOT a prompt expansion (D-03, SKLS-02)
    - BMad-style .claude/agents/*.md layout works as the discovery source for slash-addressable agents (SKLS-02 — the loader already walks this layout; the chain exposes it)
    - /init expands as a prompt-expanding command recorded as a user turn with provenance, riding expandUserBlocks UNCHANGED in behavior — engine-on parity holds because the engine adapter consumes the same invocationFor resolution (CMDS-03)
    - A skill with user-invocable: false is absent from the chain and cannot fire via slash (D-04 winners-only honesty; CC parity)
    - A discovered skill/agent/command file gains NO execution or permission authority from its frontmatter — its body enters as prompt text only and its Tools list stays advisory
  artifacts:
    - internal/runtime/commands.go — skill slash-entry synthesis (ResolveSkill body → ecosys.Command → Expand), agent dispatch entry (kind:agent → DispatchSubagent), user-invocable filtering in the chain build
    - internal/runtime/commands.go — /init class-A builtin: synthesized ecosys.Command{Name "init", Body <init prompt>, Path "builtin:init"} riding expandUserBlocks verbatim
    - internal/runtime/commands_test.go — TestSkillSlash, TestAgentSlash, TestInitExpansion batteries
  key_links:
    - Skill expansion reuses ecosys.ResolveSkill (skills.go:111–125) + Command.Expand (expand.go:51–87) — synthesizing a Command value, never re-implementing substitution (Don't-Hand-Roll)
    - Agent slash-dispatch rides Session.DispatchSubagent (subagent.go:43) whose forwarder-subscribed bus events stream the subagent's chunks into the CURRENT turn (PARA-02), then end_turn — zero parent-model turn
    - /init enters as a chain entry whose value is a class-A Command — the expansion seam (runtime.go:395–438) and the engine adapter's cfg.Invoke path (enginebridge.go:142–155) both resolve it through the same chain
  assumptions:
    - "PROBE-UNRESOLVED SKLS-01 (empty, probe: 'What is the result for empty, single-element, or null input?'): flagged. Authored behavior: no args → body expands with $ARGUMENTS→empty and NO appended arguments block (Expand append rule requires non-empty args); empty SKILL.md body → the expansion yields an empty/whitespace prompt which is REJECTED with a loud local-command-style error output naming the skill, not an empty model call. Single-element args → $1 substitution case in the battery."
    - "PROBE-UNRESOLVED SKLS-01 (encoding): flagged. Authored behavior: skill bodies are read as UTF-8 text verbatim; multi-byte content survives Expand byte-identically (battery asserts a multi-byte fixture round-trips)."
    - "PROBE-UNRESOLVED SKLS-02 (unclassified): probe returned 'unclassified — review manually'; row stays flagged. Goal-backward coverage: TestAgentSlash proves the BMad-layout agent dispatches with Prompt/Tools applied and the result streaming."
    - "PROBE-UNRESOLVED CMDS-03 (unclassified): probe returned 'unclassified — review manually'; row stays flagged. Goal-backward coverage: TestInitExpansion proves expansion + provenance + engine-on parity."
  prohibitions:
    - "P-20-01: A discovered command, skill, or agent file must NEVER gain execution or permission authority — its frontmatter Tools/AllowedTools list stays advisory, its body enters the conversation as prompt text only, and no discovered file may grant a tool permission (repo-shipped files never grant allow — safety-model amendment)."
---

<objective>
Make skills and discovered agents addressable as slash commands and land /init as the class-A builtin: `/<skill-name>` expands the SKILL.md body with args (SKLS-01), `/<agent-name>` dispatches the subagent with Prompt/Tools applied and the result streaming into the turn (D-03, SKLS-02), and `/init` expands through the untouched expandUserBlocks seam as a user turn with provenance (CMDS-03).

Purpose: criteria 4 and 5 of the phase — the two non-class-B invocation families — plus the CC-parity /init entry point; everything rides machinery that already works (Expand, DispatchSubagent, the expansion seam), so this plan is wiring plus pinning tests.
Output: skill/agent/init chain entries, the user-invocable filter, and the three batteries.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-CONTEXT.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-RESEARCH.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-PATTERNS.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-01-SUMMARY.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-03-SUMMARY.md

20-01 delivered the chain + intercept; 20-03 delivered dispatch-time model resolution (agent slash
dispatch consumes it). Known locked divergence: $0 expands to empty (1-based positionals, zcode
parity — Pitfall 9; do NOT "fix" toward CC's 0-based).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SKLS-01 — /<skill-name> expands SKILL.md body with args; user-invocable filter</name>
  <files>internal/runtime/commands.go, internal/runtime/commands_test.go</files>
  <read_first>internal/ecosys/skills.go:111–125 (ResolveSkill — re-reads the SKILL.md body by registry key), internal/ecosys/expand.go:35–87 (Expand: $ARGUMENTS, $1..$9 descending, $0→empty, append-under-heading when args non-empty and body has no placeholder), internal/ecosys/types.go (Skill struct — check which frontmatter fields parse today; user-invocable likely absent), 20-RESEARCH.md Open Question 4 + §State of the Art (commands-merged-into-skills), 20-PATTERNS.md (ResolveSkill + Expand excerpts), 20-01-SUMMARY.md (chain entry shape)</read_first>
  <behavior>
    - /review-pr arg1 arg2 with a SKILL.md body containing $ARGUMENTS and $1: expansion substitutes per the locked Expand semantics and enters the normal expansion path (provenance recorded with the skill's origin)
    - body WITHOUT placeholders + non-empty args: append-under-heading rule fires (Expand's own rule — assert the exact appended block)
    - no args: body expands with $ARGUMENTS removed/substituted empty and NO appended arguments block
    - empty body (frontmatter-only SKILL.md): loud error output naming the skill; no model call; local-command-style outcome recorded
    - multi-byte (UTF-8) body content survives expansion byte-identically
    - skill with user-invocable: false in frontmatter: absent from the chain, /name falls through as plain text, and the skill remains available to the MODEL invocation path (only the slash surface excludes it)
  </behavior>
  <action>Write the failing TestSkillSlash battery FIRST (registry fixtures with real temp-dir SKILL.md files — loader_test.go fixture style). Implement: at chain build, exclude skills whose frontmatter carries user-invocable explicitly false (extend the Skill parse if the field is absent — small additive frontmatter field following the existing tolerant parser patterns; default true when absent); a kind:skill chain entry resolves at INTERCEPT time: ResolveSkill(reg, name) → body; synthesize ecosys.Command{Name, Body: body} and call Expand(args) — the substitution semantics are locked zcode parity, reuse verbatim (Don't-Hand-Roll). The expanded text replaces the first text block and FALLS THROUGH to the existing expansion path (expandUserBlocks semantics — the invocation has been consumed; what continues is the ordinary prompt flow with provenance naming the skill source). Empty-body result: do NOT dispatch an empty prompt — emit the loud error output through the D-05 shape, record the outcome, end_turn. Ensure the rescan path (plan 20-05) re-reads bodies fresh (ResolveSkill already re-reads from disk by design).</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestSkillSlash' -x</automated>
  </verify>
  <acceptance_criteria>
    - battery cases: $ARGUMENTS+$1 substitution, append-under-heading, no-args (no appended block), empty-body loud error with zero provider calls, multi-byte round-trip, user-invocable-false exclusion
    - the exclusion test asserts the skill is ABSENT from the advertisement builder output too (D-04 winners-only)
    - provenance: the expansion path's source record names the skill (SourceChain includes the skill origin) — asserted on the transcript line
  </acceptance_criteria>
  <done>Skills fire via slash with locked Expand semantics, args edge cases pinned, user-invocable:false excluded from the slash surface only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SKLS-02 + D-03 — /<agent-name> dispatches the subagent through the PARA machinery</name>
  <files>internal/runtime/commands.go, internal/runtime/runtime.go, internal/runtime/commands_test.go</files>
  <read_first>internal/session/subagent.go:43–123 (DispatchSubagent — signature, agentDef application, restricted-set derivation, transcript dispatch line), 20-CONTEXT.md D-03, 20-RESEARCH.md §Pattern 2 (agent dispatch rides the intercept; subagent chunks are bus events reaching the client inside this turn's forwarder), 20-03-SUMMARY.md (dispatch-time model resolution — the agent's frontmatter model applies here too), internal/runtime/runtime.go Run (the 20-01 intercept — extend the winner handling), internal/ecosys/loader.go:185–224 (discoverAgents — .claude/agents walk, BMad layout first-class)</read_first>
  <behavior>
    - /agent-x do the thing: DispatchSubagent is invoked with prompt "do the thing", agentDef = the discovered agent; the agent's Prompt appears in the subagent's system context and its Tools list restricts the subagent
    - the subagent's chunks stream into the CURRENT turn (client sees them) and the turn ends end_turn after the subagent completes
    - zero parent-model turns: the fake parent provider records no Stream call for the parent loop (the subagent's own provider calls are separate and expected)
    - the agent's frontmatter model (20-03 resolution) applies to this dispatch; ResolvedModel lands on the dispatch line (D-16 both-ways from 20-03)
    - unknown /name that matches no chain entry: falls through as plain text (registry miss = plain text, never an error)
    - an agent sharing a name with a skill loses (chain order) but remains dispatchable via the Agent tool (D-02 native-surface guarantee — assert both surfaces)
  </behavior>
  <action>Extend the 20-01 intercept's winner handling: kind:agent → resolve the LIVE agent def through the chain accessor (rescan-safe lookup from 20-03), then call the session's DispatchSubagent with the invocation args as the subagent prompt and the agentDef; the call site already applies Prompt/Tools through subagentProfile and writes the subagent_dispatch line (now with ResolvedModel via 20-03). The subagent's bus events flow through the forwarder subscriptions the intercept runs inside (20-01 placement is what makes this work — do not move the intercept). Await the subagent turn's completion within the class-B turn, then return end_turn. Model resolution: pass the agent's frontmatter slug through the 20-03 dispatch-time resolver exactly as the Agent-tool path does — one resolution path for both invocation surfaces. Failure of the subagent (provider error) surfaces as the turn's error path per existing DispatchSubagent behavior — do not add a second error channel. This task adds NO dispatch logic of its own — it is routing plus pinning tests.</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestAgentSlash' -x</automated>
  </verify>
  <acceptance_criteria>
    - TestAgentSlash asserts: DispatchSubagent called once with the exact args-as-prompt; subagent system context contains the agent's Prompt; restricted toolset matches the agent's Tools; end_turn after completion; parent fake provider Stream count == 0
    - resolvedModel assertion: the subagent_dispatch line carries the D-16 field for an agent with frontmatter model (integration with 20-03)
    - collision test: skill named like the agent wins the slash; the agent still dispatches via the Agent tool path (both surfaces proven in one test)
    - unknown-name test asserts plain-text fallthrough (provider receives the raw "/name" text)
  </acceptance_criteria>
  <done>Discovered agents are slash-addressable subagent dispatches with Prompt/Tools/model applied and results streaming — D-03 honored, SKLS-02 proven over the BMad layout the loader already walks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CMDS-03 — /init class-A builtin rides expandUserBlocks unchanged, engine-on parity</name>
  <files>internal/runtime/commands.go, internal/runtime/runtime.go, internal/runtime/commands_test.go</files>
  <read_first>internal/runtime/runtime.go:395–438 (expandUserBlocks — the seam /init rides VERBATIM; provenance + mutating-boundary discipline), runtime.go:458–476 (invocationFor — the engine adapter consumes it via cfg.Invoke, :808–817), internal/runtime/enginebridge/enginebridge.go:142–155 (engine-path expansion), 20-CONTEXT.md (CMDS-03 + code anchors), 20-RESEARCH.md §Code Examples (initCmd synthesis shape), internal/ecosys/expand.go (Expand semantics for the init body's $ARGUMENTS capability), e2e_opsx_test.go (expansion-seam test style)</read_first>
  <behavior>
    - /init with no args: the first text block is replaced by the init prompt body (codebase-analysis instructions producing a CLAUDE.md guide); the turn proceeds as a NORMAL user turn (model called exactly once for the parent loop)
    - the invocation is recorded as a user turn with provenance naming builtin:init (SourceChain)
    - /init with args: $ARGUMENTS in the body substitutes (Expand semantics)
    - engine-on parity: with the engine adapter active, the SAME expansion happens through cfg.Invoke/invocationFor — behavior identical engine-on vs engine-off (the battery runs both)
    - /init is a chain winner: the advertisement lists it; a discovered commands/init.md is shadowed with the D-01 warning (already enforced by 20-01's reserved set — assert it here end-to-end)
  </behavior>
  <action>Author the init prompt body (the one piece of NEW content in this phase): a codebase-analysis prompt instructing the model to inspect the repository and create/maintain a CLAUDE.md project guide — sections for build/test/lint commands, architecture overview, conventions; $ARGUMENTS-capable for focus hints; keep it CC-parity in intent (Description "Analyze the codebase and create a CLAUDE.md guide"). Register it at chain build as the class-A builtin entry (Name "init", Path "builtin:init") — from 20-01's reservation it already blocks shadowing; this task makes it a LIVE entry with the body so the advertisement includes it and resolution returns the Command. The intercept's class-A handling: resolve → the entry IS an ecosys.Command → fall through to expandUserBlocks UNCHANGED (the seam does the replacement + provenance — 20-01's intercept already passes class-A winners through; verify no double-expansion: the intercept consumes the invocation, the seam expands the Command). Engine-on: the engine adapter resolves through the same invocationFor→chain path — one test runs the turn with the engine enabled and asserts identical expansion + provenance. The turn is a normal user turn: recorded with provenance, model called once.</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestInitExpansion' -x && go test -race -count=1 ./internal/runtime/ -run 'TestClassB' -x</automated>
  </verify>
  <acceptance_criteria>
    - TestInitExpansion cases: no-args expansion (body replaces the text block, model called once), args substitution, provenance record naming builtin:init, engine-on parity (identical expansion both modes)
    - the advertisement includes "init"; a fixture commands/init.md is shadowed with the D-01 warning and never fires via slash
    - expandUserBlocks itself is UNMODIFIED (git diff on the function body shows no change — the seam rides untouched per CMDS-03's letter)
    - full TestClassB battery still green (the /init entry did not disturb class-B resolution)
  </acceptance_criteria>
  <done>/init expands as a user turn with provenance through the untouched seam, engine-on parity proven, and the reserved name is now a live advertised winner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo filesystem (SKILL.md / agents/*.md) → prompt + dispatch | Untrusted repo content becomes prompt text and, for agents, shapes a subagent's system prompt and toolset |
| agent frontmatter → subagent toolset | `tools:` restricts (advisory posture) — a malicious file declaring broad tools must not GAIN authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-14 | Elevation | agent/skill frontmatter (tools, allowed-tools) | high | mitigate | P-20-01 (must_haves.prohibitions): frontmatter tool lists stay ADVISORY — the dispatch's restricted set derives from the existing PARA restricted-default discipline and the executor's permission gate (Phase 17) still applies; no discovered file grants allow; battery asserts the restricted set is applied but the gate posture is unchanged |
| T-20-15 | Tampering | SKILL.md body (prompt injection) | medium | accept | Accepted CC-parity surface: bodies enter as prompt text; Expand is pure substitution (expand.go:38) and never executes; the model sees what a repo author wrote — same trust as any repo file the model reads |
| T-20-16 | Denial of Service | oversized SKILL.md / agent files | medium | mitigate | Read through the loader's existing read-cap precedent (pluginArtifactMaxBytes family); expansion of a capped body is bounded; battery includes a large-fixture case proving no unbounded read on the slash path |
| T-20-17 | Repudiation | skill/init provenance | low | mitigate | SourceChain records the skill/builtin origin on the transcript (asserted in both batteries) — post-hoc inspection shows which file produced a prompt |

Security posture: ASVS L1; block_on=high — T-20-14 is high and mitigated by the advisory-posture invariant + existing gate pipeline; the prohibition is authored descriptor-less and stays flagged-unverified downstream.
</threat_model>

<verification>
- TestSkillSlash + TestAgentSlash + TestInitExpansion green under -race
- Prohibition P-20-01 enforced: frontmatter authority stays advisory (restricted-set applied, no allow-granting path added)
- CMDS-03's seam-unchanged letter proven (expandUserBlocks diff-free)
- Known divergences preserved: $0→empty (zcode parity), skills-over-commands chain order (matches CC's merged-skills state of the art)
</verification>

<success_criteria>
1. /<skill-name> expands the SKILL.md body with args per locked Expand semantics with provenance (SKLS-01, criterion 5's skill leg).
2. /<agent-name> dispatches the subagent — prompt from args, Prompt/Tools/model applied, result streaming, no parent-model turn (D-03, SKLS-02, criterion 5's agent leg).
3. /init expands as a user turn with provenance through the unchanged seam, engine-on parity (CMDS-03, criterion 4).
4. user-invocable:false skills are excluded from the slash surface and the advertisement (D-04 honesty).
</success_criteria>

## Artifacts this phase produces

- internal/runtime/commands.go (MOD): skill slash-entry synthesis (ResolveSkill → Command → Expand), agent dispatch entry (kind:agent → DispatchSubagent), user-invocable frontmatter filter in the chain build, /init class-A builtin entry with its authored prompt body (Path "builtin:init")
- internal/ecosys (MOD, minimal): Skill frontmatter gains the user-invocable field (tolerant parse, default true) if absent today
- internal/runtime/commands_test.go (MOD): TestSkillSlash, TestAgentSlash, TestInitExpansion batteries
- No new wire frames, no CLI flags, no env vars, no user setup

<output>
Create `.planning/phases/20-built-in-commands-skills-per-agent-model/20-04-SUMMARY.md` when done
</output>
