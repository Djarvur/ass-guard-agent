---
phase: 20-built-in-commands-skills-per-agent-model
plan: 06
type: execute
wave: 5
depends_on: ["20-02", "20-05"]
files_modified:
  - internal/acpserve/simulator_e2e_test.go
autonomous: false
requirements: [ACP-04, CMDS-01, CMDS-02, CMDS-03, CMDS-04, SKLS-01, SKLS-02, SKLS-03]
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Over the wire (Zed-client simulator, 16-06 pattern): session start advertises the full winner set; /status round-trips echo + chunks + end_turn with zero model calls; a file created under a watched root re-fires the advertisement; /<agent> dispatch streams and reports resolvedModel
    - The full gate (mise ci: vet + lint + CGO_ENABLED=0 build + go test -race ./...) is green at phase close
    - Every phase success criterion (1–6) maps to an executed automated check or the operator checkpoint below — none verified by assertion alone
    - The operator confirms live-Zed behavior: autocomplete shows the full set, /status answers instantly with no model turn, a newly installed skill is picked up mid-session, and the resolvedModel note is visible on agent dispatch
  artifacts:
    - internal/acpserve/simulator_e2e_test.go (MOD) — command-surface E2E scenarios riding the existing simulator harness
    - 20-06-SUMMARY.md recording the phase-close verification matrix + operator checkpoint outcome
  key_links:
    - simulator fake provider enters through the REAL seam (16-06 precedent: temp project config → factory-built provider → scripted SSE stub) — zero production changes for testability
    - the E2E advertisement assertion consumes the same frames the golden tests pinned in 20-01 (schema spellings hold over the wire)
  assumptions:
    - "PROBE FLAG CARRIED (all 9 rows): the phase's nine unresolved probe rows are flagged in plans 20-01..20-05; this plan's E2E exercises the observable behaviors behind ACP-04, CMDS-01..04, SKLS-01..02 but does not retro-resolve the probe rows — they stay flagged per the fallback protocol."
    - "OPERATOR-UAT RISK (RESEARCH A2): if Zed renders input.hint or autocomplete differently than assumed, the protocol-conformant behavior still holds; the operator checkpoint captures any UX divergence as follow-up notes rather than a phase failure."
---

<objective>
Close the phase: a wire-level E2E battery over the existing Zed-client simulator proving the command surface end-to-end (advertisement, class-B round-trip, live rescan re-fire, agent dispatch + resolvedModel), the full criteria-to-evidence mapping, and the live-Zed operator checkpoint per the Phase 15/16 WINDOWS precedent (criterion 1/2 UX confirmation, RESEARCH A2).

Purpose: unit batteries prove components; this plan proves the composition over the real wire and puts the editor-native UX in front of the operator before the phase seals.
Output: simulator E2E scenarios, a green mise ci at phase close, and the recorded operator verdict.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-CONTEXT.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-RESEARCH.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-01-SUMMARY.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-02-SUMMARY.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-03-SUMMARY.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-04-SUMMARY.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-05-SUMMARY.md

All five build plans executed. The simulator harness and its fake-provider seam exist from 16-06
(internal/acpserve/simulator_e2e_test.go); this plan adds scenarios, it does not touch production
code except where a scenario exposes a real defect (fix forward with a unit test alongside).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simulator E2E — command surface over the wire</name>
  <files>internal/acpserve/simulator_e2e_test.go</files>
  <read_first>internal/acpserve/simulator_e2e_test.go (the 16-06 harness: fake provider via the REAL seam — temp project config aiming the factory-built provider at a scripted SSE stub; frame capture/sink helpers), 20-01-SUMMARY.md (frame shapes), 20-05-SUMMARY.md (rescan timing + debounce constant), 20-04-SUMMARY.md (skill/agent fixtures to reuse), 20-RESEARCH.md §Validation Architecture (E2E row for ACP-04/CMDS-04), 16-06-SUMMARY.md in .planning/phases/16-acp-wire-foundation/ (simulator lessons: SSE flush-at-next-block determinism)</read_first>
  <action>Add four scenarios to the simulator battery: (1) ADVERTISEMENT: initialize + session/new over the simulator → assert the session/update frame with kind available_commands_update carries the sorted full winner set including builtins (status, init, help...) and any fixture-discovered entries; frame shape matches the 20-01 golden spellings. (2) CLASS-B ROUND-TRIP: send "/status" as a session/prompt → assert the user_message_chunk echo (distinct messageId), agent_message_chunk output, stopReason end_turn, ZERO requests to the scripted SSE stub (the fake provider saw no model call), and a local_command line in the transcript on disk. (3) LIVE RESCAN: after session start, write a new commands/<name>.md under the watched temp project root → within debounce+epsilon assert a re-fired available_commands_update whose set includes the new command; send "/<name>" and assert it fires (expansion or class-B per fixture type). (4) AGENT DISPATCH: with a fixture .claude/agents/<agent>.md declaring model: and tools:, send "/<agent> hello" → assert the subagent's chunks streamed into the turn, the resolvedModel live note appeared, the dispatch line on disk carries ResolvedModel, and end_turn. Determinism: follow the 16-06 flush-at-next-block lesson when scripting SSE; poll with deadlines for rescan timing. If any scenario exposes a production defect, fix it minimally WITH a unit test beside it (fix forward), keeping the E2E as the regression proof.</action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ -run 'TestSimulatorCommandSurface' -x</automated>
  </verify>
  <acceptance_criteria>
    - four scenarios pass under -race: advertisement shape, class-B round-trip (zero SSE-stub requests asserted), rescan re-fire + invocation of the new command, agent dispatch with both resolvedModel channels
    - the E2E transcript assertions read the on-disk JSONL (transcript-as-truth), not in-memory state
    - no production file changes UNLESS a defect surfaced; any such change carries its own unit test in the same commit
  </acceptance_criteria>
  <done>The command surface is proven over the real wire composition: advertisement, class-B, live rescan, agent dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Full gate + criteria-to-evidence mapping</name>
  <files>.planning/phases/20-built-in-commands-skills-per-agent-model/20-06-SUMMARY.md</files>
  <read_first>.planning/ROADMAP.md Phase 20 section (the six success criteria — the mapping target), the five prior SUMMARYs (evidence pointers per criterion), 20-VALIDATION.md (the verification map this phase executed)</read_first>
  <action>Run the full gate: `mise ci` (vet + lint + CGO_ENABLED=0 build + go test -race -count=1 ./...) — must be green; record the command outputs in the SUMMARY. Then write the criteria-to-evidence matrix into the SUMMARY: criterion 1 → 20-06 E2E scenario 1 + 20-01 goldens + Task 3 operator leg; criterion 2 → 20-06 scenario 3 + 20-05 race battery; criterion 3 → 20-02 battery (zero model turns, local_command lines; /compact leg = routing + loud degrade until Phase 19 registers, explicitly noted); criterion 4 → 20-04 TestInitExpansion; criterion 5 → 20-04 TestSkillSlash/TestAgentSlash; criterion 6 → 20-03 TestDispatchModel + 20-06 scenario 4. Also record: the nine flagged probe rows (unchanged), the two phase prohibitions (P-20-01, P-20-02) with their enforcing tests, the D-01..D-16 decision coverage, and any deferred findings for the retrospective. Do not claim a criterion verified without naming its command and file.</action>
  <verify>
    <automated>mise ci</automated>
  </verify>
  <acceptance_criteria>
    - mise ci exits 0 (vet, golangci-lint, CGO_ENABLED=0 build, go test -race ./...)
    - 20-06-SUMMARY.md contains the six-criterion matrix with a command + artifact per row
    - the SUMMARY explicitly states the /compact real-compaction leg is owned by Phase 19 (PAR-01) and what Phase 20 shipped for it (routing + recording + loud degrade + the registered-seam invocation)
    - no criterion row says "verified" without an executable command or the operator checkpoint reference
  </acceptance_criteria>
  <done>Full gate green; every success criterion has named, executable evidence; the Phase-19 ordering is documented in the record.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator checkpoint — live Zed command-surface confirmation</name>
  <what-built>The complete Phase 20 command surface: slash autocomplete driven by available_commands_update (builtins + discovered commands + skills + agents, winners-only), instant class-B commands (/help /status /cost /mcp /memory /permissions /doctor /config /model /clear /resume /compact) with zero model turns, /init and /<skill> prompt expansion, /<agent> subagent dispatch, live rescan without restart, and per-agent model routing with resolvedModel reporting.</what-built>
  <how-to-verify>
    1. Launch ass-guard in Zed from a project with at least one skill (e.g. .claude/skills/demo/SKILL.md), one agent (.claude/agents/demo-agent.md with model: frontmatter), and one command file (.claude/commands/notes.md).
    2. Type "/" in the prompt: confirm the autocomplete lists the builtins AND demo/notes/demo-agent — no duplicates, no shadowed entries.
    3. Run /status: confirm it answers instantly (no spinner-for-a-model-turn), the echo bubble + output appear, and /help's inventory matches what autocomplete showed.
    4. While a session is open, create .claude/commands/extra.md; within a second or two, re-type "/" and confirm extra appears WITHOUT restarting the session; run /extra once to confirm it fires.
    5. Run /demo-agent summarize this repo: confirm the subagent's output streams into the conversation and a note names the resolved model (from the agent's frontmatter).
    6. Run /cost: confirm a number appears with a source note (transcript derivation unless a provider endpoint is configured).
    7. Run /compact: confirm the loud named-absence output (Phase 19 machinery not yet registered) rather than silence or an error — expected behavior at this milestone position.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (note divergences concretely: what was typed, what appeared, what was expected)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator scripted responses → E2E assertions | The simulator's fake provider is test infrastructure — it must enter through the REAL seam so the proof is of production wiring, not of a test double's private path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-22 | Repudiation | verification claims | medium | mitigate | Criteria-to-evidence matrix names an executable command per row; no assertion-only claims; the operator checkpoint is recorded with its verdict in the SUMMARY |
| T-20-23 | Tampering | E2E-induced production changes | low | mitigate | Fix-forward rule: any defect fix ships with a unit test in the same commit; the E2E remains the regression proof |

Security posture: ASVS L1; block_on=high — verification plan, no new attack surface.
</threat_model>

<verification>
- TestSimulatorCommandSurface green under -race (four scenarios)
- mise ci green at phase close
- Operator checkpoint completed with a recorded verdict (approved or issue list)
- Criteria matrix complete in 20-06-SUMMARY.md
</verification>

<success_criteria>
1. The command surface is proven end-to-end over the real wire composition.
2. All six phase success criteria have named evidence; the Phase-19 /compact ordering is explicit in the record.
3. The operator has confirmed (or flagged) the live-Zed UX: autocomplete, instant class-B, live pickup, resolvedModel visibility.
</success_criteria>

## Artifacts this phase produces

- internal/acpserve/simulator_e2e_test.go (MOD): TestSimulatorCommandSurface (advertisement, class-B round-trip, live rescan, agent dispatch scenarios)
- .planning/phases/20-built-in-commands-skills-per-agent-model/20-06-SUMMARY.md: criteria-to-evidence matrix + operator checkpoint record
- No production artifacts (fix-forward only if scenarios expose defects), no CLI flags, no env vars, no user setup

<output>
Create `.planning/phases/20-built-in-commands-skills-per-agent-model/20-06-SUMMARY.md` when done
</output>
