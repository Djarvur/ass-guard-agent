---
phase: 20-built-in-commands-skills-per-agent-model
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - internal/runtime/commands.go
  - internal/runtime/commands_test.go
  - internal/modelrouting/config.go
autonomous: true
requirements: [CMDS-02]
estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Every one of the twelve class-B commands (/help /status /cost /mcp /memory /permissions /doctor /config /model /clear /resume /compact) executes at the runner seam with NO model turn (fake provider Stream count == 0 per command in the battery) and writes a local_command transcript line (CMDS-02, D-05)
    - /help renders its inventory FROM the live chain itself — adding a skill or command file changes /help's output without any hand-maintained list (D-08)
    - /clear writes a full context-reset boundary in the SAME session — the Projector's lean window starts empty while transcript and session id survive; resume is unaffected (D-06)
    - /cost tries the provider's usage endpoint live under the ~10s FAST-CONTROL budget when the provider declares a capability, and falls back to transcript usage × modelrouting cost table WITH a source note naming which produced the number (D-07)
    - /model switches session-scope routing via the ApplyTurnModel live-apply seam and takes effect on the next request — it never persists into project/global config layers (D-05 family; 16-D-12)
    - /compact and /resume route through their delegation seams; when the Phase 19/18 machinery is absent each degrades LOUDLY with a named outcome recorded in the local_command line — never silently, never a wedged turn
    - /status, /memory, /mcp, /doctor, /config are pure-local — zero network in their handlers (Pitfall 8)
  artifacts:
    - internal/runtime/commands.go — eleven remaining class-B handlers completing the table from 20-01 (help memory permissions doctor config model clear resume compact cost mcp)
    - builtin handler dispatch under the per-session FAST-CONTROL budget for the one network-capable handler (/cost)
    - Runner delegation seams: compaction entry point (Phase 19 registers; loud unregistered-degrade until then) and resume-picker entry point (Phase 18; same posture)
    - /cost provider usage-endpoint capability seam (config-declared per provider, default none) + transcript×Pricing fallback with source note
    - internal/runtime/commands_test.go — TestClassB battery covering all twelve commands
  key_links:
    - /model → Runner.ApplyTurnModel (runtime.go:1470) + Session.SetTurnModel (session.go:296) — the 16-05 live-apply seam; a SECOND config-write path is the research Anti-Pattern (must not persist to layers)
    - /clear → the session boundary machinery (internal/session/boundary.go) — the Projector lean-window reset, not a new session
    - /cost fallback math → modelrouting Target.Pricing (config.go:131–138) + CostCeilingTracker accounting (cost.go:66/179)
    - /compact → Runner compaction seam → Phase 19 CompactNow (19-D-11 same-machinery-immediate contract) when present
  prohibitions:
    - "P-20-02: Every /cost output carries a source note naming which producer made the number (live provider endpoint vs transcript usage × cost table) — a transcript-derived figure is never presented as provider-live, and no cost figure is shown without its provenance (D-07 source-note transparency)."
  assumptions:
    - "PROBE-UNRESOLVED CMDS-02 (unclassified): probe returned 'unclassified — review manually'; row stays flagged, never auto-resolved. Goal-backward coverage: the twelve-command battery asserts zero-model-turn + local_command line + echo/output/end_turn shape per command."
    - "DEPENDENCY FLAG (Phase 19/18, per orchestrator constraint): Phase 19 (Compaction) and Phase 18 (Session Family) are planned but NOT executed. /compact and /resume are built here as invocation routing + durable recording + loud degradation through registered seams; the real machinery lands when PAR-01 (19-04) and the picker (18-04) execute and register. This plan does NOT build compaction or session-listing internals."
---

<objective>
Complete the class-B family: all twelve control-plane commands executing at the runner seam with zero model turns, the D-05 output shape, durable local_command records, and the three hard behaviors — /clear's same-session context-reset boundary (D-06), /cost's endpoint-first-then-transcript cost derivation with source note (D-07), and /model's session-scope live-apply (16-D-12) — plus loud-degrading delegation seams for /compact (Phase 19) and /resume (Phase 18) per CMDS-02.

Purpose: criterion 3 of the phase ("respond instantly with NO model turn") is won or lost here; the delegation seams keep Phase 20 honest about the Phase 19/18 ordering without pretending unbuilt machinery exists.
Output: eleven remaining handlers in internal/runtime/commands.go, the /cost capability seam, the two delegation seams, and the full TestClassB battery.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-CONTEXT.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-RESEARCH.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-PATTERNS.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-01-SUMMARY.md

Plan 20-01 delivered the chain, the intercept, the D-05 shape, and /status. This plan fills the
table. Phase 16's live-apply seam (Runner.ApplyTurnModel at runtime.go:1470 + Session.SetTurnModel
at session.go:296) and config surface are the /model and
/config backends; Phase 19/18 seams do NOT exist yet — handlers degrade loudly through the seams.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-local snapshot family — /help /memory /permissions /mcp /doctor /config</name>
  <files>internal/runtime/commands.go, internal/runtime/commands_test.go</files>
  <read_first>internal/runtime/commands.go (20-01 chain + /status handler — copy its snapshot style), 20-CONTEXT.md D-08/D-09, internal/ecosys/loader.go:1178–1222 (AllX sorted accessors for /help's inventory), internal/session/session.go + manager.go (loaded memory sources: agent-md files list, learning-store summary accessors), internal/modelrouting/config.go (Load overlay shape for /config's view), internal/acpserve/config_surface.go (16-05 config surface for /config values), 20-RESEARCH.md §Pitfall 8 + §Pattern 5, 20-PATTERNS.md §Shared Patterns</read_first>
  <behavior>
    - /help output enumerates every chain winner (builtins with live handlers + skills + agents + file commands) with one line each: name, kind, description, argument hint when present — and NOTHING else; a fixture with an added skill changes the output
    - /memory lists loaded agent-md source paths + a learning-store summary line, read-only — no edit affordance, no file writes
    - /permissions reports the current gate mode (ungated default per the safety-model amendment) and notes when the Phase 17 permission machinery is not present in this build
    - /mcp lists discovered MCP server names from r.mcpServers with connection state; shape is a compact list (discretion per CONTEXT)
    - /doctor runs its fixed check list (discretion): workdir resolvable, config layers load, provider credential PRESENCE (env var NAMES found/unfound — never values), transcript directory writable, registry load status with D-11 skip count
    - /config renders the current resolved view (tier→model bindings, session tier, compaction keys when present) from the loaded config
    - all six handlers complete with zero network I/O and zero provider calls
  </behavior>
  <action>Write failing battery cases FIRST (TestClassB table: one case per command asserting Stream count == 0, echo+chunks+end_turn shape, local_command line with the right Name and outcome). Implement the six handlers in commands.go following the /status template from 20-01: each returns output text built purely from live state. /help is generated FROM the live chain (D-08) — iterate the advertisement builder's entries, never a static list. /doctor's credential check reports the ResolveCredential ENV VAR NAME and whether it is set — never the value (T-20-03 mitigation). /permissions reads the mode from config; when the Phase 17 gate machinery is absent the output says so plainly (same loud-degrade family as D-12). Keep every handler under the turn mutex trivially (they are local) — no goroutines, no locks beyond the runner's existing state guards.</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestClassB' -x</automated>
  </verify>
  <acceptance_criteria>
    - TestClassB covers all six commands with Stream count == 0 asserted per command
    - /help test proves self-description: registry fixture A (no skills) vs fixture B (one skill added) produce different /help output, and B's output names the skill
    - /doctor test asserts output contains credential env var NAMES and does NOT contain any resolved credential value (fixture sets a dummy value and asserts it is absent from output)
    - /memory test asserts read-only: no file in the session dir changes mtime/content during the handler (compare before/after)
    - /permissions output names the current mode; with default config the string includes the ungated mode
  </acceptance_criteria>
  <done>Six pure-local class-B handlers live in the chain with self-describing /help, read-only /memory, credential-safe /doctor, and the battery proves zero model turns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mutating + delegating commands — /model /clear /resume /compact (D-06, delegation seams)</name>
  <files>internal/runtime/commands.go, internal/runtime/commands_test.go</files>
  <read_first>internal/runtime/runtime.go:1470 (Runner.ApplyTurnModel) + internal/session/session.go:296 (Session.SetTurnModel — the 16-05 live-apply seam), internal/session/boundary.go (existing boundary machinery for /clear), internal/session/projector.go (lean-window/reset-point semantics), 20-CONTEXT.md D-06 + Deferred/integration notes, .planning/phases/19-compaction-cache-control/*-PLAN.md if present else 19 section in ROADMAP (CompactNow naming per 19-D-11 — the seam NAME this plan must match), internal/session/manager.go:362–367 (AppendLocalCommand outcome + expansion fields for recording), 20-RESEARCH.md §Anti-Patterns (second-config-write-path; agent-side TUI for /resume)</read_first>
  <behavior>
    - /model glm-5.2 (or any declared slug) applies at the live-apply seam and the NEXT request carries it; /model with no args prints the current session model + tier; an unknown slug produces a loud error output and the session model is UNCHANGED
    - /model never writes a project or global config layer (layer files byte-identical after the command — assert mtime+content)
    - /clear writes a full context-reset boundary in the SAME session: subsequent projector window is empty; transcript file and session id unchanged; a later resume replays the full history including the boundary
    - /resume with Phase 18 machinery absent: loud output naming the absence + pointer text; local_command outcome records the degraded result; the turn still ends end_turn
    - /compact with Phase 19 machinery absent: same loud-degrade posture; typed args recorded verbatim in the local_command line (16-D-22); when the seam IS registered (Phase 19 lands), the handler invokes it under the same turn-mutex discipline and reports the outcome
  </behavior>
  <action>Implement /model as a thin caller of the existing 16-05 live-apply seam — Runner.ApplyTurnModel (runtime.go:1470) plus Session.SetTurnModel (session.go:296) — session-scope ephemeral override only; persist-then-apply layer writes stay editor-owned (16-D-12). Unknown slug → loud output + unchanged state (degrade family). Implement /clear via the session boundary machinery: append the same context-reset boundary class the Projector already treats as a hard reset — the lean window starts empty on the next turn; do NOT create a new session or touch the transcript's existence (D-06 locks same-session). Implement /resume and /compact as delegating handlers over two named Runner seams (registered callback fields following the SetCommandsNotify pattern from 20-01): the compaction entry point named for Phase 19's immediate-trigger contract (19-D-11) and the resume-picker entry point named for the Phase 18 picker (18-D-10). When a seam is unregistered, the handler emits the loud named-absence output and records outcome "unavailable: <machinery> not registered in this build" in the local_command line. When registered, invoke under the held turn mutex, pass /compact's typed args through to the seam's signature if it admits them (19-RESEARCH open question 3), and surface the outcome text. These handlers never touch the network and never fabricate compaction or session-listing behavior (orchestrator Phase-19 constraint: route the invocation, do not build PAR-01 here). Conversely, if Phase 18/19 have already executed when this plan runs and the machinery exists, register the real CompactNow / session-picker entry points into these callback fields during this task — never leave a seam unregistered when the machinery it names is present in the build.</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestClassBModel|TestClassBClear|TestClassBDelegate' -x</automated>
  </verify>
  <acceptance_criteria>
    - /model happy-path test: next provider request (fake provider captures the request) carries the switched model; unknown-slug test: error output emitted AND captured request still carries the previous model
    - /model test asserts project + global layer files are byte-identical before/after (read + compare)
    - /clear test: after the command, the projector window for the next turn is empty while the transcript file still contains all prior lines and the session id is unchanged
    - /compact and /resume tests (seams unregistered): output names the absent machinery, local_command outcome field records the degraded result, stopReason end_turn, Stream count == 0
    - /compact test with a REGISTERED fake seam: the seam is invoked exactly once with the typed args and the outcome text reflects the fake's result
  </acceptance_criteria>
  <done>/model applies session-scope without persistence, /clear resets context in-session, /resume and /compact route through named seams and degrade loudly until Phases 18/19 register their machinery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /cost per D-07 — endpoint-first under FAST-CONTROL, transcript×Pricing fallback with source note</name>
  <files>internal/runtime/commands.go, internal/runtime/commands_test.go, internal/modelrouting/config.go</files>
  <read_first>20-CONTEXT.md D-07 (operator directive verbatim), internal/modelrouting/cost.go:24–187 (CostCeilingTracker.Account pricing formula :61–89, Spent :179–184), internal/modelrouting/config.go:131–138 (Target.Pricing) + resolver.go:101–122 (buildTarget), internal/acp/request_registry.go:105 (FAST-CONTROL timeout class registration) + internal/acp/handlers.go:207 (class usage), internal/session/transcript.go (usage lines available for reconstruction — 18-D-01 transcript-as-truth), 20-RESEARCH.md §Pattern 5 + Open Question 1 (per-provider capability seam, config-declared none default), internal/modelrouting/dispatch.go (provider request plumbing for the live leg)</read_first>
  <behavior>
    - provider with declared usage capability + working endpoint: /cost returns the live number with the source note naming the provider endpoint
    - endpoint timeout (>~10s) or error: falls back to transcript usage × Target.Pricing with the source note naming the transcript derivation — never a failed turn
    - provider with capability "none" (the default for every provider until declared): goes straight to the fallback with its source note — no network attempt
    - transcript-derived number survives resume (recomputed from transcript lines, not in-memory state)
    - output never includes credential values (only amounts, currencies, model names, source note)
  </behavior>
  <action>Implement the /cost handler: (1) a per-provider usage-endpoint capability field on the modelrouting provider config (declared capability; default "none" — RESEARCH Open Question 1's seam-over-surprise rule); (2) when a capability is declared, fetch on-demand via the provider plumbing under the FAST-CONTROL timeout class (~10s — reuse the registered class at request_registry.go:105 via the same mechanism handlers.go:207 uses, or an equivalent context.WithTimeout if the class registry is acp-request-scoped); (3) on absence/timeout/error, derive from transcript usage lines × modelrouting Target.Pricing using the CostCeilingTracker accounting formula (cost.go:61–89) — reuse, never re-implement the math (Don't-Hand-Roll table); (4) ALWAYS append the source note: which producer (live endpoint name vs transcript×cost-table) made the number (D-07; this is prohibition P-20-02 — transparency). The output is a small table: per-model usage, cost, source note. Do not emit credential material (T-20-03). Zero model turns. Bound the whole handler: pure-local config/transcript reads + at most one bounded live fetch, so the turn mutex is never wedged past the FAST-CONTROL budget (Pitfall 8).</action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestClassBCost' -x</automated>
  </verify>
  <acceptance_criteria>
    - TestClassBCost covers three cases: declared-capability live success (source note names the endpoint), capability "none" (fallback note, zero network attempts — assert via a counting transport), and endpoint timeout (fake transport sleeps past the budget; fallback note appears; elapsed handler time bounded near the ~10s class)
    - fallback math test: a fixture transcript with known usage lines × known Target.Pricing produces the exact expected amount (hand-computed in the test)
    - every output variant contains a source note; the transcript-derived note explicitly names the derivation (never presented as provider-live — P-20-02)
    - no output variant contains the fixture credential value
  </acceptance_criteria>
  <done>/cost honors the operator directive: live endpoint when declared and reachable, transcript×cost-table otherwise, source note always, zero model turns, bounded latency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider HTTP API → /cost handler | The live usage endpoint's response is external data rendered into the conversation |
| slash args → /model /clear /compact | Typed arguments drive state mutation (model switch, context reset) — hostile/garbled args must degrade, not corrupt |
| handler output → client UI | Command output is rendered as agent messages — it must not carry secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Information Disclosure | /cost /status /memory /doctor output builders | high | mitigate | Builders never interpolate ResolveCredential values; /doctor reports env var NAMES + set/unset only; battery asserts fixture credential value absent from all output (Task 1 + Task 3 acceptance) |
| T-20-06 | Denial of Service | /cost live fetch under turn mutex | high | mitigate | Single bounded fetch under the FAST-CONTROL ~10s class; timeout → fallback (D-07's own rule); all other handlers pure-local; battery asserts elapsed bounds |
| T-20-07 | Tampering | /model slug handling | medium | mitigate | Unknown slug → loud error + unchanged session model (battery asserts next request carries the previous model); application rides the existing 16-05 live-apply seam with its typed failures — no new write path (research Anti-Pattern 4) |
| T-20-08 | Elevation | /clear /compact /resume | low | accept | Same-trust-as-prompt operations by design: the user typing slash commands already controls the session; boundaries ride existing Projector/boundary machinery with no new authority |
| T-20-09 | Repudiation | delegation seams (/compact /resume) | low | mitigate | Absent-machinery degradations are recorded as local_command outcomes (durable, transcript-as-truth) — the operator can always see what a command actually did |

Security posture: ASVS L1; block_on=high — T-20-05/T-20-06 are high and mitigated by enforced battery assertions; no unmitigated high remains.
</threat_model>

<verification>
- TestClassB battery: all twelve class-B commands, zero provider Stream calls each, local_command line each
- /clear boundary proven at the projector level; /model persistence-free; /cost three-case matrix
- Delegation seams degrade loudly with Phases 18/19 absent (the orchestrator's Phase-19 constraint honored)
- `go test -race -count=1 ./internal/runtime/` green
</verification>

<success_criteria>
1. All twelve CMDS-02 commands execute at the runner seam with NO model turn and full local_command records (criterion 3, minus /compact's real-compaction leg which Phase 19 owns — this plan proves the routing + loud degrade).
2. /help is self-describing from the chain (D-08); /memory read-only (D-09); /clear same-session boundary (D-06).
3. /cost endpoint-first with source-noted fallback (D-07); /model session-scope live-apply without persistence (16-D-12).
4. /resume and /compact route through named seams; unregistered machinery degrades loudly, registered machinery is invoked (Phase-19 ordering handled explicitly).
</success_criteria>

## Artifacts this phase produces

- internal/runtime/commands.go (MOD): handlers — helpCommand, memoryCommand, permissionsCommand, mcpCommand, doctorCommand, configCommand, modelCommand, clearCommand, resumeCommand, compactCommand, costCommand (names indicative; keep the table's existing naming style from 20-01)
- Runner delegation seams: compaction entry-point callback (Phase 19 registers — named for the 19-D-11 immediate-trigger contract), resume-picker callback (Phase 18 — 18-D-10)
- modelrouting provider config: per-provider usage-endpoint capability field (default "none")
- internal/runtime/commands_test.go (MOD): TestClassB twelve-command battery, TestClassBCost three-case matrix
- No new wire frames, no CLI flags, no env vars, no user setup

<output>
Create `.planning/phases/20-built-in-commands-skills-per-agent-model/20-02-SUMMARY.md` when done
</output>
