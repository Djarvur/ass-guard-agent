---
phase: 09-serve-path-audit-zcode-parity-re-capture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/profile/stability_test.go
  - internal/profile/testdata/divergent-session/model-io-sess_testdivergent.jsonl
  - internal/profile/types.go
  - internal/profile/coverage_test.go
  - cmd/extract-profile/main.go
  - cmd/ass-guard/profile_check.go
  - cmd/ass-guard/profile_check_test.go
  - docs/recapture-runbook.md
requirements: [AUD-05]
# code + runbook doc only; the operator capture itself is plan 09-04's checkpoint
autonomous: true
must_haves:
  truths:
    - "The within-session stability test consumes the PINNED session ID from profiles/zcode/coverage.yaml target_capture_ref — never ScanRolloutDir + PickRichestMain wholesale selection (AUD-05, D-04, Pitfall 17's selection-bias fix)"
    - "The test skip-cleans when the pinned session file is absent from the rollout dir — CI on machines without the capture stays green, and the skip message names the runbook step that unblocks it"
    - "A synthetic session fixture with a mid-session tool-catalog change makes extraction FAIL — the stability assertion is proven non-vacuous (it CAN fail; Pitfall 17's canary requirement)"
    - "coverage.yaml + meta.yaml record BOTH versions at capture time: zcode version (new zcode_version field, from the new -zcode-version flag) and extractor version (existing) — `profile check` surfaces both (Pitfall 18)"
    - "The operator re-capture runbook exists in-repo at docs/recapture-runbook.md with the divergence-prone workload spec, session pinning, and re-baselining steps written verbatim from research Pitfalls 17/18 (D-04)"
  artifacts:
    - path: "internal/profile/stability_test.go"
      provides: "pinned-session within-session stability test (reads coverage.yaml via LoadCoverage; resolves the pinned file under the rollout dir; skip-clean when absent) + the divergence canary test over the synthetic fixture"
      min_lines: 100
    - path: "internal/profile/types.go"
      provides: "TargetCaptureRef.ZcodeVersion string (yaml zcode_version, json zcode_version) — additive"
    - path: "cmd/extract-profile/main.go"
      provides: "-zcode-version flag recorded into coverage.yaml target_capture_ref + meta.yaml"
    - path: "docs/recapture-runbook.md"
      provides: "the verbatim operator runbook: fresh rollout dir, zcode version recording, the scripted divergence-prone workload (>=5 turns, >=10 distinct tools, >=1 subagent, >=1 MCP attach/detach mid-session), pin + threshold verification, drift classification BEFORE profile update, re-extraction, re-baseline, run-record section"
  key_links:
    - from: "internal/profile/stability_test.go"
      to: "profile.LoadCoverage(../../profiles/zcode/coverage.yaml)"
      via: "TargetCaptureRef.Sessions[0].ID resolves the pinned rollout file"
      pattern: "LoadCoverage"
    - from: "internal/profile/stability_test.go"
      to: "profile.ExtractFromRollout(pinnedPath)"
      via: "the stability assertion runs on the pinned session, not PickRichestMain"
      pattern: "ExtractFromRollout"
    - from: "cmd/extract-profile/main.go"
      to: "profile.TargetCaptureRef.ZcodeVersion"
      via: "-zcode-version flag → coverage.yaml + meta.yaml"
      pattern: "zcode-version"
---

# Plan 09-03 — Parity re-capture machinery: pinned stability test, version provenance, the operator runbook

## Phase Goal

**As an** operator of a hands-off agent, **I want** the zcode parity stability test re-grounded on a newly pinned capture session, **so that** I can trust that the mimicry hasn't drifted.

**This plan's slice:** the CODE and RUNBOOK half of AUD-05 (locked D-04). Today `TestStability_WithinSessionExtractionSource` selects its subject via `ScanRolloutDir` + `PickRichestMain` — selection-biased toward a vacuous stability proof (Pitfall 17), and its would-be subject (`eea3dc48`, the Phase-1 pinned session) is absent on disk so the test cannot run at all. This plan flips the mechanism: the test consumes a session ID pinned in `profiles/zcode/coverage.yaml`, proves non-vacuity with a divergence canary fixture, records BOTH capture-time versions (zcode + extractor — Pitfall 18), and lands the operator runbook (workload spec, pinning, re-baselining) verbatim in-repo. The operator capture run itself is plan 09-04's checkpoint; this plan needs no capture and no API key.

## Truths

- Carries AUD-05's machinery: pinned-session consumption, canary, version recording, runbook — and implements D-04 verbatim (scripted divergence-prone workload spec, session pinning, re-baselining steps from research Pitfalls 17/18).
- Research ground truth: PITFALLS Pitfall 17 (`PickRichestMain` = selection bias; wholesale dir scanning mixes zcode builds; the canary requirement — "include one known-divergent event and verify the test would catch it"); Pitfall 18 (record BOTH versions; re-baseline deliberately; drift report before profile update; extraction needs NO API key — parity does); SUMMARY Phase-2 "Delivers" + Research Flags ("not library research but operator-procedure design — written into the phase plan verbatim from Pitfalls 17/18").
- The extractor ALREADY pins sessions when told: `cmd/extract-profile -sessions <id>` (chooseSession, main.go:103) — no extractor redesign; the runbook just forbids the flag-less `PickRichestMain` default for re-capture.
- The cross-session header-names test (`TestStability_CrossSessionHeaderNames`) keeps its existing scan — its invariant (12 header names identical across roles) is version-insensitive, and Pitfall 17's bias critique targets the extraction-SOURCE selection; note this in a comment, do not redesign it here.
- STATE.md carries the blocker this plan unblocks: "stability test blocked on absent pinned session `eea3dc48` — consumed by Phase 9 (AUD-05)".
- Discretion decision (CONTEXT): the runbook's exact prompt sequence below is the planner's concrete design (the workload SPEC — divergence classes + thresholds — is locked by D-04; the literal prompts are discretion and are written out in full so the operator never improvises).

<objective>
Rewire `internal/profile/stability_test.go` onto the pinned session (coverage.yaml `target_capture_ref.sessions[0].id`, skip-clean when absent), add the divergence canary (a synthetic mid-session-catalog-change fixture the extraction MUST reject), add `ZcodeVersion` capture provenance (`-zcode-version` flag on extract-profile, surfaced by `profile check`), and write `docs/recapture-runbook.md` carrying the operator procedure verbatim from Pitfalls 17/18. Output: everything the operator needs so that plan 09-04's single capture run re-grounds the stability test end-to-end.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-serve-path-audit-zcode-parity-re-capture/09-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md

<interfaces>
From the current code (read before editing):
```go
// internal/profile — the pieces being rewired
func ScanRolloutDir(dir string) ([]SessionStat, error)              // extract.go:276 — lists model-io-sess_*.jsonl (keep: used by cross-session test + extract CLI)
func PickRichestMain(stats []SessionStat) (SessionStat, error)      // extract.go:345 — REMOVED from the within-session stability test's path (selection bias, Pitfall 17)
func ExtractFromRollout(path string) (*Profile, error)              // the within-session stability assertion (errors on intra-session disagreement)
func LoadCoverage(path string) (CoverageManifest, error)            // coverage.go:22
type TargetCaptureRef struct { Sessions []SessionRef; ExtractedAt time.Time; ExtractorVersion string }  // types.go:112 — gains ZcodeVersion
type SessionRef struct { ID, Path, Role string }                    // types.go:105

// internal/profile/stability_test.go — the test being re-grounded
func rolloutDir(t *testing.T) string                                // resolves ~/.zcode/cli/rollout, "" when absent
func TestStability_WithinSessionExtractionSource(t *testing.T)      // TODAY: ScanRolloutDir + PickRichestMain — the selection bias being removed
func TestStability_CrossSessionHeaderNames(t *testing.T)            // unchanged (version-insensitive invariant; comment only)

// cmd/extract-profile/main.go — the pinning that already exists
func main()                                                         // -sessions flag (comma IDs; default PickRichestMain), -rollout-dir, -out, -parity-session, -name
func chooseSession(stats, sessions string) (profile.SessionStat, error)  // main.go:103 — explicit ID pinning already works
const extractorVersion = "extract-profile/01-02"                    // stamped into coverage + meta
// meta.yaml write site: main.go:194-200 (os.WriteFile, 0600)

// cmd/ass-guard/profile_check.go — the version surfacing point
func runProfileCheck(name, profilesDir, captureFile string) error   // loads the manifest, runs drift.Detect, reports TIER-1/2 drifts + footer to stderr
```
Current pinned state (verified): `profiles/zcode/coverage.yaml` pins `sess_016eee8a-f2f0-44c3-abf9-9d57a2cf04a1` (absent on disk); the rollout dir holds only 3 unrelated sessions — the test skips today and MUST keep skipping cleanly until plan 09-04 re-pins.
</interfaces>
</context>

<tasks>

<task type="tdd" id="T1">
  <name>Task 1: Stability test consumes the pinned session + the divergence canary (RED→GREEN)</name>
  <files>internal/profile/stability_test.go, internal/profile/testdata/divergent-session/model-io-sess_testdivergent.jsonl</files>
  <behavior>
    - Test 1 (pinned consumption, AUD-05): `TestStability_WithinSessionExtractionSource` loads `../../profiles/zcode/coverage.yaml` via `profile.LoadCoverage`, resolves `TargetCaptureRef.Sessions[0].ID` to `<rolloutDir>/model-io-sess_<ID>.jsonl`, and runs `ExtractFromRollout` on THAT file. The words `PickRichestMain` and `ScanRolloutDir` do not appear in the within-session test's selection path (grep-assertable).
    - Test 2 (skip-clean): when the pinned file is absent from the rollout dir (today's state), the test `t.Skipf`s with a message naming the pinned ID and pointing at `docs/recapture-runbook.md` — CI green without the capture.
    - Test 3 (divergence canary, Pitfall 17): a synthetic fixture `testdata/divergent-session/model-io-sess_testdivergent.jsonl` containing two full-request lines whose tool-catalog DIFFERS mid-session; `ExtractFromRollout` on it FAILS (non-empty error) — proving the stability assertion can fail, so a green run on the real pinned session means something.
    - Test 4 (homogeneous fixture passes): the existing stable-session fixture (or a two-line same-catalog fixture) extracts cleanly — the canary fails ONLY on real divergence, not on any two-line session.
  </behavior>
  <action>
    Rewrite `TestStability_WithinSessionExtractionSource` in `internal/profile/stability_test.go`: load the shipped manifest (`profile.LoadCoverage(filepath.Join("..", "..", "profiles", "zcode", "coverage.yaml"))` — skip-clean with a clear message if the repo layout ever hides it), read the pinned ID, `os.Stat` the rollout file (`rolloutDir(t)` helper stays), skip-clean naming the runbook when absent, else assert `ExtractFromRollout(pinned)` succeeds. Add a doc comment on the test stating WHY pinned-not-richest (Pitfall 17: richness-biased selection yields vacuous stability; wholesale scans mix zcode builds). Leave `TestStability_CrossSessionHeaderNames` untouched except one comment noting its invariant is version-insensitive. Build the divergent fixture by copying the shape of the existing testdata session fixtures in `internal/profile/testdata/` (read them first): two request lines sharing system blocks + header names but with DIFFERENT `tools` arrays (e.g. second line adds one tool) — the mid-session catalog-change class. Add `TestStability_CanaryDetectsDivergence` (Test 3) and `TestStability_HomogeneousPasses` (Test 4) over fixtures.
  </action>
  <read_first>
    - internal/profile/stability_test.go (the test being rewired + rolloutDir helper)
    - internal/profile/extract.go (ExtractFromRollout + ScanRolloutDir + PickRichestMain — what the assertion actually checks)
    - internal/profile/testdata/ (existing session fixture shapes to copy for the divergent fixture)
    - internal/profile/coverage.go (LoadCoverage — the manifest read)
    - .planning/research/PITFALLS.md (Pitfall 17 — selection bias, thresholds, the canary requirement)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/profile/ -race -run 'TestStability' -v 2>&1 | tail -15 && grep -c "PickRichestMain" internal/profile/stability_test.go || true</automated></verify>
  <done>The within-session test runs on the pinned session and skips cleanly today; the canary proves divergence detection (fails on the fixture) while homogeneous sessions pass; PickRichestMain gone from the test's selection path</done>
  <acceptance_criteria>
    - `go test ./internal/profile/ -race -run TestStability -v` shows the pinned test SKIP (naming the pinned ID + runbook) and the canary test PASS (extraction error on the divergent fixture)
    - `awk '/TestStability_WithinSessionExtractionSource/,/^}/' internal/profile/stability_test.go | grep -c "PickRichestMain\|ScanRolloutDir"` returns 0
    - `ls internal/profile/testdata/divergent-session/` lists the fixture
    - `go test ./internal/profile/ -race` exits 0
  </acceptance_criteria>
</task>

<task type="auto" id="T2">
  <name>Task 2: Version provenance — zcode_version in coverage + meta, surfaced by `profile check`</name>
  <files>internal/profile/types.go, internal/profile/coverage_test.go, cmd/extract-profile/main.go, cmd/ass-guard/profile_check.go, cmd/ass-guard/profile_check_test.go</files>
  <action>
    (a) Add `ZcodeVersion string` with tags `json:"zcode_version" yaml:"zcode_version"` to `TargetCaptureRef` in `internal/profile/types.go` (additive; doc comment: capture-time zcode version — Pitfall 18's "record BOTH versions" — empty = captured before the field existed). (b) In `cmd/extract-profile/main.go`: add flag `-zcode-version` (default empty; documented "verbatim output of `zcode --version` at capture time"); thread it into the manifest/meta write path so `coverage.yaml` `target_capture_ref.zcode_version` AND a top-level `meta.yaml` `zcode_version` line are both written when set (follow the existing meta write at main.go:194-200). (c) In `cmd/ass-guard/profile_check.go`: after the drift footer, print a provenance footer to stderr naming BOTH versions read from the manifest — `capture provenance: zcode <ZcodeVersion or "unrecorded">, extractor <ExtractorVersion>` — so every drift report is self-describing (Pitfall 18: "`profile check` surfaces both"). (d) Extend `coverage_test.go` with a round-trip case (yaml with `zcode_version: zcode 1.2.3` parses back) and `profile_check_test.go` with a footer assertion (manifest fixture carrying versions → stderr output contains both). Keep the shipped `profiles/zcode/coverage.yaml` UNTOUCHED (it gets its zcode_version in plan 09-04's re-capture, from real data — never hand-edited).
  </action>
  <read_first>
    - internal/profile/types.go (TargetCaptureRef — the additive field site)
    - cmd/extract-profile/main.go (flag set + manifest/meta write path, lines 31-45 + 185-205)
    - cmd/ass-guard/profile_check.go (runProfileCheck + the footer site)
    - .planning/research/PITFALLS.md (Pitfall 18 — version recording + surfacing)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/profile/ ./cmd/ass-guard/ -race -run 'TestCoverage|TestProfileCheck' 2>&1 | tail -4 && go build ./... && mise run vet && mise run lint</automated></verify>
  <done>zcode_version round-trips through the manifest; extract-profile stamps it into coverage.yaml + meta.yaml when given; profile check's footer names both versions; build/vet/lint clean</done>
  <acceptance_criteria>
    - `grep -n "zcode_version" internal/profile/types.go cmd/extract-profile/main.go` matches both
    - `grep -n "zcode-version" cmd/extract-profile/main.go` matches the flag
    - `grep -n "capture provenance" cmd/ass-guard/profile_check.go` matches the footer
    - `git diff --name-only` shows profiles/zcode/ NOT modified by this task
  </acceptance_criteria>
</task>

<task type="auto" id="T3">
  <name>Task 3: The operator re-capture runbook, in-repo, verbatim from Pitfalls 17/18 (D-04)</name>
  <files>docs/recapture-runbook.md</files>
  <action>
    Create `docs/recapture-runbook.md` with EXACTLY the following content structure (this IS the runbook — do not paraphrase the thresholds or reorder the drift-before-update step; a "Run record" section at the end starts empty and plan 09-04 fills it):

    1. **Purpose** — produce a newly pinned, DIVERGENCE-PRONE capture session re-grounding the within-session stability test (AUD-05). NOT richest-session selection: the pinned session must be able to FAIL the test (Pitfall 17 — `PickRichestMain` optimizes for richness; a homogeneous session passes vacuously and proves nothing).
    2. **Preconditions** — (a) FRESH ROLLOUT DIR: archive existing sessions (`mkdir -p ~/.zcode/cli/rollout-archive && mv ~/.zcode/cli/rollout/model-io-sess_*.jsonl ~/.zcode/cli/rollout-archive/`) or, if archiving is unacceptable, record the current newest filename and consider ONLY sessions newer than it — NEVER scan the whole dir wholesale (mixed zcode builds make sessions incomparable). (b) RECORD THE ZCODE VERSION first: run `zcode --version`, save the verbatim output (feeds `-zcode-version`). (c) Note the ass-guard git commit the extractor runs from (the drift report records it).
    3. **The scripted divergence-prone workload — run ONCE, in real zcode, ONE session** (D-04). REJECTION THRESHOLDS — the capture is REJECTED unless the session shows: at least 5 user turns, at least 10 distinct tools, at least 1 subagent dispatch (a subagent session file appears on disk), at least 1 mid-session tool-catalog change (MCP attach AND detach). Below-threshold captures are re-run, never relaxed:
       - Turn 1 (read/search variety): explore a small codebase question requiring Read + Grep or Glob + WebSearch or WebFetch.
       - Turn 2 (mutation variety): a code edit requiring Edit or Write + Bash (build/test) + Read to verify.
       - Turn 3 (subagent fan-out): dispatch a subagent task that itself uses 2-3 tools; follow up referencing its result.
       - Turn 4 (MCP attach MID-SESSION — the catalog-change trigger): add an MCP server to the workspace config, then a turn that lists and invokes one of its tools.
       - Turn 5 (MCP detach + mixed finale): remove the MCP server, then a mixed turn (text + a couple of tool classes) so the session ends with a second catalog change.
       - Optional Turn 6: a real SDD scenario via the toolkit's slash-commands when available (nice-to-have; not required for thresholds).
    4. **Pin + verify** — identify the new main session file (`model-io-sess_<uuid>.jsonl`, largest main-role file created after step 2a); verify the thresholds (count turns = full-request lines, distinct tool names, subagent files, distinct tool-sets across the session) and record the counts in the drift report; sanity-run the synthetic canary test (`go test ./internal/profile/ -run TestStability_CanaryDetectsDivergence -v`) to confirm the assertion CAN fail.
    5. **Extraction — NO API key needed (do NOT gate this on ZAI_API_KEY)**:
       - (a) Classify drift BEFORE any profile update (Pitfall 18): with the OLD profile still in place, run `go run ./cmd/ass-guard profile check zcode --capture-file <pinned session file>` and save the output as `profiles/zcode/drift-reports/<YYYY-MM-DD>-recapture.md`; COMMIT the drift report BEFORE the profile-update commit (a profile update PR without a drift-report artifact is a Pitfall 18 warning sign).
       - (b) Re-extract with the pinned session: `go run ./cmd/extract-profile -sessions <sessionID> -zcode-version "<verbatim version output>" -out profiles/zcode` (the `-sessions` flag pins explicitly — the flag-less `PickRichestMain` default is FORBIDDEN for re-capture).
    6. **Re-baseline — the parity leg DOES need `ZAI_API_KEY` (separate gate from step 5)**: (a) `go test ./internal/profile/ -run TestStability_WithinSessionExtractionSource -v` MUST be green on the newly pinned session; (b) re-run A/B parity and record the FRESH numbers in the drift report — thresholds are EXPLICITLY re-baselined, the Phase-1 numeric baseline is SUPERSEDED, and parity numbers are never compared across baselines silently; (c) commit the profile update referencing the drift-report commit; append session ID + both versions + threshold counts to the **Run record** section below.
    7. **Run record** (filled per capture; newest first): table with date | session ID | zcode version | extractor version | turns/tools/subagents/catalog-changes counts | drift-report path.
  </action>
  <read_first>
    - .planning/research/PITFALLS.md (Pitfalls 17 + 18 — the runbook's source; the doc must carry their substance verbatim, not a summary)
    - .planning/research/SUMMARY.md (§Phase 2 "Delivers" — the runbook scope line)
    - cmd/extract-profile/main.go (the exact flags the runbook names)
    - docs/install.md (the docs/ house style)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && test -f docs/recapture-runbook.md && grep -c "PickRichestMain\|ZAI_API_KEY\|zcode-version\|drift-reports" docs/recapture-runbook.md && grep -c "10 distinct tools\|5 user turns" docs/recapture-runbook.md</automated></verify>
  <done>The runbook exists in-repo carrying the workload spec, thresholds, pinning procedure, drift-before-update ordering, both version recordings, the key-gating note, and the re-baseline rule — verbatim from Pitfalls 17/18</done>
  <acceptance_criteria>
    - `grep -c "MCP attach" docs/recapture-runbook.md` returns >= 1 (the catalog-change trigger documented)
    - `grep -c "BEFORE" docs/recapture-runbook.md` returns >= 2 (drift-report-before-update + version-before-workload orderings pinned)
    - `grep -c "Run record" docs/recapture-runbook.md` returns >= 1
    - The doc names `-sessions` as the pinning flag and forbids the flag-less default for re-capture
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zcode rollout dir (external agent's logs) → stability test / extractor | Untrusted-on-disk JSONL from another application is parsed and its agreement asserted. |
| operator-supplied version strings (-zcode-version) → committed profile artifacts | Free-text operator input lands in version-controlled provenance fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-10 | Tampering | a hostile/divergent rollout session silently accepted as ground truth | mitigate | THE canary: the synthetic divergent fixture proves extraction rejects intra-session disagreement; thresholds (>=5 turns, >=10 tools, subagent, catalog change) reject homogeneous captures; pinned-ID consumption removes wholesale-scan mixing (Pitfall 17's three bars). |
| T-9-11 | Repudiation | profile updated with no record of what changed or which versions produced it | mitigate | Drift report committed BEFORE the profile update (runbook step 5a, order pinned); both versions stamped into coverage.yaml/meta.yaml and surfaced by `profile check` (Pitfall 18). |
| T-9-12 | Information disclosure | rollout sessions may contain secrets; fixtures committed to the repo | mitigate | The synthetic fixture is hand-built with fake values only (copy the STRUCTURE of existing testdata, never real session content); the real pinned session stays OUT of the repo (only its ID + counts are recorded). |
| T-9-13 | DoS | the stability test scanning a huge rollout dir | mitigate | Pinned-ID consumption reads exactly ONE file; the skip-clean path stats one path — no wholesale scan remains on the within-session path. |
</threat_model>

<verification>
- `go test ./internal/profile/ ./cmd/ass-guard/ -race` — green (pinned test skips cleanly naming the runbook; canary detects divergence)
- `mise run vet && mise run lint && mise run build` — clean
- `test -f docs/recapture-runbook.md` — the operator procedure is in-repo
</verification>

<success_criteria>
- The stability test's subject is the PINNED session from coverage.yaml (never richest-session selection) and it is provably able to fail (canary green on the divergent fixture) — Pitfall 17 closed
- Both capture-time versions are recorded and surfaced — Pitfall 18's ambiguity killer
- The operator runbook (workload, thresholds, pinning, drift-before-update, re-baseline) lives in-repo verbatim — D-04 — ready for plan 09-04's single operator run
</success_criteria>

<output>
Create `.planning/phases/09-serve-path-audit-zcode-parity-re-capture/09-03-SUMMARY.md` when done
</output>
