---
phase: 12-product-functional-completeness
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/toolexec/ddg.go
  - internal/toolexec/ddg_test.go
  - internal/session/session.go
  - internal/session/session_test.go
  - internal/toolexec/batch_test.go
autonomous: true
gap_closure: true
gap_ids: [G-12-3b]
requirements: [ACP-07]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A DefaultBackend.Fetch of a non-HTML body (text/plain, JSON, anything) ALWAYS returns valid JSON as the tool Output — a text/plain fetch lands as {"content": "<body>"} (truncated at fetchRawCap before wrapping), never raw bytes that json.Marshal cannot embed
    - A tool call's result ALWAYS lands in the transcript and reaches the model — an AppendToolResult Marshal failure logs loudly to stderr AND appends a structured fallback payload line keyed by the call id, so the model sees an error form instead of flying blind
    - The G-12-3b storm shape is mechanically impossible to reintroduce silently: a regression battery proves text/plain fetch → transcript-visible tool_result through the real session loop
  artifacts:
    - internal/toolexec/ddg.go — Fetch non-HTML passthrough wrapped as {"content": string}
    - internal/session/session.go — every swallowed AppendToolResult error site degrades loud (stderr log + fallback payload)
    - regression tests pinning both layers in ddg_test.go / session_test.go
  key_links:
    - DefaultBackend.Fetch output ↔ session.go boundedToolResult/AppendToolResult path (today invalid-JSON Output ⇒ appendLine Marshal fails ⇒ '_ =' swallows ⇒ zero transcript lines, unpaired tool_use blocks accumulate per round-trip)
    - RealExecutor WebFetch routing (real.go :63) ↔ Backend.Fetch — the single production passthrough path for text/plain bodies
---

<objective>
Close UAT gap G-12-3b (UAT test 6b, severity major): 33 identical WebFetch calls to one raw.githubusercontent URL over ~2m10s with ZERO tool_result lines landing — the model retried blind each round-trip (~280B request growth per step from accumulated unpaired tool_use blocks). Root cause REPRODUCED and pinned at commit 7bd70d3: `DefaultBackend.Fetch`'s non-HTML branch returns raw body bytes as `json.RawMessage` WITHOUT JSON-wrapping (internal/toolexec/ddg.go ~138-142) — for a text/plain response that is invalid JSON, `json.Marshal(Line)` then fails ("invalid character p looking for beginning of value"), `appendLine` returns the error, and the caller swallows it via `_ =` at internal/session/session.go:519. Successes only landed when bodies happened to be valid JSON or HTML-wrapped.

Purpose: the model MUST see its tool results, success or error — this is the gap truth verbatim. Same defect class as 14-05 CR-02 (subagent payloads, fixed there); the web-passthrough path was missed.

Output: two defense layers — (1) the source fix (passthrough wrapped like the HTML branch), (2) the chokepoint fallback (a bad payload can never vanish again), plus the regression battery proving a text/plain fetch produces a transcript-visible tool_result end-to-end.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-product-functional-completeness/12-UAT.md (## Gaps → G-12-3b, root_cause reproduced)
@internal/toolexec/ddg.go (:102-142 — Fetch; HTML branch wraps {"content": markdown}; passthrough returns raw bytes after truncation at fetchRawCap=512KiB)
@internal/toolexec/ddg_test.go (:125-141 TestFetchNonHTMLPassthrough — PINS the buggy unwrapped shape; must be deliberately re-pinned, not deleted)
@internal/session/session.go (:441, :452, :470, :519-520, :551 — five `_ =` AppendToolResult sites in the main loop; ask.go :403 and subagent.go :181/:183 carry the same pattern)
@internal/session/manager.go (:76-105 appendLine — Marshal error is returned BEFORE redaction/write; the error is real but currently discarded)
@cmd/ass-guard/ask_wiring_test.go (server-level harness conventions if an end-to-end leg is wanted beyond the session-level battery)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — passthrough must be valid JSON; session loop must surface dropped results</name>
  <files>internal/toolexec/ddg_test.go, internal/session/session_test.go</files>
  <behavior>
    - ddg_test.go: rewrite TestFetchNonHTMLPassthrough's expectation — Fetch on a text/plain body ("plain text body") returns VALID JSON unmarshalable into struct{ Content string }{"content":"plain text body"}; over-cap (>512 KiB) text/plain bodies truncate FIRST then wrap (content length == fetchRawCap); an application/json content-type body parses through unchanged as content carrying the original JSON TEXT (string, not re-encoded object).
    - session_test.go new test TestSession_ToolResultAppendFailureSurfaces: drive a real Session loop where an executor returns invalid-JSON Output (json.RawMessage of bare bytes) → assert (a) a tool_result line EXISTS in the transcript keyed by the failing call's id (the fallback payload), (b) something was logged to stderr (capture via slog default-handler swap or an injected writer seam — follow existing house seams), i.e. the loss is LOUD not silent.
    - RED expectation: both fail against the current tree exactly as diagnosed — passthrough returns "plain text body" bare (unmarshal fails) and the session test finds ZERO tool_result lines for the poisoned call.
  </behavior>
  <action>
    Update the existing TestFetchNonHTMLPassthrough expectations IN PLACE (re-pin, never delete — house discipline from SESS-04). Add the session-loop surfacing test beside the existing session_test.go wiring tests, reusing their Session construction helpers and transcript-reading idioms. Commit RED separately before any production change.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; go test ./internal/toolexec/ -run 'TestFetchNonHTMLPassthrough' -count=1 2&gt;&amp;1 | tee /tmp/12-10-red.txt; go test ./internal/session/ -run 'TestSession_ToolResultAppendFailureSurfaces' -count=1 2&gt;&amp;1 | tee -a /tmp/12-10-red.txt; grep -c 'FAIL' /tmp/12-10-red.txt</automated>
  </verify>
  <done>Both RED tests fail on the current tree with the diagnosed signatures (bare-bytes passthrough; silent transcript loss) — the UAT reproduction is mechanical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — wrap the passthrough + loud AppendToolResult fallback</name>
  <files>internal/toolexec/ddg.go, internal/session/session.go</files>
  <action>
    Layer 1 (source fix, ddg.go): in Fetch's non-HTML branch, after the fetchRawCap truncation, wrap the body EXACTLY like the HTML branch does — marshal map[string]string{"content": string(body)} with the same error convention on marshal failure. This changes the wire-visible form the model sees for non-HTML fetches: BEFORE capture-fidelity note — zcode's own captured WebFetch result forms are corpus-absent in all three fixtures (grep confirms zero WebFetch families in zcode-recaptured-2026-08.json, zcode-core-results.json, zcode-interactive-results.json), so there is NO captured form to diverge from; the {"content": ...} shape mirrors the already-shipped HTML branch, which IS the established ass-guard convention for fetched content. Note this capture-fidelity disposition explicitly in the SUMMARY (no fixture update needed; no conformance test pins the old unwrapped shape outside TestFetchNonHTMLPassthrough which Task 1 re-pins).

    Layer 2 (chokepoint fallback, session.go): introduce ONE tiny helper (e.g. appendToolResultLoud) used by ALL FIVE `_ = s.Manager.AppendToolResult(...)` sites in the main loop (:441, :452, :470, :519-520, :551) AND apply the same treatment to ask.go :403 and subagent.go :181/:183 — same file-package, same pattern, seven sites total. On nil error: behave exactly as today. On error: (a) log to stderr via the package's existing logging seam (slog at warn level naming turn/call-id/tool + the underlying error), (b) append a FALLBACK payload line — marshal map[string]string{"error": "tool result could not be recorded: <cause>"} (the established structured-error convention) — so the model always sees SOMETHING keyed by the call id; if even the fallback fails to marshal, use the hardcoded literal fallback byte form (same guard pattern already present at session.go :445-447). Do NOT change Manager.appendLine itself (redaction ordering stays untouched). Keep the helper small; no interface churn.

    Then run the FULL affected batteries: go test ./internal/toolexec/ ./internal/session/ ./cmd/ass-guard/ -count=1 — any test asserting the OLD unwrapped passthrough shape gets deliberately re-pinned (expected: only Task 1's own test; batch_test.go/real_test.go/backend_test.go should be unaffected since they exercise other paths — verify, don't assume). Close with mise ci.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; go test ./internal/toolexec/ ./internal/session/ ./cmd/ass-guard/ -count=1 &amp;&amp; mise ci</automated>
  </verify>
  <done>A text/plain fetch lands as valid {"content": ...} JSON; ANY AppendToolResult Marshal failure now produces stderr warning + a model-visible fallback tool_result keyed by the call id; full toolexec/session/cmd batteries green; mise ci green.</done>
</task>

<task type="auto">
  <name>Task 3: OPTIONAL hardening — repeated-identical-call hint (only if it stays small)</name>
  <files>internal/session/session.go, internal/session/session_test.go</files>
  <action>
    Include ONLY if implementation stays under ~40 lines total. In the main tool loop, before dispatching a batch call, check whether the SAME (tool name + input) pair has appeared N times (N>=3) earlier in THIS turn (track a tiny per-turn map[string]int keyed by name+string(input)); on reaching N, append a hint to the model as a synthetic tool_result... NO — do NOT fabricate results. Instead: log a stderr warning AND include a bounded hint INSIDE the next projected request only if an existing injection seam already supports it cleanly; if no clean seam exists, SKIP this task entirely and record the skip honestly in the SUMMARY (the UAT marks this hardening OPTIONAL; the two mandatory layers are Tasks 1-2). Never invent tool_result payloads the executor did not produce — that would corrupt pairing discipline (08-07).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; go test ./internal/session/ -count=1</automated>
  </verify>
  <done>EITHER a minimal same-turn repeat detector lands behind the existing seam without touching pairing discipline, OR the SUMMARY records an honest skip with rationale (optional item; both outcomes close the gap).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote HTTP body → tool Output → transcript | Untrusted remote content crosses into the model-visible transcript via Fetch; malformed/unwrapped bytes previously corrupted the JSONL transcript write path |
| executor errors → model visibility | Executor-level failures must reach the model as structured error forms, never vanish |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-10-01 | Information Denial (model blindness) / Repudiation | internal/session/session.go append sites | high | mitigate | The chokepoint fallback makes silent transcript loss impossible — stderr + fallback payload on every Marshal failure |
| T-12-10-02 | Tampering (transcript integrity) | internal/toolexec/ddg.go Fetch | high | mitigate | Invalid-JSON Output can no longer be produced by the passthrough; truncation-before-wrap bounds size |
| T-12-10-03 | Denial of Service (retry storms) | session tool loop | medium | mitigate | Optional Task 3 bounds blind-retry storms IF it stays small; otherwise accepted residual (recorded in SUMMARY) |
| T-12-SC | Tampering | dependency installs | high | mitigate | Zero new dependencies (cross-phase v1.1 invariant; Go stdlib + existing modules only) |
</threat_model>

<verification>
- RED commit precedes each behavior-adding GREEN commit (house TDD discipline; evidence /tmp/12-10-red.txt).
- Validity gate: Fetch on text/plain returns json.Valid output; over-cap bodies truncate before wrap; JSON content-types carry original JSON as a STRING inside content.
- Loudness gate: injected Marshal failure → stderr line + fallback tool_result keyed by the call id; zero silent drops possible on the seven covered sites.
- Regression battery: text/plain fetch → transcript-visible tool_result end-to-end through the real session loop.
- No fixture updates required (WebFetch corpus-absent in all three committed fixtures — verified by grep during planning); any test found asserting the old shape is deliberately re-pinned, not deleted.
- mise ci green.
</verification>

<success_criteria>
- G-12-3b closed at BOTH layers: the source defect (invalid-JSON passthrough) is gone, and even a hypothetical future bad payload degrades loud instead of vanishing.
- The UAT truth holds: a tool call's result always lands in the transcript and reaches the model, whether success or error.
- The retry-storm preconditions (silent loss + unpaired accumulation) are eliminated; optional dedup hint landed or honestly skipped.
- mise ci green; no regressions across toolexec/session/cmd batteries.
</success_criteria>

<output>
Create .planning/phases/12-product-functional-completeness/12-10-SUMMARY.md when done (list gap_ids [G-12-3b], the capture-fidelity disposition for the wrapped passthrough shape, the Task 3 land/skip outcome, and commit hashes).
</output>
