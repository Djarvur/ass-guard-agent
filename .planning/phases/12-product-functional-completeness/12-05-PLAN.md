---
phase: 12-product-functional-completeness
plan: 05
type: execute
wave: 2
depends_on: ["12-01", "12-03"]
files_modified:
  - tools/zcode-recapture/scripted-workload.mjs
  - tools/zcode-recapture/harvest-deferred-forms.mjs
  - tools/zcode-recapture/README.md
  - internal/coreexec/testdata/zcode-recaptured-2026-08.json
  - internal/coreexec/ask.go
  - internal/coreexec/ask_test.go
  - internal/coreexec/bash.go
  - internal/coreexec/bash_test.go
  - internal/coreexec/files.go
  - internal/coreexec/files_test.go
autonomous: true
requirements: [ACP-07]

estimate:
  tokens: 84000
  raw_tokens: 84000     # factor 1 — estimate-calibration history unavailable in this install
  tasks: 3
  confidence: low       # <3 calibrated samples; weigh file/task thresholds over the figure

must_haves:
  truths:
    - "The archived driver kit runs a fresh qualifying capture against CURRENT live zcode (the re-record path D-04 makes primary), with the scripted workload extended by a deferred-tools tour that exercises AskUserQuestion, EnterPlanMode/ExitPlanMode, SendMessage, ReadSessionContext, CronCreate/CronList/CronUpdate/CronDelete, TaskStop/TaskOutput, and Bash run_in_background — so the surviving ground truth for the 9 deferred tools + background flags exists IN THE REPO as a committed, redacted harvest fixture"
    - "The corpus-absent result forms are re-pinned where the fresh capture shows them: the harvested forms land in a committed fixture with full provenance (zcode version, session id, harvest date), and the ACP-07 families named by the requirement — truncation markers, the Bash timeout form, the Bash default timeout, the file-tool failure forms — plus the ask non-answer/answered forms are implemented in the executors from the capture"
    - "Operator-environment side effects are contained: any automation the tour creates in live zcode is deleted before the run ends (the kit's cleanup step proves it — CronList empty at exit), and every committed artifact passes the D-03 redaction self-grep before commit"
    - "The fixed extractor (12-03) processes the fresh capture: the from-rollout A/B's before/after artifact decomposition is recorded — empty-expectation turns and state-pollution turns both ZERO (the D-04 follow-up #2 acceptance)"
  artifacts:
    - path: "tools/zcode-recapture/scripted-workload.mjs"
      provides: "the extended workload: the original 5 divergence-prone turns + the deferred-tools tour turns (ask answered + ask timed-out under yolo, plan-mode enter/exit, subagent fan-out + SendMessage, ReadSessionContext self-read, cron create/list/update/delete with cleanup, background bash + TaskOutput + TaskStop)"
    - path: "tools/zcode-recapture/harvest-deferred-forms.mjs"
      provides: "the harvester: reads the fresh rollout JSONL, extracts every deferred-tool tool_result (name → form family → redacted template + observed counts), emits the fixture JSON + a stdout report"
    - path: "internal/coreexec/testdata/zcode-recaptured-2026-08.json"
      provides: "the committed re-pinned forms fixture (zcode-core-results.json shape) — the capture-pinned upgrade source for the corpus-absent families; families the capture still does not show stay flagged corpus_absent with the hunt documented"
    - path: "internal/coreexec/{ask,bash,files}.go"
      provides: "executor upgrades: harvested forms replace the documented defaults where the capture shows them (conformance tests re-pinned, late-harvest markers per the 08-08 precedent)"
  key_links:
    - from: "tools/zcode-recapture/zcode-driver.mjs (existing)"
      to: "tools/zcode-recapture/scripted-workload.mjs (extended)"
      via: "the driver's session/create + session/send app-server protocol drives the tour turns; the interactive ask leg is answered by a follow-up session/send while the turn holds its question state (the mechanism the driver already proved for resume boundaries)"
      pattern: "session/send|session/create"
    - from: "tools/zcode-recapture/harvest-deferred-forms.mjs"
      to: "internal/coreexec/testdata/zcode-recaptured-2026-08.json"
      via: "harvest → redact (D-03 walk) → fixture; the executors' conformance tests read the fixture families by name"
      pattern: "corpus_absent|observed_count"
    - from: "internal/parity (12-03's fixed extractor)"
      to: "the fresh capture"
      via: "ExtractTurnsFromRollout over the fresh rollout file → the artifact-decomposition report (empty/pollution counts) recorded in the SUMMARY"
      pattern: "ExtractTurnsFromRollout"
---

# Plan 12-05 — Re-record from live zcode: the deferred-tool forms + ACP-07 corpus-absent families (D-04's primary path)

## Phase Goal

**As a** developer driving ass-guard from an ACP editor, **I want** every tool in the captured catalog
to execute for real with capture-pinned result forms, and turn behavior guarded by a behavioral-eval
regression net, **so that** the product machinery is complete — the model never hits an unimplemented
dead end mid-task.

**This plan's slice:** the ground-truth half of capture-pinning. The pinned rollout file is LOST
(D-04's verified fact — rotated off every live location), so "re-pin against the Phase-9 pinned
session" now means: RE-RECORD from current live zcode using the archived driver kit
(`tools/zcode-recapture/`, quick task 260817-uv3), with a fresh qualifying capture standing in for
the original pin. The workload gains a deferred-tools tour so the 9 tools + background flags leave
REAL captured result forms, the ACP-07 families (truncation markers, Bash timeout form, Bash default
timeout, file-tool failure forms) get their answer, and the 12-03-fixed extractor proves its worth
on a real capture.

## Truths

- Carries D-04 (the whole decision block) and ACP-07. The re-record route is PRIMARY; the
  lost-rollout OS-salvage is the deferred idea (operator-side, optional) and is NOT attempted here.
- The driver kit is the PROVEN mechanism (09-04's operator-authorized app-server stdio route:
  `/Applications/ZCode.app/Contents/Resources/glm/zcode.cjs app-server --stdio`, session/create +
  session/send, mode:"yolo", config-change + session/resume boundaries for MCP attach/detach).
  The kit's README + docs/recapture-runbook.md document the procedure this plan automates further.
- Cleanup discipline (operator environment): the cron tour MUST delete what it creates (CronList
  empty at exit — asserted by the workload's final step); the config backup/restore pattern the
  kit already carries stays in force; the kit writes only under its scratch workspace + /tmp.
- Redaction before commit (D-03 discipline): the harvester runs the walkRedact-equivalent over
  every harvested payload (values scrubbed, keys + structure preserved); a self-grep gate over the
  fixture (uuids, keys, tokens, paths, prompt text, header values) must be CLEAN before the commit.
- Honest-negative results are results: if the tour cannot make live zcode produce a form (e.g. a
  timeout form needs a genuinely hanging command — the tour CAN produce that with a sleep above
  the timeout), the family stays corpus_absent with the attempt documented — never invent a
  literal (the 08-08 rule). The zcode version at capture is recorded in the fixture provenance.
- The `agents/`/`artifacts/`/`exec/` sess_3cee56ae side-dirs salvage check (CONTEXT Discretion
  item): Task 1 performs it read-only and records the finding in the SUMMARY (even "nothing
  salvageable" is the answer); salvageable content folds into the harvest only with provenance.

<objective>
Produce and land the re-recorded ground truth: (1) re-run the archived driver end-to-end for a
freshness proof; (2) extend the scripted workload with the deferred-tools tour + write the forms
harvester; (3) run the capture, harvest + redact + commit the forms fixture, implement the
ACP-07 families + the ask forms in the executors from the capture, and record the fixed-extractor
artifact decomposition over the fresh capture. Output: the corpus-absent routing finally resolves —
every family either capture-pinned or honestly still-absent with the hunt documented.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-product-functional-completeness/12-CONTEXT.md
@profiles/zcode/drift-reports/2026-08-16-recapture.md
@docs/recapture-runbook.md

<scope_note>
10 files_modified, six of them executor/test sibling pairs — the same accepted shape as 08-08's
12-file precedent (executor siblings + their conformance tests + one fixture + the dev-time kit).
The alternative (splitting the executor upgrades into a follower plan) would leave the harvested
fixture unconsumed by any test between plans — exactly the fixture-rot the committed-fixture
convention exists to prevent. Splitting the DRIVER work from the EXECUTOR upgrades is already the
task boundary inside this plan.
</scope_note>

<interfaces>
From the current code + kit (read before editing):

```
tools/zcode-recapture/            # the archived driver kit (17 files; the 09-04 mechanism)
  zcode-driver.mjs                # app-server stdio driver: session/create + session/send (mode:yolo)
  scripted-workload.mjs           # the 5 divergence-prone turns (T1 reads/search → T2 mutation →
                                  #   T3 subagent fan-out → T4 MCP-attached → T5 mixed finale)
  probe-server.mjs                # the 30-line stdio MCP server (attach/detach via config+resume)
  capture-line-base.json          # full redacted request records (the surviving partial ground truth)
  cli-config-backup.json          # the config backup/restore pattern this plan MUST reuse
  README.md                       # the kit's documentation (extend with the tour + cleanup steps)

internal/parity/replay.go         # 12-03's FIXED ExtractTurnsFromRollout (delta-aware) — the
                                  #   artifact-decomposition leg consumes it
internal/coreexec/testdata/zcode-core-results.json   # the fixture SHAPE to clone:
                                  # {_provenance, tools:{<Name>:{input_keys_observed, input_note,
                                  #   results:{<family>:{isError, template|literal, observed_*,
                                  #   corpus_absent?, lateHarvest?}}}}}
internal/coreexec/bash.go         # the corpus-absent sites to upgrade: timeout form (:217-222),
                                  #   default timeout (schema-informed, :44-48), truncation policy
internal/coreexec/files.go        # the file-tool failure forms (Read missing-file + Edit/Write
                                  #   error families flagged in the 08-08 fixture)
internal/coreexec/ask.go          # 12-01's non-answer + answered forms (corpus_absent today)
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — the archived driver runs end-to-end against current live zcode (freshness proof + side-dirs salvage check)</name>
  <precondition>/Applications/ZCode.app exists with the glm/zcode.cjs app-server binary and the operator's GLM plan is logged in (the 09-04 route); tools/zcode-recapture/ is present (quick task 260817-uv3 archived it — verified 2026-08-17)</precondition>
  <files>tools/zcode-recapture/README.md</files>
  <action>
    Run the kit as-is: `node tools/zcode-recapture/zcode-driver.mjs` driving the original 5-turn workload against a fresh scratch workspace (the 09-04 procedure: scratch ws, config backup taken before any config touch, restored after). Verify the run's threshold shape (5 prompts → request records; a tool-set timeline in the capture). If the app-server protocol has drifted since 0.16.3 (zcode-version.txt is the kit's record — check the current `zcode --version`), fix only the driver's broken framing and record the delta in the README. While the scratch exists: perform the CONTEXT Discretion salvage check — read-only listing of `~/.zcode/cli/{agents,artifacts,exec}/sess_3cee56ae*` side-dirs; record in the README + SUMMARY what survives and whether any tool-result content is salvageable (fold into Task 3's harvest ONLY with provenance if it is). Document the extended procedure (tour + cleanup) in the README as the runbook addendum. This task proves the mechanism; the tour lands next.
  </action>
  <verify>
    <automated>cd tools/zcode-recapture && node zcode-driver.mjs --selftest 2>&1 | tail -5; test -s "$(ls -t ~/.zcode/cli/rollout/model-io-sess_*.jsonl | head -1)" && echo FRESH-ROLLLOT-PRESENT</automated>
  </verify>
  <done>The driver completes a fresh scripted run against current live zcode (or the protocol drift is fixed + recorded); the freshness evidence (new rollout file, run log tail) is preserved under tools/zcode-recapture/ or /tmp with paths in the task record; the side-dirs salvage finding is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The deferred-tools tour workload + the forms harvester</name>
  <precondition>Task 1's driver run is green (the mechanism works against current zcode)</precondition>
  <files>tools/zcode-recapture/scripted-workload.mjs, tools/zcode-recapture/harvest-deferred-forms.mjs</files>
  <behavior>
    - Behavior 1 (tour coverage): the extended workload's turn list covers every deferred tool at least once — ask (an answered leg via a follow-up session/send while the question is pending + a yolo-mode non-answer leg), EnterPlanMode then ExitPlanMode with a real plan, a subagent fan-out followed by SendMessage to the spawned agent id, ReadSessionContext targeting the capture session itself, CronCreate (delayMinutes-based, NOT a recurring cron — bounded operator-env footprint) + CronList + CronUpdate + CronDelete, Bash run_in_background of a sleep + TaskOutput (block + non-block) + TaskStop, and a Bash leg engineered to time out (sleep above the timeout) + a leg engineered to produce a large output (the truncation-marker hunt).
    - Behavior 2 (cleanup proof): the tour's final step asserts CronList is empty (everything created was deleted) and exits non-zero otherwise.
    - Behavior 3 (harvester determinism): harvest-deferred-forms.mjs over a given rollout file emits the same fixture JSON on repeated runs (no timestamps inside families; provenance carries one harvest date).
    - Behavior 4 (redaction): no harvested payload retains a real path, uuid, key, or prompt-verbatim content — the harvester redacts at extraction (values scrubbed, structure preserved).
  </behavior>
  <action>
    (1) Extend scripted-workload.mjs with the tour turns per Behavior 1 — write prompts that reliably provoke each tool (the model obeys its tool descriptions; phrase instructions imperatively: "ask me which X before proceeding", "enter plan mode and present a plan for Y", "start a background shell sleeping 60s, then check its output, then stop it"). The answered-ask leg: after the turn surfaces the question (the driver observes the ask state on the turn's output), send the answer as the next session/send — the same resume-boundary mechanism the kit proved for MCP attach. The timeout leg: instruct a Bash call with timeout 10000 on a `sleep 30`. The truncation hunt: a command emitting ~200KB (yes/seq loop). delayMinutes-based crons only (never recurring — a missed cleanup cannot then persist forever in the operator's env); the cleanup step still proves CronList empty. (2) Write harvest-deferred-forms.mjs: parse the fresh rollout JSONL, collect tool_result blocks by tool name (delta-aware — reuse 12-03's offset arithmetic translated to JS, or read via the Go extractor through a tiny `go run` shim if simpler), cluster into form families (isError, template with placeholder-substituted variable spans, observed counts), redact (D-03 value scrub), emit the zcode-core-results.json-shaped fixture + a stdout report of hits/misses per family. RED→GREEN where testable (Behaviors 2-4 assert via node's assert against a synthetic rollout fixture kept beside the kit).
  </action>
  <read_first>
    - tools/zcode-recapture/zcode-driver.mjs + scripted-workload.mjs (the protocol + existing turn shapes)
    - tools/zcode-recapture/README.md + docs/recapture-runbook.md (the procedure + thresholds)
    - internal/coreexec/testdata/zcode-core-results.json (the fixture shape the harvester emits)
  </read_first>
  <verify>
    <automated>cd tools/zcode-recapture && node --test ./ 2>&1 | tail -3 || node harvest-deferred-forms.mjs --selftest</automated>
  </verify>
  <done>The tour workload covers every deferred tool + the timeout/truncation hunts with cleanup-proof; the harvester emits deterministic redacted fixtures; both carry runnable self-tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run the capture, commit the re-pinned fixture, implement the ACP-07 families + ask forms, record the extractor decomposition</name>
  <precondition>Task 2's tour + harvester are self-test-green; the operator env is untouched-by-construction (scratch ws + delayMinutes crons + cleanup)</precondition>
  <files>internal/coreexec/testdata/zcode-recaptured-2026-08.json, internal/coreexec/bash.go, internal/coreexec/bash_test.go, internal/coreexec/files.go, internal/coreexec/files_test.go, internal/coreexec/ask.go, internal/coreexec/ask_test.go</files>
  <behavior>
    - Behavior 1 (fixture provenance): the committed fixture carries _provenance (zcode version at capture, session id, harvest date, tool list) and every family is either template-pinned-from-capture or corpus_absent with the hunt documented — zero invented literals (a test greps the fixture for the corpus_absent flags and asserts each absent family carries a hunt note).
    - Behavior 2 (executor conformance): where the capture shows a form, the executor's conformance test string-compares against the NEW fixture family (bash timeout form, bash default timeout resolution if the corpus reveals the omitted-field behavior, truncation markers if observed, file-tool failure forms, ask answered + non-answer forms) — tests re-pinned with lateHarvest markers per the 08-08 precedent.
    - Behavior 3 (still-absent honesty): families the capture does not show keep their current documented defaults; their fixture entries gain the updated hunt note; no test changes for them.
    - Behavior 4 (extractor decomposition): ExtractTurnsFromRollout over the fresh capture yields zero empty-expectation turns and the per-turn isolation holds (the before/after recorded in the SUMMARY vs the drift report's 6/13 + 4/13).
  </behavior>
  <action>
    (1) Run the full tour capture (driver + extended workload), then the harvester over the fresh rollout. (2) Self-grep the emitted fixture (uuids, key shapes, token literals, real paths, prompt-verbatim spans) — must be CLEAN — then commit it as internal/coreexec/testdata/zcode-recaptured-2026-08.json. (3) Upgrade the executors family-by-family where the capture pins: bash.go (timeout form → captured template; truncation markers if observed — apply at the same trim site with the captured marker shape; default-timeout resolution if the corpus shows omitted-field behavior), files.go (failure forms), ask.go (answered/non-answer forms — 12-01's interim defaults upgrade in place). Each upgrade: re-pin the conformance test against the new family, mark lateHarvest, keep the prior default in the fixture's history note. (4) Run the extractor decomposition (go run the parity extractor over the fresh rollout; record counts). (5) `mise ci` + the full coreexec battery. If a tour leg fails to provoke its tool (model refuses), record the honest negative + retry with a rephrased prompt once; a persistent miss stays corpus_absent — never hand-write the form.
  </action>
  <read_first>
    - internal/coreexec/bash.go + files.go + ask.go (the exact corpus_absent sites being upgraded)
    - internal/coreexec/fixtures_test.go (the conformance pattern the re-pinned tests follow)
    - internal/parity/replay.go (12-03's extractor for Behavior 4)
  </read_first>
  <verify>
    <automated>go test ./internal/coreexec/ ./internal/parity/ -count=1 && python3 -c "import json;d=json.load(open('internal/coreexec/testdata/zcode-recaptured-2026-08.json'));assert d['_provenance']['zcode_version'] and d['tools'];print('fixture-ok')"</automated>
  </verify>
  <done>The fresh capture is harvested + redacted + committed with provenance; every ACP-07 family + ask form is either capture-implemented (conformance re-pinned, lateHarvest-marked) or honestly corpus_absent with the hunt documented; the extractor decomposition shows zero empty-expectation + zero pollution turns; mise ci green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver → operator's live zcode | The kit drives the real app-server under the operator's logged-in plan and touches ~/.zcode/cli/config.json (attach/detach pattern) |
| live zcode → operator environment | The cron tour can create REAL automations in the operator's zcode if cleanup fails |
| fresh capture → committed fixture | Redacted provider traffic lands in the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05-01 | Tampering | operator config | high | mitigate | The kit's config backup/restore discipline (cli-config-backup.json pattern) is mandatory: backup before any touch, restore in a finally-block; a post-run diff against the backup must be empty |
| T-12-05-02 | Tampering | persistent automations | high | mitigate | delayMinutes-only crons (no recurring schedules — bounded lifetime) + the CronList-empty cleanup assertion as the workload's final step (non-zero exit on failure) |
| T-12-05-03 | Information disclosure | committed capture | high | mitigate | Redaction at extraction + the self-grep gate (uuids/keys/tokens/paths/prompt text/header values) CLEAN before commit — the D-03 discipline as a hard gate |
| T-12-05-04 | DoS | capture cost | low | mitigate | The tour is one session, ~10 turns, minutes-scale — the same cost class as the 09-04 run; the driver is dev-time only, never shipped |
| T-12-05-05 | Repudiation | form provenance | medium | mitigate | Every fixture family carries observed counts + zcode version + session id; lateHarvest markers preserve the upgrade history (the 08-08 audit shape) |
</threat_model>

<verification>
- `mise ci` green; go test ./internal/coreexec/ ./internal/parity/ green.
- The committed fixture passes the provenance + redaction gates; the kit's cleanup assertion ran green (CronList empty).
- The SUMMARY records: capture metadata, the family hits/misses table, the extractor before/after decomposition, the side-dirs salvage finding.
</verification>

<success_criteria>
- ACP-07 truth holds: the corpus-absent forms are re-pinned from a fresh qualifying capture (the D-04 primary route) and implemented where the corpus shows them.
- D-04's follow-up #2 acceptance holds on real data: zero empty-expectation + zero pollution turns via the fixed extractor.
- The repo carries its own ground truth for the deferred tools — the lost-rollout gap is closed by construction.
</success_criteria>

<output>
Create `.planning/phases/12-product-functional-completeness/12-05-SUMMARY.md` when done
</output>
