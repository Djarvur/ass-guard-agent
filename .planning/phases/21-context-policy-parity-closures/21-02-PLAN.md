---
phase: 21-context-policy-parity-closures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/ecosys/memory.go
  - internal/ecosys/memory_test.go
  - internal/runtime/runtime.go
  - internal/runtime/memory_wiring_test.go
autonomous: true
requirements: [PAR-04]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A fresh session in a repo with AGENTS.md/CLAUDE.md files shows those files' content in system context automatically — one trailing profile.TextBlock merged into the per-session profile COPY after the agent listing, with no manual step (PAR-04, criterion 3)"
    - "Same-directory collision resolves CC-natively: CLAUDE.md wins, AGENTS.md is skipped WITH a note; different levels accumulate (D-05)"
    - "Discovery walks UP from cwd to the git repo root and STOPS there (the .git boundary) — no walk above the project; the user global joins from both ~/.ass-guard/{AGENTS,CLAUDE}.md and ~/.claude/CLAUDE.md with ~/.ass-guard winning on conflict (D-06, D-07)"
    - "Injected content is capped: over-cap files truncate rune-safe with a loud truncation note inside the injection block (24 KB per file, 64 KB total budget — D-08 discretion values pinned here)"
    - "Zero memory files anywhere → zero injection block (the SkillListing render-then-skip contract); the shared r.profile is never mutated — the double-append copy discipline holds"
    - "Re-reads are mtime-cached: unchanged files are served from the path+mtime+size-keyed cache; a changed file is re-read within the same process (edits picked up on change, criterion 3)"
  artifacts:
    - path: "internal/ecosys/memory.go"
      provides: "the memory walker: DiscoverMemoryFiles(workDir string) []MemoryFile (cwd→git-root levels + user global, D-05/D-06/D-07 collision + precedence rules), the mtime+size-keyed package cache, MemoryInjection(workDir string) string returning the capped, note-annotated injection body (empty string when no files)"
      contains: "func MemoryInjection"
    - path: "internal/runtime/runtime.go"
      provides: "the FOURTH trailing-TextBlock merge in sessionFor (after AgentListing, :1072-1075 site) — ecosys.MemoryInjection joined with the established double-append copy discipline"
      contains: "MemoryInjection"
  key_links:
    - from: "internal/ecosys/memory.go MemoryInjection"
      to: "internal/runtime/runtime.go sessionFor"
      via: "the exact vehicle of the skills/agent listings (runtime.go:1056-1075 verbatim pattern): empty string → no merge; non-empty → one trailing blockText TextBlock on a fresh copy of prof.System"
      pattern: "trailing TextBlock"
    - from: "internal/ecosys/memory.go walker"
      to: "the git boundary"
      via: "level collection stops at the first ancestor containing .git (file or dir — worktree-safe); above the root nothing is read (D-06's deliberate narrowing vs CC)"
      pattern: "repo root"
  prohibitions: []
---

<objective>
Land PAR-04: AGENTS.md/CLAUDE.md auto-injection into system context — the discovery walker (D-05 collision, D-06 span, D-07 user-global precedence), the mtime cache, the D-08 caps, and the fourth trailing-TextBlock merge at the established sessionFor site. Fully independent of Phase 17 and of the other Phase-21 legs.

Purpose: criterion 3 — a fresh session in a repo with memory files shows them without any manual step, mtime-cached. The merge vehicle already exists verbatim (skills + agent listings); this plan adds the fourth merge of exactly that shape.
Output: internal/ecosys/memory.go + sessionFor wiring + table-pinned walker tests and a wiring integration test, green under -race.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-context-policy-parity-closures/21-CONTEXT.md
@.planning/phases/21-context-policy-parity-closures/21-RESEARCH.md
@.planning/phases/21-context-policy-parity-closures/21-PATTERNS.md
@internal/ecosys/skills.go
@internal/ecosys/loader.go
@internal/runtime/runtime.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/ecosys/memory.go` (NEW) — `MemoryFile{Path, Level, Content, Truncated bool}`, `DiscoverMemoryFiles(workDir string) []MemoryFile`, `MemoryInjection(workDir string) string`, package cache `memCache` (mutex-guarded map[path]{mtime, size, content}), constants `memoryPerFileCapBytes = 24 << 10`, `memoryTotalBudgetBytes = 64 << 10`.
- `internal/runtime/runtime.go` (MOD) — the memory merge block in `sessionFor` beside the AgentListing merge (:1072-1075), same double-append shape.
- `internal/runtime/memory_wiring_test.go` (NEW) — the sessionFor integration test.
</artifacts_this_phase_produce>

<flagged_assumptions>
- **PAR-04 edge-probe `adjacency` row (status: unresolved — surfaced, never auto-resolved):** when two memory files are exactly equal or just touch — same-directory AGENTS.md + CLAUDE.md, or identical content at different levels — the D-05 rule separates them (CLAUDE.md wins per directory, AGENTS.md skipped with a note) and levels accumulate WITHOUT content dedup (identical bodies at two levels inject twice, each under its own level header). The row stays flagged until verification confirms the collision test and the accumulate-no-dedup behavior.
- **PAR-04 edge-probe `empty` row (status: unresolved — surfaced, never auto-resolved):** zero files → no injection block; a single file → a single level; a directory entry that is unreadable (permissions) or a directory named AGENTS.md → skipped with a note, never an error. Flagged until the empty/zero table rows run green.
- **PAR-04 edge-probe `ordering` row (status: unresolved — surfaced, never auto-resolved):** equal-precedence levels have ONE pinned stable order — user global first, then repo root descending to cwd (deepest last) — so injection content is deterministic for a given tree. Flagged until the ordering test pins it.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Memory walker + mtime cache + capped injection body</name>
  <files>internal/ecosys/memory.go, internal/ecosys/memory_test.go</files>
  <behavior>
    - Walker (t.TempDir trees, HOME redirected via test env): levels collected from cwd UP to the directory containing .git (a .git FILE — worktree pointer — also terminates the walk); each level contributes CLAUDE.md if present, else AGENTS.md, else nothing; when both exist in one directory only CLAUDE.md is read and a skip-note is recorded for AGENTS.md (D-05).
    - No .git anywhere up to / → only the cwd level is considered (plus the user global) — the walk never reads above the discovered boundary (D-06; the deliberate narrowing vs CC's walk-to-$HOME, which is DEFERRED and must not be implemented).
    - User global: ~/.ass-guard/CLAUDE.md or ~/.ass-guard/AGENTS.md (CLAUDE.md preferred on same-dir collision per D-05, ~/.ass-guard preferred as the location per D-07); when ~/.ass-guard has neither, ~/.claude/CLAUDE.md is read; when ~/.ass-guard has one, ~/.claude/CLAUDE.md is NOT read (D-07 win-on-conflict).
    - Deterministic order (the flagged ordering row): user global first, then repo root → ... → cwd (deepest last), stable for a given tree.
    - Caps (D-08): a file over 24 KB truncates rune-safe (the truncateSkillDesc hard-cut + ellipsis idiom at file scale) and its entry carries a loud truncation note with original byte size; the TOTAL budget 64 KB — once exhausted, remaining files are skipped with per-file loud notes naming them.
    - MemoryInjection returns "" when no files were found (render-then-skip contract, SkillListing analog); otherwise the body carries a framing header, one entry per level (source path + content), and inline truncation/skip notes.
    - Cache: second DiscoverMemoryFiles call with unchanged files does not re-read (read-counting via a test hook or mtime-preserving fixtures asserts the cache hit); touching a file (new mtime) → content re-read; a file whose size changed but mtime is identical (touch -t trick or write+restore) is still re-read on the NEXT miss — the cache key is path+mtime+size (a pure optimization; a miss always re-reads, never serves stale within the keyed guarantee).
  </behavior>
  <read_first>internal/ecosys/skills.go (SkillListing :30-57 — the render-then-skip contract + header/entry shape; truncateSkillDesc :61-68 — the rune-safe truncation idiom), internal/ecosys/loader.go (loadTree walk discipline :141-178; readCapped :580-596), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (memory.go section — both analogs verbatim), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 3 — walker + cache + merge; D-05/D-06/D-07/D-08 decisions; A5 — the suggested cap values adopted here), .planning/phases/21-context-policy-parity-closures/21-CONTEXT.md (Deferred Ideas — walk-to-$HOME and import syntax are OUT OF SCOPE)</read_first>
  <action>
RED: write memory_test.go with the t.TempDir tree table: collision (both files in one dir), repo-root stop (a memory file ABOVE the .git boundary must not appear), no-git-repo fallback, user-global precedence (all four HOME combinations), caps (a >24KB fixture built in-test, a multi-file tree exceeding 64KB), empty tree, unreadable entry, and the cache hit/re-read counting cases. Commit failing.
GREEN: implement memory.go — MemoryFile + DiscoverMemoryFiles walking filepath.Dir ascent from workDir until a .git entry (os.Stat; the boundary dir ITSELF contributes) or filesystem root; per-level os.ReadFile via readCapped; the user-global pair per D-07; the pinned ordering; the mtime cache (sync.Mutex + map[path]memCacheEntry{modTime, size, content}, hit requires all three to match); MemoryInjection assembling the body with the framing header, per-level entries, and truncation/skip notes; constants memoryPerFileCapBytes/memoryTotalBudgetBytes as declared in artifacts. All failures (unreadable file, stat error) skip with a note — mirror the loader's degrade-softly discipline; nothing returns an error to the caller. Do NOT implement the deferred full-CC span (no walk above the git root), CLAUDE.local.md, or @file import syntax inside memory files (CONTEXT Deferred Ideas — out of scope).
  </action>
  <verify>
    <automated>go test -race ./internal/ecosys/ -run TestMemoryDiscovery -count=1 && go test ./internal/ecosys/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/ecosys/ -run TestMemoryDiscovery -count=1` exits 0 with every table case green.
    - The collision case asserts CLAUDE.md's content appears and AGENTS.md's does not, with exactly one skip-note; the repo-root-stop case asserts the above-root file's content appears nowhere in the injection body (D-06 pinned).
    - The user-global case asserts the ~/.ass-guard-wins combination table all four ways (D-07).
    - The caps case asserts the truncation note carries the original byte count and the total-budget case names every skipped file.
    - The cache case proves a second discovery with unchanged files performs zero file reads (counting reader or unchanged-mtime fixture) and a touched file is re-read (the flagged ordering/empty/adjacency rows are pinned by their table cases).
    - `go vet ./internal/ecosys/` clean.
  </acceptance_criteria>
  <done>The walker, cache, and capped injection body are table-pinned against every D-05/D-06/D-07/D-08 rule, degrading softly on every malformed or unreadable input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sessionFor wiring — the fourth trailing-TextBlock merge</name>
  <files>internal/runtime/runtime.go, internal/runtime/memory_wiring_test.go</files>
  <behavior>
    - Integration test (wiring level, mirroring the skills-listing wiring test shape): a session constructed in a temp project containing an AGENTS.md shows the memory body as the LAST system TextBlock of the session's profile copy — after cwd composition, skills listing, and agent listing.
    - Empty tree (no memory files anywhere incl. HOME) → prof.System length identical to a no-memory baseline (the skip contract end-to-end).
    - The shared r.profile is byte-identical after session construction with a memory body present (the double-append copy discipline — assert the shared profile's System slice was not appended to).
    - Truncation notes flow through: a >24KB memory file in the temp project produces the truncated body + note in the block (D-08 end-to-end).
  </behavior>
  <read_first>internal/runtime/runtime.go (sessionFor :976-1075 — the vehicle: the System copy at :1056, SkillListing merge :1056-1059, AgentListing merge :1072-1075; the hookRunner construction :1106 for orientation), internal/runtime/runner_battery_test.go or integration_test.go (the existing sessionFor-facing test setup to mirror — fake registry + temp project), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (sessionFor section — the verbatim double-append pattern to copy)</read_first>
  <action>
RED: write memory_wiring_test.go with the three cases above using the established sessionFor test harness (temp project + HOME redirection + a real Loader over the temp tree).
GREEN: add the memory merge in sessionFor immediately after the AgentListing block, copying the double-append shape verbatim: read ecosys.MemoryInjection(dir) once; when non-empty, append ONE profile.TextBlock{Type: blockText, Text: body} onto a fresh copy of prof.System. Zero new error paths (MemoryInjection cannot fail — it degrades internally per Task 1). Keep the merge AFTER AgentListing so memory is the closest-to-conversation static block, matching the pinned ordering.
  </action>
  <verify>
    <automated>go test -race ./internal/runtime/ -run TestMemoryInjection -count=1 && go test ./internal/runtime/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/runtime/ -run TestMemoryInjection -count=1` exits 0: memory block is last, empty tree adds nothing, shared profile unmutated (all three asserted).
    - `grep -c 'MemoryInjection' internal/runtime/runtime.go` is exactly 1 (one merge site — no second injection path).
    - `mise ci` green.
  </acceptance_criteria>
  <done>Criterion 3 holds at the wiring level: a fresh session in a memory-bearing repo carries the injected block automatically, mtime-cached and capped, with the shared profile untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-shipped AGENTS.md/CLAUDE.md → system context | repo-authored content enters every session's system prompt — injection is BY DESIGN (operator/project memory), bounded by caps and provenance |
| user HOME memory files → system context | cross-tool reading of ~/.claude/CLAUDE.md is read-only; ~/.ass-guard wins per D-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering | memory content steering the model (context-level injection) | medium | mitigate | content is capped (24 KB/file, 64 KB total), per-level source paths are carried in the block (auditable provenance), and truncation notes make cutting observable; tool authority is never derived from memory content |
| T-21-07 | DoS | giant memory files bloating every request | medium | mitigate | DecodeConfig-free byte caps enforced before injection; over-budget files skipped with loud notes (D-08) |
| T-21-08 | Information Disclosure | walking above the repo leaks unrelated context | low | mitigate | D-06's git-root stop enforced by test (above-root file provably absent); user-global reads limited to the two pinned locations |
| T-21-SC | Tampering | package installs | high | accept | No installs (stdlib only); package-legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/ecosys/ ./internal/runtime/ -count=1` green.
- `mise ci` green.
</verification>

<success_criteria>
PAR-04 is complete: both memory file names recognized with the CC-native collision rule, discovery bounded at the git root, both user-global locations honored with ~/.ass-guard precedence, caps enforced with loud notes, mtime-cached reads, and the injection riding the established trailing-TextBlock vehicle into every fresh session.
</success_criteria>

<output>
Create `.planning/phases/21-context-policy-parity-closures/21-02-SUMMARY.md` when done
</output>
