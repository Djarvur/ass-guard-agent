---
phase: 21-context-policy-parity-closures
plan: 04
type: execute
wave: 3
depends_on: ["21-03"]
files_modified:
  - internal/ecosys/mentions.go
  - internal/ecosys/mentions_test.go
  - internal/session/manager.go
  - internal/session/transcript.go
  - internal/runtime/runtime.go
  - internal/runtime/mention_expand_test.go
autonomous: true
requirements: [PAR-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An @file mention in the prompt expands to the file's content with a provenance transcript line, gated by the Read-rule evaluator (injected accessor; implicit allow until the 21-06 join wires the perm rule set) (PAR-06, D-10)"
    - "An @dir mention expands to a ONE-LEVEL directory listing (names + sizes, no recursion); an unresolvable @path produces ONE loud note and the turn proceeds (D-10)"
    - "Every @file consults the injected readRuleEvaluator; a denying evaluator replaces the mention with a loud denied note, no content section, and a provenance line recording the denial form — the gate is observable, not implied (PAR-06's Read-rule letter)"
    - "A prompt with zero mentions passes through byte-identical (expandUserBlocks' existing early-return shape preserved); multiple mentions expand in token order, each with its own section + provenance line"
  artifacts:
    - path: "internal/ecosys/mentions.go"
      provides: "ParseMentions(text string) []Mention — parse-only token extraction (the expand.go ParseInvocation split precedent); resolution/gating stays in runtime"
      contains: "func ParseMentions"
    - path: "internal/session/manager.go"
      provides: "AppendMentionProvenance(turnID, rawToken, resolvedPath, form string) error mirroring AppendCommandProvenance; TypeMentionProvenance = \"mention_provenance\" in transcript.go's additive-kind family"
      contains: "func (m *Manager) AppendMentionProvenance"
    - path: "internal/runtime/runtime.go"
      provides: "expandUserBlocks mention expansion + the readRuleEvaluator injected accessor (implicit-allow default + seam comment naming the 17 contract the 21-06 join wires)"
      contains: "readRuleEvaluator"
  key_links:
    - from: "internal/ecosys/mentions.go ParseMentions"
      to: "internal/runtime/runtime.go expandUserBlocks"
      via: "the same seam slash-commands ride (:395, invoked :540 pre-turn): parse-only in ecosys, resolution + Read-rule consult + provenance in runtime (the expand.go split precedent)"
      pattern: "ParseMentions"
  prohibitions:
    - requirement_id: PAR-06
      category: privacy
      statement: "Ingress resolution MUST NOT become a filesystem existence oracle: @-paths resolve only within the repo root or declared absolute paths the Read rules already admit, an unresolvable path yields only the fixed loud note (no per-path detail beyond existence class), and mention notes never carry file content — the note family names the outcome, not the content"
      status: unresolved
---

<objective>
Land the @-mention leg of PAR-06 rich prompt content: parse-only mention extraction in internal/ecosys, runtime expansion (file content sections, one-level dir listings, loud unresolvable notes) gated by the injected Read-rule evaluator, with a provenance transcript line per mention.

Purpose: D-10 — @ expansion produces the corresponding prompt content Read-rule-gated with provenance, riding the exact seam slash-commands already use. The sibling image leg (auto-downscale, provider capability) is 21-05; the perm rule-set wiring is the 21-06 join.
Output: mentions.go parser + expandUserBlocks extension + AppendMentionProvenance + TypeMentionProvenance, green under -race with the parse and expansion batteries.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-context-policy-parity-closures/21-CONTEXT.md
@.planning/phases/21-context-policy-parity-closures/21-RESEARCH.md
@.planning/phases/21-context-policy-parity-closures/21-PATTERNS.md
@internal/ecosys/expand.go
@internal/runtime/runtime.go
@internal/session/transcript.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/ecosys/mentions.go` (NEW) — `Mention{Token, Path string, IsDir bool}`, `ParseMentions(text string) []Mention` (whitespace-delimited @tokens; quoted paths with spaces supported).
- `internal/session/transcript.go` (MOD) — `TypeMentionProvenance = "mention_provenance"` in the additive-kind family (D-10 provenance duty).
- `internal/session/manager.go` (MOD) — `AppendMentionProvenance(turnID, rawToken, resolvedPath, form string) error` mirroring AppendCommandProvenance (redacted path, same append semantics).
- `internal/runtime/runtime.go` (MOD) — the mention branch inside `expandUserBlocks` + the `readRuleEvaluator` unexported Runner field (nil = implicit allow; seam comment names the internal/perm rule-set contract and the 21-06 join).
- `internal/runtime/mention_expand_test.go` (NEW) — the expansion battery.
</artifacts_this_phase_produce>

<flagged_assumptions>
- **@-parse mechanics (CONTEXT discretion):** whitespace-delimited token-initial @, quoted paths with spaces, trailing punctuation excluded, email-like mid-word @ never a mention — pinned by the Task 1 table so the parse contract is deterministic before expansion builds on it.
- **Evaluator default (PAR-06 letter vs Phase-17 timing):** the injected accessor defaults to implicit allow so this plan is 17-independent; the denying-evaluator test proves the gate bites the moment 21-06 wires the perm rule set. The seam comment records both halves of that contract.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ParseMentions — parse-only token extraction in ecosys</name>
  <files>internal/ecosys/mentions.go, internal/ecosys/mentions_test.go</files>
  <behavior>
    - ParseMentions table (parse-only, the expand.go ParseInvocation split): whitespace-delimited @tokens extracted in order; @path/to/file.md parses with IsDir unknown at parse time; quoted @"path with spaces"/file or @'path with spaces' parses as one token; trailing punctuation (. , :) does not join the path; @ mid-word (email-like foo@bar) does NOT parse as a mention (only token-initial @).
    - Multiple tokens in one text parse in order; zero mentions → empty slice (no error).
  </behavior>
  <read_first>internal/ecosys/expand.go (ParseInvocation :24-33 — the parse/registry split precedent), internal/runtime/runtime.go (expandUserBlocks :395-449 — the firstTextBlockIndex scope the tokenizer must match), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (expandUserBlocks section), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 5 step 2; @-parse discretion), .planning/phases/21-context-policy-parity-closures/21-CONTEXT.md (D-10; Deferred Ideas — no import syntax)</read_first>
  <action>
RED: write mentions_test.go with the parse table above (ordered extraction, quoted-space paths, trailing punctuation, mid-word @ exclusion, empty input). Commit failing.
GREEN: implement mentions.go — Mention + ParseMentions per the parse table: a tokenizer over the text content matching expandUserBlocks' firstTextBlockIndex scope, token-initial @ only, quote-aware path capture, trailing-punctuation trim. No resolution, no I/O, no errors — parse-only (the expand.go split precedent; resolution/gating belongs to the runtime layer in Task 2).
  </action>
  <verify>
    <automated>go test -race ./internal/ecosys/ -run TestParseMentions -count=1 && go test ./internal/ecosys/ -count=1 && go vet ./internal/ecosys/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/ecosys/ -run TestParseMentions -count=1` exits 0 with every table case green.
    - The mid-word case asserts foo@bar yields zero mentions; the quoted case asserts one token with the full spaced path.
    - `go vet ./internal/ecosys/` clean.
  </acceptance_criteria>
  <done>Mention extraction is a pure, table-pinned function in ecosys — no I/O, no resolution — ready for the runtime layer to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime expansion — Read-rule gate, sections, per-mention provenance</name>
  <files>internal/session/transcript.go, internal/session/manager.go, internal/runtime/runtime.go, internal/runtime/mention_expand_test.go</files>
  <behavior>
    - Runtime expansion end-to-end (TestMentionExpand family): @file resolving to an existing file under the repo → the message text gains a labeled section (path header + content) and an AppendMentionProvenance line lands (TypeMentionProvenance: raw token, resolved path, form=file) mirroring the AppendCommandProvenance discipline.
    - @dir resolving to a directory → a one-level listing section (one entry per line: name + size in bytes; subdirectories marked with a trailing slash; NO recursion — pinned by a fixture tree two levels deep asserting the child dir's FILES never appear).
    - Read-rule gating: every @file consults the injected readRuleEvaluator(tool="Read", path) — with the default (no evaluator wired) the consult allows (current behavior preserved; a seam comment names the perm rule-set contract the 21-06 join wires). A test injects a denying evaluator and asserts the mention is replaced by a loud denied note, no content section, and a provenance line recording the denial form.
    - Unresolvable @path (no such file/dir, outside the admitted roots) → the message gains ONE loud note naming the token and the outcome class; the turn proceeds (D-10) — the note text is fixed-form (no per-path filesystem detail beyond the existence class, the flagged privacy prohibition).
    - Multiple mentions in one prompt expand in token order, each with its own section + provenance line; a prompt with zero mentions passes through byte-identical (expandUserBlocks' existing early-return shape preserved).
    - Append fails (transcript write error) → loud stderr + continue (the :426-434 provenance-failure discipline).
  </behavior>
  <read_first>internal/runtime/runtime.go (expandUserBlocks :395-449 — the seam, copy-on-write, firstTextBlockIndex; the provenance call + failure log :426-434; the invocation site :540), internal/session/manager.go (AppendCommandProvenance — the provenance method to mirror; appendLine discipline), internal/session/transcript.go (TypeCommandProvenance :40; additive-kind family :144-155), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (expandUserBlocks section — both analogs verbatim), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 5 step 2; D-10; the Unresolvable-path threat row), .planning/phases/21-context-policy-parity-closures/21-CONTEXT.md (D-10 verbatim)</read_first>
  <action>
RED: write mention_expand_test.go with the expansion families over a t.TempDir repo tree (a two-level fixture, a spaced-name file, a denying injected evaluator, an unresolvable token, a multi-mention prompt, a zero-mention control). Commit failing.
GREEN: (1) transcript.go — TypeMentionProvenance const in the additive-kind family with a doc comment naming the D-10 provenance duty. (2) manager.go — AppendMentionProvenance mirroring AppendCommandProvenance's shape (redacted path, same append semantics). (3) runtime.go — extend expandUserBlocks AFTER the existing command-invocation logic (zero behavior change when no @ token is present): iterate ParseMentions (Task 1), resolve each against the workDir (bounded to the repo root + absolute paths the evaluator admits), consult r.readRuleEvaluator (new unexported Runner field, nil = implicit allow, doc comment naming the internal/perm rule-set contract and the 21-06 join), build the labeled sections into the first text block on a copied slice, and append provenance per mention with the loud-continue failure discipline. Resolution failure classes (missing, denied, decode) each produce the fixed-form note — no path content in notes beyond the token itself.
  </action>
  <verify>
    <automated>go test -race ./internal/runtime/ -run TestMentionExpand -count=1 && go test ./internal/session/ ./internal/runtime/ -count=1 && go vet ./internal/runtime/ ./internal/session/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/runtime/ -run TestMentionExpand -count=1` exits 0 with every family green.
    - The @dir case asserts the two-level fixture's nested files NEVER appear (no recursion, D-10 pinned).
    - The denied-evaluator case asserts no content section, a loud note, and a provenance line with the denial form (the gating is observable, not implied).
    - The zero-mention case asserts the returned blocks equal the inputs (existing seam behavior byte-identical).
    - `grep -c 'TypeMentionProvenance' internal/session/transcript.go` is >= 1 (the additive kind is declared beside the 16-02 family).
    - `mise ci` green.
  </acceptance_criteria>
  <done>@-mentions expand with Read-rule gating, one-level dir listings, loud unresolvable notes, and per-mention provenance — riding the exact seam slash-commands already use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user prompt (ACP client) → @-expansion | @-paths are untrusted input — resolution bounds and fixed-form notes contain them |
| repo filesystem → @-expansion | @-paths could probe or exfiltrate outside admitted roots |
| @file content → prompt | file content enters the model's context — Read-rule authority must bite before it does |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-14 | Information Disclosure | @-path filesystem probing | medium | mitigate | resolution bounded to repo root + evaluator-admitted absolute paths; fixed-form notes leak only the existence class (the flagged privacy prohibition, pinned by the unresolvable-path test) |
| T-21-15 | Elevation of Privilege | @file bypassing Read-tool authority | high | mitigate | every @file consults the injected readRuleEvaluator before content enters the prompt; the denying-evaluator test proves the gate bites; the 21-06 join wires the perm rule set (PAR-06's Read-rule letter) |
| T-21-SC | Tampering | package installs | high | accept | No installs (stdlib only in this plan); package-legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/ecosys/ ./internal/runtime/ ./internal/session/ -count=1` green.
- `mise ci` green.
</verification>

<success_criteria>
The @-mention leg of PAR-06 holds: parse-only extraction pinned in ecosys, runtime expansion gated by the injected Read-rule evaluator (denial observable end-to-end), one-level dir listings without recursion, fixed-form unresolvable notes, and a provenance transcript line per mention — with zero-mention prompts provably untouched. The image leg continues in 21-05; the perm rule-set wiring lands at the 21-06 join.
</success_criteria>

<output>
Create `.planning/phases/21-context-policy-parity-closures/21-04-SUMMARY.md` when done
</output>
