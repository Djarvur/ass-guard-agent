---
phase: 21-context-policy-parity-closures
plan: 04
type: execute
wave: 3
depends_on: ["21-03"]
files_modified:
  - internal/ecosys/mentions.go
  - internal/ecosys/mentions_test.go
  - internal/session/manager.go
  - internal/session/transcript.go
  - internal/acp/types.go
  - internal/provider/provider.go
  - internal/provider/anthropic.go
  - internal/provider/openai.go
  - internal/shaper/shaper.go
  - internal/shaper/shaper_test.go
  - internal/runtime/runtime.go
  - internal/runtime/mention_expand_test.go
  - internal/runtime/imgscale.go
  - internal/runtime/imgscale_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [PAR-06]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An @file mention in the prompt expands to the file's content with a provenance transcript line, gated by the Read-rule evaluator (injected accessor; implicit allow until the 21-05 join wires the perm rule set) (PAR-06, D-10)"
    - "An @dir mention expands to a ONE-LEVEL directory listing (names + sizes, no recursion); an unresolvable @path produces ONE loud note and the turn proceeds (D-10)"
    - "An over-limit image AUTO-DOWNSCALES to fit provider limits using pure-Go x/image/draw scaling — CGO_ENABLED=0 holds; original bytes are preserved on disk and the transcript records the resize provenance (original dims/size → scaled) (D-09)"
    - "A provider without image support drops the image block with exactly ONE loud degrade note naming the provider; the turn proceeds with the text — never silent, never a dead turn (D-11)"
    - "Image ingress validates BEFORE pixel decode (image.DecodeConfig header-only) — dimensions, byte size, and media type are checked first so a pixel bomb cannot allocate (Pitfall 6)"
    - "The transcript user_message line carries image metadata + Ref (09-05 discipline: metadata-in-line, bytes-on-disk) — no base64 blobs in the transcript; text-only ContentBlocks serialize byte-identically (additive omitempty fields)"
  artifacts:
    - path: "internal/ecosys/mentions.go"
      provides: "ParseMentions(text string) []Mention — parse-only token extraction (the expand.go ParseInvocation split precedent); resolution/gating stays in runtime"
      contains: "func ParseMentions"
    - path: "internal/runtime/imgscale.go"
      provides: "the pure-Go scaler: ValidateAndScaleImage(data []byte, mediaType string, limits ImageLimits) (scaled []byte, outMediaType string, w, h int, origW, origH int, scaled bool, err error) — DecodeConfig-first, provider-limit validation, draw.CatmullRom downscale only when over"
      contains: "func ValidateAndScaleImage"
    - path: "internal/provider/provider.go"
      provides: "SupportsImages() bool on the Provider interface — only the adapter knows its protocol's image support (anthropic: true, openai-shape: false today)"
      contains: "SupportsImages"
    - path: "internal/shaper/shaper.go"
      provides: "image block mapping in toMessageParams — anthropic.ImageBlockParam + Base64ImageSourceParam from the ContentBlock Ref (file read at shape time)"
      contains: "ImageBlockParam"
  key_links:
    - from: "internal/ecosys/mentions.go ParseMentions"
      to: "internal/runtime/runtime.go expandUserBlocks"
      via: "the same seam slash-commands ride (:395, invoked :540 pre-turn): parse-only in ecosys, resolution + Read-rule consult + provenance in runtime (the expand.go split precedent)"
      pattern: "ParseMentions"
    - from: "internal/runtime toContentBlocks"
      to: "internal/runtime imgscale ingress"
      via: "acp image content blocks (:55-62, additive fields) map through toContentBlocks (:1632) into the ingress pipeline BEFORE transcript append (the Responsibility Map's ingress tier)"
      pattern: "ingress"
    - from: "internal/shaper/shaper.go"
      to: "internal/provider SupportsImages"
      via: "D-11: the shaper/turn path consults the active provider's SupportsImages; false → drop the image block + one loud note naming the provider"
      pattern: "SupportsImages"
  prohibitions:
    - requirement_id: PAR-06
      category: privacy
      statement: "Ingress resolution MUST NOT become a filesystem existence oracle: @-paths resolve only within the repo root or declared absolute paths the Read rules already admit, an unresolvable path yields only the fixed loud note (no per-path detail beyond existence class), and image bytes are never logged — the note family names the outcome, not the content"
      status: unresolved
---

<objective>
Land PAR-06 rich prompt content: @-mention expansion (file content, one-level dir listings, loud unresolvable notes) and base64 image ingress (DecodeConfig-first validation, pure-Go auto-downscale per D-09, originals preserved on disk, Ref-based transcript lines) with per-provider capability validation and D-11 loud degradation.

Purpose: criterion 5 — pasting an image reference or @-mention produces the corresponding content block in the outgoing request, Read-rule gated with provenance, validated per provider shape. The only new dependency of the phase lands here (golang.org/x/image, D-09's build-contract decision).
Output: mentions.go + imgscale.go + ContentBlock image variant + provider capability + shaper mapping, green under -race with `mise ci` (CGO_ENABLED=0) proving the pure-Go constraint.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-context-policy-parity-closures/21-CONTEXT.md
@.planning/phases/21-context-policy-parity-closures/21-RESEARCH.md
@.planning/phases/21-context-policy-parity-closures/21-PATTERNS.md
@internal/ecosys/expand.go
@internal/runtime/runtime.go
@internal/session/transcript.go
@internal/shaper/shaper.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/ecosys/mentions.go` (NEW) — `Mention{Token, Path string, IsDir bool}`, `ParseMentions(text string) []Mention` (whitespace-delimited @tokens; quoted paths with spaces supported).
- `internal/session/manager.go` (MOD) — `AppendMentionProvenance(turnID, rawToken, resolvedPath, form string) error` mirroring AppendCommandProvenance; `TypeMentionProvenance = "mention_provenance"` in transcript.go's additive-kind family.
- `internal/session/transcript.go` (MOD) — ContentBlock additive omitempty fields: `DataRef, MediaType string`, `Width, Height, OrigWidth, OrigHeight int`, `OrigSize int64`, `Scaled bool` (image variant; text-only serialization byte-identical).
- `internal/acp/types.go` (MOD) — ContentBlock additive `Data, MimeType, URI string` fields (ACP v1 image block shape, omitempty).
- `internal/runtime/imgscale.go` (NEW) — `ImageLimits{MaxW, MaxH int, MaxBytes int64, TargetLongEdge int}`, `ValidateAndScaleImage(...)`, `LoadImageBytes(ref string) ([]byte, error)` (the shaper's Ref reader).
- `internal/runtime/runtime.go` (MOD) — `expandUserBlocks` mention expansion + the `readRuleEvaluator` injected accessor (implicit-allow default + seam comment naming the 17 contract); the image ingress pipeline at the turn entry; toContentBlocks image mapping.
- `internal/provider/provider.go` (MOD) — `SupportsImages() bool` on the Provider interface; anthropic.go returns true; openai.go returns false.
- `internal/shaper/shaper.go` (MOD) — image ContentBlock → anthropic.ImageBlockParam{Source: Base64ImageSourceParam{Data, MediaType}} mapping.
- `go.mod`/`go.sum` — `golang.org/x/image v0.45.0` (registry-verified, RESEARCH Package Legitimacy Audit: OK, pure Go).
</artifacts_this_phase_produce>

<flagged_assumptions>
- **PAR-06 edge-probe `concurrency` row (status: unresolved — surfaced, never auto-resolved):** ingress runs synchronously within turn entry (never concurrently with the turn's own provider calls); a cancellation between downscale and transcript append leaves the original on disk and the next identical ingress re-derives idempotently (sha-keyed file writes use temp+rename so two sessions ingressing the same image converge on identical bytes). Flagged until the idempotent re-ingress test runs green.
- **D-09 mechanics (CONTEXT discretion):** provider hard limits pinned as Anthropic's documented classes — 8000×8000 max dims, 10 MB per-image byte cap — with the downscale target set to a 1568 px long edge (the documented recommendation) and JPEG re-encode at quality 85 when re-encoding is required; media types accepted: jpeg, png, gif (stdlib) + webp (x/image). A format that cannot be decoded is dropped with a loud note (the D-10 note family), never a dead turn.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: @-mention expansion — parse in ecosys, resolve + gate + provenance in runtime</name>
  <files>internal/ecosys/mentions.go, internal/ecosys/mentions_test.go, internal/session/manager.go, internal/session/transcript.go, internal/runtime/runtime.go, internal/runtime/mention_expand_test.go</files>
  <behavior>
    - ParseMentions table (parse-only, the expand.go ParseInvocation split): whitespace-delimited @tokens extracted in order; @path/to/file.md parses with IsDir unknown at parse time; quoted @"path with spaces"/file or @'path with spaces' parses as one token; trailing punctuation (. , :) does not join the path; @ mid-word (email-like foo@bar) does NOT parse as a mention (only token-initial @).
    - Runtime expansion end-to-end (TestMentionExpand family): @file resolving to an existing file under the repo → the message text gains a labeled section (path header + content) and an AppendMentionProvenance line lands (TypeMentionProvenance: raw token, resolved path, form=file) mirroring the AppendCommandProvenance discipline.
    - @dir resolving to a directory → a one-level listing section (one entry per line: name + size in bytes; subdirectories marked with a trailing slash; NO recursion — pinned by a fixture tree two levels deep asserting the child dir's FILES never appear).
    - Read-rule gating: every @file consults the injected readRuleEvaluator(tool="Read", path) — with the default (no evaluator wired) the consult allows (current behavior preserved; a seam comment names the perm rule-set contract the 21-05 join wires). A test injects a denying evaluator and asserts the mention is replaced by a loud denied note, no content section, and a provenance line recording the denial form.
    - Unresolvable @path (no such file/dir, outside the admitted roots) → the message gains ONE loud note naming the token and the outcome class; the turn proceeds (D-10) — the note text is fixed-form (no per-path filesystem detail beyond the existence class, the flagged privacy prohibition).
    - Multiple mentions in one prompt expand in token order, each with its own section + provenance line; a prompt with zero mentions passes through byte-identical (expandUserBlocks' existing early-return shape preserved).
    - Append fails (transcript write error) → loud stderr + continue (the :426-434 provenance-failure discipline).
  </behavior>
  <read_first>internal/runtime/runtime.go (expandUserBlocks :395-449 — the seam, copy-on-write, firstTextBlockIndex; the provenance call + failure log :426-434; the invocation site :540), internal/ecosys/expand.go (ParseInvocation :24-33 — the parse/registry split precedent), internal/session/manager.go (AppendCommandProvenance — the provenance method to mirror; appendLine discipline), internal/session/transcript.go (TypeCommandProvenance :40; additive-kind family :144-155), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (expandUserBlocks section — both analogs verbatim), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 5 step 2; D-10; the Unresolvable-path threat row; @-parse discretion), .planning/phases/21-context-policy-parity-closures/21-CONTEXT.md (D-10 verbatim; Deferred Ideas — no import syntax)</read_first>
  <action>
RED: mentions_test.go (parse table) + mention_expand_test.go (expansion families over a t.TempDir repo tree with a two-level fixture, a spaced-name file, a denying injected evaluator, and an unresolvable token). Commit failing.
GREEN: (1) mentions.go — Mention + ParseMentions per the parse table (tokenizer over the first text block's content only, matching expandUserBlocks' firstTextBlockIndex scope). (2) transcript.go — TypeMentionProvenance const in the additive-kind family with a doc comment naming the D-10 provenance duty. (3) manager.go — AppendMentionProvenance mirroring AppendCommandProvenance's shape (redacted path, same append semantics). (4) runtime.go — extend expandUserBlocks AFTER the existing command-invocation logic (zero behavior change when no @ token is present): iterate ParseMentions, resolve each against the workDir (bounded to the repo root + absolute paths the evaluator admits), consult r.readRuleEvaluator (new unexported Runner field, nil = implicit allow, doc comment naming the internal/perm rule-set contract and the 21-05 join), build the labeled sections into the first text block on a copied slice, and append provenance per mention with the loud-continue failure discipline. Resolution failure classes (missing, denied, decode) each produce the fixed-form note — no path content in notes beyond the token itself.
  </action>
  <verify>
    <automated>go test -race ./internal/ecosys/ -run TestParseMentions -count=1 && go test -race ./internal/runtime/ -run TestMentionExpand -count=1 && go test ./internal/session/ ./internal/ecosys/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/ecosys/ -run TestParseMentions -count=1` and `go test -race ./internal/runtime/ -run TestMentionExpand -count=1` both exit 0 with every table case green.
    - The @dir case asserts the two-level fixture's nested files NEVER appear (no recursion, D-10 pinned).
    - The denied-evaluator case asserts no content section, a loud note, and a provenance line with the denial form (the gating is observable, not implied).
    - The zero-mention case asserts the returned blocks equal the inputs (existing seam behavior byte-identical).
    - `grep -c 'TypeMentionProvenance' internal/session/transcript.go` is >= 1 (the additive kind is declared beside the 16-02 family).
    - `go vet ./internal/ecosys/ ./internal/runtime/` clean.
  </acceptance_criteria>
  <done>@-mentions expand with Read-rule gating, one-level dir listings, loud unresolvable notes, and per-mention provenance — riding the exact seam slash-commands already use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Image ingress — DecodeConfig guard, pure-Go downscale, Ref-based transcript lines</name>
  <files>go.mod, go.sum, internal/runtime/imgscale.go, internal/runtime/imgscale_test.go, internal/acp/types.go, internal/session/transcript.go, internal/runtime/runtime.go</files>
  <reversibility rating="costly">D-09 makes golang.org/x/image part of the build contract — removing it later means re-deciding the oversize policy (CONTEXT carries this rating); the go get lands here once and stays.</reversibility>
  <behavior>
    - ValidateAndScaleImage table (images synthesized in-test — no committed binary fixtures): a within-limits 800×600 JPEG passes through with scaled=false and identical bytes; an over-dims 9000×6000 synthetic JPEG (generated via image/jpeg encode in-test) downscale-fits the 1568 long edge with scaled=true, provenance origW/origH/origSize recorded, output decodable; an over-bytes case (a high-detail synthetic exceeding the byte cap) downscales until within the cap; a PNG within limits passes through unmodified (format preserved).
    - Pixel bomb: a hand-crafted PNG whose IHDR declares 40000×40000 but is a tiny file — DecodeConfig reports the dims and the validator REFUSES (dims over hard limits → loud error before any pixel allocation; a test asserts no large allocation occurs by checking the refusal precedes decode).
    - Undecodable bytes (random data, unsupported format) → typed error, loud-note class (never a panic).
    - GIF and WebP decode paths accept their formats (stdlib gif; x/image webp registered via image.RegisterFormat side-effect import or explicit decoder).
    - Ingress wiring (runtime): an acp prompt image block (types.go additive Data/MimeType fields, the ACP v1 image shape) maps through toContentBlocks into the session ContentBlock image variant (transcript.go additive omitempty fields); ingress runs at the turn entry BEFORE the transcript append — original bytes written to <session .ass-guard dir>/images/<sha256>.<ext>, scaled bytes to <sha256>.<w>x<h>.<ext> (temp+rename atomic writes), and the ContentBlock that reaches the transcript carries Ref=the scaled path + metadata + resize provenance, NEVER the base64 (the 09-05 metadata-in-line discipline — a test asserts the appended user_message line contains no base64 payload).
    - Idempotent re-ingress (the flagged concurrency row): the same image ingested twice (fresh session state) converges on identical Ref paths and bytes — second write is a no-op rename onto identical content.
    - Text-only ContentBlocks serialize byte-identically after the field additions (a transcript round-trip test on a pre-change-shaped line).
  </behavior>
  <read_first>.planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 5 steps 1+3 — the DecodeConfig-first discipline and the draw scaler sketch; Pitfall 6 — the pixel-bomb law; Pitfall 7 — the CGO gate; Open Question 3 resolution — Ref discipline; the vision limits), internal/acp/types.go (ContentBlock :55-62 — the forward-compatible shape the image fields join), internal/session/transcript.go (ContentBlock :79-84; the additive-kind comment block), internal/runtime/runtime.go (toContentBlocks :1632-1641; the turn-entry seam where expandUserBlocks is invoked :540), .mise.toml ([tasks.build] CGO_ENABLED=0 — the gate the dependency must survive), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (transcript.go + image scaler no-analog rows)</read_first>
  <action>
Dependency first: go get golang.org/x/image@v0.45.0 (the RESEARCH Package Legitimacy Audit's approved row) and verify CGO_ENABLED=0 go build ./... still passes before anything else (Pitfall 7 — a cgo requirement fails the gate loudly; x/image is pure Go).
RED: imgscale_test.go with the six families (synthesized in-test: image/jpeg + image/png encoders for fixtures; the pixel-bomb PNG built by writing a minimal IHDR-declaring chunk set). Commit failing.
GREEN: (1) imgscale.go — ImageLimits (MaxW/MaxH 8000, MaxBytes 10 MB, TargetLongEdge 1568 per the pinned discretion), ValidateAndScaleImage decoding config FIRST (image.DecodeConfig; register webp via the x/image/webp import), refusing over-limit dims before any pixel decode, decoding pixels only when scaling is needed, scaling via draw.CatmullRom into an RGBA dst, re-encoding (JPEG quality 85 when the source was JPEG or when re-encode is required; PNG preserved when within limits), and returning the provenance tuple; LoadImageBytes reading a Ref back for the shaper. (2) acp/types.go — Data/MimeType/URI omitempty fields on the ACP ContentBlock (additive; the doc comment's forward-compatibility note realized). (3) transcript.go — the image variant fields on ContentBlock (all omitempty). (4) runtime.go — toContentBlocks maps image blocks through; the turn-entry ingress (beside the expandUserBlocks invocation) validates/scales each image block, persists original+scaled under the session .ass-guard images dir (sha-keyed, temp+rename), and rewrites the block to the Ref+metadata form. All ingress failures (decode, limits) produce ONE loud stderr note and drop the block — the turn proceeds (the D-10 note family; D-11's provider-shape drop is Task 3).
  </action>
  <verify>
    <automated>go test -race ./internal/runtime/ -run 'TestImageIngress|TestValidateAndScale' -count=1 && CGO_ENABLED=0 go build ./... && go test ./internal/acp/ ./internal/session/ -run 'TestContentBlock|TestToContentBlocks' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/runtime/ -run 'TestImageIngress|TestValidateAndScale' -count=1` exits 0 with all six families green.
    - The pixel-bomb case asserts refusal happens at the DecodeConfig stage (the test's refusal-path assertion fires before any decode call — observable via the validator's stage reporting or a decode-counting hook).
    - The transcript-line case asserts the appended user_message contains Ref + provenance and NO base64 payload (grep the marshaled line for a data field absence).
    - `CGO_ENABLED=0 go build ./...` exits 0 (the D-09 build-contract gate) and `go list -m golang.org/x/image` reports v0.45.0.
    - The text-only round-trip case asserts pre-change-shaped ContentBlock lines marshal byte-identically (omitempty discipline).
    - `mise ci` green.
  </acceptance_criteria>
  <done>Images enter validated and scaled (auto-downscale, pure Go), originals preserved on disk, transcript lines lean (Ref + provenance) — with the pixel-bomb guard proven before decode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Provider capability + shaper image mapping — D-11 loud degradation</name>
  <files>internal/provider/provider.go, internal/provider/anthropic.go, internal/provider/openai.go, internal/shaper/shaper.go, internal/shaper/shaper_test.go</files>
  <behavior>
    - SupportsImages: the Provider interface gains the method; the Anthropic adapter returns true; the OpenAI-shape adapter returns false (text-only today — RESEARCH-verified: no MultiContent/image path in openai.go); the conformance harness's fake providers updated (a compile-level guarantee no adapter is missed).
    - Shaper mapping (TestImageCapability family, shaper_test.go): a Message carrying an image ContentBlock with Ref renders an anthropic.ImageBlockParam whose Source is Base64ImageSourceParam{Data: <base64 of the Ref'd file>, Media: the block's MediaType} — the file is read at shape time via LoadImageBytes; dims/provenance fields do NOT reach the wire (the SDK param carries only source).
    - Order preservation: an image block between two text blocks renders in position (content-block order is parity-relevant).
    - D-11 end-to-end: a turn against a provider whose SupportsImages() is false with an image block in the prompt → the image block is dropped from the outgoing request, exactly ONE loud structured note naming the provider lands (stderr note family), the text proceeds, and the turn completes normally — never silent, never a dead turn. A test with the OpenAI-shape provider + a fake backend asserts the request body contains no image content and the note fired once.
    - A missing Ref file at shape time (disk loss between ingress and shape) → loud error-class note + the block dropped, turn proceeds (degrade-softly, never a 400-spamming empty block).
  </behavior>
  <read_first>internal/provider/provider.go (the Provider interface — where SupportsImages joins; StreamChunk region for orientation), internal/provider/openai.go (the text-only Send path — RESEARCH-verified no image support), internal/provider/anthropic.go (the Stream path — the image-capable adapter), internal/provider/conformance_test.go (the fake-provider harness the new method must compile against), internal/shaper/shaper.go (toMessageParams :165-226 — the content-block union assembly; the user-message branch), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 5 step 4 + the ImageBlockParam/Base64ImageSourceParam quotes; D-11; the Alternatives row blessing drop+note over go-openai MultiContent), internal/runtime/imgscale.go (Task 2's LoadImageBytes — the Ref reader)</read_first>
  <action>
RED: extend shaper_test.go with the mapping/order/degrade families (fake providers stubbing SupportsImages; image fixtures synthesized in-test and Ref'd through temp files). Commit failing.
GREEN: (1) provider.go — SupportsImages() bool on the Provider interface with a doc comment stating the D-11 contract; anthropic.go true; openai.go false with a comment naming the MultiContent future as explicitly out of PAR-06's letter; update the conformance fakes (compile-level completeness). (2) shaper.go — in the user-message branch, image blocks map via LoadImageBytes(Ref) → anthropic.NewImageBlockParam-equivalent construction with Base64ImageSourceParam (SDK param structs only — never hand-marshaled JSON, RESEARCH Don't-Hand-Roll); the D-11 consult happens at the turn path (runtime already knows the provider) OR the shaper receives a capability flag — pick the seam the existing code shape favors (the turn path consults provider.SupportsImages and strips+notes BEFORE shaping, keeping the shaper pure) and document the choice in the SUMMARY. Missing-Ref at shape time degrades loudly and drops the block.
  </action>
  <verify>
    <automated>go test -race ./internal/shaper/ -run TestImageCapability -count=1 && go test ./internal/provider/ -count=1 && go test -race ./internal/shaper/ ./internal/provider/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/shaper/ -run TestImageCapability -count=1` exits 0: Ref→base64 mapping, order preservation, D-11 drop+single-note on the false-capability provider, missing-Ref degrade — all asserted.
    - `go build ./...` fails to compile if any Provider implementation omits SupportsImages (interface-level completeness, verified by the conformance fakes compiling).
    - The D-11 case asserts exactly ONE note (a counting sink) naming the provider — never zero, never two (D-11's letter).
    - `mise ci` green.
  </acceptance_criteria>
  <done>Rich content is provider-aware end-to-end: Anthropic carries image blocks in order and shape; unsupported providers degrade with exactly one loud note and the turn lives on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user prompt (ACP client) → ingress | image bytes and @-paths are untrusted input — decode guards, limits, and path resolution bound them |
| repo filesystem → @-expansion | @-paths could probe or exfiltrate outside admitted roots |
| image bytes → provider request | media type and size claims are untrusted; validation happens before any wire send |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-13 | DoS | pixel bomb / decompression blowup | high | mitigate | image.DecodeConfig BEFORE decode; hard dims refusal (8000×8000) pre-allocation; byte cap 10 MB; the bomb fixture test proves refusal precedes decode (Pitfall 6) |
| T-21-14 | Information Disclosure | @-path filesystem probing | medium | mitigate | resolution bounded to repo root + evaluator-admitted absolute paths; fixed-form notes leak only the existence class (the flagged privacy prohibition, pinned by the unresolvable-path test) |
| T-21-15 | Elevation of Privilege | @file bypassing Read-tool authority | high | mitigate | every @file consults the injected readRuleEvaluator before content enters the prompt; the denying-evaluator test proves the gate bites; the 21-05 join wires the perm rule set (PAR-06's Read-rule letter) |
| T-21-16 | Tampering | media-type spoofing (bytes ≠ declared type) | medium | mitigate | format detection via decode (magic bytes), not the declared mimeType; mismatch → the decoded format wins or the block is dropped with a note |
| T-21-17 | DoS | transcript bloat via base64 images | low | mitigate | Ref+metadata lines only (09-05 discipline); a test asserts no base64 payload reaches the transcript |
| T-21-SC | Tampering | golang.org/x/image install | high | mitigate | RESEARCH Package Legitimacy Audit: official golang.org/x governance, registry-verified v0.45.0, pure Go under the CGO_ENABLED=0 gate; go get lands in Task 2 with the build gate asserted in acceptance criteria |
</threat_model>

<verification>
- `go test -race ./internal/ecosys/ ./internal/runtime/ ./internal/session/ ./internal/shaper/ ./internal/provider/ ./internal/acp/ -count=1` green.
- `CGO_ENABLED=0 go build ./...` green (the D-09 gate).
- `mise ci` green.
</verification>

<success_criteria>
PAR-06 holds: @-mentions expand gated and provenanced with one-level dir listings and loud unresolvable notes; images ingress with pre-decode validation, pure-Go auto-downscale, preserved originals, and lean Ref-based transcript lines; provider capability is declared per adapter and unsupported providers degrade with exactly one loud note.
</success_criteria>

<output>
Create `.planning/phases/21-context-policy-parity-closures/21-04-SUMMARY.md` when done
</output>
