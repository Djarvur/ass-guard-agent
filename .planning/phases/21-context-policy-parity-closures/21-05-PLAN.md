---
phase: 21-context-policy-parity-closures
plan: 05
type: execute
wave: 4
depends_on: ["21-01", "21-04"]
files_modified:
  - internal/session/gate.go
  - internal/session/gate_test.go
  - internal/coreexec/register.go
  - internal/coreexec/hooks_wiring_test.go
  - internal/runtime/runtime.go
autonomous: true
requirements: [PAR-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A settings.json hook returning deny on PreToolUse blocks the tool call at the gate chokepoint with the first denying hook's reason as a structured denial — in BOTH runTurn branches (batch-eligible and subagent dispatch) (criterion 1, PAR-03)"
    - "Hook verdicts flow through the SAME pipeline as permission asks with documented precedence hook verdict → permission ask → execute at ONE consumption site — the gateCall head REPLACES 17-02's implicit-allow no-op; no second gate exists (criterion 2, D-04, 17-D-05)"
    - "A hook 'ask' routes the call to the permission ask EVEN IN UNGATED mode (gateSuspend opens the dialog) — the operator's escalation lever, and the only dialog a repo-shipped file can surface (D-04)"
    - "A user-scope hook allow executes the call directly (skips the ask); a project-scope allow was already demoted by 21-01's resolver — the gate never sees a project allow (D-01)"
    - "Exactly ONE PreToolUse consultation site exists post-join: the executor-level leg is disposed (PostToolUse observation stays executor-side) — no double hook firing, one result form per decision (Pitfall 2)"
    - "The @-mention Read-rule consult (21-04's injected accessor) evaluates through the perm rule set post-join — mentions and tool calls answer to the same rule authority (PAR-06 ↔ PAR-03 join)"
  artifacts:
    - path: "internal/session/gate.go"
      provides: "the hook-verdict head of gateCall — replaces the implicit-allow no-op seam comment with the delegation to ecosys PreToolUseVerdict + the verdict mapping (deny→gateDeny, ask→gateSuspend, user-allow→gateExecute, none→rule evaluation) + the documented precedence comment"
      contains: "PreToolUseVerdict"
    - path: "internal/coreexec/register.go"
      provides: "withHooks reduced to PostToolUse observation — the PreToolUse consultation leg removed (the seam 12-02 built, disposed at the join per Pitfall 2)"
      contains: "withHooks"
  key_links:
    - from: "internal/session/gate.go gateCall head"
      to: "internal/ecosys/hookverdict.go ResolveVerdict (via PreToolUseVerdict)"
      via: "the ONE delegation: the gate head calls PreToolUseVerdict (21-01's seam), maps the structured verdict onto the 17 gateVerdict enum, and falls through to rule evaluation only on NO-DECISION"
      pattern: "gateCall"
    - from: "internal/runtime/runtime.go"
      to: "internal/session gate deps + readRuleEvaluator"
      via: "composition: the HookRunner instance wired into the gate deps and the 21-04 Read-rule accessor pointed at the perm rule-set provider — one wiring site"
      pattern: "composition"
  prohibitions:
    - requirement_id: PAR-03
      category: safety
      statement: "The gate-join MUST NOT leave any second PreToolUse consumption path alive — after this plan, hook verdicts are consumed at the gate head only; an executor-level or subagent-branch bypass that re-consults hooks (double-firing side effects, or a deny surfacing through two result forms) is a policy bypass, not a convenience"
      status: unresolved
---

<objective>
Join the hooks to Phase 17's single gate pipeline: replace gateCall's reserved implicit-allow head with the real verdict delegation (deny→gateDeny, ask→gateSuspend even ungated, user-allow→gateExecute), dispose the executor-level PreToolUse leg so exactly one consultation site remains, wire the Read-rule accessor to the perm rule set, and document the precedence hook verdict → permission ask → execute at the chokepoint.

Purpose: criterion 1 + criterion 2 — the phase's policy closure. This is the ONLY 17-blocked slice; everything it needs from Phase 17 is contract-locked in 17-01/17-02 and precondition-marked below. If 17's landed shape diverges from the locked contract, the executor STOPS and reports loudly — never bolts a second gate (17-D-05, criterion 2).
Output: the joined gate, the disposed executor leg, the single-consultation audit, green under -race.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-context-policy-parity-closures/21-CONTEXT.md
@.planning/phases/21-context-policy-parity-closures/21-RESEARCH.md
@.planning/phases/21-context-policy-parity-closures/21-PATTERNS.md
@.planning/phases/17-permissions-elicitation/17-02-PLAN.md
@internal/session/gate.go
@internal/ecosys/hooks.go
@internal/coreexec/register.go
@internal/runtime/runtime.go
</context>

<assumption_delta_decision>
Detector fired (pluralization — "second gate" in criterion 2's prose). The underlying transition: policy decisions go singular (the gate) → the hooks leg could arrive as a second evaluator. The noun now primary: **the gateCall head** (the ONE per-call policy decision point). Decision: **promote** — hook verdicts join as the head INPUT of the existing gate (a new first stage inside the single pipeline), and the old executor-level PreToolUse consultation is demoted to nothing (removed; PostToolUse observation remains executor-side). Rationale: 17-CONTEXT D-05 and Phase-21 criterion 2 both forbid an alongside second consumption path; RESEARCH Pitfall 2 names the double-fire as the exact failure. Companion invariant (Task 2): the single-consultation grep audit — exactly one production call site for the verdict seam.
</assumption_delta_decision>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/session/gate.go` (MOD) — the live hook-verdict head: an internal head helper delegating to the injected PreToolUseVerdict; gateCall's doc comment carries the precedence contract (hook verdict → permission ask → execute).
- `internal/coreexec/register.go` (MOD) — withHooks keeps only PostToolUse; the PreToolUse consultation and its legacy refusal path are deleted (no new dispatch path).
- `internal/runtime/runtime.go` (MOD) — gate-dep composition gains the PreToolUseVerdict injection; the readRuleEvaluator accessor (21-04) wired to the perm rule-set provider.
</artifacts_this_phase_produce>

<flagged_assumptions>
- **Phase-17 contract risk (the central precondition):** this plan codes against the LOCKED 17-01/17-02 contracts verbatim — gateCall(ctx, turnID, callID, tool, input) gateVerdict with gateExecute/gateDeny/gateSuspend/gateDeclineAutomation; internal/perm VerdictDeny/VerdictAsk/VerdictAllow/Unmatched. If the LANDED 17 shape diverges (renamed seam, moved chokepoint, different verdict vocabulary), the executor HALTS and reports — the remedy is a plan revision against the real shape, never a parallel gate or a shim layer.
- **Executor-leg disposal vs kit symmetry (RESEARCH Open Question 1):** resolved per the research recommendation — DELETE the executor PreToolUse consultation (keep PostToolUse); Phase 25 re-homes if the kit needs the seam. A dead policy seam is a bypass risk, not a convenience.
</flagged_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — hook verdicts join the gateCall head end-to-end</name>
  <precondition>Phase 17 executed and its artifacts hold verbatim: internal/session/gate.go exists with (*Session).gateCall(ctx, turnID, callID, tool string, input json.RawMessage) gateVerdict carrying the implicit-allow hook-verdict head seam comment naming Phase 21 + D-04 (17-02 Task 1's reservation); internal/perm exists with RuleSet.Evaluate returning VerdictDeny/VerdictAsk/VerdictAllow/Unmatched (17-01); internal/session/gate_test.go exists (17-02's battery to extend). Verify with: test -f internal/session/gate.go && grep -c gateCall internal/session/gate.go — halt and report if unmet.</precondition>
  <files>internal/session/gate.go, internal/session/gate_test.go, internal/runtime/runtime.go</files>
  <behavior>
    - End-to-end (extending 17-02's gate_test.go battery): a project-scope hook script emitting a deny JSON → gateCall returns gateDeny, the tool does NOT execute, the appended result is the structured denial carrying the FIRST denying hook's reason, and the transcript shows the denial (criterion 1's spine through the REAL hook runner, not only the fake).
    - Hook ask in UNGATED mode (the D-04 tracer path): a hook returning ask on a mutating call under default config → gateSuspend — the permission dialog OPENS even though no rule matched and mode is ungated; answering allow_once executes; answering reject_once denies. The ungated default WITHOUT hooks remains provably dialog-free (17-02's criterion-4 battery stays green — zero new dialogs from this join on hookless sessions).
    - User-scope hook allow: gateExecute without consulting rules or opening a dialog; the same call with the hook removed behaves per rules/mode (the allow was hook-scoped, never persisted).
    - NO-DECISION (silent/timeout/failed hooks): falls through to rule evaluation exactly as the pre-join implicit-allow head did — the 17-02 ungated/gated decision tree is untouched below the head.
    - Precedence documentation: gateCall's doc comment states hook verdict → permission ask → execute verbatim (criterion 2's documented precedence becomes code + comment at ONE chokepoint).
    - Both runTurn branches: the hook-deny and hook-ask cases each run through the batch-eligible path AND the isSubagentTool dispatch branch (inheriting 17-02's chokepoint-bypass coverage — the gate runs in both branches, so the hook head does too).
    - Read-rule join: with the perm rule set denying a Read class, an @mention of an admitted-class path resolves to the denied note (21-04's accessor now backed by perm) — the accessor's seam comment is replaced by live wiring.
  </behavior>
  <read_first>internal/session/gate.go (17-02's landed shape: gateCall, the hook-verdict head seam comment naming Phase 21 + D-04, the verdict enum, the mode/class decision tree), .planning/phases/17-permissions-elicitation/17-02-PLAN.md (Task 1's head reservation verbatim; the verdict-mapping contract; the must_haves this plan inherits), .planning/phases/17-permissions-elicitation/17-01-PLAN.md (the perm Verdict vocabulary), internal/ecosys/hooks.go (PreToolUseVerdict — 21-01's seam), internal/ecosys/hookverdict.go (ResolveVerdict semantics: deny-wins, first-deny-reason, demoted project allow), internal/session/gate_test.go (17-02's fakes: fake perm store, fake surface, stub executor — extend, never fork), internal/runtime/runtime.go (the gate-dep composition site from 17-02; the readRuleEvaluator construction from 21-04), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (the 17-locked verdict vocabulary mapping; Pitfall 2; the Phase-17 Dependency Map)</read_first>
  <action>
RED: extend gate_test.go with the families above (scripted-hook fakes for the head; the real-script rows reuse 21-01's script-hook helpers via a temp settings tree wired through the composition). Commit failing.
GREEN: (1) gate.go — replace the implicit-allow no-op head: call the injected PreToolUseVerdict (the HookRunner instance composed in runtime), map the structured verdict — deny → gateDeny carrying the reason as the structured-error result form; ask → gateSuspend unconditionally (D-04: even ungated — the existing queued permission ask from 17-02/17-03 fires; NO new ask path is built); user-allow → gateExecute; NO-DECISION → fall through to the existing rule evaluation unchanged. The head consults NOTHING else — one delegation, one mapping. Rewrite gateCall's doc comment with the precedence contract and the D-04 ungated-ask clause. (2) runtime.go — inject the HookRunner's PreToolUseVerdict into the gate deps at the existing composition site; point the 21-04 readRuleEvaluator accessor at the rule-set provider already injected for the gate (one wiring site; the accessor's implicit-allow fallback remains for nil rule sets). NO changes to internal/perm, the AskQueue, or the ask surface — this plan consumes 17's surfaces, never forks them. If any 17 artifact diverges from the locked contract: STOP, write the divergence into the SUMMARY, execute nothing further (the flagged assumption's halt clause).
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestGateHookVerdict|TestGatePermissionSuspend' -count=1 && go test ./internal/session/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The hook-deny end-to-end asserts: no executor call, the structured denial with the first-deny reason, transcript line present — in BOTH branches (two subtests).
    - The hook-ask-ungated case asserts the dialog opened (surface fired) under default config, and the hookless-ungated control asserts ZERO surface firings (D-04 and 17's criterion 4 coexist — both pinned in one battery).
    - The user-allow case asserts execution with no dialog and no rule write.
    - The NO-DECISION cases (silent hook, timed-out hook) assert fall-through behavior identical to the pre-join tree (17-02's ungated/gated tests stay green unmodified).
    - `go test -race ./internal/session/ -count=1` green (17-02's full battery + the new families under -race).
    - `mise ci` green.
  </acceptance_criteria>
  <done>The ONE gate pipeline's head is live: settings.json hooks deny at the chokepoint, hook asks open dialogs even ungated, user allows execute — with precedence documented where the decision is made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Executor-leg disposal — one consultation site, no double fire</name>
  <files>internal/coreexec/register.go, internal/coreexec/hooks_wiring_test.go</files>
  <behavior>
    - Double-fire regression: a script hook with an observable side effect (marker-file append) matching a tool call → after the join, the side effect occurs EXACTLY ONCE per call (pre-join it would occur twice: executor leg + gate head; the test pins the post-join count at 1).
    - PostToolUse observation unchanged: a PostToolUse script hook still fires with the tool output after execution (the wrap's remaining leg is intact).
    - The withHooks wrap no longer consults hooks before execution: a hook that would deny does NOT produce the legacy errHookRefused result form at the executor — denials arrive only as gate results now (one result form per decision, Pitfall 2's two-forms hazard closed).
    - Single-consultation audit (the assumption-delta companion invariant): exactly one production site resolves PreToolUse hook verdicts — the gate head; the executor package contains zero consultation calls (filtered grep == 0 in coreexec, >= 1 in the session gate).
    - coreexec's remaining hook wiring tests updated to the reduced contract (no forked legacy suite).
  </behavior>
  <read_first>internal/coreexec/register.go (withHooks :72-88 — the wrap; the wiring at runtime.go:1106-1110 for orientation), internal/coreexec/hooks_wiring_test.go (the executor-level tests to reduce), internal/session/gate.go (Task 1's head — the sole survivor), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pitfall 2 — the double-fire law and the grep-audit post-join discipline; Open Question 1 resolution), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (the withHooks section — the exact wrap being reduced)</read_first>
  <action>
RED: extend hooks_wiring_test.go with the once-only side-effect count and the PostToolUse-still-fires cases; adjust the legacy executor-level PreToolUse expectations to the reduced contract. Commit failing.
GREEN: (1) register.go — reduce withHooks to PostToolUse observation: the wrap invokes the tool, then fires the PostToolUse hook with output; the executor's PreToolUse consultation and its errHookRefused path are deleted (a dead policy seam is a bypass risk — RESEARCH Open Question 1's recommendation; Phase 25 re-homes if the kit needs the shape). (2) Audit: verdict resolution has exactly one production call site (gate.go's head) — the acceptance gate runs the filtered greps; update any coreexec doc comments that still describe executor-owned PreToolUse responsibility (the 12-02 seam comment records its disposal at the Phase-21 join). (3) No changes to the ToolHooks interface beyond what the removal requires — PostToolUse keeps its signature.
  </action>
  <verify>
    <automated>go test -race ./internal/coreexec/ -run 'TestHookWiring|TestHooksOnceOnly' -count=1 && go test ./internal/coreexec/ ./internal/session/ -count=1 && bash -c 'test "$(grep -rn "hooks\.PreToolUse(" internal/coreexec/ --include="*.go" | wc -l | tr -d " ")" = "0" && test "$(grep -c "PreToolUseVerdict(" internal/session/gate.go)" -ge 1'
  </verify>
  <acceptance_criteria>
    - The once-only case asserts the hook side-effect count is exactly 1 per tool call (the double-fire regression pin, Pitfall 2).
    - The PostToolUse case asserts the observation hook still fires with output after execution (the surviving leg).
    - The filtered grep gate passes: zero executor-level consultation call sites in internal/coreexec, at least one verdict-resolution call site in the session gate (the single-consumption invariant from the assumption-delta decision).
    - No legacy refusal-form test remains asserting executor-level hook denials (the two-result-forms hazard is closed).
    - `mise ci` green.
  </acceptance_criteria>
  <done>The join is complete and audit-clean: one gate, one consultation site, one result form per decision — criterion 2's "no second gate exists" holds by construction and by grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook verdict → gate decision | the verdict (deny/ask/allow + reason) is untrusted input selecting an execution decision — mapping must be total and fail-safe |
| subagent dispatch branch → gate | the second runTurn branch must route through the same head or the gate is bypassable |
| executor wrap → tool execution | the disposed leg must not survive as a shadow path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-18 | Elevation of Privilege | gate bypass via the disposed executor leg surviving | high | mitigate | the leg is deleted, not stubbed; the filtered-grep acceptance gate proves zero executor consultation sites; the once-only side-effect test catches any residual double path |
| T-21-19 | Elevation of Privilege | hook ask ignored in ungated mode (D-04 regression) | high | mitigate | the ungated-ask test opens the dialog under default config; the hookless-ungated control proves criterion 4 still holds — both in one battery |
| T-21-20 | Spoofing | hook deny spoofed as fall-through on mapping gaps | medium | mitigate | the mapping is total over the four-valued verdict enum (deny/ask/allow/none each has exactly one gateVerdict target); NO-DECISION is the only fall-through |
| T-21-21 | Repudiation | undocumented precedence (which gate decided?) | low | mitigate | the precedence contract is gateCall's doc comment AT the decision site; transcript denial lines carry the hook reason (auditable per decision) |
| T-21-SC | Tampering | package installs | high | accept | No installs (stdlib + in-tree packages); package-legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/session/ ./internal/coreexec/ ./internal/ecosys/ -count=1` green.
- The single-consultation grep audit: zero executor-level PreToolUse call sites in internal/coreexec; the gate head is the sole production consumer of PreToolUseVerdict.
- `mise ci` green.
</verification>

<success_criteria>
PAR-03 is closed end-to-end: settings.json hooks (project + user scopes) resolve through deny-wins verdicts and consume at Phase 17's single gateCall head — deny blocks with the first reason, ask opens the dialog even ungated, user-allow executes — with the executor leg disposed, one consultation site proven by grep and by the once-only side-effect test, and the precedence documented at the chokepoint. If Phase 17's landed shape diverged, the executor halted loudly instead of improvising (the flagged contract risk).
</success_criteria>

<output>
Create `.planning/phases/21-context-policy-parity-closures/21-05-SUMMARY.md` when done
</output>