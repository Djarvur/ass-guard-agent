---
phase: 21-context-policy-parity-closures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/ecosys/hooks.go
  - internal/ecosys/hookverdict.go
  - internal/ecosys/loader.go
  - internal/ecosys/hooks_test.go
  - internal/ecosys/hookverdict_test.go
  - internal/ecosys/testdata/settings-project.json
  - internal/ecosys/testdata/settings-user.json
autonomous: true
requirements: [PAR-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Hooks load from BOTH settings scopes: project .claude/settings.json and user ~/.claude/settings.json hooks entries parse into the existing registry with a Scope tag, and a malformed settings file degrades to a stderr warning + skip — never an error up through Load/sessionFor (PAR-03, Pitfall 8, D-02)"
    - "Both verdict channels parse: a hookSpecificOutput JSON body with permissionDecision allow/deny/ask + permissionDecisionReason, AND the legacy exit-2 refusal — exit 2 blocks regardless of any JSON verdict on stdout (D-02, CC parity)"
    - "Verdict resolution is deny-wins over the deterministic scope order project → user → plugin: ANY deny wins carrying the FIRST denying hook's reason; a USER-scope allow applies only when no deny exists; ask survives unless a deny exists (D-03, D-04)"
    - "A PROJECT-scope allow is demoted to a loud ignored-allow warning and contributes no decision — repo-shipped files can never widen trust (D-01)"
    - "Silent exit-0 without valid decision JSON and schema-invalid JSON both yield NO-DECISION (fail-open proceeds) — silence never approves (Pitfall 4)"
    - "Hook EXECUTION failure (timeout, non-2 non-zero exit, spawn error) is fail-open: the hook contributes NO verdict and the call proceeds through the normal flow (PAR-03 letter)"
    - "Settings-scope matchers use CC's two-path dialect (exact/alternatives when the matcher contains only [alphanumeric _ - space , |], else unanchored regex); plugin hooks keep the legacy regex-everything behavior unchanged (D-02, Pitfall 3)"
  artifacts:
    - path: "internal/ecosys/hookverdict.go"
      provides: "parseHookVerdict (hookSpecificOutput channel: permissionDecision + permissionDecisionReason; first-non-whitespace-{ gate; schema-invalid → no-decision) + resolveVerdict([]ScopedResult) Verdict — the pure deny-wins resolver kept SEPARATE from the Fire loop so the Phase-17 gate join is a one-line delegation"
      contains: "func ResolveVerdict"
    - path: "internal/ecosys/hooks.go"
      provides: "HookConfig grows Scope (scopeProject/scopeUser/scopePlugin); settings matcher dialect; NewHookRunner partitions hooks into D-03 firing order at construction (call site runtime.go:1106 unchanged); PreToolUseVerdict(ctx, toolName, input) returns the structured four-valued verdict for the gate join"
      contains: "func (r *HookRunner) PreToolUseVerdict"
    - path: "internal/ecosys/loader.go"
      provides: "settings-hook loading: project .claude/settings.json + user ~/.claude/settings.json parsed with the hooks.json shape, Scope set at parse time, merged into loadAll's hooks assembly"
      contains: "loadSettingsHooks"
  key_links:
    - from: "internal/ecosys/loader.go loadAll"
      to: "internal/ecosys/hooks.go NewHookRunner"
      via: "settings hooks join the flat Registry.Hooks slice tagged with Scope; the runner partitions by scope at construction — the loader's merge order stays untouched (Pitfall 1: other consumers depend on overlay precedence)"
      pattern: "Scope"
    - from: "internal/ecosys/hookverdict.go ResolveVerdict"
      to: "internal/session/gate.go gateCall (Phase 17, joined in 21-05)"
      via: "the pure resolver is the substrate the gate hook-verdict head will consume; in this plan it is consumed by PreToolUseVerdict and exercised only by tests"
      pattern: "ResolveVerdict"
  prohibitions:
    - requirement_id: PAR-03
      category: safety
      statement: "A hook verdict that is silent (exit 0 without valid decision JSON), schema-invalid, or emitted from project scope MUST never be honored as allow — the only trust-widening verdict is an explicit permissionDecision 'allow' from user scope, and every other path yields no-decision or a loud demotion"
      status: unresolved
---

<objective>
Land the Phase-21 hook substrate entirely inside internal/ecosys — settings.json scope parsing (project + user), the CC hookSpecificOutput verdict channel alongside legacy exit-2, the four-valued per-hook outcome, and the pure deny-wins ResolveVerdict — all 17-independent: every test here runs green without internal/perm or internal/session/gate.go existing.

Purpose: PAR-03's letter minus the join. The runner RESOLVES verdicts; the gate (Phase 17, joined in 21-05) CONSUMES them — one consumption site. Keeping the resolver pure and the scope order resolved at runner construction makes the join a delegation, not a rewrite (21-RESEARCH Pattern 2).
Output: internal/ecosys/hookverdict.go + hooks.go/loader.go extensions + table-pinned tests, green under -race.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-context-policy-parity-closures/21-CONTEXT.md
@.planning/phases/21-context-policy-parity-closures/21-RESEARCH.md
@.planning/phases/21-context-policy-parity-closures/21-PATTERNS.md
@internal/ecosys/hooks.go
@internal/ecosys/loader.go
@internal/ecosys/types.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/ecosys/hookverdict.go` (NEW) — `Verdict` (verdictDeny/verdictAsk/verdictAllow/verdictNone), `ScopedResult{Scope, Verdict, Reason}`, `parseHookVerdict(stdout string, runErr error) (Verdict, string)` implementing the hookSpecificOutput channel (D-02) with the exit-2-overrides-JSON rule, `ResolveVerdict([]ScopedResult) (Verdict, string)` implementing D-01/D-03/D-04 deny-wins over scope order.
- `internal/ecosys/hooks.go` (MOD) — `HookScope` enum + `HookConfig.Scope` field; `matchSettingsHook` two-path matcher predicate (exact/alternatives vs unanchored regex); `NewHookRunner` scope partition (project → user → plugin); `(*HookRunner).PreToolUseVerdict(ctx, toolName string, input json.RawMessage) (Verdict, string)` — the structured verdict seam; existing `PreToolUse` delegates to it (boolean shape preserved for coreexec until 21-05 disposes that leg).
- `internal/ecosys/loader.go` (MOD) — `loadSettingsHooks(claudeDir string, scope HookScope) []HookConfig` reading `<claudeDir>/settings.json` and `$HOME/.claude/settings.json` with the parseHooksJSON shape, `readCapped`, skip-with-stderr-warning on malformed.
- Test fixtures — `internal/ecosys/testdata/settings-project.json`, `internal/ecosys/testdata/settings-user.json` (RESEARCH Wave-0 gap).
</artifacts_this_phase_produce>

<flagged_assumptions>
- **PAR-03 edge-probe `concurrency` row (status: unresolved — surfaced, never auto-resolved):** the probe asks what is guaranteed if hook execution is interrupted or run in parallel. The design answers it by construction — hooks execute SEQUENTIALLY in D-03 scope order within one Fire/PreToolUseVerdict call (never concurrently), and interruption (ctx cancellation mid-hook) kills the child and yields NO-DECISION fail-open — but the row stays flagged until verification confirms the sequential-execution guarantee and the cancel→no-decision path with an explicit test.
- **Hook hard-timeout value (CONTEXT discretion, A2):** this plan keeps the existing 60s `hookDefaultTimeoutSec` (bounded tool-loop latency) rather than matching CC's documented 600s command-hook default; the divergence is documented in the hookverdict.go doc comment. Revisit only if a legitimate operator hook needs longer.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings-scope hook loading — project + user settings.json into the registry</name>
  <files>internal/ecosys/hooks.go, internal/ecosys/loader.go, internal/ecosys/hooks_test.go, internal/ecosys/testdata/settings-project.json, internal/ecosys/testdata/settings-user.json</files>
  <behavior>
    - Table test over fixture files: a project .claude/settings.json with hooks.PreToolUse → matcher-group → {type,command,timeout} entries loads into HookConfig values with Scope=project; a user ~/.claude/settings.json (HOME redirected via test env) loads with Scope=user; the SAME file shape the plugin parseHooksJSON accepts is accepted verbatim (D-02: CC-compatible layout).
    - Malformed settings JSON (truncated bytes, wrong types, empty hooks map) → zero hooks contributed + one stderr warning naming the file path — never an error returned up through Load (Pitfall 8: a repo must not be able to brick session construction).
    - Oversized settings file (> readCapped's pluginArtifactMaxBytes) → skipped with a warning, same discipline as hooks.json.
    - Matcher dialect: settings hook with matcher "Edit" matches tool "Edit" and does NOT match "NotebookEdit" (exact path — the divergence from legacy plugin regex behavior, Pitfall 3); matcher "Edit|Write" matches both; matcher "mcp__github__.*" (contains a char outside the exact-set) compiles as an unanchored regex and matches "mcp__github__get_issue"; matcher "Bash(git *)" contains parens → regex path.
    - Plugin hooks.json entries keep matching by the legacy compile-everything-as-regex behavior (D-02: existing plugin hooks unchanged — a table row pins "NotebookEdit" still matches plugin matcher "Edit").
    - Missing settings files (no project file, no HOME file) → zero contributions, zero warnings (absent is normal).
  </behavior>
  <read_first>internal/ecosys/hooks.go (parseHooksJSON :124-182 — the shape to accept; hookDefaultTimeoutSec/hookOutputCap/hookRefusalExitCode bounds :91-97; matchingHooks :256-284 — the legacy matcher compile to keep for plugin scope; HookConfig :34), internal/ecosys/loader.go (Load :57 / loadAll :66-114 — the assembly the settings hooks join; readCapped :580-596; logPluginSkipf :801-803 — the warning idiom), internal/ecosys/types.go (Registry.Hooks :73-83), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (hooks.go + loader.go sections — verbatim excerpts), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 1 + Pitfalls 1/3/8 — the CC settings shape and dialect rules are the test oracle)</read_first>
  <action>
RED: extend hooks_test.go with the fixture-driven table above; commit the two testdata fixtures (settings-project.json with a PreToolUse matcher group and a malformed sibling case inline; settings-user.json likewise).
GREEN: (1) add HookScope enum (scopeProject/scopeUser/scopePlugin) + Scope field to HookConfig in hooks.go — zero-value stays scopePlugin so existing constructors are unchanged. (2) Implement the two-path matcher predicate per RESEARCH Pattern 1/Pitfall 3: classify the matcher string — if every rune is alphanumeric, underscore, hyphen, space, comma, or pipe, split on pipes and commas into exact alternatives compared with == against the tool name; otherwise compile as an unanchored Go regexp (the existing compile path). Apply the dialect ONLY to scopeProject/scopeUser hooks; scopePlugin keeps matchingHooks' current behavior byte-for-byte. (3) Add loadSettingsHooks to loader.go reading the project path from the existing claudeDir argument and the user path from os.UserHomeDir(); parse with the same anonymous-struct shape as parseHooksJSON, tag Scope at parse time, set timeout default to hookDefaultTimeoutSec when absent; call it from loadAll so both scopes land in the merged Registry.Hooks slice AFTER the plugin hooks (merge order is irrelevant to firing — the runner partitions in Task 2; do NOT reorder loadAll's existing merge, Pitfall 1). (4) Every failure path warns via the logPluginSkipf family and returns nil — grep-audit that no new error return escapes Load.
Per D-02 the plugin parsing and firing semantics stay untouched; per PAR-03 this is scope parsing, not a new runner.
  </action>
  <verify>
    <automated>go test -race ./internal/ecosys/ -run 'TestSettingsHooks|TestHookMatcherDialect' -count=1 && go test ./internal/ecosys/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/ecosys/ -run 'TestSettingsHooks|TestHookMatcherDialect' -count=1` exits 0 with every table case green.
    - The exact-path case asserts matcher "Edit" fires on "Edit" and NOT on "NotebookEdit"; the legacy-plugin case asserts "NotebookEdit" still matches plugin matcher "Edit" (both rows present — the dialect split is pinned, Pitfall 3).
    - The malformed-settings case asserts zero hooks AND that Load returns a nil error (stderr warning observed via the test's log capture or warning sink).
    - `grep -c 'Scope' internal/ecosys/hooks.go` is >= 3 (enum + field + partition usage).
    - `go vet ./internal/ecosys/` clean.
  </acceptance_criteria>
  <done>Both settings scopes parse into the tagged registry with CC's matcher dialect for settings hooks and untouched legacy behavior for plugin hooks; malformed config degrades loudly-but-softly exactly like hooks.json always has.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verdict channels + pure deny-wins ResolveVerdict</name>
  <files>internal/ecosys/hookverdict.go, internal/ecosys/hookverdict_test.go</files>
  <behavior>
    - parseHookVerdict: stdout whose first non-whitespace char is `{` parses as JSON — hookSpecificOutput.permissionDecision "deny"/"ask"/"allow" with permissionDecisionReason yields the matching verdict + reason; "deny" with an empty reason yields deny with an empty reason string (the resolver's first-deny-reason clause handles it).
    - Schema-invalid JSON (missing permissionDecision, wrong-typed reason, unknown permissionDecision value) → NO-DECISION, never honored (Pitfall 4).
    - Non-JSON stdout (plain text, empty) → NO-DECISION.
    - Exit-2 legacy: runErr with ExitCode 2 → deny with the capped stderr (falling back to stdout) as reason — and exit 2 BLOCKS even when the stdout carries a valid JSON allow verdict (CC rule: the refusal code overrides any JSON, D-02).
    - Execution failure (timeout error, spawn error, non-2 non-zero exit) → NO-DECISION (fail-open, PAR-03 letter).
    - ResolveVerdict table (inputs arrive in D-03 order project → user → plugin): any deny wins, carrying the FIRST denying result's reason — including a project deny beating a user allow, and a user deny beating a project deny (first in order wins the reason); a user-scope allow applies only when no deny exists; a project-scope allow is demoted: contributes no decision AND records a loud ignored-allow warning (D-01); ask survives unless a deny exists — {project ask, user allow} → ask (D-04: the operator's escalation lever); all-NO-DECISION → NO-DECISION; empty input → NO-DECISION (edge probe: empty).
    - The demoted-allow warning is observable via a settable package warning sink (same pattern as the loader's skip logger) — one warning per demoted result, none for user-scope allows.
  </behavior>
  <read_first>.planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 2 — the resolver contract is the test oracle; Code Examples — the CC verdict JSON shapes; Pitfall 4 — the three-valued NO-DECISION rule; A1 — the ask value), internal/ecosys/hooks.go (classifyHookRun :353-377 — the exit-2 classification to mirror; capHookOutput — the output cap), internal/ecosys/loader.go (logPluginSkipf :801-803 — the warning-sink idiom to reuse), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (hooks.go section — classifyHookRun verbatim)</read_first>
  <action>
RED: write hookverdict_test.go as two table batteries (parseHookVerdict channel cases; ResolveVerdict order/authority cases named after the D-facts: deny-wins, first-deny-reason, project-allow-demoted, ask-survives, silent-is-nothing, exit2-overrides-json). Commit failing.
GREEN: implement hookverdict.go — a Verdict enum (verdictDeny/verdictAsk/verdictAllow/verdictNone), parseHookVerdict(stdout string, runErr error) (Verdict, string) with the exit-2 check FIRST (classification mirrors classifyHookRun: runErr nil → JSON channel; exit 2 → deny regardless of stdout content; other failure → verdictNone), and ResolveVerdict(results []ScopedResult) (Verdict, string) as a PURE function over the ordered slice — single pass tracking first-deny, then user-allow, then ask, emitting the ignored-allow warning through the package warning sink when a project-scope allow is encountered. Keep the resolver free of exec/context imports so it stays table-testable and the 21-05 gate join is a delegation (RESEARCH Pattern 2's "keep this resolver SEPARATE from the Fire loop"). Doc comment states D-01/D-02/D-03/D-04 verbatim and names the consumer (the Phase-17 gate head, joined in 21-05).
  </action>
  <verify>
    <automated>go test -race ./internal/ecosys/ -run TestHookVerdict -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/ecosys/ -run TestHookVerdict -count=1` exits 0 with every table case green.
    - The exit2-overrides-json case asserts deny even when stdout holds a valid allow verdict (the CC spoofing rule, RESEARCH threat table row 2).
    - The project-allow-demoted case asserts both the verdict outcome AND exactly one warning-sink record (loud, not silent — D-01).
    - The first-deny-reason case asserts the reason string equals the FIRST denying result's reason in D-03 order (not the last, not the most severe).
    - `go vet ./internal/ecosys/` clean.
  </acceptance_criteria>
  <done>Both verdict channels parse with CC's precedence rules, and the deny-wins resolver is a pure, exhaustively table-pinned function ready for the gate to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Runner integration — scope partition at construction + PreToolUseVerdict seam</name>
  <files>internal/ecosys/hooks.go, internal/ecosys/hooks_test.go</files>
  <behavior>
    - NewHookRunner partitions its input slice into D-03 firing order (scopeProject → scopeUser → scopePlugin, stable within scope) — a test constructs a runner from a deliberately scrambled slice and asserts matchingHooks returns them in partitioned order; the runtime.go:1106 call site needs zero changes (same signature).
    - PreToolUseVerdict(ctx, toolName, input): runs every matching hook sequentially in partitioned order via the existing runOne bounds (per-hook timeout, output cap, sanitized env), builds ScopedResults, delegates to ResolveVerdict, and returns (Verdict, reason).
    - End-to-end verdict integration (script hooks via sh): a project-scope script emitting a deny JSON → PreToolUseVerdict returns deny with that reason; a user-scope script emitting allow with no project hooks → allow; a project-scope allow script (no user hooks) → NO-DECISION + the ignored-allow warning; a script that sleeps past its timeout → NO-DECISION (fail-open, bounded); a hook killed by ctx cancellation mid-run → NO-DECISION (the flagged PAR-03 concurrency assumption's pinned test: sequential execution + cancel → no-decision).
    - Existing PreToolUse keeps its (bool, string) contract by delegating to PreToolUseVerdict (deny → false+reason, everything else → true) — hooks_wiring_test.go in internal/coreexec stays green untouched (the executor leg's disposal is 21-05's job, Pitfall 2).
  </behavior>
  <read_first>internal/ecosys/hooks.go (NewHookRunner :115, Fire :190-228 — the loop shape to preserve; runOne :304; buildPayload :382-392 — the stdin contract PreToolUse fields extend :232-241; sanitized env :400-414), internal/coreexec/hooks_wiring_test.go (the existing executor-level wiring test that must stay green), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pitfall 2 — why the executor leg stays put until 21-05; the hook stdin payload example)</read_first>
  <action>
RED: extend hooks_test.go with the partition-order test (scrambled input slice → ordered matchingHooks) and the script-driven PreToolUseVerdict integration battery (reuse the existing script-hook test helpers in hooks_test.go if present; otherwise shell scripts via t.TempDir).
GREEN: (1) NewHookRunner performs a stable partition of its hooks slice by Scope into project → user → plugin order before storing — one sort.SliceStable with a scope rank, no behavior change for all-plugin inputs. (2) Implement PreToolUseVerdict mirroring Fire's loop: matchingHooks (which now reflects the partition), sequential runOne per hook (existing timeout/cap/env bounds — never concurrent, the flagged concurrency assumption), assemble ScopedResults from classifyHookRun + parseHookVerdict, delegate to ResolveVerdict. (3) Rewrite PreToolUse as a thin delegation to PreToolUseVerdict preserving today's boolean refusal semantics for the executor — a one-line body change, no coreexec edits in this plan.
This plan deliberately does NOT touch internal/coreexec or internal/session — the executor leg stays until 21-05 disposes it at the join (Pitfall 2: avoid double-fire only when the gate head goes live; until then the executor consultation is the only consumer and must keep working).
  </action>
  <verify>
    <automated>go test -race ./internal/ecosys/ -run 'TestHookScopeOrder|TestPreToolUseVerdict' -count=1 && go test ./internal/coreexec/ -run TestHookWiring -count=1 && go test -race ./internal/ecosys/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The partition test asserts matchingHooks order equals project → user → plugin for a scrambled input (D-03 firing order pinned at construction).
    - The script battery covers all five rows (deny JSON, user allow, project allow demoted + warning, timeout fail-open, ctx-cancel fail-open) — the cancel row is the flagged PAR-03 concurrency assumption's explicit pin.
    - `go test ./internal/coreexec/ -run TestHookWiring -count=1` stays green with zero edits to internal/coreexec (the delegation preserved the executor contract).
    - `mise ci` green (the whole-package -race run is part of it).
  </acceptance_criteria>
  <done>The runner resolves structured hook verdicts in D-03 order with CC's failure semantics, the existing executor wiring is untouched and green, and the substrate sits ready for 21-05's one-line gate delegation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-shipped .claude/settings.json → hook loader | a malicious repo authors hooks and verdict JSON — the exact repo-trust hole D-01 exists to close |
| hook process stdout/exit → verdict parser | hook output is untrusted data; schema-invalid or silent output must never widen trust |
| hook command execution | operator/project-authored commands run bounded-sync with sanitized env (PATH/HOME/CLAUDE_PLUGIN_ROOT only — unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Elevation of Privilege | project-scope allow verdict | high | mitigate | D-01 demotion in ResolveVerdict: project allow → no-decision + loud ignored-allow warning; table test pins "project allow + no user hooks → NO-DECISION, never allow" |
| T-21-02 | Tampering | exit-code games (exit 2 + allow JSON) | high | mitigate | exit 2 checked BEFORE the JSON channel and overrides it (CC-verified rule); explicit test row |
| T-21-03 | Elevation of Privilege | silent/invalid output read as approval | high | mitigate | four-valued outcome: exit-0-silent and schema-invalid JSON yield NO-DECISION (Pitfall 4); test rows pin both |
| T-21-04 | DoS | hung/malicious hook wedging the tool loop | medium | mitigate | existing per-hook timeout + output cap bounds reused; timeout and ctx-cancel both fail open with NO-DECISION (pinned rows) |
| T-21-05 | Repudiation | silent trust decisions | low | accept | hook verdicts land in stderr structured warnings today; the transcript-level verdict record arrives with 21-05's gate join where the decision is consumed |
| T-21-SC | Tampering | package installs | high | accept | No installs (stdlib only in this plan); package-legitimacy gate not applicable |
</threat_model>

<prohibition_probe_breadcrumbs>
Dropped as canon, not minted: hook-command injection / env leakage — owned by the existing sanitized-env + bounded-exec construction (V5/V8 ASVS rows in 21-RESEARCH Security Domain); prompt-injection-via-hook-stdout — the capped-output + structured-verdict schema is the mitigation, generic injection canon not minted here.
</prohibition_probe_breadcrumbs>

<verification>
- `go test -race ./internal/ecosys/ -count=1` green.
- `go test ./internal/coreexec/ -count=1` green (executor wiring untouched).
- `mise ci` green.
</verification>

<success_criteria>
PAR-03's substrate is complete and 17-independent: both settings scopes load with loud-soft degradation, both verdict channels parse with CC's precedence rules, deny-wins resolution over the deterministic scope order is table-pinned, and repo-shipped files provably cannot widen trust — with the resolver pure and separate so 21-05's gate join is a delegation.
</success_criteria>

<output>
Create `.planning/phases/21-context-policy-parity-closures/21-01-SUMMARY.md` when done
</output>
