---
phase: 21-context-policy-parity-closures
plan: 03
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - internal/provider/provider.go
  - internal/provider/streaming.go
  - internal/provider/streaming_test.go
  - internal/event/events.go
  - internal/session/session.go
  - internal/session/projector.go
  - internal/session/projector_test.go
  - internal/session/testdata/thinking-golden/sse-thinking.jsonl
  - internal/shaper/shaper.go
  - internal/shaper/shaper_test.go
  - internal/runtime/runtime.go
autonomous: true
requirements: [PAR-05]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A provider thinking block streams end-to-end: SSE thinking_delta/signature_delta accumulate and content_block_stop emits StreamChunk{Type: thinking, Raw: assembled block JSON}; redacted_thinking emits immediately from content_block_start (PAR-05, RESEARCH Pattern 4 hop 1)"
    - "The transcript stores the block as json.RawMessage verbatim via the EXISTING AppendRawThinking (redactor-exempt by construction, zero Redactor invocations — the type-scoped exemption never widens) (D-12, 16-D-23)"
    - "Thinking streams live to the editor as agent_thought_chunk through the bus → routeBusEvent → ThoughtChunk chain, and the raw_thinking transcript line is real history the Phase-18 replay re-emits — no synthetic markers (D-13)"
    - "The projector folds raw_thinking INTO its assistant message's mid-turn accumulation — thinking lives and dies with its turn unit through reset-boundary cuts, never cut separately (Pitfall 5, 19-D-04 discipline)"
    - "The shaper maps BOTH block types — ThinkingBlockParam{Signature, Thinking} AND RedactedThinkingBlockParam{Data} — in original order into the assistant message; filtering by type=='thinking' only would drop redacted blocks and 400 (RESEARCH Pattern 4 hop 5b)"
    - "Byte-identity is proven by golden fixtures from captured wire pairs through SSE → transcript → projector → outgoing request: the thinking/signature/data FIELD VALUES are identical at every hop (D-14)"
  artifacts:
    - path: "internal/provider/streaming.go"
      provides: "the thinking accumulator sibling to the tool-use lifecycle state in drainSSE: opens on content_block_start type thinking, accumulates thinking_delta/signature_delta, emits StreamChunk{Type: chunkTypeThinking, Raw} on content_block_stop, flushes on EOF/error/[DONE]; redacted_thinking emits immediately"
      contains: "chunkTypeThinking"
    - path: "internal/event/events.go"
      provides: "AgentThoughtChunk event (TurnID/MessageID/Content) mirroring AgentMessageChunk + BufAgentThoughtChunk buffer const wired into the forwarder subscribe sites"
      contains: "AgentThoughtChunk"
    - path: "internal/session/projector.go"
      provides: "case TypeRawThinking in accumulateMidTurn — pending-thinking stashed and flushed INTO the assistant batch (with tool_use blocks, original order)"
      contains: "TypeRawThinking"
    - path: "internal/shaper/shaper.go"
      provides: "thinking block mapping in the assistant-batch branch of toMessageParams — anthropic.ThinkingBlockParam / anthropic.RedactedThinkingBlockParam, both types, additive Message fields; text-only rendering byte-identical when no thinking present"
      contains: "ThinkingBlockParam"
    - path: "internal/session/testdata/thinking-golden/sse-thinking.jsonl"
      provides: "captured wire-pair golden fixtures (thinking + signature + redacted_thinking) with harvest provenance, pinning the D-14 identity chain"
      contains: "signature"
  key_links:
    - from: "internal/provider/streaming.go"
      to: "internal/session/session.go streamAndEmit"
      via: "case chunkTypeThinking → s.Manager.AppendRawThinking(turnID, model, chunk.Raw) + s.Bus.Publish(event.AgentThoughtChunk{...}) — the manager.go:349 endpoint gains its first production caller"
      pattern: "AppendRawThinking"
    - from: "internal/event/events.go AgentThoughtChunk"
      to: "internal/runtime/runtime.go routeBusEvent"
      via: "new case → emit.ThoughtChunk(messageID, content) through the ActivityEmitter type-assert idiom (emitter.go:519 exists; zero new frame kinds — 16-D-20 additive)"
      pattern: "ThoughtChunk"
    - from: "internal/session/projector.go"
      to: "internal/shaper/shaper.go"
      via: "raw_thinking field values extracted ONLY at the projector, passed through untouched to ThinkingBlockParam/RedactedThinkingBlockParam — the SDK re-serializes; goldens pin field-value identity provider→transcript→projector→outgoing"
      pattern: "field-value identity"
  prohibitions:
    - requirement_id: PAR-05
      category: safety
      statement: "Thinking and signature bytes MUST never be parsed-and-reserialized for storage, edited, reordered, filtered by type, or recomputed — raw json.RawMessage in, RawMessage stored, field VALUES extracted only at the projector and passed through untouched; a modified thinking block is a provider 400 by the Anthropic preservation law"
      status: unresolved
---

<objective>
Fill the thinking middle (PAR-05): provider SSE → StreamChunk → transcript (raw, redactor-exempt) → bus → agent_thought_chunk → projector → shaper → outgoing request, with D-14 golden fixtures proving field-value identity end-to-end. Independent of Phase 17; respects Phase 19's never-rewrite discipline.

Purpose: D-12/D-13/D-14. Phase 16 built both endpoints (AppendRawThinking manager.go:349, EmitterHandle.ThoughtChunk emitter.go:519) with zero production callers; this plan wires the five hops between them and pins the byte-identity contract with real captured bytes.
Output: thinking streaming live in Zed + round-tripping into outgoing requests, green under -race including the golden battery.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-context-policy-parity-closures/21-CONTEXT.md
@.planning/phases/21-context-policy-parity-closures/21-RESEARCH.md
@.planning/phases/21-context-policy-parity-closures/21-PATTERNS.md
@internal/provider/streaming.go
@internal/session/session.go
@internal/session/projector.go
@internal/shaper/shaper.go
@internal/runtime/runtime.go
</context>

<artifacts_this_phase_produce>
Symbols this plan contributes to the phase inventory:
- `internal/provider/provider.go` — `chunkTypeThinking = "thinking"` const beside blockText/blockToolUse; StreamChunk unchanged in shape (Raw already exists) — the type joins the additive discriminated union.
- `internal/provider/streaming.go` — thinking accumulator state (thText/thSignature strings.Builder, inThinking bool, thType string) + flushThinking emitting the assembled block JSON.
- `internal/event/events.go` — `AgentThoughtChunk{TurnID, MessageID, Content string}` with `Kind() "AgentThoughtChunk"`; `BufAgentThoughtChunk = 32` const beside BufAgentMessageChunk.
- `internal/session/session.go` — the `case chunkTypeThinking` in streamAndEmit.
- `internal/runtime/runtime.go` — the AgentThoughtChunk subscribe wiring (startChunkForwarder channels) + the routeBusEvent case.
- `internal/session/projector.go` — `case TypeRawThinking` in accumulateMidTurn + the pending-thinking flush into the assistant batch.
- `internal/shaper/shaper.go` — additive `Message.ThinkingBlocks []ThinkingBlock` (Type/Text/Signature/Data fields) + mapping in toMessageParams.
- `internal/session/testdata/thinking-golden/sse-thinking.jsonl` (NEW) — the golden fixtures.
</artifacts_this_phase_produce>

<flagged_assumptions>
- **PAR-05 edge-probe `boundary` row (status: unresolved — surfaced, never auto-resolved):** at the edges — a thinking block with zero thinking_deltas, a missing signature_delta before content_block_stop, and a raw_thinking line landing exactly at / one line either side of a reset boundary — the contract is: empty-text blocks still emit (type+signature only), a missing signature stores verbatim (the provider's own bytes are the truth; the projector extracts whatever fields exist), and boundary cuts take the WHOLE turn unit (thinking + assistant message + tool batch together, never think-without-its-turn). Flagged until the golden battery's boundary-case row runs green.
- **PAR-05 edge-probe `precision` row (status: unresolved — surfaced, never auto-resolved):** the exact identity contract is FIELD-VALUE identity, not byte-identity of the full wire envelope — the transcript stores the assembled block JSON whose field values equal the provider's delta-concatenated values (assembled from raw delta strings, never through a typed round-trip); the projector extracts values untouched; the SDK re-serializes. No overflow/rounding surface exists (strings only). Flagged until the golden fixtures pin the chain.
- **Golden fixture sourcing (A4, MEDIUM risk):** if the captured corpus (profiles/zcode/, internal/provider testdata) lacks thinking-bearing SSE responses, the fixtures must be captured fresh via the existing BuildWithCapturer seam before the golden test can exist — a capture step is built into Task 3 as contingency; synthetic fixtures are permitted ONLY as corpus_absent-flagged fallback with a loud SUMMARY note, never silently.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSE thinking accumulator — StreamChunk grows the thinking type</name>
  <files>internal/provider/streaming.go, internal/provider/provider.go, internal/provider/streaming_test.go</files>
  <behavior>
    - Table test over scripted SSE event sequences (the streaming_test.go harness style): content_block_start {content_block:{type:"thinking"}} opens the accumulator; thinking_delta events (delta.thinking strings) concatenate in order; signature_delta (delta.signature) is captured; content_block_stop emits StreamChunk{Type:"thinking"} whose Raw unmarshals to field values thinking == the exact concatenation of all delta.thinking strings and signature == the exact signature_delta string (D-12: assembled from raw delta strings — never a typed round-trip).
    - redacted_thinking: content_block_start carrying content_block.type "redacted_thinking" with the data field → StreamChunk{Type:"thinking"} emitted IMMEDIATELY (no deltas follow), Raw field values type=="redacted_thinking" + data verbatim (RESEARCH anti-pattern: filtering thinking by one type name drops these).
    - Interleaving: a thinking block between text deltas and a tool_use block — all three chunk types emit, text buffered chunks unaffected, tool-use lifecycle unaffected (the accumulator is SIBLING state, not a refactor of the existing machine).
    - Flush-on-termination: stream ending (EOF, error, [DONE]) with an open thinking block flushes it exactly as flushToolUse does at its call sites (streaming.go:300-330 family).
    - Multiple thinking blocks in one response emit one StreamChunk each, in block order.
    - A thinking block whose content_block_stop never arrives AND the stream ends cleanly → flushed once (no double-emit with the stop handler).
  </behavior>
  <read_first>internal/provider/streaming.go (drainSSE :265-446 — the tool-use lifecycle state :275-286, transitions :348-382, flush :405-446, tuInputBytes :449-456 — the accumulated-string→RawMessage idiom to copy; the :300-330 flush call sites), internal/provider/provider.go (StreamChunk :38-58 — Raw json.RawMessage already on the struct), internal/provider/streaming_test.go (the scripted-SSE test harness to extend), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (streaming.go section — the sibling-state pattern verbatim), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 4 hop 1+2 — the event sequence and CC delta discipline are the oracle; A6 — string-const naming convention)</read_first>
  <action>
RED: extend streaming_test.go with the six table families above, asserting on the Raw field values via a minimal unmarshal in the TEST only.
GREEN: add chunkTypeThinking to provider.go beside the existing block consts (goconst discipline — no raw "thinking" string literals scattered). In drainSSE add sibling accumulator state mirroring the tool-use block's shape: open on content_block_start with type thinking (reset builders), accumulate thinking_delta into the text builder and signature_delta into the signature builder, and on content_block_stop flush — assembling the block JSON from the raw accumulated strings exactly as tuInputBytes does for tool input (the assembled bytes carry the provider's field values verbatim). redacted_thinking short-circuits: build the block JSON from the content_block_start payload's data value and emit immediately without opening the accumulator. Register the flush at the same termination call sites flushToolUse uses. No changes to any existing case's behavior.
  </action>
  <verify>
    <automated>go test -race ./internal/provider/ -run TestStreamThinking -count=1 && go test ./internal/provider/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./internal/provider/ -run TestStreamThinking -count=1` exits 0 with all six families green.
    - The interleaving case asserts the existing text/tool-use emissions are byte-identical to their pre-change forms (the additive-only guarantee).
    - The redacted_thinking case asserts the data field value survives verbatim and NO signature field is invented.
    - The termination case asserts exactly one flush (no double-emit) under -race.
    - `go vet ./internal/provider/` clean.
  </acceptance_criteria>
  <done>Provider thinking blocks — signed and redacted — emerge as typed StreamChunks with raw assembled bytes, without disturbing any existing stream behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session leg — transcript append + live agent_thought_chunk forward</name>
  <files>internal/event/events.go, internal/event/events_test.go, internal/session/session.go, internal/runtime/runtime.go</files>
  <behavior>
    - streamAndEmit case chunkTypeThinking: calls s.Manager.AppendRawThinking(turnID, model, chunk.Raw) AND publishes event.AgentThoughtChunk{TurnID, MessageID: turnID, Content: thinkingText} where thinkingText is the "thinking" FIELD VALUE extracted from the assembled RawMessage (display extraction only — the stored transcript bytes remain the verbatim RawMessage, D-12).
    - Redactor counting (the T-16-04 pin, now load-bearing for real): the session-level test with a counting redactor asserts ZERO redactor invocations for a turn containing thinking blocks (text/tool content still redacted — the exemption stays type-scoped).
    - AgentThoughtChunk mirrors AgentMessageChunk: Kind() discriminator, buffer const, delivery through the bus to a subscriber.
    - Forwarder: routeBusEvent gains the AgentThoughtChunk case → emit.ThoughtChunk(c.MessageID, ContentBlock{Type: blockText, Text: c.Content}) via the ActivityEmitter interface; the plain-ChunkEmitter fakes skip it via the existing type-assert idiom (runtime.go:599 family) — existing emitter tests stay green.
    - Both subscribe sites (the runtime forwarder channels per the startChunkForwarder pattern :591-645) carry the new kind with BufAgentThoughtChunk buffering.
    - End-to-end wiring test (runtime level, the TestThoughtForward family): a scripted provider turn emitting thinking + text produces, in order, agent_thought_chunk then the message chunk through a recording emitter — kind order preserved (the ordered-emitter guarantee, 16-D-02).
  </behavior>
  <read_first>internal/session/session.go (streamAndEmit :663-701 — the switch and its existing cases), internal/session/manager.go (AppendRawThinking :344-358; appendLineUnredacted :106-134 — DO NOT touch or extract a shared helper, 16-02 locked), internal/event/events.go (AgentMessageChunk :43-53, BufAgentMessageChunk :12), internal/runtime/runtime.go (startChunkForwarder :591-645; routeBusEvent :651-660; the type-assert idiom :599), internal/acp/emitter.go (ThoughtChunk :519-525; updKindThoughtChunk :23), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (session.go + events.go + routeBusEvent sections — verbatim), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 4 hops 3+4; A3 — the frame payload)</read_first>
  <action>
RED: write the session-level test (case behavior + zero-redactor counting) into session_test.go or a new thinking path test, the event test into events_test.go, and the forwarder test into the runtime test family (emitter_e2e_test.go harness) — commit failing.
GREEN: (1) events.go — AgentThoughtChunk + Kind + BufAgentThoughtChunk. (2) session.go — the new case: append via s.Manager.AppendRawThinking(turnID, s.model, chunk.Raw) following the call-shape of the neighboring cases (model attribution from the same source the assistant message uses), extract the thinking field value for the event Content via a minimal json.Unmarshal into a throwaway view (display only; never re-marshaled into storage), publish when Content is non-empty. (3) runtime.go — subscribe the kind in both forwarder channel sites with the new buffer const; add the routeBusEvent case delegating to emit.ThoughtChunk through the ActivityEmitter assertion. No emitter.go changes (the endpoint exists); no manager.go changes (the append path is locked).
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestRawThinking|TestStreamAndEmit' -count=1 && go test -race ./internal/runtime/ -run TestThoughtForward -count=1 && go test ./internal/event/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The zero-redactor case asserts a counting Redactor records zero invocations across a thinking-bearing turn while non-thinking content in the same turn IS redacted (the type-scoped exemption holds under real traffic, D-12).
    - `grep -rn 'AppendRawThinking' internal/ --include='*.go' | grep -v _test | grep -v 'func (m \*Manager)'` shows at least one production caller (session.go) — the zero-caller state is over.
    - The forwarder test asserts thought-chunk emission order relative to message chunks is preserved and fakes without ActivityEmitter skip cleanly (no panic, 16-01 embedding discipline).
    - `go test -race ./internal/session/ ./internal/runtime/ ./internal/event/ -count=1` green.
  </acceptance_criteria>
  <done>Thinking lands in the transcript unredacted by construction and streams live to the editor through the existing ordered-emitter chain — AppendRawThinking and ThoughtChunk gain their production callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Projector + shaper mapping + D-14 golden fixtures</name>
  <files>internal/session/projector.go, internal/session/projector_test.go, internal/shaper/shaper.go, internal/shaper/shaper_test.go, internal/session/testdata/thinking-golden/sse-thinking.jsonl</files>
  <behavior>
    - Projector: accumulateMidTurn gains case TypeRawThinking — the block stashes onto the in-progress assistant accumulation and flushes INTO the assistant message at flushBatch alongside its tool_use blocks in ORIGINAL block order; a raw_thinking line whose assistant batch never forms (turn truncated before any assistant content) is dropped WITH its turn unit — never emitted as a standalone thinking-only message (Pitfall 5).
    - splitAtResetBoundary is untouched: a golden case with a boundary line mid-thinking-turn asserts the projection window keeps or drops the whole turn unit and never splits thinking from its assistant message (the flagged boundary row pinned).
    - Shaper: the assistant-batch branch maps ThinkingBlocks — type thinking → anthropic.ThinkingBlockParam{Thinking, Signature}; type redacted_thinking → anthropic.RedactedThinkingBlockParam{Data}; both in one list, original order, BEFORE/interleaved with text+tool_use exactly as accumulated. A message with zero thinking blocks renders byte-identically to today (additive fields omitted).
    - Golden battery (TestThinkingGolden): the committed sse-thinking.jsonl replays through the REAL chain — scripted SSE → drainSSE → StreamChunk → AppendRawThinking → projector → shaper Message → toMessageParams — and asserts the outgoing params' thinking/signature/data field values equal the fixture's provider values at every hop (D-14). Includes one redacted_thinking block and one boundary-adjacent case.
    - Emitter replay pin (D-13's data path): a test asserts the raw_thinking transcript line re-read from disk (Load/scan path) yields the identical RawMessage bytes that were appended — the history Phase 18's replay will re-emit is real, not synthetic.
    - Fixture provenance: the golden file carries a header comment naming its source (captured corpus session id / capture run date) per the committed-fixture house style (08/12 harvest headers); corpus_absent synthetic fallback only with a loud flag.
  </behavior>
  <read_first>internal/session/projector.go (accumulateMidTurn :170-227 — the turn-scoped fold and flushBatch; splitAtResetBoundary :123-157 — read-only, must not change), internal/shaper/shaper.go (toMessageParams :165-226 — the assistant-batch branch; Message :35-44), internal/session/manager_test.go (the 16-02 append-path tests to extend for the replay pin), .planning/phases/21-context-policy-parity-closures/21-PATTERNS.md (projector.go + shaper.go sections — verbatim analogs), .planning/phases/21-context-policy-parity-closures/21-RESEARCH.md (Pattern 4 hop 5a+5b — the SDK param quotes; Pitfall 5 — the reset-boundary law; A4 — fixture sourcing), profiles/zcode/ + internal/provider/testdata (the corpus hunt for thinking-bearing SSE responses)</read_first>
  <action>
Fixture first: hunt profiles/zcode/ and internal/provider testdata for thinking-bearing SSE responses; if found, extract one thinking+signature pair and one redacted_thinking block into internal/session/testdata/thinking-golden/sse-thinking.jsonl with a provenance header; if absent, run one capture via the existing BuildWithCapturer seam against the configured provider and harvest the same shapes; only if capture is impossible, synthesize fixtures flagged corpus_absent in the header AND the SUMMARY (A4 contingency, loud never silent).
RED: projector_test.go cases (fold-into-assistant, orphan-drop-with-turn, boundary-adjacent), shaper_test.go cases (both param types, order, zero-thinking byte-identity), TestThinkingGolden driving the full chain off the fixture file. Commit failing.
GREEN: (1) projector.go — add the pending-thinking stash to the accumulation state (a small slice of raw blocks with their extracted field views), case TypeRawThinking appends, flushBatch materializes them into the assistant Message's new ThinkingBlocks before/with text+tool_use per original order; the orphan rule falls out of the flushBatch ownership (no batch → stash discarded with the turn). (2) shaper.go — Message gains ThinkingBlocks []ThinkingBlock (additive, omitted when empty); the assistant branch interleaves them per accumulated order mapping to the two SDK param types via their field names (Signature/Thinking vs Data) — use the SDK param structs directly, never hand-marshaled JSON (RESEARCH Don't-Hand-Roll). (3) The golden test reads the fixture, runs the chain, compares field values at each hop with testify equality on the extracted views. splitAtResetBoundary receives zero edits.
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestThinkingGolden|TestProjector' -count=1 && go test -race ./internal/shaper/ -run TestShaperThinking -count=1 && go test ./internal/session/ ./internal/shaper/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestThinkingGolden asserts field-value identity at every hop (fixture provider values == transcript RawMessage values == projector extraction == outgoing param values) for both block types plus the boundary-adjacent case (D-14; the flagged precision row pinned).
    - The orphan-drop case asserts no thinking-only assistant message is ever produced (Pitfall 5).
    - The zero-thinking shaper case asserts toMessageParams output is byte-identical to the pre-change form for text+tool messages (additive-only).
    - The replay pin asserts appended-then-re-read raw_thinking bytes are identical (D-13's real-history data path).
    - The fixture header names its provenance (session id + harvest date, or corpus_absent flag).
    - `mise ci` green.
  </acceptance_criteria>
  <done>Thinking round-trips: SSE bytes → transcript → live frames → outgoing request with field values provably identical through golden fixtures, and both endpoints from Phase 16 fully consumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider SSE stream → accumulator | wire bytes are untrusted input; malformed deltas must degrade, not corrupt the stream state machine |
| thinking content → redactor surface | raw_thinking is UNREDACTED BY DESIGN — the exemption must never widen to other line kinds |
| transcript → outgoing request | replayed thinking bytes go back to the provider; any tampering produces 400s (detectable, not silent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-09 | Tampering | thinking/signature bytes altered in transit through the stack | high | mitigate | D-12 RawMessage-by-construction storage + D-14 golden field-value identity chain; no typed re-serialization anywhere in the path (pinned by goldens) |
| T-21-10 | Information Disclosure | redactor exemption widening beyond thinking | high | mitigate | appendLineUnredacted stays type-scoped with zero-code-touch discipline; the counting-redactor test pins zero invocations on the thinking path and continued redaction elsewhere (T-16-04 inheritance) |
| T-21-11 | DoS | pathological thinking deltas (huge blocks, thousands of blocks) | low | accept | deltas accumulate as strings bounded by the response body size the transport already accepts; no per-block cap beyond the provider's own limits — matching CC behavior |
| T-21-12 | Elevation of Privilege | projector emitting thinking without its turn (protocol break) | medium | mitigate | fold-into-assistant-batch ownership + orphan-drop-with-turn rule; boundary-adjacent golden case pins the whole-turn-unit guarantee |
| T-21-SC | Tampering | package installs | high | accept | No installs (stdlib + pinned SDK); package-legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/provider/ ./internal/session/ ./internal/shaper/ ./internal/event/ ./internal/runtime/ -count=1` green.
- `mise ci` green.
</verification>

<success_criteria>
PAR-05 holds end-to-end: provider thinking blocks stream live as agent_thought_chunk, land in the transcript as verbatim RawMessage with the redactor provably uninvoked, survive projector boundary logic with their turn unit, and re-enter outgoing requests field-value-identical — proven by captured-bytes golden fixtures including redacted_thinking.
</success_criteria>

<output>
Create `.planning/phases/21-context-policy-parity-closures/21-03-SUMMARY.md` when done
</output>
