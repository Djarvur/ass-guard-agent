---
phase: 00-spike-re-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - spikes/03-acp-handshake/main.go
  - spikes/03-acp-handshake/RESULT.md
autonomous: true
requirements: [STACK-Phase0-#3]
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A throwaway Go spike performs a real ACP v1 exchange — initialize → session/new → session/prompt → observe session/update — against any reachable ACP v1 server, using hand-rolled newline-delimited JSON-RPC framing"
    - "The spike confirms the canonical method names (initialize, session/new, session/prompt, session/update) and the result field name agentCapabilities, recording the Tier-A correction that STACK omitted session/new"
    - "STACK item #3 is recorded in spikes/03-acp-handshake/RESULT.md (the per-spike evidence file) with Status VERIFIED (or FAILED + corrected method names if a method name differs) — Plan 05 folds this into VERIFIED-FACTS.md"
  artifacts:
    - spikes/03-acp-handshake/main.go
    - spikes/03-acp-handshake/RESULT.md
  key_links:
    - "spikes/03-acp-handshake/main.go is package main inside module github.com/djarvur/ass-guard-spikes (compiles against the go.mod from plan 01)"
    - "spikes/03-acp-handshake/RESULT.md is the D-02-shaped item-#3 draft Plan 05 reads to author VERIFIED-FACTS.md item #3 (Plan 05 is the single writer of VERIFIED-FACTS.md)"
---

<objective>
Empirically verify STACK item #3: the ACP v1 method names and wire shape. Per the user's decision (CONTEXT.md "Claude's Discretion" + the per-item assignment), this is a SPIKE (a running `initialize` handshake), not a doc-pin — the user explicitly wants empirical confirmation of the wire shape that is load-bearing for Phase 2.

Purpose: ACP is the primary interface (PROJECT.md). The exact method names (`initialize`, `session/new`, `session/prompt`, `session/update`), the newline-delimited framing (NOT Content-Length), and the `agentCapabilities` result field are what Phase 2's ACP adapter implements. STACK omitted the required `session/new` step — this spike confirms the correct lifecycle and records the Tier-A correction.

Output: `spikes/03-acp-handshake/main.go` (throwaway) and `spikes/03-acp-handshake/RESULT.md` (the D-02-shaped item-#3 draft with the redacted frame dump and the lifecycle correction) that Plan 05 folds into VERIFIED-FACTS.md.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-spike-re-verification/00-CONTEXT.md
@.planning/phases/00-spike-re-verification/00-RESEARCH.md
@AGENTS.md
@spikes/go.mod
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write and run the #3 ACP v1 handshake spike</name>
  <files>spikes/03-acp-handshake/main.go</files>
  <behavior>
    - Framing assertion: each frame sent/received is one JSON object terminated by \n; no Content-Length header is used
    - initialize assertion: the initialize response contains protocolVersion and an agentCapabilities object (field name exactly "agentCapabilities", NOT "capabilities"/"serverInfo")
    - session/new assertion: session/new with cwd (tempdir) + mcpServers:[] returns a result containing a sessionId
    - session/prompt assertion: after session/prompt, at least one session/update notification frame arrives before the session/prompt response, and the session/update carries a sessionUpdate discriminator (e.g. agent_message_chunk)
  </behavior>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §2 (the load-bearing framing rule: newline-delimited JSON, no embedded newlines, stdout MUST NOT carry non-ACP bytes; the full method list; initialize/session/new/session/prompt/session/update param + result shapes; the Tier-A correction that STACK omitted session/new; the protocolVersion string-vs-integer note; the agentCapabilities-not-capabilities note)
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-04 throwaway, D-07 tiers)
    - .planning/phases/00-spike-re-verification/00-VALIDATION.md row "00-03-0x"
    - spikes/go.mod (confirm the module)
    - AGENTS.md "Transport discipline" (stdout = ACP only — this spike IS an ACP peer, so its stdout legitimately carries ACP frames it SENDS; its DIAGNOSTIC output goes to stderr)
  </read_first>
  <action>
    Write a throwaway Go program (package main) under `spikes/03-acp-handshake/main.go` in module `github.com/djarvur/ass-guard-spikes`. It acts as an ACP CLIENT (or a paired client+server) that performs the v1 lifecycle empirically.

    Framing (RESEARCH.md §2): hand-rolled, ~trivial Go. Read frames with a `bufio.Scanner` over the peer's stdout (or stdin if the peer is the agent); write frames with `json.Encoder` followed by an explicit `\n`. NO Content-Length headers. Each frame is a JSON-RPC 2.0 request (`{jsonrpc,id,method,params}`), notification (`{jsonrpc,method,params}` — no id), or response (`{jsonrpc,id,result|error}`).

    Peer selection (executor's discretion per RESEARCH.md §2): the spike needs SOME reachable ACP v1 server. Acceptable options in priority order: (a) zcode itself if it speaks ACP and can be spawned; (b) another installed ACP agent; (c) a tiny in-process mock ACP server (a goroutine speaking the same framing) that responds to initialize/session/new/session/prompt with spec-shaped results — this proves the framing + method names against the spec even without a real peer. Pick whatever is reachable; the GOAL is confirming method names + wire shape, not a specific peer. Document which peer was used in the stderr footer.

    The exchange (all four steps required, RESEARCH.md §2 Tier-A correction):
    1. Send `initialize` with params `{protocolVersion: "1", clientInfo: {name:"ass-guard-spike", version:"0"}, clientCapabilities: {fs:{readTextFile:false,writeTextFile:false},terminal:false}}`. Read the response. Assert `result.protocolVersion` is present and `result.agentCapabilities` is an object (field name exactly `agentCapabilities`).
    2. Send `session/new` with params `{cwd: <a tempdir absolute path>, mcpServers: []}`. Read the response. Assert the result contains a `sessionId` (capture it).
    3. Send `session/prompt` with params `{sessionId: <captured>, prompt: [{type:"text", text:"reply with the single word: ok"}]}`. Read frames until the `session/prompt` response (`id` matches) arrives. Assert at least one `session/update` notification (method `session/update`) arrived in between, and that it carries an `update.sessionUpdate` discriminator. Optionally exercise `session/load` as a 5th step if cheap.
    4. Print to STDERR: each frame sent (method + redacted params) and received (method/result/error), a per-step PASS/FAIL, and the structured `=== SPIKE 03 RESULT ===` footer.

    Transport discipline (AGENTS.md): this spike speaks ACP on its OWN stdout/stdin when acting as the agent side, OR writes ACP frames to a peer subprocess when acting as client. In EITHER case, all DIAGNOSTIC logging (the frame dumps, PASS/FAIL, the result footer) goes to STDERR. If the spike spawns a peer subprocess and tees its stdout for assertion, that tee is captured to a buffer/file, not printed to the spike's own stdout. The spike's own stdout carries either nothing (pure client) or only legitimate ACP frames it is sending (peer mode) — never free-form diagnostics.

    Plausible failure tier (D-07): Tier A by default — if a method name differs from the spec (e.g. the peer expects `session/create` not `session/new`), record FAILED + the corrected name in Notes and CONTINUE. Escalate to Tier B only if a load-bearing method the architecture assumed is ABSENT from v1 entirely (e.g. v1 has no equivalent of session/prompt) — then STOP and surface the three Tier-B options. Research predicts Tier A at most (the methods are confirmed against the canonical spec; the only correction is the omitted session/new step, which the spec DOES include).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent/spikes &amp;&amp; go vet ./03-acp-handshake/ &amp;&amp; go run ./03-acp-handshake/ 2&gt;/tmp/spike03.out; grep -q '=== SPIKE 03 RESULT ===' /tmp/spike03.out &amp;&amp; grep -qi 'session/new' /tmp/spike03.out &amp;&amp; grep -qi 'agentCapabilities' /tmp/spike03.out &amp;&amp; echo SPIKE03_OK</automated>
  </verify>
  <done>
    `go run ./spikes/03-acp-handshake/` exits 0 (PASS) or non-zero with a Tier-A FAILED note; its stderr contains the structured result footer referencing `initialize`, `session/new`, `session/prompt`, `session/update`, and `agentCapabilities`; all diagnostic output is on stderr (transport discipline honored).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the item #3 finding to spikes/03-acp-handshake/RESULT.md</name>
  <files>spikes/03-acp-handshake/RESULT.md</files>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §2 (the canonical method list + framing rule to cite) and §8 (D-02 section template + D-03 checklist)
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-02, D-03, D-07)
    - the spike's stderr output from Task 1 (the redacted frame dump + PASS/FAIL)
  </read_first>
  <action>
    Write `spikes/03-acp-handshake/RESULT.md` as the DRAFT VERIFIED-FACTS.md content for item #3 (Plan 05 will fold this in verbatim-ish). This plan does NOT touch VERIFIED-FACTS.md — Plan 05 is the single writer of that file (keeps Wave-1 files_modified non-overlapping).

    Structure RESULT.md with the D-02 fields so Plan 05 can copy block-by-block: a heading `# #3. ACP v1 method names + wire shape`, then the seven fields:
    - `Fact`: quote STACK item #3 verbatim (the ACP v1 row: method names `initialize / session/prompt / session/update / session/load`, v1 stable / v2 Draft).
    - `Source`: STACK.md §"Open Verification Items" item #3 + the ACP table row (~line 33).
    - `Verified`: 2026-08-09.
    - `Verified against`: ACP spec v1 (agentclientprotocol.com/protocol/v1/) AND the peer actually exercised (name it: zcode / another agent / an in-process mock — whichever the spike used).
    - `Status`: `VERIFIED` if all four methods round-tripped; `FAILED` with a corrected method name in Notes if a name differed (Tier A); only escalate per D-07 Tier B if a method was structurally absent.
    - `Evidence`: a redacted frame dump (inline fenced block) — one representative frame each for the initialize request, initialize response (showing `agentCapabilities`), session/new request, session/new response (showing `sessionId`), session/prompt request, one session/update notification, and the session/prompt response. Sanitize per D-03: redact cwd paths, sessionIds, prompt content, any tokens; PRESERVE method names, field names (`protocolVersion`, `agentCapabilities`, `sessionId`, `sessionUpdate`), and the JSON-RPC structure. Redaction note in Evidence.
    - `Notes`: (a) the Tier-A correction — STACK omitted `session/new`; the correct v1 lifecycle is `initialize → session/new → session/prompt` (you cannot send session/prompt without a sessionId from session/new); (b) the framing rule — newline-delimited JSON, no Content-Length, no embedded newlines, stdout MUST NOT carry non-ACP bytes (this is the project transport discipline and is what spike #5 proves); (c) the result field is `agentCapabilities` NOT `capabilities`/`serverInfo` (differs from LSP/MCP); (d) `protocolVersion` is schema-string but example-integer — the spike sent `"1"`; note which the peer accepted; (e) which peer was exercised.

    Sanitization (D-03): grep RESULT.md for absolute paths and session-id UUIDs before commit — redact to `[REDACTED: path]` / `[REDACTED: uuid]`. Method names and field names are intentionally preserved.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; F=spikes/03-acp-handshake/RESULT.md &amp;&amp; grep -q '^# #3\.' "$F" &amp;&amp; grep -q '\*\*Status:\*\*' "$F" &amp;&amp; grep -qE 'VERIFIED|FAILED' "$F" &amp;&amp; grep -q 'session/new' "$F" &amp;&amp; grep -q 'agentCapabilities' "$F" &amp;&amp; echo RESULT3_OK</automated>
  </verify>
  <done>
    `spikes/03-acp-handshake/RESULT.md` has a `# #3.` heading with all seven D-02 fields; Status VERIFIED or FAILED-with-correction; Evidence shows the redacted frame dump with `session/new` and `agentCapabilities`; Notes record the Tier-A lifecycle correction; no un-redacted paths or session ids. Plan 05 folds this into VERIFIED-FACTS.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ACP peer subprocess ↔ spike | the spike may spawn or connect to an ACP server; frames cross a stdio/socket boundary |
| spike output (stderr) → spikes/03-acp-handshake/RESULT.md (committed) | captured frames may contain cwd paths, session ids, or prompt content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-06 | Information Disclosure | spikes/03-acp-handshake/RESULT.md (committed frame dump) | medium | mitigate | D-03 redaction: cwd paths, sessionIds, prompt text, tokens → `[REDACTED]`. Method names and field names are safe (public spec) and preserved. Plan 05 re-runs the grep over the merged VERIFIED-FACTS.md as a second barrier. |
| T-00-07 | Tampering / protocol violation | spikes/03-acp-handshake stdout discipline | low | accept | ACP frames on stdout are legitimate for this spike (it IS an ACP peer); diagnostics go to stderr. The assertion that "no non-ACP byte reaches stdout" is spike #5's job, not this spike's. Low residual risk. |
</threat_model>

<verification>
- `go run ./spikes/03-acp-handshake/` exits 0; structured PASS footer on stderr.
- The stderr references all four methods + `agentCapabilities`.
- `spikes/03-acp-handshake/RESULT.md` has the complete D-02-shaped item #3 draft; the `session/new` Tier-A correction is recorded in Notes.
- This plan does NOT create or modify `.planning/research/VERIFIED-FACTS.md` (Plan 05 is the single writer).
- No un-redacted absolute paths or session UUIDs in the committed file.
</verification>

<success_criteria>
- STACK item #3 closed empirically: the ACP v1 method names + wire shape (newline-delimited JSON-RPC, initialize → session/new → session/prompt → session/update) confirmed against a real exchange.
- The Tier-A correction (STACK omitted session/new; result field is agentCapabilities) recorded for Phase 2's ACP adapter.
- Transport discipline modeled: diagnostics on stderr only.
</success_criteria>

<output>
Create `.planning/phases/00-spike-re-verification/00-03-SUMMARY.md` when done.
</output>
