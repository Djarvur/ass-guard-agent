---
phase: 00-spike-re-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spikes/02-openai-toolschema/main.go
  - spikes/02-openai-toolschema/RESULT.md
autonomous: true
requirements: [STACK-Phase0-#2]
user_setup:
  - service: minimax
    why: "OpenAI-shape provider for the #2 tool-calling round-trip (one of two target providers)"
    env_vars:
      - name: MINIMAX_API_KEY
        source: "MiniMax platform console → API keys"
  - service: groq
    why: "OpenAI-shape provider for the #2 tool-calling round-trip (one of two target providers)"
    env_vars:
      - name: GROQ_API_KEY
        source: "Groq Console → API Keys"

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A throwaway Go spike performs a real OpenAI Chat Completions tool-call round-trip via sashabaranov/go-openai@v1.42.0 against at least one of {MiniMax M3, Groq} and prints the raw request/response JSON plus per-assertion PASS/FAIL to stderr"
    - "The spike never writes the API key or the Authorization header value to stdout, to stderr, or to any captured/committed artifact"
    - "STACK item #2 is recorded in spikes/02-openai-toolschema/RESULT.md (the per-spike evidence file) with Status VERIFIED or PARTIAL (PARTIAL if a provider key is unavailable), with the verified version v1.42.0 — Plan 05 folds this into VERIFIED-FACTS.md"
  artifacts:
    - spikes/02-openai-toolschema/main.go
    - spikes/02-openai-toolschema/RESULT.md
  key_links:
    - "spikes/02-openai-toolschema/main.go is package main inside module github.com/djarvur/ass-guard-spikes (compiles against the go.mod from plan 01)"
    - "spikes/02-openai-toolschema/RESULT.md is the D-02-shaped item-#2 draft Plan 05 reads to author VERIFIED-FACTS.md item #2 (Plan 05 is the single writer of VERIFIED-FACTS.md)"
---

<objective>
Empirically verify STACK item #2: that `sashabaranov/go-openai@v1.42.0` (the pinned latest tag) round-trips an OpenAI Chat Completions tool call against the OpenAI-shape providers ass-guard targets (MiniMax M3, Groq), and that the request/response schema matches the OpenAI Chat Completions spec (NOT the newer Responses API).

Purpose: item #2 is empirical — only a real round-trip is honest evidence. It is security-relevant: the spike holds provider API keys, so transport discipline (stderr only) and D-03 redaction are load-bearing. The verified schema (the `tools[].function` wrapper, the string-encoded `arguments`, the `tool`-role result message) is what Phase 1's OpenAI-shape provider adapter conforms to.

Output: `spikes/02-openai-toolschema/main.go` (throwaway, exit code = assertion result) and `spikes/02-openai-toolschema/RESULT.md` (the D-02-shaped item-#2 draft with redacted request/response and per-provider PASS/PARTIAL status) that Plan 05 folds into VERIFIED-FACTS.md.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-spike-re-verification/00-CONTEXT.md
@.planning/phases/00-spike-re-verification/00-RESEARCH.md
@AGENTS.md
@spikes/go.mod
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write and run the #2 go-openai tool-schema spike</name>
  <files>spikes/02-openai-toolschema/main.go</files>
  <behavior>
    - Schema assertion: the request serialized by go-openai's Tool/FunctionDefinition types has the Chat Completions shape — tools[].type == "function", tools[].function.{name,description,parameters}, tool_choice accepted
    - Round-trip assertion: with tool_choice forcing a call, the response tool_calls[0].function.arguments is a STRING parseable as JSON into the declared parameters schema (not a parsed object)
    - Follow-up assertion: a second turn carrying a role:"tool" message with tool_call_id + content is consumed and the model references the tool result
    - Redaction assertion: the key string and any Authorization/Bearer value never appear in the spike's stdout or stderr output
  </behavior>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §1 (pin go-openai@v1.42.0) and §4 (full Chat Completions schema: tools[] element with function wrapper, assistant tool_calls[], tool_choice values, tool-role result message, provider quirks for MiniMax M3 + Groq, explicit "DO NOT assert Responses API shape")
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-03 redaction, D-04 throwaway, D-07 tiers)
    - .planning/phases/00-spike-re-verification/00-VALIDATION.md row "00-02-0x" (assertion: API keys never logged to stdout; redacted before commit)
    - spikes/go.mod (confirm the module + pin this spike compiles against)
    - AGENTS.md "Transport discipline" (stdout = ACP only; this spike uses stdout for NOTHING — all output to stderr)
  </read_first>
  <action>
    Write a throwaway Go program (package main) under `spikes/02-openai-toolschema/main.go` in the module `github.com/djarvur/ass-guard-spikes` (created by plan 01). It performs a real OpenAI Chat Completions tool-call round-trip. Concretely:

    1. Build a `go-openai` client via `openai.NewClient` wrapped with `option.WithBaseURL`/config driven by env vars. Support TWO provider targets, each selected by a base URL + key + model slug env var: MiniMax M3 (`MINIMAX_API_KEY` + the MiniMax base URL + MiniMax M3 model slug) and Groq (`GROQ_API_KEY`, base URL `https://api.groq.com/openai/v1`, a Groq chat model slug). Also accept a generic `OPENAI_BASE_URL`/`OPENAI_API_KEY`/`OPENAI_MODEL` triple for any other OpenAI-shape endpoint. If a provider's key env var is unset, SKIP that provider (do not fail the whole spike — record PARTIAL with the reason). If NO provider key is set at all, exit non-zero with a clear stderr message (PARTIAL — operator must provision a key).

    2. Declare one minimal tool, e.g. `get_weather` with a single string parameter `location`. Use go-openai's `Tool`/`FunctionDefinition` types so the assertion exercises the library's own serialization (not hand-built JSON).

    3. Send a chat request that forces a tool call (`tool_choice` set to force/required, or a prompt that obviously needs the tool). Capture the raw request body go-openai sends and the raw response body.

    4. Assert (a) the request `tools[]` element serialized to `{"type":"function","function":{"name":...,"description":...,"parameters":{...}}}`; (b) the response `tool_calls[0].function.arguments` is a STRING that `json.Unmarshal`s into the declared `{location: string}` schema; (c) `tool_choice` was honored (a call was returned).

    5. Send a follow-up turn with a `role:"tool"` message carrying `tool_call_id` and the JSON-encoded result `{"temperature":25}`. Assert the model's reply references the tool result.

    6. Print ALL diagnostic output to STDERR only: the redacted raw request JSON, the redacted raw response JSON, a per-assertion PASS/FAIL list, and any provider quirk observed (e.g. does MiniMax accept `tool_choice:"required"`? is `arguments` a string or an object?). Write NOTHING to stdout. End with the structured `=== SPIKE 02 RESULT ===` ... `=== END ===` footer (RESEARCH.md §9) so the result is parseable.

    REDACTION (D-03, load-bearing security): before printing any request JSON, walk it and replace every `Authorization`/`Bearer`/`api_key`/`key` value with `[REDACTED]`. Never print the env var contents. The redacted request/response excerpts printed here are what get copied into VERIFIED-FACTS.md item #2 in Task 2.

    Transport discipline (AGENTS.md): this spike writes only to stderr. stdout is reserved for ACP frames — this spike emits none, modeling the project discipline from day one.

    Plausible failure tier (D-07): Tier A by default — if one provider round-trips and the other does not, record VERIFIED for the working provider and PARTIAL/FAILED with the quirk in Notes for the other, then CONTINUE. Escalate to Tier B only if BOTH providers fail AND the failure is a schema divergence go-openai cannot represent (the library cannot express the tool-calling shape the architecture requires) — then STOP, record FAILED, surface the three Tier-B options (the OpenAI-shape provider substrate is load-bearing for MiniMax M3 and Groq). Research predicts Tier A at most (Groq is a close OpenAI clone).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent/spikes &amp;&amp; go vet ./02-openai-toolschema/ &amp;&amp; (go run ./02-openai-toolschema/ 2&gt;&amp;1 | tee /tmp/spike02.out | grep -q '=== SPIKE 02 RESULT ===') &amp;&amp; (go run ./02-openai-toolschema/ 2&gt;/tmp/spike02b.out 1&gt;/tmp/spike02b_stdout.out; [ ! -s /tmp/spike02b_stdout.out ]) &amp;&amp; echo SPIKE02_STDERR_ONLY_OK</automated>
  </verify>
  <done>
    `go run ./spikes/02-openai-toolschema/` exits 0 when at least one provider key is set; its stderr contains the structured result footer with PASS lines for the schema/round-trip/follow-up assertions and the redacted request+response JSON; its stdout is EMPTY (transport discipline honored); the API key never appears in any output or captured file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the item #2 finding to spikes/02-openai-toolschema/RESULT.md</name>
  <files>spikes/02-openai-toolschema/RESULT.md</files>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §1 (the go-openai v1.42.0 pin + pkg.go.dev cadence nuance to record in Notes) and §8 (D-02 section template + D-03 sanitization checklist)
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-02 field schema, D-03 sanitization, D-07 tier semantics)
    - the spike's stderr output from Task 1 (the redacted request/response excerpt + PASS/PARTIAL lines)
  </read_first>
  <action>
    Write `spikes/02-openai-toolschema/RESULT.md` as the DRAFT VERIFIED-FACTS.md content for item #2 (Plan 05 will fold this in verbatim-ish). This plan does NOT touch VERIFIED-FACTS.md — Plan 05 is the single writer of that file (keeps Wave-1 files_modified non-overlapping).

    Structure RESULT.md with the D-02 fields so Plan 05 can copy block-by-block: a heading `# #2. sashabaranov/go-openai tool-calling schema (OpenAI-shape providers)`, then the seven fields:
    - `Fact`: quote STACK item #2 verbatim (the `sashabaranov/go-openai` row: latest 1.x, "verify the tool-calling schema matches the target provider's expectation during Phase 0").
    - `Source`: STACK.md §"Open Verification Items" item #2 + the go-openai table row (~line 36).
    - `Verified`: 2026-08-09.
    - `Verified against`: `github.com/sashabaranov/go-openai@v1.42.0` (released 2026-08-02), against provider(s) actually round-tripped (name them: MiniMax M3 and/or Groq, with the model slugs used).
    - `Status`: `VERIFIED` if at least one provider round-tripped all assertions; `PARTIAL` if keys were unavailable for one or both (record the reason — "operator had no MINIMAX_API_KEY provisioned" — and note which provider DID pass, if any). Per VALIDATION.md, PARTIAL is the Tier-A fallback when a key is missing.
    - `Evidence`: the redacted request JSON and response JSON from the spike (inline fenced blocks), 5-10 representative lines each, sanitized per D-03 (Authorization/Bearer/key values → `[REDACTED]`; keep the `tools[].function` wrapper shape, the string-typed `arguments`, and the `tool`-role result message — these ARE the schema being verified). Include the per-assertion PASS/PARTIAL lines. Redaction note: "request+response JSON; Authorization header and api key redacted; tool-call schema shape preserved."
    - `Notes`: record (a) the Chat Completions vs Responses API clarification (RESEARCH.md §4 — the spike correctly asserts Chat Completions, the shape go-openai targets); (b) the pkg.go.dev cadence nuance (RESEARCH.md §1 — pkg.go.dev indexing lags ~1 year but the GitHub repo is active at v1.42.0; Phase 1 should not re-litigate this); (c) any provider quirk observed (MiniMax/Groq `tool_choice` behavior, `arguments` string-vs-object); (d) the Tier disposition applied (Tier A if PARTIAL, with the reason).

    Sanitization (D-03, security-critical for this item): before commit, grep RESULT.md for the literal key value and for `sk-`/`Bearer ` patterns — must return nothing. Header NAMES and JSON schema keys are intentionally preserved. (Plan 05 re-runs this grep over the merged VERIFIED-FACTS.md as a second barrier.)
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; F=spikes/02-openai-toolschema/RESULT.md &amp;&amp; grep -q '^# #2\.' "$F" &amp;&amp; grep -q '\*\*Status:\*\*' "$F" &amp;&amp; grep -qE 'VERIFIED|PARTIAL' "$F" &amp;&amp; grep -q 'v1.42.0' "$F" &amp;&amp; grep -qi 'tools\[\].function\|tool_calls' "$F" &amp;&amp; ! grep -qiE 'Bearer [A-Za-z0-9]{20}|sk-[A-Za-z0-9]{10}' "$F" &amp;&amp; echo RESULT2_OK</automated>
  </verify>
  <done>
    `spikes/02-openai-toolschema/RESULT.md` has a `# #2.` heading with all seven D-02 fields populated; Status is VERIFIED or PARTIAL with the reason; `Verified against` cites `go-openai@v1.42.0`; Evidence shows the Chat Completions tool-call schema (tools[].function wrapper, string arguments, tool-role result); no Authorization value or `sk-` key in the file. Plan 05 folds this into VERIFIED-FACTS.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars (provider keys) → spike process | MiniMax/Groq API keys enter the process via env; the spike must never echo them |
| spike output (stderr) → spikes/02-openai-toolschema/RESULT.md (committed) | captured request JSON crosses into a committed evidence file; it can carry the Authorization header value if not redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-03 | Information Disclosure | spikes/02-openai-toolschema API key handling | high | mitigate | Read key from env var only; never print the env var or the key; never log the `Authorization`/`Bearer` value; walk the captured request JSON and replace `Authorization`/`api_key`/`key` values with `[REDACTED]` BEFORE printing to stderr. |
| T-00-04 | Information Disclosure | spikes/02-openai-toolschema/RESULT.md (committed evidence) | high | mitigate | D-03 redaction applied to the embedded request/response before commit; grep RESULT.md for `sk-`/`Bearer <token>` patterns before declaring done (must return nothing). Plan 05 re-runs the grep over the merged VERIFIED-FACTS.md as a second barrier. Schema keys and header names are safe and preserved. |
| T-00-05 | Tampering / protocol violation | spikes/02-openai-toolschema stdout | medium | mitigate | The spike writes NOTHING to stdout (transport discipline — stdout is reserved for ACP frames). All output to stderr. The Task-1 verify asserts stdout is empty. |
</threat_model>

<verification>
- `go run ./spikes/02-openai-toolschema/` exits 0 with at least one provider key set; structured PASS footer on stderr.
- The spike's stdout is empty (transport discipline).
- No API key or Authorization value in the spike's stderr or in spikes/02-openai-toolschema/RESULT.md.
- RESULT.md has the complete D-02-shaped item #2 draft; Status VERIFIED or PARTIAL with reason.
- This plan does NOT create or modify `.planning/research/VERIFIED-FACTS.md` (Plan 05 is the single writer).
</verification>

<success_criteria>
- STACK item #2 closed with empirical evidence: go-openai@v1.42.0 round-trips a Chat Completions tool call against the OpenAI-shape provider(s).
- The verified schema (tools[].function wrapper, string arguments, tool-role result) is recorded for Phase 1's OpenAI-shape adapter.
- No secret leakage at any stage (transport discipline + D-03 redaction).
</success_criteria>

<output>
Create `.planning/phases/00-spike-re-verification/00-02-SUMMARY.md` when done.
</output>
