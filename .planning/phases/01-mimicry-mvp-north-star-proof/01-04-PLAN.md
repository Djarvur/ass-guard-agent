---
phase: 1
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - internal/shaper/shaper.go
  - internal/shaper/shaper_test.go
  - internal/shaper/prof02_test.go
  - internal/provider/openai.go
  - internal/provider/openai_test.go
  - internal/provider/provider_test.go
  - internal/provider/conformance_test.go
requirements: [MIMC-01, PROF-02, PROV-01, PROV-02, PROV-03]
autonomous: true   # OpenAI adapter unit-tested with httptest mocks; live round-trip is Phase-0 #2 PARTIAL (deferred)
---

# Plan 01-04 — Shaper completeness, PROF-02 enforcement, OpenAI-shape adapter, provider conformance

## Truths

- Implements D-09 (full-fidelity Shaper: byte-faithful TIER-1 fields via native SDK types + escape hatch), D-10 (schema adapter at the catalog/Shaper boundary), D-11 (PROF-02 structural enforcement: synthetic-profile conformance test + `grep -rn zcode internal/shaper/` lint — no zcode-specific code paths in the chokepoint). Completes MIMC-01 (the mimicry chokepoint at full fidelity).
- Carries PROF-02 (no zcode-specific Shaper code — structural guarantee), PROV-01 (both Anthropic-shape and OpenAI-shape supported), PROV-02 (common `Provider` interface with `TranslateToInternal`/`TranslateFromInternal`), PROV-03 (Anthropic adapter via `anthropic-sdk-go` with Z.ai base URL; OpenAI adapter via `go-openai@v1.42.0` Chat Completions, NOT Responses API).

<objective>
Complete the mimicry mechanism: (1) bring the Profile Shaper to full fidelity (byte-faithful system blocks, all 77 tools, thinking, tool_choice, the 12 identity headers — round-trip-verified), (2) enforce PROF-02 ("no zcode-specific code paths in the Shaper") structurally via the synthetic-profile conformance test + lint (D-11), (3) build the OpenAI-shape adapter (PROV-03 secondary — Chat Completions, not Responses API), and (4) prove both adapters round-trip via common-interface conformance tests (PROV-02). This plan makes MIMC-01 structurally complete and PROF-02 a guarantee, not a code-review hope.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/01-mimicry-mvp-north-star-proof/01-CONTEXT.md
@.planning/phases/01-mimicry-mvp-north-star-proof/01-RESEARCH.md
@.planning/phases/01-mimicry-mvp-north-star-proof/01-PATTERNS.md
@.planning/phases/01-mimicry-mvp-north-star-proof/01-01-PLAN.md
@.planning/phases/01-mimicry-mvp-north-star-proof/01-02-PLAN.md
@.planning/phases/01-mimicry-mvp-north-star-proof/01-03-PLAN.md
@.planning/research/VERIFIED-FACTS.md
</context>

<must_haves>
  <truths>
    - The Shaper reproduces the zcode profile's TIER-1 fields byte-faithfully (system blocks, tool schemas, thinking, tool_choice) — round-trip-verified.
    - PROF-02 is a structural guarantee: a synthetic (non-zcode) profile loads through the SAME Shaper code path and shapes correctly; no `if profile.Name=="zcode"` branch exists.
    - Both Anthropic-shape and OpenAI-shape adapters implement the common `Provider` interface (PROV-02) with `TranslateToInternal`/`TranslateFromInternal` tool-call translation.
    - The OpenAI-shape adapter targets Chat Completions (NOT Responses API — VERIFIED-FACTS.md item #2).
    - The Shaper contains no "zcode" string outside test fixtures (D-11 lint).
  </truths>
  <goals>
    - A round-trip fidelity test proves the shaped request's serialized `system[]` is byte-equal to the profile's `system/block-N.txt`.
    - A synthetic-profile conformance test (D-11) breaks on any zcode-specific Shaper branch.
    - Both adapters pass common-interface conformance tests (PROV-02).
  </goals>
</must_haves>

<tasks>

<task type="tdd" id="T1">
  <action>
    Bring `internal/shaper` to full fidelity + add the round-trip fidelity test. Extend the Shaper tests (in `shaper_test.go`) to assert: (a) the shaped `MessageRequest`, when serialized via the SDK's JSON marshaler, has a `system` array byte-equal to `profile.System` (the 3 blocks byte-faithful — RESEARCH §13.4); (b) the `tools` array has exactly 77 entries with names matching `profile.Tools`; (c) `thinking` and `tool_choice` match `profile.Thinking`/`profile.ToolChoice` byte-for-byte (the SDK may normalize — assert against the SDK-serialized form, or use the escape hatch if normalization mangles); (d) the returned `[]option.RequestOption` contains exactly 12 `option.WithHeader` calls, one per `profile.Headers` entry with the rendered value. If the SDK mangles any TIER-1 field (e.g. TextBlockParam whitespace normalization), document it and use the raw-body escape hatch (RESEARCH §5.1/§13.4) to restore byte-faithfulness. Write the fidelity assertions FIRST (RED), fix the Shaper until GREEN.
  </action>
  <read_first>
    - internal/shaper/shaper.go (from Plan 01-01 T4 — the current Shaper to bring to full fidelity)
    - profiles/zcode/ (the extracted artifact from Plan 01-02 — system/block-{0,1,2}.txt, tools.json, thinking.json, tool_choice.json, identity.yaml)
    - ~/go/pkg/mod/github.com/anthropics/anthropic-sdk-go@v1.61.0/message.go (TextBlockParam line 5639, ToolParam line 6696 — confirm serialization fidelity)
    - .planning/phases/01-mimicry-mvp-north-star-proof/01-RESEARCH.md (§5.1 the escape hatch; §13.4 byte-faithfulness vs SDK normalization; §13.5 thinking/tool_choice exact reproduction)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/shaper/... -run TestFidelity -v 2>&1 | tee /dev/stderr | grep -q '^--- PASS'</verify>
  <acceptance_criteria>
    - `internal/shaper/shaper_test.go` contains a `TestFidelity*` suite asserting: serialized `system[]` byte-equal to the profile's blocks; `tools` has 77 entries with matching names; `thinking`/`tool_choice` byte-match; 12 `option.WithHeader` options emitted with rendered values
    - The tests pass against the extracted zcode profile (Plan 01-02's artifact) — RED→GREEN
    - If the SDK normalizes any TIER-1 field, the escape hatch (raw body merge) is applied and a code comment documents WHY; the byte-fidelity assertion still holds
    - `go test ./internal/shaper/... -run TestFidelity` exits 0
  </acceptance_criteria>
</task>

<task type="tdd" id="T2">
  <action>
    Add the PROF-02 structural enforcement: `internal/shaper/prof02_test.go` (the synthetic-profile conformance test, D-11) and a grep-based lint check. Create the `profiles/synthetic/` fixture (a minimal non-zcode profile: system blocks "You are a synthetic test agent." × 3, tool names `synth_tool_a`/`synth_tool_b` with simple schemas, headers `X-Synth-Test`/`X-Synth-Run-Id`, a different model `synth-model`). The test loads `profiles/synthetic/` through the SAME `Shaper.Shape` code path as zcode and asserts the shaped request carries the SYNTHETIC fields (not zcode fields): system blocks match the synthetic text, tools are `[synth_tool_a, synth_tool_b]`, headers include `X-Synth-Test`. Add a test helper that runs `exec.Command("grep", "-rn", "zcode", "internal/shaper/")` and asserts the output is empty (excluding `_test.go` files). Write FIRST (RED — the test fails until the fixture exists and the Shaper is profile-agnostic), then GREEN.
  </action>
  <read_first>
    - .planning/phases/01-mimicry-mvp-north-star-proof/01-CONTEXT.md (D-11 the synthetic-profile conformance test + grep lint; PROF-02 wording)
    - .planning/phases/01-mimicry-mvp-north-star-proof/01-RESEARCH.md (§5.3 the enforcement design; the lint scope is `internal/shaper/` specifically — `internal/profile/loader.go` MAY contain "zcode" as data-driven name lookup)
    - internal/shaper/shaper.go (confirm no zcode-specific branches exist or remain)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/shaper/... -run TestPROF02 -v 2>&1 | tee /dev/stderr | grep -q '^--- PASS' && ! grep -rn "zcode" internal/shaper/*.go | grep -v _test.go</verify>
  <acceptance_criteria>
    - `profiles/synthetic/` exists with `profile.yaml` (name: synthetic, model: synth-model), `system/block-{0,1,2}.txt` ("You are a synthetic test agent."), `tools.json` (synth_tool_a, synth_tool_b), `identity.yaml` (X-Synth-Test, X-Synth-Run-Id)
    - `internal/shaper/prof02_test.go` loads the synthetic profile, runs `Shaper.Shape`, and asserts the shaped request carries the SYNTHETIC system text, the 2 synthetic tools, and the synthetic headers (NOT zcode's)
    - A lint assertion in the test runs `grep -rn "zcode" internal/shaper/*.go | grep -v _test.go` and fails if any match — PROF-02 is a structural guarantee
    - `go test ./internal/shaper/... -run TestPROF02` exits 0
    - `! grep -rn "zcode" internal/shaper/*.go | grep -v _test.go` (shell check, mirrors the test's lint)
  </acceptance_criteria>
</task>

<task type="tdd" id="T3">
  <action>
    Build `internal/provider/openai.go` — the OpenAI-shape adapter (PROV-03 secondary) using `github.com/sashabaranov/go-openai@v1.42.0`. Implement `OpenAIProvider` satisfying the `Provider` interface: construct `openai.NewClientWithConfig(openai.ClientConfig{BaseURL: cfg.BaseURL, HTTPClient: ...})` (configurable base URL — PROV-01), build a `ChatCompletionRequest` with `Messages`, `Tools []openai.Tool` (each `Tool{Type: ToolTypeFunction, Function: &FunctionDefinition{Name, Description, Parameters}}` — the Chat Completions `tools[].function` wrapper per VERIFIED-FACTS.md item #2), `ToolChoice`, `Model`, call `client.CreateChatCompletion(ctx, req)`, parse the response's `Choices[0].Message.ToolCalls` (each `ToolCall.Function.Arguments` is a JSON-encoded STRING — parse into `json.RawMessage`) into `[]ToolCall{Name, Input}`. This is the `TranslateToInternal` direction (OpenAI → internal). Implement `TranslateFromInternal` for the tool-result follow-up message shape (`role:"tool"`, `tool_call_id`, JSON-string content — VERIFIED-FACTS.md item #2). Write adapter tests FIRST (RED) with `httptest` mocking the OpenAI Chat Completions endpoint (canned response with `tool_calls`), then implement (GREEN). MUST target Chat Completions (`/chat/completions`), NOT the Responses API.
  </action>
  <read_first>
    - spikes/02-openai-toolschema/main.go (the WHOLE spike is the reference: lines 114-141 buildWeatherToolRequest, the round-trip, the parse; the `=== SPIKE 02 RESULT ===` assertions for the schema shape)
    - .planning/research/VERIFIED-FACTS.md (item #2: Chat Completions shape; tools[].function wrapper; string-typed arguments; tool-role result message; NOT Responses API)
    - ~/go/pkg/mod/github.com/sashabaranov/go-openai@v1.42.0/ (the client API: NewClientWithConfig, ClientConfig.BaseURL, ChatCompletionRequest, Tool, FunctionDefinition, ToolCall)
    - .planning/phases/01-mimicry-mvp-north-star-proof/01-RESEARCH.md (§6.3 the OpenAI-shape adapter scope — Phase-1 conformance only, not on the parity path)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/provider/... -run TestOpenAI -v 2>&1 | tee /dev/stderr | grep -q '^--- PASS'</verify>
  <acceptance_criteria>
    - `internal/provider/openai.go` defines `OpenAIProvider` implementing `Provider` via `go-openai@v1.42.0`
    - The client is constructed with a configurable `BaseURL` (PROV-01)
    - `Send` builds a `ChatCompletionRequest` with the `tools[].function` wrapper (each tool `FunctionDefinition{Name, Description, Parameters}`), calls `/chat/completions` (NOT a Responses-API endpoint)
    - `Send` parses `Choices[0].Message.ToolCalls` — each `ToolCall.Function.Arguments` (a JSON-encoded string) is parsed into `ToolCall.Input json.RawMessage`
    - `TranslateFromInternal` builds the tool-result follow-up: `role:"tool"`, `tool_call_id`, JSON-string content (VERIFIED-FACTS.md item #2)
    - `internal/provider/openai_test.go` (RED→GREEN) uses `httptest.NewServer` mocking Chat Completions and asserts: request body has the `tools[].function` wrapper; a canned `tool_calls` response parses to `[{Name, Input}]`; the result-message shape serializes correctly
    - `go test ./internal/provider/... -run TestOpenAI` exits 0 (no live network — mock server)
    - No reference to the Responses API (`output[].function_call`) anywhere in the adapter
  </acceptance_criteria>
</task>

<task type="tdd" id="T4">
  <action>
    Build `internal/provider/conformance_test.go` — the PROV-02 round-trip conformance tests proving both adapters implement the common `Provider` interface correctly. Define a table-driven test that, for each provider (`AnthropicProvider`, `OpenAIProvider`), runs the same scenarios via the `Provider` interface: (a) given a profile + a user message, `Send` returns a `Response` with the expected `ToolCalls` (using each adapter's mock server); (b) `TranslateToInternal` maps the provider's native tool-call representation to `[]ToolCall{Name, Input}` correctly; (c) `TranslateFromInternal` builds the correct tool-result follow-up for that provider's shape. The test asserts the COMMON contract holds — both adapters are drop-in interchangeable through the interface (PROV-02). Write FIRST (RED), then ensure both adapters satisfy it (GREEN — may require minor adapter adjustments).
  </action>
  <read_first>
    - internal/provider/provider.go (the common interface — Plan 01-01 T5)
    - internal/provider/anthropic.go, internal/provider/openai.go (both adapters — confirm they satisfy the interface)
    - .planning/REQUIREMENTS.md (PROV-02 "common Provider interface with TranslateToInternal/TranslateFromInternal ... verified by round-trip conformance tests")
    - .planning/phases/01-mimicry-mvp-north-star-proof/01-RESEARCH.md (§6.1 the interface + the zcode-normalized ToolCall shape)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/provider/... -run TestConformance -v 2>&1 | tee /dev/stderr | grep -q '^--- PASS'</verify>
  <acceptance_criteria>
    - `internal/provider/conformance_test.go` is table-driven over `[AnthropicProvider, OpenAIProvider]`
    - Each provider passes: `Send` returns expected ToolCalls via its mock; `TranslateToInternal` maps native → `[]ToolCall{Name, Input}`; `TranslateFromInternal` builds the provider-correct tool-result message
    - The test asserts the COMMON contract — both adapters are interchangeable through the `Provider` interface (PROV-02)
    - `go test ./internal/provider/... -run TestConformance` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `go test ./internal/shaper/... ./internal/provider/...` exits 0
2. The Shaper fidelity test proves byte-faithful TIER-1 reproduction (system blocks, tools, thinking, tool_choice)
3. The PROF-02 synthetic-profile test passes; the grep lint returns nothing
4. The OpenAI adapter targets Chat Completions (no Responses API references)
5. Both adapters pass the common-interface conformance tests
6. `go vet ./internal/shaper/... ./internal/provider/...` exits 0
</verification>

<success_criteria>
- The Profile Shaper reproduces all TIER-1 fields byte-faithfully (MIMC-01 complete).
- PROF-02 is structurally enforced — a synthetic profile shapes correctly; no zcode-specific code in the chokepoint (D-11).
- Both Anthropic-shape and OpenAI-shape adapters implement the common `Provider` interface (PROV-01, PROV-02).
- The OpenAI-shape adapter uses Chat Completions via `go-openai@v1.42.0` (PROV-03).
</success_criteria>

<threat_model>
- **T-01-07 (LOW — Shaper fidelity drift undetected):** if the round-trip fidelity test is weak, a Shaper regression could silently change the shaped request and break mimicry without failing tests. Mitigation: T1's fidelity assertions are byte-equality on TIER-1 fields (not structural/loose); the test runs against the extracted profile; any normalization the SDK applies is caught and either asserted-against or escape-hatched. Belt-and-suspenders: Plan 01-06's parity test is the final gate — a Shaper drift that escapes T1 will fail parity.
- **T-01-08 (LOW — OpenAI adapter Responses-API drift):** a future contributor could accidentally use the Responses API shape. Mitigation: T3's test asserts the Chat Completions endpoint + shape; the acceptance criterion explicitly checks "No reference to the Responses API"; the spike 02 reference (study, not import) pins the correct shape.
</threat_model>
