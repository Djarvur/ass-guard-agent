---
phase: 23-seed-gaps-close-out
plan: 05
type: execute
wave: 4
depends_on: [23-04]
files_modified:
  - internal/runtime/commands.go
  - internal/runtime/commands_test.go
  - internal/runtime/runtime.go
autonomous: false
requirements: [SEEDG-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "/undo restores the last checkpoint of the current session with ZERO provider calls, D-05 class-B output shape (echo user_message_chunk -> agent_message_chunk output -> stopReason end_turn), and a durable local_command record with args verbatim (SEEDG-03, 20-01/20-02 contract)"
    - "Repeated /undo walks the stack backward and /undo N jumps N steps; each restore snapshots current state first, so the walk itself is reversible (D-11, D-09); depth is bounded naturally by the age+count GC"
    - "/undo with an ACTIVE turn or chain auto-cancels via the existing cancel contract (clean drain), THEN restores — classified BEFORE the session turn mutex so the handler never waits while holding it (D-12, Pitfall 4)"
    - "The pre-restore snapshot ALWAYS precedes the restore and its failure aborts the undo (fail-closed, D-09/D-12 ordering)"
    - "Nested repositories refuse /undo outright — no auto-cancel path exists for the refusal (D-12 letter)"
    - "The restored workspace is byte-identical to the checkpointed state (treeMap-proven, ROADMAP success criterion 4)"
  artifacts:
    - /undo as the 14th RESERVED class-B name in internal/runtime/commands.go (undo)
    - undoCommand handler + parseUndoDepth
    - Runner active-turn cancel registry (parkedCancels structural analog) in runtime.go
    - class-B resolve slot filled in the 23-02 classifier (before steering enqueue)
    - internal/runtime/commands_test.go /undo battery (TestClassB-style)
  key_links:
    - "classifier (pre-turnMu) -> nested check -> snapshot -> cancel -> turnMu acquire -> restore: reorder ANY of these and either a turn dies unsnapshotted or the handler self-deadlocks (D-12 ordering + Pitfall 4; -race will NOT catch the deadlock — the behavioral test does)"
    - "Store.List() ordering -> D-11 walk target selection: pre-restore entries participate, so the walk progresses through both families"
  flagged_assumptions:
    - "SEEDG-03 probe row (category: unclassified, status: unresolved — FLAGGED): manual-review edge cases are pinned by battery — /undo with empty store (loud nothing-to-restore, zero provider calls), /undo N edge parsing (0/negative -> 1, non-numeric -> D-05 error text, beyond-depth -> clamps to oldest), /undo across session boundary (targets THIS session's entries only), undo-of-undo reversibility."
  prohibitions:
    - "/undo must never fire a model turn and must never send checkpoint/restore text to the provider: zero Stream calls is a battery-pinned invariant, and the local_command record rides the REDACTED path."
---

<objective>
SEEDG-03: /undo as the fourteenth RESERVED class-B command — D-11 stack walk over the checkpoint store, D-12 auto-cancel-then-restore composed from the existing cancel contract, nested-repo refusal without an auto path — plus the live-Zed operator confirmation of the phase's two interactive surfaces (steering + /undo).

Purpose: /undo is the operator's one-keystroke safety net; its auto-cancel path is the compound destructive action the CONTEXT flags costly, so ordering (snapshot before cancel's state changes, classification before the mutex) is the whole game.

Output: commands.go with /undo live, the cancel registry in runtime.go, the /undo battery, and the recorded live-Zed UAT.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-seed-gaps-close-out/23-CONTEXT.md
@.planning/phases/23-seed-gaps-close-out/23-RESEARCH.md
@.planning/phases/23-seed-gaps-close-out/23-PATTERNS.md
@.planning/phases/23-seed-gaps-close-out/23-04-SUMMARY.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-01-PLAN.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-02-PLAN.md

@internal/runtime/runtime.go
@internal/checkpoint/store.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /undo registration — 14th RESERVED name, D-11 walk, D-05 shape (idle path)</name>
  <files>internal/runtime/commands.go, internal/runtime/commands_test.go, internal/runtime/runtime.go</files>
  <precondition>Phase 20 class-B machinery per the LOCKED PLAN CONTRACTS (not shipped code): .planning/phases/20-built-in-commands-skills-per-agent-model/20-01-PLAN.md Task 2 (buildChain + the thirteen-name RESERVED set: help status cost mcp memory permissions doctor config model clear resume compact init) and 20-02-PLAN.md (the twelve class-B handlers + TestClassB battery in commands_test.go). If internal/runtime/commands.go exists, this task MODIFIES it (adds undo to the RESERVED set + one handler); if it does NOT exist on the branch, HALT and surface to the orchestrator — do not re-create Phase 20's machinery under this plan. The intercept placement contract (Runner.Run after routeAskReply, before the engine branch — 20-01 key_links) and the D-05 output shape belong to those plans.</precondition>
  <behavior>
    - /undo (idle session, checkpoints exist) restores the newest checkpoint of THIS session: zero provider Stream calls; output follows D-05 (user_message_chunk echo, agent_message_chunk summary naming the restored id and the pre-restore snapshot id, stopReason end_turn); a local_command line lands with key "undo" and args verbatim.
    - Repeated /undo walks back one entry per invocation (D-11): because every restore first snapshots (D-09 pre-restore family), the second /undo restores the state before the first /undo — undo-of-undo works.
    - /undo N jumps N entries back over List() ordering (pre-restore entries included in the walk).
    - Edge parsing (CONTEXT discretion): /undo 0 and negative N behave as /undo 1; non-numeric args produce the D-05-shaped error text (no provider call, still a local_command record); beyond-depth clamps to the oldest available entry.
    - /undo with an empty store returns the loud nothing-to-restore text via D-05 shape.
    - /undo targets ONLY the current session's entries — another session's checkpoints are never restored.
  </behavior>
  <action>
Test-first in commands_test.go (the battery follows the checkpoint_session_test.go fixture style and joins the TestClassB family; file availability is governed solely by the plan-level precondition — if Phase 20's machinery is absent on the branch, HALT there and surface to the orchestrator rather than scaffolding Phase 20 test internals under this plan). Then: add undo to the RESERVED name set in commands.go (the set becomes fourteen — 20-01's D-01 shadow-check warning already covers discovered-file collisions from this commit onward, per its shipped warning mechanism); implement undoCommand: parseUndoDepth(args) per the edge rules above; target selection walks r.ckpt List() entries filtered to the current session id, N steps from the newest; idle-path sequence = nested-repo guard (23-03 RestoreGuard — refuse outright with the named paths) -> SnapshotPreRestore (fail-closed: on error the undo aborts with the D-05 error text) -> Restore(target). Fill the class-B resolve slot the 23-02 classifier reserved in runtime.go (position: after the pending-ask route, before steering enqueue — Pattern 6): an /undo invocation NEVER becomes steering text (Pitfall 11's warning sign is exactly a mid-turn /undo reaching the model). Degraded sessions (nil Runner store, 23-04): /undo reports "unavailable: checkpoint store disabled" through D-05 shape — never silent (Pitfall 9).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestClassB.*Undo|TestUndoWalk' -v</automated>
  </verify>
  <acceptance_criteria>
    - Idle-path battery green: zero Stream calls per /undo (fake-provider counter), D-05 output frame sequence asserted, local_command line asserted with args verbatim.
    - Walk battery green: three checkpoints + two /undo calls land two steps back; /undo 2 from fresh lands the same state; undo-of-undo restores the pre-undo tree (treeMap byte-identical).
    - Edge battery green: 0/negative -> 1, non-numeric -> error text, beyond-depth -> oldest, empty store -> loud nothing-to-restore.
    - Cross-session test: entries of a second session id are invisible to the walk.
  </acceptance_criteria>
  <done>/undo is a live class-B command with the D-11 walk, correct D-05 shape, verbatim local_command records, and loud degradation — on the idle path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-12 auto-cancel-then-restore — cancel registry, ordering, fail-closed</name>
  <files>internal/runtime/runtime.go, internal/runtime/commands.go, internal/runtime/commands_test.go</files>
  <precondition>Same Phase 20 locked-contract mark as Task 1 (20-01/20-02 plan files + sections); additionally 18-CONTEXT.md D-12 (cancel-and-drain family) and the 23-04 restoreBlockers guard must be live.</precondition>
  <behavior>
    - /undo while a client turn runs: the turn cancels via the existing cancel contract (cancelled ctx -> recordCanceled -> stopReason "cancelled" with the updates-before-response barrier), the undo completes, and the handler NEVER waits for the turn while holding turnMu (the behavioral deadlock test — Pitfall 4).
    - /undo while an engine chain is parked (chainCount > 0, no mutex): the chain cancels via the Runner's parked-chain cancel path and the undo completes.
    - Steering enqueued while that chain sat parked (the 23-02 classifier enqueue path) resolves cancelled-normal when the chain cancel fires — the parked teardown has no runTurn exit, so the teardown step resolves the queue itself; nothing zombie-delivers into the next unrelated turn's first boundary (23-01 cancelled-exit truth extended to this exit class).
    - Ordering: the pre-restore snapshot happens BEFORE the cancel is initiated (D-12: the snapshot must precede the cancel's state changes where ordering permits) and its failure aborts the undo WITHOUT cancelling anything (fail-closed).
    - Nested-repo presence + active turn: refusal wins over auto-cancel — nothing is cancelled, nothing is restored, refusal names the paths (D-12: no auto path for the nested case).
    - The cancelled turn's queued steering items (23-01 watermark) resolve cancelled-normal, never delivering to a later window.
  </behavior>
  <action>
Test-first in commands_test.go: the active-turn /undo test uses a fake provider blocked mid-stream — /undo must complete WITHOUT the provider releasing (the prompt-return-vs-turn-death assertion), and the blocked turn must end cancelled. Implementation: (1) add a Runner-level per-session active-turn cancel registry (the structural analog of the parkedCancels family, runtime.go:885-918: map keyed by sessionID holding the turn ctx's CancelFunc, registered when Run starts an actual turn, unregistered on exit) — this is the runner-side half of the existing cancel contract, whose ACP half (st.cancelTurn) stays untouched; (2) in undoCommand's active path, the sequence is strictly: nested guard -> restoreBlockers consultation -> SnapshotPreRestore (fail-closed) -> cancel (registry for the client turn + the parked-chain cancel for chains) -> acquire turnMu (the clean drain — the turn finishes dying under the mutex the undo now takes) -> Restore(target) -> D-05 output naming restored id + pre-restore snapshot id. Classification for the active path happens in the pre-mutex classifier (23-02 slot) — the undo handler itself only runs the sequence after the classifier routed it there; the mutex is acquired INSIDE the sequence, after the cancel. (3) Chain-teardown queue resolution: the parked-chain cancel step (cancelParkedChains, runtime.go:905-914) has no runTurn exit, so the recordCanceled funnel (23-01) never fires on this path — that step therefore resolves the session's SteerQueue itself (CancelAll through the highest assigned ticket), reached via the queue handle the 23-02 sessionFor wiring already gives the Runner (retain it where the Runner keeps per-session state; do not widen Session's exported surface for this), immediately after cancelling the session's chains — items enqueued while the chain sat parked resolve cancelled-normal and can never zombie-deliver into a later turn's window. The same teardown-side resolution closes the panic-recover residual (session.go:357-364) as a one-line rider on the registry: runTurn recovers its own panics, so the Runner-side unregister-on-exit observes every exit class (normal, cancelled, error, panic-recovered) as a plain return — the unregister step resolves the queue too (idempotent: CancelAll returns 0 when the recordCanceled funnel already emptied it at mid-turn cancel sites). Pin the chain-cancel half with one steering battery scenario in commands_test.go: steer while a chain is parked, cancel the chain, start a fresh turn, and assert the fresh turn's captured request is clean of the parked steering with the queue left empty. Mark this task's reversibility: the compound action is CONTEXT-flagged costly; the snapshot-first ordering is the mitigation.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestUndoAutoCancel|TestUndoFailClosed|TestUndoNestedRefusal|TestParkedChainCancelSteering' -v</automated>
  </verify>
  <acceptance_criteria>
    - Auto-cancel test passes: /undo completes while the fake provider is blocked; the blocked turn ends with the cancelled stop path; no provider call from the undo itself.
    - Deadlock behavioral test passes with a timeout guard: the /undo call returns within the test timeout (a mutex-ordering regression hangs the test — this is the documented -race-invisible hazard).
    - Fail-closed test passes: an injected SnapshotPreRestore failure aborts the undo AND leaves the running turn untouched (still running, not cancelled).
    - Nested+active test passes: refusal text names the paths; the turn was never cancelled.
    - Queued-steering resolution: the cancelled turn's undelivered steering resolves cancelled-normal (no steering_delivery line, no later-window delivery).
    - Parked-chain steering battery green: steering enqueued while a chain is parked, the chain then cancelled, a fresh turn started — the fresh turn's request contains no marker tag and no steered text, and the queue reports empty afterwards (no zombie delivery into the unrelated turn's first boundary).
  </acceptance_criteria>
  <done>D-12 is composed exactly per ordering: snapshot first, cancel second, drain under the mutex, restore last — with the nested refusal outranking the auto path and fail-closed snapshot semantics.</done>
  <reversibility rating="costly">Auto-cancel-then-restore is a compound destructive action on one keystroke (CONTEXT D-12 reversibility note); the pre-restore snapshot is the only undo, hence the snapshot-first ordering and the fail-closed abort are load-bearing.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-Zed operator UAT — steering mid-turn + /undo (Open Question on Zed client behavior)</name>
  <what-built>
    Boundary steering over ACP (23-01/23-02): input typed while a turn runs is queued with a note and applied at the next model-request boundary as one marker-wrapped user-role message with the "steering applied: N inputs" note; parked asks show "ask waiting behind running turn: <summary>" and accept cancel-grammar input. /undo (this plan): instant checkpoint restore with zero model turns, including the auto-cancel-then-restore path against a running turn.
  </what-built>
  <how-to-verify>
    1. Launch ass-guard acp in Zed on a scratch git repo; start a substantive turn (something that runs several tool calls).
    2. Mid-turn, type a short steering instruction (e.g. "prefer table output for the rest of this turn"). Expected: an immediate queued note; at the next boundary a "steering applied: 1 inputs" note; the agent visibly changes course WITHOUT restarting the turn.
    3. Let the turn finish. Run /undo. Expected: instant restore summary (restored id + pre-restore snapshot id), no model spin; the workspace files match the pre-turn state (git status/diff shows a clean tree modulo untracked store dir, which must NOT appear in git status).
    4. Start another turn; mid-turn run /undo. Expected: the running turn cancels visibly (cancelled), then the restore summary appears.
    5. Check Zed's ACP logs (dev: open acp logs) for any malformed frames during steered turns — none expected.
    6. Note for the record: if Zed itself queues mid-turn prompts client-side (RESEARCH A2), step 2 may not deliver mid-turn — record what Zed actually did; the transport-neutral core is required regardless and this UAT documents the client behavior for TG-02 planning.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator slash input -> command intercept | /undo text must resolve as a command, never leak to the model as content |
| undo sequence -> workspace | A compound destructive action (cancel + restore) mutates the operator's working tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-15 | Elevation of Privilege | /undo text reaching the model as steering | high | mitigate | The class-B resolve slot sits BEFORE steering enqueue in the classifier (23-02 reserved, filled here); battery asserts a mid-turn /undo never appears in any captured request (Task 2) |
| T-23-16 | Denial of Service | mutex-ordered deadlock on active-turn /undo | high | mitigate | Classification + snapshot + cancel all happen BEFORE turnMu acquisition; the behavioral timeout test pins it (-race cannot catch this class) (Task 2) |
| T-23-17 | Tampering | restore destroying concurrent work unsnapshotted | high | mitigate | Snapshot-before-cancel ordering, fail-closed abort, and the 23-03 guard battery; treeMap byte-identity asserted per restore (Tasks 1-2) |
| T-23-18 | Repudiation | undo without durable record | medium | mitigate | Every /undo (success, refusal, error, unavailable) writes the local_command record with args verbatim through the REDACTED path (Task 1) |
| T-23-SC | Tampering | package installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- go test -race -count=1 ./internal/session/ ./internal/runtime/ ./internal/checkpoint/ ./internal/acpserve/ green (phase gate)
- mise ci green before /gsd:verify-work (RESEARCH sampling-rate phase gate)
- The live-Zed UAT is recorded in the checkpoint resume (approved or issues listed)
</verification>

<success_criteria>
/undo is live as the fourteenth RESERVED class-B name with the D-11 walk, D-05 shape, zero provider calls, byte-identical restores, and the D-12 auto-cancel-then-restore path in the locked ordering; the live-Zed UAT for steering + /undo is operator-confirmed or its issues recorded.
</success_criteria>

<output>
Create .planning/phases/23-seed-gaps-close-out/23-05-SUMMARY.md when done
</output>

## Artifacts this phase produces (this plan: undo in the RESERVED class-B set, undoCommand + parseUndoDepth, Runner active-turn cancel registry, commands_test.go /undo battery, live-Zed UAT record)
