---
phase: 23-seed-gaps-close-out
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - internal/runtime/runtime.go
  - internal/runtime/steering_ingress_test.go
  - internal/session/transcript.go
  - internal/session/manager.go
  - internal/session/session.go
  - internal/session/steering_test.go
autonomous: true
requirements: [SEEDG-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An input typed while a turn runs is classified BEFORE the session turn mutex: pending-ask reply routing (incl. the parked-cancel grammar) first, then steering enqueue when a turn or chain is active, then ordinary new turn — the 12-07 queue-behind block is gone for steerable input (SEEDG-01, Pattern 6)"
    - "A steered prompt returns promptly: the operator sees the queued note immediately and the request resolves with stopReason end_turn while the running turn continues (Open Question 1 resolution: return-after-enqueue)"
    - "A parked ask is visible immediately (one note: ask waiting behind running turn: <summary>), cancelable while waiting, and never blocks the active turn (D-05, D-06, D-07)"
    - "Steering, parked-ask cancel, and ordinary inputs combined in ONE scenario classify correctly — no input is swallowed or misrouted (Pitfall 11)"
    - "A steered engine-chain turn applies steering at the boundary and the chain still completes (Open Question 4: zero engine changes)"
    - "A class-B invocation typed mid-turn never reaches the model as steering text (Pitfall 11) — the classifier leaves the class-B resolve slot ahead of steering enqueue per the Phase 20 locked contract (filled by 23-05)"
  artifacts:
    - internal/runtime/steering_ingress_test.go
    - Runner ingress classifier (pre-turnMu) in internal/runtime/runtime.go
    - SteerQueue wiring via sessionFor (SetSteerQueue)
    - transcript kind parked_ask (TypeParkedAsk) + Manager.AppendParkedAsk
    - parked-ask note + cancel grammar resolution path
  key_links:
    - "Runner.Run head (runtime.go:479-541) -> classifier BEFORE turnMu.Lock() -> SteerQueue.Enqueue -> immediate return with queued note: breakage here = queue-behind masquerading as steering (the forbidden descope) or a mutex deadlock"
    - "routeAskReply (runtime.go:1611-1629) -> parked-cancel grammar parse BEFORE ordinary reply interpretation: breakage here = a cancel swallows an answer or vice versa (D-06)"
  flagged_assumptions:
    - "RESEARCH A2 (FLAGGED): Zed may never deliver a mid-turn session/prompt (it may queue client-side or cancel first) — the ACP ingress path is exercised by tests + the simulator regardless; the transport-neutral core lands for TG-02 either way. Live-Zed confirmation is a 23-05 UAT checkpoint, not a blocker for this plan."
  prohibitions:
    - "Parked-ask cancellation must never kill or interrupt the running turn: cancellation resolves the ask cancelled-normal through the 17-D-13 drain semantics, nothing more (D-06)."
---

<objective>
SEEDG-01 ingress: classify every input at Runner.Run entry BEFORE the session turn mutex — pending-ask answer / parked-ask cancel / steering enqueue / ordinary turn — wire the SteerQueue per session, resolve the steered-prompt response semantics, and make parked asks visible + cancelable without blocking the running turn.

Purpose: this is where queue-behind (today's 12-07 turnMu blocking) dies. Without the pre-mutex classifier, steering would still block on the turn mutex and SEEDG-01 would be silently descoped.

Output: the classifier + wiring in runtime.go, parked_ask record + D-05/D-06 behavior, and the combined-scenario + engine-chain tests.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-seed-gaps-close-out/23-CONTEXT.md
@.planning/phases/23-seed-gaps-close-out/23-RESEARCH.md
@.planning/phases/23-seed-gaps-close-out/23-PATTERNS.md
@.planning/phases/23-seed-gaps-close-out/23-01-SUMMARY.md

@internal/runtime/runtime.go
@internal/session/ask.go
@internal/session/session.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-mutex ingress classifier + SteerQueue wiring + steered-prompt response semantics</name>
  <files>internal/runtime/runtime.go, internal/runtime/steering_ingress_test.go</files>
  <behavior>
    - Runner.Run with an active client turn (turnActive true) and a plain-text prompt: assigns a ticket via the session's SteerQueue, emits the queued note through the in-hand emit parameter, and returns (stopReason end_turn-mapped stop, nil error) WITHOUT acquiring the turn mutex — the running turn is untouched (D-04).
    - Runner.Run with an active ENGINE CHAIN (chainCount > 0, no mutex held) and plain text: same steering enqueue path (Pattern 6 — parked chains hold no mutex while chainCount > 0).
    - Runner.Run with NO active turn/chain and plain text: the ordinary new-turn path, byte-for-byte today's behavior (runOneTurn under turnMu).
    - Runner.Run with a pending ask and an answer-shaped reply: routeAskReply unchanged (17-D-11 contract preserved).
    - The steered enqueue path returns the ticket to the transport layer's disposal (the transport-neutral API returns the ticket; the ACP adapter drops it — documented in code).
    - Classification order is: pending-ask route (Task 2 adds the cancel grammar ahead of ordinary reply interpretation) -> RESERVED class-B resolve slot (documented no-op until 23-05; per the Phase 20 locked contract it sits after the ask route and before steering enqueue) -> steering enqueue -> ordinary turn.
  </behavior>
  <action>
Write the failing ingress tests FIRST in steering_ingress_test.go following the checkpoint_session_test.go fixture style (temp workspace, fake provider, Runner construction), then extend Runner.Run. Insert the classifier at the very top of Run BEFORE turnMu.Lock() (runtime.go:490) — this is load-bearing: everything after the Lock holds the session mutex, so a classifier that waited there could never observe an active turn without self-deadlock (Pitfall 4's shape). Active-state detection uses the existing race-tested primitives only: the turnActive sync.Map (runtime.go:181-182) and chainCount/WaitChainIdle (runtime.go:946-969) — build no new flags. SteerQueue wiring: in sessionFor, construct a SteerQueue per session and attach via the Session's SetSteerQueue from 23-01 (mirror the AskBroker wiring at runtime.go:1120-1125); the Runner reaches the session's queue through the session for enqueue classification. Response semantics — Open Question 1 is RESOLVED here per the research recommendation: a steered prompt returns promptly (queued note + end_turn-mapped stop) rather than blocking until delivery; the running turn's stream continues via the session-lifetime forwarder (WINDOWS #3 split). The queued note goes through the in-hand emit parameter (the live emitter precedent runtime.go:561-563) — never a post-turn bus publish (timing hazard documented at runtime.go:543-549). Document the class-B resolve slot with a comment naming the 20-01 contract position (after routeAskReply, before steering enqueue) so 23-05 fills it without re-deriving the order.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestSteerIngress' -v</automated>
  </verify>
  <acceptance_criteria>
    - Steered enqueue test passes: ticket assigned, queued note emitted, end_turn returned, running turn's request count and stream unaffected (fake provider still streaming).
    - Ordinary-turn test passes: no active turn -> runOneTurn path with turnMu acquired, behavior identical to pre-change (existing runner battery stays green).
    - The classifier provably runs before turnMu: a test enqueues steering while a blocked fake provider holds the turn — the steered prompt returns WITHOUT waiting for the provider release (this is the anti-queue-behind assertion; it goes red if classification happens after the Lock).
    - Engine-chain steering test passes: chainCount > 0 (parked) + plain text -> steering enqueued, chain untouched.
  </acceptance_criteria>
  <done>Inputs classify pre-mutex in the locked order; steered prompts return promptly with a queued note; the session's SteerQueue is wired in sessionFor; today's ordinary path is behavior-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parked asks — visible note, cancelable, never blocking (D-05, D-06, D-07)</name>
  <files>internal/session/transcript.go, internal/session/manager.go, internal/session/session.go, internal/runtime/runtime.go, internal/session/steering_test.go</files>
  <behavior>
    - When an ask parks behind a running turn, the operator immediately sees one note: ask waiting behind running turn: <summary> (D-05 wording; <summary> from the ask's question summary), and the transcript gains a parked_ask record (TypeParkedAsk = "parked_ask", TurnID + summary Text, REDACTED append path).
    - Input matching the parked-cancel grammar while an ask is pending resolves that ask cancelled-normal (17-D-13 drain semantics applied proactively) WITHOUT killing any running turn (D-06); the resolved outcome rides the existing ask resolution path (Settle/cancelled mapping).
    - The parked-cancel grammar is parsed BEFORE ordinary reply interpretation inside the pending-ask route (Pattern 6 step 1) — a cancel-shaped input never becomes an answer and an answer-shaped input never becomes a cancel.
    - Parking never blocks: the active turn's request count, stream, and completion are unaffected by a parked ask (D-07).
    - Parked-ask records fold into replay windows as nothing (no Projector case — readers tolerate the kind, 16-D-20); replay of a session with parked_ask lines is identical to pre-phase behavior.
  </behavior>
  <action>
Add TypeParkedAsk = "parked_ask" to transcript.go with the additive-kind doc-block discipline, and AppendParkedAsk(turnID, summary string) to manager.go over the REDACTED appendLine path (same template as AppendLocalCommand at manager.go:362-367; the T-16-04 unredacted exemption stays type-scoped — never widen it). Wire the D-05 note + record at the point an ask is surfaced while the session's turn is running (the suspendForAsk path in session.go:597 already suspends without blocking — attach the note + record there, wording per D-05). Implement the cancel grammar in the pending-ask route (the routeAskReply entry at runtime.go:1611): parse the FIRST text block against a small predicate (isParkedCancel) BEFORE ResolveAsk; the grammar is CONTEXT discretion — keep it conservative and explicit (e.g. exact phrases naming the wait/cancel intent) and document the accepted phrases in one place; a grammar match resolves the pending ask cancelled-normal via the existing settle path and returns the mapped stop without touching the running turn. Emit the cancelled acknowledgment as a note through the same live chunk family (never a steering_delivery line — cancelled content must never reach a later model window).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ ./internal/session/ -run 'TestParkedAsk' -v</automated>
  </verify>
  <acceptance_criteria>
    - Park note test: parking emits exactly one "ask waiting behind running turn:" note and one parked_ask transcript line.
    - Cancel test: a cancel-grammar input resolves the ask cancelled-normal; the running turn completes its full scripted request sequence untouched.
    - Answer test: a non-cancel reply still routes through ResolveAsk unchanged (17-D-11 preserved).
    - Replay tolerance test: transcripts containing parked_ask lines project identically to pre-phase windows.
  </acceptance_criteria>
  <done>Parked asks are visible the moment they park, cancelable by grammar without killing the turn, and never block the active turn.</done>
</task>

<task type="auto">
  <name>Task 3: Combined-scenario + engine-chain steering E2E</name>
  <files>internal/runtime/steering_ingress_test.go, internal/session/steering_test.go</files>
  <read_first>
    - internal/runtime/enginebridge/enginebridge.go — the engine chain path through sess.Prompt (191), ParkMu around post-park injection turns (182-189), LastTurnOutput scanning only assistant/ask_suspended terminal lines (225-306 — steering lines are invisible to Decide signals, which is WHY zero engine changes are needed)
    - 23-RESEARCH.md Pitfall 11 (the combined ambiguity matrix) and Open Question 4
  </read_first>
  <action>
One E2E scenario (the milestone prescription — the warning sign is tests that NEVER combine steering + parked ask + cancel): a scripted turn where, mid-iteration, the operator sends (1) a steering text, (2) an ask parks behind the running turn, (3) a parked-cancel grammar input, (4) another steering text — assert per-input classification: steered texts coalesce and deliver at the next boundary as ONE block; the ask parks with its note + record; the cancel resolves the ask cancelled-normal without interrupting the turn; the turn completes; the transcript shows the correct record sequence; nothing cancelled reaches any request. Plus the engine-chain E2E (Open Question 4 resolution is NO engine changes): run a steered turn driven through the engine bridge and assert the steering applies at the boundary AND the chain completes normally (Decide signals never observe steering lines — LastTurnOutput's kind filter guarantees it; the test pins that guarantee).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestCombinedSteerScenario|TestEngineChainSteered' -v</automated>
  </verify>
  <acceptance_criteria>
    - The combined scenario passes: each of the four inputs classifies per the matrix, exactly one coalesced steering block delivers, the cancelled ask never reaches a request.
    - The engine-chain test passes: steering applied + chain completes with zero changes to internal/runtime/enginebridge/.
    - go test -race -count=1 ./internal/session/ ./internal/runtime/ green (both whole packages).
  </acceptance_criteria>
  <done>The disambiguation core is proven under the worst-case combined input; engine chains steer cleanly with no engine modifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input -> ingress classifier | Untrusted text is classified (answer / cancel / steering / command / new turn) before any queueing — misclassification loses or misroutes operator intent |
| classifier -> SteerQueue -> model window | Steered text crosses into the model as user speech |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04 | Denial of Service | classifier stall / mutex ordering | high | mitigate | Classification runs BEFORE turnMu.Lock with only non-blocking checks (turnActive map, chainCount) — the queued note returns immediately; the pre-mutex test proves a blocked turn cannot stall a steered prompt (Task 1) |
| T-23-05 | Spoofing | parked-cancel grammar | medium | mitigate | Grammar is conservative and exact-phrase (CONTEXT discretion documented in one place); a cancel resolves only the session's own pending ask through the existing settle path — no authority, no turn kill (Task 2) |
| T-23-06 | Repudiation | lost/misrouted input | medium | mitigate | Every classification outcome is observable: queued note + ticket, parked note + parked_ask record, cancelled acknowledgment — transcript-as-truth reconstructs the sequence (Task 3 combined scenario) |
| T-23-SC | Tampering | package installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- go test -race -count=1 ./internal/session/ ./internal/runtime/ green (whole packages — the existing runner battery must stay green)
- The anti-queue-behind assertion (steered prompt returns while the turn blocks) is explicit and green
- golangci-lint clean on modified files
</verification>

<success_criteria>
Pre-mutex classification in the locked order; steered prompts return promptly; parked asks visible + cancelable + non-blocking; combined-scenario and engine-chain E2E green; class-B resolve slot reserved at the 20-01 contract position.
</success_criteria>

<output>
Create .planning/phases/23-seed-gaps-close-out/23-02-SUMMARY.md when done
</output>

## Artifacts this phase produces (this plan: Runner ingress classifier + wiring, parked_ask kind + append, D-05/D-06 behavior, response-semantics resolution, combined/engine-chain tests)
