---
phase: 23-seed-gaps-close-out
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/checkpoint/store.go
  - internal/checkpoint/store_test.go
autonomous: true
requirements: [SEEDG-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A pre-restore snapshot can be minted as a first-class checkpoint object — same store, same GC lifecycle, restorable via the same machinery (D-09) — under an extended id grammar updated in ALL THREE enforcement sites together (store.go:114, 364-380, 454-488)"
    - "A restore NEVER proceeds without its pre-restore snapshot: snapshot failure aborts the restore (fail-closed, D-12 ordering)"
    - "Restores are refused outright when the workspace contains nested repositories (.git as dir OR file — worktrees/submodules), because gitlink contents are silently unprotected (SEEDG-02, verified failure shape: gitlink 160000 + clean -fd non-descent)"
    - "Checkpoint expiry is AGE + COUNT, whichever binds first (defaults 7d / 50 per session), swept under the whole-store lock with object expiry (reflog expire + gc --prune=now), so the shadow store stops growing (D-08)"
    - "The USER repo's .git/info/exclude carries .ass-guard/ — appended idempotently, existing lines preserved, structured stderr note when the workdir is not a git repo (SEEDG-02, verified by git check-ignore -v)"
    - "git status in the user's worktree never shows .ass-guard/ (Pitfall 8's warning sign, asserted negatively)"
  artifacts:
    - extended idPattern + validateTurnID + parseRefLine (three-site grammar update) in internal/checkpoint/store.go
    - Store.SnapshotPreRestore (per-session -pre- id family, A4)
    - findNestedRepos workspace walk (dir OR file detection, skipping the store root)
    - Store.Sweep(ctx, maxAge, perSession) + expireByAge/expireByCount
    - ensureUserRepoExclude workdir exclude append
    - store_test.go extensions: pre-restore family, nested-repo fixture (dir + .git FILE), GC, exclude
  key_links:
    - "the three grammar sites move TOGETHER: a partial update mints snapshot ids that fail validation and the restore proceeds unsnapshotted — the exact Pitfall 6 worst case"
    - "Sweep's ref deletion and its reflog expire + gc --prune=now sit in the SAME withLock critical section: split them and the store grows forever (Pitfall 5)"
  flagged_assumptions:
    - "SEEDG-02 probe row (category: unclassified, status: unresolved — FLAGGED): the probe asks for manual review of edge behavior; the edge cases owned by this plan (snapshot-failure aborts restore, nested .git-file variant, GC boundary at exactly maxAge/maxCount, non-repo workdir degrade) are each pinned by a named test below. Residual: cross-process CLI restore keeps today's unguarded behavior (Open Question 3 resolution — guards are in-process only, documented caveat)."
  prohibitions:
    - "A restore must never overwrite workspace state without a preceding pre-restore snapshot, and the GC must never touch anything but checkpoint refs in the shadow store — audit transcript lines are never purged (18-D-09 family discipline)."
    - "Nested repositories (.git directory or .git file) must be refused outright — never auto-restored over, never descended into with a force-clean; the store's clean vocabulary never gains the recursive-untracked flag that descends into nested repos."
---

<objective>
SEEDG-02 store level: the four store-owned guards — pre-restore snapshot family (D-09), nested-repo detection + refusal, age+count GC (D-08), and the user-repo exclude append — as test-pinned Store operations the Runner composes in 23-04.

Purpose: the checkpoint store is the only thing standing between a destructive restore and the operator's workspace. These are exactly the five SEEDG-02 extension points the research mapped, minus the Runner wiring.

Output: extended store.go + store_test.go with the pre-restore id family, nested-repo refusal, Sweep, and exclude append — no runtime changes (23-04 wires them).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-seed-gaps-close-out/23-CONTEXT.md
@.planning/phases/23-seed-gaps-close-out/23-RESEARCH.md
@.planning/phases/23-seed-gaps-close-out/23-PATTERNS.md

@internal/checkpoint/store.go
@internal/checkpoint/store_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-restore snapshot family — one grammar, three sites, fail-closed</name>
  <files>internal/checkpoint/store.go, internal/checkpoint/store_test.go</files>
  <behavior>
    - SnapshotPreRestore(ctx, sessionID) mints a per-session id in a -pre- family (A4 shape: <sessionID>-pre-<NNN>, NNN zero-padded 3+), commits the current tree, and records the ref; the returned id passes the SAME validation every turn id passes.
    - The counter is deterministic across process restarts: minting twice in a row yields NNN and NNN+1 (seeded by scanning existing refs for the session's -pre- ids, max+1).
    - A pre-restore id restores exactly like a turn id: Restore(preRestoreID) produces a byte-identical treeMap to the snapshotted state (D-09: restorable via the same machinery).
    - validateTurnID rejects malformed -pre- ids exactly as it rejects malformed turn ids (bad session prefix, short sequence) — the grammar extension does not loosen anything.
    - A SnapshotPreRestore failure returns the error (the CALLER aborts the restore — fail-closed contract asserted from the store side by the error surface, and end-to-end in 23-05).
    - List includes pre-restore entries ordered naturally among turn entries (23-05's /undo walk consumes this).
  </behavior>
  <action>
Write the failing store_test.go cases FIRST using the existing helpers (treeMap/writeTestFile/seedWorkspace/openStore/snap, store_test.go:24-129), then extend store.go. Update the id grammar in ALL THREE coupled sites in one commit: the compiled idPattern at store.go:114, validateTurnID at 364-380, and parseRefLine at 454-488 (its SessionID derivation currently uses a -turn- suffix trim — add the parallel -pre- derivation). Accept BOTH families as first-class checkpoint ids; neither site may accept anything the other rejects (add a table test enumerating accept/reject across both families). SnapshotPreRestore lives beside Snapshot (store.go:214-237) and reuses commitSnapshot + updateRef + the post-snapshot prune call under withLock — no new git vocabulary. Never bypass the validate-before-refspec gate (T-14-01): the new family inherits it by construction because validation is centralized.
  </action>
  <verify>
    <automated>go test -count=1 ./internal/checkpoint/ -run 'TestPreRestore|TestIDGrammar' -v</automated>
  </verify>
  <acceptance_criteria>
    - Grammar table test passes: both families accepted, malformed variants rejected at validate AND at parse.
    - Mint-restart determinism test passes: counter resumes at max+1 after store reopen.
    - Restore-of-pre-restore test passes: treeMap byte-identical round trip.
    - grep -v '^#' internal/checkpoint/store.go | grep -c 'turn-(\\d{3,})' returns 0 matches of the OLD un-extended pattern alone (the pattern now alternates both families).
  </acceptance_criteria>
  <done>Pre-restore snapshots are first-class checkpoint objects with a validated id family, deterministic counters, and restore parity — all three grammar sites updated together.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nested-repo detection + refusal (dir AND .git-file variants)</name>
  <files>internal/checkpoint/store.go, internal/checkpoint/store_test.go</files>
  <behavior>
    - findNestedRepos(workDir) returns the relative paths of every nested repo root found by walking the workspace for .git entries — directories AND files (worktrees/submodules use a .git FILE) — skipping the store root directory itself.
    - A fixture with a nested repo (real git init inside) is detected; a fixture with a bare .git FILE containing a gitdir pointer is detected; a clean flat workspace returns empty (the existing seedWorkspace tree).
    - A refusal wrapper (RestoreGuard on the store side, composed by the Runner in 23-04) returns a typed error naming the nested paths and the reason (silently unprotected gitlink contents) when detection is non-empty; the error message enumerates the paths.
    - Detection cost is bounded: symlinks are not followed (no cycles, no escaping the workspace).
  </behavior>
  <action>
Write the failing tests FIRST: extend seedWorkspace-style fixtures with (a) a real nested repo via git init (the store's git subprocess pattern is available for fixture setup) and (b) a plain file named .git containing a gitdir: line — no subprocess needed for (b). Implement findNestedRepos as a filepath.WalkDir over workDir skipping the store root dir; detection is presence of a .git entry (os.Stat IsDir false-positive handling: a .git FILE counts). Add the typed refusal error (sentinel + path list) and a store-level guard method RestoreGuard(workDir) error that runs detection and refuses; KEEP the refusal policy decision at the composition layer (23-04/23-05 decide refuse-vs-auto paths) — the store only reports and refuses. Never enter any clean variant that descends into nested repos; the restore's existing clean invocation keeps its -e store-root exclusion untouched.
  </action>
  <verify>
    <automated>go test -count=1 ./internal/checkpoint/ -run 'TestNestedRepo' -v</automated>
  </verify>
  <acceptance_criteria>
    - Nested-dir fixture: detected and refused with a message naming the nested path.
    - .git-FILE fixture: detected and refused (this is the worktree/submodule variant the research experiment flagged).
    - Flat workspace: detection returns empty, guard passes.
    - The store's clean args never gain the flag that makes clean descend into nested repos (inspect the invocation in the test).
  </acceptance_criteria>
  <done>Nested repos — including the .git-file worktree variant — are detected and refused with named paths; the silent-gitlink-loss failure shape is closed at the store boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Age+count GC sweep + user-repo exclude append</name>
  <files>internal/checkpoint/store.go, internal/checkpoint/store_test.go</files>
  <behavior>
    - Sweep(ctx, maxAge, perSession) deletes refs that violate EITHER axis: committed-older-than maxAge (D-08 axis 1) or beyond perSession-per-session newest-count (axis 2, default 50 — A3, aligned with today's DefaultKeep); survivors of the other axis are never evicted by accident (per-session accounting, not global).
    - After deletions, the SAME withLock critical section runs reflog expire --expire=now --all and gc --prune=now inside the shadow store — the object store actually shrinks (assert via git count-objects or du-style probe on the fixture store).
    - Pre-restore entries participate in BOTH axes identically (D-09: one store, one lifecycle).
    - ensureUserRepoExclude(workDir): appends the store-root-relative ignore rule to <workDir>/.git/info/exclude ONLY when workDir/.git is a directory; idempotent (second call changes nothing); existing exclude lines preserved byte-for-byte; when .git is absent OR is a file (worktree), returns a typed skip (caller logs a structured stderr note — never a failure).
    - The appended rule makes git check-ignore fire for paths under the store root (fixture-asserted via the git subprocess).
  </behavior>
  <action>
Write failing tests FIRST (fixtures: a store with refs of controlled ages — commit-then-backdate via the ref entry's CommittedAt metadata path used by listRefs; a user-repo fixture with a pre-seeded exclude file containing a marker line). Implement Sweep over the verified primitives: listRefs (423-449) for entries + timestamps, expireByAge/expireByCount as pure helpers (table-testable), victim deletion via the update-ref -d idiom from prune (494-511), then reflog expire + gc --prune=now, ALL inside one withLock (518-563) critical section. Document the relationship to the existing global keep-50 prune in one comment: the session-start Sweep (wired in 23-04) is the retention authority; keep the per-snapshot prune call as-is unless it double-deletes (if it does, retire it and say so in the comment — the decision must be visible, not silent). Implement ensureUserRepoExclude exactly per the verified experiment: MkdirAll the info dir at 0700, read-check-append via O_APPEND|O_CREATE|O_WRONLY at 0600, never truncate, skip rule = the store root directory name with a trailing slash; the .git-FILE (worktree) case returns the typed skip.
  </action>
  <verify>
    <automated>go test -count=1 ./internal/checkpoint/ -run 'TestCheckpointGC|TestUserRepoExclude' -v</automated>
  </verify>
  <acceptance_criteria>
    - Age-axis test: a ref older than maxAge is gone, a younger ref survives; count-axis test: the per-session newest N survive, older overflow evicted; a second quiet session's refs survive a chatty session's overflow (no cross-session eviction).
    - Object-expiry test: after Sweep the shadow store's loose-object count drops (gc ran — refs-deleted-only would leave them).
    - Exclude tests: append happens once (idempotent), the pre-seeded marker line survives byte-for-byte, check-ignore fires for a store-root path, non-repo and .git-file workdirs return the typed skip with no file created.
    - go test -race -count=1 ./internal/checkpoint/ green (whole package).
  </acceptance_criteria>
  <done>Dual-axis GC with real object expiry under one lock; the user's repo excludes the store via .git/info/exclude with idempotent, non-destructive appends and loud degradation outside git repos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace filesystem -> store operations | Snapshot/restore/GC read and mutate the operator's working tree via the git subprocess |
| user repo .git/info/exclude | The one sanctioned write surface into the operator's repo config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07 | Tampering | destructive restore | high | mitigate | Pre-restore snapshot family with fail-closed contract (Task 1); composition ordering (snapshot BEFORE restore) enforced at 23-04/23-05 call sites and tested end-to-end there |
| T-23-08 | Tampering | nested-repo silent data loss (gitlink 160000, clean non-descent) | high | mitigate | Detection of .git dir AND file variants + outright refusal with named paths (Task 2); the force-clean-descent flag never enters the store's vocabulary |
| T-23-09 | Information Disclosure | shadow store leaking into the user repo | medium | mitigate | User-repo .git/info/exclude append (Task 3, check-ignore-asserted) + existing shadow self-exclusion + 0700 store perms; gitlink/nested refusal bounds what add -A can ingest |
| T-23-10 | Tampering | GC destroying restorable state | medium | mitigate | Conservative dual-axis defaults (7d / 50), per-session accounting (no cross-session eviction test), pre-restore entries share the lifecycle (undo-of-undo survives within the window); audit transcript lines are outside the GC's universe by construction (refs-only sweep) |
| T-23-11 | Injection | git refspec from ids | low | mitigate | Canon-adjacent item — NOT minted as a prohibition: the existing validate-before-refspec gate (T-14-01, store.go:246-256) extends to the new id family via the centralized grammar (Task 1) |
| T-23-SC | Tampering | package installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none — platform git is a documented pre-existing dependency, store.go:53-56) |
</threat_model>

<verification>
- go test -race -count=1 ./internal/checkpoint/ green (whole package, existing store battery included)
- golangci-lint clean on store.go/store_test.go
- Fixture-proof of the research experiment's warning signs: check-ignore fires; clean never descends; gc shrinks the object store
</verification>

<success_criteria>
All four store-owned SEEDG-02 guards exist and are test-pinned: pre-restore snapshot family (three-site grammar, fail-closed surface), nested-repo refusal (dir + file variants), age+count Sweep with object expiry, idempotent user-repo exclude append with graceful non-repo degradation.
</success_criteria>

<output>
Create .planning/phases/23-seed-gaps-close-out/23-03-SUMMARY.md when done
</output>

## Artifacts this phase produces (this plan: Store.SnapshotPreRestore + extended id grammar (store.go:114/364-380/454-488), findNestedRepos + RestoreGuard, Store.Sweep + expireByAge/expireByCount, ensureUserRepoExclude, store_test.go extensions)
