---
phase: 23-seed-gaps-close-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/steerqueue.go
  - internal/session/steerqueue_test.go
  - internal/session/steering_test.go
  - internal/session/transcript.go
  - internal/session/manager.go
  - internal/session/session.go
  - internal/session/projector.go
autonomous: true
requirements: [SEEDG-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A steering input enqueued while a turn runs reaches the model as a user-role message wrapped in the steering marker at the NEXT model-request boundary — never mid-in-flight-request, never splitting a tool_use/tool_result pair (SEEDG-01, D-01)"
    - "All inputs queued since the last boundary deliver as ONE coalesced marker block in arrival order (D-02)"
    - "Steering never cancels or interrupts the running turn (D-04); on turn cancellation, undelivered items resolve cancelled-normal and never reach any later model window"
    - "steering_delivery transcript lines fold into the projected window identically live and on replay, and never move the Projector's mid-turn anchor (D-03, 18-D-01, Pitfall 1)"
    - "Each boundary drain emits the live note exactly: steering applied: N inputs (D-03)"
    - "The SteerQueue is transport-neutral: its file's import block contains no ACP-wire package reference (TG-02 consumer contract, compile-level)"
  artifacts:
    - internal/session/steerqueue.go
    - internal/session/steerqueue_test.go
    - internal/session/steering_test.go
    - TypeSteeringDelivery kind in internal/session/transcript.go
    - Manager.AppendSteeringDelivery in internal/session/manager.go
    - Session.SetSteerQueue + boundary drain in internal/session/session.go
    - accumulateMidTurn steering fold in internal/session/projector.go
  key_links:
    - "runTurn iteration top (session.go:349-394, between the ctx.Err() check and Project) -> SteerQueue drain -> AppendSteeringDelivery -> Projector fold: breakage here = mid-flight injection or anchor wipe"
    - "ticket/cutoff watermark <-> every abnormal exit's resolution funnel: the turn's cancelled-exit CancelThrough at the recordCanceled funnel, PLUS the no-live-runTurn teardowns that the funnel cannot reach and which resolve the queue explicitly (23-05 T2) — the parked-chain cancel (cancelParkedChains) and the panic-recovered turn exit: breakage here = silently lost or zombie-delivered steering (Pitfall 3)"
  flagged_assumptions:
    - "SEEDG-01 probe row (category: concurrency, status: unresolved — FLAGGED, not silently resolved): the guarantee under interruption/parallelism is defined by the ticket/cutoff protocol — every enqueued input either drains at a boundary before turn death or resolves cancelled-normal via the cancelled-exit CancelThrough at the recordCanceled funnel; nothing is silently dropped. Assertion scope: the -race producer/cancel hammer pins the QUEUE-LEVEL conservation invariant only (delivered + cancelled == assigned, no duplicates); the turn-level CALL SITE is pinned separately by the steering_test.go anti-zombie scenario (steer -> cancel before the next boundary -> next turn's request is clean), and 23-05 Task 2's battery validates the same resolution end-to-end through /undo's auto-cancel. Residual accepted risk: an input enqueued between the final boundary drain and the turn-death CancelThrough resolves cancelled (never lost, never delivered) — revisit only if TG-02 needs a stronger ack contract."
  prohibitions:
    - "Steering text must never carry tool or permission authority: it reaches the model strictly as user speech inside the marker, steering_delivery lines are agent-appended only (never derived from model or tool output), and the marker wrapper must not be renderable from model/tool content (anti-spoofing, D-01)."
---

<objective>
SEEDG-01 core: a transport-neutral SteerQueue and the boundary-drain seam that delivers queued steering into the NEXT model request as one marker-wrapped user-role message, with a typed steering_delivery transcript record the Projector folds without moving the mid-turn anchor.

Purpose: this is the correction-reaches-the-model primitive SEEDG-01 forbids descoping to queue-behind, and the transport-neutral core TG-02 (Telegram) will consume. Session-internal only — no Runner wiring yet (23-02), no class-B surface (23-05).

Output: internal/session/steerqueue.go + the drain/fold/append wiring in the session package, proven end-to-end by a fake-provider test that watches steering appear in the second shaped request.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-seed-gaps-close-out/23-CONTEXT.md
@.planning/phases/23-seed-gaps-close-out/23-RESEARCH.md
@.planning/phases/23-seed-gaps-close-out/23-PATTERNS.md

@internal/session/session.go
@internal/session/projector.go
@internal/session/transcript.go
@internal/session/manager.go
@internal/session/ask.go
@internal/session/end_to_end_test.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end steering delivery — enqueue mid-turn, marker-wrapped user-role text in the NEXT request</name>
  <files>internal/session/steerqueue.go, internal/session/steerqueue_test.go, internal/session/steering_test.go, internal/session/transcript.go, internal/session/manager.go, internal/session/session.go, internal/session/projector.go</files>
  <read_first>
    - internal/session/session.go — runTurn (349-583: the iteration top between the ctx.Err() check at 367-371 and Project at 373 is the ONLY drain point), the Session struct field pattern ask *AskBroker (113-116, "Nil = not wired"), SetAskBroker (ask.go:315-325, the nil-safe setter template), the loud-degrade discipline (197-204)
    - internal/session/projector.go — accumulateMidTurn (170-227: the anchor loop matches ONLY TypeUserMessage at 174-180; the switch folds tool_call/tool_result/assistant_message and silently skips other kinds), boundMidTurn pair-safety (261-276)
    - internal/session/manager.go — AppendLocalCommand (362-367, the appendLine template) and the REDACTED appendLine path (77-104); NEVER appendLineUnredacted (106-113, type-scoped to raw_thinking — T-16-04 must not widen)
    - internal/session/transcript.go — the kind family and doc-block discipline (19-77), Line struct reusable fields (89-156), reader tolerance rule (144-148, 16-D-20)
    - internal/session/end_to_end_test.go — the established fake-provider test pattern for this package (scripted Stream calls, request capture)
    - 23-RESEARCH.md Pattern 1 (drain point) and Pitfalls 1+2 (anchor hazard, pair splitting)
  </read_first>
  <action>
Wire ONE path end-to-end, production-quality: (1) NEW SteerQueue in steerqueue.go structurally modeled on AskBroker (single sync.Mutex, slice of items, NO channels): fields items []SteerItem, next uint64, cutoff uint64; type SteerItem {Ticket uint64, Text string, At time.Time}; methods Enqueue(text string) uint64 (assigns next++, appends), Drain() []SteerItem (returns all items above the watermark in arrival order and advances the watermark; empty batch when nothing queued), CancelThrough(t uint64) int (resolves items with Ticket <= t as cancelled and removes them; returns count). Keep it minimal in this task — Task 2 adds the concurrency battery. (2) transcript.go: add TypeSteeringDelivery = "steering_delivery" with the same doc-block discipline as the four additive-kind precedents (what it carries, which append path, how the Projector treats it). (3) manager.go: AppendSteeringDelivery(turnID, text string, count int) over m.appendLine (the REDACTED path — steering is model-visible untrusted user content), storing the marker text in Text, the turn id in TurnID, and the input count via the existing Input json.RawMessage field (a small JSON object {count: N}). (4) session.go: unexported field steer *SteerQueue + nil-safe setter SetSteerQueue (exact SetAskBroker shape); in runTurn, drain between the ctx.Err() check and Project: if the queue is wired and Drain returns a non-empty batch, render ONE marker block via a small renderSteeringMarker(batch) helper — the coalesced texts inside a <system-reminder>-shaped tag pair (the captured wire convention, corpus_scan.go:29-31; exact inner layout is CONTEXT discretion but it MUST be one block, one user-role message), append via AppendSteeringDelivery, and emit the live note exactly "steering applied: N inputs" through the same live chunk path streamAndEmit uses (TurnEmitter sink / agent_message_chunk family) — never a deferred post-turn bus publish (timing hazard runtime.go:543-549: it would be LOST). Then wire the CANCELLED exits: all three cancelled returns in runTurn funnel through recordCanceled (session.go:714 — the iteration-top site at 367-371, semaphore-acquire at 388, mid-stream at 401), so resolve the queue THERE — if the queue is wired, CancelThrough through the highest assigned ticket (add a CancelAll() int wrapper on SteerQueue that resolves every remaining item and returns the count) so undelivered steering resolves cancelled-normal at turn death and can never zombie-deliver into the NEXT turn's window (Pitfall 3; this is the cancelled-exit half of the ticket/cutoff key link below). (5) projector.go accumulateMidTurn: add case TypeSteeringDelivery that folds l.Text as a user-role provider.Message in arrival position AFTER flushBatch() (steering is never part of a tool batch). The anchor loop (174-180) must keep matching ONLY TypeUserMessage — do not touch it; that is precisely why steering is its own kind (Pitfall 1: a user_message-kind steering line would move the anchor and wipe the turn's accumulated window). (6) In steering_test.go, the end-to-end test on the fake-provider pattern from end_to_end_test.go: a Session whose first Stream call blocks until released; start Prompt in a goroutine; once request 1 is observed, SetSteerQueue + Enqueue a steering text; release; assert the SECOND captured request contains a user-role message carrying both the marker tag and the steered text, assert tool pairs from iteration 1 are intact in that request, assert the transcript holds a steering_delivery line, and assert the drained watermark means nothing re-delivers on a subsequent turn. Add the anti-zombie scenario: enqueue steering mid-turn, cancel the turn BEFORE the next boundary (cancel the ctx while the first Stream call is still blocked), then start a second turn and assert its captured request carries NO marker tag and NO steered text (the cancelled-exit CancelThrough resolved the items), and the queue reports empty afterwards.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run 'TestSteeringDelivery' -v</automated>
  </verify>
  <acceptance_criteria>
    - The end-to-end test passes: request 2 of a steered turn contains the steered text inside a user-role message wrapped in the marker tag; request 2 also still contains iteration 1's tool_call/tool_result pairs (no split).
    - The transcript contains exactly one steering_delivery line per boundary drain with the coalesced text and count.
    - A second turn after steering delivery does NOT re-receive the delivered text (watermark advanced).
    - The anti-zombie scenario passes: steering enqueued mid-turn then cancelled before the next boundary never appears in any later captured request (cancelled-exit CancelThrough via the recordCanceled funnel); the queue is empty afterwards.
    - go vet ./internal/session/ is clean.
  </acceptance_criteria>
  <done>The single happy path — enqueue while running, drain at the iteration top, one marker-wrapped user-role message in the next request, steering_delivery recorded, note emitted — works end-to-end and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ticket/cutoff semantics — coalesce order, cancel resolution, -race hammer, transport-neutrality gate</name>
  <files>internal/session/steerqueue.go, internal/session/steerqueue_test.go</files>
  <behavior>
    - Enqueue returns strictly increasing tickets across calls; Drain returns queued items in arrival order (D-02) and advances the watermark so a second Drain returns empty.
    - Drain on an empty/fully-drained queue returns an empty batch without error (the every-iteration drain is a no-op hot path).
    - CancelThrough(c) removes exactly the items with Ticket <= c and returns their count; items with Ticket > c survive and are returned by the next Drain (PITFALLS.md:329 protocol).
    - CancelAll() resolves every remaining item (CancelThrough over the highest assigned ticket) and returns their count — the wrapper session.go's cancelled exits call; on an empty queue it returns 0.
    - Producer hammer: 8 goroutines Enqueue continuously while the test issues CancelThrough at random cutoffs, then a final Drain — the union of delivered + cancelled tickets equals exactly the set of assigned tickets with no duplicates and no losses; run under -race (milestone prescription, PITFALLS.md:329).
    - Transport-neutrality: a parse-level test reads steerqueue.go's import block (go/parser) and asserts zero imports from the ACP wire package (TG-02 compile-level contract, 15-D-20 layering).
  </behavior>
  <action>
Write the failing steerqueue_test.go battery FIRST (no sleeps — use the seamEventLog mutex-guarded event-log pattern from checkpoint_seam_test.go:13-32 for ordering assertions; the hammer uses plain goroutines + sync.WaitGroup), then harden SteerQueue to satisfy it. Add Pending() int (inspection count of undelivered, un-cancelled items — 23-02's parked/queued notes need it). Decisions to encode: cancelled items NEVER become steering_delivery lines (a cancelled input must never reach a later model window on replay — the acknowledgment is the CancelThrough return count + a stderr slog note, not a transcript line the Projector folds); enqueues never block the drain (single mutex, no channels — the AskBroker structural rule). No ACP-wire package imports in this file, no runtime-package imports.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run 'TestSteerQueue' -v</automated>
  </verify>
  <acceptance_criteria>
    - TestSteerQueue battery green under -race including the producer/cancel hammer.
    - go test -race -count=1 ./internal/session/ -run 'TestSteerQueueNoACP' passes (import-block parse: zero wire-package imports).
    - Ticket conservation invariant holds in the hammer: delivered + cancelled == assigned, zero duplicates.
  </acceptance_criteria>
  <done>The queue's concurrency contract is pinned by tests: monotonic tickets, arrival-order coalesced drain, cutoff cancel, no lost/duplicated inputs under -race, transport-neutral by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Projector fold hardening — anchor safety, pair safety, live/replay parity</name>
  <files>internal/session/projector.go, internal/session/steering_test.go</files>
  <behavior>
    - A turn transcript of user_message -> assistant_message -> steering_delivery -> assistant_message projects to a window containing the turn-start user content, the first assistant message, the steering marker as user-role, and the second assistant message — in that order (anchor NOT moved by the steering line; this test goes red if steering ever folds as user_message).
    - steering_delivery appearing between a tool_call and its tool_result cannot occur by construction of the drain point, but the fold case still calls flushBatch() first — a steering line after an unclosed tool batch never produces a user message inside the batch.
    - findCurrentIntent and extractSummary outputs are unchanged by the presence of steering_delivery lines (steering never becomes the turn's intent or summary).
    - Replay parity: projecting a transcript containing steering deliveries after a full Manager close/reopen produces a window byte-identical to the live projection (18-D-01 transcript-as-truth; extends reconstruction_test.go's approach).
    - A transcript from a phase-23-older build with NO steering lines projects unchanged (reader tolerance, 16-D-20).
  </behavior>
  <action>
Write the failing projector tests FIRST in steering_test.go (build Line slices directly — the projector_test.go fixture style), then adjust accumulateMidTurn only where a case is missing. The steering fold must sit in the existing mid-turn switch as its own case calling flushBatch() before appending the user-role message. Do not modify the anchor loop, findCurrentIntent, extractSummary, or boundMidTurn — the tests prove they are safe by construction with a dedicated kind. For replay parity, follow the reconstruction_test.go pattern of re-reading the transcript from disk and re-projecting.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run 'TestSteeringProject|TestSteeringReplay' -v</automated>
  </verify>
  <acceptance_criteria>
    - Anchor-safety test passes: the window after a mid-turn steering line still contains the turn's earlier exchanges (a user_message-kind regression would fail this).
    - Replay parity test passes: live window == post-reload window for a steered transcript.
    - Legacy-transcript tolerance test passes: pre-phase transcripts project identically.
    - go test -race -count=1 ./internal/session/ is fully green (whole package, not just -run filters).
  </acceptance_criteria>
  <done>Steering folds into the mid-turn window with the anchor, pairs, intent/summary extraction, and replay all proven safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/steered input -> SteerQueue | Untrusted user text crosses into the queue and, at the boundary, into the model window and the transcript |
| transcript -> Projector | steering_delivery lines fold into provider requests on replay as well as live |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Spoofing | steering marker / steering_delivery fold | high | mitigate | Marker is a rendering convention on agent-appended lines only: AppendSteeringDelivery is the sole writer (REDACTED path), nothing parses model/tool output into steering lines; the fold treats steering as user speech with no tool/permission authority (Task 1 + Task 3 anchor test) |
| T-23-02 | Denial of Service | SteerQueue growth during a long turn | medium | mitigate | Coalesced single-block delivery (D-02) bounds request growth regardless of enqueue count; Drain removes delivered items so the queue cannot accumulate across boundaries; Pending() inspection lets 23-02 surface queue depth |
| T-23-03 | Tampering | transcript replay of steering lines | medium | mitigate | steering_delivery is a distinct kind the Projector folds deterministically live and on replay (Task 3 parity test); cancelled inputs never become steering_delivery lines so they can never reach a later window |
| T-23-SC | Tampering | package installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none — stdlib + existing go.mod deps only) |
</threat_model>

<verification>
- go test -race -count=1 ./internal/session/ green (whole package)
- golangci-lint clean on the new/modified files (mise-following repos: the task-level runs above plus a final lint pass)
- The steered-request shape is cache-friendly: appending steering only extends the window tail — the prefix up to the new tail is unchanged (asserted implicitly by the Task 1 window-order criterion)
</verification>

<success_criteria>
All three tasks done: transport-neutral SteerQueue with pinned concurrency contract; boundary drain delivering one coalesced marker-wrapped user-role message into the next request; steering_delivery kind folded anchor-safely with live/replay parity; note "steering applied: N inputs" emitted at the boundary.
</success_criteria>

<output>
Create .planning/phases/23-seed-gaps-close-out/23-01-SUMMARY.md when done
</output>

## Artifacts this phase produces (full phase inventory — this plan creates the items marked HERE)

- HERE internal/session/steerqueue.go + steerqueue_test.go — SteerQueue, SteerItem, Enqueue/Drain/CancelThrough/Pending
- HERE internal/session/steering_test.go — session-package steering tests
- HERE transcript kind steering_delivery (TypeSteeringDelivery) + Manager.AppendSteeringDelivery
- HERE Session.SetSteerQueue + runTurn boundary drain + renderSteeringMarker
- HERE accumulateMidTurn steering_delivery fold (projector.go)
- (23-02) transcript kind parked_ask + Manager.AppendParkedAsk; Runner ingress classifier (pre-turnMu) + SteerQueue wiring in sessionFor; steered-prompt response = immediate queued note + end_turn; parked-cancel grammar; combined-scenario + engine-chain E2E tests
- (23-03) checkpoint pre-restore id family (grammar extended at store.go:114/364-380/454-488 together), Store.SnapshotPreRestore, nested-repo detection (findNestedRepos), Store.Sweep age+count GC, user-repo .git/info/exclude append
- (23-04) Runner-level checkpoint store handle + restore guard (active turn/chain refusal) + session-start GC sweep wiring; configOptions entries checkpoint.expiry_days (default "7") and checkpoint.max_per_session (default "50") persisted under a checkpoint: layer key and read back via the generic layer map
- (23-05) /undo as the 14th RESERVED class-B name (undo + D-11 stack walk + parseUndoDepth), D-12 auto-cancel-then-restore composition, commands_test.go /undo battery
