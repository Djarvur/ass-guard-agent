---
phase: 23-seed-gaps-close-out
plan: 06
type: execute
wave: 5
depends_on: ["23-05"]
files_modified:
  - internal/runtime/runtime.go
  - internal/runtime/commands.go
  - internal/runtime/restore_guard_test.go
autonomous: true
gap_closure: true
gap_ids: [G-23-1]
requirements: [SEEDG-02]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low # estimate-calibration: 0 samples, factor 1 (not applied); modeled on the 22-07/22-08 gap-closure rows (26k/28k for two runtime-cluster tasks) — this round is narrower: one guard widening + one composition seam + the battery

must_haves:
  truths:
    - "G-23-1 (CR-01, SEEDG-02 truth 8): a checkpoint restore NEVER proceeds while ANY session of the Runner has activity — /undo from an idle session B while session A's client turn is mid-flight REFUSES with a message naming A and its state, and Store.Restore (checkout --no-overlay -f + clean -fd over the SHARED worktree) never runs under A's live turn"
    - "The cross-session refusal OUTRANKS the D-12 auto-cancel on the active path: when B's OWN turn/chain is active AND another session is live, /undo refuses (nothing cancelled, nothing restored — the nested-refusal precedent); B's own state is auto-cancelled only when NO other session is busy, and that path stays byte-stable (TestUndoAutoCancel / TestUndoAutoCancelParkedChain pass unmodified)"
    - "The idle mutation half consults the guard BEFORE minting its pre-restore snapshot: a refusal under a busy workspace mints no torn mid-A-turn snapshot (such a snapshot would become a D-11 walk target) and mutates nothing"
    - "Same-session refusal messages stay byte-stable and the guard still returns promptly under a blocked turn: the workspace walks read turnActive (sync.Map Range over *atomic.Bool) and a chainMu-held activeChains snapshot ONLY — never a per-session turn mutex"
    - "Every cross-session refusal renders through the D-05 shape with a durable local_command record (outcome 'refused: workspace busy') and zero provider calls"
  artifacts:
    - "internal/runtime/runtime.go — restoreBlockers is workspace-scoped: a workspaceBlockers(sessionID) helper (turnActive.Range + chainMu-held activeChains scan, skipping the calling session) consulted BEFORE the own-session legs; restoreBlockedError gains an other field naming the busy session, with Error() rendering the cross-session wording; restoreUndoActive consults workspaceBlockers BEFORE SnapshotPreRestore (early refusal — nothing minted, cancelled, or restored) and branches on the discriminator at its post-snapshot detection consult; the guard's doc comment states the workspace-scoped truth, the residual check-then-act window, and the unchanged cross-PROCESS (v1.1 CLI) caveat"
    - "internal/runtime/commands.go — restoreUndo (the idle mutation half) consults restoreBlockers BEFORE SnapshotPreRestore and refuses on ANY hit (an idle session has no own state to auto-cancel; a late-own-state hit refusing is the fail-safe direction)"
    - "internal/runtime/restore_guard_test.go — TestRestoreGuardCrossSessionMatrix (guard level: other-turn cell naming the busy session, other-chain cell, idle-after-exit, promptness under a blocked turn, own-message stability) + TestUndoCrossSessionRefusal (full path: A blocked-live, idle B /undo → refusal naming A, sentinel file intact, retry after A releases → restore completes) + TestUndoActivePathCrossSessionRefusal (B parked chain + A live → refusal, B's chain NOT cancelled, nothing restored)"
  key_links:
    - "restoreBlockers workspace walk → BOTH /undo mutation halves (restoreUndoActive step 0, restoreUndo pre-snapshot): break either leg and one of the two interleavings re-opens the destructive race — B idle/A live re-opens via the idle half (which today consults NOTHING), B active/A live via the active half (which today auto-cancels then restores under A); the two full-path cells are the witnesses"
    - "restoreBlockedError.other → the D-05 refusal output + local_command outcome: the busy session id is named in the operator-visible text (the gap's naming requirement), not only in stderr logs"
  prohibitions:
    - "The walk must not take, hold, or wait on any per-session turn mutex (turnMus) and must not call sessionFor: the guard's prompt-return-under-blocked-turn property is pinned behavior; the ONLY lock the walk takes is chainMu (the leaf mutex chainCount already takes) for the activeChains snapshot."
    - "The nil-store availability probe (prepareUndo's 'unavailable: checkpoint store disabled' degrade) is untouched: the gap's truth concerns the active-state refusal of real restores, the nil-store degrade keeps its precedence (prepareUndo returns before any restore half runs), and TestRestoreGuardNilStoreAxis / TestClassBUndoDegradedStore pass unmodified."
---

<objective>
Close Phase 23 verification gap G-23-1 (23-VERIFICATION.md frontmatter `gaps:` entry 1 = review CR-01, SEEDG-02 truth 8): the restore guard is session-scoped while the workspace it mutates is process-shared. restoreBlockers (runtime.go:1995-2005) checks only the CALLING session's clientTurnActive/chainCount, but every session of the Runner shares one workDir (runtime.go:113), one workspace-scoped store (runtime.go:274, checkpointStore() at :1927), and the worktree Store.Restore force-checkouts and clean -fd's (store.go:556-583) — while turn serialization is per-session (turnMus sync.Map at :278). The code read found the hole is WIDER than the review sketched: the idle mutation half (restoreUndo, commands.go:1283) consults the guard not at all (it leans on "idle by construction", which is true only of the CALLING session), and the active half (restoreUndoActive, runtime.go:1193) uses the guard purely as auto-cancel detection — so a naive widening would make it "cancel nothing, then restore anyway". The fix is one widened guard plus a discriminator branch at both mutation halves.

Purpose: this is the exact data-loss class the guard exists to prevent — an idle session's /undo destroys a concurrent session's mid-flight work, which the live turn then launders into its next snapshot. The phase invariant "checkpoint restore hardened against active turns" (ROADMAP SC-3, SEEDG-02) is stated unscoped; neither Phase 24 nor 25 covers this.

Output: restoreBlockers workspace-scoped with the busy session named; both /undo mutation halves refusing cross-session (early — before any snapshot/cancel/restore); three regression tests in internal/runtime, red against HEAD and green after; the four-package gate green with the two documented pre-existing skips.

Gap mapping: G-23-1 = CR-01 = the verification `missing:` list, covered as — (a) the workspace-wide activity walk in restoreBlockers → Task 1; (b) the cross-session refusal-matrix test cells → Tasks 1 and 2; (c) the doc-comment truth at the guard → Task 1.

Scope guard (whole plan): WR-01 (steering turn-death race), WR-02 (prune grammar-order eviction), WR-03 (inert blob fills), IN-01..IN-05 stay deferred review debt — do not expand into them. One incidental rider is allowed: IN-05's dead `var _ = errors.Is` keeper at restore_guard_test.go:213 may be deleted IF the discriminator helper makes the errors import live (errors.As) — no other cleanup.
</objective>

<execution_context>
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/home/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-seed-gaps-close-out/23-VERIFICATION.md
@.planning/phases/23-seed-gaps-close-out/23-REVIEW.md
@.planning/phases/23-seed-gaps-close-out/23-05-SUMMARY.md

@internal/runtime/runtime.go
@internal/runtime/commands.go
@internal/runtime/restore_guard_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (G-23-1a, G-23-1c): restoreBlockers goes workspace-scoped — busy-session walk + typed naming + doc truth (CR-01 core)</name>
  <files>internal/runtime/runtime.go, internal/runtime/restore_guard_test.go</files>
  <behavior>
    - Other session's client turn live (session A blocked in the fake provider), guard consulted for idle session B: non-nil error whose message names A's session id AND the turn state (strings.Contains for both) — against HEAD this returns nil (the RED).
    - Other session's parked chain (chainEnter(A), no mutex held anywhere), guard for B: non-nil naming A's session id AND the chain state; after chainExit(A) the guard for B is nil again.
    - Calling session's own legs stay byte-stable: with A's turn live, restoreBlockers(A) still returns the SAME message shape the existing matrix pins (contains "turn", the same "for the session" wording — no "another session" text for own-state).
    - Promptness under load: the workspace-wide consult for B returns within the 2s behavioral timeout while A's turn holds A's turn mutex (the walk reads state only — sync.Map Range + one chainMu snapshot — and never blocks on any turn mutex).
    - Fully idle runner: restoreBlockers(B) is nil.
    - Existing same-session cells pass unmodified: TestRestoreGuardRefusalMatrix, TestRestoreGuardNilStoreAxis.
  </behavior>
  <read_first>internal/runtime/runtime.go :1966-2005 (restoreBlockedError + restoreBlockers + its doc comment — the cross-PROCESS caveat to keep), :278-279 (turnMus/turnActive declarations), :341-344 (chainMu/activeChains), :1722-1751 (chainEnter/chainExit/chainCount — the leaf-lock discipline the walk copies); internal/runtime/cron_wiring.go :42-58 (clientTurnActive/markClientTurn — the *atomic.Bool value type the Range asserts); internal/runtime/restore_guard_test.go :28-94 (TestRestoreGuardRefusalMatrix — the harness to extend: newBlockingRunner, prov.entered/release, the guardDone 2s-timeout pattern); 23-REVIEW.md CR-01 (the fix sketch this plan refines — note its caller-first walk ordering is DELIBERATELY inverted here, see action)</read_first>
  <action>
RED: add TestRestoreGuardCrossSessionMatrix to restore_guard_test.go with the rows above (one runner, two session ids: "sess-a-live" drives the blocked turn, "sess-b-idle" is the guard's subject; A's turn via the goroutine + <-prov.entered pattern; the promptness row reuses the matrix's channel-plus-timeout guardDone discipline). Commit failing.
GREEN: (1) New helper workspaceBlockers(sessionID) error on Runner: first, r.turnActive.Range skipping the key equal to sessionID, returning &restoreBlockedError{turn: true, other: key} for the first session whose *atomic.Bool loads true (mirror clientTurnActive's type assertion, same nolint precedent); second, under r.chainMu (Lock/defer Unlock — the ONLY lock taken), scan r.activeChains for any key other than sessionID with count > 0, returning &restoreBlockedError{chain: true, other: key}. The caller-exclusion in the walk is PRESENTATIONAL, not a predicate gap: the calling session's own activity refuses via the own legs below with the pinned message, so the net contract is "any session of the Runner" — the exclusion exists so a self-hit cannot displace the byte-stable same-session wording. (2) Widen restoreBlockers: call workspaceBlockers(sessionID) FIRST, then the existing clientTurnActive/chainCount legs unchanged. Cross-session-before-own is the deliberate inversion of the review's sketch: when B's own state AND A's are both live, the safe direction is refusal (the restore would race A regardless of what happens to B's own state), and the only production caller branches on the discriminator (Task 2) — same-session cells see no other session, so their messages are untouched. (3) restoreBlockedError gains other string; Error() branches on other != "" FIRST, rendering the cross-session wording: the existing turn/chain sentence adapted to name the OTHER session id and the shared-workspace reason (workDir and checkpoint store are shared by every session of this process; cancel that session's activity or wait) — exact bytes are the implementer's, the tests pin contains(session-id) + contains(state-word). (4) Doc comment at restoreBlockers (missing item c): state the workspace-scoped truth (the guard covers EVERY session of the Runner because the worktree and store are process-shared), the best-effort snapshot-in-time nature (state read without turn mutexes; activity STARTING after the consult is the residual window — same class as the same-session guard), and keep the existing cross-PROCESS v1.1 CLI caveat verbatim (in-process detection is what this guard provides; cross-process remains documented-undetected). If a crossSessionBlocked(err) bool helper via errors.As lands for Task 2's branch, the file's errors import goes live and the dead `var _ = errors.Is` keeper at restore_guard_test.go:213 may go; otherwise leave the keeper.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestRestoreGuard' -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/runtime/ -run 'TestRestoreGuardCrossSessionMatrix|TestRestoreGuardRefusalMatrix|TestRestoreGuardNilStoreAxis'` exits 0 (new row plus both existing cells unmodified).
    - `grep -c 'func (r \*Runner) workspaceBlockers' internal/runtime/runtime.go` is 1, and restoreBlockers consults it before the clientTurnActive leg (`grep -n 'workspaceBlockers(sessionID)' internal/runtime/runtime.go` shows the call inside restoreBlockers above the clientTurnActive check).
    - The walk's only lock acquisition is chainMu: the workspaceBlockers body contains no turnMus/sessionTurnMu/sessionFor reference (`sed -n '/func (r \*Runner) workspaceBlockers/,/^}/p' internal/runtime/runtime.go | grep -c 'sessionTurnMu\|sessionFor'` is 0).
    - `go vet ./internal/runtime/` clean.
  </acceptance_criteria>
  <done>The guard refuses for ANY busy session of the Runner — own-state messages byte-stable, cross-session refusals naming the busy session and state, prompt under a blocked turn, and the doc comment tells the workspace-scoped truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (G-23-1b): both /undo mutation halves refuse cross-session — never cancel-nothing-then-restore, never a torn snapshot</name>
  <files>internal/runtime/runtime.go, internal/runtime/commands.go, internal/runtime/restore_guard_test.go</files>
  <precondition>Task 1 landed — `grep -c 'func (r \*Runner) workspaceBlockers' internal/runtime/runtime.go` is 1 and the cross-session matrix is green.</precondition>
  <behavior>
    - Full idle path: session A's turn blocked in the fake provider, session B idle with a seeded checkpoint plus a post-snapshot sentinel file; B's /undo (through r.Run, the real classifier + tryLocalCommand intercept) returns promptly with a D-05 refusal naming A's session id, the sentinel file still exists (no checkout/clean ran), A's turn is still alive (turnDone open), and the transcript carries the local_command record with outcome "refused: workspace busy" — against HEAD the restore runs and the sentinel is deleted (the RED).
    - Transience: after A's turn finishes (release + <-turnDone), B's /undo again RESTORES — "undo complete" output, sentinel gone (the refusal was a state refusal, not a wedge).
    - Active path branch: A's turn live AND B's own engine chain parked (chainEnter(B)); B's mid-turn "/undo" (through r.Run — the classifier routes it to routeUndoActive) returns the cross-session refusal naming A, r.chainCount(B) is STILL 1 (B's own chain was not cancelled — the refusal outranks the auto-cancel), the sentinel is intact, and A is still blocked. Against HEAD this cell auto-cancels B's chain and restores under A's live turn (RED).
    - Own-state auto-cancel unchanged: TestUndoAutoCancel, TestUndoAutoCancelParkedChain, TestUndoFailClosed, TestUndoNestedRefusal, TestParkedChainCancelSteering pass unmodified (single-session fixtures — the widened guard finds no other session and falls through to the own legs).
    - Zero provider calls for every B /undo invocation (class-B invariant holds on the refusal legs).
    - Nil-store precedence untouched: TestClassBUndoDegradedStore passes unmodified (the availability degrade still returns from prepareUndo before any restore half runs).
  </behavior>
  <read_first>internal/runtime/runtime.go :1108-1159 (routeUndoActive — prepareUndo then restoreUndoActive, the D-05 echo/output/local_command plumbing to reuse verbatim), :1161-1219 (restoreUndoActive + its numbered-ordering doc: where step 0 slots in and what must NOT move), internal/runtime/commands.go :1237-1322 (prepareUndo — DO NOT add a guard here: a full-guard consult at the shared head would refuse B's OWN active state and kill the D-12 auto-cancel path; restoreUndo :1283-1304 — the idle mutation half that today consults nothing), internal/runtime/restore_guard_test.go (writeGuardFile, snapDatedAt helpers), internal/runtime/commands_test.go :1749-1790 (seedUndoSnap — writes the canary then Snapshots through the real store; undoRun — the Run-end-to-end driver; both same-package and reusable from restore_guard_test.go), 23-05-SUMMARY.md (D-11/D-12 wiring + the pinned ordering contract this task must preserve)</read_first>
  <action>
RED: add TestUndoCrossSessionRefusal and TestUndoActivePathCrossSessionRefusal to restore_guard_test.go. Shared shape: newBlockingRunner (A's scripted turn), seedUndoSnap for B (session id "sess-b-idle"), then a post-snapshot sentinel file via writeGuardFile (a file created AFTER B's checkpoint — a successful restore's checkout --no-overlay + clean -fd deletes it, making "restore happened" directly observable). Drive B's /undo as r.Run in a goroutine with a result channel and the 2s timeout select (the matrix's guardDone discipline — a regression that makes B wait on A's mutex must fail the timeout, not hang the suite); assert the emitted output and transcript lines per the behavior rows. Commit failing (both cells: HEAD restores under A's live turn).
GREEN: (1) restoreUndoActive gains step 0 (before SnapshotPreRestore): consult r.workspaceBlockers(sessionID); on non-nil, return the refusal output (fmt shape "undo refused: %v\n" over the typed error) with outcome "refused: workspace busy" — nothing snapshotted, nothing cancelled, nothing restored. Rationale for the early position: the pre-restore snapshot would mint a commit of the SHARED tree possibly mid-write under A's turn, and that snapshot becomes a D-11 recency-walk target B could later restore INTO — minting it under a busy workspace is the torn-tree shape; and the D-12 snapshot-before-CANCEL ordering is untouched because a refusal cancels nothing. (2) At the EXISTING post-snapshot detection consult (blocked := r.restoreBlockers(sessionID)), branch on the discriminator (a crossSessionBlocked helper via errors.As, or a same-package type assertion on *restoreBlockedError checking other != ""): cross-session → the same refusal output/outcome (the safety net for activity starting between step 0 and here); own-state → the existing auto-cancel sequence byte-stable. Update restoreUndoActive's numbered doc comment: insert step 0 and note the outranking (cross-session refusal, like the nested refusal, never cancels). (3) commands.go restoreUndo (idle half): BEFORE undoSnapshotPreRestore, consult r.restoreBlockers(sess.SessionID); on non-nil return the same refusal output + outcome. Any hit here is cross-session or a late-start own state (the idle intercept runs only after the classifier found B inactive, and B holds its own turn mutex — nothing of B's can legitimately auto-cancel here), and refusing is the fail-safe direction; the pre-snapshot position avoids the same torn-mint. Do NOT touch prepareUndo (the nil-store degrade keeps precedence there — a nil store restores nothing, so the availability note is the correct first exit) or routeUndoActive's plumbing (the refusal reuses its D-05 echo/output/local_command path verbatim).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestUndoCrossSessionRefusal|TestUndoActivePathCrossSessionRefusal|TestRestoreGuard|TestUndoAutoCancel|TestUndoFailClosed|TestUndoNestedRefusal|TestClassB.*Undo|TestUndoWalk' -v</automated>
  </verify>
  <acceptance_criteria>
    - Both new cells pass; the sentinel-intact and chain-count-unchanged assertions hold on every refusal leg, and the transience row (retry after A exits → "undo complete", sentinel gone) passes.
    - The single-session /undo battery passes unmodified: `go test -race -count=1 ./internal/runtime/ -run 'TestClassBUndoIdle|TestClassBUndoEdges|TestClassBUndoCrossSession|TestClassBUndoDegradedStore|TestUndoWalk|TestUndoAutoCancel|TestUndoFailClosed|TestUndoNestedRefusal'` exits 0.
    - `grep -c 'restoreBlockers(sess.SessionID)' internal/runtime/commands.go` is 1 and it sits above the undoSnapshotPreRestore call inside restoreUndo.
    - restoreUndoActive consults workspaceBlockers before undoSnapshotPreRestore (`sed -n '/func (r \*Runner) restoreUndoActive/,/turnMu.Lock/p' internal/runtime/runtime.go | grep -n 'workspaceBlockers'` shows a hit preceding the first undoSnapshotPreRestore line).
    - `go test -race -count=1 -skip 'TestRescanConcurrency' ./internal/runtime/` exits 0 (the -skip excludes ONLY the documented pre-existing rescan race — deferred-items.md; nothing else).
    - `go vet ./internal/runtime/` clean.
  </acceptance_criteria>
  <done>/undo can no longer mutate the shared worktree under another session's live turn on either path: idle refuses before minting anything, active refuses instead of cancelling-and-restoring, own-state auto-cancel and every 23-05 pin are untouched, and every refusal is a named, durable, transient state — retry after the busy session ends restores normally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session B slash input → shared worktree | An idle session's /undo triggers destructive git operations (checkout -f + clean -fd) over a worktree OTHER sessions are concurrently mutating |
| runner-wide activity state → restore admission | The guard reads cross-session state (turnActive/activeChains) to admit or refuse a mutation — its snapshot-in-time nature is part of the trust calculation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-19 | Tampering | restoreBlockers/restoreUndo/restoreUndoActive vs Store.Restore | critical | mitigate | Tasks 1-2: workspace-wide refusal on BOTH mutation halves, consulted before any snapshot/cancel/restore, naming the busy session; the sentinel-file battery witnesses that no checkout/clean runs under a live turn |
| T-23-20 | Tampering | pre-restore snapshot minted under a busy workspace | medium | mitigate | Task 2: the early consult precedes SnapshotPreRestore on both halves — no mid-A-turn tree commit can become a D-11 walk target B later restores into |
| T-23-21 | Denial of Service | cross-session refusal wedging /undo | low | accept | The refusal is state-based and transient — the battery pins the retry-after-release restore; a permanently-busy workspace refusing restores is the correct conservative posture (the same class as the same-session refusal) |
| T-23-SC | Tampering | package installs | low | accept | No package installs this plan (RESEARCH Package Legitimacy Audit: none for this phase) |
</threat_model>

<verification>
- `go test -race -count=1 -skip 'TestRescanConcurrency' ./internal/runtime/` green — the three new rows plus every existing 23-01..23-05 row (strictly additive on paths the green batteries already prove). The -skip excludes ONLY the documented pre-existing spawnMCP/installRegistry race (deferred-items.md, status open) — with it included the package is red at HEAD regardless of this plan.
- `go test -race -count=1 -skip 'TestPermissionsE2E' ./internal/session/ ./internal/checkpoint/ ./internal/acpserve/` green — the TestPermissionsE2E skip is the documented cross-workstream regression (Phase 23 commit 40b2bbc, STATE.md Blockers/Concerns), outside this plan's files.
- `go vet ./internal/runtime/ ./internal/checkpoint/` clean; `GOOS=linux CGO_ENABLED=0 go build ./...` green (no new deps — sync/atomic and sync are already imported).
- Re-verification mapping: G-23-1 → TestRestoreGuardCrossSessionMatrix (guard scope + naming + promptness) + TestUndoCrossSessionRefusal (idle full path) + TestUndoActivePathCrossSessionRefusal (active-path branch + outranking).
- Operator item (not a gate of this plan): the two-live-Zed-session UAT (23-VERIFICATION.md Human Verification #2) becomes confirmable once this lands — it rides the phase's WINDOWS ledger beside #23, per the 23-05 precedent; the offline battery is the gap's witness.
</verification>

<success_criteria>
- The gap truth holds and is pinned by tests that fail against HEAD: restore refuses while ANY session of the Runner is active, on both /undo paths, with the busy session and state named in the operator-visible refusal and the durable local_command record.
- No behavior change on any path the existing batteries pin: same-session messages byte-stable, D-12 own-state auto-cancel and its locked ordering untouched, nil-store degrade precedence untouched, zero-provider-call invariant extended to the refusal legs.
- Zero scope creep: WR-01..WR-03 and IN-01..IN-05 untouched (the errors-import keeper deletion is the one allowed rider, and only if the import goes live).
</success_criteria>

<output>
Create `.planning/phases/23-seed-gaps-close-out/23-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan: workspace-scoped restoreBlockers + workspaceBlockers walk + restoreBlockedError.other naming, cross-session refusal branches at restoreUndoActive/restoreUndo, TestRestoreGuardCrossSessionMatrix + TestUndoCrossSessionRefusal + TestUndoActivePathCrossSessionRefusal)
