---
phase: 23-seed-gaps-close-out
plan: 04
type: execute
wave: 3
depends_on: [23-02, 23-03]
files_modified:
  - internal/runtime/runtime.go
  - internal/runtime/restore_guard_test.go
  - internal/acpserve/config_surface.go
autonomous: true
requirements: [SEEDG-02]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Runner holds the checkpoint store once per workspace — /undo, the restore guard, and the GC sweep never depend on a session-scoped handle, and a degraded (nil-store) session reports /undo unavailable loudly instead of silently (Pitfall 9, AUD-03)"
    - "Restore refuses when a turn or engine chain is active for the session — including parked chains (chainCount > 0 with no mutex held) — with a refusal that names the active state (SEEDG-02)"
    - "The age+count GC sweeps at session start (D-08, 18-D-09 grace-family precedent) and its failure is loud but never fatal to the session"
    - "checkpoint.expiry_days (default 7) and checkpoint.max_per_session (default 50) are advertised in the configOptions menu as enumerated selects, accept-validated, persisted under a checkpoint: layer key, and read back for the GC via the generic layer map (D-10, Open Question 2 resolution)"
    - "Set accepts the new option ids without the unknown-option rejection (isMenuOption learns them — Pitfall 10)"
  artifacts:
    - Runner-level checkpoint store field + sessionFor wiring change in internal/runtime/runtime.go
    - Runner restore-guard composition over turnActive + chainCount/WaitChainIdle
    - session-start Sweep call with config-driven maxAge/perSession
    - optCheckpointExpiryDays / optCheckpointMaxPerSession selects in internal/acpserve/config_surface.go
    - checkpoint: layer-key persistence (WriteLayerOption) + readLayerMap read-back
    - internal/runtime/restore_guard_test.go
  key_links:
    - "sessionFor store construction (runtime.go:1004-1011, 1201-1203) -> Runner field: today the store vanishes into the session adapter; the promotion is what makes /undo and the guard reachable (Pitfall 9)"
    - "readLayerMap/mapHasPath (config_surface.go:629-668) -> Sweep arguments: the typed modelrouting Config silently DROPS the checkpoint: key (load.go verified), so the generic map read is the ONLY path that sees the persisted values — break it and the GC silently runs on defaults forever"
  flagged_assumptions:
    - "Open Question 3 resolution (documented constraint, not silent): the v1.1 CLI restore path is a separate process — in-process turn/chain state is invisible cross-process; guards + auto-cancel apply to the agent's in-process restore path ONLY, the CLI keeps today's behavior with its documented caveat. No cross-process turn detection is built."
  prohibitions:
    - "A nil/degraded checkpoint store must never disable /undo silently: the unavailable state is reported to the operator in the command output, never swallowed (AUD-03 loud-degradation discipline)."
---

<objective>
SEEDG-02 composition: promote the checkpoint store to a Runner-level service, compose the restore guard over the existing race-tested turn/chain state, wire the D-08 session-start GC sweep with config-driven bounds, and ship the D-10 configOptions entries with their persistence + read-back path.

Purpose: the 23-03 store operations are inert until the Runner owns them; the menu entries are the operator's only knob over GC bounds (16-05's advertise-all, apply-as-landed rule).

Output: runtime.go store promotion + guard + sweep wiring, config_surface.go two selects, and the TestRestoreGuard behavioral battery.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-seed-gaps-close-out/23-CONTEXT.md
@.planning/phases/23-seed-gaps-close-out/23-RESEARCH.md
@.planning/phases/23-seed-gaps-close-out/23-PATTERNS.md
@.planning/phases/23-seed-gaps-close-out/23-03-SUMMARY.md

@internal/runtime/runtime.go
@internal/acpserve/config_surface.go
@internal/modelrouting/load.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runner-level store handle + restore guard + session-start GC sweep</name>
  <files>internal/runtime/runtime.go, internal/runtime/restore_guard_test.go</files>
  <read_first>
    - internal/runtime/runtime.go — sessionFor store construction (1004-1011, the loud open-failure log at 1010) and adapter attach (1201-1203); turnActive sync.Map (181-182); chainCount/WaitChainIdle (946-969); the checkpointerAdapter seam (256-268) that keeps the session package import-free of the checkpoint package
    - internal/checkpoint/store.go — the 23-03 surfaces: SnapshotPreRestore, RestoreGuard (nested detection), Sweep(ctx, maxAge, perSession)
    - 23-RESEARCH.md Pattern 5 consequence 5 (the Runner must hold the store) + Pitfall 9
  </read_first>
  <action>
Promote the store: sessionFor currently builds ckptStore as a local var and drops it after attaching the adapter — hoist construction to a Runner-level field built once per workspace (lazy-init with the existing open-failure log discipline: nil store = sessions run WITHOUT undo snapshots, logged loudly exactly as runtime.go:1010 does today; the per-session adapter attach stays). Guard composition: a Runner method (restoreBlockers(sessionID) error shape) that returns a typed refusal when turnActive marks the session active OR chainCount(sessionID) > 0 — the parked-chain case is the trap (no mutex held, chains still active), so the check is over chainCount, never over mutex ownership; the refusal message names which state blocked. This method is the SEEDG-02 guard every restore path calls (23-05's /undo composes it; non-/undo in-process restore paths refuse). Session-start sweep: in sessionFor, after a successful store open, run Sweep with bounds read per Task 2 (defaults 7d/50 until Task 2 lands the read-back — wire the call through a small accessor so Task 2 fills it without reshaping); failure logs via slog to stderr and continues (AUD-03 — never fails the session). exclude-append: also at store-open, call the 23-03 ensureUserRepoExclude for the workspace and log the typed skip when not a repo. Tests in restore_guard_test.go following checkpoint_session_test.go fixtures: guard refuses with an in-flight turn (blocked fake provider), refuses with a parked chain (chainCount > 0 after suspension), passes when idle; sweep fires on session start (fixture store with an over-age ref gets evicted by merely creating a session); nil-store degradation keeps sessions fully functional.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run 'TestRestoreGuard|TestSessionStartSweep' -v</automated>
  </verify>
  <acceptance_criteria>
    - Guard tests pass: active-turn refusal, parked-chain refusal, idle pass-through — each naming the blocking state in the error.
    - Sweep-on-start test passes: creating a session evicts an expired fixture ref without any explicit GC call.
    - Degradation test passes: with the store unopenable, sessions run normally and the loud log fires (assert via the log hook or stderr capture).
    - The mutex-ordering behavioral property: the guard check itself never blocks on the turn mutex (it reads turnActive/chainCount only) — asserted by calling it while a fake provider holds the turn; it returns the refusal promptly.
  </acceptance_criteria>
  <done>The Runner owns the store; every restore path can refuse on active turn/chain; GC sweeps at session start with loud non-fatal failures; the user-repo exclude rides store open.</done>
</task>

<task type="auto">
  <name>Task 2: D-10 configOptions — checkpoint.expiry_days + checkpoint.max_per_session (menu, persist, read-back)</name>
  <files>internal/acpserve/config_surface.go</files>
  <read_first>
    - internal/acpserve/config_surface.go — the build closure (501-531), setPendingLocked accept-and-log (352-371), isMenuOption/isPendingOption/pendingValues (601-625 — the compaction numeric-as-select precedent), readLayerMap/mapHasPath (629-668)
    - internal/modelrouting/load.go — the verified decode path (43-79): plain-yaml into map[string]any, typed decode silently drops unknown top-level keys — the reason read-back MUST use the generic map
    - 23-RESEARCH.md Pitfall 10 + Open Question 2
  </read_first>
  <action>
Two new menu entries per the pending-select pattern: optCheckpointExpiryDays = "checkpoint.expiry_days" (select values "1","3","7","14","30", current default "7") and optCheckpointMaxPerSession = "checkpoint.max_per_session" (select values "10","25","50","100", current default "50" — A3, aligned with DefaultKeep). Wire them into isMenuOption (or Set rejects them with the unknown-option error — Pitfall 10), isPendingOption, and pendingValues; advertise through the existing build closure with category + description consistent with the compaction entry. Accept: membership-validated via pendingValues, one structured log line, persist via WriteLayerOption under the checkpoint: key path (expiry as checkpoint.expiry_days) using the D-07 write-half round-trip contract from 16-04. Read-back: a small accessor (checkpointGCBounds(layers) (days int, perSession int)) that reads the two keys via readLayerMap/mapHasPath and falls back to the embedded defaults when absent — this accessor is what 23-04 Task 1's sweep call consumes. The typed modelrouting.Config is NOT extended (verified: it drops the key silently; the generic map is the sanctioned path — Open Question 2 RESOLVED as recommended by research). Values are strings on the wire (select) — parse to ints at the accessor with a loud default-fallback on an unparseable persisted value.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ -run 'TestCheckpointOptions' -v && go test -count=1 ./internal/acpserve/ -run TestConfig -v</automated>
  </verify>
  <acceptance_criteria>
    - Advertisement test: the configOptions payload contains both new ids as select-type frames with the enumerated values and correct defaults.
    - Set-accept test: setting each id to a listed value persists the checkpoint: layer key (verify by re-reading the layer file generically); setting an unlisted value is rejected with the membership error.
    - Read-back test: checkpointGCBounds returns persisted values after a Set + reopen, and the embedded defaults (7/50) when no key exists; an unparseable persisted value falls back to defaults with a logged note.
    - The existing config battery (TestConfig*) stays green — no regression on the pending-option family.
  </acceptance_criteria>
  <done>Both GC knobs are advertised, validated, persisted under checkpoint:, and read back through the generic layer map with default fallback — the operator controls GC bounds from the editor.</done>
</task>

<task type="auto">
  <name>Task 3: Guard integration battery + wave gate</name>
  <files>internal/runtime/restore_guard_test.go</files>
  <action>
Consolidate the guard behaviors into the integration battery: (a) the full refusal matrix (active client turn / running chain / parked chain / idle) x (store present / nil store); (b) the session-start sequence ordering: exclude append -> sweep -> session ready, each step's failure degrading loudly without failing the session; (c) one test asserting the config-driven bounds actually reach the sweep (persist max_per_session=10 via the Task 2 accessor path in a fixture layer file, create sessions until 11 checkpoints of one session exist, observe the oldest evicted at the next session start). This is the wave gate: run the full four-package suite before commit.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ ./internal/runtime/ ./internal/checkpoint/ ./internal/acpserve/</automated>
  </verify>
  <acceptance_criteria>
    - The refusal-matrix table test passes in all 8 cells.
    - The config-to-GC test passes: persisted bound 10 is honored by the session-start sweep.
    - Full four-package suite green under -race (wave gate).
  </acceptance_criteria>
  <done>Guard matrix, session-start ordering, and the config-to-GC wiring are proven end-to-end; the wave gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor configOptions Set -> layer file | Wire-supplied values persist into operator config files |
| persisted layer keys -> GC bounds | Config values parameterize a destructive operation (checkpoint eviction) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-12 | Tampering | restore during active turn/chain | high | mitigate | The guard composes over turnActive + chainCount (race-tested primitives) and is on every restore path; prompt return + non-blocking check proven by test (Task 1) |
| T-23-13 | Denial of Service | config-driven GC bounds abused (0/expired-huge values) | medium | mitigate | Select-type wire constraint pins values to enumerated sets (Pitfall 10); the accessor validates parse + falls back loud to defaults on junk (Task 2) |
| T-23-14 | Information Disclosure | checkpoint keys leaking into typed config surfaces | low | accept | The checkpoint: key is intentionally invisible to modelrouting.Config (verified decode path); read-back is the generic map — no new disclosure surface |
| T-23-SC | Tampering | package installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- go test -race -count=1 ./internal/session/ ./internal/runtime/ ./internal/checkpoint/ ./internal/acpserve/ green (wave gate)
- golangci-lint clean on modified files
- The 16-05 config-surface battery stays green (no pending-option regressions)
</verification>

<success_criteria>
Runner owns the store; restore paths refuse on active turn/chain (incl. parked chains); session-start GC sweeps with persisted, editor-controlled bounds (checkpoint.expiry_days / checkpoint.max_per_session); nil-store degradation stays loud and non-fatal.
</success_criteria>

<output>
Create .planning/phases/23-seed-gaps-close-out/23-04-SUMMARY.md when done
</output>

## Artifacts this phase produces (this plan: Runner ckpt store field + restoreBlockers guard + session-start Sweep wiring + ensureUserRepoExclude call, optCheckpointExpiryDays/optCheckpointMaxPerSession selects, checkpoint: persistence + checkpointGCBounds read-back, restore_guard_test.go)
