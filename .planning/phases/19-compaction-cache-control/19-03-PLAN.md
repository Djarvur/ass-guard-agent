---
phase: 19-compaction-cache-control
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/transcript.go
  - internal/session/manager.go
  - internal/session/projector.go
  - internal/session/transcript_newkinds_test.go
  - internal/session/projector_test.go
autonomous: true
requirements: [PAR-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The compaction line carries the summary payload additively: AppendCompaction writes it through the redacted append path, it round-trips on disk, and readers that predate the field still parse the line (D-20 additive-only, 16-D-21 contract extended)"
    - "A transcript with no compaction line projects byte-identically to pre-phase behavior — every existing session and every 16-02 tolerance pin stays true (backward compat)"
    - "When the winning reset point before the projected turn is a compaction marker, the seed is the marker's summary in the single-user-message lean-seed shape, and it SURVIVES later TypeBoundary lines (D-06 durable seed); after a later boundary the seed is the summary alone (research Open Question 3, planner-pinned summary-only reading)"
    - "The marker resets only turns that START after it, exactly like TypeBoundary; a subagent whose turn is in flight when a marker lands keeps its own mid-turn window unchanged (Pitfall 5)"
    - "The post-compaction window is the durable summary seed followed by a budget-fill tail of the most recent post-marker messages, cut only at group boundaries — the tail never starts with an orphaned tool result (D-04, D-05)"
    - "Thinking content is never rewritten by the cut: projection folds transcript lines mechanically and only ever drops complete leading groups (PAR-01's never-rewrite letter, by construction)"
  artifacts:
    - path: "internal/session/transcript.go"
      provides: "Summary field on the compaction Line (on-disk summary, omitempty, camelCase + tagliatelle marker per file convention)"
      contains: "Summary"
    - path: "internal/session/manager.go"
      provides: "AppendCompaction extended to accept and write the summary payload (additive signature change; the method has no pre-phase production callers)"
      contains: "AppendCompaction"
    - path: "internal/session/projector.go"
      provides: "the compaction reset-point class: scan accepts TypeCompaction, durable summary seed, budget-fill pair-safe tail cut with an injectable token budget"
      contains: "TypeCompaction"
    - path: "internal/session/projector_test.go"
      provides: "TestProjector_CompactionResetPoint (boundary survival, subagent safety, no-marker identity) + TestProjector_CompactionTailCut (pair atomicity, budget fill)"
      contains: "TestProjector_CompactionResetPoint"
  key_links:
    - from: "internal/session/manager.go"
      to: "internal/session/projector.go"
      via: "AppendCompaction's line is the reset point the projector scan consumes: summary → durable seed, post-pointer → tail start"
      pattern: "TypeCompaction"
    - from: "internal/session/projector.go"
      to: "internal/session/compaction.go (plan 19-04)"
      via: "the projector's compaction budget is injected by the session's modelrouting-resolved context window; zero budget falls back to the MidTurnWindowMessages count"
      pattern: "MidTurnWindowMessages"
  prohibitions:
    - requirement_id: PAR-01
      category: transcript-integrity
      statement: "compaction MUST never mutate, rewrite, or truncate existing transcript lines — the transcript is append-only; compaction appends a marker and changes only future projections"
    - requirement_id: PAR-01
      category: projection
      statement: "the durable seed MUST never be displaced or re-derived by a later mutating boundary — only a NEWER compaction marker's summary replaces it (D-06's letter)"
    - requirement_id: PAR-01
      category: pair-atomicity
      statement: "the tail cut MUST never leave an orphaned tool result at its head — the cut advances past tool-role messages to the next group head (boundMidTurn discipline, generalized)"
---

<objective>
Land PAR-01's projection semantics: the compaction marker becomes the Projector's third class — a durable reset point whose summary survives later mutating boundaries — and the budget-fill tail cut generalizes the existing pair-safe bounding from message count to token budget. This plan is pure internal/session semantics, testable entirely from synthetic transcripts; 19-04 attaches the engine that produces real markers.

Purpose: the two genuine design acts of this phase live here (RESEARCH's key insight): durable-seed semantics and budget-fill math. Pinning them projector-first means the engine in 19-04 lands against proven semantics instead of debugging both at once.
Output: summary-bearing marker kind consumption, reset-point scan extension, budget-fill cut, and the pinned Projector tests ROADMAP criterion 2 demands.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-compaction-cache-control/19-CONTEXT.md
@.planning/phases/19-compaction-cache-control/19-RESEARCH.md
@.planning/phases/19-compaction-cache-control/19-PATTERNS.md
@.planning/phases/16-acp-wire-foundation/16-02-PLAN.md
@internal/session/transcript.go
@internal/session/manager.go
@internal/session/projector.go
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/session/transcript.go` — compaction Line Summary field (consumes TypeCompaction from 16-02; adds the summary payload home).
- `internal/session/manager.go` — AppendCompaction with the summary parameter.
- `internal/session/projector.go` — compaction reset-point scan + durable seed + budget-fill tail cut + injectable compaction budget.
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Summary payload on the compaction line</name>
  <files>internal/session/transcript.go, internal/session/manager.go, internal/session/transcript_newkinds_test.go</files>
  <precondition>Phase 16's 16-02 landed TypeCompaction and AppendCompaction (the phase executes after 16 by milestone order). Verify with grep before starting: internal/session/transcript.go declares TypeCompaction and manager.go declares AppendCompaction per the 16-02 contract (boundary id, timestamp, usage snapshot, pre/post pointers). If the kind is absent because Phase 16 changed scope, halt and reconcile with the orchestrator — do not silently re-add a divergent kind.</precondition>
  <behavior>
    - AppendCompaction (extended signature) writes type "compaction" with the 16-02 field set PLUS the summary text; the on-disk field name is summary (camelCase, omitempty, tagliatelle marker per file convention).
    - The summary rides the REDACTED append path (16-02/D-23 scoping: only raw_thinking is redaction-exempt) — a counting-Redactor fake records a call for an AppendCompaction invocation.
    - The summary round-trips: append, re-read the file, the line's Summary equals the written text exactly.
    - A compaction line WITHOUT the summary field (hand-written fixture, pre-field shape) parses with an empty Summary and no error — readers are field-tolerant.
  </behavior>
  <read_first>internal/session/transcript.go (kind const block :18-49 — TypeCompaction landed by 16-02; Line struct :61-115 field-grouping convention with omitempty and the tagliatelle markers), internal/session/manager.go (AppendCompaction as 16-02 landed it — extend, do not duplicate; AppendUsage :234 for the one-liner style; the redacted appendLine path), internal/session/transcript_newkinds_test.go (16-02's compaction cases — extend in place; the counting-Redactor fake already exists there), .planning/phases/16-acp-wire-foundation/16-02-PLAN.md (the AppendCompaction field contract this task extends)</read_first>
  <action>
RED: extend transcript_newkinds_test.go's compaction cases with the four behaviors above (extended append + redactor-called + round-trip + field-tolerant parse). Commit failing.
GREEN: add the Summary field to the compaction group in the Line struct following the file's field-grouping convention; extend AppendCompaction's signature with the summary parameter and set the field. The method's only callers are this phase (16-02 shipped it schema-only), so the signature change is additive with no call-site fallout — verify with grep that no other package calls it. Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestTranscriptNewKinds' -count=1 && go test ./internal/session/ -count=1 && go vet ./internal/session/</automated>
  </verify>
  <acceptance_criteria>
    - The extended compaction cases pass: summary written, redactor invoked (counting fake), round-trip exact, field-tolerant parse.
    - internal/session/transcript.go's Line carries the Summary field with the summary json tag.
    - grep confirms no non-session package calls AppendCompaction (no signature fallout).
  </acceptance_criteria>
  <done>The marker's payload home exists: 18-D-01's marker+summary reconstruction guarantee now has an on-disk field, additively.</done>
  <reversibility rating="costly">The one-way door (additive-only transcript schema) was walked and user-confirmed as D-20 in Phase 16; this extension is field-additive under that locked discipline with tolerant readers proven by 16-02's unknown-field fixture — removing the summary field later would cost a reconstruction migration for markers already on disk, but does not reopen the schema decision.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Durable reset-point semantics in the Projector</name>
  <files>internal/session/projector.go, internal/session/projector_test.go</files>
  <behavior>
    - The reset-point scan (today: TypeBoundary only, most recent before the projected turn's user message) also accepts TypeCompaction lines as reset points.
    - Winning reset point is a compaction marker: the projected seed is the marker's Summary text in the same single-user-message lean-seed shape the mechanical post-boundary seed uses (the "Task summary" wrapper shape at the seed assembly site — reuse the shape, source it from the marker).
    - DURABILITY: a TypeBoundary line AFTER the marker does not displace the summary — the seed stays the marker's summary alone (D-06; summary-only after a later boundary).
    - A transcript with no compaction line projects byte-identically to pre-phase output (existing projector tests + a new explicit pin over a boundary-only transcript).
    - SUBAGENT SAFETY: a marker appended during a subagent's in-flight turn leaves that subagent's own mid-turn window unchanged; the next parent turn after the marker gets the summary seed (the marker resets turns that START after it, never the producing or in-flight turn's own window).
    - The seed lookup is the most recent marker BEFORE the projected turn's user message — an earlier marker plus later boundaries still yields that marker's summary.
  </behavior>
  <read_first>internal/session/projector.go (the reset-point scan :141-147 verbatim in 19-PATTERNS; splitAtResetBoundary's semantics — a boundary resets projections of turns that START after it; the seed assembly site :92-101 — the one-user-message lean-seed shape with the mechanical summary wrapper; accumulateMidTurn's TurnID filter :195-199; extractSummary — the mechanical no-marker path that must remain the no-marker behavior), internal/session/projector_test.go (existing boundary/lean-seed pins to keep green), .planning/phases/19-compaction-cache-control/19-RESEARCH.md (Pattern 1 + Pitfall 5), .planning/phases/19-compaction-cache-control/19-PATTERNS.md (projector.go section)</read_first>
  <action>
RED: write TestProjector_CompactionResetPoint in projector_test.go covering all six behaviors with synthetic transcripts built through the Manager (or the test-fixture builder pattern the file already uses): marker-as-winner seed, boundary-after-marker durability, no-marker byte-identity, subagent in-flight window unchanged, most-recent-marker-before-user-message lookup, and the later-marker-replaces-earlier case. Commit failing.
GREEN: extend the scan to accept TypeCompaction; thread the winning marker's summary into the seed assembly site in the existing one-user-message shape (the compaction seed replaces the mechanical extractSummary output only when a marker is the winning reset point or survives as the durable seed behind later boundaries — when a boundary wins and a marker exists earlier, the seed is that marker's summary alone). Keep every no-marker path byte-identical: the new code paths must be reachable only when a compaction line exists in the scanned range. Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestProjector_CompactionResetPoint|TestProjectorToleratesNewKinds' -count=1 && go test ./internal/session/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestProjector_CompactionResetPoint passes under -race: seed durability across a later boundary, no-marker byte-identity, subagent window unchanged, marker selection by position.
    - The pre-existing projector tests pass unmodified (no regression in boundary or lean-seed behavior).
    - 16-02's tolerance pin still passes: kinds inert without markers, active as reset points with them.
  </acceptance_criteria>
  <done>The durable reset-point class exists: the summary is a seed that survives mutating boundaries, and every transcript without a marker behaves exactly as before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Budget-fill pair-safe tail cut</name>
  <files>internal/session/projector.go, internal/session/projector_test.go</files>
  <behavior>
    - The post-marker tail is filled from the most recent post-marker messages (converted with the same line-to-message folding accumulateMidTurn uses) up to an injectable token budget, cutting only at group boundaries.
    - The cut advances past tool-role messages so the tail never STARTS with an orphaned tool result (the boundMidTurn group-advance discipline, generalized from fixed count to token bound).
    - Budget math: a message's cost is its folded JSON size divided by 4 (truncating); the tail accumulates most-recent-first (or drops leading groups) until the next group would exceed the budget.
    - Fill target (planner-pinned discretion under D-05): summary estimate + tail cost stay at or under 60 percent of the context limit, leaving headroom under the 80 percent trigger.
    - Zero/unset budget with a marker present degrades to the existing MidTurnWindowMessages count applied to the post-marker tail (never an empty window, never an unbounded one).
    - Thinking-bearing transcripts: the cut drops only complete leading groups; no line's content is rewritten (assert a transcript interleaving raw_thinking lines — folded per today's rules — survives the cut untouched within the kept groups).
  </behavior>
  <read_first>internal/session/projector.go (boundMidTurn :261-276 — the verbatim group-advance loop to generalize; MidTurnWindowMessages :32; the projector's construction path — wherever the Session wires it, to place the injectable budget), internal/session/session.go (the Projector construction/wiring site), internal/session/projector_test.go (Task 2 fixtures to build on), .planning/phases/19-compaction-cache-control/19-RESEARCH.md (Pattern 2 + Pitfall 5)</read_first>
  <action>
RED: write TestProjector_CompactionTailCut with the behaviors above: a long post-marker transcript with interleaved tool_use/result pairs where the budget forces a cut inside pair territory — assert the kept tail starts at a group head, no orphaned tool result leads the tail, the budget is respected (plus at most one group of overshoot never exceeding the last group), the 60 percent fill target holds for a sized case, the zero-budget fallback matches the 64-message bound, and raw_thinking lines inside kept groups are untouched. Commit failing.
GREEN: generalize the cut: a budget-parameterized variant of the group-advance loop (keep boundMidTurn itself intact for the no-marker path — extract or duplicate the advance loop rather than changing its behavior); add the injectable compaction budget to the projector's construction/wiring path following the file's existing option/setter conventions (zero value = fallback); the session-side population of the real budget from the modelrouting context window is 19-04's wiring, not this task's. Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run 'TestProjector_CompactionTailCut' -count=1 && go test -race ./internal/session/ ./internal/runtime/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestProjector_CompactionTailCut passes: pair-atomic cut, budget respected, fallback bound, thinking untouched.
    - boundMidTurn's no-marker behavior is unchanged (its existing tests pass unmodified).
    - The runtime package stays green (projector wiring compiles and behaves as before absent markers).
  </acceptance_criteria>
  <done>ROADMAP criterion 2's projector half is pinned: pair atomicity, thinking preservation, and boundary-survival are test-enforced properties of the reset-point class.</done>
  <reversibility rating="reversible">D-05's marked reversibility: the fill policy is internal to the cut computation; switching to fixed-N later changes only this task's budget branch.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript file → projector | on-disk lines (including marker summaries — model output over tool content) feed the model-visible window |
| marker payload → future requests | the summary becomes every later request's seed until the next compaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-05 | Tampering | summary as durable seed (prompt injection via tool output steering the summary) | medium | mitigate | The marker rides the redacted append path (this plan, Task 1); the summarizer prompt demands extractive condensation (19-04 Task 1, where the text is produced); the seed is data inside a user-role message, never executed |
| T-19-06 | Information Disclosure | secrets from earlier turns persisting in the summary | medium | mitigate | The summarizer's input is already-redacted transcript content (19-04); the append-side redactor applies to the marker line (Task 1's counting proof) |
| T-19-07 | DoS | a marker whose summary bloats every future request | low | mitigate | 19-04's hard output cap bounds the summary; this plan's budget-fill accounts the summary estimate against the fill target so an oversized summary shrinks the tail rather than the window |
| T-19-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs this phase (stdlib only) |
</threat_model>

<verification>
- `go test -race ./internal/session/ -count=1` green including TestProjector_CompactionResetPoint and TestProjector_CompactionTailCut.
- No-marker transcripts: the full pre-phase projector suite passes unmodified.
- The 16-02 tolerance pins stay green (kinds inert without markers).
</verification>

<success_criteria>
PAR-01's projection semantics are real and pinned: the marker is a durable reset-point class (summary immune to later boundaries), the tail cut is pair-atomic and budget-filled, thinking is never rewritten, and every pre-phase transcript projects byte-identically.
</success_criteria>

<output>
Create `.planning/phases/19-compaction-cache-control/19-03-SUMMARY.md` when done
</output>
