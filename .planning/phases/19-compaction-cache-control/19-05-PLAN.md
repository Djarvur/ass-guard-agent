---
phase: 19-compaction-cache-control
plan: 05
type: execute
wave: 3
depends_on: ["19-04"]
files_modified:
  - internal/modelrouting/config.go
  - internal/modelrouting/defaults/config.yaml
  - internal/modelrouting/config_test.go
  - internal/acpserve/config_surface.go
  - internal/acpserve/config_test.go
  - internal/session/compaction.go
autonomous: true
requirements: [PAR-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "compaction.threshold_pct defaults to 80 and compaction.enabled defaults to true: absent keys load the defaults, and both keys round-trip through the 16-04 layer writer (write then Load resolves exactly what was written, in either layer)"
    - "The configOptions menu advertises compaction-threshold and compaction-enabled (with their _global/ twins) with EFFECTIVE currentValues resolved through the layer precedence chain (D-03: advertise all, effective values advertised)"
    - "set_config_option for both ids persists first (atomic 0600 layer write via the 16-04 primitive) then applies live: the running session's next pre-request check uses the new values through the serialized apply seam; the compaction-threshold pending-handler no-op is gone"
    - "Out-of-range thresholds (at or below zero, above 100) are typed-rejected at the set path with the option key and violation in the error data; the session-side check clamps defensively to the 1..100 range"
    - "An inbound value equal to the option's currently-effective value is a logged idempotent no-op — no layer write (16-05's D-10 discipline extended to the compaction ids)"
    - "The effective-value round-trip invariant: a threshold written through the menu is the value the session's pre-request check compares against (the assumption-delta companion test)"
    - "context_limit stays sourced from the modelrouting capability table — no context-limit menu entry or config key exists (discretion item resolved)"
  artifacts:
    - path: "internal/modelrouting/config.go"
      provides: "Config.Compaction struct (ThresholdPct int yaml threshold_pct, Enabled bool yaml enabled) under the yaml key compaction, defaulted 80/true at the defaults application site"
      contains: "ThresholdPct"
    - path: "internal/acpserve/config_surface.go"
      provides: "compaction menu entries (compaction-threshold flipped from pending no-op to handled; compaction-enabled added) with effective-value resolution, persist-then-apply, and live apply through the runner/session seam"
      contains: "compaction"
    - path: "internal/session/compaction.go"
      provides: "SetCompactionSettings (the serialized live-apply landing the surface calls — the SetTurnModel precedent)"
      contains: "SetCompactionSettings"
  key_links:
    - from: "internal/acpserve/config_surface.go"
      to: "internal/session/compaction.go"
      via: "the surface's apply path calls the session's SetCompactionSettings through the same serialized runner seam 16-05 built for tier/model"
      pattern: "SetCompactionSettings"
    - from: "internal/modelrouting/config.go"
      to: "internal/session/compaction.go"
      via: "the session's settings holder initializes from the modelrouting-loaded effective values at construction and re-reads on live apply"
      pattern: "threshold_pct"
  prohibitions:
    - requirement_id: PAR-01
      category: config-integrity
      statement: "the menu and config surface MUST never grow a context-limit key — context_limit is sourced from the modelrouting capability table (the resolved discretion item); a config override would fork the measurement baseline"
    - requirement_id: PAR-01
      category: validation
      statement: "the set path MUST never persist an out-of-range threshold — typed rejection precedes the layer write (validation-first, the 16-05 ordering)"
---

<assumption_delta_decision>
Detector fired (kind: chosen, term: configurable — the ~80 percent threshold became a parameter). Question answered: has a configuration decision become a modeling decision?

- Noun now primary: context-compaction policy (threshold_pct, enabled) as a citizen of the modelrouting layered Config — the same identity model session_tier occupies since 16-04.
- Decision: no-change — the constant-to-parameter transition slots into the existing layered-config + configOptions identity model; no promote, no add-alongside, no new identity noun. The projector's reset-point class (19-03) stays measured in real provider tokens against the capability table's context_window; only the trigger percentage is configurable.
- Companion invariant (accepted, included): the effective-value round-trip test — a threshold written through the menu is provably the value the pre-request check compares against. It goes red if a future phase reintroduces a shadow default.
</assumption_delta_decision>

<objective>
Make the threshold configurable the way D-03 locked it: compaction.threshold_pct (80 default) and compaction.enabled join the configOptions menu under Phase 16's rule — advertise all, apply-as-landed, persist-then-apply, effective values advertised. Phase 16 advertised compaction-threshold as a pending-handler no-op; this plan flips it to handled, adds compaction-enabled under the same rule, and lands the modelrouting keys both read from and write to.

Purpose: PAR-01's "configurable" letter and ROADMAP criterion 1's "(configurable)" parenthetical; also the operator's live lever without restart (live apply on the running session's next check).
Output: modelrouting config keys + defaults, acpserve menu wiring, session live-apply setter, round-trip + apply tests.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-compaction-cache-control/19-CONTEXT.md
@.planning/phases/19-compaction-cache-control/19-RESEARCH.md
@.planning/phases/16-acp-wire-foundation/16-04-PLAN.md
@.planning/phases/16-acp-wire-foundation/16-05-PLAN.md
@internal/modelrouting/config.go
@internal/acpserve/config_surface.go
@internal/session/compaction.go
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/modelrouting/config.go` — Config.Compaction (ThresholdPct, Enabled) with yaml keys compaction.threshold_pct / compaction.enabled.
- `internal/modelrouting/defaults/config.yaml` — explicit compaction defaults (80 / true).
- `internal/acpserve/config_surface.go` — compaction menu entries with persist-then-apply + live apply.
- `internal/session/compaction.go` — SetCompactionSettings live-apply setter.
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: modelrouting compaction keys with defaults and round-trip</name>
  <files>internal/modelrouting/config.go, internal/modelrouting/defaults/config.yaml, internal/modelrouting/config_test.go, internal/session/compaction.go</files>
  <precondition>Phase 16's 16-04 landed WriteLayerOption in internal/providerfactory (the persist-then-apply layer writer) — verify with grep before starting; if the writer landed under a different name or home, follow the landed artifact (research assumption A5's fallback) and note the divergence in the SUMMARY.</precondition>
  <behavior>
    - A config without compaction keys loads with ThresholdPct 80 and Enabled true (defaults application at the same site other absent-key defaults apply — the session_tier precedent).
    - A config with compaction threshold_pct 60 and enabled false loads those values; the keys round-trip through marshal then Load.
    - WriteLayerOption(global layer, compaction.threshold_pct, 60) followed by Load resolves 60; the same for enabled in the project layer, leaving the global file untouched (byte comparison).
    - The session's settings holder initializes from the loaded effective values at construction (replacing 19-04's hardcoded defaults as the source) and SetCompactionSettings(thresholdPct, enabled) updates them for the next pre-request check.
    - The session-side threshold comparison clamps pct into 1..100 defensively (a hand-edited config file with 0 or 500 cannot disable or break the check).
  </behavior>
  <read_first>internal/modelrouting/config.go (Config struct :13; the defaults application flow — where SessionTier gets its heavy default; CapabilityProfile.ContextWindow :58 stays the limit source), internal/modelrouting/defaults/config.yaml (the tiers block :33-36 with no light binding — A4 stands; where the explicit 80/true defaults live alongside), internal/modelrouting/config_test.go (the session_tier load/round-trip cases to mirror), internal/providerfactory/config_write.go (WriteLayerOption — the 16-04 writer this task's round-trip exercises cross-package), internal/session/compaction.go (19-04's settings holder — the initialization seam this task rewires)</read_first>
  <action>
RED: extend config_test.go with the load-defaults, explicit-values, and marshal-round-trip cases; add the cross-package round-trip case (write then Load resolves; project-layer write leaves global untouched) in the writer's test file per the 16-04 precedent; add the session-side case: a session constructed over a config with threshold 60 compares against 60, and SetCompactionSettings(40, true) changes the next comparison. Commit failing.
GREEN: add the Compaction struct to Config with yaml tags under the compaction key; apply the 80/true defaults at the defaults site; carry the explicit values in defaults/config.yaml alongside the tiers block; rewire the session settings holder to initialize from the loaded config; implement SetCompactionSettings on the session (serialized the way SetTurnModel is — read how 16-05 landed it and follow) and the defensive clamp inside the threshold comparison. Commit green.
  </action>
  <verify>
    <automated>go test ./internal/modelrouting/ ./internal/providerfactory/ -run 'Compaction|ConfigWrite' -count=1 && go test -race ./internal/session/ -run 'TestCompaction' -count=1 && go vet ./internal/modelrouting/</automated>
  </verify>
  <acceptance_criteria>
    - Defaults load (80/true), explicit values load, marshal round-trip passes.
    - The layer-writer round-trip passes in both layers with layer isolation proven by byte comparison.
    - A session over a 60-percent config compares against 60; SetCompactionSettings takes effect on the next check.
    - Clamp proven: pct 0 and 500 from a hand-edited config compare as 1 and 100 respectively.
  </acceptance_criteria>
  <done>D-03's persistence half: the keys exist in the layered config with correct defaults, round-trip exactly, and feed the engine's live settings holder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Menu wiring — compaction-threshold handled, compaction-enabled added</name>
  <files>internal/acpserve/config_surface.go, internal/acpserve/config_test.go, internal/session/compaction.go</files>
  <precondition>Phase 16's 16-05 landed the ConfigSurface implementation at internal/acpserve/config_surface.go with the compaction-threshold entry advertised as a pending-handler no-op — verify with grep (the pending log names compaction-threshold); if the entry is absent or named differently, follow the landed menu ids and record the divergence in the SUMMARY.</precondition>
  <behavior>
    - The menu carries compaction-threshold and compaction-enabled with their _global/ twins; every advertised currentValue equals the surface's effective resolution for that id at response time (layer precedence: explicit project over explicit global over default).
    - set_config_option for a known compaction id: validation first (threshold out of 1..100 typed-rejected with the option key and violation detail), persist (atomic 0600 layer write), then live apply through the serialized seam to the running session's settings holder.
    - The pending-handler no-op branch for compaction-threshold is removed — both ids are handled.
    - An inbound value equal to the currently-effective value is a logged idempotent no-op with no layer write.
    - The effective-value round-trip invariant: set compaction-threshold to 60 through the handler, then assert the session's next pre-request comparison uses 60 (the assumption-delta companion).
    - Scope routing: the _global/ twins write the global layer; bare ids write the project layer (16-05's rule, unchanged).
  </behavior>
  <read_first>internal/acpserve/config_surface.go (the 16-05 ConfigSurface implementation — menu construction, effective-value resolution, scope routing, the pending-handler branches this task replaces, the Set flow), internal/acpserve/config_test.go (16-05's surface tests — the fake surface/harness to extend), internal/acp/handlers.go (handleSetConfigOption's validation-first-then-Set ordering — the handler stays untouched; the SURFACE owns handled-ness per 16-05's design), internal/session/compaction.go (SetCompactionSettings from Task 1 — the live-apply landing), internal/runtime/runtime.go (the runner seam 16-05 built for tier/model apply — follow it for the compaction apply passthrough if the runner must relay)</read_first>
  <action>
RED: extend config_test.go with the six behaviors: advertisement shape + effective values, valid set (persist-then-apply observed by a recording writer + the session holder), out-of-range typed rejection preceding any write, pending-branch removal (a set on compaction-threshold now applies — no pending log), idempotent no-op on equal value, and the round-trip invariant. Commit failing.
GREEN: add the compaction-enabled entry in the menu construction beside the landed compaction-threshold entry (same v1 ConfigOptionFrame shape, select type, options enumerated 50/60/70/80/90 or the landed entry's convention — mirror compaction-threshold); flip the threshold entry's handled-ness from pending to the real Set flow; wire the apply leg to the session's SetCompactionSettings through the same serialized runner path tier/model use (add the runner relay only if the landed seam requires it — read how ApplyTurnModel routes and mirror); extend validation with the 1..100 bounds for the threshold id. The internal/acp handler layer stays untouched — handled-ness is surface-owned. Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'Compaction' -count=1 && go test ./internal/acp/ ./internal/acpserve/ ./internal/modelrouting/ -count=1 && mise ci</automated>
  </verify>
  <acceptance_criteria>
    - All six behavior cases pass; the pending branch for compaction-threshold no longer exists in the surface (grep the pending log path).
    - The internal/acp package's tests pass unmodified (the handler layer is untouched).
    - The round-trip invariant test proves the menu-written threshold is the comparison value.
    - mise ci green (the standing phase gate).
  </acceptance_criteria>
  <done>D-03 is fully landed: both compaction keys are advertised with effective values, set through persist-then-apply, and live-applied to the running session — PAR-01's configurable letter closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor settings → config layers → session behavior | client-pushed values reach operator config files and the running engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-12 | Tampering | editor-pushed config values | medium | mitigate | Typed validation-first rejection (1..100 bounds) precedes any write; values are scalars (int/bool) with no expansion surface; the 16-05 handler's shape validation stands in front |
| T-19-13 | Tampering | value injection into arbitrary config keys | medium | mitigate | The writable key set stays whitelisted to the menu ids at the surface (16-05's T-16-12 boundary extended, not reopened); compaction keys land under that whitelist |
| T-19-14 | DoS | threshold pushed to 1 forcing compaction every turn | low | mitigate | Each compaction still costs one summarizer call on the normal pipeline (rate-visible in /cost); the operator sees it and can reset via the same menu; not a crash or loop vector |
| T-19-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs this phase (stdlib + existing yaml dep only) |
</threat_model>

<verification>
- `go test -race ./internal/acpserve/ ./internal/modelrouting/ ./internal/providerfactory/ -count=1` green.
- `go test -race ./internal/session/ -run TestCompaction -count=1` still green (engine unaffected by the settings source change).
- `mise ci` green.
- The pending-handler log for compaction-threshold is gone from the surface.
</verification>

<success_criteria>
Phase 19 closes with D-03 satisfied: the compaction keys are first-class configOptions citizens under Phase 16's rule — advertised with effective values, persisted atomically, applied live — and the assumption-delta invariant pins the menu-to-engine round trip.
</success_criteria>

<output>
Create `.planning/phases/19-compaction-cache-control/19-05-SUMMARY.md` when done
</output>
