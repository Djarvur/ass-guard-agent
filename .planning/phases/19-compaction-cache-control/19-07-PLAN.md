---
phase: 19-compaction-cache-control
plan: 07
type: execute
wave: 5
depends_on: ["19-06"]
files_modified:
  - internal/session/projector.go
  - internal/session/projector_test.go
  - internal/session/compaction_test.go
  - internal/session/session.go
autonomous: true
gap_closure: true
requirements: [PAR-01]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low # estimate-calibration has 0 samples (factor 1, not applied); raw projection modeled on Phase 19 per-plan metrics (P04 37 min / P06 23 min, 2-3 tasks)

must_haves:
  truths:
    - "Armed retry with a prior marker: when the engine armed SetRetryCompactedTurn(turnID) and a TypeCompaction line with TurnID == turnID exists, Project(turnID) resolves through projectSameTurnCompacted EVEN WHEN an earlier TypeCompaction line precedes the turn's user message — the retry seed carries the SAME-TURN marker's summary, never the earlier marker's (CR-01 residual closed; PAR-01 retry-once works on every overflow, not just a session's first)"
    - "Prior-marker overflow leg completes end-to-end: rejected fat send → bounded summarize → new marker M2 → exactly one retry that is STRICTLY smaller, seeded with the NEW summary, turn ends stopEndTurn with no provider error line and exactly 3 stream calls"
    - "Tamper safety unchanged: with the override NOT armed, a marker-bearing transcript projects byte-identically to the pre-fix projection (the not-armed reflect.DeepEqual pin passes unmodified); the carve-out still requires the non-empty, EXACT in-memory turnID match — transcript content alone never reaches it"
    - "Degraded compact keeps the fail-through: armed with NO same-turn marker (degraded compact with no marker at all, or with only a pre-user marker present) the projection is byte-identical to today's — the existing degraded fail-through subtest passes unmodified"
    - "Every other pinned 19-03/19-04/19-05/19-06 fixture passes unmodified; the only edited pin is the deliberately inverted precedence subtest in TestProjector_SameTurnCarveOut (its old expectation encoded exactly the defect this plan fixes)"
  artifacts:
    - "internal/session/projector.go — in Project(), the armed carve-out branch precedes the compactionMarkerIdx pre-user scan; SetRetryCompactedTurn/Project/retryCompactedTurn doc comments state the precedence and the not-armed/degraded fallback"
    - "internal/session/projector_test.go — TestProjector_SameTurnCarveOut carries the inverted precedence pin (armed + pre-user marker + same-turn marker → same-turn summary wins) plus a fallback pin (armed + pre-user marker + NO same-turn marker → pre-user scan governs)"
    - "internal/session/compaction_test.go — TestCompaction_OverflowRetryCarriesSummary carries the prior-marker regression subtest (planted AppendCompaction marker + AppendBoundary before the producing turn's user message)"
    - "internal/session/session.go — comment-only update on the overflow retry branch noting the precedence (no behavior change in this file)"
  key_links:
    - "session.go overflow-retry branch (SetRetryCompactedTurn arming, ~:582) → Project() armed carve-out branch (now evaluated FIRST) → projectSameTurnCompacted — the retry always projects post-M2 even when M1 precedes the turn's user message"
---

<objective>
Close the CR-01 residual (Phase 19 VERIFICATION 2026-09-07, gap 1 / truth 5; 19-REVIEW CR-01 Critical): the
G-19-1 same-turn carve-out is currently bypassed once ANY earlier compaction marker precedes the producing
turn's user message — Project() consults the pre-user scan (compactionMarkerIdx) before the armed carve-out,
and foldExchanges is marker-blind, so the retried request is byte-identical to the rejected one, overflows
again, and the turn fails through appendError after burning the forced compact's summarizer call. Recovery
works exactly once per session.

Purpose: PAR-01's letter is retry-ONCE recovery on provider overflow — as shipped it is retry-once PER
SESSION. The main-line threshold path is unaffected (it fires before overflow), so this residual is the
backstop degrading; reachable via compaction-disabled-with-prior-marker or WR-06's stale-limit door.
Output: the armed override takes precedence over the pre-user scan (retry always projects post-marker,
carrying the NEW marker's summary), pinned by a prior-marker regression leg the current battery cannot
detect (zero AppendCompaction calls in compaction_test.go today).

Scope guard (do NOT expand): no new features, no refactors beyond the precedence flip. G-19-2 (operator
live-threshold retest on the fresh binary) is a human verification leg — NOT this plan. CR-02 (in-band SSE
error swallow) and WR-01..06 stay deferred as review debt.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-compaction-cache-control/19-VERIFICATION.md
@.planning/phases/19-compaction-cache-control/19-REVIEW.md

# The fix sketch this plan implements (CR-01 Fix paragraph) and the ruling it serves
# (G-19-1 operator ruling (b), implemented by 19-06, residual closed here).
@.planning/phases/19-compaction-cache-control/19-CONTEXT.md

# Prior plan whose artifacts this plan modifies (carve-out seam, battery shape).
@.planning/phases/19-compaction-cache-control/19-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — prior-marker regression leg + inverted precedence pin</name>
  <files>internal/session/projector_test.go, internal/session/compaction_test.go</files>
  <precondition>HEAD is the post-19-06 state and the battery is green before any edit: go test ./internal/session/ -run 'TestProjector_SameTurnCarveOut|TestCompaction_OverflowRetryCarriesSummary' -count=1 exits 0 (the 19-VERIFICATION baseline).</precondition>
  <read_first>
  - internal/session/projector.go — Project() :147-181 (branch order :153 pre-user scan vs :161-165 carve-out), projectSameTurnCompacted :236-268, compactionMarkerIdx :377-396, sameTurnMarkerIdx :398-417, boundCompactionTail :286-302 — the exact defect surface and the functions the new pins exercise
  - internal/session/projector_test.go :2150-2435 — TestProjector_SameTurnCarveOut battery, the constants (sumSameTurnNew, sumSameTurnOld, sumPreUserStc, intentStcParent :2157-2160), stcAppendExchanges :2165, and the subtest at :2279-2315 to invert (its transcript shape is reused verbatim)
  - internal/session/compaction_test.go :1594-1853 — sizeRejectProvider (content-sensitive rejection), g191TurnID/g191Intent consts, newSizeRejectSession, g191ProviderErrorLine, and the three existing TestCompaction_OverflowRetryCarriesSummary subtests (the shape the new leg mirrors); also markersOf :268, compFatSpanGroups :1251
  - internal/session/manager.go :232 (AppendBoundary signature) and :390 (AppendCompaction signature)
  - internal/session/session.go :549-590 — the overflow-retry branch that arms the override (what the session-level leg drives through runTurn)
  - .planning/phases/19-compaction-cache-control/19-REVIEW.md — CR-01 (Fix sketch + test-shape requirement)
  - .planning/phases/19-compaction-cache-control/19-VERIFICATION.md — gap 1 missing list (precedence fix + regression leg definitions this task tests)
  </read_first>
  <behavior>
  - Inverted precedence pin (FAILS pre-fix): transcript = turn_0 user message "early work" + AppendCompaction(turn_0, …, sumPreUserStc) + turnT user message (intentStcParent) + stcAppendExchanges(turnT, 1) + AppendCompaction(turnT, …, sumSameTurnNew); SetRetryCompactedTurn(turnT); Project(turnT) → msgs[0].Content contains sumSameTurnNew, does NOT contain sumPreUserStc, contains intentStcParent
  - Fallback pin (passes pre- AND post-fix, guards the fallback): same transcript MINUS the same-turn AppendCompaction; armed → Project(turnT) → seed contains sumPreUserStc and intentStcParent (the degraded-compact-with-prior-marker shape keeps today's behavior: armed but no same-turn marker → pre-user scan governs)
  - Session-level prior-marker leg (FAILS pre-fix): pre-built transcript carries, BEFORE the producing turn's user message, a prior turn's user message + AppendCompaction marker (TurnID "t-prior", summary "OLD-PRIOR-SUMMARY") + AppendBoundary line; then the g191 fat producing turn (40 × 600-char exchanges). Settings SetCompactionSettings(false, 80, 1000), rejection bound 4000, script = summarizer "SHRUNK-PRIOR-SUMMARY" then retry "all good now" (both stopEndTurn). runTurn → stop == stopEndTurn, err == nil; len(p.sizes) == 3 (rejected, summarize, retry); p.sizes[0] > p.maxBytes; p.sizes[1] <= p.maxBytes; markersOf == 2 (the planted M1 + the forced M2); p.sizes[2] < p.sizes[0] (strictly smaller); p.firsts[2] contains "SHRUNK-PRIOR-SUMMARY" and g191Intent and does NOT contain "OLD-PRIOR-SUMMARY"; g191ProviderErrorLine == false
  - Pre-fix RED expectation for the session leg: the turn FAILS (err != nil, stop == "") — the retry re-projects through the prior marker byte-identically, overflows again, and lands the appendError provider line
  </behavior>
  <action>
  Write the failing tests FIRST; do not touch projector.go or session.go in this task.

  1) projector_test.go — inside TestProjector_SameTurnCarveOut, REPLACE the subtest at :2279-2315 (the one whose
  name asserts the pre-user marker cannot be displaced) with the inverted pin: keep its transcript appends
  VERBATIM (turn_0 "early work" user message, AppendCompaction turn_0 with sumPreUserStc 100/10/10, turnT
  user message intentStcParent, stcAppendExchanges turnT 1, AppendCompaction turnT with sumSameTurnNew
  9000/900/900) and its SetRetryCompactedTurn(turnT) arming; rename it to state the armed override wins over
  the pre-user scan (CR-01); flip the assertions — seed contains sumSameTurnNew, seed does NOT contain
  sumPreUserStc, seed contains intentStcParent. Then ADD the fallback pin as a NEW sibling subtest: the same
  transcript minus the same-turn AppendCompaction, armed → Project(turnT) → seed contains sumPreUserStc and
  intentStcParent (armed but no same-turn marker changes nothing — the pre-user scan remains the fallback).
  Do not modify any other subtest of the battery.

  2) compaction_test.go — add a prior-marker subtest to TestCompaction_OverflowRetryCarriesSummary following
  the existing regression subtest's shape. Build the session with a NEW helper (recommended:
  newPriorMarkerSizeRejectSession, a near-copy of newSizeRejectSession per the repo's Pitfall-5 near-copy
  discipline — do NOT change newSizeRejectSession's signature, the three existing subtests keep their fixture
  code untouched) that, BEFORE the m.AppendUserMessage(g191TurnID, …) call, appends for the prior turn:
  m.AppendUserMessage("t-prior", []ContentBlock{{Type: blockText, Text: "earlier turn work"}}), then
  m.AppendCompaction("t-prior", "line:0", "line:1", "OLD-PRIOR-SUMMARY", 100, 10, 10), then a boundary line
  via m.AppendBoundary (manager.go:232 signature; cause naming your choice, commandRef "", turnID
  "t-prior"). The boundary is LOAD-BEARING — document it in the helper's comment: with limit 1000 injected,
  a boundary-less prior marker routes the first projection through projectCompacted's budget path
  (boundCompactionTail ≈ 600 tokens ≈ 2.4K chars), which fits UNDER the 4000-byte bound — no overflow, the
  leg would pass vacuously pre- AND post-fix. The boundary makes boundaryAfterMarker true
  (projector.go:274-284), so the pre-fix projection's tail is boundMidTurn(accumulateMidTurn) — count-capped
  at 64, NOT byte-budgeted — and the 40 × 600-char fat span is deterministically over the bound. This is
  also the canonical CR-01 shape: a real earlier turn compacted, later turns appended boundaries, then the
  producing turn overflows.
  Script and settings exactly as the existing regression subtest: []compScript{{text:
  "SHRUNK-PRIOR-SUMMARY", finish: stopEndTurn}, {text: "all good now", finish: stopEndTurn}}, bound 4000,
  s.SetCompactionSettings(false, 80, 1000), s.runTurn(context.Background(), g191TurnID). Assert everything
  in the behavior block (including markersOf == 2 — distinct from the existing leg's 1 — and the
  OLD-PRIOR-SUMMARY exclusion on p.firsts[2]).

  3) Run the battery and CONFIRM RED before committing: the inverted precedence pin and the prior-marker
  session leg FAIL on current code (the session leg fails at runTurn: turn fails through appendError after
  the byte-identical resend); the fallback pin, the not-armed byte-identity subtest, and every other
  existing subtest still PASS. Commit the failing tests:
  git commit -m "test(19-07): add failing prior-marker regression legs (CR-01 residual)"
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/session/ -run 'TestProjector_SameTurnCarveOut|TestCompaction_OverflowRetryCarriesSummary' -count=1 -v; echo "EXIT=$?" — RED expected: EXIT non-zero (go test's own exit code, not a pipe's) with the inverted precedence subtest and the prior-marker subtest FAILING; all other subtests PASS</automated>
  </verify>
  <done>Failing tests committed; RED demonstrated on the two new legs; zero edits to any other pinned subtest body.</done>
  <acceptance_criteria>
  - grep -c 'OLD-PRIOR-SUMMARY' internal/session/compaction_test.go returns >= 2 (fixture plant + retry-seed exclusion assertion)
  - grep -c 'SHRUNK-PRIOR-SUMMARY' internal/session/compaction_test.go returns >= 2 (script + seed assertion)
  - The new projector pin's name contains the phrase "armed override wins" (grep -c 'armed override wins' internal/session/projector_test.go == 1) and its body asserts sumSameTurnNew present / sumPreUserStc absent
  - The prior-marker subtest plants exactly one AppendCompaction with TurnID "t-prior" and one AppendBoundary before the g191TurnID user message (grep -c 't-prior' internal/session/compaction_test.go >= 3)
  - go test ./internal/session/ -run 'TestProjector_SameTurnCarveOut|TestCompaction_OverflowRetryCarriesSummary' -count=1 exits NON-ZERO with exactly the two new legs failing (RED proof in the task log)
  - git diff HEAD~1 --stat shows only internal/session/projector_test.go and internal/session/compaction_test.go touched
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — armed carve-out takes precedence over the pre-user marker scan</name>
  <files>internal/session/projector.go, internal/session/session.go</files>
  <read_first>
  - internal/session/projector.go :84-181 — the retryCompactedTurn field doc, SetRetryCompactedTurn doc, Project doc, and the two branches being reordered (bodies must move VERBATIM)
  - internal/session/session.go :549-590 — the overflow-retry branch comment block whose "transcript-gated" wording gets the precedence note
  - .planning/phases/19-compaction-cache-control/19-VERIFICATION.md gap 1 missing[0] — the two sanctioned fix shapes (this plan implements the precedence variant; sameTurnMarkerIdx already returns the MOST RECENT TurnID-matched marker regardless of position, so the reorder IS the "most recent marker for the matching turnID" variant)
  </read_first>
  <behavior>
  - Project(turnID) evaluation order becomes: (1) armed carve-out — non-empty retryCompactedTurn == turnID AND sameTurnMarkerIdx >= 0 → projectSameTurnCompacted; (2) pre-user scan — compactionMarkerIdx >= 0 → projectCompacted; (3) pre-phase path. With a prior marker M1 AND an armed same-turn marker M2, the projection is now post-M2 (the NEW summary seed) — the RED legs of Task 1 turn green
  - Not armed: identical behavior to today (branch 1 skipped; the not-armed byte-identity DeepEqual pin passes unmodified)
  - Armed but no same-turn marker (degraded compact, with or without a prior pre-user marker): branch 1 finds nothing, branch 2/3 behave exactly as today (the Task 1 fallback pin and the existing degraded fail-through subtest pass unmodified)
  </behavior>
  <action>
  In Project() (projector.go :147-181): move the armed carve-out branch (the current :161-165 block — the
  retryCompactedTurn guard plus the sameTurnMarkerIdx call and projectSameTurnCompacted return) to BEFORE the
  compactionMarkerIdx branch (the current :153-155 block). Move both branch BODIES verbatim — this is a pure
  reorder; do not rewrite either body, do not touch projectSameTurnCompacted, compactionMarkerIdx,
  sameTurnMarkerIdx, projectCompacted, or foldExchanges.

  Update the affected doc comments to state the new rule (comments only, no code):
  a) Project doc (:141-146) — the 19-06 sentence saying the carve-out runs when "the pre-user scan found
  nothing" becomes: the armed override takes PRECEDENCE over the pre-user scan, so the retry projects
  post-marker even when an earlier marker precedes the turn's user message (the CR-01 residual);
  b) the inline comment above the branch (:157-160) — same precedence statement, plus the fallback: armed
  with no same-turn marker, the pinned pre-user scan governs unchanged (degraded-compact fail-through);
  c) SetRetryCompactedTurn doc (:103-111) — drop the "with no pre-user marker present" clause; state that
  the armed override wins over any pre-user marker for the matching turnID;
  d) session.go retry-branch comment (:560-572) — append one sentence: the armed override now defeats an
  earlier pre-user marker, so the retry always projects post-marker (retry-once recovery on EVERY overflow,
  not just the session's first — CR-01). No behavior change in session.go.

  Run the Task 1 batteries to GREEN, then commit:
  git commit -m "fix(19-07): armed carve-out takes precedence over pre-user marker scan"
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/session/ -run 'TestProjector_SameTurnCarveOut|TestCompaction_OverflowRetryCarriesSummary' -count=1 -race && go vet ./internal/session/</automated>
  </verify>
  <done>Both batteries green under -race; the reorder is the only behavior change; all four doc sites updated; fix committed.</done>
  <acceptance_criteria>
  - In Project(), the guard line matching 'retryCompactedTurn != "" && p.retryCompactedTurn == turnID' appears at a LOWER line number than the 'compactionMarkerIdx(lines, turnID)' call (assert with grep -n ordering on internal/session/projector.go)
  - grep -c 'takes precedence\|takes PRECEDENCE' internal/session/projector.go >= 2 (Project doc + inline comment)
  - The SetRetryCompactedTurn doc block states the precedence and the degraded-case fallback (positive check: the doc block contains "precedence" and the word "fallback"); the position clause named in the action is gone from the doc (inspect the setter's doc block)
  - go test ./internal/session/ -run 'TestProjector_SameTurnCarveOut|TestCompaction_OverflowRetryCarriesSummary' -count=1 -race exits 0
  - go vet ./internal/session/ exits 0
  - git diff HEAD~1 --stat shows only internal/session/projector.go and internal/session/session.go touched, with session.go changes comment-only
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full battery gate + fixture-integrity audit</name>
  <files>internal/session/projector_test.go, internal/session/compaction_test.go</files>
  <read_first>
  - .planning/phases/19-compaction-cache-control/19-VERIFICATION.md — Behavioral Spot-Checks table (the exact command set this gate replays) and Prohibition Checks table (the two diff audits below)
  </read_first>
  <action>
  Replay the verifier's full gate on the fixed tree — no code changes in this task:

  1) Full session battery under -race (the 13 named test functions from the 19-VERIFICATION Behavioral
  Spot-Checks row): TestProjector_SameTurnCarveOut, TestCompaction_OverflowRetryCarriesSummary,
  TestCompaction_BoundedSpanAndReFireGuard, TestReconcileSameTurnMarker, TestCompaction_OverflowRetryOnce,
  TestProjector_CompactionResetPoint, TestProjector_CompactionTailCut, TestCompaction_EndToEnd,
  TestCompactNow, TestCompaction_Threshold, TestCompaction_Chaining, TestCompaction_SummarizerFailure,
  TestCompaction_LoopHead — all must pass. The TestCompaction_OverflowRetryOnce legs are the reorder's
  canary: their fixtures mint fresh single-turn sessions (no pre-user marker ever exists when the retry
  projects), so they must pass UNMODIFIED — if one fails, the reorder leaked beyond the precedence change.

  2) Regression packages: go test ./internal/shaper/ ./internal/profile/ ./internal/paritycli/
  ./internal/provider/ ./internal/modelrouting/ ./internal/providerfactory/ -count=1 — all ok. Plus the
  acpserve compaction surface: go test ./internal/acpserve/ -run 'TestCompaction' -count=1.

  3) Fixture-integrity audits (the phase's pinned prohibitions):
  - git diff 06d92d1 -- internal/session/projector_test.go internal/session/compaction_test.go —
    deletions must be confined to the inverted :2279 subtest's replaced lines; every other hunk is
    insertions (the near-copy helper + the two new subtests). The not-armed byte-identity subtest, the
    subagent-shadow, twice-armed, subagent-safe, determinism, and the three existing
    TestCompaction_OverflowRetryCarriesSummary subtests must show ZERO deletions.
  - git diff --name-only 73440bd -- internal/parity/cacheprobe.go — EMPTY (probe baseline untouched).

  4) go vet ./internal/session/ and CGO_ENABLED=0 go build ./... both clean.

  If everything is green, write the summary (see output). If the canary or any pinned fixture fails, STOP
  and report — do not adapt fixtures to the failure.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test -race ./internal/session/ -run 'TestProjector_SameTurnCarveOut|TestCompaction_OverflowRetryCarriesSummary|TestCompaction_BoundedSpanAndReFireGuard|TestReconcileSameTurnMarker|TestCompaction_OverflowRetryOnce|TestProjector_CompactionResetPoint|TestProjector_CompactionTailCut|TestCompaction_EndToEnd|TestCompactNow|TestCompaction_Threshold|TestCompaction_Chaining|TestCompaction_SummarizerFailure|TestCompaction_LoopHead' -count=1 && go test ./internal/shaper/ ./internal/profile/ ./internal/paritycli/ ./internal/provider/ ./internal/modelrouting/ ./internal/providerfactory/ -count=1 && go test ./internal/acpserve/ -run 'TestCompaction' -count=1 && go vet ./internal/session/ && CGO_ENABLED=0 go build ./... && echo GATE-OK</automated>
  </verify>
  <done>Full battery + regression packages + acpserve surface green under the verifier's own command set; diff audits show deletions confined to the inverted subtest; probe baseline empty; vet + static build clean.</done>
  <acceptance_criteria>
  - The chained gate command prints GATE-OK (exit 0)
  - git diff 06d92d1 -- internal/session/projector_test.go shows deletions ONLY within the replaced precedence subtest hunk (inspect the diff hunks; all other hunks are pure insertions)
  - git diff --name-only 73440bd -- internal/parity/cacheprobe.go outputs nothing
  - .planning/phases/19-compaction-cache-control/19-07-SUMMARY.md exists and records the RED→GREEN evidence (failing-leg output pre-fix, green battery post-fix) plus the two audit results
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript (disk) → Projector | untrusted-at-rest content: kill-9 replay, hand-crafted transcripts (the 19-06 tamper-safety gate lives exactly here) |

No new trust boundaries; no package installs; no network, config, or wire-surface changes — this plan only
reorders two branches inside an in-process pure function and adds tests.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-07-01 | Tampering | Projector.Project armed carve-out branch | high | mitigate | Precedence flip must NOT weaken the engine gate: the carve-out still requires the non-empty, EXACT in-memory retryCompactedTurn == turnID match — transcript content alone never reaches it. Pinned by the UNMODIFIED not-armed byte-identity DeepEqual subtest and the subagent-safe (armed-for-other-turn) pins; Task 3 audits zero deletions outside the inverted subtest. |
| T-19-07-02 | Tampering | Test fixtures (projector_test.go / compaction_test.go) | medium | mitigate | Fixture-integrity audit (Task 3): git diff 06d92d1 over the two test files must show deletions confined to the deliberately inverted subtest — no quiet weakening of the 19-03/19-04/19-06 pins. |
| T-19-07-SC | Tampering | package installs | low | accept | No package-manager installs in this plan (pure internal Go change) — the legitimacy gate does not apply. |
</threat_model>

<verification>
- The Task 1 RED run (pre-fix): inverted precedence pin + prior-marker session leg FAIL; every other subtest PASSes — proving the regression detector actually detects the defect.
- The Task 2 GREEN run (post-fix): both batteries PASS under -race; the reorder is provably the only behavior change (git diff scope).
- The Task 3 gate replays the 19-VERIFICATION command set verbatim: 13-fn session battery under -race, six regression packages, acpserve compaction surface, both pinned diff audits, go vet, CGO_ENABLED=0 static build.
- Gap-closure mapping: 19-VERIFICATION gap 1 missing[0] (precedence fix, TurnID key kept, not-armed byte-identity pin kept) → Task 2; missing[1] (prior-marker regression leg per 19-REVIEW CR-01 fix sketch) → Task 1. The G-19-2 operator live retest remains in the verification/UAT domain — untouched by this plan.
</verification>

<success_criteria>
- On a session whose transcript already carries a compaction marker BEFORE the producing turn's user message, a provider overflow still recues the producing turn: forced compact → the ONE retry projects post-new-marker (NEW summary seed, strictly smaller request) → turn completes end-to-end (pinned by the prior-marker leg, which fails on the pre-fix tree).
- PAR-01's retry-once recovery now holds on every overflow, not just a session's first — the truth-5 PARTIAL verdict's reason (projector.go:153 branch order + foldExchanges marker-blindness making the retry byte-identical) is eliminated at the projection layer.
- Tamper safety, degraded-compact fail-through, retry-budget (exactly 3 stream calls), and every other pinned fixture survive unchanged.
- Not in scope (unchanged): G-19-2 live retest (operator leg), CR-02 SSE error swallow, WR-01..06 (review debt).
</success_criteria>

<output>
Create `.planning/phases/19-compaction-cache-control/19-07-SUMMARY.md` when done
</output>
