---
phase: 19-compaction-cache-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/profile/types.go
  - internal/profile/loader.go
  - internal/profile/extract.go
  - profiles/zcode/profile.yaml
  - internal/shaper/shaper.go
  - internal/shaper/shaper_test.go
  - internal/shaper/midturn_test.go
  - internal/shaper/fidelity_test.go
  - internal/paritycli/parity.go
  - internal/paritycli/parity_test.go
autonomous: true
requirements: [PAR-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every outgoing request marshals cache_control exactly {\"type\":\"ephemeral\"} on every system block whose profile flag is set — visible in the marshaled request body and therefore the request log (PAR-02, D-12; docs/compaction-decision.md disposition 1)"
    - "A profile without the flag produces request bodies byte-identical to pre-phase output — zero-value marshaling keeps old profiles unchanged (backward-compat edge: empty/absent input)"
    - "With exactly 4 flagged system blocks all 4 carry the breakpoint; with 5 or more exactly the LAST 4 do, original block order preserved — the Anthropic 4-breakpoint API cap degrade (edge: adjacency + ordering)"
    - "A profile with zero system blocks emits zero breakpoints and no error (edge: empty input)"
    - "The parity cache probe derives its composition flags from the profile's captured value and reports placement green against the committed pin fixture with AssertPlacementAgainstPin and PinClasses untouched — WINDOWS ledger item 5 closes"
    - "Future rollout extractions set the profile-level cache_control declaration when the scanned corpus shows system-block placements, so re-captures do not lose emission"
  artifacts:
    - path: "internal/profile/types.go"
      provides: "TextBlock.CacheControl bool (json/yaml cache_control, omitempty) — the captured value per disposition 1's TIER-1 route"
      contains: "CacheControl"
    - path: "internal/profile/loader.go"
      provides: "profile-level yaml declaration (system_cache_control) applied to every loaded system block at Load"
      contains: "system_cache_control"
    - path: "internal/shaper/shaper.go"
      provides: "the gated emission: TextBlock.CacheControl maps onto anthropic.TextBlockParam.CacheControl via NewCacheControlEphemeralParam, with keep-last-4 over-cap degrade at the same loop"
      contains: "NewCacheControlEphemeralParam"
    - path: "internal/paritycli/parity.go"
      provides: "composeCacheProbeInput deriving ProbeSystemBlock.CacheControl from the profile field (the seam's own routing comment, now executed)"
      contains: "composeCacheProbeInput"
  key_links:
    - from: "profiles/zcode/profile.yaml"
      to: "internal/shaper/shaper.go"
      via: "system_cache_control: true in profile.yaml → loader applies the flag to every system TextBlock → Shape emits anthropic.NewCacheControlEphemeralParam() per flagged block"
      pattern: "system_cache_control"
    - from: "internal/shaper/shaper.go"
      to: "internal/paritycli/parity.go"
      via: "the probe composition mirrors the same profile field the shaper consumes — one source of truth for emission and verification"
      pattern: "ProbeSystemBlock"
  prohibitions:
    - requirement_id: PAR-02
      category: parity
      statement: "cache_control MUST never be emitted on tool declarations or message content blocks — system blocks only (D-12); the probe reports any tools/messages placement as a failure"
    - requirement_id: PAR-02
      category: parity-integrity
      statement: "the probe's expected-placement baseline (internal/parity/cacheprobe.go AssertPlacementAgainstPin / PinClasses / placementClasses) MUST never be edited to manufacture the green — only the composition side changes; the baseline stays byte-stable (D-12)"
    - requirement_id: PAR-02
      category: wire-fidelity
      statement: "the emitted value MUST never carry a ttl or any field beyond type ephemeral — the corpus is 910/910 uniform {\"type\":\"ephemeral\"} (docs/compaction-decision.md section 2)"
---

<objective>
Close the corpus-proven parity lever end-to-end: cache_control {"type":"ephemeral"} ships on every system block of every outgoing request, the standing parity cache-probe FAIL flips green, and WINDOWS ledger item 5 closes. This is the phase's tracer — the thinnest slice that runs from a profile artifact through the loader, the Shaper, the marshaled wire body, to the probe verdict, closing ROADMAP criterion 4 completely.

Purpose: docs/compaction-decision.md disposition 1 routed exactly this (profile TextBlock gains the captured value, shaper maps it onto the wire); the seam's own comment in parity.go names this phase as the fix. Zero new dependencies — anthropic-sdk-go v1.63.0 is pinned and its NewCacheControlEphemeralParam marshals to exactly the corpus form.
Output: profile field + yaml declaration + shaper emission + probe derivation + regenerated goldens with the new pin; WINDOWS #5 fixed.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-compaction-cache-control/19-CONTEXT.md
@.planning/phases/19-compaction-cache-control/19-RESEARCH.md
@.planning/phases/19-compaction-cache-control/19-PATTERNS.md
@docs/compaction-decision.md
@internal/profile/types.go
@internal/profile/loader.go
@internal/shaper/shaper.go
@internal/paritycli/parity.go
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/profile/types.go` — TextBlock.CacheControl bool field.
- `internal/profile/loader.go` — system_cache_control profile-level declaration applied at block load.
- `internal/profile/extract.go` — extractor sets the profile-level cache_control declaration from scanned rollouts.
- `internal/shaper/shaper.go` — gated emission + keep-last-4 over-cap degrade at the systemBlocks loop.
- `internal/paritycli/parity.go` — composeCacheProbeInput flag derivation from the profile field.
- `profiles/zcode/profile.yaml` — system_cache_control: true.
</artifacts_this_phase_produce>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — profile flag to wire emission, one path end-to-end</name>
  <files>internal/profile/types.go, internal/profile/loader.go, profiles/zcode/profile.yaml, internal/shaper/shaper.go, internal/shaper/shaper_test.go</files>
  <behavior>
    - TextBlock gains CacheControl bool with json and yaml tags cache_control (omitempty), matching the struct's existing tag style; the zero value means no emission.
    - The Profile gains a system_cache_control yaml key (bool); profiles/zcode/profile.yaml declares it true; Load applies it to every system block after readSystemBlocks returns them, so all blocks carry the flag (no per-block sidecar file — the corpus value is 910/910 uniform, a presence flag carries full fidelity).
    - Shaper.Shape's systemBlocks loop: when b.CacheControl is set, the constructed anthropic.TextBlockParam gets CacheControl from anthropic.NewCacheControlEphemeralParam(); blocks without the flag produce a TextBlockParam with the zero CacheControl value.
    - Marshaling a shaped request with flagged blocks: every system array entry carries cache_control marshaling to exactly {"type":"ephemeral"} — no ttl key (the SDK field is omitzero on TTL).
    - The same request with the flag absent from the profile marshals byte-identically to pre-phase output.
    - A profile with zero system blocks shapes without error and emits no breakpoints.
  </behavior>
  <read_first>internal/profile/types.go (TextBlock :44-48 and the tag style of neighboring structs), internal/profile/loader.go (Load flow + readSystemBlocks :83-126 — blocks load as TextBlock{Type: "text", Text: string(raw)} with no per-block metadata channel), profiles/zcode/profile.yaml (flat key style), internal/shaper/shaper.go (Shape's systemBlocks loop :99-102; ComposeRuntimeWorkDir :82-90 for the profile-copy discipline), .planning/phases/19-compaction-cache-control/19-RESEARCH.md (Code Examples: the SDK-verified landing form + Pitfall 2), .planning/phases/19-compaction-cache-control/19-PATTERNS.md (shaper + profile sections)</read_first>
  <action>
RED: write TestShape_CacheControlEmission in shaper_test.go with the behavior cases above — build profiles via the real loader path (a temp profile dir with one system block file + the yaml key, and one without), shape, marshal, then json-decode the system array and assert per-block cache_control equals a map with exactly the type key "ephemeral"; assert the flag-off body equals a pre-phase captured baseline (golden or explicit byte compare). Commit failing.
GREEN: add the TextBlock field and the Profile yaml key with defaults absent (no default — absence means off); apply the flag in Load after readSystemBlocks; set system_cache_control: true in profiles/zcode/profile.yaml; change the shaper loop to the gated form from the RESEARCH Code Examples (construct TextBlockParam{Text: b.Text}, then set CacheControl when the flag is set). No other shaper changes in this task — the over-cap degrade is Task 3. Commit green.
  </action>
  <verify>
    <automated>go test ./internal/shaper/ ./internal/profile/ -run 'CacheControl' -count=1 && go test ./internal/shaper/ ./internal/profile/ -count=1 && go vet ./internal/shaper/ ./internal/profile/</automated>
  </verify>
  <acceptance_criteria>
    - TestShape_CacheControlEmission passes: every flagged system block in the marshaled body carries exactly {"type":"ephemeral"}; unflagged profiles stay byte-identical to pre-phase output; zero-block profiles shape without error.
    - internal/profile/types.go TextBlock contains the CacheControl field with yaml tag cache_control.
    - profiles/zcode/profile.yaml contains system_cache_control: true.
    - internal/shaper/shaper.go contains NewCacheControlEphemeralParam.
    - Both packages' full existing suites stay green.
  </acceptance_criteria>
  <done>The tracer slice is live: one profile declaration flows through the loader into every system block and out onto the wire in the corpus-proven form, with flag-off output provably unchanged.</done>
  <reversibility rating="reversible">Flag-gated emission: unsetting the profile key restores pre-phase bytes exactly; no on-disk or protocol commitment beyond the additive wire field the disposition routed.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Probe flip — composition derives from the profile, WINDOWS 5 closes</name>
  <files>internal/paritycli/parity.go, internal/paritycli/parity_test.go</files>
  <behavior>
    - composeCacheProbeInput sets each ProbeSystemBlock's CacheControl flag from the corresponding profile system block's CacheControl field, so a flagged profile's probe composition carries placements on every system block.
    - The probe against the committed pin fixture (internal/profile/testdata/context-behavior/cache-control.jsonl) reports placement-vs-pin green for the zcode profile (which now carries the flag via Task 1).
    - An unflagged profile's probe composition carries no placements and the placement assertion reports the gap exactly as today (the composition side is the only thing that changed).
    - AssertPlacementAgainstPin, PinClasses, and placementClasses in internal/parity/cacheprobe.go are untouched — the baseline is unchanged per D-12.
  </behavior>
  <read_first>internal/paritycli/parity.go (composeCacheProbeInput :57-87 and the routing comment at :53-56 that names this phase), internal/paritycli/parity_test.go (existing probe test harness + fixtures), internal/parity/cacheprobe.go (:261-281 placementClasses — compares placement CLASSES, not per-block counts), cmd/ass-guard/parity.go (:60 — the pin fixture path internal/profile/testdata/context-behavior/cache-control.jsonl)</read_first>
  <action>
RED: extend parity_test.go with TestCacheProbe_PlacementFlip — load the zcode profile through the real loader, run the cache probe composition, and assert the placement verdict is green against the committed pin fixture; plus the unflagged-profile counter-case asserting no placements in the composition. Commit failing (the flag is hard-coded off today, so the flip case fails).
GREEN: change composeCacheProbeInput to mirror the profile field into each ProbeSystemBlock's CacheControl flag; replace the seam comment's future tense with the landed truth. Run the parity cache-probe path once against the pin fixture (the test does this). Then close the ledger entry: run `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 5 "19-01: cache_control emission landed — composition derives from the profile, placement-vs-pin green on the committed pin fixture"` and confirm the ledger shows id 5 fixed. Commit green.
  </action>
  <verify>
    <automated>go test ./internal/paritycli/ -run 'TestCacheProbe' -count=1 && go test ./internal/paritycli/ ./internal/parity/ -count=1 && git diff --stat HEAD -- internal/parity/cacheprobe.go | grep -c cacheprobe | grep -qx 0 || true</automated>
  </verify>
  <acceptance_criteria>
    - TestCacheProbe_PlacementFlip passes: flagged profile → placement green on the pin fixture; unflagged profile → no placements in the composition.
    - internal/paritycli/parity.go composeCacheProbeInput references the profile's CacheControl field (no literal boolean for the system-block flags remains).
    - internal/parity/cacheprobe.go is byte-identical to pre-task (baseline untouched).
    - .planning/WINDOWS.md item 5 status is fixed.
  </acceptance_criteria>
  <done>ROADMAP criterion 4's probe half is proven: the standing cache-discipline probe flips green because the emission is real, not because the baseline moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Over-cap degrade, golden regeneration, extractor capture</name>
  <files>internal/shaper/shaper.go, internal/shaper/shaper_test.go, internal/shaper/midturn_test.go, internal/shaper/fidelity_test.go, internal/profile/extract.go</files>
  <behavior>
    - Composing 5 or more flagged system blocks emits breakpoints on exactly the LAST 4 blocks, original order preserved; exactly 4 blocks → all 4 carry it; 3 or fewer → all carry it (corpus shape).
    - The degrade is documented at the shaper site: the Anthropic API caps cache_control at 4 breakpoints per request (a 5th returns 400); keeping the last breakpoints preserves the deepest cache prefixes; the probe compares placement classes so it stays green under either policy.
    - TestMidTurnCapture_GoldenShape's regenerated golden shows cache_control on every system block, with a NEW assertion pinning that every system block in the golden carries {"type":"ephemeral"}.
    - The fidelity test (decodes only text + type) stays green without weakening its decode struct.
    - ExtractFromRollout's result sets the profile-level system_cache_control declaration true when the scanned rollout shows cache_control on system blocks (reuse the corpus-scan detection already in internal/profile/corpus_scan.go), false when absent.
  </behavior>
  <read_first>internal/shaper/shaper.go (the systemBlocks loop after Task 1), internal/shaper/midturn_test.go (TestMidTurnCapture_GoldenShape — the golden regeneration discipline), internal/shaper/fidelity_test.go (:57-78 — the text+type decode struct that keeps this test safe), internal/profile/extract.go (ExtractFromRollout :104, buildResult :244 — where the profile is assembled), internal/profile/corpus_scan.go (the cache_control census detection to reuse), .planning/phases/19-compaction-cache-control/19-RESEARCH.md (Pitfalls 2 and 6)</read_first>
  <action>
RED: add TestShape_CacheControlCapDegrade to shaper_test.go (compose 3, 4, 5, and 6 flagged blocks; count placements and their positions in the marshaled system array — expect all, all, last-4, last-4 respectively, order preserved). Add the golden-system-block pin to midturn_test.go asserting every system block in the (about-to-change) golden carries the ephemeral form. Add the extractor case: a minimal rollout fixture with cache_control on system blocks extracts with the declaration set. Commit failing.
GREEN: implement the keep-last-4 degrade inside the same shaper loop (count placements while appending; stop setting the field once 4 are placed — the LAST blocks win, so the counter must track from the end or the loop must know the total), with the cap rationale in the site comment. Regenerate the goldens deliberately (they are supposed to change once, visibly). Wire the extractor: when the scan of the chosen rollout records system-block cache_control occurrences, the emitted profile carries system_cache_control: true. Commit green.
  </action>
  <verify>
    <automated>go test ./internal/shaper/ -run 'CacheControl|Golden|Fidelity' -count=1 && go test ./internal/profile/ -count=1 && go test -race ./internal/shaper/ ./internal/profile/ ./internal/paritycli/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestShape_CacheControlCapDegrade passes for the 3/4/5/6-block cases with order preservation.
    - The regenerated golden files differ from their pre-phase versions only by the cache_control additions, and a new assertion pins every system block carries {"type":"ephemeral"}.
    - The fidelity test passes unmodified in its decode expectations.
    - An extraction over a cache_control-bearing rollout fixture produces system_cache_control: true; one without produces it unset.
    - mise-task equivalents green: go vet + package tests under -race.
  </acceptance_criteria>
  <done>PAR-02 is complete and drift-proof: emission honors the API cap with a pinned degrade policy, goldens prove the wire form, and future re-captures preserve the lever.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile files → wire | operator-local trusted config becomes request-shaping flags |
| provider API → availability | breakpoint over-placement returns 400 and would fail every turn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | profiles/zcode/profile.yaml | low | accept | Operator-local trusted artifact (same trust class as model name and max_tokens already there); no untrusted input reaches the flag |
| T-19-02 | DoS | shaper emission vs 4-breakpoint API cap | medium | mitigate | Task 3's keep-last-4 degrade with a pinned test (3/4/5/6 cases); a 5th breakpoint would 400 every request once dynamic merges stack system blocks |
| T-19-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs this phase (SDK pinned at v1.63.0 in go.mod; zero new dependencies per RESEARCH legitimacy audit) |
</threat_model>

<verification>
- `go test -race ./internal/shaper/ ./internal/profile/ ./internal/paritycli/ -count=1` green.
- The parity cache probe green on the committed pin fixture (TestCacheProbe_PlacementFlip).
- WINDOWS ledger item 5 status fixed with the 19-01 reason recorded.
- `git diff` over internal/parity/cacheprobe.go across the whole plan is empty.
</verification>

<success_criteria>
PAR-02 closed per D-12: every system block of every outgoing request carries cache_control {"type":"ephemeral"} (visible in the request log), the probe flips green with an untouched baseline, the over-cap policy is pinned, and old profiles behave byte-identically.
</success_criteria>

<output>
Create `.planning/phases/19-compaction-cache-control/19-01-SUMMARY.md` when done
</output>
