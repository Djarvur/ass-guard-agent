---
phase: 24-documentation-ops-tails
plan: 05
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - internal/ecosys/testdata/modes-matrix/plugin.json
  - internal/ecosys/testdata/modes-matrix/commands/matrix-echo.md
  - internal/ecosys/testdata/modes-matrix/skills/matrix-skill/SKILL.md
  - internal/ecosys/testdata/modes-matrix/hooks/hooks.json
  - internal/modesmatrix/matrix.go
  - internal/acpserve/modesmatrix_interactive_test.go
  - internal/session/modesmatrix_subagent_test.go
  - internal/runtime/modesmatrix_cron_test.go
  - internal/runtime/modesmatrix_wake_test.go
autonomous: false
requirements: [TAIL-03]
user_setup:
  - service: real-claude-code-plugin
    why: "D-14's spot-check anchors the unchanged claim on ONE real installed Claude Code plugin per surface — an operator-environment fact the executor must be told or discover"
    dashboard_config:
      - task: "Name (or confirm) an installed Claude Code plugin on this machine for the spot-check"
        location: "~/.claude/plugins/ (or the operator's plugin config)"
estimate:
  tokens: 80000
  raw_tokens: 47000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A synthetic in-repo fixture plugin (D-14) exists under internal/ecosys/testdata/modes-matrix/ with a command, a skill, and PreToolUse/PostToolUse hooks whose commands are deterministic /bin/echo-family binaries with zero network and zero mutation of any real .claude/ tree (fixtures are COPIED into t.TempDir projects by the harness)"
    - "The interactive-ACP leg drives the REAL acpserve.Run composition (16-06 simulator discipline: scripted JSON-RPC over io.Pipes, guard timeouts, staged t.Helper assertions) with the fixture installed, and asserts a FUNCTIONAL outcome: the fixture hook FIRED with an observed effect (marker file content), not merely that discovery listed it (D-12/D-13)"
    - "The subagent leg drives a real subagent dispatch and asserts the same three surfaces functionally; the automation/cron leg drives the existing runAutomationTurn path (internal/sched store as cron substrate — never a re-implemented parser)"
    - "[probe TAIL-03 adjacency: equal things merge/collide/separate] When the fixture name collides with an existing command/skill name, resolution follows the locked D-06 two-phase precedence (project over user, plugin namespaced) — the harness asserts the collision resolves to the SAME winner the ecosys precedence tests lock, cells do not silently merge"
    - "[probe TAIL-03 empty: empty/single/null input] Each mode with NO fixture installed asserts the EMPTY outcome functionally: the command does not dispatch beyond its fallthrough, the hook marker file stays ABSENT — absence assertions, not errors"
    - "[probe TAIL-03 ordering: equal elements stable order] The harness records, for every cell it exercises, the resolution order it observed and asserts it matches the locked chain contract (builtins→skills→agents→file-commands per 20-CONTEXT) — order assertions are skipped loudly, never guessed, where the chain phase has not executed"
    - "Every cell whose substrate phase is PLANNED-but-unexecuted (background wake per 22-CONTEXT D-01; any command/skill cell whose resolver-chain behavior is not yet live per 20-CONTEXT; hook-authority cells per 21-CONTEXT) reports PRECONDITION-UNMET(<phase>, <contract-ref>) and is SKIPPED LOUDLY in the printed matrix — never a silent pass, never a fabricated green"
    - "The harness prints a final 4 modes x 3 surfaces matrix (run/pass, run/fail, precondition-unmet per cell) so the ECOS-04 state is readable in one place"
  artifacts:
    - internal/ecosys/testdata/modes-matrix/ (fixture plugin)
    - internal/modesmatrix/matrix.go (cell registry + report + loud-skip helper)
    - internal/acpserve/modesmatrix_interactive_test.go
    - internal/session/modesmatrix_subagent_test.go
    - internal/runtime/modesmatrix_cron_test.go
    - internal/runtime/modesmatrix_wake_test.go
  key_links:
    - "fixture hooks.json ↔ ecosys.HookRunner event vocabulary (internal/ecosys/hooks.go:54-66 — PreToolUse/PostToolUse, never renamed)"
    - "interactive cell ↔ acpserve.Run real composition (simulator_e2e_test.go pattern)"
    - "cron cell ↔ runtime.runAutomationTurn + sched.ScheduleStore (cron_wiring.go:133)"
    - "wake cells ↔ 22-CONTEXT D-01 contract (precondition-cited loud skips until Phase 22 executes)"
---

<objective>
TAIL-03 / ECOS-04: prove plugins/skills are not just discovered but FUNCTIONAL in every interaction mode — a scripted, repeatable, CI-runnable 4x3 mode/surface harness (D-11/D-12/D-13) over the real compositions, with per-cell loud precondition marks against the planned-but-unexecuted Phase 20/21/22 contracts (phase-ordering constraint), plus the one real-plugin spot-check (D-14).

Purpose: success criterion 4 of the phase — "working unchanged in EVERY interaction mode" — must be a repeatable proof, not a one-time checklist, and must not fabricate greens for legs whose substrate phases have not shipped.

Output: fixture plugin, mode-matrix harness tests across acpserve/session/runtime, matrix report, real-plugin spot-check evidence. Sequenced LAST per the phase-ordering constraint (wave 3, after 24-02 instruments the turn path these cells ride).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-documentation-ops-tails/24-CONTEXT.md
@.planning/phases/24-documentation-ops-tails/24-RESEARCH.md
@.planning/phases/24-documentation-ops-tails/24-PATTERNS.md
@.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md
@.planning/phases/20-built-in-commands-skills-per-agent-model/20-CONTEXT.md
@.planning/phases/21-context-policy-parity-closures/21-CONTEXT.md
@internal/acpserve/simulator_e2e_test.go
@internal/ecosys/hooks.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "fixture hook fires in a real interactive session" — one matrix cell, fully real</name>
  <files>internal/ecosys/testdata/modes-matrix/plugin.json, internal/ecosys/testdata/modes-matrix/hooks/hooks.json, internal/modesmatrix/matrix.go, internal/acpserve/modesmatrix_interactive_test.go</files>
  <read_first>internal/acpserve/simulator_e2e_test.go:14-56 + :359-420 (the 16-06 harness: real Run over io.Pipes, simGuardTimeout, string request ids, staged helpers, t.Setenv determinism, simRepointConfig writing .ass-guard/config.yaml at 0600), internal/ecosys/hooks.go:54-66 (hook event vocabulary) + the hooks.json schema in testdata/plugins-installed (PATTERNS fixture shape), internal/ecosys/testdata/plugins-installed/cache/acme-market/skill-plugin/1.0.0/ (the .claude-plugin/plugin.json fixture layout to copy), internal/ecosys/loader.go:39-66 + :118 (Load/loadAll two-phase precedence the fixture rides)</read_first>
  <action>
    Create the fixture plugin skeleton under internal/ecosys/testdata/modes-matrix/: plugin.json (name modes-matrix, version 1.0.0, description naming ECOS-04) and hooks/hooks.json with a PreToolUse hook (matcher scoped so it fires deterministically in the scripted turn) whose command appends its stdin JSON to the file named by env var ASSGUARD_MATRIX_HOOK_LOG (tee -a style with /bin-family binaries only — no network, no writes outside the env-named path; the harness sets that env to a t.TempDir file per test). Build internal/modesmatrix/matrix.go: a Cell{Mode, Surface} registry over the fixed 4x3 vocabulary (modes: interactive, subagent, wake, automation; surfaces: commands, skills, hooks — D-11/D-12 exactly, steering/parked-asks are mechanics inside interactive turns and are NOT rows), a status enum (pass, fail, precondition-unmet, skipped-empty), a Report() renderer printing the 4x3 grid with per-cell status + one-line reason, and a SkipPrecondition helper that marks a cell precondition-unmet with a phase + contract-ref and LOUDLY t.Skipfs — the skip message must begin with PRECONDITION-UNMET so the reason is grep-able and can never masquerade as a pass. Then the tracer cell: internal/acpserve/modesmatrix_interactive_test.go drives the REAL acpserve.Run (simulator pattern verbatim: scripted provider stub, temp project with the fixture copied in, guard timeouts) through one scripted turn whose tool call triggers the PreToolUse hook, and asserts the marker file RECEIVED the hook JSON (functional effect — D-13's bar). Register the cell pass in the matrix registry and print the 4x3 report at test end.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ -run TestModesMatrixInteractive -v 2>&1 | tail -10; go test -race -count=1 ./internal/acpserve/ -run TestModesMatrixInteractive 2>&1 | grep -q 'PRECONDITION-UNMET' && echo UNEXPECTED-PRECONDITION-SKIP || echo CELL-RAN; go test -race -count=1 ./internal/acpserve/ -run TestModesMatrixInteractive</automated>
  </verify>
  <acceptance_criteria>
    - TestModesMatrixInteractive passes under -race and the marker file contains the hook's stdin JSON with the expected event field PreToolUse
    - The printed matrix shows the interactive x hooks cell as pass and the other eleven cells as not-yet-exercised (no cell fabricated)
    - The fixture contains no network reference and no absolute path outside the env-var-indirected log target
    - matrix.go compiles as a non-test package (importable by the session/runtime test files coming in Task 2)
  </acceptance_criteria>
  <done>The harness skeleton is proven on one real cell: fixture → discovery → hook execution → observed effect → matrix pass, over the real ACP composition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: expand the fixture + exercise the existing substrate cells (commands/skills/hooks across interactive, subagent, automation)</name>
  <files>internal/ecosys/testdata/modes-matrix/commands/matrix-echo.md, internal/ecosys/testdata/modes-matrix/skills/matrix-skill/SKILL.md, internal/session/modesmatrix_subagent_test.go, internal/runtime/modesmatrix_cron_test.go, internal/acpserve/modesmatrix_interactive_test.go</files>
  <behavior>
    - Test 1 (interactive commands + skills cells): the scripted interactive turn exercises the fixture command and skill; each cell asserts its FUNCTIONAL outcome (the expansion/dispatch behavior the current live path actually exhibits) or records PRECONDITION-UNMET(20, resolver-chain contract) via a runtime functional probe when the chain behavior is not yet live — decided by observing the provider stub's received prompt, never by compile-time guesses
    - Test 2 (subagent cells): a real subagent dispatch with the fixture mounted exercises the three surfaces; hooks cell asserts the marker file; commands/skills cells use the same functional-probe-or-precondition rule
    - Test 3 (automation cells): internal/runtime same-package test drives runAutomationTurn through a sched.Create'd automation with the fixture project mounted; asserts the surfaces functionally; cron parsing comes from internal/sched only
    - Test 4 (empty-input row, probe empty): with the fixture ABSENT, each exercised mode asserts absence outcomes (no marker file, command falls through) — absence is a pass condition for the empty row, not an error
  </behavior>
  <read_first>internal/session/subagent.go (DispatchSubagent — the real subagent entry), internal/session/session.go:443 (subagent dispatch call shape), internal/runtime/cron_wiring.go:133 (runAutomationTurn signature + how automation turns enter the session), internal/runtime/cron_wiring_test.go + e2e_opsx_matrix_test.go (existing runtime e2e/matrix fixtures to copy), internal/sched/sched.go (ScheduleStore Create/List/MarkFired — the cron substrate), internal/ecosys/testdata/opsx-real/.claude/ (command + skill file shapes: frontmatter fields, $ARGUMENTS), internal/acpserve/simulator_e2e_test.go (prompt-stage scripting for the command/skill probes)</read_first>
  <action>
    Complete the fixture: commands/matrix-echo.md (frontmatter description + body invoking $ARGUMENTS with deterministic echo output) and skills/matrix-skill/SKILL.md (frontmatter name + description + a body whose execution leaves a detectable trace — the functional probe reads the provider stub's received prompt for the skill's expansion marker). Extend the interactive test with the commands and skills cells, each implemented as a functional probe: drive the turn, inspect what the provider stub RECEIVED, and classify pass (functional outcome observed) vs PRECONDITION-UNMET(Phase 20 resolver chain — cite 20-CONTEXT D-01..D-04) when the live path does not yet exhibit the chain behavior; per the phase-ordering constraint these marks are loud matrix statuses, never silent passes and never failures blamed on regression. Add the subagent test in internal/session (real DispatchSubagent over a fixture-mounted session with the scripted provider) and the automation test in internal/runtime (same package, runAutomationTurn + sched store with SetNow injected for determinism). Add the empty-fixture row asserting absence outcomes. Every exercised cell registers its observed resolution order where applicable (probe ordering) and asserts it only against the LOCKED chain contract; where the chain is not live the cell is precondition-marked instead (probe ordering, authored honestly). Print the full 4x3 matrix after each test file's cells run.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ ./internal/session/ ./internal/runtime/ -run TestModesMatrix -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All TestModesMatrix* tests pass under -race; the combined printed matrix covers ALL TWELVE cells with an explicit status each (pass, precondition-unmet, or skipped-empty) — zero cells missing, zero cells unmarked
    - Every PRECONDITION-UNMET line names its phase and contract reference (20/21/22-CONTEXT)
    - The hooks cells in all three exercised modes assert marker-file content (functional), and the empty row asserts marker-file ABSENCE
    - No test imports or references Phase 20/21/22 identifiers that do not exist yet (grep confirms — Pitfall 9: no compile-time coupling to unbuilt APIs)
    - The fixture command/skill files follow the opsx-real frontmatter shapes
  </acceptance_criteria>
  <done>Nine of twelve cells carry real scripted evidence on today's substrates; the matrix prints the honest ECOS-04 state; the empty-input row proves absence behavior is specified.</done>
</task>

<task type="auto">
  <name>Task 3: wake-mode loud-skip cells, matrix report gate, and the D-14 real-plugin spot-check</name>
  <files>internal/runtime/modesmatrix_wake_test.go, internal/modesmatrix/matrix.go, .planning/phases/24-documentation-ops-tails/24-05-SUMMARY.md</files>
  <read_first>.planning/phases/22-background-execution-sandbox-reality/22-CONTEXT.md (D-01 wake-turn contract — cited, never imported), internal/modesmatrix/matrix.go (Task 1-2 registry), internal/ecosys/testdata/plugins-installed/ (real-plugin layout reference for the spot-check mount)</read_first>
  <action>
    Author the three wake-mode cells (wake x commands/skills/hooks) as explicit precondition stubs: each registers in the matrix, cites 22-CONTEXT D-01 as its contract, invokes the SkipPrecondition helper with PRECONDITION-UNMET(22, background wake-turn D-01), and carries a TODO-free prose comment stating exactly what functional assertion will replace the skip once Phase 22 executes (no fabricated exercise bodies against unbuilt APIs — Pitfall 9). Harden matrix.go so a cell registered but never resolved fails the report (the registry cannot silently omit a cell — the 12-cell grid is total). Then the D-14 real-plugin spot-check: identify ONE real installed Claude Code plugin in the operator environment (user_setup surface — halt at the checkpoint below if none exists), mount it read-only into a temp project, run the harness's interactive leg against it, and record per-surface functional observations in the summary. Finish by writing the final 4x3 matrix into the plan summary as the phase's ECOS-04 evidence table.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ -run TestModesMatrixWake -v 2>&1 | grep -c 'PRECONDITION-UNMET(22' | grep -qx '3' && echo WAKE-CELLS-LOUD; go test -race -count=1 ./internal/acpserve/ ./internal/session/ ./internal/runtime/ -run TestModesMatrix 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Exactly three wake cells report PRECONDITION-UNMET(22, ...) — the count assertion proves loudness
    - The matrix report fails if any of the twelve cells is unregistered or unresolved (totality enforced by a test asserting the grid is complete)
    - The real-plugin spot-check evidence (plugin name, per-surface observations) exists in the plan summary
    - The summary carries the final 4x3 ECOS-04 table as the phase evidence
  </acceptance_criteria>
  <done>ECOS-04's state is total, honest, and repeatable: exercised cells prove function, unshipped cells cite their contracts loudly, the real plugin anchors the unchanged claim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: confirm the ECOS-04 matrix and real-plugin spot-check</name>
  <what-built>The 4x3 mode/surface matrix harness with fixture plugin, loud precondition marks, and the D-14 real-plugin spot-check evidence</what-built>
  <how-to-verify>
    1. Run: go test -race -count=1 ./internal/acpserve/ ./internal/session/ ./internal/runtime/ -run TestModesMatrix -v — confirm the printed 4x3 matrix shows the nine exercised cells' statuses and exactly three PRECONDITION-UNMET(22) cells.
    2. Review the summary's real-plugin spot-check section: the named plugin and the per-surface functional observations match what you see when you use that plugin in your own Claude Code setup.
    3. Skim the fixture at internal/ecosys/testdata/modes-matrix/ — confirm you are comfortable with these commands/hooks standing as the repo's ECOS-04 fixture.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture hook commands → host execution | The harness executes hook commands from fixture definitions in temp project dirs |
| real installed plugin → harness session | The D-14 spot-check mounts operator-installed third-party plugin content into a live session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-05-01 | Elevation | fixture hook command execution | medium | mitigate | Fixture commands are /bin-family echo/tee only, write solely to the env-indirected temp log path, zero network; fixtures are copied into t.TempDir projects and never executed in place from testdata |
| T-24-05-02 | Tampering | real .claude/ trees during the spot-check | high | mitigate | Read-only discipline: the spot-check mounts/copies the real plugin into a temp project; no test writes under any real .claude/ path (ecosys EnsureGitignore's refuse-.claude rule is the in-tree precedent) |
| T-24-05-03 | Repudiation | fabricated greens for unshipped legs | high | mitigate | The totality-enforced matrix + PRECONDITION-UNMET vocabulary + the count assertion (exactly 3 loud wake skips) make a silent pass structurally impossible; this is the plan's namesake prohibition below |
| T-24-05-04 | Information Disclosure | real-plugin content in evidence | low | mitigate | The summary records the plugin NAME and functional observations only — never its file contents or any private data it carries |
</threat_model>

<verification>
- go test -race -count=1 ./internal/acpserve/ ./internal/session/ ./internal/runtime/ -run TestModesMatrix green with the full 12-cell matrix printed
- The wake cells' PRECONDITION-UNMET(22) count is exactly three
- Fixture is hermetic (no network strings, no absolute paths)
- Real-plugin spot-check evidence + final matrix recorded in 24-05-SUMMARY.md
</verification>

<success_criteria>
Plugins/skills are proven FUNCTIONAL (not merely discovered) in the interactive, subagent, and automation modes on today's substrates; unshipped legs cite their phase contracts loudly; the empty row specifies absence behavior; one real installed plugin anchors the unchanged claim (D-11 through D-14).
</success_criteria>

<!-- Artifacts this phase produces (plan 24-05): fixture plugin files under internal/ecosys/testdata/modes-matrix/ (plugin.json, commands/matrix-echo.md, skills/matrix-skill/SKILL.md, hooks/hooks.json); internal/modesmatrix package (Cell/Status types, Report renderer, SkipPrecondition helper); test files modesmatrix_interactive_test.go, modesmatrix_subagent_test.go, modesmatrix_cron_test.go, modesmatrix_wake_test.go; env var convention ASSGUARD_MATRIX_HOOK_LOG; matrix statuses pass/fail/precondition-unmet/skipped-empty; the 4x3 ECOS-04 evidence table in 24-05-SUMMARY.md. -->

<output>
Create `.planning/phases/24-documentation-ops-tails/24-05-SUMMARY.md` when done
</output>
