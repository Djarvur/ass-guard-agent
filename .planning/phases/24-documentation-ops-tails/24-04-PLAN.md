---
phase: 24-documentation-ops-tails
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/profilecheckcmd/nightly_check.go
  - internal/profilecheckcmd/nightly_check_test.go
  - cmd/ass-guard/nightly_check.go
  - cmd/ass-guard/profile_check.go
  - profiles/zcode/structure-pin.json
  - .mise.toml
  - .github/workflows/nightly-parity.yml
autonomous: true
requirements: [TAIL-02]
user_setup:
  - service: github-actions-self-hosted-runner
    why: "The drift job (D-08) runs on a self-hosted runner where the zcode corpus lives; the hosted build-test job needs nothing"
    dashboard_config:
      - task: "Ensure a self-hosted runner with the exact label used in nightly-parity.yml is online for Djarvur/ass-guard-agent"
        location: "GitHub → repo Settings → Actions → Runners"
      - task: "Confirm repo Workflow permissions allow GITHUB_TOKEN issues:write (default may be read-only)"
        location: "GitHub → repo Settings → Actions → General → Workflow permissions"
  - service: schedule-activation
    why: "Scheduled workflows fire ONLY when the workflow file exists on the default branch master (research Pitfall 2) — activation lands at the milestone merge, not at this phase's merge"
    dashboard_config:
      - task: "After the v1.2 milestone merges to master, check the Actions tab shows the nightly-parity schedule"
        location: "GitHub → repo Actions → nightly-parity → scheduled runs"
estimate:
  tokens: 65000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ass-guard parity nightly-check (or the profile-check group sibling that holds `profile check`) runs OFFLINE: zcode version probe through the injectable paritycli.zcodeInstalledVersion seam + sha256 structure comparison of the profiles/zcode/ bundle against the committed profiles/zcode/structure-pin.json, emitting a drift report JSON and exiting 0 on parity / non-zero on drift"
    - "A --write-pin mode regenerates the pin file from the current bundle (single source of truth; updates only via the recapture runbook, stated in the command help)"
    - "[probe TAIL-02 unclassified: review manually → authored] A missed or failed nightly is an ACCEPTED state by design: drift-only (zero API spend), next scheduled run catches up; every run uploads its report artifact whether or not drift was found (D-10's belt)"
    - ".github/workflows/nightly-parity.yml schedules at an off-peak odd minute (NOT :00/:30, D-08), adds workflow_dispatch as the manual smoke/backstop, grants least-privilege permissions (contents: read; issues: write only on the drift job, Pitfall 6), runs the hosted build-test job (checkout → setup-go go-version-file: go.mod → go build + go test -race), and runs the drift job on runs-on with the self-hosted label set (zcode corpus stays local, D-08)"
    - "On drift the workflow opens a GitHub issue via actions/github-script reading the report FILE into the issue body — drift content never passes through shell arguments (Pitfall 10); the behavioral-eval machinery (ASSGUARD_EVAL_GATE) is untouched (D-09)"
    - "OQ3 RESOLVED: the nightly executes the LIGHT path only — probe + structure hash + build/test; recapture stays the operator runbook (docs/recapture-runbook.md), never part of the unattended job"
  artifacts:
    - internal/profilecheckcmd/nightly_check.go
    - internal/profilecheckcmd/nightly_check_test.go
    - cmd/ass-guard/nightly_check.go
    - profiles/zcode/structure-pin.json
    - .github/workflows/nightly-parity.yml
    - .mise.toml ([tasks.parity-nightly] added)
  key_links:
    - "nightly_check.go ↔ paritycli.zcodeInstalledVersion seam (injected in tests, real exec in the workflow)"
    - "nightly_check.go ↔ profiles/zcode/ pinned bundle + structure-pin.json (the comparison contract, A4)"
    - "workflow drift job ↔ internal/profilecheckcmd report JSON (artifact + issue body source)"
    - "workflow build-test job ↔ D-09's build/test letter (zero LLM spend)"
---

<objective>
TAIL-02: the nightly upstream-parity gate runs unattended — a unit-testable Go drift core (probe + structure hash vs the pinned capture, OQ3 light path) wrapped by the repo's FIRST GitHub Actions scheduled workflow (off-peak cron, self-hosted drift job, artifact-always, auto-issue-on-drift).

Purpose: MIMC drift (zcode version or profile structure changes) currently surfaces only when a human runs the check; D-08/D-09/D-10 make the signal unattended without spending a token or touching the 12-08 eval-gate machinery.

Output: drift-core command + tests, committed structure pin, `.github/workflows/nightly-parity.yml`, mise task, dispatch-smoke evidence.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-documentation-ops-tails/24-CONTEXT.md
@.planning/phases/24-documentation-ops-tails/24-RESEARCH.md
@.planning/phases/24-documentation-ops-tails/24-PATTERNS.md
@internal/paritycli/parity.go
@internal/drift/drift.go
@profiles/zcode/meta.yaml
@.mise.toml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one command proves parity locally" — drift core + committed pin</name>
  <files>internal/profilecheckcmd/nightly_check.go, internal/profilecheckcmd/nightly_check_test.go, cmd/ass-guard/nightly_check.go, cmd/ass-guard/profile_check.go, profiles/zcode/structure-pin.json</files>
  <read_first>internal/profilecheckcmd/profile_check.go (RunProfileCheck shape, --capture-file offline-flag pattern), internal/paritycli/parity.go:20-43 (zcodeInstalledVersion seam var + zcodeVersionTimeout — REUSE, never fork), internal/drift/drift.go:11-24 (existing field-level drift semantics — the nightly is the bundle-level complement, not a fork), profiles/zcode/ directory listing (the pinned file set: profile.yaml, tools.json, thinking.json, tool_choice.json, coverage.yaml, identity.yaml, meta.yaml) + meta.yaml zcode_version field (0.16.3 — the pin's version source), cmd/ass-guard/profile_check.go:12-42 (cobra shell pattern + the group that registers profile check — register the new subcommand THERE), cmd/ass-guard/parity.go (naming conventions)</read_first>
  <action>
    internal/profilecheckcmd/nightly_check.go: NightlyCheckOptions{ProfilesDir, PinPath, ReportPath, WritePin, VersionFunc} where VersionFunc defaults to the paritycli version seam (assign through it, do not copy the exec). RunNightlyCheck walks the pinned bundle's FIXED file set (the seven profiles/zcode/ files listed above — A4: the exact set is chosen here and pinned), computes sha256 per file with stdlib crypto/sha256, and compares against structure-pin.json {generated_at, zcode_version, files: {name: sha256}}. Behavior: --write-pin writes the pin from the current bundle + the probed zcode version and exits 0; check mode emits the report JSON (generated_at, zcode_version expected/found from pin vs probe, per-file expected/actual/match, drift bool, summary string) to ReportPath always, prints a one-line human summary to stderr, exit 0 on parity, exit 7 on drift (distinct code so CI maps it), exit 1 on operational error (missing pin, missing bundle file, probe failure treated as DRIFT with reason probe-failed — an unprovable environment must never read as parity). cmd/ass-guard/nightly_check.go: newParityNightlyCheckCmd cobra shell (Use "nightly-check") registered in the SAME group that holds profile check (profile_check.go:42 AddCommand site); flags --profiles-dir, --pin, --report, --write-pin; human summary → stderr, report JSON → file only (transport discipline: no stdout unless a future --json is added, which is NOT in scope). Tests (offline, injected VersionFunc + t.TempDir bundle): parity case; version-drift case (probe returns a different version); content-drift case (one bundle file's bytes change); probe-failure case (VersionFunc errors → drift with probe-failed reason); missing-pin case → exit 1; write-pin then check round-trip → parity. THEN generate the real committed pin: run the command with --write-pin against profiles/zcode/ and commit structure-pin.json.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/profilecheckcmd/ -run TestNightlyCheck -v 2>&1 | tail -6 && go build -o /tmp/ass-guard-nightly ./cmd/ass-guard/ && /tmp/ass-guard-nightly parity nightly-check --profiles-dir profiles/zcode --pin profiles/zcode/structure-pin.json --report /tmp/nightly-report.json; echo "exit=$?"; test -s /tmp/nightly-report.json && echo REPORT-OK</automated>
  </verify>
  <acceptance_criteria>
    - All TestNightlyCheck* cases pass under -race with no network and no zcode binary required (injected seam)
    - The committed profiles/zcode/structure-pin.json lists exactly the seven pinned files with sha256 digests and zcode_version 0.16.3 (or the current probed value if the operator's zcode moved — record which)
    - The local end-to-end run of the built binary against the real bundle exits 0 and writes a non-empty report JSON
    - The command appears in the same cobra group as profile check
  </acceptance_criteria>
  <done>The drift core is proven offline end-to-end and the committed pin freezes today's bundle as the comparison baseline.</done>
</task>

<task type="auto">
  <name>Task 2: .github/workflows/nightly-parity.yml — the repo's first workflow (D-08/D-09/D-10)</name>
  <files>.github/workflows/nightly-parity.yml, .mise.toml</files>
  <read_first>.mise.toml ([tasks.eval-gate] comment-annotated task shape to copy for [tasks.parity-nightly]; [tasks.ci] composition), 24-RESEARCH.md Pattern 3 (the workflow shape + first-party action versions verified 2026-08-28: actions/checkout v7.0.1, actions/setup-go v7.0.0, actions/github-script v9.0.0, actions/upload-artifact v7.0.1), cmd/ass-guard/nightly_check.go (Task 1 flags — the workflow invokes exactly these), go.mod (go-version-file source)</read_first>
  <action>
    Create .github/workflows/nightly-parity.yml: name nightly-parity; on schedule cron at an OFF-PEAK ODD minute in the 17-53 range off the hour/half-hour (D-08 — Claude's discretion per CONTEXT; pick e.g. minute 41 at a low-traffic UTC hour, comment citing the docs.github.com delay/drop warning) plus workflow_dispatch (manual smoke backstop — the local-validation substitute, no actionlint/act installed). Top-level permissions contents: read. Job build-test on ubuntu-latest: checkout, setup-go with go-version-file: go.mod, run CGO_ENABLED=0 go build ./... then go test -race -count=1 ./... (exactly D-09's build/test letter; mise is NOT required on hosted runners — do not add a mise setup action). Job drift with runs-on set to the self-hosted label array (all-labels-must-match; pick a concrete label set and use the SAME labels in the Task 3 smoke and the user_setup note), permissions contents: read + issues: write (Pitfall 6), steps: checkout; run the drift core via the new [tasks.parity-nightly] mise task (mise exists on the operator machine — the task wraps the same binary invocation the cobra command builds, building first with CGO_ENABLED=0); upload-artifact with the report path and if: always() so the artifact exists on EVERY run (D-10); an issues step gated on the drift exit code that uses actions/github-script to read the report FILE with fs.readFileSync and create the issue via github.rest.issues.create with a fixed title prefix plus a body composed in JS from the report's summary and per-file table — drift content never interpolated through shell arguments (Pitfall 10). Re-pin every action to its latest major tag at write time via gh api repos/<owner>/<action>/releases/latest (A2) and record the pinned tags in the workflow comments. Do NOT touch the ASSGUARD_EVAL_GATE machinery anywhere (D-09). Add [tasks.parity-nightly] to .mise.toml copying the eval-gate task's comment-annotated shape.
  </action>
  <verify>
    <automated>ruby -ryaml -e "w = YAML.load_file('.github/workflows/nightly-parity.yml'); raise 'no schedule' unless w['on']['schedule']; raise 'no dispatch' unless w['on']['workflow_dispatch']; raise 'bad perms' unless w['permissions']['contents'] == 'read'; puts 'WORKFLOW-YAML-OK'" && grep -q 'parity-nightly' .mise.toml && echo MISE-TASK-OK</automated>
  </verify>
  <acceptance_criteria>
    - The YAML parses; schedule + workflow_dispatch present; cron minute is off the :00/:30 boundaries and commented with the citation
    - drift job declares issues: write; build-test job does not; no pull_request_target anywhere in the file
    - The four actions are pinned to version tags re-verified via gh api on the execution date
    - The issue step reads the report from a file (grep confirms github-script + fs.readFile or equivalent, no $-interpolated drift content in run: blocks)
    - grep confirms no ASSGUARD_EVAL_GATE reference in the workflow
  </acceptance_criteria>
  <done>The workflow exists, parses, honors D-08 (off-peak + self-hosted labels), D-09 (drift + build/test only), and D-10 (artifact always, issue on drift) — ready for the dispatch smoke.</done>
</task>

<task type="auto">
  <name>Task 3: dispatch smoke — prove the unattended path fires (Pitfall 2/6 live check)</name>
  <files>.planning/phases/24-documentation-ops-tails/24-04-SUMMARY.md</files>
  <read_first>.github/workflows/nightly-parity.yml (Task 2 — job names, labels, dispatch trigger), RESEARCH.md Environment Availability (gh 2.98.0 available; zcode binary absent on this machine — the drift job's live leg is operator infrastructure)</read_first>
  <action>
    Trigger the workflow from the current branch: gh workflow run with the workflow file name, then poll with gh run view/gh run list to completion. Assert the build-test job runs green end-to-end (this proves checkout/setup-go/build/test on the hosted runner). Record the drift job's outcome honestly: if a self-hosted runner matching the label array is online, the job's report artifact must exist; if no runner is online the job stays queued — record exactly that in the summary as an operator-infrastructure note (never fake, never mark green, never silently drop — this is the D-08 user_setup surface). Also record the schedule-activation caveat in the summary: the cron fires only after this file exists on master (milestone merge — research Pitfall 2), and workflow_dispatch is the meanwhile backstop.
  </action>
  <verify>
    <automated>gh run list --workflow nightly-parity.yml --limit 1 --json status,conclusion,jobs 2>/dev/null | grep -q 'completed' && echo SMOKE-RAN; gh workflow view nightly-parity.yml 2>/dev/null | grep -qi 'workflow_dispatch' && echo DISPATCH-TRIGGER-OK</automated>
  </verify>
  <acceptance_criteria>
    - A workflow_dispatch run exists for the branch with the build-test job conclusion success
    - The drift job's state (ran-with-artifact OR queued-no-runner) is recorded verbatim in the plan summary with the runner label situation
    - The summary states the master-merge schedule-activation condition explicitly
  </acceptance_criteria>
  <done>The hosted unattended path is proven live; the self-hosted leg's dependency on operator infrastructure is surfaced, not assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedule/dispatch events → workflow execution | GitHub-hosted and self-hosted runners execute repo code triggered by time or by operator dispatch |
| drift report → GitHub issue | Report content (paths, hashes, version strings) flows into a public-to-repo issue body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-04-01 | Elevation | self-hosted drift job | high | mitigate | Triggers are schedule + workflow_dispatch ONLY — never pull_request_target; repo is private; the zcode corpus stays local per D-08; branch protection on master is the tamper boundary for the scheduled file |
| T-24-04-02 | Denial of Service | GITHUB_TOKEN read-only default breaks issue-on-drift | high | mitigate | Explicit job-level issues: write (Pitfall 6); the report artifact uploads with if: always() so the signal survives even when the issue step fails; Task 3 smoke + user_setup permission check |
| T-24-04-03 | Tampering / Elevation | drift content in issue body | high | mitigate | actions/github-script composes the body in JS from the report file — zero shell interpolation of drift content (Pitfall 10) |
| T-24-04-04 | Information Disclosure | report artifact + issue contents | medium | mitigate | The report carries file names, sha256 digests, and version strings only — no capture bytes, no profile content, no credentials (D-04's spirit applied to TAIL-02) |
| T-24-04-05 | Tampering | third-party action supply chain | medium | mitigate | First-party actions/* org only, pinned to verified version tags, re-pinned via gh api at execution (A2); SHA-pinning noted as an available hardening upgrade |
| T-24-04-06 | Denial of Service | cost runaway via schedule | low | accept | Drift-only nightly with zero API spend (D-09 prohibition below); a missed run is a no-op catch-up by design |
</threat_model>

<verification>
- go test -race ./internal/profilecheckcmd/ green; mise ci green
- Local end-to-end: nightly-check exits 0 with a report against the real bundle + committed pin
- Workflow YAML parses; dispatch smoke: build-test job green on the hosted runner
- Schedule-activation caveat + self-hosted runner state recorded in the summary
</verification>

<success_criteria>
The parity gate is unattended: the drift core is offline-testable Go, the pin freezes the baseline, the workflow runs on an off-peak schedule with least-privilege permissions, always uploads its report, and auto-opens an issue on drift without a single API token spent (D-08/D-09/D-10; OQ3 light path).
</success_criteria>

<!-- Artifacts this phase produces (plan 24-04): functions RunNightlyCheck + NightlyCheckOptions (internal/profilecheckcmd); cobra command `nightly-check` (parity/profile-check group); file profiles/zcode/structure-pin.json; mise task parity-nightly; workflow .github/workflows/nightly-parity.yml (cron + workflow_dispatch + jobs build-test/drift); report JSON schema {generated_at, zcode_version, files, drift, summary}; exit codes 0 parity / 7 drift / 1 operational. -->

<output>
Create `.planning/phases/24-documentation-ops-tails/24-04-SUMMARY.md` when done
</output>
