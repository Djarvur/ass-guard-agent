---
phase: 24-documentation-ops-tails
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - internal/session/session.go
  - internal/session/subagent.go
  - internal/session/session_outcomes_test.go
  - internal/runtime/runtime.go
  - internal/runtime/apply_model_test.go
  - internal/acpserve/acp_serve.go
  - internal/acpserve/config_surface.go
  - internal/acpserve/config_surface_outcomes_test.go
  - internal/modelroutingcmd/modelrouting.go
  - cmd/ass-guard/modelrouting.go
  - cmd/ass-guard/modelrouting_test.go
autonomous: true
requirements: [TAIL-01]
estimate:
  tokens: 75000
  raw_tokens: 44000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "A completed provider attempt on the live turn path (session.Prompt streamAndEmit, session.go:394 call site) appends exactly one DispatchOutcome line to .ass-guard/routing/outcomes.jsonl with the real provider name, effective model, outcome class from the error kind, turn latency, and streamed usage tokens (0 stays 0-meaning-unknown, Pitfall 7)"
    - "A completed subagent dispatch (subagent.go Stream call site) appends one record with origin subagent and the effective (possibly overridden) subagent model"
    - "Store I/O failure never fails a turn: recording errors surface as one loud stderr note and the turn proceeds (graceful degradation house pattern)"
    - "ass-guard model-routing stats reads the store and prints aggregated per-(provider,model) stats to STDERR in human form; --json prints the aggregate to STDOUT as the ONLY stdout path (transport discipline, C1)"
    - "[probe TAIL-01 live observable] With a seeded store whose breaker for the primary (provider,model) is open, the config-surface model resolution (resolveModelLocked) and the subagent light-tier resolution (resolveSubagentModel) each return the first ALLOWED fallback candidate instead of the open primary — routing decisions provably change after enough evidence accumulates (D-06)"
    - "With an empty/absent store the resolution output is byte-identical to today's behavior (zero behavior change when no evidence exists)"
  artifacts:
    - internal/session/session_outcomes_test.go
    - internal/acpserve/config_surface_outcomes_test.go
    - internal/modelroutingcmd/modelrouting.go (EmitStatsHuman/EmitStatsJSON added)
    - cmd/ass-guard/modelrouting.go (stats subcommand added)
  key_links:
    - "session/subagent record sites → OutcomeStore.Append (plan 24-01 store)"
    - "acp_serve.go:211 composition → ConfigSurface.SetOutcomeBreakers fed by modelrouting.ReplayBreakers over the WorkDir store"
    - "runtime.go:1185 SubagentModel population → resolveSubagentModel with replayed breakers + FirstAllowed demotion"
    - "cmd/ass-guard stats shell → modelroutingcmd renderers (Phase-15 CLI-support split)"
---

<objective>
TAIL-01 live wiring: record outcomes at the REAL dispatch sites (Pitfall 1 — Scheduler.Dispatch has zero production callers), expose `ass-guard model-routing stats`, and make the replayed evidence bend the live routing decisions at the two existing Resolve call sites.

Purpose: D-06's letter — feedback observable as changed routing decisions — dies if it lands only in a Scheduler nothing constructs. This plan attaches recording where provider calls actually happen (session turn + subagent dispatch) and consumption where resolution actually happens (runtime.go:1316 shape + config_surface.go:321 shape).

Output: instrumented live path, stats CLI, seeded-store demotion behavior with tests proving all three.

OQ2 RESOLUTION implemented here (decided in plan 24-01's context): recording at Send/Stream sites, feedback via the Breaker seam at the Resolve sites; Scheduler.Dispatch stays unwired.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-documentation-ops-tails/24-CONTEXT.md
@.planning/phases/24-documentation-ops-tails/24-RESEARCH.md
@.planning/phases/24-documentation-ops-tails/24-PATTERNS.md
@.planning/phases/24-documentation-ops-tails/24-01-SUMMARY.md
@internal/session/session.go
@internal/runtime/runtime.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one real turn leaves one outcome record" — the live record path</name>
  <files>internal/session/session.go, internal/session/subagent.go, internal/session/session_outcomes_test.go, internal/runtime/runtime.go</files>
  <read_first>internal/session/session.go (Session struct fields :44-100 — nil-allowed injected deps pattern like Hooks/Checkpointer; turn loop :394 streamAndEmit call; streamAndEmit :637-700 incl. the chunk.Usage → event.UsageUpdate publish :685-688), internal/session/subagent.go:190-230 (the subagent Stream attempt + subagentProfile model override), internal/runtime/runtime.go:1160-1200 (Session construction :1170 + SubagentModel population :1185; r.providerName :137-141), internal/modelrouting/outcomes.go (plan 24-01 store), internal/provider/errors.go:24-48 (KindTransient/KindStructural/KindExhausted, ProviderError.Provider/Model), internal/session/ existing provider-stub test harness (any *_test.go constructing a Session with a scripted provider — copy its fixture pattern)</read_first>
  <action>
    Wire ONE path end-to-end: a parent turn's provider attempt → one outcome line on disk. Add to Session three nil-allowed injected fields following the Hooks/Checkpointer pattern: Outcomes (*modelrouting.OutcomeStore), ProviderName (string), SessionTier (string). In streamAndEmit, sum chunk.Usage input/output into per-turn counters alongside the existing UsageUpdate publish. In the turn loop immediately after the streamAndEmit call at :394, when Outcomes is non-nil: measure latency around the attempt, classify streamErr via errors.As into *provider.ProviderError (nil error → outcome ok; KindTransient/KindStructural/KindExhausted → that class; non-ProviderError error → transient, the conservative class), build one DispatchOutcome (provider = ProviderName, model = the effective profile model s.Profile.Model at attempt time, tier = SessionTier, origin = turn, fallback_used = false — the live path walks no fallback today, state that on the field), Append to the store. Append error → exactly ONE stderr note via the session's existing stderr discipline, never a turn failure. Mirror the same single record in subagent.go's Stream site with origin subagent and the effective subagentProfile model. Populate ProviderName/SessionTier where SubagentModel is populated (runtime.go:1185 area) from r.providerName and the session tier already resolved by the composition. In the test, drive a real Session with the existing scripted-provider fixture through one Prompt turn (success case) and one failing case (scripted provider returning a *provider.ProviderError KindTransient) against a t.TempDir store; assert the file holds exactly the expected records with exact field values including summed usage tokens from a scripted usage chunk.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run TestSessionOutcomeRecording -v 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - TestSessionOutcomeRecording exists in internal/session/session_outcomes_test.go and passes under -race
    - The test asserts the JSONL line contains the real provider name, the effective model, outcome ok on the success case and transient on the ProviderError case, and non-zero in/out tokens when the scripted provider emits a usage chunk
    - The failing-store case (store path pointing at an unwritable dir) leaves the turn completing with exactly one stderr note and no error returned
    - No production code path imports time.Now for the record timestamp without routing through an injectable now seam used by tests
  </acceptance_criteria>
  <done>The thinnest live path is proven: real turn → real file → real fields — committed, production-quality, with the nil-store path (every existing Session construction site) byte-identical in behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ass-guard model-routing stats — aggregated store dump (D-07 CLI half)</name>
  <files>internal/modelroutingcmd/modelrouting.go, cmd/ass-guard/modelrouting.go, cmd/ass-guard/modelrouting_test.go</files>
  <behavior>
    - Test 1: stats over a seeded temp store prints one line per (provider,model) with ok/transient/structural/exhausted counts and token/cost totals to STDERR; STDOUT stays byte-empty
    - Test 2: stats --json prints the aggregate JSON to STDOUT and writes nothing to STDERR beyond cobra's own silence; the JSON parses and round-trips the seeded counts exactly
    - Test 3: stats against a nonexistent store path exits 0 with an empty-aggregate human note (no store yet is a normal state, not an error)
    - Test 4: --store flag overrides the default path (test seeds a non-default location)
  </behavior>
  <read_first>cmd/ass-guard/modelrouting.go (newModelRoutingCmd group :17-28 Use "model-routing"; newModelRoutingResolveCmd :64-127 flag/emit shape), internal/modelroutingcmd/modelrouting.go (EmitResolveHuman stderr discipline :12-40; EmitResolveJSON stdout encoder :42-78 with SetIndent + //nolint:wrapcheck), internal/modelrouting/outcomes_agg.go (Aggregate from plan 24-01), cmd/ass-guard/modelrouting_test.go (existing wire-discipline test pattern to extend)</read_first>
  <action>
    Add newModelRoutingStatsCmd to the model-routing group: flags --store (default .ass-guard/routing/outcomes.jsonl relative to cwd), --json bool, --config (optional, to enrich the dump with the configured tier binding per model for operator context; default = embedded zero-config floor like the resolve command). Logic lives in internal/modelroutingcmd as LoadAndAggregate(storePath) plus EmitStatsHuman(w io.Writer, ...) and EmitStatsJSON(w io.Writer, ...) mirroring the EmitResolve* signature discipline exactly (io.Writer first, human variant ignores write errors, JSON variant returns the encode error). Human → cmd.ErrOrStderr(); --json → cmd.OutOrStdout(); register via cmd.AddCommand in newModelRoutingCmd. Per D-07.
  </action>
  <verify>
    <automated>go test -count=1 ./cmd/ass-guard/ -run TestSchedulingStats -v 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - TestSchedulingStats passes and asserts stdout byte-cleanliness on the human path (the standing C1 discipline test pattern)
    - grep confirms newModelRoutingStatsCmd is registered inside newModelRoutingCmd
    - The JSON aggregate includes per-(provider,model) outcome-class counts plus token and cost totals
  </acceptance_criteria>
  <done>Operators can dump aggregated routing outcomes; transport discipline holds (human→stderr, --json→stdout only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: feedback consumption — replayed breakers bend the two live Resolve sites (D-06 observable)</name>
  <files>internal/acpserve/config_surface.go, internal/acpserve/acp_serve.go, internal/acpserve/config_surface_outcomes_test.go, internal/runtime/runtime.go, internal/runtime/apply_model_test.go</files>
  <behavior>
    - Test 1: ConfigSurface with SetOutcomeBreakers containing an OPEN breaker for the resolved primary's (provider,model) → resolveModelLocked returns the first allowed fallback's model, and the advertisement/pending path reflects it
    - Test 2: same surface with an EMPTY breaker map → resolution identical to the pre-change behavior (model = primary)
    - Test 3: resolveSubagentModel with an open breaker on the light-tier primary → returns the first allowed fallback slug; unchanged when breakers empty
  </behavior>
  <read_first>internal/acpserve/config_surface.go (resolveModelLocked :315-330; SetNotify setter precedent :124-128), internal/acpserve/acp_serve.go:200-230 (NewConfigSurface call site :211 — the Run composition), internal/runtime/runtime.go:1297-1334 (resolveSubagentModel + its single call site :1185), internal/modelrouting/outcomes_agg.go (ReplayBreakers, FirstAllowed from plan 24-01), internal/acpserve/config_test.go (existing surface test fixtures)</read_first>
  <action>
    ConfigSurface: add an unexported outcomeBreakers map field + SetOutcomeBreakers setter (SetNotify precedent, wired by the Run composition). resolveModelLocked: after a successful Resolve, if outcomeBreakers is non-empty and the primary's breaker denies time.Now, walk primary+fallbacks through modelrouting.FirstAllowed and use the demoted target's model; emit exactly ONE loud stderr note naming the demoted (provider,model) and the chosen replacement (graceful-degradation-plus-loud-note house pattern); empty map = zero change. acp_serve.go: after the NewConfigSurface call at :211, load the WorkDir store via modelrouting.ReadOutcomes (absent file → nil map, no note) and wire ReplayBreakers through SetOutcomeBreakers. runtime.go: extend resolveSubagentModel with a breakers parameter — its single call site :1185 passes the Runner's replayed breakers (Runner loads the store once at composition from its work dir; nil/absent → nil map); when the light-tier primary is breaker-denied, FirstAllowed picks the first allowed fallback and one loud stderr note names the demotion. The Runner loads via ReadOutcomes + ReplayBreakers with the config's breaker thresholds from the already-loaded schedCfg. Per D-06: no new preference layer — demotion rides the existing Breaker seam over the chain Resolve already returned.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ ./internal/runtime/ -run 'TestOutcome|TestResolveSubagentModel|TestConfigSurface' 2>&1 | tail -6 && go test -race -count=1 ./internal/modelrouting/ ./internal/session/ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - config_surface_outcomes_test.go Test 1 and Test 2 pass, proving the demotion fires only when seeded evidence exists
    - The runtime test proves resolveSubagentModel demotes only under an open breaker and returns the fallback slug
    - grep confirms SetOutcomeBreakers is called exactly once in acp_serve.go composition and resolveSubagentModel's only production caller passes the replayed map
    - Full package suites for internal/acpserve, internal/runtime, internal/session, internal/modelrouting stay green under -race
  </acceptance_criteria>
  <done>D-06 is observable in the LIVE path: seed enough failure evidence in `.ass-guard/routing/outcomes.jsonl` and subsequent session-model / subagent-model resolutions pick the fallback — with an empty store, behavior is byte-identical to today (Pitfall 1's warning sign structurally impossible: the store IS read by the path that routes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| turn path → recorder | Every provider attempt (any mode) now touches the outcome store; a hostile/broken store must not take turns down |
| store → routing decisions | Aggregated evidence now changes which model serves sessions — poisoned records could degrade service |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-02-01 | Denial of Service | record sites in session.go/subagent.go | high | mitigate | Recording is fire-and-forget: Append error → one stderr note, turn proceeds; store construction failure leaves Outcomes nil (recording disabled, loud note) — no turn can fail because of the store |
| T-24-02-02 | Information Disclosure | outcome records | medium | mitigate | Only mechanical fields recorded (plan 24-01 schema gate); provider/model/tier names are configuration facts, not secrets; no prompt or response content can reach the file |
| T-24-02-03 | Tampering | replayed breakers → model demotion | medium | mitigate | Local attacker with write access to `.ass-guard/` already controls session state (checkpoints, schedules — same trust tier); replay is bounded by CircuitBreaker cooldowns so damage is temporary; perms 0600/0750 |
| T-24-02-04 | Elevation | stats CLI | low | accept | Read-only operator command over a user-owned file; --json path writes only to the explicitly-requested stdout, no execution surface |
</threat_model>

<verification>
- `mise test` green (full race suite)
- `mise ci` green
- End-to-end operator check: `ass-guard model-routing stats` on a work dir with a seeded store prints the seeded aggregates; with the seeded breaker open, a new session resolves to the fallback model (asserted by config_surface_outcomes_test.go at the seam level)
</verification>

<success_criteria>
Live provider attempts land in `.ass-guard/routing/outcomes.jsonl` (turn + subagent origins); the stats dump exposes aggregates; seeded evidence changes live resolution at both Resolve sites; an empty store changes nothing. Zero LLM calls anywhere (D-04); no new tier-preference layer (D-06).
</success_criteria>

<!-- Artifacts this phase produces (plan 24-02): Session fields Outcomes/ProviderName/SessionTier; ConfigSurface.SetOutcomeBreakers; Runner replayed-breaker loading; resolveSubagentModel breakers parameter; modelroutingcmd functions LoadAndAggregate, EmitStatsHuman, EmitStatsJSON; cobra command `ass-guard model-routing stats` with flags --store/--json/--config; tests TestSessionOutcomeRecording, TestSchedulingStats, config-surface/subagent demotion tests; no new file paths beyond the store convention from plan 24-01. -->

<output>
Create `.planning/phases/24-documentation-ops-tails/24-02-SUMMARY.md` when done
</output>
