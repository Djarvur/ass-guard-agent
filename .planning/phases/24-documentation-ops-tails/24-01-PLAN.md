---
phase: 24-documentation-ops-tails
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/modelrouting/outcomes.go
  - internal/modelrouting/outcomes_agg.go
  - internal/modelrouting/outcomes_test.go
  - internal/modelrouting/dispatch.go
autonomous: true
requirements: [TAIL-01]
estimate:
  tokens: 60000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Appending N DispatchOutcome records then ReadOutcomes returns exactly N records with every D-04 field intact: provider, model, tier, outcome class (ok|transient|structural|exhausted), fallback_used, latency_ms, in_tokens, out_tokens, cost_usd, origin, at"
    - "[probe idempotency: what if this runs twice on the same input] Duplicate Append of an identical record yields TWO stored records — the store is append-only with NO dedup; one record per provider attempt is guaranteed by the recording sites (plan 24-02), not by the store"
    - "[probe concurrency: if interrupted or run in parallel, what is guaranteed] Concurrent Append from 10 goroutines produces exactly 10xN intact JSONL lines (mutex + O_APPEND); an interrupted write leaves at most one truncated trailing line, which ReadOutcomes SKIPS (never fails the read, never drops earlier records)"
    - "Replaying a seeded store through the existing breaker seam (ReplayBreakers) opens the breaker for a (provider,model) with enough consecutive transients and leaves unseen keys Closed — Allow(now) returns false/true respectively"
    - "Replaying seeded token/cost records through the existing cost seam (ReplayCostTracker) returns CostDegrade from Check once windowed spend crosses the configured ceiling"
    - "Structural and exhausted records NEVER feed the breaker on replay — replay mirrors dispatch.go:250-272 exactly (structural does not open breakers)"
    - "FirstAllowed over a candidate chain returns the first candidate whose breaker Allow(now) is true (absent key = allowed) and reports whether a demotion occurred"
  artifacts:
    - internal/modelrouting/outcomes.go
    - internal/modelrouting/outcomes_agg.go
    - internal/modelrouting/outcomes_test.go
  key_links:
    - "outcomes.go record schema ↔ dispatch.go:250-272 outcome learning points (ok = RecordSuccess path; transient = RecordTransient path; structural/exhausted = terminal no-breaker-feed paths)"
    - "ReplayBreakers output ↔ Breaker seam (dispatch.go:25-33) — consumable via Scheduler.SetBreakers and FirstAllowed"
    - "ReplayCostTracker output ↔ CostTracker seam (dispatch.go:37-40) — CostCeilingTracker semantics (cost.go:61-83 pricing formula)"
---

<objective>
TAIL-01 core: the deterministic outcome store (append-only JSONL under `.ass-guard/`, per D-04/D-05) and the seam-replay aggregation that feeds the EXISTING breaker/cost/fallback seams (per D-06) — built test-first.

Purpose: D-07's deterministic test (seed store → breaker opens / degrade fires) is the acceptance arbiter for the whole tail; this plan builds the tested core that plan 24-02 wires into the live turn path.

Output: `internal/modelrouting/outcomes.go` (record + store), `outcomes_agg.go` (aggregate + replay + FirstAllowed), `outcomes_test.go` (RED→GREEN suite).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-documentation-ops-tails/24-CONTEXT.md
@.planning/phases/24-documentation-ops-tails/24-RESEARCH.md
@.planning/phases/24-documentation-ops-tails/24-PATTERNS.md

OQ2 RESOLUTION (research Open Question 2, decided in-plan per A3): the Scheduler is NOT wired into the live turn loop (blast radius; the live path is Stream-based while Dispatch is Send-based). Recording attaches at the real Send/Stream sites (plan 24-02), and aggregation re-enters routing through the existing Breaker seam at the two live Resolve sites. This plan delivers the store + replay core that makes that wiring possible and testable.
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): failing outcome-store + feedback test suite</name>
  <files>internal/modelrouting/outcomes_test.go</files>
  <read_first>internal/modelrouting/dispatch.go (Breaker/CostTracker seams :25-40, SetBreakers/SetCostTracker :107-127, Dispatch outcome learning points :250-272, providerModelKey :55-60), internal/modelrouting/breaker.go (CircuitBreakerConfig, NewCircuitBreaker :59-77, Allow/RecordSuccess/RecordTransient semantics), internal/modelrouting/cost.go (CostCeilingTracker :24-58, Account :61-83, CostAction values dispatch.go:45-54), internal/sched/sched.go:22-39 (storeDirName/filePerm 0600/dirPerm 0750 house conventions), internal/session/transcript.go:158-191 (selfGitignoreContent + O_APPEND|O_CREATE|O_WRONLY open pattern), internal/modelrouting/dispatch_test.go + breaker_test.go (stdlib testing, injected clock, no testify — follow these conventions)</read_first>
  <action>
    Write the failing test suite FIRST (all tests must fail on the not-yet-written API — the packages do not compile until Task 2 provides the types, which counts as RED; if you prefer a compiling RED, write the test file plus empty type/function skeletons returning zero values so tests RUN and FAIL). Suite, all table-driven where natural, stdlib testing only, injected clock via a now func parameter (never time.Now inside logic under test):

    TestOutcomeStoreAppendReadRoundtrip — NewOutcomeStore over a t.TempDir root, Append three DispatchOutcome records with distinct (provider, model, tier, outcome, fallback_used, latency_ms, in_tokens, out_tokens, cost_usd, origin), ReadOutcomes returns exactly 3 with every field equal.

    TestOutcomeStoreTolerantRead — write a store file containing: one valid record line, one malformed JSON line, one valid record carrying an UNKNOWN extra JSON field; ReadOutcomes returns the 2 valid records (unknown fields tolerated, 16-D-20 additive discipline), skipped count exactly 1, no error.

    TestOutcomeStoreInterruptedTail — file ending in a partial line (valid JSON prefix, no newline terminator); ReadOutcomes returns the earlier records, skips the truncated tail, no error.

    TestOutcomeStorePermsAndGitignore — after first Append: store dir mode at most 0750, file mode at most 0600, and the `.ass-guard/.gitignore` self-exclusion file exists with body exactly newline-asterisk-newline-bang-dotgitignore-newline (the transcript.go selfGitignoreContent pattern — the store writes it itself; modelrouting must NOT import internal/ecosys for this).

    TestOutcomeStoreConcurrentAppend — 10 goroutines each Append 10 distinct records; ReadOutcomes returns exactly 100 records, each parsing as one complete JSON object.

    TestOutcomeFeedbackBreakerOpens — seed a store file with CircuitBreakerConfig.ConsecutiveFailures consecutive transient records for (providerA, modelA); ReplayBreakers over the records returns a map whose key for (providerA, modelA) has Allow(fixedNow) false; a key never seen in the store is ABSENT from the map (absent = allowed by convention, dispatch.go:110-118).

    TestOutcomeFeedbackBreakerRecoversAndSkipsStructural — seed transient, transient, ok for one key → Allow true; seed only structural + exhausted records for another key → that key absent from the replayed map (structural/exhausted never feed breakers).

    TestOutcomeFeedbackCostDegrade — construct a CostCeilingConfig whose ceiling is crossed by the seeded records' in/out tokens against the zero-config pricing floor (or a small in-test pricing map); ReplayCostTracker returns a CostTracker whose Check(fixedNow) equals CostDegrade.

    TestFirstAllowedDemotes — candidates: primary (breaker open) then fallback (no entry); FirstAllowed returns the fallback Target with demoted true; with an all-allowing chain it returns the primary with demoted false.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/modelrouting/ -run 'TestOutcome|TestFirstAllowed' 2>&1 | tail -5; test "$(go test -race -count=1 ./internal/modelrouting/ -run 'TestOutcome|TestFirstAllowed' 2>&1 | grep -c 'FAIL\|cannot\|undefined')" -gt 0 && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - internal/modelrouting/outcomes_test.go exists and references DispatchOutcome, OutcomeStore/NewOutcomeStore, ReadOutcomes, ReplayBreakers, ReplayCostTracker, FirstAllowed
    - The test run FAILS or fails-to-compile (RED confirmed) — zero tests green at this point
    - No testify import anywhere in the new file
  </acceptance_criteria>
  <done>RED state proven: the suite compiles-and-fails or fails-to-compile against the absent API, with every listed test present in the file.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): outcome store + seam-replay aggregation</name>
  <files>internal/modelrouting/outcomes.go, internal/modelrouting/outcomes_agg.go, internal/modelrouting/dispatch.go</files>
  <read_first>internal/modelrouting/outcomes_test.go (Task 1 — the contract), internal/modelrouting/dispatch.go (full file — seams, Dispatch walk, CostAction), internal/modelrouting/breaker.go, internal/modelrouting/cost.go, internal/session/transcript.go:158-191, internal/sched/sched.go:113-163 (tolerant load + quarantine precedent)</read_first>
  <action>
    outcomes.go: type DispatchOutcome with json-tagged fields per D-04 — at (RFC3339Nano), provider, model, tier, outcome (string enum ok|transient|structural|exhausted), fallback_used bool, latency_ms int64, in_tokens int64, out_tokens int64, cost_usd float64, origin string (turn|subagent). Zero in_tokens/out_tokens/cost_usd means unknown-on-this-path, never free (Pitfall 7 — document on the type). NewOutcomeStore(root string) resolves the store path as root/.ass-guard/routing/outcomes.jsonl, MkdirAll 0750, writes the self-gitignore body to root/.ass-guard/.gitignore when absent. Append serializes on a sync.Mutex, opens O_APPEND|O_CREATE|O_WRONLY 0600, writes one line + newline, closes per call (open-per-append keeps the interrupted-write window to one line). ReadOutcomes(path string) returns ([]DispatchOutcome, skipped int, err): per-line json.Unmarshal, skip + count malformed lines and lines with unknown shapes, never fail the whole read. Store I/O failures are returned, never panicked; callers (plan 24-02) treat them as loud-non-fatal.

    outcomes_agg.go: Aggregate(records, window) producing per-ProviderModelKey counts (ok/transient/structural/exhausted, total tokens, total cost) with a rolling window expressed in RECORD COUNT (last N records per key) — Pitfall 8: window in recorded outcomes, not wall-clock alone; Claude's discretion per CONTEXT, documented on the type. ReplayBreakers(records, breakerCfg, log) map[ProviderModelKey]Breaker: chronological replay constructing NewCircuitBreaker per key, ok → RecordSuccess, transient → RecordTransient(now, synthetic *provider.ProviderError with Kind KindTransient and the record's Provider/Model), structural/exhausted skipped entirely. ReplayCostTracker(records, costCfg, pricing, bus, log) CostTracker: NewCostCeilingTracker + Account per record. FirstAllowed(candidates []Target, breakers map[ProviderModelKey]Breaker, now time.Time) (Target, bool): first candidate whose breaker (absent = allow) admits now, second return = demoted.

    dispatch.go: rename the unexported providerModelKey type to exported ProviderModelKey (in-package uses in dispatch.go + breaker.go only — mechanical rename, no behavior change) so the replay map is consumable across packages.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/modelrouting/ && go vet ./internal/modelrouting/</automated>
  </verify>
  <acceptance_criteria>
    - Every Task 1 test passes under -race; the full package suite stays green
    - grep confirms outcomes.go carries json tags for at/provider/model/tier/outcome/fallback_used/latency_ms/in_tokens/out_tokens/cost_usd/origin
    - grep confirms ProviderModelKey is exported and no unexported providerModelKey remains in internal/modelrouting
    - Store file perms verified 0600 by the Task 1 test
  </acceptance_criteria>
  <done>D-07's core observable holds deterministically: seeded store → breaker opens / cost degrade fires, through the EXISTING seams only (D-06 — no new tier-preference layer anywhere in the diff).</done>
</task>

<task type="auto">
  <name>Task 3 (REFACTOR + gate): commit discipline and full-suite green</name>
  <files>internal/modelrouting/outcomes.go, internal/modelrouting/outcomes_agg.go, internal/modelrouting/outcomes_test.go</files>
  <read_first>internal/modelrouting/doc.go (package vocabulary), .mise.toml ([tasks.ci])</read_first>
  <action>
    REFACTOR pass: extract any duplicated replay logic, confirm comment discipline (comments state contracts, not narrate code), confirm no LLM-judged-quality or request-content field exists anywhere in the record schema. Then run the standing gate and commit in the TDD commit shape (test commit, feat commit, refactor commit if the refactor was non-trivial).
  </action>
  <verify>
    <automated>mise ci</automated>
  </verify>
  <acceptance_criteria>
    - mise ci green (vet + lint + build + test)
    - git log for this plan shows the RED then GREEN commit sequence
    - golangci-lint reports zero findings on the three new/modified files
  </acceptance_criteria>
  <done>Store + aggregation merged with the phase gate green and TDD commit history intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider errors → outcome store | Error classifications and provider/model names cross from the provider layer into durable on-disk records |
| store file → aggregation/replay | The JSONL file is read back and drives routing-affecting breaker state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01-01 | Information Disclosure | DispatchOutcome schema | high | mitigate | Schema carries mechanical signals only — no request/response content, no prompts, no LLM-judged quality (D-04 hard boundary); TestOutcomeStoreAppendReadRoundtrip pins the exact field set, so any content-bearing field fails the suite |
| T-24-01-02 | Denial of Service | OutcomeStore.Append on the turn path | medium | mitigate | Append errors are returned, never panicked; plan 24-02's recording sites treat store failure as loud-stderr + continue (graceful degradation house pattern) |
| T-24-01-03 | Tampering | store JSONL → ReplayBreakers | medium | mitigate | Owner-only perms 0600/0750 + `.ass-guard/` self-gitignore; replay routes through the existing CircuitBreaker whose open state has a bounded cooldown — a tampered store can degrade routing but never exfiltrate or execute; tolerant read drops malformed lines |
| T-24-01-04 | Repudiation | records lacking provenance | low | accept | Records carry at/provider/model/origin timestamps by design; the store is an ops signal, not an audit log — the audit family (internal/audit) remains the integrity surface |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/modelrouting/` green including all TestOutcome*/TestFirstAllowed tests
- `mise ci` green
- No new external dependency in go.mod (stdlib + in-tree only, per RESEARCH Standard Stack)
</verification>

<success_criteria>
The store appends/reads tolerantly under concurrency and interruption; seeded synthetic outcomes provably change breaker and cost-degrade state through the existing seams (D-07's deterministic half); structural errors never open breakers; the record schema is mechanically-scoped (D-04).
</success_criteria>

<!-- Artifacts this phase produces (plan 24-01): types DispatchOutcome, OutcomeStore, AggregateStats/window type, functions NewOutcomeStore, OutcomeStore.Append, ReadOutcomes, Aggregate, ReplayBreakers, ReplayCostTracker, FirstAllowed; exported type ProviderModelKey (renamed from providerModelKey); file path convention .ass-guard/routing/outcomes.jsonl; no commands, no config keys, no workflow files. -->

<output>
Create `.planning/phases/24-documentation-ops-tails/24-01-SUMMARY.md` when done
</output>
