---
phase: 24-documentation-ops-tails
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/lsp-setup.md
  - README.md
autonomous: false
requirements: [DOC-01]
estimate:
  tokens: 55000
  raw_tokens: 32000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "[probe DOC-01 unclassified: review manually → authored] docs/lsp-setup.md presents the verified THREE-LEG Zed surface with an honest status table: (1) Zed's native built-in diagnostics tool = Zed's own Agent Panel, not exposed to external ACP agents; (2) Zed context_servers settings key forwards configured MCP servers to external agents per Zed docs, extension-installed servers NOT forwarded (zed#52449, status re-checked at writing time), and ass-guard currently discards the forwarded set (internal/acp/handlers.go:253-261); (3) ass-guard's project .mcp.json (camelCase mcpServers, internal/mcp/host.go:75-89) = the leg that works today and THE dry-run leg"
    - "Following the doc's ass-guard-side steps top-to-bottom (the D-03 dry-run executed by the doc's author during this phase) produces working LSP-symbol tools inside an ass-guard session — tool ids visible in the session catalog under the mcp__<server>__<tool> naming; evidence (commands + observed tool list) recorded in the plan summary"
    - "The guide is linked from the README docs index in the existing mid-sentence dot-separated style, positioned alongside docs/install.md (D-02), and is its own file in docs/ (not a section inside install.md)"
    - "The doc's scope statement says explicitly that agent-side LSP support is OUT of scope (the 2026-08-25 operator decision), per D-01"
  artifacts:
    - docs/lsp-setup.md
    - README.md (docs index extended)
  key_links:
    - "docs/lsp-setup.md ↔ README.md docs index (relative link)"
    - "docs/lsp-setup.md dry-run leg ↔ ass-guard .mcp.json loading (internal/mcp/host.go + runtime spawnMCP)"
    - "docs/lsp-setup.md Zed legs ↔ zed.dev/docs/ai/mcp + /ai/tools + /ai/external-agents (cited in-doc)"
  prohibitions:
    - requirement_id: DOC-01
      category: transparency
      statement: "MUST NOT present Zed-forwarded context servers as reaching ass-guard sessions — the doc states ass-guard parses but ignores the ACP-forwarded mcpServers field today, and any change to that is agent-side work that is explicitly OUT of scope"
    - requirement_id: DOC-01
      category: transparency
      statement: "MUST NOT promise or imply agent-side LSP implementation ships with this guide — the 2026-08-25 operator decision (documentation only) is stated in the doc itself per D-01"
---

<objective>
DOC-01: document LSP support as an IDE-side MCP configuration guide (D-01/D-02) with a followable dry-run as its success criterion (D-03) — documentation only, zero agent-side implementation (operator decision 2026-08-25).

Purpose: a user with an existing `.claude/`-style setup should be able to give ass-guard sessions LSP-symbol tools by following the guide; the doc must be honest about the verified Zed surface (research Pitfall 5/A1) or it will dead-end exactly where the operator's mental model assumes a built-in that does not exist.

Output: `docs/lsp-setup.md`, README docs-index link, recorded dry-run evidence.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-documentation-ops-tails/24-CONTEXT.md
@.planning/phases/24-documentation-ops-tails/24-RESEARCH.md
@.planning/phases/24-documentation-ops-tails/24-PATTERNS.md
@docs/install.md
@README.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Which LSP-capable MCP server anchors the worked example and the D-03 dry-run (research Assumption A1 / Open Question 1)</decision>
  <context>Zed ships no built-in LSP-providing MCP context server (research-verified): Zed's native LSP data reaches only Zed's own Agent Panel via the built-in diagnostics tool, and forwarded context servers are discarded by ass-guard today. D-01 keeps Zed as the worked-example FRAMING; the dry-run leg is ass-guard's own .mcp.json. But D-01 leaves WHICH concrete LSP-capable MCP server the worked example configures unspecified, and the operator explicitly excluded Serena. The doc's status-table structure works under either answer; this choice fixes the doc's concrete config snippets and the dry-run's install surface.</context>
  <options>
    <option id="mcp-language-server">
      <name>isaacphi/mcp-language-server + gopls (research default)</name>
      <pros>Go-based (go install — no npm/pip supply gate); pairs with gopls which already exists on the dev machine; exposes LSP symbol/definition/references tools; hermetic dry-run against this repo's own Go code; fits D-01's "generic pattern secondary" framing as the concrete instance</pros>
      <cons>Third-party single-maintainer project — legitimacy checked at execution (go install path, outside the npm/pip/cargo gate scope but recorded in the summary)</cons>
    </option>
    <option id="operator-named">
      <name>Operator names the server</name>
      <pros>Matches the operator's actual toolchain; no surprise dependency</pros>
      <cons>Blocks until the operator answers; the generic-pattern framing (D-01 secondary) then carries the doc</cons>
    </option>
    <option id="no-concrete-server">
      <name>No named server — doc shows the pattern + status table only</name>
      <pros>No third-party dependency in the doc at all</pros>
      <cons>D-03's dry-run needs a real server to reach "working LSP-symbol tools" — this option cannot satisfy D-03's letter; listed for completeness and expected to be rejected</cons>
    </option>
  </options>
  <resume-signal>Select: mcp-language-server, operator-named (state the server), or no-concrete-server</resume-signal>
</task>

<task type="auto">
  <name>Task 1: write docs/lsp-setup.md around the verified three-leg surface (D-01/D-02)</name>
  <files>docs/lsp-setup.md</files>
  <read_first>docs/install.md (whole file — the structural pattern: verb-phrase title, positioning paragraph with explicit scope, Quick start numbered steps each ending in a verifiable command, config tables, honest-status "known rough edges" prose :81-89, See-also relative links :124-133), 24-RESEARCH.md DOC-01 Deep Dive table (the three-leg surface + citations), internal/mcp/host.go:75-89 (mcpServers field the dry-run configures), internal/acp/handlers.go:253-261 (the parse-but-ignore fact the doc must state), internal/runtime/runtime.go spawnMCP body (how .mcp.json servers spawn), README.md:100-104 (index style)</read_first>
  <action>
    Follow docs/install.md's structure exactly. Content, per D-01: title "Setting up LSP tools for ass-guard sessions" (verb-phrase family); scope paragraph stating (a) documentation-only per the 2026-08-25 operator decision — no agent-side LSP implementation is included or promised, (b) Zed is the worked example IDE, the generic any-LSP-MCP pattern is secondary. Sections: 1. The three surfaces (status table with the three legs, each row: what it is / reaches ass-guard sessions? / what to do) with the honest forwarding caveats — Zed context_servers forwarding semantics per zed.dev/docs/ai/mcp and /ai/external-agents, the zed#52449 extension gap with a re-checked status and link, ass-guard's current discard of forwarded mcpServers stated as today's behavior with the code reference; 2. Zed-side setup (context_servers local command/args/env shape and remote url/headers shape per zed.dev/docs/ai/mcp, the Settings → AI → MCP Servers GUI path, the mcp:<server>:<tool_name> permission-rule format, and a note that header-based credentials belong in Zed settings never in shared project files); 3. ass-guard-side setup (project .mcp.json camelCase mcpServers with the chosen worked-example server from the decision checkpoint, spawn behavior, and how the tools appear in a session catalog as mcp__<server>__<tool>); 4. Dry run (the numbered, command-verbatim walk the author executes in Task 2); 5. Known limitations (forwarding not consumed; extension servers not forwarded upstream; native diagnostics tool is Zed-Panel-only); See also (relative links: docs/install.md, docs/recapture-runbook.md, zed docs URLs). Never cite a Zed settings key other than context_servers; never promise LSP tools appearing from Zed forwarding.
  </action>
  <verify>
    <automated>grep -c 'context_servers' docs/lsp-setup.md | grep -qx '0' && echo MISSING-ZED-KEY && exit 1; grep -q 'mcp.json' docs/lsp-setup.md && grep -qi 'out of scope\|explicitly out' docs/lsp-setup.md && grep -q '52449' docs/lsp-setup.md && grep -q 'diagnostics' docs/lsp-setup.md && grep -q 'mcpServers' docs/lsp-setup.md && echo DOC-STRUCTURE-OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/lsp-setup.md exists with all five sections and the three-leg status table
    - The doc contains the string context_servers, mcpServers, mcp.json, diagnostics, 52449, and an explicit out-of-scope statement for agent-side LSP
    - The doc contains no claim that ass-guard consumes ACP-forwarded mcpServers (grep for 'consumes the forwarded' or equivalent affirmative phrasing returns nothing)
    - Every external claim carries a citation (zed.dev URL or in-repo file reference)
  </acceptance_criteria>
  <done>The guide is structurally complete, honest about the verified surface, and citable — ready for the dry-run pass.</done>
</task>

<task type="auto">
  <name>Task 2: README index link + execute the D-03 dry-run end-to-end</name>
  <files>README.md, docs/lsp-setup.md</files>
  <read_first>README.md:96-106 (the Docs: dot-separated index — extend, never restructure), docs/lsp-setup.md Task 1 output (the dry-run section being executed), internal/mcp/host.go (config the dry-run exercises)</read_first>
  <action>
    Add the index entry in the exact mid-sentence style: lsp setup pointing at docs/lsp-setup.md, placed adjacent to the install link (D-02). Then EXECUTE the doc's dry-run top-to-bottom as its author (D-03 — review-only accuracy does not satisfy): per the decision checkpoint's chosen server, install it (go install for the research default; record any install command + legitimacy note in the summary), write the doc's exact .mcp.json into a scratch project dir, build ass-guard, run a real ass-guard session against that project, and confirm LSP-symbol tools appear in the session catalog under the mcp__<server>__<tool> naming — using the scripted newline-delimited JSON-RPC client discipline from internal/acpserve/simulator_e2e_test.go against the REAL binary for the catalog check, plus the doc's manual steps followed verbatim. Capture the actual commands and the observed tool listing as dry-run evidence. Wherever the doc's steps diverge from reality, FIX THE DOC (the dry-run exists to prove followability) and re-run. Finally re-check zed#52449's current status and update the doc's caveat line if it changed.
  </action>
  <verify>
    <automated>grep -q 'lsp setup' README.md && grep -q 'docs/lsp-setup.md' README.md && test -f docs/lsp-setup.md && echo LINK-OK; go build -o /tmp/ass-guard-dryrun ./cmd/ass-guard/ && test -x /tmp/ass-guard-dryrun && echo BUILD-OK</automated>
  </verify>
  <acceptance_criteria>
    - README docs index contains the lsp setup link adjacent to install (D-02)
    - Dry-run evidence exists in the plan summary: install command used, the scratch .mcp.json content, and the observed mcp__-prefixed LSP tool list from a real session
    - Every doc fix discovered during the dry-run is applied and the affected step re-executed
    - The zed#52449 status line in the doc reflects its status as of execution date
  </acceptance_criteria>
  <done>D-03 satisfied by execution: the author followed the guide top-to-bottom and reached working LSP-symbol tools in an ass-guard session, with evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc reader → IDE/config surfaces | The guide instructs users to place configuration (potentially credential-bearing headers) into Zed settings and .mcp.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03-01 | Information Disclosure | credential guidance in docs/lsp-setup.md | medium | mitigate | The doc explicitly instructs that remote context-server auth uses Zed's settings/OAuth support and that bearer headers never belong in shared project files; no worked example embeds a real token |
| T-24-03-02 | Tampering | dry-run supply surface | medium | mitigate | The chosen server's install command is recorded in the summary with its upstream repo; go install from a pinned module path (not curlpipe); no npm/pip/cargo gate triggered |
| T-24-03-03 | Repudiation | doc drift from Zed's moving surface | low | accept | Every external claim carries a citation and an as-of re-check note (zed#52449); the doc's honest-status section is the designed drift absorber |
</threat_model>

<verification>
- Both automated checks above green
- D-03 dry-run evidence present in the plan summary (commands + observed LSP tool listing)
- README index renders the new link in the existing style (no list restructure)
</verification>

<success_criteria>
A user can follow docs/lsp-setup.md and reach working LSP-symbol tools in an ass-guard session via the .mcp.json leg; the doc honestly bounds what Zed forwarding does and does not do today; the agent-side-OUT scope is stated in the doc itself (D-01/D-02/D-03 all satisfied).
</success_criteria>

<!-- Artifacts this phase produces (plan 24-03): docs/lsp-setup.md; README.md docs-index entry; dry-run evidence recorded in 24-03-SUMMARY.md; no types, functions, commands, config keys, or workflow files (documentation-only per operator decision 2026-08-25). -->

<output>
Create `.planning/phases/24-documentation-ops-tails/24-03-SUMMARY.md` when done
</output>
