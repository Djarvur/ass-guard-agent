---
phase: 13-openspec-workflow-completion
plan: 04
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - internal/evalsuite/scenarios/opsx-new.json
  - internal/evalsuite/scenarios/opsx-continue.json
  - internal/evalsuite/scenarios/opsx-ff.json
  - internal/evalsuite/scenarios/opsx-verify.json
  - internal/evalsuite/scenarios/opsx-bulk-archive.json
  - internal/evalsuite/scenarios/opsx-onboard.json
  - internal/evalsuite/suite.go
  - internal/evalsuite/suite_test.go
autonomous: true
requirements: [OS-03]

estimate:
  tokens: 52000
  raw_tokens: 52000     # factor 1 — estimate-calibration has 0 samples in this install
  tasks: 2
  confidence: low       # derived from sample_count=0

must_haves:
  truths:
    - "Six per-command eval suites exist — one per expanded-matrix command (new, continue, ff, verify, bulk-archive, onboard), each carrying its happy AND fixable case (D-04) — so a gate failure names the failing command immediately (exact failure attribution)"
    - "Every suite runs pass@k=1 against the real openspec binary in a fresh scratch with the expanded profile enabled (13-01's guard wired into the scenario bootstrap path) — the suites certify the SAME behavior the matrix legs proved, on the SAME re-run gate Phase 12 locked (ASSGUARD_EVAL_GATE=1, change-class triggered)"
    - "Adding these suites required adding scenario files, not harness surgery — where a matrix-specific assertion was needed it landed as a NAMED assertion key resolved by the harness (free-form assertions stay forbidden), each new key unit-tested offline"
    - "The suites are wired into the existing gate surface: LoadScenarios picks all six up, the change-class detector's classes are unchanged (turn-behavior class already covers internal/engine + the seeded config), mise ci gains NO new dependency"
  artifacts:
    - path: "internal/evalsuite/scenarios/opsx-<command>.json (×6)"
      provides: "the per-command suite definitions: stage prompts (subject on first invocation), expected stage progressions, the named assertion keys (zero-continue, provenance, real-artifact, fixable-recovery), the happy + fixable cases per D-04"
    - path: "internal/evalsuite/suite.go (targeted additions only)"
      provides: "any matrix-specific named assertion keys + the expanded-profile bootstrap option on the scenario path (consuming internal/evalharness's guard) — schema-compatible additions, never harness surgery"
  key_links:
    - from: "internal/evalsuite/scenarios/opsx-*.json"
      to: "internal/evalharness (GuardOpenSpecGlobalConfig + ExpandedMatrixProfileJSON)"
      via: "the scenario bootstrap path applies the guard before openspec init — the same primitive the 13-01 matrix legs use (ONE bootstrap truth for E2E and eval)"
      pattern: "ExpandedMatrixProfileJSON|GuardOpenSpecGlobalConfig"
    - from: "the suites"
      to: "the Phase-12 re-run gate (mise eval-gate, ASSGUARD_EVAL_GATE=1)"
      via: "LoadScenarios(dir) discovers all six files; the gate runs them at k=1; the result artifact carries per-scenario outcomes under .ass-guard/eval/ — failure attribution is the scenario id"
      pattern: "opsx-(new|continue|ff|verify|bulk-archive|onboard)"
---

# Plan 13-04 — Per-command eval suites extending the Phase-12 net (OS-03, D-04, D-06 at the gate)

## Phase Goal

**As a** developer practicing SDD with OpenSpec, **I want** the toolkit's full command matrix covered by
the behavioral-eval regression net, **so that** future profile / model / turn-behavior changes cannot
silently break the expanded workflows the flagship loop's protection already covers.

**This plan's slice:** OS-03. Phase 12's 12-08 landed the net (harness + runner + flagship scenario + the
change-class gate); this plan extends it across the expanded matrix with six per-command suites (D-04) —
happy + fixable cases each, exact failure attribution, running in the SAME re-run gate. The dependency is
real and sequenced: the suites assert zero-continue chained flows, which exist only after 13-02's seeds.

## Ground truth that binds

- 12-08's contract: "adding a scenario is adding a JSON scenario file — no harness change"; scenarios carry
  prompts, expected stage progressions, and NAMED assertion keys resolved by the harness (free-form
  assertions forbidden); pass@k=1 in the gate behind ASSGUARD_EVAL_GATE=1; result artifacts under
  .ass-guard/eval/; the gate triggers on the locked change classes, NOT every commit.
- Disk is location truth, plan-docs are schema truth (PATTERNS): internal/evalsuite + internal/evalharness
  exist at execution time (Phase 12 closed); grep for the actual LoadScenarios/Scenario/bootstrap APIs and
  follow them — the 12-08-PLAN sketch names shapes, the disk names reality.
- The suites need the expanded profile in their scratches: 13-01's guard is the ONE primitive (no second
  bootstrap truth).
- D-06 applies to gated eval runs: one re-run max at zero source/config delta, both runs recorded.

<objective>
Extend the eval net across the expanded matrix: wire the expanded-profile guard into the scenario bootstrap
path, land the six per-command suites (happy + fixable each), prove one green behind the gate at k=1, then
all six + the failure-attribution and gate-integration checks. Output: the OS-03 truth — the expanded
matrix is eval-covered on the same re-run gate as the flagship loop.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-openspec-workflow-completion/13-CONTEXT.md
@.planning/phases/13-openspec-workflow-completion/13-PATTERNS.md
@.planning/phases/12-product-functional-completeness/12-08-PLAN.md
@.planning/phases/12-product-functional-completeness/12-08-SUMMARY.md

<interfaces>
```go
// 12-08's landed surface (grep the disk for the real names; the plan-doc sketch as schema source):
// internal/evalharness/harness.go — BootstrapScratch/RunScenarioPass + 13-01's
//   GuardOpenSpecGlobalConfig + ExpandedMatrixProfileJSON (this plan wires them into the scenario path)
// internal/evalsuite/suite.go — Scenario{ID, Description, Stages, Asserts []string /*named keys*/},
//   LoadScenarios(dir), RunSuite(ctx, scenarios, k) with pass@k scoring + JSON artifacts
//   under .ass-guard/eval/<timestamp>.json; loud skip when env gates unset
// .mise.toml — eval-gate (k=1, ASSGUARD_EVAL_GATE=1 + binary/model gates, 15m timeout), eval-deep (k=3,
//   manual), eval-check-changed (the locked change classes); mise ci depends on NONE of them
// scripts/eval-change-class.sh — exit 3 + matched classes; turn-behavior class already covers
//   internal/engine + the seeded config surface (verify; extend ONLY if the seeded-config path is
//   somehow outside a class — record the check either way)
```
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — the profile-enabled scenario path + the opsx-new suite green once at k=1 behind the gate</name>
  <precondition>13-02 complete (seeds exist — the suites assert chained flows). Gated run needs ASSGUARD_EVAL_GATE=1 + ASSGUARD_OPENSPEC_BIN=1 + ASSGUARD_E2E_LLM=1 + provider creds; absent gates skip LOUD</precondition>
  <files>internal/evalsuite/suite.go, internal/evalsuite/scenarios/opsx-new.json, internal/evalsuite/suite_test.go</files>
  <read_first>
    - internal/evalsuite/suite.go + internal/evalharness/harness.go (the ACTUAL landed APIs — schema per 12-08, names per disk)
    - internal/evalharness/profile_guard_test.go (13-01's guard — the primitive being wired into the scenario path)
    - .planning/phases/12-product-functional-completeness/12-08-SUMMARY.md (the net's landed truth: scenario schema, assertion-key registry, gate flags)
    - cmd/ass-guard/e2e_opsx_matrix_test.go (13-01/13-02's legs — the fixture preconditions + assertion set each suite encodes)
    - .planning/phases/13-openspec-workflow-completion/13-CONTEXT.md (D-04 per-command suites with happy + fixable cases)
  </read_first>
  <action>
    (1) Wire the expanded profile into the scenario bootstrap path: the opsx-* scenarios' scratches get GuardOpenSpecGlobalConfig(t, ExpandedMatrixProfileJSON) before openspec init — as a scenario-schema option (e.g. an openspec_profile field or a suite-level default for opsx-* ids; follow the landed Scenario shape and keep it schema-compatible — a JSON field, not harness surgery). Assert in the bootstrap that the 6 expanded command files exist (the same FAIL-LOUD check the matrix runner uses). (2) opsx-new.json: the first suite — the /opsx:new happy case (stage prompt with the subject typed, the named assertion keys the harness already owns for zero-continue/provenance/artifacts, plus the fixable case per D-04: the already-existing-name trigger from 13-01's probed evidence, asserting fail-then-adapt-then-goal via the named fixable-recovery key). If a needed assertion is not yet a named key (e.g. a new-command-specific artifact check), ADD it as a named key resolved by the harness with an offline unit test — free-form assertions stay forbidden (T-12-08-01 discipline). (3) Prove it: run the suite ONCE green behind ASSGUARD_EVAL_GATE=1 at k=1 with the real binary + model; preserve the result artifact + run log under /tmp/eval-matrix-evidence/ and reference both in the SUMMARY (no stub-only evidence). TDD: the offline schema/option tests commit RED (test(13-04)) before the wiring (feat(13-04)).
  </action>
  <verify>
    <automated>go test ./internal/evalsuite/ -count=1 -run 'TestScenario|TestLoadScenarios' && ASSGUARD_EVAL_GATE=1 ASSGUARD_OPENSPEC_BIN=1 ASSGUARD_E2E_LLM=1 go test ./internal/evalsuite/ -count=1 -run TestEvalSuite -timeout 15m 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The scenario path applies the expanded-profile guard for opsx-* scenarios (grep ExpandedMatrixProfileJSON in internal/evalsuite/ or the bootstrap call chain >= 1) and FAILS LOUD when the 6 command files are absent post-init
    - internal/evalsuite/scenarios/opsx-new.json exists, validates against the landed schema, and carries BOTH the happy and the fixable case (D-04) with named assertion keys only
    - Any new named assertion key has an offline unit test and is resolved by the harness (no free-form assertion strings in any scenario JSON)
    - The gated run is green once at k=1; the result artifact (under .ass-guard/eval/) + run log preserved under /tmp/eval-matrix-evidence/ and referenced in the SUMMARY
    - TDD sequence in git log: test(13-04) precedes feat(13-04)
    - mise run ci green at task close (the offline universe; ci gains NO eval dependency)
  </acceptance_criteria>
  <done>The eval net runs an expanded-matrix command end-to-end: profile-enabled scratch, happy + fixable cases, named-key assertions, green behind the Phase-12 gate at k=1 with preserved evidence.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining five suites + failure attribution + gate integration (D-04 complete)</name>
  <precondition>Task 1's tracer suite green behind the gate</precondition>
  <files>internal/evalsuite/scenarios/opsx-continue.json, internal/evalsuite/scenarios/opsx-ff.json, internal/evalsuite/scenarios/opsx-verify.json, internal/evalsuite/scenarios/opsx-bulk-archive.json, internal/evalsuite/scenarios/opsx-onboard.json, internal/evalsuite/suite_test.go</files>
  <read_first>
    - internal/evalsuite/scenarios/opsx-new.json (Task 1's suite — the form the five follow)
    - cmd/ass-guard/e2e_opsx_matrix_test.go (each command's fixture preconditions + fixable triggers — the probed evidence each suite encodes)
    - internal/openspec/seeded.toml (13-02's chaining rows — the expected stage progressions the suites assert against)
    - .planning/phases/13-openspec-workflow-completion/13-CONTEXT.md (D-04 exact-attribution requirement; D-06 at the gate)
  </read_first>
  <action>
    (1) The five remaining suites, one per command, each with its happy + fixable case: continue/ff encode their mid-flight-change fixtures; verify encodes the D-07 chained fix workflow (its expected stage progression crosses the seeded handoff — the suite asserts the chained stage's provenance key); bulk-archive encodes the multi-change batch + the incomplete-tasks fixable trigger; onboard encodes the fresh-scratch full-tilt happy case + the D-08 idempotent re-run fixable case. Fixture preconditions copy the matrix legs' seeders (via the harness's bootstrap options — no parallel fixture truth). (2) Failure attribution (D-04's exact-attribution requirement): an offline test feeds a deliberately failing opsx-* scenario fixture through RunSuite's scoring and asserts the result artifact + failure output NAME the scenario id (command attribution immediate); plus a check that LoadScenarios over the scenarios dir discovers exactly the flagship + six opsx suites. (3) Gate integration: run the full gated suite set once (flagship + six) at k=1; verify eval-check-changed still exits 3 on a turn-behavior-class diff (the seeded-config path must fall inside an existing class — record the verification; extend the script ONLY if genuinely outside, with its selftest updated); D-06 discipline for any failing leg (single re-run at zero delta, both recorded). (4) Evidence preserved; SUMMARY carries the artifact paths + the attribution proof.
  </action>
  <verify>
    <automated>go test ./internal/evalsuite/ -count=1 && ASSGUARD_EVAL_GATE=1 ASSGUARD_OPENSPEC_BIN=1 ASSGUARD_E2E_LLM=1 go test ./internal/evalsuite/ -count=1 -run TestEvalSuite -timeout 60m 2>&1 | tail -6 && ./scripts/eval-change-class.sh --selftest</automated>
  </verify>
  <acceptance_criteria>
    - Six opsx-*.json suite files exist under internal/evalsuite/scenarios/, each validating and carrying happy + fixable cases with named assertion keys (grep the scenario ids: opsx-new, opsx-continue, opsx-ff, opsx-verify, opsx-bulk-archive, opsx-onboard)
    - The verify suite's expected progression asserts the D-07 chained stage (its provenance assertion key present in opsx-verify.json)
    - The failure-attribution offline test passes: a failing scenario is named by id in the result artifact + output; the discovery test counts the flagship + six opsx suites
    - The full gated set runs green at k=1 (flagship + six) with artifacts preserved under .ass-guard/eval/ + /tmp/eval-matrix-evidence/; any re-run followed D-06 (single, zero-delta, both recorded)
    - eval-check-changed selftest green; the change-class verification (seeded-config path inside an existing class) recorded in the SUMMARY; mise ci dependency set UNCHANGED
    - mise run ci green at task close
  </acceptance_criteria>
  <done>OS-03 holds: the Phase-12 eval suites are extended with six per-command scenario suites for the expanded matrix (pass@k, real binary, scratch projects, expanded profile), running in the same re-run gate with exact failure attribution per command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scenario JSON → runner | Suite definitions drive gated execution (a tampered scenario could weaken coverage) |
| eval runs → scratch + global config + model | Suites mutate the global openspec config (via the guard) and spend real model calls |
| result artifacts → consumers | Artifacts carry transcripts-derived outcomes under .ass-guard/eval/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04-01 | Tampering | scenario weakening (free-form assertions) | high | mitigate | The T-12-08-01 discipline carries over: assertions are NAMED keys resolved by the harness; the schema validation rejects unknown keys; every NEW key added this phase is unit-tested offline — a scenario cannot relax what the harness does not own |
| T-13-04-02 | Tampering | eval-path global-config restore failure | high | mitigate | The scenario bootstrap consumes 13-01's guard (the ONE primitive with its invariant battery); no second restore implementation exists to get wrong |
| T-13-04-03 | DoS | gated eval cost (7 suites × real model) | medium | mitigate | k=1 in the gate; the change-class trigger (not every commit); 15m/60m timeouts; mise ci untouched (D-03/D-04 cost discipline) |
| T-13-04-04 | Repudiation | silent suite skips | medium | mitigate | The runner's loud-skip discipline (unset gates are named in the skip); the gate's exit-3 protocol + result artifacts make "should have run" checkable |
| T-13-04-05 | Information disclosure | result artifacts | low | mitigate | Artifacts under the gitignored .ass-guard/eval/ family; the redaction chokepoint covers referenced transcript content (12-08 disposition carries over) |
</threat_model>

<verification>
- mise run ci green with its dependency set unchanged (no eval task joined ci).
- go test ./internal/evalsuite/ green offline (schema, attribution, discovery, named-key batteries).
- The full gated set (flagship + six opsx suites) green once at k=1 with preserved artifacts; the operator's global openspec config byte-identical after the runs.
</verification>

<success_criteria>
- OS-03 truth holds: the Phase-12 eval suites are extended with per-command scenario suites for the expanded matrix (pass@k=1, real binary, scratch projects), running in the same re-run gate.
- D-04 delivered: six suites, happy + fixable each, exact failure attribution by scenario id.
- The extension honored 12-08's contract: scenario files + (at most) named keys — no harness surgery.
</success_criteria>

## Artifacts this phase produces (plan 13-04)

- internal/evalsuite/scenarios/opsx-{new,continue,ff,verify,bulk-archive,onboard}.json — the six per-command suites (happy + fixable cases, named assertion keys, expanded-profile bootstrap option)
- internal/evalsuite/suite.go — the scenario-path expanded-profile wiring + any new NAMED assertion keys (each with an offline unit test)
- internal/evalsuite/suite_test.go — the failure-attribution test (failing scenario named by id), the discovery test (flagship + six), the new-key batteries
- No .mise.toml changes unless the change-class verification genuinely demands one (recorded either way); no new dependencies

<output>
Create `.planning/phases/13-openspec-workflow-completion/13-04-SUMMARY.md` when done
</output>
