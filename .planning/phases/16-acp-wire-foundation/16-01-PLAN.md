---
phase: 16-acp-wire-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/acp/types.go
  - internal/acp/emitter.go
  - internal/acp/emitter_test.go
  - internal/acp/server.go
  - internal/acp/handlers.go
  - internal/acpserve/acp_serve.go
  - internal/runtime/runtime.go
  - internal/runtime/cron_wiring.go
autonomous: true
requirements: [ACP-03]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A live turn's tool_call / tool_call_update activity reaches the client as v1 session/update frames through ONE TurnEmitter: its drain goroutine is the only producer of notification frames into acp.Writer, giving a single-writer total order (D-02, ROADMAP criterion 2)"
    - "Foreground frames preempt the queue head over queued background frames, background frames stay FIFO among themselves, and nothing is ever dropped under a fake slow writer — proven by a bounded -race stress that runs in every mise ci (D-01, D-02, D-04)"
    - "A producer arriving at an exactly-full lane blocks with ctx awareness rather than dropping or merging frames; once the full state passes the stall threshold (~5s default) the detector logs one structured stderr line and increments a counter — no disconnect, no silent block (D-03)"
    - "Background frames of equal class preserve arrival order (FIFO is stable among equal-priority frames)"
    - "A turn with zero streamed activity emits zero session/update frames and its session/prompt response still arrives — the turn-end barrier returns immediately on an empty queue"
    - "The session/prompt response is written only after all that turn's queued notification frames have been written (updates-before-response, the verified cancel contract)"
    - "A TodoWrite tool call renders as a v1 plan update (full-replacement entries) rather than a tool_call card; the agent_thought_chunk frame method exists in v1 ContentChunk shape even though no provider thinking source exists until Phase 21 (PAR-05)"
  artifacts:
    - path: "internal/acp/emitter.go"
      provides: "TurnEmitter: bounded fg/bg lanes, nested-select single drain, stall detector, turn-end barrier, foreground + background handle constructors"
      contains: "TurnEmitter"
    - path: "internal/acp/types.go"
      provides: "v1 wire frame structs ToolCallFrame, ToolCallUpdateFrame, ToolCallContent, DiffContent, ToolCallLocation, PlanFrame, PlanEntry, ThoughtChunkFrame with verbatim schema-v1 camelCase names"
      contains: "ToolCallFrame"
    - path: "internal/runtime/runtime.go"
      provides: "ToolCall/ToolCallUpdate bus subscriptions in Run's forwarder and the session-lifetime forwarder, forwarded through the ActivityEmitter interface"
      contains: "ActivityEmitter"
  key_links:
    - from: "internal/acpserve/acp_serve.go"
      to: "internal/acp/emitter.go"
      via: "TurnEmitter constructed at the srv/emitFor junction (acp_serve.go:200-215) so runner.SetEmitter receives an emitter-backed handle"
      pattern: "SetEmitter"
    - from: "internal/runtime/runtime.go"
      to: "internal/acp/emitter.go"
      via: "forwarders type-assert emit to ActivityEmitter and call ToolCall/ToolCallUpdate; the mute discipline of startSessionForwarder is unchanged"
      pattern: "emit\\.\\(ActivityEmitter\\)|ok := emit\\("
  prohibitions:
    - requirement_id: ACP-03
      category: transparency
      statement: "Client-visible session/update frames MUST correspond to real turn activity — the emitter must never synthesize or fabricate tool_call / plan / thought frames (e.g. fake progress cards, cached stale plan lists) that do not mirror an actual bus event or TodoWrite state"

---

<objective>
Tracer slice for the phase: ONE frame path end-to-end — a bus ToolCall event published during a live turn flows through the runtime forwarder into the TurnEmitter's foreground lane, out the single drain goroutine into the existing Writer, and reaches stdout as a v1 session/update tool_call frame — then the expansion tasks prove the backpressure/priority invariants (D-01..D-04) and complete the ACP-03 frame vocabulary (plan-from-TodoWrite, thought-chunk method, update shapes).

Purpose: ACP-03's live-rendering and ROADMAP criterion 2's raced ordering invariant both stand on this spine; Phases 17-23 ride it. Proving the architecture on the best early context catches an ordering dead-end after one commit instead of ten.
Output: internal/acp/emitter.go (+test), extended wire types in internal/acp/types.go, runtime forwarding, acpserve wiring.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-CONTEXT.md
@.planning/phases/16-acp-wire-foundation/16-RESEARCH.md
@.planning/phases/16-acp-wire-foundation/16-PATTERNS.md
@internal/acp/framer.go
@internal/acp/server.go
@internal/acp/handlers.go
@internal/acp/types.go
@internal/acpserve/acp_serve.go
@internal/runtime/runtime.go
@internal/event/events.go
@internal/coreexec/todo.go
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/acp/emitter.go` — `TurnEmitter` type; foreground/background handle constructors; `ActivityEmitter` interface (ChunkEmitter + ToolCall/ToolCallUpdate/PlanUpdate/ThoughtChunk methods); stall detector + `StallThreshold` knob; `Barrier()` turn-end flush.
- `internal/acp/types.go` — `ToolCallFrame`, `ToolCallUpdateFrame`, `ToolCallContent`, `DiffContent`, `ToolCallLocation`, `PlanFrame`, `PlanEntry`, `ThoughtChunkFrame` (v1 verbatim names).
- `internal/runtime/runtime.go` + `internal/runtime/cron_wiring.go` — ToolCall/ToolCallUpdate subscriptions in the per-Run forwarder and `startSessionForwarder`.
- `internal/acpserve/acp_serve.go` — TurnEmitter construction between `acp.NewServer` and `runner.SetEmitter`.
- `internal/acp/emitter_test.go` — TestTurnEmitterPriority / TestTurnEmitterStall / TestTurnEmitterBarrier / TestPlanFrameFromTodoWrite / TestThoughtChunkFrame.
</artifacts_this_phase_produce>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — one tool_call frame end-to-end through the ordered TurnEmitter</name>
  <files>internal/acp/types.go, internal/acp/emitter.go, internal/acp/emitter_test.go, internal/acp/server.go, internal/acpserve/acp_serve.go, internal/runtime/runtime.go</files>
  <behavior>
    - RED first: an end-to-end test in internal/runtime constructs a real acp.Server over pipes plus a minimal NewRunner fixture, publishes event.ToolCall and event.AgentMessageChunk on the bus during a Run turn, and asserts stdout carries a session/update frame with sessionUpdate "tool_call" (toolCallId, title from the tool name, content) AND the agent_message_chunk frames, in emission order — this test fails before the emitter exists.
    - Every session/update notification frame passes through the TurnEmitter drain goroutine; the test asserts the emitter's written-frame count equals the notification frames observed on stdout.
    - Server.Emitter(sessionID) returns a foreground-class handle; a background-class constructor exists and routes to the bg lane; both are exercised in the lane test.
  </behavior>
  <read_first>internal/acp/frimer.go is framer.go — read framer.go (Writer: bounded channel, drain, Close discipline), internal/acp/server.go (Server struct, Emitter at :143-145, adapter at :308-329, Serve dispatch at :180-196), internal/acp/handlers.go (handleSessionPrompt at :102-147, newSessionID at :227-239), internal/acp/types.go (Message at :33-40), internal/acpserve/acp_serve.go (Run junction at :200-215), internal/runtime/runtime.go (Run forwarder at :473-560, emitFor at :182), internal/runtime/cron_wiring.go (startSessionForwarder at :229-257), internal/event/events.go (ToolCall/ToolCallUpdate structs), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (Pattern 1 nested select, Pitfalls 2/3/8)</read_first>
  <action>
RED: write the failing end-to-end test described in behavior (reuse the NewRunner fixture pattern from internal/runtime/runner_battery_test.go or integration_test.go; SetEmitter(srv.Emitter) with a real Server built over an io.Pipe/bytes.Buffer pair, Close the Writer before reading stdout). Commit it failing.
GREEN: create internal/acp/emitter.go per RESEARCH Pattern 1 — TurnEmitter with two bounded buffered channels (lane capacities are planner-discretion: pick fg 128 / bg 256; invariant tests must be capacity-independent), one drain goroutine running the nested select (foreground-first always; both lanes with equal chance only when fg is empty; ctx.Done exit), each drained frame written via the existing Writer.Write so at most one notification frame is in flight (Pitfall 2 guard). Producers block on their lane channel with select on their ctx (D-01/D-03 — copy framer.go:114-121's bounded-channel comment style and add the nothing-ever-dropped invariant to the doc comment). Copy the Close discipline reasoning from framer.go:133-149 — the emitter stops before server close (Pitfall 8). Provide a foreground handle and a background handle over the same emitter (class selected per handle — the handle's methods enqueue into its lane). Retire the adapter's direct notification writes: the adapter (server.go:308-329) enqueues through the emitter lanes instead of calling out.Write, and handleSessionPrompt's local adapter construction swaps to the emitter foreground handle. Extend types.go with ToolCallFrame/ToolCallUpdateFrame/ToolCallContent/DiffContent/ToolCallLocation using RESEARCH's verbatim v1 field names, each with the //nolint:tagliatelle marker (repo convention). Define the ActivityEmitter interface in emitter.go: ChunkEmitter plus ToolCall(ToolCallFrame), ToolCallUpdate(ToolCallUpdateFrame), PlanUpdate([]PlanEntry), ThoughtChunk(messageID string, content ContentBlock) — do NOT widen the existing ChunkEmitter interface (test fakes across the repo must keep compiling; RESEARCH Open Question 1 resolution recorded in this plan). In internal/runtime: subscribe "ToolCall" (event.BufToolCall) and "ToolCallUpdate" (event.BufToolCallUpdate) alongside AgentMessageChunk in Run's forwarder and in startSessionForwarder, forwarding via a type assertion of emit to ActivityEmitter (mute discipline of the session forwarder unchanged — client-turn muting keeps working exactly as today). In internal/acpserve.Run: construct the TurnEmitter between srv := acp.NewServer(...) (acp_serve.go:200) and runner.SetEmitter (acp_serve.go:214) so both the runner's emitFor and any background emitters route through the lanes. Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/runtime/ -run TestTurnEmitterEndToEnd -count=1 && go test ./internal/acp/ -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
- `go test -race ./internal/runtime/ -run TestTurnEmitterEndToEnd -count=1` passes and asserts both tool_call and agent_message_chunk stdout frames in emission order.
- internal/acp/emitter.go declares TurnEmitter with two lane channels, one drain goroutine started at construction, and ctx-aware producer blocking.
- internal/acp/types.go contains the struct ToolCallFrame with json tag toolCallId and the //nolint:tagliatelle marker.
- The emitter's drain is the sole producer of session/update frames into the Writer (the tracer test asserts written-count equals observed notification frames).
- The existing ChunkEmitter interface in server.go still declares exactly AgentMessageChunk (no widening); the new ActivityEmitter interface embeds it.
- `go test -race ./internal/acp/ ./internal/acpserve/ ./internal/runtime/ -count=1` is green (no regression in existing serve/runner tests).
  </acceptance_criteria>
  <done>The thinnest frame path is live end-to-end and committed: bus ToolCall event, runtime forwarder, emitter foreground lane, single drain, Writer, stdout — production-quality and carrying the phase's ordering invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backpressure, priority preemption, stall detector, turn-end barrier</name>
  <files>internal/acp/emitter.go, internal/acp/emitter_test.go, internal/acp/handlers.go</files>
  <behavior>
    - With a fake slow writer (a controllable blocker io.Writer under newWriter — Writer accepts any io.Writer) and the bg lane pre-filled beyond the fg lane: a late foreground frame is written BEFORE any of the queued background frames (preemption at the head).
    - Background frames emitted concurrently are written in exact FIFO arrival order (equal-class stability), and total frames written equal total frames enqueued (nothing dropped) under -race.
    - A lane held full past the stall threshold emits exactly one structured stderr log line naming the lane and increments the stall counter; when the drain resumes, producers unblock and no disconnect/close occurs. The threshold is a settable field so tests run in milliseconds (D-03's ~5s stays the production default).
    - A producer blocked on a full lane with a cancelled ctx returns promptly (no goroutine leak; -race shutdown clean).
    - handleSessionPrompt calls the emitter barrier before returning: the session/prompt response frame appears on stdout strictly AFTER every notification frame queued during the turn (updates-before-response, verified by a test that stalls the writer mid-turn, queues frames, then releases and asserts ordering).
    - An empty-queue barrier returns immediately without blocking the response.
  </behavior>
  <read_first>internal/acp/emitter.go (as built in Task 1), internal/acp/framer.go (drain + Close), internal/acp/handlers.go (handleSessionPrompt :102-147), internal/acp/server_test.go (existing fake-writer test patterns), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (Pitfalls 2, 3, 4, 8; Pattern 1 stall-watermark sketch)</read_first>
  <action>
Add the stall detector per RESEARCH Pattern 1: a per-lane "full since" watermark sampled by a time.Ticker in the drain goroutine; when a lane has been continuously full longer than the configurable StallThreshold (default 5s), log one structured line to the emitter's stderr logger (field names per the repo's audit/tracer conventions — planner discretion per CONTEXT) and increment an atomic counter; never close, never drop. Implement the barrier: a mutex-guarded enqueue/written counter pair on TurnEmitter; Barrier() waits until written reaches the enqueue count observed at call time (the drain increments written only after Writer.Write returns, so at most one in-flight frame precedes the response — order-safe by construction). Call the barrier in handleSessionPrompt after turnRunner.Run returns and before the handler returns (RESEARCH Open Question 2 resolution: barrier at the handler, not a response rerouting — responses keep using writeResult directly, which stays legal because only notification-vs-response order at turn end is spec-constrained). Write the -race stress test with the fake slow writer per behavior; keep it bounded to seconds so mise ci stays fast (D-04). Do not hold any lock across a lane enqueue (Pitfall 3) and keep the registry-facing surfaces free of writer-mutex coupling.
  </action>
  <verify>
    <automated>go test -race ./internal/acp/ -run 'TestTurnEmitterPriority|TestTurnEmitterStall|TestTurnEmitterBarrier|TestTurnEmitterEmptyTurn' -count=1 && mise ci</automated>
  </verify>
  <acceptance_criteria>
- TestTurnEmitterPriority passes under -race with a fake slow writer and asserts fg-before-queued-bg plus zero dropped frames plus bg FIFO order.
- TestTurnEmitterStall asserts one log line + counter increment past threshold and no close of the writer.
- TestTurnEmitterBarrier asserts the prompt response lands after all queued turn frames under a stalled-then-released writer; the empty-turn case asserts a prompt response with zero notification frames.
- handleSessionPrompt contains the barrier call after the Run call returns.
- `mise ci` is green end to end (the stress now rides the standing gate via go test -race ./...).
  </acceptance_criteria>
  <done>D-01 through D-04 are implemented and enforced by the standing CI gate: bounded lanes, block-never-drop, foreground preemption, loud stall detection, and updates-before-response at turn end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frame-surface completion — plan from TodoWrite, thought-chunk method, update shapes</name>
  <files>internal/acp/types.go, internal/acp/emitter.go, internal/acp/emitter_test.go, internal/runtime/runtime.go</files>
  <behavior>
    - A ToolCall bus event whose Name is TodoWrite and whose Input carries the captured {todos:[...]} shape (coreexec/todo.go fixtures) produces a session/update frame with sessionUpdate "plan" whose entries carry content/priority/status mapped from the todos, full-replacement semantics (v1: the client replaces the entire plan); the TodoWrite call does not also emit a tool_call card (presentation rule).
    - A ToolCall event for any other tool name still emits the tool_call frame exactly as in Task 1.
    - ThoughtChunk(messageID, content) emits a session/update frame with sessionUpdate "agent_thought_chunk" carrying messageId + a ContentBlock (v1 ContentChunk shape) — unit-proven only; no live provider source exists until Phase 21 (PAR-05).
    - ToolCallUpdate frames carry kind, status, content (including the diff variant with path/oldText/newText), and locations (absolute path + optional line) under verbatim v1 names; v1 status values only (pending/in_progress/completed/failed — no cancelled, Pitfall 7).
  </behavior>
  <read_first>internal/coreexec/todo.go (TodoWrite input/output shapes, captured status enum at :64, marshalTodos at :88), internal/acp/emitter.go, internal/acp/types.go, internal/runtime/runtime.go (forwarder from Task 1), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (Code Examples section — verbatim frame structs; State of the Art v1/v2 drift table)</read_first>
  <action>
Add PlanFrame/PlanEntry/ThoughtChunkFrame structs to types.go with verbatim v1 names and tagliatelle markers (PlanFrame.Entries is required and full-replacement per the schema annotation). Implement PlanUpdate and ThoughtChunk on the emitter handle. Place the TodoWrite-to-plan presentation rule INSIDE internal/acp (the adapter/emitter layer inspects the ToolCall frame's tool name and input; the runtime stays ACP-word-free per 15-D-20): when the forwarded tool name equals the TodoWrite tool name (read the exact spelling from coreexec/register.go), derive entries from the input's todos array (content/status/priority keys per coreexec/todo.go's captured fixture shapes) and emit PlanUpdate instead of the tool_call frame. Read the exact JSON key spellings from coreexec/todo.go rather than assuming. Extend the runtime forwarder to pass tool-call updates through ToolCallUpdate frames carrying the v1 shapes from RESEARCH's Code Examples. Pin every wire name against schema v1 (use `id`/`plan`/`tool_call` — never the v2 `configId`/`plan_update` spellings, Pitfall 7).
  </action>
  <verify>
    <automated>go test ./internal/acp/ -run 'TestPlanFrameFromTodoWrite|TestThoughtChunkFrame|TestToolCallUpdateFrame' -count=1 && go test -race ./internal/acp/ ./internal/runtime/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- TestPlanFrameFromTodoWrite feeds a TodoWrite-shaped ToolCall event and asserts a plan frame with mapped entries and no tool_call frame for that call.
- TestThoughtChunkFrame asserts a sessionUpdate agent_thought_chunk frame with messageId and a content block.
- TestToolCallUpdateFrame asserts diff + locations + kind/status fields under camelCase v1 names.
- No v2 vocabulary appears in the new frame structs (grep -c 'plan_update\|configId' internal/acp/types.go returns 0 — the strings appear nowhere in that file).
- The TodoWrite mapping lives in internal/acp, not internal/runtime (runtime.go gains no plan/todo vocabulary).
  </acceptance_criteria>
  <done>The ACP-03 frame vocabulary is complete on the emitter — tool cards, live plan panel mirroring TodoWrite, thought-chunk shape — with the v1 names pinned against the canonical schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→editor stdout | session/update frames cross to the untrusted-slow client; ordering and bounded memory are the contract |
| bus→emitter | concurrent producers (turn, subagents, engine) share the lanes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | DoS | chatty emitters vs slow client | high | mitigate | Bounded lanes block producers (D-01); the existing 256-slot Writer stays the final boundary; stall detector logs loudly, never auto-disconnects (D-03) |
| T-16-02 | DoS | deadlock via lock-held enqueue | medium | mitigate | No lock spans an enqueue; -race stress with fake slow writer exercises exactly this (Pitfall 3) |
| T-16-03 | Tampering | priority inversion through the shared Writer buffer | medium | mitigate | Emitter drain is the sole notification producer into Writer (≤1 in flight), pinned by the tracer test's written-count assertion (Pitfall 2) |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs this phase (RESEARCH Standard Stack: stdlib only); legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/acp/ ./internal/runtime/ ./internal/acpserve/ -count=1` green.
- `mise ci` green (the D-04 stress rides the standing gate).
- The tracer test proves bus-to-stdout ordering through one emitter with no direct-Writer notification writes.
</verification>

<success_criteria>
The phase's riskiest primitive exists and is proven under concurrency: one ordered TurnEmitter owns every client notification frame, foreground preempts, background is FIFO, nothing drops, stalls are loud, and the turn's epilogue cannot overtake its frames. Later plans (16-03 registry, 16-05 config) attach to this spine without touching the ordering invariant.
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-01-SUMMARY.md` when done
</output>
