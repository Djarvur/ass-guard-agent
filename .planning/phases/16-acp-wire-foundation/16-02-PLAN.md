---
phase: 16-acp-wire-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/transcript.go
  - internal/session/manager.go
  - internal/session/projector.go
  - internal/session/transcript_newkinds_test.go
autonomous: true
requirements: [ACP-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Transcript lines of type raw_thinking / local_command / compaction append through dedicated Append* methods; the raw-thinking path never calls the Redactor (zero-call asserted with a counting fake) and its json.RawMessage payload round-trips byte-identical (D-20, D-23)"
    - "local_command lines record command key + verbatim args + resolution-source chain + expansion outcome (D-22)"
    - "compaction lines record boundary id + timestamp + token-usage snapshot + pre/post transcript pointers (D-21)"
    - "raw_thinking lines carry provider attribution (model) so Phase 21 (PAR-05) can render provenance (CONTEXT discretion item 5, decided YES)"
    - "Replay readers tolerate the three new kinds AND unknown future kinds/fields; the Projector's projected window is unchanged by their presence — compaction activates as a reset-point class only in Phase 19 (additive-only guarantee, D-20)"
  artifacts:
    - path: "internal/session/transcript.go"
      provides: "TypeRawThinking / TypeLocalCommand / TypeCompaction constants in the kind block + new Line fields (boundary id, token-usage snapshot, pre/post pointers, resolution-source chain, provider attribution)"
      contains: "TypeRawThinking"
    - path: "internal/session/manager.go"
      provides: "appendLineUnredacted (near-copy of appendLine minus the redact block) + AppendRawThinking / AppendLocalCommand / AppendCompaction one-liner appenders"
      contains: "appendLineUnredacted"
    - path: "internal/session/transcript_newkinds_test.go"
      provides: "TestTranscriptNewKinds / TestProjectorToleratesNewKinds — byte-identity, redactor-zero-calls, replay tolerance"
      contains: "TestTranscriptNewKinds"
  key_links:
    - from: "internal/session/manager.go"
      to: "internal/session/transcript.go"
      via: "the three Append* one-liners set the new kind constants and Line fields, appending via the type-appropriate path (raw_thinking unredacted, others redacted)"
      pattern: "AppendRawThinking|AppendLocalCommand|AppendCompaction"
  prohibitions:
    - requirement_id: ACP-03
      category: privacy
      statement: "The unredacted append path MUST never be extended beyond raw_thinking lines — no other transcript kind (tool output, command output, user content) may bypass the Redactor, or credential-bearing payloads would reach the transcript on disk"

---

<objective>
Lock the extended transcript schema as three additive line kinds with an explicitly unredacted thinking path: raw-thinking passthrough (json.RawMessage, provider attribution), local-command provenance lines, and compaction boundary markers. Phases 19-21 (PAR-01, CMDS-02, PAR-05) consume these kinds; this plan lands the schema now so their wire work needs no on-disk-format change, and proves replay tolerance so old binaries never choke on new lines.

Purpose: D-20's additive-only weak schema is the phase's one-way door — locking it now with tests makes every later consumer a reader-only change. The redaction exclusion (D-23) must be a distinct code path, provable by counting Redactor calls.
Output: three new kinds + appenders in internal/session, tolerance tests.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-CONTEXT.md
@.planning/phases/16-acp-wire-foundation/16-PATTERNS.md
@internal/session/transcript.go
@internal/session/manager.go
@internal/session/projector.go
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/session/transcript.go` — kind constants `TypeRawThinking`, `TypeLocalCommand`, `TypeCompaction`; new `Line` fields: boundary id, token-usage snapshot, pre/post transcript pointers, resolution-source chain, expansion outcome, provider model attribution.
- `internal/session/manager.go` — `appendLineUnredacted`, `AppendRawThinking(turnID, model string, payload json.RawMessage)`, `AppendLocalCommand(...)`, `AppendCompaction(...)`.
- `internal/session/transcript_newkinds_test.go` — byte-identity round-trip, redactor-zero-calls, replay tolerance, projector tolerance.
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three additive kinds + unredacted append path</name>
  <files>internal/session/transcript.go, internal/session/manager.go, internal/session/transcript_newkinds_test.go</files>
  <behavior>
    - AppendRawThinking writes a line with type "raw_thinking", a turn id, a timestamp, the provider model string, and the payload as json.RawMessage whose bytes survive a marshal→readTranscriptFile round-trip exactly (including nested oddities: whitespace inside strings, unicode escapes left as-is).
    - AppendLocalCommand writes type "local_command" with command key + verbatim args (no shell re-quoting or normalization) + resolution-source chain (builtin → skills → agents → file order, recorded as the chain that resolved it) + expansion outcome.
    - AppendCompaction writes type "compaction" with a fresh boundary id (UUID per the in-repo crypto/rand pattern), timestamp, token-usage snapshot (input/output/cache totals), and pre/post transcript pointers.
    - A counting-Redactor fake injected via NewManager records ZERO Redact calls across an AppendRawThinking call, and at least one call for a control line of an existing redacted kind (e.g. AppendUserMessage).
    - readTranscriptFile parses a fixture containing one line of each new kind plus one line of an unknown future kind "quantum_teleport" and one known kind with an unknown extra field — no error, discriminators intact.
  </behavior>
  <read_first>internal/session/transcript.go (kind const block :18-49, Line struct :61-115, filePermOwner :11), internal/session/manager.go (appendLine :76-103 — note lines 82-89 are the redact block the unredacted path must not share, AppendSessionStart one-liner style :108-110, NewManager :39 for the fake-injection seam), internal/acp/handlers.go (newSessionID :227-239 — the UUID pattern to import or replicate; prefer importing if exported, else replicate locally in session with the same crypto/rand construction), .planning/phases/16-acp-wire-foundation/16-PATTERNS.md (manager.go and transcript.go sections)</read_first>
  <action>
RED: write transcript_newkinds_test.go with the five behavior cases (fixtures built inline; use a counting Redactor fake satisfying the Redactor interface NewManager requires). Commit failing.
GREEN: extend the kind const block with the three constants in the same doc-comment style (each comment names the decision — D-20 additive, D-21 compaction contract, D-22 local-command contract — and the payload contract per kind). Add the new Line fields in the transcript.go field-grouping convention: boundary id and pre/post pointers for compaction; resolution-source chain + verbatim args + expansion outcome for local_command; provider model attribution for raw_thinking; all json.RawMessage where the payload is provider-shaped. On-disk names camelCase with the tagliatelle marker per file convention. Implement appendLineUnredacted as a deliberate near-copy of appendLine with the redact block removed — do NOT extract a shared newline/marshal helper with the redacted path (Pitfall 5's warning sign is any shared append helper); same mutex discipline, same file-nil checks. Write the three Append* one-liners in the AppendSessionStart style, each routing raw_thinking through appendLineUnredacted and the other two through the existing redacted appendLine (local-command args and compaction metadata go through the redactor like every other kind — only thinking bytes are excluded, per D-23's type-level scoping). Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/session/ -run TestTranscriptNewKinds -count=1 && go test ./internal/session/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- TestTranscriptNewKinds passes: all five behavior cases asserted in one run under -race.
- A test asserts the raw-thinking payload round-trip is byte-identical (compare original RawMessage bytes against the re-marshaled field bytes with bytes.Equal after unmarshal).
- The counting fake records zero redactor invocations for raw_thinking and a positive count for the control kind.
- internal/session/manager.go contains the symbol appendLineUnredacted and its body has no redactor call.
- internal/session/transcript.go declares the three kind constants and the new Line fields; `go vet ./internal/session/` and the package's existing tests stay green.
  </acceptance_criteria>
  <done>The extended schema exists as three additive kinds with a provably redaction-exempt thinking path, and old readers tolerate the new lines (unknown-kind fixture parses clean).</done>
  <reversibility rating="one-way">D-20 additive-only schema: once lines of these kinds exist on disk, removing or renumbering them breaks replay; the human chose this exact door in CONTEXT D-20, so this records the rating without a new checkpoint.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Projector tolerance — new kinds invisible until their phases activate</name>
  <files>internal/session/projector.go, internal/session/transcript_newkinds_test.go</files>
  <behavior>
    - The Projector consuming a transcript containing raw_thinking / local_command / compaction / unknown-kind lines produces a projected window identical to the same transcript with those lines removed (the kinds are inert to projection today).
    - No error, no panic, no dropped-turn side effect when scanning past the new kinds.
  </behavior>
  <read_first>internal/session/projector.go (the kind dispatch/scan logic — find the switch or if-chain over line types and confirm whether new kinds fall through harmlessly; only touch it if the current code errors or mis-buckets unknown kinds), internal/session/transcript_newkinds_test.go (Task 1 fixtures to reuse)</read_first>
  <action>
Write the tolerance test: build two transcripts over the same turn content — one plain, one interleaved with the three new kinds plus an unknown kind — project both and assert the projected windows are deeply equal. If the Projector's existing unknown-kind handling already satisfies this (verify by reading the dispatch logic, not by assumption), the test pins it and projector.go needs no change; if the Projector errors on or mis-buckets the new kinds, add explicit inert cases at its dispatch point with a comment naming Phase 19 (compaction reset-point activation) and Phase 21 (thinking replay). Do not add projection semantics for compaction or thinking here — their consuming phases own that; this task only locks additive inertness (D-20).
  </action>
  <verify>
    <automated>go test ./internal/session/ -run TestProjectorToleratesNewKinds -count=1 && go test ./internal/session/ ./internal/runtime/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- TestProjectorToleratesNewKinds passes: projected window identical with and without the new-kind lines.
- The test names the activation owners (Phase 19 compaction, Phase 21 thinking) in a comment at the inert-case site or test header.
- The full session and runtime suites stay green (no projection regression).
  </acceptance_criteria>
  <done>The additive-only guarantee is proven end-to-end: write side appends the new kinds, read side tolerates them, projection is unchanged until Phases 19/21 switch their semantics on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider→transcript | raw provider thinking bytes land on disk unredacted by design (D-23) |
| disk→replay | transcript files may contain kinds a given binary does not know |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-04 | Information Disclosure | raw_thinking lines on disk | high | mitigate | Exclusion is type-scoped: only the raw_thinking kind uses the unredacted path; file perms stay 0600 (filePermOwner); the zero-calls test plus the prohibition above pin the boundary. Provider thinking is not user content — no credential-bearing kind gains the exemption |
| T-16-05 | Tampering | transcript replay with unknown kinds | medium | mitigate | Tolerant parse (unknown kinds/fields preserved in discriminators, never executed); test fixture pins the behavior (D-20) |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (stdlib + existing yaml dep only); legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/session/ -count=1` green including both new tests.
- Raw-thinking byte identity and redactor-zero-calls both asserted in CI from now on.
</verification>

<success_criteria>
D-20..D-23 are locked on disk: three additive kinds, type-scoped redaction exemption with a counting proof, provenance-complete local-command and compaction payloads, and replay tolerance for unknown kinds — with zero projection behavior change before Phases 19/21.
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-02-SUMMARY.md` when done
</output>
