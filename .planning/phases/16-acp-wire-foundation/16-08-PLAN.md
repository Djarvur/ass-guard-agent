---
phase: 16-acp-wire-foundation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/acpserve/config_surface.go
  - internal/acpserve/config_test.go
autonomous: true
gap_closure: true
requirements: [ACP-08]

estimate:
  tokens: 44000
  raw_tokens: 22000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "A _global/-scoped set whose value equals the COMBINED (project-won) effective value but differs from the global layer's own current value PERSISTS into the global layer (closes 16-VERIFICATION gap 3 — 16-04 truth 3, D-08's independently addressable layers)"
    - "The _global/model and _global/tier advertisement twins display the GLOBAL LAYER's own resolved values — the editor never renders the project's value under a '(global default)' label (closes 16-VERIFICATION gap 4a — 16-05 truth 1, D-11 true current values)"
    - "The idempotence guard for _global/-scoped ids compares against the ADDRESSED layer, so a legitimate global-layer mutation is never classified as a redundant re-push and silently dropped (closes 16-VERIFICATION gap 5 — 16-05 truth 2; same single root cause as gap 3)"
    - "Concurrent or interleaved Set calls serialize through the surface mutex; a persist failure at any scope leaves both layer files byte-identical, returns the typed error, and touches no live state (probe ACP-08 concurrency predicate — D-07 holds under the new comparison basis)"
    - "A project-scoped set whose value equals the combined effective value remains a logged idempotent no-op — the D-10 anti-promotion guard on the default (Zed re-push) scope is preserved"
  artifacts:
    - "internal/acpserve/config_surface.go — scope-aware idempotence basis in Set + global-layer-only resolution for the _global twins (new unexported helpers, Set/Options signatures unchanged)"
    - "internal/acpserve/config_test.go — TestScopeRouting_GlobalWritePersistsWhenCombinedMatches + TestConfigSurface_GlobalTwinsAdvertiseGlobalLayer"
  key_links:
    - "config_surface.go Set → addressed-layer resolution: global-scope writes load the global layer alone (modelrouting.Load over the global path) instead of the combined resolution before declaring idempotence"
    - "config_surface.go optionsLocked → globalOnlyResolvedLocked: the _global twins' currentValue comes from the global layer file (embedded floor when the file is absent), never from the project-won combined values"
  prohibitions:
    - "A scoped write whose value differs from the addressed layer's current value is never classified as a redundant re-push and silently dropped"
    - "A menu entry advertising a layer default never displays a different layer's resolved value"
    - "The default (project) scope's D-10 guard is never weakened: a client default re-push equal to the combined effective value neither churns the layer file nor promotes a blob-derived value into persisted config"
---

# Gap Closure 16-08: Scope-aware config surface semantics (WR-05) — gaps 3, 4a, 5

<objective>
Fix WR-05's single root cause in acpserve.ConfigSurface: the effective-value basis used for both the _global twin display and the set-channel idempotence guard is the COMBINED (project-won) resolution, so (a) an operator's explicitly global-scoped mutation whose value equals the combined effective value is silently swallowed and never reaches the global layer (Set's guard at config_surface.go:182-191, basis effectiveFor :398-404), and (b) the _global/model and _global/tier twins are built with the project-won res.model/res.tier (config_surface.go:518-521), so the editor renders the project's value under "Model (global default)". Closes 16-VERIFICATION gaps 3, 4a, and 5 (16-04 truth 3 PARTIAL, 16-05 truth 1 leg-a PARTIAL, 16-05 truth 2 PARTIAL — one root cause, one fix surface).

Purpose: D-08 promises two independently addressable editor-editable layers; D-11 promises the advertisement can never display stale truth; both are currently false for the global scope.
Output: scope-aware idempotence + layer-true global twins in internal/acpserve/config_surface.go, with both RED-first regressions in internal/acpserve/config_test.go.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-VERIFICATION.md
@.planning/phases/16-acp-wire-foundation/16-05-SUMMARY.md
@internal/acpserve/config_surface.go
@internal/acpserve/config_test.go

TDD mode is enabled: each task writes its failing regression first (RED), then the minimal surface change (GREEN). The two tasks share config_surface.go and run inside one plan sequentially — do not parallelize them.

Scope discipline: gaps 4b (chip truthfulness: runner default vs advertisement) is a DIFFERENT artifact and plan (16-09). This plan owns only the config-surface scope semantics.
</context>

<assumptions>
Flagged spec-less probe assumption surfaced per the probe-fallback contract (the ACP-08 concurrency row was `unresolved`; resolved HERE by construction + tests, never silently dropped):

- **ACP-08 concurrency (unresolved probe: "if interrupted or run in parallel, what is guaranteed?")** — DECIDED: concurrent/interleaved Set calls serialize through s.mu (existing TestConfigSurface_ConcurrentMutation pattern); the new addressed-layer reads happen under that same lock, so no torn read; an interrupted (failed) persist leaves both layer files byte-identical, returns the typed acp.ConfigPersistError, and applies nothing live (D-07's no-half-state, unchanged by this plan and re-pinned by the existing suite).
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope-aware idempotence — global writes compare against the addressed layer</name>
  <files>internal/acpserve/config_surface.go, internal/acpserve/config_test.go</files>
  <read_first>
  - internal/acpserve/config_surface.go — Set's idempotence guard (lines 149-207, the defective comparison at :182), resolveLocked (:298-315), effectiveFor (:398-404), persistLocked (:411-428), layerPaths (:537-552), splitScope (:600-606)
  - internal/acpserve/config_test.go — temp-layer fixture helpers and the existing pins this task must keep green: TestScopeRouting_GlobalPrefixWritesGlobalLayer (:414), TestSetIdempotent_BlobDerivedEffective (:469), TestSetIdempotent_ExplicitFileEffective (:514)
  - .planning/phases/16-acp-wire-foundation/16-VERIFICATION.md — gap 3's prescribed fix ("compare against the ADDRESSED layer's current value (load the global layer through modelrouting.Load(s.globalPath))")
  </read_first>
  <behavior>
    - RED test TestScopeRouting_GlobalWritePersistsWhenCombinedMatches: temp project layer sets tiers.heavy.model = model-x (so the combined effective model is model-x); temp global layer sets tiers.heavy.model = model-y; Set(sessionID, "_global/model", "model-x") must return the refreshed set with a nil error AND the global file read back must show tiers.heavy.model == model-x. On current code the guard classifies this as an idempotent re-push and the global file keeps model-y — RED proves the silent swallow.
    - Subtest global-true-idempotence: Set("_global/model", "model-y") when the global layer already holds model-y returns the refreshed set with NO file churn (the guard still fires when the ADDRESSED layer genuinely holds the value).
    - Invariant: both existing TestSetIdempotent_* tests pass UNMODIFIED — a project-scoped set equal to the combined effective value is still a logged no-op (D-10's anti-promotion guard on the Zed re-push scope).
    - Invariant: a failed persist under either scope still returns the typed persist error with both files byte-identical (existing D-07 tests stay green).
  </behavior>
  <action>
  Make Set's idempotence basis scope-aware (per D-08: the layers are independently addressable write targets):
  1. Add an unexported helper globalOnlyResolvedLocked() (*resolvedConfig, error) that loads ONLY the global layer — modelrouting.Load(s.globalPath) when the file exists (reuse the layerPaths existence filtering for the single path), modelrouting.Load() with no paths (embedded floor) when globalPath is empty or the file is absent — resolving tier (cfg.SessionTier) and model exactly like resolveLocked but WITHOUT blobFills and WITHOUT the project layer.
  2. In Set, compute the idempotence basis per scope: scope == scopeProject keeps the existing combined comparison (val == s.effectiveFor(bare, res.tier, res.model)); scope == scopeGlobal compares val against s.effectiveFor(bare, g.tier, g.model) where g comes from globalOnlyResolvedLocked() (load error → return the wrapped resolve error, same style as the existing resolve failure path).
  3. Adjust the idempotence log line to name the addressed layer for global-scope no-ops (e.g. "equals the global layer's current value — idempotent re-push, no layer write"), keeping the structured single-line convention.
  4. Change nothing else: validateSettableLocked, persistLocked, applyLocked, and the blob-fill deletion path keep their current behavior and order (persist-then-apply per D-07).
  Reference existing code: modelrouting.Load signature (internal/modelrouting/load.go:43) and the floor default for SessionTier (load.go:130-131).
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestScopeRouting|TestSetIdempotent' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestScopeRouting_GlobalWritePersistsWhenCombinedMatches passes: the global file contains tiers.heavy.model == model-x after the global-scoped set of model-x
    - The global-true-idempotence subtest passes with no byte change to the global file (read the file before/after and compare)
    - go test -race ./internal/acpserve/ -run 'TestScopeRouting|TestSetIdempotent|TestMetaBlob|TestConfigSurface' -count=1 exits 0 with TestSetIdempotent_BlobDerivedEffective and TestSetIdempotent_ExplicitFileEffective UNMODIFIED (git diff shows no change to them)
    - grep -c "globalOnlyResolvedLocked" internal/acpserve/config_surface.go returns at least 2 (definition + use)
  </acceptance_criteria>
  <done>A _global/-scoped mutation whose value differs from the global layer's own value always reaches the global layer; the project-scope D-10 guard is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Global twins advertise the global layer's own values</name>
  <files>internal/acpserve/config_surface.go, internal/acpserve/config_test.go</files>
  <read_first>
  - internal/acpserve/config_surface.go — optionsLocked (lines 479-531; the defective twin construction at :521-524) and Task 1's globalOnlyResolvedLocked helper
  - internal/acpserve/config_test.go — TestConfigSurface_MenuDefaults (:136) and TestConfigSurface_ExplicitLayerEffectiveValues (:198) — the pins that may encode the defective combined-value display
  </read_first>
  <behavior>
    - RED test TestConfigSurface_GlobalTwinsAdvertiseGlobalLayer: temp project layer sets tiers.heavy.model = model-x and session_tier default; temp global layer sets tiers.heavy.model = model-y; Options()[id == "_global/model"].CurrentValue == model-y (current code returns model-x — RED). Symmetric subtest for _global/tier against a global-layer session_tier value.
    - Invariant: the BARE model/tier entries keep displaying the combined effective values (TestConfigSurface_ExplicitLayerEffectiveValues stays green unmodified — D-11's chain resolution for the default scope).
    - Subtest absent-global-file: with no global layer file, both twins display the embedded floor's resolved values (modelrouting.Load() with no paths) rather than the project's values.
  </behavior>
  <action>
  In optionsLocked, build the _global/model and _global/tier twins' currentValue from Task 1's globalOnlyResolvedLocked() — the global layer's own tier/model — replacing the res.model/res.tier arguments at lines 521-524. The twins never consult blobFills (they describe the layer FILE; the blob channel stays on the bare options per the fills-unset boundary). When globalOnlyResolvedLocked() errors, degrade exactly like the existing optionsLocked error arm: log one structured line and fall back to the embedded floor's values for the twins. If TestConfigSurface_MenuDefaults pinned twin values to the combined resolution, update those pins to the layer-true values — a pin asserting the defective display is itself defective; note each pin change in the commit message.
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestConfigSurface|TestScopeRouting|TestSetIdempotent|TestMetaBlob' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestConfigSurface_GlobalTwinsAdvertiseGlobalLayer passes: _global/model CurrentValue == the global layer's model value, _global/tier CurrentValue == the global layer's tier value
    - The absent-global-file subtest shows the embedded floor's values on both twins
    - go test -race -count=1 ./internal/acpserve/ exits 0 (full package, including the simulator suite's config stages)
    - The bare options' combined-value display is unchanged: TestConfigSurface_ExplicitLayerEffectiveValues passes without modification
  </acceptance_criteria>
  <done>The editor's global-default entries display the global layer's truth; the set channel and the display channel share one scope-aware resolution basis (gaps 3, 4a, 5 closed at the surface).</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / path | Kind | Notes |
|---|---|---|
| `ConfigSurface.globalOnlyResolvedLocked` (internal/acpserve/config_surface.go) | new unexported method | loads the global layer alone (file present) or the embedded floor (absent/unavailable); resolver-true tier/model without blobFills or the project layer |
| `ConfigSurface.Set` idempotence basis (internal/acpserve/config_surface.go) | modified behavior | scope-aware comparison: global scope → addressed-layer value, project scope → combined value (unchanged) |
| `ConfigSurface.optionsLocked` `_global/` twins (internal/acpserve/config_surface.go) | modified behavior | twins' currentValue from globalOnlyResolvedLocked instead of the project-won res.model/res.tier |
| `TestScopeRouting_GlobalWritePersistsWhenCombinedMatches` (internal/acpserve/config_test.go) | new test func | the silent-swallow regression (RED→GREEN) |
| `TestConfigSurface_GlobalTwinsAdvertiseGlobalLayer` (internal/acpserve/config_test.go) | new test func | layer-true twin display, incl. absent-global-file floor subtest |
| exported surface | none | Set/Options signatures unchanged; no new exported symbols; no new files |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor set_config_option → ConfigSurface.Set | untrusted option id + value cross into layer-file mutation |
| ConfigSurface → operator layer files (0600) | the persisted operator config this surface writes |
| _meta blob (initialize) → blobFills overlay | client-supplied defaults, in-memory only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-08-01 | Tampering | Set idempotence basis (config_surface.go:182) | medium | mitigate | Scope-aware basis: global-scope writes can no longer be silently dropped (the gap itself); the project scope KEEPS the combined comparison so a client default re-push can neither churn operator files nor promote blob-derived values (D-10) |
| T-16-08-02 | Tampering | _global twin display (config_surface.go:521-524) | medium | mitigate | Layer-true resolution: the display channel can no longer misattribute the project's value to the global layer (D-11); pinned by TestConfigSurface_GlobalTwinsAdvertiseGlobalLayer |
| T-16-08-03 | Tampering | global-scoped value validation | low | mitigate | Unchanged D-09 typed reject: values must be declared models/tiers in the combined config before any scope routing (validateSettableLocked runs before the idempotence check) — no new write surface opened by the scope-aware basis |
| T-16-08-04 | Information Disclosure | structured log lines | low | accept | The menu vocabulary carries no credential options (16-VERIFICATION unverified-prohibition #3, pass_non_authoritative); log lines name option ids and values of that vocabulary only |
| T-16-08-SC | Tampering | package installs | low | accept | No package-manager installs in this plan (existing go.mod deps only: yaml.v3, modelrouting, providerfactory) |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/acpserve/` — full package green including both new regressions and the untouched D-10 idempotence pins
- `go test -race ./internal/acpserve/ -run TestZedSimulatorE2E -count=1` — the whole-surface story still passes with layer-true global twins (stage fixtures that write global layers see the corrected display)
- `go test ./internal/providerfactory/ ./internal/modelrouting/ -run 'TestConfigWrite|TestSessionTier'` — the write/read halves this surface composes remain green
</verification>

<success_criteria>
16-VERIFICATION gaps 3, 4a, and 5 are closed: the global layer is genuinely independently addressable (a global-scoped write whose value differs from the addressed layer always persists — 16-04 truth 3), every menu entry advertises its true current value for its addressed scope (16-05 truth 1 leg-a), and the idempotence no-op is declared only when the ADDRESSED layer already holds the value (16-05 truth 2).
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-08-SUMMARY.md` when done
</output>
