---
phase: 16-acp-wire-foundation
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/runtime/runtime.go
  - internal/runtime/apply_model_test.go
  - internal/acpserve/chip_truth_test.go
autonomous: true
gap_closure: true
requirements: [ACP-08]

estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "With no editor-driven model stamp, the model on the wire equals the tier-resolved config model — the same value the ACP-08 advertisement displays — so the pre-stamp divergence (operator-observed: turn-001 went out GLM-5.3 while the chip showed glm-5.2) cannot recur (closes 16-VERIFICATION gap 4b — 16-05 truth 1 leg-b, ROADMAP criterion 4 'true current values')"
    - "An explicit editor/applied model stamp keeps precedence over the tier-resolution default — ApplyTurnModel behavior and the future-sessions stamp leg are unchanged (D-12: the editor write tops the chain)"
    - "The bare model advertisement entry is resolver-true by construction: its currentValue equals an independent modelrouting resolver evaluation over the same config (chip==wire invariant pinned from the advertisement side)"
    - "Test runners and configs with no schedCfg keep today's profile-slug behavior — the nil-config documented default is preserved (matches schedCfg's documented nil semantics)"
  artifacts:
    - "internal/runtime/runtime.go — new unexported Runner.defaultTurnModel() mirroring the resolver-then-static-binding fallback, stamped by sessionFor when no editor model is set"
    - "internal/runtime/apply_model_test.go — TestDefaultTurnModel_FollowsTierResolution (the wire-side half of chip==wire)"
    - "internal/acpserve/chip_truth_test.go — TestConfigAdvertisement_ResolverTruth (the advertisement-side half of chip==wire; new file, no shared-file conflict with 16-08)"
  key_links:
    - "runtime.go sessionFor → defaultTurnModel → modelrouting resolver: the default request model and the advertisement currentValue now resolve through the SAME chain (tier + time window + static binding fallback), making D-11's 'never display stale truth' literally true"
    - "acpserve chip_truth_test.go → modelrouting.NewResolver: an independent resolver evaluation in the test pins the advertisement to resolver truth, so future drift fails a test instead of surfacing in a live Zed chip"
  prohibitions:
    - "An explicit editor/applied model stamp is never overridden by the tier-resolution default — the editor write stays the top of the precedence chain (D-12)"
    - "The default model resolution never rewires the live provider to a different provider — the default rides the same heavy-tier resolution that picked the session provider (14-05), and cross-provider targets degrade loudly (the resolveSubagentModel precedent)"
---

# Gap Closure 16-09: Chip truthfulness — one source of truth for the wire model (gap 4b)

<objective>
Close 16-VERIFICATION gap 4b: the runner's wire model defaults to the profile slug (runtime.go effectiveModel "" = profile model) while the ACP-08 advertisement resolves the tier-resolved config value (config_surface.go resolveModelLocked) — code-confirmed and operator-observed live (turn-001 GLM-5.3 on the wire vs glm-5.2 on the chip). The verifier's Step 9b scan found no later-phase owner (Phase 20's /model is session-scope command routing), so the gap closure must make ONE source of truth win.

Decision (recorded, implementing): the RUNNER's default follows the tier resolution. Grounding: D-11 locks that advertised options carry the EFFECTIVE value resolved through the precedence chain and the editor can never display stale truth; D-12 locks that precedence stays ONE chain (session override > time-window > project > global). The tier-resolved model IS the chain's answer; the profile slug sits outside the chain (a leftover of the abandoned mimicry bar, pivoted 2026-08-25 per STATE.md). Route "chip follows runner" is rejected because it would stamp runner state into entries that advertise LAYER values (the _global twins, the pending options) — incoherent with D-11; route "operator override" is rejected because no operator disposition exists and the locked decisions entail the chain as the single source of truth.

Purpose: ROADMAP criterion 4 ("true current values") is only true when the advertised value is the value the next request carries.
Output: runner default resolution in internal/runtime/runtime.go + wire-side and advertisement-side invariant tests.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-VERIFICATION.md
@.planning/phases/16-acp-wire-foundation/16-05-SUMMARY.md
@internal/runtime/runtime.go
@internal/runtime/apply_model_test.go
@internal/acpserve/config_surface.go

TDD mode is enabled: Task 1 is RED-first. This plan has NO file overlap with 16-08 (config_surface.go/config_test.go) — the advertisement-side invariant test lives in a NEW file, internal/acpserve/chip_truth_test.go, keeping both plans in wave 1.

Chip truthfulness is a source-of-truth contract later phases (18 reconciliation, 20 /model) build on; the decision above is recorded here and MUST be restated in the SUMMARY (output section).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN — runner default turn model follows the tier resolution</name>
  <files>internal/runtime/runtime.go, internal/runtime/apply_model_test.go</files>
  <read_first>
  - internal/runtime/runtime.go — sessionFor's model stamp block (lines 1061-1066), the field docs for schedCfg/effectiveModel (lines 124-135), effectiveModelFor (lines 1512-1519), resolveSubagentModel (lines 1311-1331 — the exact resolver call shape to mirror), the tier constants (internal/runtime/goconst_constants.go), NewRunner/RunnerConfig (lines 218-243)
  - internal/runtime/apply_model_test.go — the gatedStreamProvider harness that records the profile model of every Stream (lines 31-60) and TestApplyTurnModel_StampsFutureSessions (line 197)
  - internal/acpserve/config_surface.go — resolveModelLocked (lines 320-331): the resolver-then-static-binding fallback semantics to mirror EXACTLY for chip parity
  - internal/modelrouting/load.go — SessionTier floor default (lines 130-131); internal/modelrouting/resolver.go — Resolve signature (line 43)
  </read_first>
  <behavior>
    - RED test TestDefaultTurnModel_FollowsTierResolution: build a Runner whose schedCfg loads a temp config with tiers.heavy.model = model-from-config (no time windows) while the Runner's profile.Model = model-profile-slug; drive one turn with NO ApplyTurnModel call; the gated provider must observe model == model-from-config. On current code the profile slug goes out — RED.
    - Subtest explicit-stamp-wins: after ApplyTurnModel(model-explicit), the next turn carries model-explicit (editor write tops the chain, D-12; existing TestApplyTurnModel tests must stay green unmodified).
    - Subtest nil-config-default: a Runner with schedCfg == nil sends the profile slug (documented test-runner default preserved).
    - Subtest resolver-declines-fallback: with a time window that declines resolution but a static tiers binding present, the static binding's model is stamped (mirrors resolveModelLocked's fallback, chip parity).
  </behavior>
  <action>
  1. Add unexported func (r *Runner) defaultTurnModel() string with exactly this ladder: schedCfg == nil → ""; tier := r.schedCfg.SessionTier, empty → tierHeavy; tgt, _, err := modelrouting.NewResolver(r.schedCfg).Resolve(tier, "", time.Now(), modelrouting.CapabilityReq{}) — err == nil && tgt.Model != "" → tgt.Model; else cfg.Tiers[tier].Model when the binding exists; else "".
  2. In sessionFor's stamp block, replace the current if with: m := r.effectiveModelFor(); if m == "" { m = r.defaultTurnModel() }; if m != "" { prof.Model = m } — the explicit stamp keeps absolute precedence.
  3. Update the effectiveModel field doc comment (lines 130-133): "" now means the tier-resolved config default (chip parity), not the profile model.
  4. Do not touch ApplyTurnModel, SetTurnModel, or the session turn-mutex serialization — the live-apply seam is verified and out of gap scope.
  Rationale citation: this implements the D-11/D-12-consistent route of the gap-4b decision (see objective); note the route choice in the commit message.
  </action>
  <verify>
    <automated>go test -race ./internal/runtime/ -run 'TestDefaultTurnModel|TestApplyTurnModel|TestTierSwitch' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestDefaultTurnModel_FollowsTierResolution passes all four subtests (tier-resolution default, explicit-stamp precedence, nil-config profile default, static-binding fallback)
    - TestApplyTurnModel and TestApplyTurnModel_StampsFutureSessions pass UNMODIFIED (git diff shows no change to existing tests in the file)
    - go test -race -count=1 ./internal/runtime/ exits 0
    - grep -c "defaultTurnModel" internal/runtime/runtime.go returns at least 2 (definition + sessionFor call site)
  </acceptance_criteria>
  <done>With no editor stamp, the model on the wire equals the tier-resolved config model — the same value the advertisement displays.</done>
  <reversibility rating="reversible">Flipping the winning source of truth is a code+test change with no persisted state; the direction itself is entailed by locked D-11/D-12, and the regression pins whichever contract is live.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Pin the advertisement-side resolver truth + full-suite regression sweep</name>
  <files>internal/acpserve/chip_truth_test.go</files>
  <read_first>
  - internal/acpserve/config_surface.go — optionsLocked (lines 479-531) and resolveModelLocked (lines 320-331): what the chip advertises and how
  - internal/acpserve/config_test.go — the temp-layer file-writing helper patterns to replicate (TestConfigSurface_ExplicitLayerEffectiveValues at :198 builds layer files; copy that fixture style into the new file)
  - internal/modelrouting/resolver.go — Resolve (line 43) for the independent evaluation the test performs
  </read_first>
  <behavior>
    - Test TestConfigAdvertisement_ResolverTruth: with temp layer files (project + global), build the ConfigSurface over them, take Options(), and assert the bare `model` entry's CurrentValue equals an INDEPENDENT evaluation: modelrouting.NewResolver(loadedCfg).Resolve(loadedCfg.SessionTier, "", time.Now(), modelrouting.CapabilityReq{}).Model computed inside the test from the same files — advertisement is resolver-true by construction
    - The test tolerates the time-window-free fixture only; no flaky boundary-crossing assertions
  </behavior>
  <action>
  Create internal/acpserve/chip_truth_test.go (NEW file — deliberate: no files_modified overlap with 16-08's config_test.go, keeping both plans wave 1). The test builds temp layer files with the same fixture style as config_test.go, constructs NewConfigSurface directly (no full serve needed), and pins the resolver-truth invariant for the bare model entry (the entry whose wire counterpart Task 1 aligned). Then run the regression sweep: the full -race suite for internal/runtime and internal/acpserve, and TestZedSimulatorE2E — if any 16-06 simulator fixture assumed the old profile-slug default (the temp project config drives the factory-built provider; the SSE stub is turn-keyed, not model-keyed), align the fixture's declared models so the resolved default matches the scripted turns, and note the alignment in the commit message.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/runtime/ ./internal/acpserve/ && go test -race ./internal/acpserve/ -run TestZedSimulatorE2E -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/acpserve/chip_truth_test.go exists and TestConfigAdvertisement_ResolverTruth passes
    - go test -race -count=1 ./internal/runtime/ ./internal/acpserve/ exits 0 (both packages fully green after the default-model change)
    - TestZedSimulatorE2E passes under -race (the five-stage whole-surface story intact with the new default)
    - go test -race -count=1 ./internal/acp/ ./internal/session/ exits 0 (adjacent wire/session packages untouched but re-proven)
  </acceptance_criteria>
  <done>The chip==wire invariant is pinned from both sides (wire: Task 1; advertisement: Task 2) and the full suite proves no fixture regressed; the SUMMARY records the chip-truthfulness decision as implemented (runner follows the tier resolution), closing gap 4b by implementation rather than override.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / path | Kind | Notes |
|---|---|---|
| `Runner.defaultTurnModel` (internal/runtime/runtime.go) | new unexported method | resolver → static-binding → "" ladder over schedCfg; nil schedCfg returns "" (profile slug default preserved) |
| `Runner.sessionFor` model stamp (internal/runtime/runtime.go) | modified behavior | "" effective model now falls through to defaultTurnModel before leaving the profile slug |
| `Runner.effectiveModel` field doc (internal/runtime/runtime.go) | modified comment | "" documented as the tier-resolved config default |
| `TestDefaultTurnModel_FollowsTierResolution` (internal/runtime/apply_model_test.go) | new test func | four subtests: tier-resolution default, explicit-stamp precedence, nil-config default, static-binding fallback |
| `TestConfigAdvertisement_ResolverTruth` (internal/acpserve/chip_truth_test.go) | new test func + new file | advertisement bare-model currentValue pinned to an independent resolver evaluation |
| exported surface | none | no new exported symbols; ApplyTurnModel/SetTurnModel signatures unchanged |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config layers (operator files) → runtime default model selection | the tier/time-window table steers which model answers turns |
| runtime default → provider request | the stamped slug determines the outgoing request's model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09-01 | Tampering | defaultTurnModel config-driven default | medium | mitigate | The default rides the SAME heavy-tier resolution that picked the session provider at startup (14-05), so config cannot steer the wire model to a provider the session was not built for; the static-binding fallback mirrors resolveModelLocked and cross-provider targets keep the loud-degrade guards (resolveSubagentModel precedent) |
| T-16-09-02 | Repudiation | transcript model attribution | low | accept | Transcript lines keep recording the actual stamped model (existing Model field); attribution now always equals the chain-resolved value, improving auditability |
| T-16-09-03 | Denial of Service | resolver error paths | low | mitigate | Every decline path falls back down the documented ladder (resolver → static binding → profile slug) — a malformed time window or missing binding degrades to a working default, never a failed turn |
| T-16-09-SC | Tampering | package installs | low | accept | No package-manager installs in this plan (existing modelrouting dep only) |
</threat_model>

<verification>
- `go test -race ./internal/runtime/ -run 'TestDefaultTurnModel|TestApplyTurnModel|TestTierSwitch' -count=1` — the new default + precedence ladder
- `go test -race -count=1 ./internal/runtime/ ./internal/acpserve/ ./internal/acp/ ./internal/session/` — the phase's four wire packages fully green
- `go test -race ./internal/acpserve/ -run TestZedSimulatorE2E -count=1` — the whole-surface story with the aligned default
- `go vet ./internal/runtime/ ./internal/acpserve/` clean
</verification>

<success_criteria>
16-VERIFICATION gap 4b is closed: the pre-stamp Model chip divergence cannot recur because the runner's default request model and the advertisement's currentValue resolve through the same precedence chain (D-11/D-12), with explicit editor stamps still on top — ROADMAP criterion 4's "true current values" holds pre-stamp, and the decision is recorded in the SUMMARY for Phases 18/20 to build on.
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-09-SUMMARY.md` when done
</output>
