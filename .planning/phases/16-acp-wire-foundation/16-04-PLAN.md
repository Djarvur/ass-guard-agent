---
phase: 16-acp-wire-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/providerfactory/config_write.go
  - internal/providerfactory/config_write_test.go
  - internal/modelrouting/config.go
  - internal/modelrouting/load_test.go
autonomous: true
requirements: [ACP-08]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A typed persist-then-apply layer writer exists: read existing layer as a generic map, deep-merge the write, marshal YAML, write atomically (temp file + rename) at 0600 — and it round-trips with modelrouting.Load: what Load reads back after a write is exactly what was written (D-07 write half)"
    - "Write failures leave the existing layer file byte-identical with no temp-file leftovers and return a typed error — no half-applied state (D-07)"
    - "session_tier is an additive config key defaulting to heavy that survives Load round-trip; the global layer (GlobalConfigPath) and project layer (ProjectConfigPath) are independently addressable write targets, and a project write never modifies the global file (D-08 groundwork)"
  artifacts:
    - path: "internal/providerfactory/config_write.go"
      provides: "WriteLayerOption(layerPath string, keyPath []string, value any) error — the persist-then-apply layer writer with typed errors"
      contains: "WriteLayerOption"
    - path: "internal/modelrouting/config.go"
      provides: "SessionTier field with yaml key session_tier, defaulted heavy"
      contains: "SessionTier"
    - path: "internal/providerfactory/config_write_test.go"
      provides: "TestConfigWrite round-trip / atomicity / 0600 / layer isolation"
      contains: "TestConfigWrite"
  key_links:
    - from: "internal/providerfactory/config_write.go"
      to: "internal/modelrouting/load.go"
      via: "the writer's on-disk format is inverse-compatible with Load/deepMerge — a layer written by WriteLayerOption parses through Load with the written value winning at the written key path"
      pattern: "deepMerge"
  prohibitions: []
---

<objective>
Build the write half of editor-driven configuration (ACP-08's persistence substrate): a typed layer writer that mutates the existing global/project config.yaml files with atomic replace and 0600 discipline, plus the additive session_tier key. Plan 16-05's wire surface calls this writer; nothing here knows ACP.

Purpose: D-07's persist-then-apply ordering is only trustworthy if the write primitive itself is atomic, permission-correct, and round-trip-exact against the loader it will be read back through. Building it package-locally (providerfactory owns both layer paths and imports modelrouting) keeps acp wire-only.
Output: internal/providerfactory/config_write.go (+test), session_tier key.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-CONTEXT.md
@.planning/phases/16-acp-wire-foundation/16-RESEARCH.md
@.planning/phases/16-acp-wire-foundation/16-PATTERNS.md
@internal/providerfactory/provider_factory.go
@internal/modelrouting/load.go
@internal/modelrouting/config.go
@internal/modelrouting/defaults/config.yaml
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/providerfactory/config_write.go` — `WriteLayerOption(layerPath string, keyPath []string, value any) error` + typed write errors; internal read-merge-marshal helpers reusing modelrouting merge semantics.
- `internal/modelrouting/config.go` — `SessionTier` field (yaml `session_tier`), defaulted `heavy` in the defaults application.
- `internal/providerfactory/config_write_test.go` — TestConfigWrite (round-trip, atomic failure modes, perms, layer isolation).
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist-then-apply layer writer</name>
  <files>internal/providerfactory/config_write.go, internal/providerfactory/config_write_test.go</files>
  <behavior>
    - WriteLayerOption(projectLayer, ["tiers","heavy","model"], "glm-5.3") on an empty project dir creates the file containing that nested key, mode 0600, and modelrouting.Load(global=absent, project=that file) resolves tier heavy's model to glm-5.3 (round-trip with the real loader, not a re-parse).
    - The same call on a layer file that already carries unrelated keys (copy defaults/config.yaml as the starting fixture) preserves every unrelated key byte-for-byte in structure (re-Load equals original Config plus the one changed value — compared via the loaded structs, not raw bytes, since YAML re-marshal reflows).
    - Unwritable target directory: typed error, existing file untouched, no temp leftovers (glob the dir for stray suffix files).
    - Corrupt existing layer (invalid YAML fixture): typed error naming the layer path, file left byte-identical (never clobbered by a failed merge).
    - The temp-then-rename sequence is observable: a crash between marshal and rename cannot half-write (test injects a rename failure via an unexported seam or chmod trick and asserts the original survives).
  </behavior>
  <read_first>internal/providerfactory/provider_factory.go (GlobalConfigPath :20, ProjectConfigPath :34, WarnLooseConfigPerm for the perm conventions), internal/modelrouting/load.go (Load :43, deepMerge :97 — the merge semantics the writer must be inverse to; yaml.v3 usage), internal/modelrouting/config.go (Config struct), internal/session/transcript.go (filePermOwner 0600 precedent :11), .planning/phases/16-acp-wire-foundation/16-PATTERNS.md (config writer section — no-analog note), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (Pitfall 6 half-applied state)</read_first>
  <action>
RED: write config_write_test.go with the five behavior cases (t.TempDir-based; corrupt-layer and rename-failure fixtures). Commit failing.
GREEN: implement WriteLayerOption in internal/providerfactory/config_write.go — read the target layer file if present into map[string]any via yaml.v3 (a missing file starts an empty map), apply the key-path write with map-overlay deep-merge semantics identical to modelrouting's deepMerge (reuse it by exporting it from modelrouting or replicating the overlay rules exactly — prefer exporting the existing helper over duplication; if exported, keep it unexported-visible via an internal sibling if the package layout forbids export, but do not fork the semantics), marshal with yaml.v3, then write to a sibling temp file created with 0600 and rename over the target (the atomic replace; fsync the temp file before rename per the session artifact convention if one exists — check manager.go's append path for the precedent). Typed errors: distinguish read/parse errors from write errors so callers (16-05) can map them to distinct JSON-RPC error data. Directory creation uses 0750 max (never group/world writable). No locks here — serialization is the caller's contract (16-05 applies under one path); document that contract on the function. Commit green.
  </action>
  <verify>
    <automated>go test ./internal/providerfactory/ -run TestConfigWrite -count=1 && go test ./internal/providerfactory/ ./internal/modelrouting/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- TestConfigWrite passes all five cases; the round-trip case asserts through modelrouting.Load, not a local re-parse.
- The written file's mode is exactly 0600 and the temp-file glob finds no leftovers in any failure case.
- internal/providerfactory/config_write.go declares WriteLayerOption with the caller-serializes contract documented.
- The existing modelrouting and providerfactory suites stay green (deepMerge export/refactor, if any, is behavior-neutral).
  </acceptance_criteria>
  <done>The D-07 write primitive exists: atomic, permission-correct, failure-transparent, and provably inverse-compatible with the loader that consumes it.</done>
  <reversibility rating="costly">On-disk config format extension: written layer files become operator-owned state; future format changes must keep Load-compatible with files this writer produces (additive-only discipline, mirroring D-20's logic).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: session_tier additive key</name>
  <files>internal/modelrouting/config.go, internal/modelrouting/load_test.go, internal/providerfactory/config_write_test.go</files>
  <behavior>
    - Config gains SessionTier (yaml key session_tier); a config without the key loads with SessionTier "heavy" (defaults application); a config with session_tier: light loads "light"; the key round-trips through marshal→Load.
    - WriteLayerOption(globalLayer, ["session_tier"], "light") followed by Load resolves SessionTier light — the full persist→read path for the tier option that 16-05's menu exposes.
    - A project-layer session_tier write leaves the global layer file untouched (byte comparison).
  </behavior>
  <read_first>internal/modelrouting/config.go (Config struct field layout + tag style), internal/modelrouting/load.go (defaults application — where absent values get their defaults; embedded default layering), internal/modelrouting/load_test.go (existing round-trip test conventions to extend), internal/providerfactory/config_write_test.go (Task 1 helpers)</read_first>
  <action>
Add the SessionTier field with the yaml tag; apply the heavy default wherever Load applies defaults (same site as other absent-key defaults — read the flow first; the embedded defaults/config.yaml may also carry it explicitly, in which case the code default is the fallback for configs that predate the key). Extend the load tests with the three behavior cases and add the WriteLayerOption session_tier round-trip case to config_write_test.go (cross-package: it lives in providerfactory and calls Load). No resolver behavior change in this plan — tier consumption lands in 16-05's apply seam; here the key only parses, defaults, and round-trips.
  </action>
  <verify>
    <automated>go test ./internal/modelrouting/ -run 'TestSessionTier|TestLoad' -count=1 && go test ./internal/providerfactory/ -run TestConfigWrite -count=1</automated>
  </verify>
  <acceptance_criteria>
- Absent key loads heavy; explicit value loads through; marshal round-trip holds.
- The writer round-trip case (write global session_tier light → Load sees light) passes.
- The layer-isolation byte comparison passes (project write leaves global untouched).
- `go vet ./internal/modelrouting/ ./internal/providerfactory/` clean and both suites green.
  </acceptance_criteria>
  <done>The tier option has its persistence substrate: an additive, defaulted, round-trip-safe config key addressable in either layer — ready for 16-05's advertisement and apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor-driven writes → operator config files | values arriving over the wire mutate files the operator owns |
| writer → filesystem | atomicity and permissions are the safety contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-10 | Tampering | config file corruption on failed write | high | mitigate | Temp+rename atomic replace; corrupt-layer and rename-failure tests prove the original survives; no temp leftovers (D-07, Pitfall 6) |
| T-16-11 | Elevation | loosened file permissions | medium | mitigate | Hard 0600 on the target, 0750 max on created dirs; a perms assertion in the test family (WarnLooseConfigPerm continues to police reads) |
| T-16-12 | Tampering | value injection into arbitrary config keys | medium | mitigate | This plan ships the primitive only; 16-05 whitelists the writable key set (menu ids) at the wire — noted here so the boundary is explicit |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (yaml.v3 already in tree); legitimacy gate not applicable |
</threat_model>

<verification>
- `go test ./internal/providerfactory/ ./internal/modelrouting/ -count=1` green.
- `mise ci` green.
</verification>

<success_criteria>
Editor configuration has its persistence half: an atomic, permission-correct, round-trip-exact layer writer plus the session_tier key — ACP-free, so 16-05 can compose it behind the wire without dragging protocol types into the config packages.
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-04-SUMMARY.md` when done
</output>
