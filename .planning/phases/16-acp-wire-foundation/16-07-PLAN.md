---
phase: 16-acp-wire-foundation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/acp/emitter.go
  - internal/acp/emitter_test.go
autonomous: true
gap_closure: true
requirements: [ACP-03]

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Two concurrent session/prompt turns on one connection both see their turn-end Barrier return: a wake generated by one written frame reaches EVERY parked Barrier waiter, not exactly one of them (closes 16-VERIFICATION gap 1, CR-01)"
    - "The second concurrent turn's session/prompt response is actually written — updates-before-response ordering holds for every concurrent waiter, so the ordering contract no longer degrades into a liveness failure under concurrency (closes 16-VERIFICATION gap 2)"
    - "Concurrent Barrier waiters never merge or absorb one another: each waiter re-checks the written counter independently after every written frame (probe adjacency predicate — equal/adjacent waiters stay separate)"
    - "A Barrier over an empty queue still returns immediately with zero wake traffic — the zero-activity-turn contract is unchanged by the broadcast rewrite (probe empty-input predicate)"
    - "The broadcast wake never reorders frames: one drain, one total order, foreground preemption and background FIFO unchanged (probe equal-element ordering predicate)"
  artifacts:
    - "internal/acp/emitter.go — Barrier wake mechanism is a per-generation broadcast (swap-and-close under mu), not a capacity-1 single-token handoff"
    - "internal/acp/emitter_test.go — TestTurnEmitterBarrierConcurrentWaiters regression: two connection-lifetime-ctx Barriers satisfied by the same final frame, both return"
  key_links:
    - "emitter.go writeOut → all parked Barrier waiters: every written frame broadcasts (close + fresh channel swapped atomically under mu) so no waiter is stranded at terminal state"
    - "emitter.go drain → writeOut: the drain remains the ONLY caller, keeping the close serialized and the single-writer total order intact"
  prohibitions:
    - "A full lane never drops, merges, or coalesces frames — producers block until the drain advances (D-01)"
    - "The wake mechanism never creates a second notification write path into the sink — the drain stays the only writer of session/update frames"
---

# Gap Closure 16-07: Barrier broadcast wake (CR-01) — gaps 1 + 2

<objective>
Fix the code-confirmed lost-wakeup in TurnEmitter.Barrier (CR-01 from 16-REVIEW.md, confirmed in current code by 16-VERIFICATION): the capacity-1 wake channel with single-token handoff (emitter.go:181, 261-285, 297-308) wakes only ONE waiter per written frame. With two concurrent session/prompt turns on one connection (per-request goroutine dispatch, server.go:378; one shared emitter per Server, server.go:182; Barrier receives the connection-lifetime Serve ctx, handlers.go:400), the second waiter never receives again, its ctx never fires in production, and its prompt response hangs forever. This closes 16-VERIFICATION gaps 1 and 2 (16-01 truths 5 and 6, both FAILED) and restores the phase goal's "proven under concurrency" clause for the emitter primitive.

Purpose: every interactive phase (17-23) rides this barrier for updates-before-response; a hung second turn is a liveness defect at the wire foundation.
Output: broadcast-wake Barrier in internal/acp/emitter.go + TestTurnEmitterBarrierConcurrentWaiters regression in internal/acp/emitter_test.go.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-VERIFICATION.md
@.planning/phases/16-acp-wire-foundation/16-01-SUMMARY.md
@internal/acp/emitter.go
@internal/acp/emitter_test.go

TDD mode is enabled: Task 1 is the RED gate (failing regression test), Task 2 is GREEN (the fix). Do not implement the fix before the RED test exists and fails.

Decision provenance: the verifier's alternative — accept via a recorded override documenting a single-concurrent-prompt limitation — is rejected: it would reduce the phase goal's locked "proven under concurrency" clause, which gap closure may not do. The fix is the small generation-broadcast the verifier named.
</context>

<assumptions>
Flagged spec-less probe assumptions surfaced per the probe-fallback contract (all four probe rows were `unresolved`; the ACP-03 rows are resolved HERE by construction + tests, never silently dropped):

- **ACP-03 adjacency (unresolved probe: "when two things are exactly equal or just touch, do they merge, collide, or separate?")** — DECIDED: two Barrier waiters adjacent to the same written frame stay SEPARATE — each re-checks the written counter independently; no waiter is absorbed by another's wake. This predicate is precisely what CR-01 violated and Task 1's regression pins.
- **ACP-03 empty (unresolved probe: "what is the result for empty, single-element, or null input?")** — DECIDED: an empty queue (zero-activity turn) returns from Barrier immediately, zero frames, zero delay; a nil frame argument to a handle method still returns errNilFrame. Pinned by the existing TestTurnEmitterEmptyTurn, which must stay green unmodified.
- **ACP-03 ordering (unresolved probe: "when elements compare equal, is output order specified and stable?")** — DECIDED: equal-class frames keep arrival FIFO within their lane; the broadcast wake adds NO reordering because the drain loop, the lanes, and the single-writer rule are untouched — only the wake mechanism changes.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — two-waiter Barrier regression test (connection-lifetime ctxs, no ctx escape)</name>
  <files>internal/acp/emitter_test.go</files>
  <read_first>
  - internal/acp/emitter.go — the CURRENT defect: wake field init at line 181 (capacity-1), Barrier loop at lines 261-285, writeOut tail at lines 297-308 (single non-blocking token send)
  - internal/acp/emitter_test.go — existing fake-sink and barrier harness patterns: TestTurnEmitterBarrier (line 415), TestTurnEmitterEmptyTurn (line 495), TestTurnEmitterProducerCtxAbort (line 343)
  </read_first>
  <behavior>
    - Test TestTurnEmitterBarrierConcurrentWaiters (subtest "same final frame satisfies both waiters"): construct a TurnEmitter over a sink that BLOCKS its first Write until the test releases it; enqueue exactly two foreground frames via the handle; start TWO goroutines calling Barrier(context.Background()) only AFTER both enqueues (both targets = 2, both parked while the sink holds write #1); release the sink; BOTH Barrier calls must return within a bounded deadline (select with time.After, ~2s). On the current code the final frame's single wake token wakes at most one waiter and the other times out — this is the RED.
    - Subtest "a lone waiter still wakes per frame": one Barrier with target=1 parked while a second frame is enqueued — the waiter whose target the FIRST written frame satisfies returns promptly (the broadcast must not over-correct into a lost single-waiter wake).
    - Invariant on the waiter ctx: use context.Background() (or a deliberately never-cancelled ctx) — the regression must have NO ctx.Done escape, because escaping via a short-lived ctx is exactly how every existing test avoids the broken state (emitter_soak_test.go:371-388 barrier hammer included).
    - Empty-queue truth: the existing TestTurnEmitterEmptyTurn passes unmodified (no changes to that test).
  </behavior>
  <action>
  Add TestTurnEmitterBarrierConcurrentWaiters to internal/acp/emitter_test.go following the file's existing fake-sink conventions (a sink type that records writes and gates the first Write on a channel the test closes). Join the two Barrier goroutines through a buffered done channel plus a select-timeout so a regression FAILS with a clear message rather than hanging the suite. Do not touch internal/acp/emitter.go in this task. Run the test and confirm the both-waiters subtest times out on current code (RED gate) while the lone-waiter subtest passes.
  </action>
  <verify>
    <automated>go test -race ./internal/acp/ -run TestTurnEmitterBarrierConcurrentWaiters -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestTurnEmitterBarrierConcurrentWaiters exists in internal/acp/emitter_test.go and its both-waiters subtest FAILS (deadline exceeded) on the pre-fix code
    - The waiter contexts in the test are context.Background()-class: no test-owned cancellation can satisfy the wait (grep the test body for context.WithCancel/context.WithTimeout applied to the Barrier argument returns no match)
    - The lone-waiter subtest passes on current code
    - TestTurnEmitterEmptyTurn is byte-identical to its pre-task state (git diff shows no change to it)
  </acceptance_criteria>
  <done>RED gate proven: the new regression fails on current code for exactly the lost-wakeup reason, with no ctx escape hatch in the waiter contexts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — per-generation broadcast wake in writeOut/Barrier</name>
  <files>internal/acp/emitter.go</files>
  <read_first>
  - internal/acp/emitter.go — writeOut (lines 297-308), Barrier (lines 261-285), the mu/wake field block (lines 128-131, 181), and the drain (lines 313-335) whose single-writer property this task must preserve
  - internal/acp/emitter_test.go — Task 1's regression, now the acceptance target
  </read_first>
  <behavior>
    - Every completed sink.Write wakes ALL currently-parked Barrier waiters (broadcast), each of which re-checks written >= target under mu and either returns or re-parks on the current generation's channel
    - A waiter that arrives after a broadcast parks on the fresh channel and is woken by the NEXT broadcast — no waiter stalls at terminal state, no waiter busy-spins
    - Stop() and caller-ctx escape paths behave exactly as before
  </behavior>
  <action>
  Replace the capacity-1 single-token wake with a per-generation broadcast, confined to the wake mechanism (the verifier's named fix: "swap-and-close the wake channel per generation under mu"):
  1. In writeOut, after the existing written++ under mu, atomically under the SAME critical section: close(t.wake) then t.wake = make(chan struct{}). Because the drain is writeOut's only caller, closes are serialized and never double-close.
  2. In Barrier, re-check written >= target under mu; when not done, snapshot wake := t.wake under the SAME critical section, unlock, then select on wake / ctx.Done / t.ctx.Done. A waiter that snapshotted before the close receives the closed channel and re-checks; one that snapshots after the swap parks on the fresh channel. This structure cannot spin: after a close fires, the next snapshot under mu always sees the post-swap channel.
  3. Initialize wake as an unbuffered make(chan struct{}) in NewTurnEmitter (capacity is meaningless for close-broadcast).
  4. Keep the enqueue/written counter arithmetic and both ctx escapes byte-identical; Pitfall 3 discipline holds (no mu held across any lane send or sink.Write).
  5. Update the Barrier and writeOut doc comments to describe the per-generation broadcast; remove the single-token handoff description.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acp/ && go vet ./internal/acp/</automated>
  </verify>
  <acceptance_criteria>
    - go test -race -count=1 ./internal/acp/ exits 0, including TestTurnEmitterBarrierConcurrentWaiters (both subtests), TestTurnEmitterPriority, TestTurnEmitterStall, TestTurnEmitterBarrier, TestTurnEmitterEmptyTurn, TestTurnEmitterProducerCtxAbort
    - grep -c "close(t.wake)" internal/acp/emitter.go returns at least 1 (the writeOut broadcast site)
    - go test -race -count=1 ./internal/runtime/ -run TestTurnEmitterEndToEnd passes (the cross-package e2e still proves single-order delivery)
    - go vet ./internal/acp/ is clean
  </acceptance_criteria>
  <done>Both concurrent waiters return on the same final frame; every pre-existing emitter invariant test stays green under -race; the second concurrent prompt can no longer hang.</done>
  <reversibility rating="reversible">The broadcast rewrite is confined to one mechanism in one file; flipping back (or to sync.Cond) is a code+test change with no persisted state, and the regression test pins the contract either way.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer goroutines → TurnEmitter lanes | runtime forwarders / background emitters enqueue frames; a wake bug strands them or their waiters |
| TurnEmitter drain → stdout sink | the single notification write path to the editor |
| caller ctx → Barrier | connection-lifetime Serve ctx must not be the only liveness mechanism |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-07-01 | DoS | TurnEmitter.Barrier (emitter.go:261-285) | high | mitigate | The CR-01 fix itself: per-generation broadcast wake so every concurrent waiter re-checks after each written frame; pinned by TestTurnEmitterBarrierConcurrentWaiters with no ctx escape (blocks on high per workflow.security_block_on) |
| T-16-07-02 | DoS | Barrier wake rewrite | medium | mitigate | Waiters block on a channel receive, never a spin loop — the snapshot-under-mu structure makes post-close re-checks re-park on the fresh generation; -race suite plus the existing soak task (mise emitter-soak) re-runnable |
| T-16-07-03 | DoS | lane overflow behavior | low | accept | Unchanged by this plan: D-01 block-never-drop and the D-03 stall detector already cover writer-wedge stalls (existing TestTurnEmitterStall, soak invariants) |
| T-16-07-SC | Tampering | package installs | low | accept | No package-manager installs in this plan (RESEARCH Package Legitimacy Audit: none; stdlib sync only) |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/acp/` — full package green including the new two-waiter regression
- `go test -race -count=1 ./internal/runtime/ -run TestTurnEmitterEndToEnd` — cross-package e2e ordering proof intact
- `ASSGUARD_EMITTER_SOAK=1 go test ./internal/acp/ -run TestTurnEmitterSoak -count=1 -timeout 10m` — optional eval-lane re-run; all five invariants (no-drop, fifo, no-leak, stall-fired, clean-close) must stay PASS with the broadcast wake
- Grep proof of mechanism: `grep -c "close(t.wake)" internal/acp/emitter.go` ≥ 1
</verification>

<success_criteria>
16-VERIFICATION gaps 1 and 2 are closed: a turn with zero streamed activity still emits zero frames and returns immediately (truth 5, empty-case preserved), and the session/prompt response is written after all that turn's queued notification frames for EVERY concurrent waiter — two simultaneous turn-end Barriers on one connection both complete (truth 6 under concurrency).
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-07-SUMMARY.md` when done
</output>
