---
phase: 16-acp-wire-foundation
plan: 05
type: execute
wave: 3
depends_on: ["16-03", "16-04"]
files_modified:
  - internal/acp/types.go
  - internal/acp/handlers.go
  - internal/acp/handlers_test.go
  - internal/acp/server.go
  - internal/acpserve/config_surface.go
  - internal/acpserve/config_test.go
  - internal/acpserve/acp_serve.go
  - internal/runtime/runtime.go
  - internal/session/session.go
autonomous: true
requirements: [ACP-08]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "initialize and session/new responses advertise the full v1.2 configOptions menu — model, tier, permissions.mode, compaction-threshold plus their _global/ twins — each in the v1 SessionConfigOption shape with an EFFECTIVE currentValue resolved through the precedence chain, never a static default (D-05, D-06, D-11)"
    - "session/set_config_option persists first (atomic 0600 layer write via the 16-04 primitive) then applies live: tier/model take effect on the running session's next model request; a write failure returns a typed JSON-RPC error with the effective state untouched (D-07); an inbound value equal to the option's currently-effective value (Zed re-pushes stored defaults every connection) is a logged idempotent no-op — no layer write, so a redundant client default can never churn the operator's file or promote a blob-derived effective value into persisted explicit config (D-10's fills-unset discipline extended to the set channel)"
    - "Invalid option values are typed-rejected with option key + violation detail in the error data; advertised-but-unhandled keys (permissions.mode until Phase 17, compaction-threshold until Phase 19) apply as logged pending-handler no-ops — accepted, logged, currentValue unchanged (D-09, D-05)"
    - "The initialize _meta blob is parsed schema-tolerantly: recognized keys fill unset slots only (explicit config always wins), unknown keys are retained verbatim (round-trip survival), and a config_option_update carrying the full set follows application (D-10)"
    - "A set_config_option racing initialize-time _meta application serializes through one apply path — atomic rename plus serialized apply yield no torn YAML and no half-applied state (edge-probe: concurrency)"
    - "After a mid-session model or tier change, the very next provider request carries the new model, sessions created later stamp it at construction, and a mid-turn change never tears an in-flight request (serialized apply behind the session's turn)"
  artifacts:
    - path: "internal/acp/types.go"
      provides: "ConfigOptionFrame (v1 SessionConfigOption shape: id/name/description/category/type with currentValue+options) and the config_option_update frame type"
      contains: "ConfigOptionFrame"
    - path: "internal/acp/handlers.go"
      provides: "handleSetConfigOption (typed reject, persist-then-apply via the injected surface, full-set response), advertisement builders on initialize/session-new responses, _meta blob reader"
      contains: "handleSetConfigOption"
    - path: "internal/acp/server.go"
      provides: "WithConfigSurface ServerOption injecting the acp-side ConfigSurface interface (wire-only: acp defines the interface, acpserve implements)"
      contains: "WithConfigSurface"
    - path: "internal/acpserve/config_surface.go"
      provides: "the ConfigSurface implementation: menu construction from modelrouting layers, effective-value resolution, scope routing (_global/ prefix → global layer), _meta fills-unset application, live apply through the runner seam"
      contains: "ConfigSurface"
    - path: "internal/runtime/runtime.go + internal/session/session.go"
      provides: "Runner.ApplyTurnModel(model) + Session.SetTurnModel — the serialized live-apply seam (subagentProfile per-dispatch-copy precedent)"
      contains: "ApplyTurnModel"
  key_links:
    - from: "internal/acp/handlers.go"
      to: "internal/acpserve/config_surface.go"
      via: "the set_config_option handler and the advertisement builders call the ConfigSurface interface injected by WithConfigSurface — acp stays free of modelrouting/providerfactory imports"
      pattern: "ConfigSurface"
    - from: "internal/acpserve/config_surface.go"
      to: "internal/providerfactory/config_write.go"
      via: "Set routes scoped option ids to WriteLayerOption on the project or global layer path, then triggers live apply"
      pattern: "WriteLayerOption"
    - from: "internal/acpserve/config_surface.go"
      to: "internal/runtime/runtime.go"
      via: "successful writes call runner.ApplyTurnModel so the changed model reaches live and future sessions"
      pattern: "ApplyTurnModel"
  prohibitions:
    - requirement_id: ACP-08
      category: privacy
      statement: "API keys and credential material MUST never appear as editor config options — the menu is fixed to the enumerated option ids; any option whose value resolution touches provider credentials is rejected at the surface"
    - requirement_id: ACP-08
      category: ownership
      statement: "Client-supplied default values (the initialize _meta blob) MUST never override explicit operator file config — blob keys fill unset slots only; the operator's files stay authoritative"
---

<objective>
Land the full ACP-08 wire surface: advertise the complete v1.2 configOptions menu with effective current values on initialize/new (and the shared builder ready for load/resume once Phase 18 wires replay), handle session/set_config_option with typed validation and persist-then-apply semantics, read the initialize _meta blob as fills-unset defaults, emit config_option_update on out-of-band changes, and make tier/model changes take effect live on the running session through a serialized runner seam.

Purpose: criterion 4 of the phase — Zed's settings UI shows the agent's options with true current values, and switching model or tier from the editor changes the very next request. The surface composes the 16-03 emitter/registry spine and the 16-04 writer without letting protocol types leak into config packages.
Output: wire shapes + handlers in internal/acp, ConfigSurface implementation + composition in internal/acpserve, ApplyTurnModel seam in runtime/session.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-CONTEXT.md
@.planning/phases/16-acp-wire-foundation/16-RESEARCH.md
@.planning/phases/16-acp-wire-foundation/16-PATTERNS.md
@.planning/phases/16-acp-wire-foundation/16-01-SUMMARY.md
@.planning/phases/16-acp-wire-foundation/16-03-SUMMARY.md
@.planning/phases/16-acp-wire-foundation/16-04-SUMMARY.md
@internal/acp/handlers.go
@internal/acp/server.go
@internal/acp/types.go
@internal/acpserve/acp_serve.go
@internal/providerfactory/config_write.go
@internal/providerfactory/provider_factory.go
@internal/modelrouting/config.go
@internal/runtime/runtime.go
@internal/session/session.go
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/acp/types.go` — `ConfigOptionFrame`, `ConfigOptionValue`, the `config_option_update` frame shape (v1 verbatim: id/name/description/category/type=currentValue+options; type key "select").
- `internal/acp/handlers.go` — `handleSetConfigOption`; `configOptionsFor` advertisement builder shared by initialize and session/new (and later load/resume); `_meta` blob parsing at initialize.
- `internal/acp/server.go` — `WithConfigSurface` option + the acp-side `ConfigSurface` interface (Options/Set/BlobDefaults — exact shape executor-frozen, kept minimal).
- `internal/acpserve/config_surface.go` — the implementation: menu from modelrouting layers (models list, tiers list, fixed pending options), effective currentValue resolution, `_global/` scope routing, fills-unset blob application, config_option_update emission, live apply calls.
- `internal/runtime/runtime.go` — `ApplyTurnModel(model string)`; `internal/session/session.go` — `SetTurnModel`.
- Menus locked (D-06 enumeration): `model` (select, options = sorted cfg.Models keys), `tier` (select, options = sorted cfg.Tiers keys), `permissions.mode` (select ungated|gated, pending Phase 17), `compaction-threshold` (select off|50|65|80|95, pending Phase 19), plus the four `_global/` twins.
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire shapes, advertisement, set_config_option core</name>
  <files>internal/acp/types.go, internal/acp/handlers.go, internal/acp/handlers_test.go, internal/acp/server.go</files>
  <behavior>
    - session/new response carries configOptions with eight entries (the four options and four _global/ twins) in v1 shape: id, name, description, category, type select with currentValue and options (D-06 enumeration from artifacts above; descriptions are short editor-facing strings — CONTEXT discretion).
    - Every advertised currentValue equals the value the surface's effective-resolution returns for that id at response time (test injects a surface with known values and asserts the frame fields verbatim).
    - set_config_option {sessionId, optionId, value} for a known, handled id (model, tier, and their twins): response is the FULL option set with the updated currentValue; the surface's Set was called with the exact id/value/scope.
    - Unknown optionId → RPCError CodeInvalidParams with Data carrying optionId and a violation string; invalid value for a known id (e.g. model not in the options list) → same typed shape with a value violation (D-09).
    - Surface write failure → typed RPCError (distinct from validation), and the surface's apply path was never reached (persist before apply — assert the Set call ordering with a failing-surface fake).
    - Advertised-but-unhandled ids (permissions.mode, compaction-threshold + twins): valid value → one structured pending-handler log line + full-set response with currentValue unchanged; invalid value → typed reject (validation still applies — never accept garbage into a pending slot).
    - The initialize response keeps agentCapabilities.loadSession false (truthful; Phase 18 flips it when replay lands).
  </behavior>
  <read_first>internal/acp/handlers.go (handleInitialize :36-58 and its initializeResponse struct — the nolint:tagliatelle response-shape template; handleSessionNew; handleSessionSetMode :218-222 no-op template; params parsing :66-74), internal/acp/types.go (frame struct conventions, RPCError), internal/acp/server.go (ServerOption pattern — WithTurnRunner; how handlers are registered in registerHandlers), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (SessionConfigOption schema shape; Zed set_config_option flow at acp.rs:1303-1391 — the client re-pushes its stored defaults each connection; the A6 menu; A7 scope decision — _global/ id-namespace), .planning/phases/16-acp-wire-foundation/16-PATTERNS.md (handlers.go section — typed-error convention)</read_first>
  <action>
RED: write handlers_test.go with a fake ConfigSurface (known menu, recording Set calls, injectable failure) covering the seven behavior cases; drive via the Server over pipes (the 16-03 serve-test fixtures). Commit failing.
GREEN: define ConfigOptionFrame and friends in types.go with verbatim v1 names and tagliatelle markers; note that v1's shape key for a select option is type.currentValue + type.options (a nested object), not flat fields — read the RESEARCH schema excerpt and mirror it exactly. Define the acp-side ConfigSurface interface (minimal: a way to get the full option set, apply a set, and apply blob defaults) and the WithConfigSurface ServerOption storing it on Server; absent surface → advertisements omit configOptions entirely and set_config_option returns the typed not-available error (degrade, never crash — the acpserve-less test setups keep working). Register handleSetConfigOption for the v1 method name session/set_config_option; implement validation-first-then-Set ordering (D-07: the handler validates shape, the surface persists, then applies; a surface error maps to a typed RPCError). Build the advertisement via a shared builder called from handleInitialize (Zed re-reads options at initialize) and handleSessionNew, both reading the surface; keep the builder in one function so Phase 18's load/resume responses reuse it unchanged. Pending-handler no-ops: the SURFACE decides handled-ness (it owns the menu semantics); the handler only relays — put the pending log + unchanged-value response logic in the surface fake's contract so production and test agree. Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/acp/ -run 'TestSetConfigOption|TestConfigAdvertise' -count=1 && go test ./internal/acp/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- TestConfigAdvertise asserts eight v1-shaped entries with injected currentValues; TestSetConfigOption covers valid set, unknown id, invalid value, write failure, and both pending no-op branches.
- The write-failure case asserts Set was called and apply was not (ordering via the recording fake).
- The pending no-op asserts one log line and an unchanged currentValue in the full-set response.
- initializeResponse still reports loadSession false; `grep -c 'session/set_config_option' internal/acp/handlers.go` is at least 1 (handler registered under the v1 name).
- Existing acp tests green (no-surface degrade path covered by one fixture).
  </acceptance_criteria>
  <done>The wire half of ACP-08 exists: a truthful, effective-valued advertisement and a validated persist-then-apply set_config_option with typed errors and honest pending no-ops.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConfigSurface implementation — scope routing, _meta blob, config_option_update</name>
  <files>internal/acpserve/config_surface.go, internal/acpserve/config_test.go, internal/acp/handlers.go</files>
  <behavior>
    - Menu construction from real layers: with defaults-only config, model options list the embedded default models and tier options list the embedded tiers; currentValue for model is the resolved model of the current session tier through project>global>embedded precedence (the day-1 slice of D-12's full chain — session override arrives with Phase 20's /model, time-window resolution already lives inside modelrouting's resolver), for tier it is Config.SessionTier, for the pending options their fixed defaults (ungated, 80).
    - _meta blob at initialize: writing explicit tiers.heavy.model into the project layer then sending a blob with model=other leaves the effective model at the file's value; with the key unset, the blob value applies in-memory; one config_option_update frame with the full set follows application; unknown blob keys are retained verbatim and never executed (round-trip survival — assert the stored serve state still holds them byte-identical).
    - Scope routing: set_config_option with optionId "_global/model" writes tiers.<current tier>.model into the GLOBAL layer file; un-prefixed writes the project layer; a project write leaves the global file byte-identical (D-08).
    - set_config_option valid+explicit persists: the project layer file on disk contains the merged YAML with the written key (assert by re-loading through modelrouting.Load).
    - Blob application and an interleaved set_config_option serialize: a test firing both concurrently asserts the final file parses, exactly one write wins per key deterministically (last serialized apply), and no torn YAML (concurrency edge probe).
    - Idempotent re-push (D-10 guard on the set channel): set_config_option arriving with a value equal to the option's currently-effective value — including when that effective value came from the _meta overlay — performs NO layer write and NO overlay change: one structured log line, full-set response, no config_option_update (nothing changed); the on-disk layer file stays byte-identical.
  </behavior>
  <read_first>internal/acpserve/acp_serve.go (Run composition — where the surface constructor slots in; the profile/schedCfg handles available), internal/providerfactory/config_write.go (WriteLayerOption contract — caller serializes), internal/providerfactory/provider_factory.go (GlobalConfigPath/ProjectConfigPath), internal/modelrouting/load.go (Load precedence; SessionTier from 16-04), internal/modelrouting/config.go, internal/acp/handlers.go (Task 1 surface contract), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (D-10 Reconciliation section — blob fills-unset in-memory, never persisted; A7 _global/ namespace; config_option_update full-set semantics)</read_first>
  <action>
Implement the ConfigSurface in internal/acpserve/config_surface.go: construct from the serve's workDir + a resolution function over modelrouting.Load(global, project) (cheap enough at advertisement/write time; serve holds the paths). Menu per the locked D-06 enumeration; option ids are the literal strings from the artifacts section and their `_global/` prefixed twins; category assignments: model→model, tier→model_config, permissions.mode→mode, compaction-threshold→_custom. Blob application (initialize _meta): recognized keys = the eight ids; fills-unset semantics per D-10's reconciliation — apply to an in-memory overlay used by effective resolution, never persisted (the client re-pushes per connection; persisting would invert ownership); emit config_option_update with the full set through the emitter foreground handle (out-of-band change per v1); retain unknown keys in the overlay state verbatim. Serialization: a single mutex over mutate operations (blob apply and Set) so the caller-serializes contract of WriteLayerOption holds against racing initialize traffic. Set: validate against the menu (unknown id, invalid value per D-09), route scope by prefix, then apply the D-10 idempotence guard on this channel — resolve the option's currently-effective value (files plus overlay) and when the inbound value equals it, log one structured line and return the refreshed set WITHOUT calling WriteLayerOption (a redundant Zed defaults re-push must not churn the layer file nor promote a blob-derived effective value into persisted explicit config); a differing value persists unconditionally per D-12 — editor writes are simply another writer into the project/global layers, and D-10 itself classes editor-set options as explicit config, so the residual edge (an operator hand-edit re-clobbered by Zed's remembered default) is accepted under that locked reading rather than gated by provenance tracking. Otherwise WriteLayerOption to the addressed layer, update the overlay, trigger the apply hook (Task 3 wires it to the runner), return the refreshed set. Pending ids: log the pending-handler line, do not persist, return the set unchanged. Write config_test.go with the six behavior cases over a temp-dir serve (no live provider — fake MakeProvider where needed).
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestConfigSurface|TestMetaBlob|TestScopeRouting|TestSetIdempotent' -count=1</automated>
  </verify>
  <acceptance_criteria>
- All six behavior cases pass; the blob precedence case proves explicit-file-wins in both directions and the idempotent re-push case proves the layer file stays byte-identical (no WriteLayerOption call, one log line, no config_option_update).
- The concurrency case ends with a parseable file and deterministic per-key winner.
- config_option_update emitted exactly once per blob application carrying the full set.
- `_global/` routing verified by byte-comparing the untouched counterpart layer.
- `go vet ./internal/acpserve/` clean; existing acpserve serve tests green.
  </acceptance_criteria>
  <done>D-08/D-10 are real on disk and in-memory: two editor-editable layers, an ownership-respecting blob channel, and a serialized mutation path with full-set update notifications.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Live apply — runner seam and turn serialization</name>
  <files>internal/acpserve/config_surface.go, internal/acpserve/acp_serve.go, internal/acpserve/config_test.go, internal/runtime/runtime.go, internal/session/session.go</files>
  <behavior>
    - Set(model=X) on a serve with a live session persists, then runner.ApplyTurnModel(X) causes the session's NEXT provider request to carry model X (assert via a recording fake provider capturing the shaped params — shaper stamps Model from the profile).
    - Set(tier=light) where tiers.light.model=Y on the same provider: next request carries Y; tiers.X bound to a DIFFERENT provider: one loud structured stderr warning and the model stays unchanged (the resolveSubagentModel precedent — cross-provider tier switches degrade loudly, they do not rewire the live provider).
    - A Set arriving mid-turn is applied between turns: the in-flight request keeps its model, the next request carries the new one (no torn stamp) — assert by issuing Set while a slow fake provider holds a request open.
    - Sessions created after the change stamp the effective model at construction.
    - The composition in Run binds the surface to the runner (apply hook) and injects it into the Server via WithConfigSurface — the full serve path from wire to provider request is one integration test.
  </behavior>
  <read_first>internal/runtime/runtime.go (sessionFor construction stamping prof :1000-1075 — the fixed profile copy and SubagentModel per-dispatch precedent; SetEmitter pattern :1327-1329 for adding a setter; where turns serialize per session), internal/session/session.go (the profile copy + model stamping sites; the session's turn serialization point — find the mutex/turn gating the executor can hook for between-turns apply), internal/shaper/shaper.go (:115 Model stamping), internal/acpserve/acp_serve.go (Run composition order — surface construction after schedCfg load, before srv construction), internal/runtime/runner_battery_test.go or integration_test.go (NewRunner fixture with a recording fake provider)</read_first>
  <action>
Add Runner.ApplyTurnModel(model string) error: updates the runner's effective-model state under its mutex, then walks live sessions calling their new SetTurnModel. Add Session.SetTurnModel(model string): swaps the model on the session's profile copy under the session's existing turn serialization so a mid-turn call lands between requests (read session.go to find the precise lock/turn gate — the goal is never mutating a profile already handed to an in-flight request; if the profile is copied per dispatch as the SubagentModel precedent suggests, stamping the session's source-of-truth copy is sufficient and the per-dispatch copy naturally serializes). sessionFor stamps from the effective-model state at construction so future sessions follow. In acpserve.Run: construct the ConfigSurface after the schedCfg/profile load (it needs the layer paths + resolver) with its apply hook calling runner.ApplyTurnModel after a successful tier/model persist (resolve the tier's model through Load before calling; guard the cross-provider case with the loud-warning no-op), inject via WithConfigServer option into acp.NewServer, and keep the existing degrade-loudly style for construction failure. Extend config_test.go with the live-apply integration case over a real Runner + recording fake provider.
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run 'TestLiveModelApply|TestTierSwitch' -count=1 && go test -race ./internal/runtime/ -run TestApplyTurnModel -count=1 && go test ./internal/session/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- Next-request model change proven for both the model option and the same-provider tier option.
- Cross-provider tier switch logs one warning and keeps the current model.
- Mid-turn Set waits: in-flight request keeps its model, the following one changes.
- New sessions stamp the effective model at construction (assert on a session created post-change).
- The Run composition wires surface→runner→server with no manual step; `mise ci` green.
  </acceptance_criteria>
  <done>D-05's day-1 handlers are live end to end: the editor's model/tier switch changes the next request the agent sends, with precedence, persistence, and serialization all honoring the locked decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→config surface | untrusted option ids/values arrive over the wire and target operator files |
| surface→operator files | writes must stay within the whitelisted key set and layer files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-13 | Elevation | arbitrary config-key injection via optionId | high | mitigate | Menu-whitelist validation before any write (D-09); scope routing accepts only the _global/ prefix on whitelisted ids; unknown ids typed-rejected — the writer is unreachable with an arbitrary keyPath |
| T-16-14 | Tampering | blob overriding operator config | high | mitigate | D-10 fills-unset in-memory only (never persisted); explicit-file-wins asserted in both directions; prohibition above pins the ownership rule |
| T-16-15 | Information Disclosure | option values leaking configuration secrets | high | mitigate | Menu is fixed to non-credential options; API keys env/file-only per the phase's out-of-scope table — the surface has no code path that reads or writes credential fields |
| T-16-16 | Tampering | half-applied live state | medium | mitigate | Persist-then-apply ordering with typed errors and unchanged state on failure (D-07); serialized apply (T-16-10's atomic writer underneath) |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs; legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/acp/ ./internal/acpserve/ ./internal/runtime/ ./internal/session/ -count=1` green.
- `mise ci` green.
- The 16-06 simulator drives the whole surface as one story; live-editor confirmation happens there.
</verification>

<success_criteria>
ACP-08's agent half is complete in-phase: full truthful advertisement with effective values, typed validated writes through atomic persistence into the two operator-owned layers, an ownership-respecting client-defaults channel, and live model/tier switching that reaches the next provider request — with the pending options honestly logged rather than silently swallowed.
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-05-SUMMARY.md` when done
</output>
