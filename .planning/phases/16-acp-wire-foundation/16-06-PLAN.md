---
phase: 16-acp-wire-foundation
plan: 06
type: execute
wave: 4
depends_on: ["16-05"]
files_modified:
  - internal/acpserve/simulator_e2e_test.go
  - internal/acp/emitter_soak_test.go
  - .mise.toml
autonomous: false
requirements: [ACP-03, ACP-08]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A deterministic Zed-client simulator drives the full phase surface green in one test run with no env flags and no live model: initialize (capabilities advertised + _meta defaults) → session/new (configOptions present) → prompt streaming tool_call/plan/agent_message_chunk frames in emission order → set_config_option persisting and switching the next request's model → cancellation cascading to a cancelled prompt response"
    - "An adversarial multi-emitter soak (minutes, env-gated out of ci) asserts the standing invariants beyond what the seconds-scale ci stress proves: total order, zero drops, no leaks, no deadlocks under chaotic producers and a stuttering writer (D-04)"
    - "The live-Zed operator checkpoint is dispositioned honestly — OPERATOR-CONFIRMED or PENDING-OPERATOR-CONFIRMATION recorded in the SUMMARY, never a silent pass (ROADMAP criteria 1 and 4)"
  artifacts:
    - path: "internal/acpserve/simulator_e2e_test.go"
      provides: "the scripted Zed-client simulator: pipe client asserting frames/responses across the whole phase surface"
      contains: "TestZedSimulatorE2E"
    - path: "internal/acp/emitter_soak_test.go"
      provides: "TestTurnEmitterSoak — env-gated (ASSGUARD_EMITTER_SOAK=1) adversarial soak"
      contains: "TestTurnEmitterSoak"
    - path: ".mise.toml"
      provides: "the emitter-soak task invoking the env-gated test with a generous timeout"
      contains: "emitter-soak"
  key_links:
    - from: "internal/acpserve/simulator_e2e_test.go"
      to: "internal/acpserve/acp_serve.go"
      via: "the simulator runs the real Run composition over pipes with a fake provider — every seam this phase built is exercised in one process"
      pattern: "Run\\("
  prohibitions: []
---

<objective>
Prove the phase as one story, then hand it to a human: a deterministic Zed-client simulator exercises the full wire surface end-to-end (the RESEARCH fallback for a truth-results gap), an adversarial soak stresses the ordering invariants beyond ci scale, and a blocking operator checkpoint confirms the live-editor experience — native tool cards, plan panel, config options in Zed's settings UI.

Purpose: ROADMAP criteria 1 and 4 are human-observable ("renders natively in Zed", "Zed's settings UI shows the agent's options"); automation can prove the wire is correct, only the operator can confirm the rendering. The checkpoint disposition flows into the WINDOWS ledger exactly as Phase 15's did.
Output: simulator + soak tests, mise task, operator-confirmed (or pending) SUMMARY.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-CONTEXT.md
@.planning/phases/16-acp-wire-foundation/16-RESEARCH.md
@.planning/phases/16-acp-wire-foundation/16-01-SUMMARY.md
@.planning/phases/16-acp-wire-foundation/16-03-SUMMARY.md
@.planning/phases/16-acp-wire-foundation/16-05-SUMMARY.md
@.planning/phases/15-internal-runtime-carve-step-0/15-07-PLAN.md
@internal/acpserve/acp_serve.go
@internal/acpserve/serve_test.go
@internal/acp/emitter.go
@.mise.toml
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/acpserve/simulator_e2e_test.go` — `TestZedSimulatorE2E`: the scripted client (a small JSON-RPC client helper speaking newline frames over io.Pipe, asserting in v1 vocabulary).
- `internal/acp/emitter_soak_test.go` — `TestTurnEmitterSoak` (skips unless ASSGUARD_EMITTER_SOAK=1; minutes of chaos; final invariant assertions).
- `.mise.toml` — `emitter-soak` task.
- Operator checkpoint disposition in `16-06-SUMMARY.md` (OPERATOR-CONFIRMED / PENDING-OPERATOR-CONFIRMATION).
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic Zed-client simulator E2E</name>
  <files>internal/acpserve/simulator_e2e_test.go</files>
  <behavior>
    - Script: client sends initialize advertising clientCapabilities (elicitation.form) plus an _meta blob with one default; asserts the initialize response (protocolVersion 1, agentCapabilities.loadSession false) and that NO probe was sent (advertised); asserts the follow-up config_option_update carries the blob-applied full set.
    - session/new: asserts configOptions with the eight-entry menu and effective currentValues.
    - session/prompt with a fake provider turn that emits a ToolCall, a TodoWrite call, and message chunks: asserts stdout frames in emission order — tool_call, plan (not a second tool card for TodoWrite), agent_message_chunk — then the prompt response (updates-before-response).
    - session/set_config_option (model): asserts the persisted layer file (re-Load) and the full-set response, then a second prompt whose captured provider request carries the new model.
    - session/cancel mid-turn: asserts the turn's pending state resolves, the prompt response arrives with a cancelled stop reason, and any registry cascade frame precedes the response.
    - The whole script is deterministic: fixed seeds/timings where any choice exists, no env flags, no network.
  </behavior>
  <read_first>internal/acpserve/serve_test.go (existing serve-over-pipes fixtures and the stub-runner pattern to extend with a richer scripted provider), internal/acpserve/acp_serve.go (Run composition; where the fake provider injects via RunnerConfig.MakeProvider), internal/acp/handlers.go (initialize/session-new/prompt/set_config_option handler contracts from 16-03/16-05), internal/acp/types.go (frame vocabulary), internal/runtime/runner_battery_test.go (fake provider recording shaped params), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (Validation Architecture — the test map this battery completes; Zed behavior notes for script fidelity)</read_first>
  <action>
Build a small client helper in the test file (send request/notification, read frames with matching by id, timeouts guarding every wait) and script the five behavior cases as subtests sharing one serve instance over pipes (t.TempDir workDir, defaults-only config, recording fake provider from the runner fixtures). The TodoWrite-vs-tool-card assertion reuses the 16-01 fixture builders. Assert v1 vocabulary only (id/plan/tool_call — the Pitfall-7 drift guard at the highest level). Keep the script readable as documentation: each subtest name is one sentence of the story; the SUMMARY cites this file as the phase's self-running demonstration.
  </action>
  <verify>
    <automated>go test -race ./internal/acpserve/ -run TestZedSimulatorE2E -count=1 && go test ./internal/acpserve/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- All five subtests pass under -race in one run with zero flakiness across three consecutive invocations (`-count=3` on the -run filter).
- The ordering assertions cover updates-before-response and cascade-before-response explicitly.
- The set_config_option subtest proves persistence (re-Load) AND live apply (next request's model) in one story.
- No env flags, no sleeps longer than guard timeouts, deterministic throughout.
  </acceptance_criteria>
  <done>The phase surface is provably one coherent wire story, runnable by anyone with go test — the fallback proof standing in for the unfetchable truth-results.</done>
</task>

<task type="auto">
  <name>Task 2: Adversarial soak + mise task</name>
  <files>internal/acp/emitter_soak_test.go, .mise.toml</files>
  <read_first>internal/acp/emitter.go (the emitter API surface: handle classes, barrier, stall threshold knob, counters), internal/acp/emitter_test.go (the ci-scale stress to scale up), .mise.toml (task conventions — the eval-gate env-flag pattern ASSGUARD_EVAL_GATE; ci task composition so the soak stays OUT of ci)</read_first>
  <action>
Write TestTurnEmitterSoak gated on ASSGUARD_EMITTER_SOAK=1 (t.Skip otherwise, exactly the eval-gate pattern): for a configurable duration (default 2 minutes, overridable via a second env var for longer nightly runs), run chaotic producers — N goroutines alternating foreground/background classes at random (fixed seed) with burst pauses, some flipping a fake writer between fast and stalled modes, some cancelling their ctx mid-block, plus periodic barrier calls — then assert the closing invariants: total frames written equals total successfully enqueued (nothing dropped), background subsequences are FIFO, no goroutine leak (runtime.NumGoroutine settles after drain), stall counter is positive (the detector actually fired during the chaos), and the Writer closed cleanly. Add the emitter-soak task to .mise.toml invoking the env-gated test with a generous timeout; verify it does NOT join the ci task's test invocation (env unset there by construction). Run the soak once at the default duration locally and record the result line in the SUMMARY.
  </action>
  <verify>
    <automated>ASSGUARD_EMITTER_SOAK=1 go test ./internal/acp/ -run TestTurnEmitterSoak -count=1 -timeout 10m && mise run emitter-soak && mise ci</automated>
  </verify>
  <acceptance_criteria>
- The soak passes at the default duration with all five closing invariants asserted.
- `mise run emitter-soak` works standalone; `mise ci` completes without the soak (untimed-out).
- The test skips (not fails) with the env var unset.
- The SUMMARY records one real soak run's duration and invariant summary.
  </acceptance_criteria>
  <done>D-04's deeper adversarial proof exists as a standing, discoverable command — minutes of chaos standing behind the seconds-scale ci stress.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator live-Zed confirmation</name>
  <files>.planning/phases/16-acp-wire-foundation/16-06-SUMMARY.md</files>
  <read_first>.planning/phases/15-internal-runtime-carve-step-0/15-07-PLAN.md (the checkpoint house pattern — what-built/how-to-verify/resume-signal and the OPERATOR-CONFIRMED / PENDING-OPERATOR-CONFIRMATION disposition markers), .planning/ROADMAP.md (Phase 16 criteria 1 and 4 — the exact human-observable claims under test)</read_first>
  <what-built>
The phase's full wire surface: ordered TurnEmitter streaming tool_call/plan/message frames, the outbound request registry with capability negotiation, the extended transcript kinds, and the ACP-08 config surface (advertisement, set_config_option, live model/tier apply). Automation already proved the wire correct (Task 1 simulator, Task 2 soak, standing mise ci). What remains is human judgment: whether Zed renders it natively and shows the options truthfully — ROADMAP criteria 1 and 4.
  </what-built>
  <how-to-verify>
Spawn the agent from Zed over ACP and run a prompt that exercises a file-editing tool, then confirm each item: (a) a native tool card renders with a live diff (tool_call streaming through the emitter); (b) a TodoWrite-driven plan panel updates as the turn runs; (c) message tokens stream (never a full-turn opaque spinner); (d) the agent's config options appear in Zed's settings UI with truthful current values; (e) switching the model option changes the agent's next request (observable via the request log or changed model behavior). Explicitly NOT expected yet: live thought-chunk rendering, which arrives with Phase 21's provider thinking work (PAR-05) — its absence is not failure.
  </how-to-verify>
  <resume-signal>
Operator replies with the confirmation outcome (all items pass, or names the failing items). Record the outcome in the SUMMARY with the disposition markers OPERATOR-CONFIRMED or PENDING-OPERATOR-CONFIRMATION; a pending disposition carries into the WINDOWS ledger exactly as 15-07's did — never silently passed.
  </resume-signal>
  <action>
Pause autonomous execution and surface the blocking human checkpoint (gate: blocking — auto-advance does not clear it; the operator's reply resumes). Present what-built and how-to-verify verbatim, including the thought-chunk exclusion note so the operator does not read its absence as failure. Record the outcome in the SUMMARY with the disposition marker OPERATOR-CONFIRMED or PENDING-OPERATOR-CONFIRMATION as the first token of its own line — the anchored verify grep counts line-initial markers only, so prose mentions of the markers elsewhere in the SUMMARY never inflate the count — before closing the phase.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^(PENDING-OPERATOR-CONFIRMATION|OPERATOR-CONFIRMED)' .planning/phases/16-acp-wire-foundation/16-06-SUMMARY.md)" -eq 1 ]</automated>
  </verify>
  <acceptance_criteria>
- The SUMMARY carries exactly one disposition marker (OPERATOR-CONFIRMED or PENDING-OPERATOR-CONFIRMATION), recorded as the first token of its own line — the anchored verify grep enforces the count and prose mentions never match.
- The checklist in the SUMMARY names all five positive expectations plus the thought-chunk exclusion.
- If PENDING: the WINDOWS ledger (PROJECT.md or the roadmap notes section 15-07 used) gains a Phase-16 entry.
  </acceptance_criteria>
  <done>The phase's human-observable claims are either confirmed by the operator or explicitly carried as pending — the honest close for criteria 1 and 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator→serve | the scripted client is a stand-in for a hostile-capable editor; the soak's chaotic producers stress the shared emitter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-17 | DoS | latent deadlocks/leaks invisible at ci scale | medium | mitigate | The soak's closing invariants (no leak, no drop, clean close, stall-detector-fired) are the detector; D-04's eval-suite placement |
| T-16-18 | Repudiation | live-editor claims asserted without an operator | medium | mitigate | The blocking checkpoint plus the two-valued disposition markers make an unconfirmed claim impossible to record silently |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (test code + mise task only); legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/acp/ ./internal/acpserve/ -count=1` green.
- `mise run emitter-soak` green once; `mise ci` green.
- SUMMARY carries the checkpoint disposition.
</verification>

<success_criteria>
The phase closes with layered proof: deterministic whole-surface automation anyone can run, minutes-scale adversarial soak standing behind the ci stress, and an operator disposition on the two claims only a human can judge — with nothing silently passed.
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-06-SUMMARY.md` when done
</output>
