---
phase: 16-acp-wire-foundation
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - internal/acp/request_registry.go
  - internal/acp/request_registry_test.go
  - internal/acp/server.go
  - internal/acp/server_test.go
  - internal/acp/handlers.go
  - internal/acp/types.go
  - internal/acp/metrics.go
autonomous: true
requirements: [ACP-03]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An id'd outbound request written to the client receives its response matched by id while notification frames stream concurrently — resolution happens under the registry's own mutex, never the writer mutex (ROADMAP criterion 3)"
    - "An unanswered request degrades loudly per D-14: timeout → exactly ONE retry with a fresh window → fallback to plain behavior with a structured stderr log {request id, method, elapsed}; an error response with code -32800 resolves immediately as cancelled with no retry"
    - "Turn death resolves a pending entry as cancelled AND emits the $/cancel_request {requestId} notification through the emitter's foreground lane — the documented cascade (D-19)"
    - "Inbound response frames are intercepted before handler dispatch: a response frame gets no response, zero spurious method-not-found errors, and an unknown-id response produces one structured stderr log then drops (Pitfall 1)"
    - "Capability is negotiated once at initialize — clientCapabilities advertisement read first, elicitation probe sent only when absent/ambiguous, bounded by the FAST-CONTROL class — and the result is sticky for the connection: no mid-session re-probe, no flapping (D-13, D-18)"
    - "Two timeout classes exist: FAST-CONTROL ~10s and HUMAN-ASK mirroring session.DefaultAskTimeout (10 min) — a human-scale ask never expires while a person is thinking (D-17, pinned by a cross-package equality test)"
    - "Registry telemetry (probes, timeouts, fallbacks, cancels, writer stalls from 16-01) is visible as atomic counters with a snapshot accessor and structured stderr lines — no OTel, no new deps (D-16)"
  artifacts:
    - path: "internal/acp/request_registry.go"
      provides: "Registry: Call (UUID v4 string ids both directions, timeout classes, retry-once-then-fallback), Deliver (buffered(1) resolution channels), ResolveCancelled + $/cancel_request cascade, ctx-aware waiting, shutdown drain"
      contains: "type Registry"
    - path: "internal/acp/server.go"
      provides: "Serve response interception before the dispatch branch + registry/capability fields on Server"
      contains: "registry.Deliver"
    - path: "internal/acp/metrics.go"
      provides: "atomic counters family + Snapshot() for later /status surfacing"
      contains: "Snapshot"
    - path: "internal/acp/request_registry_test.go"
      provides: "TestRegistryConcurrentResolve / TestRegistryTimeoutFallback / TestRegistrySyntheticCancel / TestServeResponseRouting / TestInitializeProbe"
      contains: "TestRegistryConcurrentResolve"
  key_links:
    - from: "internal/acp/server.go"
      to: "internal/acp/request_registry.go"
      via: "Serve checks id-set + empty-method + result-or-error BEFORE the msg.ID == nil branch and calls Deliver; Serve ctx cancellation drains pending entries (Pitfall 8)"
      pattern: "Method == \"\""
    - from: "internal/acp/request_registry.go"
      to: "internal/acp/emitter.go"
      via: "outbound requests write directly through the Writer (id'd frames, unconstrained ordering) while the synthetic-cancel $/cancel_request notification rides the emitter foreground lane so the turn-end barrier orders it before the prompt response"
      pattern: "cancel_request"
  prohibitions: []
---

<objective>
Build the outbound request registry: id'd JSON-RPC requests with a concurrent pending-response map, D-14's loud degradation ladder (timeout → one retry → fallback), D-19's synthetic-cancel cascade, response interception in Serve, and the D-13/D-18 initialize-time capability probe with sticky degradation. This is the transport every Phase 17-23 client-ask rides.

Purpose: criterion 3 of the phase (id'd request matched under concurrent notification traffic) and the elicitation/permission asks of later phases both fail catastrophically if response routing or timeout policy is wrong — this plan proves them under -race before any consumer exists.
Output: internal/acp/request_registry.go (+test), Serve interception, capability probe, metrics family.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-acp-wire-foundation/16-CONTEXT.md
@.planning/phases/16-acp-wire-foundation/16-RESEARCH.md
@.planning/phases/16-acp-wire-foundation/16-PATTERNS.md
@.planning/phases/16-acp-wire-foundation/16-01-SUMMARY.md
@internal/acp/server.go
@internal/acp/handlers.go
@internal/acp/types.go
@internal/acp/framer.go
@internal/session/ask.go
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `internal/acp/request_registry.go` — `Registry` with `Call(ctx, method string, params any, class TimeoutClass)`, `Deliver(msg *Message)`, `ResolveCancelled(id string)`, shutdown drain; UUID v4 id generation generalized from newSessionID.
- `internal/acp/server.go` — response interception in `Serve`; registry + capability-state fields.
- `internal/acp/handlers.go` — `uuidV4` generalization; `handleInitialize` reads clientCapabilities and probes when absent; fast no-op handler for inbound `$/cancel_request` (Assumption A8).
- `internal/acp/types.go` — `CodeRequestCancelled = -32800` constant, method-name strings.
- `internal/acp/metrics.go` — counter family (probes/timeouts/fallbacks/cancels/stalls) + `Snapshot()`.
</artifacts_this_phase_produce>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry core — pending map, timeout ladder, synthetic-cancel cascade</name>
  <files>internal/acp/request_registry.go, internal/acp/request_registry_test.go, internal/acp/handlers.go, internal/acp/types.go</files>
  <behavior>
    - Call(ctx, "elicitation/create", params, FAST-CONTROL) enqueues an outbound id'd frame through the Writer and blocks; a Deliver of a Message with the same id resolves the waiter with that Message — while a concurrent goroutine streams notification frames, under -race, with no writer-mutex hold on the resolution path (TestRegistryConcurrentResolve).
    - Timeout ladder: with FAST-CONTROL shortened for test speed, no answer → the frame is re-sent once (same id, fresh window), still no answer → fallback error + one structured stderr log line carrying the id, method, and elapsed time; the frame is sent exactly twice (TestRegistryTimeoutFallback).
    - A Deliver carrying Error.Code -32800 resolves immediately as cancelled with zero retries.
    - ResolveCancelled(id): the waiter receives a cancelled resolution AND exactly one $/cancel_request notification with params {requestId: id} is emitted through the emitter foreground lane (TestRegistrySyntheticCancel — D-19 cascade).
    - Caller ctx cancellation mid-Call resolves cancelled and emits the same cascade (turn-death semantics: the caller's ctx is the turn ctx).
    - Shutdown drain: cancelling the Serve ctx releases all pending waiters (cancelled resolution, cascade suppressed — the client is gone) with no goroutine leak under -race and no write-after-close on the Writer (Pitfall 8).
    - HUMAN-ASK class default equals session.DefaultAskTimeout exactly (cross-package test pins the equality; acp does not import session in production code — mirror the constant locally per PATTERNS).
  </behavior>
  <read_first>internal/acp/server.go (sessionState own-mutex style :69-91, handlerWG, Serve ctx loop :160-200), internal/acp/handlers.go (newSessionID :227-239 — generalize to uuidV4 and update newSessionID to call it), internal/acp/types.go (Message :33-40, RPCError, code constants), internal/session/ask.go (:36 DefaultAskTimeout — the mirror target), internal/acp/framer.go (Writer.Write blocking semantics, Close discipline), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (Pattern 2 shim, the -32800 cascade contract section, Pitfalls 1/8), .planning/phases/16-acp-wire-foundation/16-PATTERNS.md (request_registry.go section)</read_first>
  <action>
RED: write request_registry_test.go covering the seven behavior cases (inject a fake clock or settable timeout constants so the ladder runs in milliseconds; use a pipe-backed Writer + capture stderr via the logger seam). Commit failing.
GREEN: implement Registry per RESEARCH Pattern 2 — map[string]*pendingEntry under the registry's OWN mutex (never the writer's), each entry holding a buffered(1) resolution channel so Deliver never blocks on a slow or vanished waiter; ids are UUID v4 strings (D-15) generated by uuidV4, the generalized newSessionID implementation (keep newSessionID as a thin wrapper so session ids keep their shape; no new dependency). Outbound request frames go through the Writer directly (id'd frames are not notification-ordered); the cascade notification goes through the emitter foreground handle so the turn-end barrier orders it before any prompt response. Implement the ladder per D-14: FAST-CONTROL default 10s, HUMAN-ASK default mirroring session.DefaultAskTimeout (local constant + the equality pin test), timeout → one retry (same id — the client may still answer the first send; a late answer to either send resolves the single entry) → fallback typed error + structured log; -32800 = immediate cancelled, no retry; caller-ctx cancel = cancelled + cascade; shutdown drain cancels all pending without cascade emission (client gone — writing to a closed Writer is the Pitfall-8 failure mode). Define CodeRequestCancelled = -32800 and the $/cancel_request method string in types.go. Commit green.
  </action>
  <verify>
    <automated>go test -race ./internal/acp/ -run 'TestRegistryConcurrentResolve|TestRegistryTimeoutFallback|TestRegistrySyntheticCancel|TestRegistryShutdown' -count=1</automated>
  </verify>
  <acceptance_criteria>
- All four registry tests pass under -race with the concurrent-streaming case interleaving notifications around resolution.
- The timeout test asserts the outbound frame count is exactly two (initial + single retry) and one structured log line.
- The -32800 case resolves cancelled with exactly one outbound frame.
- The HUMAN-ASK equality test compiles against session.DefaultAskTimeout and passes.
- internal/acp/handlers.go exposes uuidV4 and newSessionID delegates to it; `go vet ./internal/acp/` clean.
  </acceptance_criteria>
  <done>The registry primitive is complete and raced: id'd requests resolve by id under concurrent traffic, degrade loudly after one retry, cascade $/cancel_request on turn death, and drain cleanly at shutdown.</done>
  <reversibility rating="costly">D-19 synthetic-cancel resolution: once the cascade is shipped, later phases (17+) call ResolveCancelled as a load-bearing contract; CONTEXT marks this decision costly, and the retry/timeout semantics are observable by real editor clients.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve response interception + inbound cancel no-op</name>
  <files>internal/acp/server.go, internal/acp/server_test.go</files>
  <behavior>
    - An inbound frame with id set, method absent, and result or error present is delivered to the registry and produces ZERO outbound frames (no method-not-found reply — Pitfall 1; verified by asserting the stdout side stays empty).
    - An inbound response with an unknown id produces exactly one structured stderr log naming the id and is then dropped.
    - An inbound $/cancel_request notification (params {requestId}) is handled as a fast no-op: logged, no response frame (it is a notification), no error (Assumption A8 — the agent never cancels client-initiated requests mid-flight in v1.2; the log records the event for diagnosis).
    - The existing dispatch behavior for requests and notifications is unchanged (full server_test suite green).
  </behavior>
  <read_first>internal/acp/server.go (Serve dispatch :160-200 — insert before the msg.ID == nil branch at :180-187; safeDispatchInline), internal/acp/server_test.go (existing serve test fixtures — pipe-backed in/out, frame capture), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (Pattern 2 shim, Pitfall 1)</read_first>
  <action>
Add the interception per RESEARCH Pattern 2: in Serve's read loop, before the existing msg.ID == nil branch, check id-set AND method-empty AND (result-or-error present) and route to registry.Deliver with continue; the condition must not capture notifications (method present) or requests (method present). Register a handler for the $/cancel_request method name that logs via the server logger and returns without response (follow the handleSessionSetMode no-op shape but with a log line; notifications produce no frame by construction). Add the registry field to Server (constructed in NewServer alongside sessions; registry needs the Writer and the emitter foreground handle — both available at construction). Write the three behavior tests in server_test.go using the existing pipe fixtures.
  </action>
  <verify>
    <automated>go test -race ./internal/acp/ -run 'TestServeResponseRouting|TestServeInboundCancelNoOp' -count=1 && go test ./internal/acp/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- TestServeResponseRouting feeds a response frame and asserts zero outbound frames plus registry resolution of a matching pending Call made from a paired goroutine.
- The unknown-id case asserts one log line and continued serving.
- The $/cancel_request notification case asserts no outbound frame and a log record.
- The pre-existing acp test suite is green (dispatch regression-free).
  </acceptance_criteria>
  <done>Response frames route to the registry before dispatch: zero spurious errors, unknown ids logged and dropped, and inbound client cancels are cheap no-ops on the read loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Initialize capability probe + sticky degradation + telemetry</name>
  <files>internal/acp/handlers.go, internal/acp/server.go, internal/acp/metrics.go, internal/acp/server_test.go</files>
  <behavior>
    - initialize params advertising clientCapabilities with elicitation.form true: NO probe frame is sent; capability cached ok; initialize response returns promptly (advertisement-first — Zed never triggers a probe).
    - initialize params without the elicitation advertisement: exactly one elicitation/create probe is sent with a minimal schema-valid form payload (planner discretion per CONTEXT item 3 — e.g. a single short text element; the payload must be a valid v1 Form shape); the probe runs inside the initialize request's own goroutine (per-request dispatch already provides this — no reader deadlock); a result response caches ok; a -32601 error response caches degraded.
    - Degradation is sticky for the connection: after a degraded result, a second capability query returns degraded with no new probe (D-18 — no flapping); there is no automatic re-probe on later turns.
    - Probe timeout follows the D-14 ladder: one retry, then degraded fallback + counters (probes, timeouts, fallbacks incremented; structured stderr lines at each step).
    - initialize ALWAYS responds, even when the probe degrades (response carries agentCapabilities unchanged).
    - The metrics family exposes an exported Snapshot() returning the counter set (including the writer-stall counter from 16-01 wired into the same family) for Phase 20's /status surfacing.
  </behavior>
  <read_first>internal/acp/handlers.go (handleInitialize :36-58, initializeResponse struct, params parsing :66-74), internal/acp/server.go (Server fields, WithTurnRunner option pattern for adding state, per-request goroutine dispatch), internal/acp/metrics.go (Task 3 creates it — model on the atomic-counter style used elsewhere in the repo, e.g. audit/tracer counters), internal/acp/server_test.go (initialize fixtures), .planning/phases/16-acp-wire-foundation/16-RESEARCH.md (clientCapabilities facts at acp.rs:767-795, Zed initialize behavior — NO settings blob, advertisement read-first reconciliation; A3 minimal probe payload; the capability-cache pattern in Shared Patterns)</read_first>
  <action>
Create internal/acp/metrics.go: a small struct of atomic uint64 counters (probe_total, probe_timeout_total, probe_fallback_total, registry_cancel_total, writer_stall_total — names are planner discretion, keep them consistent with the repo's existing counter naming) with Snapshot() and internal increment helpers; move 16-01's stall counter into this family if it lived ad hoc on the emitter (keep the emitter's fast-path increment, just point it at the shared counter). Add capability state to Server: own mutex, map of capability key → result (ok/degraded), set by advertisement at initialize and by probe outcome; queried by later phases (Phase 17 asks consult it) via an exported method. Tier note (the responsibility map lists acpserve session state as primary for capability probing/sticky degradation): the cache deliberately rides internal/acp Server beside the registry — the probe IS a registry Call issued inside handleInitialize, Server lifetime equals connection lifetime which is exactly D-13/D-18's probe scope, and hoisting the cache to acpserve would split initialize parsing across packages; acpserve owns the Server via composition, so the state is acpserve-owned transitively. Extend handleInitialize: parse clientCapabilities from params (tolerant anonymous-struct style; unknown shapes treated as absent), read advertisement FIRST — if elicitation.form is present cache ok and skip the probe entirely; otherwise issue registry.Call(ctx, elicitation/create method, minimal valid form payload, FAST-CONTROL) inside the handler (initialize dispatches on its own goroutine, so blocking is safe — the read loop keeps serving), cache the outcome, and respond either way. All probe steps log structured lines to the server logger (stderr-bound per the transport discipline) and bump counters. Write the behavior tests: advertisement-present (no probe — assert the client never receives elicitation/create), absent-and-answered (-32601 → degraded), stickiness (second query no probe), timeout ladder (fallback + counters), always-respond.
  </action>
  <verify>
    <automated>go test -race ./internal/acp/ -run 'TestInitializeProbe|TestCapabilityStickiness|TestProbeTimeoutFallback' -count=1 && go test ./internal/acp/ ./internal/acpserve/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- TestInitializeProbe covers both branches (advertised → no probe; absent → exactly one probe frame) and both outcomes (result → ok; -32601 → degraded).
- TestCapabilityStickiness asserts degraded persists with no second probe for the connection lifetime.
- TestProbeTimeoutFallback asserts one retry, then degraded, with the three counters incremented and log lines present.
- Every test path asserts the initialize response frame still arrives.
- metrics.go exposes Snapshot() and the family includes the writer-stall counter; `mise ci` green.
  </acceptance_criteria>
  <done>D-13/D-16/D-17/D-18 land: one eager capability negotiation at initialize with sticky degradation, FAST-CONTROL vs HUMAN-ASK classes, and an observable counter family — the substrate Phase 17's asks consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→registry (inbound responses) | untrusted frames claim to answer our requests; ids are the only authenticity |
| registry→client (outbound asks) | probe/ask payloads must not leak secrets or wedge on hostile clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-06 | Spoofing | response id collision / fabrication | high | mitigate | UUID v4 ids both directions (D-15); unknown-id responses logged and dropped (never dispatched); map lookup is exact-match on the normalized id string |
| T-16-07 | DoS | hostile client withholds responses | high | mitigate | D-14 ladder bounds every Call (one retry, then fallback); FAST-CONTROL ~10s bounds probes; shutdown drain releases waiters (Pitfall 8) |
| T-16-08 | Information Disclosure | probe payloads | medium | mitigate | Probe payload is a minimal static form (CONTEXT discretion item 3) — no session content, no paths, no config values; stderr logs carry ids/methods only |
| T-16-09 | Repudiation | capability degradation decisions | low | mitigate | Sticky cache plus structured logs record advertisement source vs probe outcome (D-16) |
| T-16-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs (stdlib only); legitimacy gate not applicable |
</threat_model>

<verification>
- `go test -race ./internal/acp/ -count=1` green (registry, interception, probe, plus 16-01's emitter suite).
- `mise ci` green.
- A manual smoke in the simulator arrives in 16-06; nothing here requires a live editor.
</verification>

<success_criteria>
The outbound-request half of the wire is complete: id'd requests resolve under concurrent traffic, degrade loudly and bounded, cascade cancellation per the documented contract, and negotiate client capability exactly once per connection — all proven under -race before Phase 17 builds its asks on top.
</success_criteria>

<output>
Create `.planning/phases/16-acp-wire-foundation/16-03-SUMMARY.md` when done
</output>
