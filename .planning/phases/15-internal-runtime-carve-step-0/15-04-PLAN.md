---
phase: 15-internal-runtime-carve-step-0
plan: 04
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - cmd/ass-guard/profile_check.go
  - internal/profilecheckcmd/profile_check.go
  - cmd/ass-guard/profile_check_test.go
  - internal/profilecheckcmd/profile_check_test.go
  - cmd/ass-guard/parity.go
  - internal/paritycli/parity.go
  - cmd/ass-guard/acp_serve.go
autonomous: true
requirements: [RUNT-01]
estimate:
  tokens: 38000
  raw_tokens: 25000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Profile-check run logic is callable from its cobra shell through internal/profilecheckcmd, os.Stderr uses preserved verbatim"
    - "Parity run-logic (suite load, A/B harness, cache probe, drift warning, footer) lives in internal/paritycli with all three test-injectable seams working"
    - "The parity gate's exit semantics (non-zero on OverallPass=false) are unchanged and its comment block rides along"
    - "cmd/ass-guard/provider_factory.go no longer exists — main.go calls providerfactory directly and acp_serve.go's :339 call site is re-pointed to providerfactory.SetupModelRouting (documented transitional edit; the wave-3 tree compiles with zero cmd-local wrappers)"
  artifacts:
    - path: "internal/profilecheckcmd/profile_check.go"
      provides: "RunProfileCheck + capture/report helpers verbatim"
      contains: "RunProfileCheck"
    - path: "internal/paritycli/parity.go"
      provides: "RunParity + zcodeInstalledVersion/parityRun/composeCacheProbeInput seams + footer/drift emitters"
      contains: "RunParity"
  key_links:
    - from: "internal/paritycli/parity.go"
      to: "internal/providerfactory/provider_factory.go"
      via: "RunParity builds its provider through providerfactory.SetupProviderFactory"
      pattern: "providerfactory\\.SetupProviderFactory"
    - from: "cmd/ass-guard/acp_serve.go"
      to: "internal/providerfactory/provider_factory.go"
      via: "serve pipeline's scheduling-config call (:339) re-pointed to providerfactory.SetupModelRouting when the wrapper file is deleted"
      pattern: "providerfactory\\.SetupModelRouting"
---

<objective>
Extract the remaining two CLI-support concerns — profile-check and parity run-logic — into their own packages, completing the D-08 set (six packages total with plans 15-02/03).

Purpose: D-08 package-per-concern; OQ5 resolved as same treatment for profile_check.go. Parity's extraction is the one that consumes providerfactory (setupProviderFactory at :240), so it depends on plan 15-02 and deletes the delegation wrapper left there — retiring the LAST wrapper consumer (acp_serve.go:339) in the same task so the wave-3 tree compiles wrapper-free.
Output: `internal/profilecheckcmd`, `internal/paritycli` with relocated logic + tests; cmd shells delegate.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@cmd/ass-guard/profile_check.go
@cmd/ass-guard/parity.go
@cmd/ass-guard/profile_check_test.go
@cmd/ass-guard/parity_test.go
@internal/providerfactory/provider_factory.go
@cmd/ass-guard/goconst_constants.go
</context>

<artifacts_this_phase_produce>
- Package `internal/profilecheckcmd`: exported `profilecheckcmd.RunProfileCheck(stderr io.Writer, ...) error` plus relocated capture/report helpers (loadCaptureLine, extractCaptureCounts, reportProfileCheck, readFirstLine, readBufSize const).
- Package `internal/paritycli`: exported `paritycli.RunParity(suitePath, rollout, name, dir, results, surprise, cachePin string) error` + the three test-injectable seams exported or constructor-fed (`ZcodeInstalledVersion` func var, `SetParityRun`/`SetComposeCacheProbeInput` setters or exported vars — planner-executor discretion within D-19), plus defaultSuitePath/defaultCachePinPath/composeCacheProbeInput/assembleCacheProbe/placementCheck/emitVersionDriftWarning/emitParityFooter/countBothLayerPass.
- Relocated parity_test.go + profile_check_test.go.
</artifacts_this_phase_produce>

<tasks>

<task type="auto">
  <name>Task 1: Extract profile-check logic to its own package (OQ5 disposition)</name>
  <files>internal/profilecheckcmd/profile_check.go, cmd/ass-guard/profile_check.go</files>
  <read_first>cmd/ass-guard/profile_check.go (in full — 236 lines), .planning/phases/15-internal-runtime-carve-step-0/15-PATTERNS.md (OQ5 row), internal/providerfactory/provider_factory.go (extraction precedent)</read_first>
  <action>
Per OQ5 resolution (same treatment as the other five): git mv profile_check.go → internal/profilecheckcmd/, keep newProfileCmd + newProfileCheckCmd (:17-53) in a cmd/ass-guard/profile_check.go shell that parses flags and delegates. Move verbatim: runProfileCheck, loadCaptureLine, extractCaptureCounts, reportProfileCheck, readFirstLine, and const readBufSize (:15). Export only RunProfileCheck (or the minimal set cmd calls). Its direct os.Stderr uses (:89, :191-205) move verbatim regardless of the writer-parameterized peers (PATTERNS.md note) — no rewrite to io.Writer during the carve (that would be behavior-adjacent editing; SP-1 forbids). nolints ride lines; declaration order preserved; mise fmt. Constants referenced copy per D-10 into the package's goconst_constants.go if any apply.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/profilecheckcmd/ ./cmd/ass-guard/ -count=1 && test "$(grep -rl spf13/cobra internal/profilecheckcmd/ | wc -l | tr -d ' ')" = "0" && echo "cobra-free check passed"</automated>
  </verify>
  <acceptance_criteria>
- Build + vet clean; relocated suite passes from internal/profilecheckcmd/.
- None of internal/profilecheckcmd imports cobra.
- newProfileCmd/newProfileCheckCmd remain in cmd wired via main.go AddCommand unchanged.
- os.Stderr direct uses preserved verbatim inside moved bodies.
- No production body edits beyond package/export/import lines.
  </acceptance_criteria>
  <done>Profile-check concern has its own package; OQ5 closed by execution.</done>
</task>

<task type="auto">
  <name>Task 2: Extract parity run-logic to internal/paritycli and rewire providerfactory consumption</name>
  <files>internal/paritycli/parity.go, cmd/ass-guard/parity.go, cmd/ass-guard/parity_test.go, internal/paritycli/parity_test.go, cmd/ass-guard/acp_serve.go</files>
  <read_first>cmd/ass-guard/parity.go (FULL FILE — 376 lines, read in full at planning time), cmd/ass-guard/parity_test.go, internal/providerfactory/provider_factory.go (the SetupProviderFactory export), .planning/phases/15-internal-runtime-carve-step-0/15-PATTERNS.md (parity section)</read_first>
  <action>
git mv parity.go → internal/paritycli/parity.go, then split like the others: newParityCmd (:46-78) + defaultSuitePath/defaultCachePinPath STAY in cmd (they exist only to feed flag defaults into the cobra definition — D-09); everything else moves verbatim into package paritycli: zcodeVersionTimeout const, zcodeInstalledVersion seam var (+ its gochecknoglobals nolint), parityRun seam var (+ nolint), composeCacheProbeInput seam var (+ nolint), assembleCacheProbe, placementCheck, runParity→RunParity (exported), emitVersionDriftWarning, emitParityFooter, countBothLayerPass. Replace the setupProviderFactory call inside RunParity with providerfactory.SetupProviderFactory (this DELETES the delegation wrapper file left in cmd/ass-guard/provider_factory.go by plan 15-02; main.go's runTrace was already re-pointed by 15-02 — grep-verify it holds no wrapper reference — so the file is deleted entirely). Then retire the LAST wrapper consumer: acp_serve.go:339 still calls the cmd-local setupModelRouting — re-point it to providerfactory.SetupModelRouting (exported by plan 15-02 precisely for this handoff) in this same task, otherwise the tree cannot compile at the wave-3 boundary once the wrapper file is gone. This re-point is an EXPLICITLY DOCUMENTED TRANSITIONAL EDIT — identifier qualification only (setupModelRouting(...) → providerfactory.SetupModelRouting(...)), no logic change; the :335 comment block naming setupModelRouting rides unchanged per SP-2. It is classified in the phase's sanctioned-exceptions register as a wrapper-retirement call-site re-point and carries verbatim into acpserve.Run when plan 15-05 moves the body. Test seams visibility: parity_test.go injects all three seams — move parity_test.go wholesale to internal/paritycli/ (same-package test access keeps the unexported-var option legal); choose the smallest change that compiles: keeping them as package-level vars (unexported) is preferred since tests are same-package. If cmd still needs any parity symbol after the split (grep first), expose exactly that. nolint directives ride lines; order preserved; fmt.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/paritycli/ ./cmd/ass-guard/ -count=1 && test "$(grep -rl spf13/cobra internal/profilecheckcmd/ internal/paritycli/ | wc -l | tr -d ' ')" = "0" && test "$(grep -rn '^func setupModelRouting\|^func setupProviderFactory' --include='*.go' cmd/ | wc -l | tr -d ' ')" = "0" && test "$(grep -c 'providerfactory\.SetupModelRouting' cmd/ass-guard/acp_serve.go | tr -d ' ')" -ge 1 && test ! -f cmd/ass-guard/provider_factory.go && echo "wrapper deleted + acp_serve re-pointed"</automated>
  </verify>
  <acceptance_criteria>
- Build + vet clean; internal/paritycli suite passes; cmd tests green.
- cmd/ass-guard/provider_factory.go no longer exists; main.go's runTrace calls providerfactory.SetupProviderFactory directly.
- acp_serve.go's :339 call site reads providerfactory.SetupModelRouting; zero cmd-local wrapper definitions remain (grep '^func setupModelRouting\|^func setupProviderFactory' over cmd/ = no hits) — the wave-3 tree compiles with no wrapper bridge.
- The three parity seams (zcodeInstalledVersion, parityRun, composeCacheProbeInput) live in internal/paritycli with their //nolint:gochecknoglobals comments intact and are injected by the moved parity_test.go successfully.
- newParityCmd remains in cmd with identical flags (--suite --from-rollout --profile --profiles-dir --results --surprise-check --cache-pin).
- Exit-semantics comment ("PARITY GATE FAIL" non-zero path) preserved byte-identical.
- No cobra import inside internal/paritycli.
  </acceptance_criteria>
  <done>All six D-08 extractions complete; providerfactory wrapper bridge retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zcode binary exec | zcodeInstalledVersion execs an installed binary by fixed argv |
| corpus fixture input | placementCheck reads operator-supplied cache-pin JSONL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Tampering | zcodeInstalledVersion exec argv | low | mitigate | Fixed-argv exec preserved verbatim incl. its T-14-11/T-14-12 comment block (no-shell, bounded timeout); suite pins the seam |
| T-15-07 | Information Disclosure | parity footer stderr output | low | accept | Report-only output discipline unchanged; never feeds exit code (locked prohibition rides the comment) |
| T-15-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; legitimacy gate not applicable |

</threat_model>

<verification>
- `mise ci` green after Task 2.
- Six CLI-support packages total exist (providerfactory, checkpointcmd, learningcmd, modelroutingcmd, profilecheckcmd, paritycli), each cobra-free.
- cmd/ass-guard non-test files remaining: main.go, root/acp/checkpoint/learning/model-routing/profile/parity cobra shells only.
</verification>

<success_criteria>
The full D-08 set is extracted: six package-per-concern homes, cmd reduced to CLI shape, wrapper bridge retired, standing gate proves equivalence.
</success_criteria>

<output>
Create `.planning/phases/15-internal-runtime-carve-step-0/15-04-SUMMARY.md` when done
</output>
