---
phase: 15-internal-runtime-carve-step-0
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - cmd/ass-guard/provider_factory.go
  - cmd/ass-guard/main.go
  - internal/providerfactory/provider_factory.go
  - internal/providerfactory/goconst_constants.go
  - cmd/ass-guard/provider_factory_test.go
  - internal/providerfactory/provider_factory_test.go
autonomous: true
requirements: [RUNT-01]
estimate:
  tokens: 30000
  raw_tokens: 20000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Provider-factory infrastructure is importable as internal/providerfactory by acpserve, main.go's tracer, and the parity CLI"
    - "The loose-config perm warning (0600 advice) survives the move byte-identical and its tests still guard it"
    - "cmd/ass-guard compiles throughout via delegation wrappers until each importer's own plan re-points it"
  artifacts:
    - path: "internal/providerfactory/provider_factory.go"
      provides: "SetupProviderFactory, SetupModelRouting, LoadModelRoutingFactory, GlobalConfigPath, ProjectConfigPath, WarnLooseConfigPerm + unexported helpers"
      contains: "WarnLooseConfigPerm"
    - path: "internal/providerfactory/provider_factory_test.go"
      provides: "The relocated provider-factory suite passing from the new home"
    - path: "internal/providerfactory/goconst_constants.go"
      provides: "Constants this package's moved code references (D-10 split)"
  key_links:
    - from: "internal/acpserve/acp_serve.go"
      to: "internal/providerfactory/provider_factory.go"
      via: "acpserve.Run calls providerfactory.SetupProviderFactory for the serve pipeline"
      pattern: "providerfactory\\.SetupProviderFactory"
---

<objective>
Extract the provider-factory shared infrastructure into `internal/providerfactory` — the one D-08 extraction that is consumed by three callers (acpserve, main.go's tracer, parity CLI), so it must land first and alone.

Purpose: D-08 (package per concern). provider_factory.go is shared-infra, not cli-logic: acpserve.Run calls setupProviderFactory, runTrace calls it in main.go, runParity calls it in the parity package. Extracting it before the other five extractions means every later plan imports a stable home instead of re-pointing at cmd.
Output: `internal/providerfactory` with the entire file verbatim; cmd/ass-guard calls through the new package.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@cmd/ass-guard/provider_factory.go
@cmd/ass-guard/provider_factory_test.go
@cmd/ass-guard/main.go
@cmd/ass-guard/parity.go
@cmd/ass-guard/goconst_constants.go
</context>

<artifacts_this_phase_produce>
- Package `internal/providerfactory`: exported functions `providerfactory.SetupProviderFactory(stderr io.Writer) (*provider.Factory, string, error)`, `providerfactory.SetupModelRouting(workDir, stderr) (...)` (exported because acp_serve.go:339 consumes it through the cmd-local wrapper — live-verified call site — and plan 15-05's acpserve.Run body calls providerfactory.SetupModelRouting directly), `providerfactory.LoadModelRoutingFactory(workDir, stderr) (...)`, `providerfactory.GlobalConfigPath()`, `providerfactory.ProjectConfigPath()`, `providerfactory.WarnLooseConfigPerm(path string, stderr io.Writer)`, plus unexported helpers (`firstDeclaredProvider`, embedded-default fallbacks).
- `internal/providerfactory/provider_factory_test.go` — the relocated suite.
- Rewired importers: cmd/ass-guard/main.go (tracer path), parity CLI logic (consumed by plan 15-05), later acpserve (plan 15-04 consumes via its own task).
</artifacts_this_phase_produce>

<tasks>

<task type="auto">
  <name>Task 1: Move provider_factory.go + tests to internal/providerfactory</name>
  <files>cmd/ass-guard/provider_factory.go, internal/providerfactory/provider_factory.go, internal/providerfactory/provider_factory_test.go</files>
  <read_first>cmd/ass-guard/provider_factory.go, cmd/ass-guard/provider_factory_test.go, cmd/ass-guard/goconst_constants.go, .golangci.yml (path-exclusions section), internal/acp/goconst_constants.go (duplication precedent)</read_first>
  <action>
Per SP-1 verbatim-move method and D-08: create internal/providerfactory (package providerfactory); git mv provider_factory.go → internal/providerfactory/provider_factory.go; change `package main` → `package providerfactory`; fix imports only. Per D-09 export ONLY what fails to compile otherwise: SetupProviderFactory, SetupModelRouting, LoadModelRoutingFactory, GlobalConfigPath, ProjectConfigPath, WarnLooseConfigPerm become exported; SetupModelRouting MUST be on that list — acp_serve.go:339 calls setupModelRouting through the cmd-local wrapper (live-verified) and a cmd wrapper cannot delegate to an unexported providerfactory symbol; plan 15-04 re-points :339 to providerfactory.SetupModelRouting and plan 15-05's Run body calls it directly. firstDeclaredProvider stays unexported (no external caller — grep confirms). Every //nolint directive rides its line unchanged (SP-2); preserve relative declaration order (funcorder evaluates per-file). Copy the constants this package's code references from goconst_constants.go into internal/providerfactory/goconst_constants.go with the internal/acp header-comment style (D-10; precedent internal/acp/goconst_constants.go). warnLooseConfigPerm is ASVS V14-relevant: it MUST arrive intact (RESEARCH Security Domain) — verify byte-identical after move. git mv provider_factory_test.go likewise, package rename, duplicate-in any helpers it references from OTHER cmd test files per D-03 (writeTestModelRouting/pinEmptyHome are defined IN this file per RESEARCH inventory — they travel with it; subagent_tier_wiring_test.go's copies are plan 15-03's duplication duty). Run mise fmt. This task compiles independently of all other carve work (SP-12 wave-2 property) EXCEPT for its three importers: rewire cmd/ass-guard/main.go's runTrace (:129) and leave parity.go/acp_serve.go call sites pointing at cmd-local wrappers UNTIL their own plans re-point them — i.e., keep thin same-name wrapper funcs (setupProviderFactory, setupModelRouting) in cmd/ass-guard/provider_factory.go during this plan (delegating to the exported providerfactory.* symbols) so parity.go and acp_serve.go compile untouched; plan 15-04 deletes those wrappers when parity moves to paritycli and acp_serve.go:339 is re-pointed to providerfactory.SetupModelRouting. This wrapper bridge keeps each plan a reviewable isolated diff.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/providerfactory/ ./cmd/ass-guard/ -count=1 && grep -c "warnLooseConfigPerm\|WarnLooseConfigPerm" internal/providerfactory/provider_factory.go</automated>
  </verify>
  <acceptance_criteria>
- `go build ./...` green; `go vet ./...` clean.
- `go test ./internal/providerfactory/ -count=1` green with the full relocated suite passing.
- `go test ./cmd/ass-guard/ -count=1` still green (wrapper bridge keeps importers compiling).
- internal/providerfactory/provider_factory.go contains WarnLooseConfigPerm with body byte-identical to source lines 159-171 (0600 advice text preserved exactly).
- `git diff master -- cmd/ass-guard/provider_factory.go` shows the file deleted from cmd; `git log --follow` traces history into internal/.
- No AST tooling used; moved bodies differ from source only by package clause, exported identifiers, and import block.
- Exactly one TestMain remains in the repo's changed packages (none introduced here).
  </acceptance_criteria>
  <done>internal/providerfactory exists with the entire file verbatim; cmd compiles via delegation wrappers; tests pass from the new home.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-boundary equivalence gate</name>
  <files></files>
  <behavior>
    - Full gate proves no behavior change across the extraction boundary: vet, strict lint, static build, race suite all green on the post-move tree.
    - Test-function count across the whole repo still sums to the committed ledger total (130) — relocation moves tests, never adds or loses them.
  </behavior>
  <read_first>.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt, .mise.toml</read_first>
  <action>
Run `mise ci` (the RUNT-01 equivalence proof per RESEARCH Validation Architecture). Then verify the ledger invariant: sum of `^func Test` across ALL packages (cmd/ass-guard + internal/...) equals the ledger's recorded total — use `grep -rh "^func Test" --include="*_test.go" cmd/ internal/ | wc -l`. If the count exceeds 130 by exactly the number of new golden-test functions from plan 15-01, adjust the expected denominator accordingly and note it in the SUMMARY (the golden tests were added AFTER ledger capture by design); otherwise investigate as a lost/duplicated test. Record actual numbers in the SUMMARY.
  </action>
  <verify>
    <automated>mise ci && N=$(grep -rh '^func Test' --include='*_test.go' cmd/ internal/ | wc -l | tr -d ' '); echo "test-func-total=$N"; test "$N" -ge 130</automated>
  </verify>
  <acceptance_criteria>
- `mise ci` exits 0 on the post-extraction tree.
- Repo-wide `^func Test` count is 130 + (number of TestCLIBinaryContract functions from 15-01), accounted explicitly in the SUMMARY.
- `git diff master --stat` shows no unexpected files outside cmd/ass-guard/{main.go,parity.go,acp_serve.go} and the new internal/providerfactory/ tree.
  </acceptance_criteria>
  <done>First independently-compilable extraction lands with the standing gate proving zero behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config-file→provider credentials | loadModelRoutingFactory stat-gates config paths carrying api_key material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Information Disclosure | WarnLooseConfigPerm (0600 advice) | medium | mitigate | Verbatim move verified byte-identical (Task 1 acceptance); existing perm-warning tests relocate with the suite and keep guarding |
| T-15-03 | Repudiation | setupModelRouting degradation logging | low | mitigate | Graceful-degradation log shape (SP-3) preserved byte-identical; suite covers the embedded-default fallback |
| T-15-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs (RESEARCH Standard Stack); legitimacy gate not applicable |

</threat_model>

<verification>
- `mise ci` green post-task-2.
- Ledger accounting explicit in SUMMARY.
- warnLooseConfigPerm present and byte-identical in the new package.
</verification>

<success_criteria>
The shared-infra extraction is complete: one package, three future importers, zero behavior delta, security-relevant perm warning intact.
</success_criteria>

<output>
Create `.planning/phases/15-internal-runtime-carve-step-0/15-02-SUMMARY.md` when done
</output>
