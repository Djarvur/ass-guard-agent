---
phase: 15-internal-runtime-carve-step-0
plan: 06
type: execute
wave: 5
depends_on: ["15-05"]
files_modified:
  - internal/runtime/runtime.go
  - internal/runtime/doc.go
  - internal/runtime/cron_wiring.go
  - internal/runtime/enginebridge/enginebridge.go
  - internal/runtime/acp_engine_e2e_test.go
  - internal/runtime/advisory_wiring_test.go
  - internal/runtime/planmode_wiring_test.go
  - internal/runtime/ask_wiring_test.go
  - internal/runtime/mcp_tracer_test.go
  - internal/runtime/coreexec_wiring_test.go
  - internal/runtime/background_wiring_test.go
  - internal/runtime/subagent_tier_wiring_test.go
  - internal/runtime/e2e_opsx_test.go
  - internal/runtime/e2e_opsx_matrix_test.go
  - internal/runtime/integration_test.go
  - internal/runtime/evalsuite_bridge_test.go
  - internal/runtime/checkpoint_session_test.go
  - internal/runtime/runner_battery_test.go
  - internal/acpserve/acp_serve.go
  - internal/acpserve/serve_test.go
  - cmd/ass-guard/acp_serve.go
  - cmd/ass-guard/acp_serve_test.go
  - cmd/ass-guard/checkpoint.go
  - cmd/ass-guard/checkpoint_test.go
  - cmd/ass-guard/cron_wiring.go
  - cmd/ass-guard/integration_test.go
autonomous: true
requirements: [RUNT-01]
estimate:
  tokens: 55000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "internal/runtime houses the turn runner (as runtime.Runner) with sessionFor, expandUserBlocks, park/idle state, advisory dedupe, checkpointerAdapter and redactorAdapter beside their use sites"
    - "runtime.NewRunner(RunnerConfig) is a thin struct-fill constructor; callers invoke loadCommandRegistry/setupEngine/startScheduler explicitly in today's order"
    - "The seven engine adapters are constructible only through exported enginebridge constructors fed func values from runtime core; no unexported runtime member is reachable from enginebridge"
    - "cron_wiring.go's nine methods execute identically as methods on runtime.Runner from internal/runtime/cron_wiring.go"
    - "acpserve.Run is one function whose statement order matches the source serve pipeline line-for-line, with emitFor injected between NewServer and startScheduler (amended D-13 two-way composition + D-16 emitter injection preserved)"
    - "All runner-subject tests pass from internal/runtime with zero test-function loss and exactly one TestMain"
    - "RUNT-01 equivalence holds: mise ci green with the ledger total matched"
  artifacts:
    - path: "internal/runtime/doc.go"
      provides: "Kit-facing package doc per D-20 (SEED-001 kit ambition at Phase 25; acpserve NOT kit; no ACP words in API naming)"
      contains: "Phase 25"
    - path: "internal/runtime/runtime.go"
      provides: "type Runner + RunnerConfig + NewRunner + all runner methods verbatim-renamed"
      contains: "func NewRunner"
    - path: "internal/runtime/enginebridge/enginebridge.go"
      provides: "Mirror-config BridgeConfig + exported adapter constructors (EngineTurnAdapter, ACPDispatcher, HookSessionTurnRunner, HookSessionBoundaryOpener, RealCommandRunner, StubCatalogExec)"
      contains: "BridgeConfig"
    - path: "internal/runtime/cron_wiring.go"
      provides: "The nine scheduler-family methods on Runner, bodies byte-verbatim (receiver rename only)"
      contains: "startScheduler"
  key_links:
    - from: "internal/acpserve/acp_serve.go"
      to: "internal/runtime/runtime.go"
      via: "acp.WithTurnRunner(runner) plugs runtime.Runner into the unchanged acp.Server seam"
      pattern: "acp\\.WithTurnRunner"
    - from: "internal/runtime/runtime.go"
      to: "internal/runtime/enginebridge/enginebridge.go"
      via: "core construction sites call enginebridge constructors passing func values derived from unexported members"
      pattern: "enginebridge\\.New"
    - from: "internal/runtime/cron_wiring.go"
      to: "internal/runtime/runtime.go"
      via: "runAutomationTurn calls core runOneTurn; Run calls cron-family sessionTurnMu/markClientTurn — bidirectional same-package coupling"
      pattern: "(r|R)\\.(runAutomationTurn|sessionTurnMu|markClientTurn)"
  assumptions:
    - id: RUNT-01
      status: unresolved
      note: >
        Edge-probe classified RUNT-01 'unclassified/unresolved' (spec-less probe fallback,
        $COVERAGE coverage.resolved=0). Per #1110 this is an EXPLICIT PLANNER ASSUMPTION, not a
        silent drop: RUNT-01's verbatim-move bar is verified by proxy instruments — the standing
        mise ci gate, the committed 130-test ledger match, the CLI-contract golden pin, and the
        color-moved diff review — because no SPEC supplied an explicit verification predicate.
        The live-Zed behavioral identity leg remains operator-judged (plan 15-07 checkpoint).
  prohibitions:
    - statement: "No behavior change anywhere in the moved code — every moved body must survive git diff --color-moved review as a relocated block"
      category: safety
      disposition: flagged-unverified
    - statement: "No API widening beyond minimal exports — export only what cmd/acpserve needs to compile; no pre-emptive exports for later phases"
      category: values
      disposition: flagged-unverified
    - statement: "No feature code moved or rewritten during the carve — no drive-by cleanup, no AST tooling, no shared testutil extraction"
      category: safety
      disposition: flagged-unverified
---

<objective>
THE CARVE (SP-12 compile closure): move the sessionTurnRunner family into `internal/runtime` (Runner per D-18), its engine adapters into `internal/runtime/enginebridge` (D-12/D-14 mirror-config seam), and cron_wiring.go byte-verbatim beside its receiver in package runtime (per the AMENDED D-13/D-15) — then dissolve plan 15-05's transitional split so acpserve.Run composes runtime.NewRunner as ONE function.

Purpose: RUNT-01's core act. This is one atomic compile closure — no intermediate green tree exists (Pitfall 10), so all tasks land in this single plan with mise ci required at the boundary only.
Output: `internal/runtime` + `internal/runtime/enginebridge` packages; acpserve.Run final form; cmd reduced to shells.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@cmd/ass-guard/acp_serve.go
@cmd/ass-guard/cron_wiring.go
@cmd/ass-guard/checkpoint.go
@internal/acpserve/acp_serve.go
@internal/acp/server.go
@internal/checkpointcmd/checkpoint_test.go
</context>

<artifacts_this_phase_produce>
- Package `internal/runtime`: type `runtime.Runner` (renamed from sessionTurnRunner, all fields still unexported, D-18/D-19), `runtime.RunnerConfig`, `runtime.NewRunner(cfg RunnerConfig) *Runner` (thin ctor per D-05); methods Run/CloseSession/WaitChainIdle exported by existing signature; everything else unexported; package doc.go stating kit ambition (D-20).
- Package `internal/runtime/enginebridge`: exported adapter types EngineTurnAdapter, ACPDispatcher, HookSessionTurnRunner, HookSessionBoundaryOpener, RealCommandRunner, StubCatalogExec (+ mirror-config struct per D-14) with exported constructors; patternNextPrompter interface stays UNEXPORTED in runtime core (sole consumer is a core closure).
- `internal/runtime/cron_wiring.go`: cron_wiring.go relocated byte-verbatim into package runtime (per the AMENDED D-13/D-15, operator sign-off 2026-08-26; no cron sub-package in Phase 15).
- Dissolved transition: acpserve.Run(ctx, in, out, stderr, opts) as one function composing runtime.NewRunner.
</artifacts_this_phase_produce>

<assumption_delta_decision>
No assumption-delta signal detected (detector returned detected:false) — block not applicable.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Carve the runner family into internal/runtime + enginebridge (one compile closure)</name>
  <files>internal/runtime/runtime.go, internal/runtime/doc.go, internal/runtime/enginebridge/enginebridge.go, internal/runtime/cron_wiring.go, cmd/ass-guard/acp_serve.go</files>
  <read_first>cmd/ass-guard/acp_serve.go (FULL — the runner half :441-2115), cmd/ass-guard/cron_wiring.go (FULL), cmd/ass-guard/checkpoint.go lines 22-30 (checkpointerAdapter → D-17 runtime home), .planning/phases/15-internal-runtime-carve-step-0/15-PATTERNS.md (runner-core destination map + enginebridge construction-site evidence), .planning/phases/15-internal-runtime-carve-step-0/15-RESEARCH.md (Pattern 2 cross-package mechanics)</read_first>
  <action>
ONE ATOMIC MOVE per PATTERNS runner-core destination map and SP-12:
(a) internal/runtime: git mv the runner half of acp_serve.go (:441-544 struct + methods through :2095 EXCLUDING the seven adapter types + setupEngine) into internal/runtime/runtime.go as package runtime; rename sessionTurnRunner → Runner (D-18) via mechanical sed across moved text only; create RunnerConfig mirroring today's literal fields EXACTLY (bus, bodyStore, profile, workDir, maxConc, configAdded, makeProvider, askTimeout, serveCtx, schedCfg, providerName, stderr — names identical to struct fields per D-05) and NewRunner(cfg) doing struct-fill ONLY (D-05 thin ctor). checkpointerAdapter moves here beside sessionFor (D-17). redactorAdapter moves here per OQ2 (sole consumer sessionFor :1157,:1160). parkedChain/advisoryNote/park+idle state/advisory dedupe land here (D-11). Package doc.go per D-20 following internal/checkpoint/store.go doc form: purpose, kit ambition (SEED-001 kit core at Phase 25), non-goals (acpserve NOT kit; no ACP-specific words in API naming).
(b) internal/runtime/enginebridge: move the 7 adapter TYPES (engineTurnRunnerAdapter→EngineTurnAdapter, acpDispatcher→ACPDispatcher, hookSessionTurnRunner→HookSessionTurnRunner, hookSessionBoundaryOpener→HookSessionBoundaryOpener, realCommandRunner→RealCommandRunner, stubCatalogExec→StubCatalogExec, stubExecResult, patternNextPrompter STAYS in core) plus setupEngine BODY LOGIC as an exported constructor per D-14 MIRROR CONFIG: enginebridge defines BridgeConfig mirroring exactly the runner fields its adapters need (hookExec, hookCfg, learned, bus, patternTable + func values for invocationFor/expandUserBlocks/sessMu-guarded automationProvenance access per RESEARCH Pattern 2 — behavior crosses as func values, names never do); constructors return configured adapters; ALL construction sites stay in package runtime (setupEngine's dispatcher literal :596-608 becomes a runtime-core method calling enginebridge.NewACPDispatcher(...); runOneTurn's :915-917/:961 constructions likewise). toolCallNamesUnder + triggerFromSignal ride to enginebridge (sole consumers there).
(c) Per the AMENDED D-13 and D-15 (operator sign-off via AskUserQuestion, 2026-08-26, recorded in 15-CONTEXT.md): the cron leg of the original 3-way split is DEFERRED — cron_wiring.go relocates byte-verbatim to internal/runtime/cron_wiring.go as package runtime; NO internal/runtime/cron sub-package is created in Phase 15; the 2-way shape (runtime + enginebridge) ships now; Phase 25 introduces the interface-based cron seam as design work rather than relocation collateral. Amendment rationale (as recorded in CONTEXT): Go forbids declaring methods outside their receiver's package and the core↔cron calls are bidirectional (4 of the 9 cron methods called from runner core at :763/:770/:772/:1389/:1395; runAutomationTurn → runOneTurn at cron_wiring.go:132+), so a separate package would force non-verbatim body rewrites against the verbatim-move bar. Execute: git mv cron_wiring.go → internal/runtime/cron_wiring.go, package clause → runtime, bodies byte-verbatim, receiver renames sessionTurnRunner→Runner only. Note the Phase-25 cron seam plan in runtime's doc.go per the amendment.
(d) cmd/ass-guard/acp_serve.go retains ONLY newACPCmd/newACPServeCmd shells.
(e) Constants: each new package takes the goconst_constants.go subset it uses (D-10).
Preserve relative declaration order within every destination file (SP-2 funcorder); every nolint rides its line; mise fmt at end. Progress metric during the task: go build ./internal/... ; mise ci ONLY after the whole closure compiles.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/runtime/... -count=1 && test "$(grep -rn sessionTurnRunner --include='*.go' cmd/ internal/ | wc -l | tr -d ' ')" = "0" && echo "rename complete"</automated>
  </verify>
  <acceptance_criteria>
- Full tree compiles; vet clean; runtime-family tests pass from internal/runtime/.
- `grep -rn "sessionTurnRunner" --include='*.go' cmd/ internal/` returns ZERO hits across all module packages (rename complete; spikes/ hold no references).
- runtime.Runner implements acp.TurnRunner + acp.SessionCloser unchanged (Run/CloseSession signatures byte-identical).
- NewRunner performs struct-fill only — no loadCommandRegistry/setupEngine/startScheduler calls inside it (D-05; callers invoke explicitly).
- enginebridge types are exported with constructors; zero references FROM enginebridge TO unexported runtime members (grep enginebridge files for lowercase runtime member accesses = none beyond parameters).
- cron_wiring.go sits at internal/runtime/cron_wiring.go with bodies matching source modulo receiver rename (`git diff master --find-renames -- cmd/ass-guard/cron_wiring.go` shows a pure move+rename).
- internal/runtime/cron directory does NOT exist this phase (amended D-13: no cron sub-package in Phase 15; doc.go records the Phase-25 seam plan).
- doc.go states kit ambition + acpserve-not-kit + no-ACP-naming rule.
- The sanctioned-edit register is exactly: the two de-cobra adaptations + the wrapper-retirement call-site re-points (setupProviderFactory/setupModelRouting identifier qualifications from 15-02/15-04) + the documented RunnerConfig/NewRunner/rename mechanics; no OTHER edited blocks exist under `git diff master --color-moved=plain`.
  </acceptance_criteria>
  <done>The runner family lives in internal/runtime(+enginebridge); cmd owns CLI shape only; the carve closure compiles and passes its suite.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate the runner-subject test suites (D-01/D-02/D-03 subject rule)</name>
  <files>internal/runtime/*_test.go, cmd/ass-guard/acp_serve_test.go, cmd/ass-guard/integration_test.go</files>
  <read_first>All cmd test files constructing &sessionTurnRunner{ (10 files per SP-6 list), RESEARCH test-code subject map table, SP-7 helper duplication graph, SP-8 TestMain uniqueness</read_first>
  <action>
Apply the subject rule (D-01/D-02, planner discretion on per-test assignment):
MOVE to internal/runtime/ (package runtime, white-box same-package per D-01): acp_engine_e2e_test.go (3 literals + scriptedACPProvider/scriptedResp/noopEmitter/fakeProfileACP helpers), advisory_wiring_test.go, planmode_wiring_test.go, ask_wiring_test.go, mcp_tracer_test.go (AS A UNIT with its TestMain — SP-8), coreexec_wiring_test.go, background_wiring_test.go, subagent_tier_wiring_test.go (duplicate writeTestModelRouting/pinEmptyHome from providerfactory tests per D-03 cross-package dup), e2e_opsx_test.go, e2e_opsx_matrix_test.go, integration_test.go, evalsuite_bridge_test.go, checkpoint_test.go's sessionFor-subject remainder (TestSessionFor_WiresCheckpointer + gated live rollback from plan 15-03's holdover), AND the runner-battery portion of acp_serve_test.go (newExpansionRunner/newSkillRunner battery + writeOpsxCommandFixtures). SPLIT mechanics: within mixed files cut whole contiguous test functions (with their helpers if sole-consumed) rather than editing bodies; rename every &sessionTurnRunner{ literal to &Runner{ mechanically; duplicate shared helpers per D-03 (newExpansionRunner, scriptedACPProvider, scriptedResp, noopEmitter, fakeProfileACP, writeOpsxCommandFixtures duplicated wherever consumed across package boundaries). Env gates ride their files (SP-9). Assert exactly one TestMain in internal/runtime post-move (SP-8). cmd/ass-guard keeps ONLY: cli_contract_test.go (15-01), zeroconfig_test.go, TestACPServeCommandRegistered, and any genuinely binary-spawn subjects. Update import paths; mise fmt.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/runtime/... ./cmd/ass-guard/ -count=1 && TM=$(grep -rl '^func TestMain' internal/runtime/*_test.go | wc -l | tr -d ' '); echo "TestMain-count=$TM"; test "$TM" = "1"</automated>
  </verify>
  <acceptance_criteria>
- Build/vet clean; full relocated runtime suite passes (-race included at wave gate).
- Exactly one TestMain exists in internal/runtime (mcp_tracer's, intact with echo-server self-exec mode).
- Zero &sessionTurnRunner literals remain anywhere; all moved tests construct &Runner{...} in package runtime.
- e2e_opsx/integration/evalsuite_bridge tests pass from internal/runtime (OQ3 subject-rule disposition executed; deviation from D-04's letter documented in SUMMARY).
- cmd/ass-guard retains ≤4 test files (cli_contract, zeroconfig, cobra-shape remainder, any true-binary-spawn stragglers).
- No shared testutil package created (D-03) — helpers duplicated at destinations.
- Env-gate names unchanged (ASSGUARD_EVAL_GATE etc. grep-clean in moved files).
  </acceptance_criteria>
  <done>~8000 lines of tests redistributed by subject with zero name loss and one TestMain.</done>
</task>

<task type="auto">
  <name>Task 3: Dissolve the transitional split — acpserve.Run final form + wave-boundary equivalence proof</name>
  <files>internal/acpserve/acp_serve.go, internal/acpserve/serve_test.go</files>
  <read_first>internal/acpserve/acp_serve.go (the PrepareServe/FinishServe shape from plan 15-05), cmd/ass-guard/acp_serve.go shells, internal/runtime/runtime.go (NewRunner signature), 15-PATTERNS.md acpserve startup-order invariant</read_first>
  <action>
Reunify: acpserve.Run(ctx, in, out, stderr, opts Options) error becomes ONE function reproducing source :320-425 statement order line-for-line, with the runner literal replaced by cfg := runtime.RunnerConfig{...field-for-field from opts/context...}; runner := runtime.NewRunner(cfg); followed by runner.loadCommandRegistry(); engine-degrade branch via the enginebridge constructor; srv := acp.NewServer(in,out,stderr,acp.WithTurnRunner(runner)); schedule-store open + degrade-loudly; runner emitFor injection (srv.Emitter — WINDOWS #3 ordering preserved); runner.startScheduler(ctx); ctx-done closeAllSessions goroutine; return srv.Serve(ctx). Delete PrepareServe/FinishServe and the transitional package-doc note. Re-point the serve-layer tests adapted in 15-05 Task 2 back onto plain acpserve.Run (removing their transitional comments). Then THE PHASE EQUIVALENCE PROOF per SP-11: mise ci green; ledger match — repo-wide ^func Test count equals ledger total + 15-01 golden additions; color-moved review: git diff master --color-moved=plain -- cmd/ internal/ shows moved blocks rendered as moved (not edited) except the two sanctioned de-cobra edits + the wrapper-retirement call-site re-points inherited from 15-02/15-04 + the documented RunnerConfig/NewRunner/renames + test-file package/import lines.
  </action>
  <verify>
    <automated>mise ci && N=$(grep -rh '^func Test' --include='*_test.go' cmd/ internal/ | wc -l | tr -d ' '); D=$(grep -rh '^func Test[A-Za-z]' --include='*_test.go' cmd/ internal/ | sort | uniq -d | wc -l | tr -d ' '); echo "total=$N dupes=$D"; test "$D" = "0" && test "$N" -ge 130 && git diff master --color-moved=plain --stat -- cmd/ internal/ >/dev/null && echo GATE-OK</automated>
  </verify>
  <acceptance_criteria>
- `mise ci` exits 0 — the RUNT-01 equivalence proof.
- acpserve.Run is a single function; PrepareServe/FinishServe gone (grep = 0 hits).
- WINDOWS #3 ordering visible in Run's body: NewServer → emitFor injection → startScheduler, statement order matching source.
- Ledger total matches (130 baseline + golden additions accounted).
- Zero duplicate test names repo-wide.
- go test ./cmd/ass-guard/ -run TestZeroConfigFirstRun passes (binary handshake smoke from RESEARCH validation map).
- TestCLIBinaryContract still green (CLI surface survived the carve).
  </acceptance_criteria>
  <done>The carve is complete: runtime family in its final home, acpserve composing it, equivalence proven at the standing gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→runner (acp.TurnRunner) | Zed-driven prompts enter Run; the interface contract must survive the rename untouched |
| engine↔session seams | enginebridge adapters reach session/engine internals through func-value crossings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-11 | Tampering | acp.TurnRunner/SessionCloser contract | high | mitigate | Signatures byte-identical post-rename (Task 1 acceptance); WithTurnRunner plugs back identically; full race suite proves |
| T-15-12 | Elevation of Privilege | enginebridge mirror-config divergence | medium | mitigate | Mirror struct filled from ONE source in acpserve.Run; acceptance greps prove no unexported-member reaches across; divergence risk accepted deliberately per D-14 (operator choice) |
| T-15-13 | DoS | scheduler loop lifecycle | medium | mitigate | startScheduler ctx-only lifecycle moved verbatim incl. ticker cleanup; cron tests relocate with the family and keep covering |
| T-15-14 | Information Disclosure | redactor exclusion surfaces | medium | mitigate | redactorAdapter moves verbatim beside sessionFor; transcript/redaction discipline untouched; existing redaction-carry tests ride along |
| T-15-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; legitimacy gate not applicable |

</threat_model>

<verification>
- `mise ci` green (the phase-level equivalence proof) after Task 3.
- Ledger + duplicate-name accounting clean.
- Color-moved diff reviewable as pure relocation with only the documented exceptions.
- Binary handshake smoke + CLI golden contract green.
</verification>

<success_criteria>
RUNT-01's central act is done: the turn runner lives in internal/runtime with its adapters and cron wiring, composed by acpserve, proven equivalent by the standing gate, with zero unsanctioned edits.
</success_criteria>

<output>
Create `.planning/phases/15-internal-runtime-carve-step-0/15-06-SUMMARY.md` when done
</output>
