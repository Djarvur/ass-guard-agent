---
phase: 15-internal-runtime-carve-step-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt
  - cmd/ass-guard/cli_contract_test.go
autonomous: true
requirements: [RUNT-01]
estimate:
  tokens: 22000
  raw_tokens: 15000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "The pre-move test-function baseline (130 functions, per-file attribution) is committed BEFORE any file moves"
    - "A test fails loudly if any cobra command name, flag name, flag default, or usage string drifts during the carve"
    - "The editor spawn surface (ass-guard acp serve with --profile --max-concurrent --profiles-dir --work-dir --no-engine --ask-timeout --audit-log) is pinned byte-for-byte by an executable test"
  artifacts:
    - path: ".planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt"
      provides: "130-entry pre-move test-function ledger with per-file sections and grand total"
    - path: "cmd/ass-guard/cli_contract_test.go"
      provides: "Binary-spawn CLI contract golden tests pinning root/acp/checkpoint/learning/model-routing/profile/parity surfaces"
      contains: "TestCLIBinaryContract"
  key_links:
    - from: "cmd/ass-guard/cli_contract_test.go"
      to: "cmd/ass-guard/main.go"
      via: "golden text transcribed from the AddCommand wiring and each command's flag definitions"
      pattern: "TestCLIBinaryContract"
---

<objective>
Wave-0 measurement instruments for the carve: capture the pre-move test-function ledger and pin the CLI contract (commands + flags) with a binary-spawn golden test, so every later plan can prove "nothing changed" mechanically.

Purpose: RUNT-01's equivalence proof needs a baseline captured BEFORE any file moves (RUNT-01; RESEARCH Validation Architecture Wave-0 gaps). The golden test protects ROADMAP success criterion 2 (operator's daily-use surface unchanged) against accidental cobra drift while the shells are rewired.
Output: `.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt` (committed) + `cmd/ass-guard/cli_contract_test.go`.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@cmd/ass-guard/main.go
@cmd/ass-guard/zeroconfig_test.go
@.mise.toml
</context>

<artifacts_this_phase_produce>
Symbols/files this plan contributes to the phase's artifact inventory:
- `.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt` — the 130-entry pre-move test-function ledger (per-file counts), the denominator for the phase-close equivalence check.
- `cmd/ass-guard/cli_contract_test.go` — TestCLIBinaryContract* golden suite pinning the root/acp/checkpoint/learning/model-routing/profile/parity command + flag surface byte-for-byte via the compiled binary's help output.
No production symbols are created or modified by this plan (measurement instruments only).
</artifacts_this_phase_produce>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-move test-function ledger</name>
  <files>.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt</files>
  <read_first>cmd/ass-guard/ (directory listing of *_test.go), .planning/phases/15-internal-runtime-carve-step-0/15-VALIDATION.md</read_first>
  <action>
Generate the per-file test-function inventory of `cmd/ass-guard/*_test.go`: for each test file, its ordered list of `func Test...` declarations and a trailing grand-total line. Expected total is 130 (verified at planning time); if the working tree differs, STOP and record the actual number — the ledger must describe the tree the carve starts from, not an assumed one. Write the result to `.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt` with a two-line header stating the capture date, the HEAD commit, and that the post-carve sum across cmd/ass-guard + internal/runtime/... + internal/acpserve/... + the six new CLI packages must equal it. Commit the ledger with the plan's docs commit so the baseline is immutable history (commit_docs is enabled).
  </action>
  <verify>
    <automated>L=$(grep -hc '^func Test' cmd/ass-guard/*_test.go | awk '{s+=$1} END {print s}'); G=$(grep -c '^func Test' .planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt); test "$L" = "130" && test "$G" = "131" && echo "ledger OK: 130 live, 130 recorded + total line"</automated>
  </verify>
  <acceptance_criteria>
- `.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt` exists and is committed.
- `grep -hc '^func Test' cmd/ass-guard/*_test.go | awk '{s+=$1} END {print s}'` outputs exactly 130.
- The ledger contains exactly 130 `^func Test` lines plus one total line (131 matches of the same pattern).
- The ledger's grand-total line states 130.
- Every `*_test.go` file present in cmd/ass-guard has a corresponding section in the ledger (spot-check: acp_serve_test, e2e_opsx_matrix_test, ask_wiring_test, provider_factory_test all named).
  </acceptance_criteria>
  <done>The 130-function baseline is committed as an immutable artifact with per-file attribution.</done>
  <precondition>The working tree is at the milestone baseline (no carve work started): `git status --porcelain cmd/` is empty before capture.</precondition>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the CLI contract with a binary-spawn golden test</name>
  <files>cmd/ass-guard/cli_contract_test.go</files>
  <behavior>
    - For each pinned command surface (root, acp, acp serve, checkpoint, checkpoint list, checkpoint restore, learning, learning list, learning revert, model-routing, model-routing validate, model-routing resolve, parity, profile, profile check): invoking the compiled binary with --help exits 0 and prints a usage block whose command name, Short description line, and flag set (names + default-value text + usage strings) match the golden text exactly.
    - Root help lists the six subcommands wired in main.go's AddCommand block (lines 77-82, measured: profile, parity, acp, model-routing, learning, checkpoint) in wiring order; cobra's default help and completion commands also appear in the real --help output and ride into the golden as measured — the test never asserts a hard-coded subcommand total.
    - The golden text is inline in the test file (heredoc-style raw string literals), not read from disk — the contract must survive even if testdata moves.
    - Volatile substrings (absolute binary path) are normalized before comparison; nothing else is.
  </behavior>
  <read_first>cmd/ass-guard/main.go, cmd/ass-guard/zeroconfig_test.go (binary-build + exec pattern to reuse), cmd/ass-guard/acp_serve.go (lines 139-231: acp/serve flag definitions), cmd/ass-guard/checkpoint.go (lines 38-95), cmd/ass-guard/learning_cmd.go (lines 13-51), cmd/ass-guard/modelrouting.go (lines 15-129), cmd/ass-guard/parity.go (lines 46-78), cmd/ass-guard/profile_check.go (lines 17-53)</read_first>
  <action>
Follow zeroconfig_test.go's established recipe: build the binary once into t.TempDir() via `go build` (CGO_ENABLED=0, same as the mise build task), then `exec.CommandContext(bin, args...)` per case. Capture combined output of each `<command> --help` invocation. Normalize only the temp binary path. Transcribe each raw-string golden verbatim FROM THAT CAPTURED OUTPUT — run the built binary first, capture, then transcribe; the root golden therefore includes cobra's default help/completion command entries exactly as the binary renders them. Use the source files in read_first to cross-check the transcription (measured wiring: SIX root.AddCommand entries at main.go:77-82), never to generate the golden — do not synthesize expected help text from reading source, and do not hard-code a subcommand count anywhere in the test: every flag name, every default placeholder, every usage sentence comes from the capture. Assert exit code 0 per invocation. Write one test function per command group (root+acp, the four support commands, profile) so a future drift failure names the broken group. This is a characterization test of today's truth — it must PASS on the untouched tree before any carve work lands (that is its RED/GREEN collapsed into a baseline pin; per the phase's tdd_mode note this is one of the two sanctioned Wave-0 additions).
  </action>
  <verify>
    <automated>go test ./cmd/ass-guard/ -run TestCLIBinaryContract -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
- `go test ./cmd/ass-guard/ -run TestCLIBinaryContract -count=1` passes on the pre-carve tree.
- The test file contains golden literals for at minimum: acp serve (--profile, --max-concurrent, --profiles-dir, --work-dir, --no-engine, --ask-timeout, --audit-log), parity (--suite, --from-rollout, --profile, --profiles-dir, --results, --surprise-check, --cache-pin), checkpoint (list, restore), learning (list, revert), model-routing (validate, resolve), profile (check).
- Root-help golden names all six AddCommand entries from main.go lines 77-82 (profile, parity, acp, model-routing, learning, checkpoint — measured count) and is transcribed from the binary's actual --help output, which also carries cobra's default help/completion commands; the test asserts no hard-coded subcommand total.
- No production file is modified (`git status --porcelain cmd/ass-guard/ -- ':!cmd/ass-guard/cli_contract_test.go'` is empty after the task).
  </acceptance_criteria>
  <done>The operator-visible CLI surface is pinned by a test that fails loudly if any command or flag drifts during the carve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→binary argv | Zed spawns `ass-guard acp serve` with flags; the flag surface IS the trust contract (untrusted drift = broken or widened editor channel) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Tampering | cobra flag/command surface | medium | mitigate | cli_contract_test.go golden-pins every command + flag; fires on any drift introduced by later plans |
| T-15-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs this phase (RESEARCH Standard Stack: "this phase adds zero packages"); legitimacy gate not applicable |

</threat_model>

<verification>
- `go test ./cmd/ass-guard/ -run TestCLIBinaryContract -count=1` green.
- Ledger totals 130 and is committed.
- Full quick gate still green: `go build ./... && go vet ./... && go test ./cmd/ass-guard/ -count=1`.
</verification>

<success_criteria>
The phase now possesses both halves of its equivalence instrumentation: a counted baseline (what must still pass) and a contract pin (what the operator's editor invokes). Every subsequent plan's verify steps lean on these.
</success_criteria>

<output>
Create `.planning/phases/15-internal-runtime-carve-step-0/15-01-SUMMARY.md` when done
</output>
