---
phase: 15-internal-runtime-carve-step-0
plan: 07
type: execute
wave: 6
depends_on: ["15-06"]
files_modified:
  - .planning/phases/15-internal-runtime-carve-step-0/phase-review.md
autonomous: false
requirements: [RUNT-01]
estimate:
  tokens: 18000
  raw_tokens: 12000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "All four equivalence legs are green and recorded with actual numbers: mise ci, ledger match, zero duplicate test names, clean color-moved review"
    - "Every diff block rendered as edited (not moved) is classified as one of the documented sanctioned exceptions — zero UNEXPECTED entries"
    - "The operator has a concrete live-Zed checklist and their confirmation ( or explicit PENDING marker) is recorded"
  artifacts:
    - path: ".planning/phases/15-internal-runtime-carve-step-0/phase-review.md"
      provides: "Phase-close equivalence attestation: gate results, ledger accounting, color-moved census, sanctioned-exceptions list, live-Zed checklist"
      contains: "live-Zed"
  key_links:
    - from: ".planning/phases/15-internal-runtime-carve-step-0/phase-review.md"
      to: ".planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt"
      via: "review record cites the ledger's committed totals as the equivalence denominator"
      pattern: "test-ledger"
---

<objective>
Phase close-out: run the complete equivalence proof battery, produce the color-moved relocation review record, and hand the operator the live-Zed verification checklist.

Purpose: ROADMAP success criteria are three-legged — gate green (criterion 1), observable behavioral identity (criterion 2), reviewable pure-relocation diff (criterion 3). Plans 01–06 produce the evidence; this plan assembles and attests it.
Output: `.planning/phases/15-internal-runtime-carve-step-0/phase-review.md` + all gates green.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt
@.mise.toml
</context>

<artifacts_this_phase_produce>
- `.planning/phases/15-internal-runtime-carve-step-0/phase-review.md` — the phase-close equivalence attestation: ledger match numbers, color-moved diff summary with sanctioned-exceptions list, live-Zed checklist for the operator.
No production code is created or modified by this plan.
</artifacts_this_phase_produce>

<tasks>

<task type="auto">
  <name>Task 1: Run the full equivalence battery and write the review record</name>
  <files>.planning/phases/15-internal-runtime-carve-step-0/phase-review.md</files>
  <read_first>.planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt, .mise.toml, .planning/phases/15-internal-runtime-carve-step-0/15-PATTERNS.md (review recipe section)</read_first>
  <action>
Run and record, each with actual output numbers into phase-review.md: (1) mise ci exit status; (2) repo-wide ^func Test total vs ledger total (state both numbers and the delta explanation — golden tests from 15-01); (3) duplicate test-name check across packages (must be zero); (4) color-moved diff census: git diff master --color-moved=plain --stat -- cmd/ internal/ plus a moved-vs-edited block count — enumerate EVERY block rendered as edited rather than moved and classify it as one of the sanctioned categories: the two sanctioned de-cobra edits, the wrapper-retirement call-site re-points (setupProviderFactory/setupModelRouting identifier qualifications from 15-02/15-04), the RunnerConfig/NewRunner/rename mechanics, or test package/import lines. Write the attestation so the literal token UNEXPECTED appears ONLY as the failure marker: when every edited block is classified, record the census as "non-sanctioned edited blocks: 0 (none found)" — lowercase prose, never the uppercase token; if any block resists classification, record it under a literal "UNEXPECTED:" heading, whose presence blocks phase close; (5) go test ./cmd/ass-guard/ -run 'TestZeroConfigFirstRun|TestCLIBinaryContract' -count=1 result. Then write the operator's live-Zed checklist section: spawn ass-guard acp serve from Zed, send a prompt, observe token streaming, tool execution, restart-resume replay — comparing against daily-use memory of the v1.1 close surface (ROADMAP criterion 2). Commit the review doc.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-internal-runtime-carve-step-0/phase-review.md && test "$(grep -c UNEXPECTED .planning/phases/15-internal-runtime-carve-step-0/phase-review.md | tr -d ' ')" = "0" && mise ci</automated>
  </verify>
  <acceptance_criteria>
- `mise ci` exits 0 on the final tree.
- phase-review.md exists, is committed, and contains: ledger totals (expected vs actual), zero-duplicate confirmation, the sanctioned-exceptions enumeration with an explicit zero-count attestation for non-sanctioned edited blocks (phrased without the literal token; the token appears only when an unclassifiable block exists), and the live-Zed checklist.
- Binary handshake smoke + CLI contract test both green in the recorded output.
- No production file modified by this task (`git status --porcelain -- '*.go'` empty at close).
  </acceptance_criteria>
  <done>The equivalence evidence is assembled and attested in one committed review record.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator handoff checkpoint for the live-Zed verification</name>
  <what-built>The complete carved runtime: internal/runtime (+enginebridge) housing the turn runner, internal/acpserve composing it, six CLI-support packages extracted — all gates green per phase-review.md.</what-built>
  <how-to-verify>
Follow the live-Zed checklist in .planning/phases/15-internal-runtime-carve-step-0/phase-review.md:
1. Spawn ass-guard acp serve from Zed exactly as in daily use.
2. Send a prompt; confirm token streaming appears natively.
3. Exercise a tool call (file read/edit) and confirm native diffs render.
4. Restart Zed mid-session; confirm replay-on-restart matches v1.1 close behavior.
Comparison baseline: your daily-use memory of the pre-carve surface (ROADMAP success criterion 2).
  </how-to-verify>
  <resume-signal>Type "approved" if the session behaves identically, or describe any divergence observed.</resume-signal>
</task>
  <action>
Present the operator with the editor-session verification checklist from phase-review.md and request confirmation that their daily-use session behaves identically to v1.1 close (ROADMAP success criterion 2 — manual-only per RESEARCH validation map: requires editor + live credentials, bounded by automated handshake smoke). Record the operator's response in the SUMMARY. This is a human-verify checkpoint because criterion 2 is explicitly manual-only in the validation strategy; automated coverage bounds but cannot replace it. If workflow.auto_advance permits skipping human checkpoints in this configuration, still record the checklist as PENDING-OPERATOR-CONFIRMATION in the SUMMARY rather than silently passing it.
  </action>
  <verify>
    <automated>C=$(grep -c "live-Zed" .planning/phases/15-internal-runtime-carve-step-0/phase-review.md); echo "checklist-mentions=$C"; test "$C" -ge 1 && grep -c "PENDING-OPERATOR-CONFIRMATION\|approved" .planning/phases/15-internal-runtime-carve-step-0/*.md >/dev/null && echo "checklist present + disposition recorded"</automated>
  </verify>
  <acceptance_criteria>
- The SUMMARY records either an explicit operator confirmation or a PENDING-OPERATOR-CONFIRMATION marker for criterion 2 — never a silent pass.
- The checklist steps are concrete (spawn command, expected observations, comparison baseline).
  </acceptance_criteria>
  <done>The operator-facing verification path is documented and dispositioned honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Close-out plan touches only planning artifacts; no code, no surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-15 | Repudiation | phase-review attestation completeness | low | mitigate | Every gate result recorded with actual numbers; UNEXPECTED diff blocks block the close |
| T-15-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; legitimacy gate not applicable |

</threat_model>

<verification>
- All four legs green: mise ci, ledger match, duplicate-free tests, color-moved review clean.
- Review record committed.
</verification>

<success_criteria>
The phase closes with its three ROADMAP criteria evidenced: gate-proven equivalence, honest operator-handoff for the live session, and a reviewable pure-relocation diff record.
</success_criteria>

<output>
Create `.planning/phases/15-internal-runtime-carve-step-0/15-07-SUMMARY.md` when done
</output>
