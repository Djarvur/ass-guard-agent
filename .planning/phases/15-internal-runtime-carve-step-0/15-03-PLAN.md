---
phase: 15-internal-runtime-carve-step-0
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - cmd/ass-guard/checkpoint.go
  - cmd/ass-guard/learning_cmd.go
  - cmd/ass-guard/modelrouting.go
  - internal/checkpointcmd/checkpoint.go
  - internal/checkpointcmd/goconst_constants.go
  - internal/learningcmd/learning_cmd.go
  - internal/modelroutingcmd/modelrouting.go
  - cmd/ass-guard/checkpoint_test.go
  - cmd/ass-guard/learning_cmd_test.go
  - cmd/ass-guard/modelrouting_test.go
  - internal/checkpointcmd/checkpoint_test.go
  - internal/learningcmd/learning_cmd_test.go
  - internal/modelroutingcmd/modelrouting_test.go
autonomous: true
requirements: [RUNT-01]
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Checkpoint list/restore, learning list/revert, and model-routing resolve logic are callable from cmd shells through three cobra-free packages"
    - "The learning table's sanctioned stdout output and model-routing --json mode behave byte-identically from their new homes"
    - "cmd retains every cobra command definition with identical flags; only RunE bodies delegate"
  artifacts:
    - path: "internal/checkpointcmd/checkpoint.go"
      provides: "RunCheckpointList, RunCheckpointRestore (wrapping + stderr notes verbatim)"
      contains: "RunCheckpointRestore"
    - path: "internal/learningcmd/learning_cmd.go"
      provides: "ResolveLearnedPath, RunLearningList, RunLearningRevert"
      contains: "RunLearningRevert"
    - path: "internal/modelroutingcmd/modelrouting.go"
      provides: "EmitResolveHuman, EmitResolveJSON, DescribeCapabilities"
      contains: "DescribeCapabilities"
  key_links:
    - from: "cmd/ass-guard/checkpoint.go"
      to: "internal/checkpointcmd/checkpoint.go"
      via: "cobra shell parses flags then delegates to checkpointcmd.RunCheckpoint*"
      pattern: "checkpointcmd\\.RunCheckpoint"
---

<objective>
Extract the three self-contained CLI-support concerns — checkpoint, learning, model-routing run-logic — into their own packages per D-08, leaving only cobra shells in cmd.

Purpose: D-08 (one package per concern) + D-09 (cobra stays in cmd). These three extractions are independent of the carve closure and of each other; grouping them in one plan keeps each plan ≤3 tasks while preserving per-concern packages.
Output: `internal/checkpointcmd`, `internal/learningcmd`, `internal/modelroutingcmd` with relocated logic + tests; cmd shells delegate.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@cmd/ass-guard/checkpoint.go
@cmd/ass-guard/learning_cmd.go
@cmd/ass-guard/modelrouting.go
@cmd/ass-guard/checkpoint_test.go
@cmd/ass-guard/learning_cmd_test.go
@cmd/ass-guard/modelrouting_test.go
@internal/providerfactory/provider_factory.go
@cmd/ass-guard/goconst_constants.go
</context>

<artifacts_this_phase_produce>
- Package `internal/checkpointcmd`: exported `checkpointcmd.RunCheckpointList(stderr io.Writer, workDir string) error`, `checkpointcmd.RunCheckpointRestore(ctx context.Context, stderr io.Writer, workDir, id string) error`; unexported helpers; consts (checkpointRestoredNote etc.) ride along.
- Package `internal/learningcmd`: exported `learningcmd.ResolveLearnedPath(workDir string) (string, error)`, `learningcmd.RunLearningList(w io.Writer, stderr io.Writer) error`, `learningcmd.RunLearningRevert(...)`.
- Package `internal/modelroutingcmd`: exported `modelroutingcmd.EmitResolveHuman(w io.Writer, ...)`, `modelroutingcmd.EmitResolveJSON(w io.Writer, ...)`, `modelroutingcmd.DescribeCapabilities(...)`.
- Relocated test files per destination; cmd retains newCheckpointCmd/newLearningCmd/newModelRoutingCmd(+Validate/Resolve builders) as delegating shells.
</artifacts_this_phase_produce>

<tasks>

<task type="auto">
  <name>Task 1: Extract checkpoint + learning + model-routing logic into their own packages</name>
  <files>internal/checkpointcmd/checkpoint.go, internal/learningcmd/learning_cmd.go, internal/modelroutingcmd/modelrouting.go, cmd/ass-guard/checkpoint.go, cmd/ass-guard/learning_cmd.go, cmd/ass-guard/modelrouting.go</files>
  <read_first>cmd/ass-guard/checkpoint.go, cmd/ass-guard/learning_cmd.go, cmd/ass-guard/modelrouting.go, .claude/gsd-core/references/planner-mvp-mode.md is NOT needed here — instead read internal/providerfactory/provider_factory.go (the extraction precedent from plan 15-02), cmd/ass-guard/acp_serve.go lines 267-311 (resolveWorkDir consumer), internal/acp/goconst_constants.go (D-10 header style)</read_first>
  <action>
Per D-08/D-09 and the plan-15-02 precedent: for each of the three concerns, git mv the source file into its new package directory (checkpoint.go → internal/checkpointcmd/, learning_cmd.go → internal/learningcmd/, modelrouting.go → internal/modelroutingcmd/), then SPLIT it: the cobra command definitions (newCheckpointCmd :43-95, newLearningCmd :18-51, newModelRoutingCmd + Validate/Resolve builders :20-129) return to cmd/ass-guard/<same filename>.go as package main with bodies thinned to parse flags and call into the new packages; the run*/resolve*/emit*/describe* logic moves verbatim into the new package with ONLY export renames on what cmd calls (RunCheckpointList, RunCheckpointRestore, ResolveLearnedPath, RunLearningList, RunLearningRevert, EmitResolveHuman, EmitResolveJSON, DescribeCapabilities). Every //nolint rides its line (SP-2); preserve relative declaration order within each file (SP-2 funcorder rule); mise fmt at the end. Constants each moved body references copy from goconst_constants.go into that package's goconst_constants.go with the internal/acp header-comment style (D-10). Two specific entanglements: (1) runCheckpointRestore consumes resolveWorkDir which moves to acpserve in plan 15-05 — per PATTERNS.md planner note, prefer passing the resolved workDir string from the cmd shell (the shell already resolves flags), so checkpointcmd takes workDir as a plain parameter and does NOT import acpserve; if the current code path requires resolveWorkDir inside the logic function, move resolveWorkDir's body verbatim into checkpointcmd as an unexported helper INSTEAD of importing acpserve (duplication over import-cycle risk, consistent with D-03 discipline). (2) The learning table stdout exception (SP-4 sanctioned) rides with RunLearningList unchanged. Export nothing beyond what fails to compile otherwise (D-19).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/checkpointcmd/ ./internal/learningcmd/ ./internal/modelroutingcmd/ ./cmd/ass-guard/ -count=1 && grep -c "cobra" internal/checkpointcmd/*.go internal/learningcmd/*.go internal/modelroutingcmd/*.go | grep -v ':0' ; echo "cobra-free check done"</automated>
  </verify>
  <acceptance_criteria>
- `go build ./...` green; `go vet ./...` clean.
- All three new packages' relocated tests pass from their new homes.
- `go test ./cmd/ass-guard/ -count=1` still green (shells delegate correctly).
- None of the three new packages imports spf13/cobra (`grep -l "spf13/cobra" internal/{checkpointcmd,learningcmd,modelroutingcmd}/` returns nothing).
- cmd/ass-guard/checkpoint.go, learning_cmd.go, modelrouting.go retain only cobra definitions + flag parsing + delegation calls.
- Moved logic bodies differ from source only by package clause, exported names, imports.
- nolint directives present on their original lines (spot-check: contextcheck nolint on the Store.Open call in RunCheckpointRestore).
  </acceptance_criteria>
  <done>Three CLI concerns extracted package-per-concern; cmd owns CLI shape only.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate the three suites + wave-boundary gate</name>
  <files>internal/checkpointcmd/checkpoint_test.go, internal/learningcmd/learning_cmd_test.go, internal/modelroutingcmd/modelrouting_test.go, cmd/ass-guard/checkpoint_test.go</files>
  <read_first>cmd/ass-guard/checkpoint_test.go (lines 56-142 CLI tests vs 166+ sessionFor tests — subject split), cmd/ass-guard/learning_cmd_test.go, cmd/ass-guard/modelrouting_test.go, .planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt</read_first>
  <action>
Per D-01/D-02 subject rule: git mv learning_cmd_test.go → internal/learningcmd/ and modelrouting_test.go → internal/modelroutingcmd/ wholesale (pure CLI subjects, package rename only, helper duplication per D-03 if they reference cmd-only helpers). For checkpoint_test.go apply the MIXED split: CLI list/restore tests (:56-142) move to internal/checkpointcmd/checkpoint_test.go; TestSessionFor_WiresCheckpointer (:166+) and the gated live rollback test (:313+, constructs a runner literal :358) STAY in cmd/ass-guard/checkpoint_test.go at this wave (they construct the cmd-local runner type — plan 15-06 relocates them with the runner family; leaving them here keeps THIS task compiling before the carve lands). Env-gated skips travel with their files (SP-9: ASSGUARD_CHECKPOINT_E2E + ZAI_API_KEY gate rides the live rollback test wherever it physically sits this task). Duplicate any shared helpers per D-03 rather than extracting. Then run the wave-boundary gate per SP-11: mise ci green; ledger accounting: repo-wide ^func Test count must equal ledger total + golden-test additions from 15-01 (no losses, no dupes across packages — grep both homes for any test name appearing twice).
  </action>
  <verify>
    <automated>mise ci && N=$(grep -rh '^func Test' --include='*_test.go' cmd/ internal/ | wc -l | tr -d ' '); D=$(grep -rh '^func Test[A-Za-z]' --include='*_test.go' cmd/ internal/ | sort | uniq -d | wc -l | tr -d ' '); echo "total=$N dupe-names=$D"; test "$D" = "0"</automated>
  </verify>
  <acceptance_criteria>
- `mise ci` exits 0.
- Repo-wide test-function count equals ledger total + 15-01 additions; zero duplicate test-function names across packages.
- ASSGUARD_CHECKPOINT_E2E/ZAI_API_KEY gates present in whichever file carries the live rollback test.
- internal/checkpointcmd/checkpoint_test.go contains the list/restore CLI tests only; no runner-literal construction appears anywhere in it.
- cmd/ass-guard/checkpoint_test.go retains the sessionFor-subject tests untouched.
  </acceptance_criteria>
  <done>Test redistribution for the three CLI concerns complete; full equivalence gate green at the wave boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv→restore operation | runCheckpointRestore mutates the user's working tree from a CLI id argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering | RunCheckpointRestore arg handling | medium | mitigate | Verbatim move — wrapping ("checkpoint restore %q"), stderr notes, and store semantics byte-identical; suite covers failure paths |
| T-15-05 | Repudiation | learning table output | low | accept | Sanctioned stdout exception (SP-4) preserved as-is; format pinned by existing tests |
| T-15-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; legitimacy gate not applicable |

</threat_model>

<verification>
- `mise ci` green after Task 2.
- Three new packages exist, cobra-free, with relocated suites passing.
- Ledger accounting clean (no lost or duplicated test functions).
</verification>

<success_criteria>
The three self-contained CLI extractions are complete: package-per-concern homes exist, cmd shells delegate, the standing gate proves zero behavior change at the wave boundary.
</success_criteria>

<output>
Create `.planning/phases/15-internal-runtime-carve-step-0/15-03-SUMMARY.md` when done
</output>
