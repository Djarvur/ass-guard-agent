---
phase: 15-internal-runtime-carve-step-0
plan: 05
type: execute
wave: 4
depends_on: ["15-02", "15-04"]
files_modified:
  - cmd/ass-guard/acp_serve.go
  - internal/acpserve/acp_serve.go
  - internal/acpserve/options.go
  - internal/acpserve/goconst_constants.go
  - cmd/ass-guard/goconst_constants.go
autonomous: true
requirements: [RUNT-01]
estimate:
  tokens: 40000
  raw_tokens: 26000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "The serve pipeline (bus→profile→factory→routing→warnings→bodyStore→audit mirror→server→scheduler) executes in its original statement order from internal/acpserve"
    - "stdout still carries only ACP frames — the stdout-discipline tests guard from the new package"
    - "Exactly two non-verbatim edits ORIGINATE in this plan's moved serve code (the runACPServeCmd and resolveProfilesDir de-cobra adaptations); the only other in-body diff vs master is the setupModelRouting → providerfactory.SetupModelRouting call-site re-point inherited from 15-04's wrapper retirement (identifier qualification, no logic change)"
    - "cmd builds acpserve.Options from flags and calls into acpserve; cobra never leaks past the boundary"
  artifacts:
    - path: "internal/acpserve/acp_serve.go"
      provides: "Run composition (transitional PrepareServe/FinishServe with the finishHooks callback seam this plan, unified by 15-06), startAuditMirror, resolveWorkDir, seedACPGuard, de-cobra'd resolveProfilesDir"
      contains: "startAuditMirror"
    - path: "internal/acpserve/options.go"
      provides: "Exported Options struct, field set + comments identical to source serveOptions"
      contains: "AuditLogPath"
    - path: "internal/acpserve/serve_test.go"
      provides: "Relocated stdout-discipline, audit-shape, transcript-writer, mirror-override tests"
      contains: "TestACPServeWiresStdoutClean"
  key_links:
    - from: "cmd/ass-guard/acp_serve.go"
      to: "internal/acpserve/options.go"
      via: "cmd shell extracts flags (incl. de-cobra'd audit-log + profiles-dir changed bool) and builds acpserve.Options"
      pattern: "acpserve\\.Options"
    - from: "internal/acpserve/acp_serve.go"
      to: "internal/providerfactory/provider_factory.go"
      via: "perm warnings + factory setup call providerfactory exports in original order"
      pattern: "providerfactory\\.(WarnLooseConfigPerm|SetupProviderFactory)"
---

<objective>
Extract the ACP serve composition into `internal/acpserve` (D-06/D-07): Run(ctx, in, out, stderr, opts), exported Options, the profile-dir helpers, perm warnings, audit mirror, and the startup-order skeleton — with the two sanctioned de-cobra edits and nothing else.

Purpose: D-06 — serve wiring leaves cmd but is NOT kit; D-07 — all non-runner wiring moves verbatim with serveOptions→Options. This plan deliberately does NOT touch the runner family (acp_serve.go's runner half stays in place until plan 15-06) — acpserve initially composes today's in-cmd runner through a narrow seam, then plan 15-06 swaps it for runtime.NewRunner. This keeps each plan's diff reviewable.
Output: `internal/acpserve` package; runACPServe body relocated; cmd retains only cobra shells.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@cmd/ass-guard/acp_serve.go
@cmd/ass-guard/main.go
@internal/providerfactory/provider_factory.go
@internal/acp/server.go
</context>

<artifacts_this_phase_produce>
- Package `internal/acpserve`: exported `acpserve.Run(ctx context.Context, in io.Reader, out io.Writer, stderr io.Writer, opts Options) error`, exported struct `acpserve.Options` (verbatim field set from serveOptions :107-120), unexported startAuditMirror/resolveWorkDir/seedACPGuard/resolveProfilesDir(de-cobra'd)/runACPServe-body internals.
- The two sanctioned de-cobra adaptations (Pitfall 6 / PATTERNS de-cobra section): runACPServeCmd's audit-log flag read extracted to cmd caller; resolveProfilesDir takes a changed bool param.
- Deleted from cmd: everything except newACPCmd/newACPServeCmd shells + main.go + remaining runner-family code (until plan 15-06).
</artifacts_this_phase_produce>

<tasks>

<task type="auto">
  <name>Task 1: Create internal/acpserve with Options, helpers, and the Run body</name>
  <files>internal/acpserve/acp_serve.go, internal/acpserve/options.go, internal/acpserve/goconst_constants.go, cmd/ass-guard/acp_serve.go</files>
  <read_first>cmd/ass-guard/acp_serve.go lines 47-135 (consts, stubExecResult/nopHost/stubCatalogExec/toProfileDecls, serveOptions, redactorAdapter), lines 194-311 (runACPServeCmd, startAuditMirror, resolveWorkDir, seedACPGuard, resolveProfilesDir), lines 313-425 (runACPServe full body), .planning/phases/15-internal-runtime-carve-step-0/15-PATTERNS.md (acpserve section + startup-order invariant), .planning/phases/15-internal-runtime-carde-step-0 typo guard: correct path is .planning/phases/15-internal-runtime-carve-step-0/15-RESEARCH.md Pitfall 6/7, internal/providerfactory/provider_factory.go</read_first>
  <action>
Create package acpserve. Move verbatim per D-07: serveOptions → exported Options (identical fields incl. AuditLogPath override semantics comment); runACPServe → Run(ctx, in, out, stderr, opts Options) error reproducing the statement order line-for-line (Pitfall 7 invariant: event.NewBus → profile load → providerfactory.SetupProviderFactory → providerfactory.SetupModelRouting (the :339 call site re-pointed by 15-04 carries into the moved body verbatim) → perm warnings via providerfactory.GlobalConfigPath/ProjectConfigPath/WarnLooseConfigPerm → bodyStore → startAuditMirror → runner construction → loadCommandRegistry → engine degrade path → acp.NewServer(in,out,stderr,acp.WithTurnRunner(runner)) → sched.Open + degrade-loudly → runner.schedule assignment → runner.emitFor = srv.Emitter (WINDOWS #3) → startScheduler → ctx-done closeAllSessions goroutine → srv.Serve). Apply the ONLY two non-verbatim edits sanctioned this phase: (1) resolveProfilesDir loses its cobra dependency — cmd passes profilesDir string + changed bool; the moved function keeps identical logic on plain params; (2) runACPServeCmd's audit-log flag extraction happens in the cmd shell which passes auditPath as a param into acpserve.Run (or into Options.AuditLogPath before calling — executor's choice, document in SUMMARY). Also move verbatim: startAuditMirror (+ its staticcheck nolint), resolveWorkDir, seedACPGuard, and the const block subset acpserve uses (:47-66 members actually referenced by moved code — goconst lint catches misses per RESEARCH Don't Hand-Roll). stubCatalogExec/stubExecResult/nopHost/toProfileDecls/redactorAdapter do NOT move here (redactorAdapter's home is internal/runtime per OQ2; stubCatalogExec/nopHost/toProfileDecls are consumed by sessionFor/engine paths = runner family, plan 15-06). The runner-construction block inside Run stays AS THE CMD-TYPE LITERAL at this wave — since the runner type still lives in package main at this point, acpserve CANNOT construct it without importing main (illegal). Therefore: keep runACPServe's body IN cmd this task for its runner-touching statements ONLY by splitting differently — acpserve receives everything EXCEPT the runner literal block; cmd's runACPServe calls acpserve.PrepareServe(...) for the pre-runner pipeline (bus/profile/factory/routing/warnings/bodyStore/mirror), then constructs its local runner, then calls acpserve.FinishServe(...) for NewServer→emitFor→startScheduler→reap-goroutine→Serve. NAME THE SEAM: FinishServe's runner-touching operations cross the package boundary as CALLBACK HOOKS, never as field access — FinishServe receives the runner as the acp.TurnRunner interface plus an unexported finishHooks struct whose func fields cover exactly the three unexported-runner operations (emitFor injection, schedule assignment, startScheduler launch); cmd supplies the hooks as closures over its local runner's unexported members, and acpserve invokes each hook at the exact statement position from source :313-425 (WINDOWS #3 ordering preserved inside FinishServe). Unexported cmd-runner fields are therefore provably unreachable from internal/acpserve (package main cannot be imported); the closures stay in cmd until 15-06 dissolves the split. PrepareServe/FinishServe are INTERNAL composition helpers of the final Run shape; when plan 15-06 lands runtime.NewRunner, FinishServe's parameter becomes the runner and Run collapses to one function again (final form reached at end of plan 15-06, where this split is dissolved). Document this intermediate two-phase shape explicitly in the package doc comment as transitional, removed by plan 15-06.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/acpserve/ ./cmd/ass-guard/ -count=1</automated>
  </verify>
  <acceptance_criteria>
- Build + vet clean; existing serve-layer tests still green from wherever they sit this task.
- internal/acpserve contains Options (exported, identical field set + comments), PrepareServe, FinishServe, startAuditMirror, resolveWorkDir, seedACPGuard, de-cobra'd resolveProfilesDir(changed bool).
- Startup-order statement sequence inside PrepareServe+FinishServe matches source :321-424 order exactly (reviewable in diff).
- WINDOWS #3 ordering preserved: emitFor injection strictly between NewServer and startScheduler.
- Only the two de-cobra adaptations ORIGINATED by this plan, plus the inherited setupModelRouting→providerfactory.SetupModelRouting call-site re-point from 15-04 (identifier qualification), differ vs master within moved blocks; everything else is reviewable as moved (git diff).
- FinishServe's runner interactions flow exclusively through the finishHooks callback seam: hook invocation order matches the source :313-425 statement positions, and internal/acpserve references no cmd-local runner type or unexported field (impossible by import law; grep internal/acpserve for direct runner-member access = none).
- cmd/ass-guard/acp_serve.go retains only newACPCmd/newACPServeCmd shells + the runner-family declarations not yet moved.
- No cobra import in internal/acpserve.
  </acceptance_criteria>
  <done>acpserve exists with the serve pipeline in two transitional halves around the still-in-cmd runner; the two sanctioned edits applied.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate serve-layer tests + wave gate</name>
  <files>internal/acpserve/serve_test.go, cmd/ass-guard/acp_serve_test.go</files>
  <read_first>cmd/ass-guard/acp_serve_test.go (27 tests; subject map in RESEARCH inventory table), .planning/phases/15-internal-runtime-carve-step-0/test-ledger.txt</read_first>
  <action>
Per D-02 subject rule: move the runACPServe-driven tests to internal/acpserve/serve_test.go — specifically TestACPServeWiresStdoutClean (:84), TestACPServeNoStdoutPollutionFromLogs (:132), TestServeAudit_RequestShapedThroughRealSeam (:1537), TestServeTranscriptWriter_OnePerSession (:1412), TestServeMirror_Override (:1926), and TestACPServeCommandRegistered (:35) STAYS in cmd (cobra-shape subject). Adapt their runACPServe call sites to the PrepareServe/FinishServe transitional shape (call-through equivalent; the adaptation dissolves in plan 15-06 when Run reunifies — keep the test edits mechanical and note them as transitional in comments). Duplicate helpers per D-03 (fakeProfileACP etc. stay available to BOTH homes during transition — duplicate what the moved tests need). Keep runner-subject tests (newExpansionRunner battery etc.) in cmd/ass-guard/acp_serve_test.go untouched until plan 15-06 relocates them with the runner family. Then wave gate per SP-11: mise ci green; ledger accounting (no lost/dup tests).
  </action>
  <verify>
    <automated>mise ci && N=$(grep -rh '^func Test' --include='*_test.go' cmd/ internal/ | wc -l | tr -d ' '); D=$(grep -rh '^func Test[A-Za-z]' --include='*_test.go' cmd/ internal/ | sort | uniq -d | wc -l | tr -d ' '); echo "total=$N dupes=$D"; test "$D" = "0" && test "$N" -ge 130</automated>
  </verify>
  <acceptance_criteria>
- `mise ci` exits 0.
- Serve-layer stdout-discipline tests (WiresStdoutClean / NoStdoutPollutionFromLogs) pass from internal/acpserve.
- Zero duplicate test names repo-wide; ledger total accounted in SUMMARY.
- TestACPServeCommandRegistered remains in cmd passing.
  </acceptance_criteria>
  <done>Serve layer extracted with its guarding tests riding along; wave boundary proven green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdio frames | stdout carries only ACP frames — any diagnostic leak corrupts the editor channel |
| schedule store | sched.Open reads operator workdir state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-08 | Information Disclosure | stdout transport discipline | high | mitigate | WiresStdoutClean + NoStdoutPollutionFromLogs relocate WITH acpserve and keep guarding (RESEARCH threat table row 2) |
| T-15-09 | Repudiation | startAuditMirror + bodyStore ordering | medium | mitigate | Statement order pinned line-for-line in FinishServe; TestServeAudit_*/TestServeMirror_Override cover from new home |
| T-15-10 | DoS | schedule store open failure | low | mitigate | Degrade-loudly branch (schedule disabled message) preserved byte-identical |
| T-15-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package installs; legitimacy gate not applicable |

</threat_model>

<verification>
- `mise ci` green after Task 2.
- internal/acpserve compiles cobra-free; Options exported; startup-order invariant intact.
- Ledger accounting clean.
</verification>

<success_criteria>
Serve composition has its final home (transitional two-phase shape documented and scheduled for dissolution in 15-06); both sanctioned de-cobra edits landed; transport + audit guards ride along.
</success_criteria>

<output>
Create `.planning/phases/15-internal-runtime-carve-step-0/15-05-SUMMARY.md` when done
</output>
