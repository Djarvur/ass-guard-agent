---
phase: 18-session-family
plan: 06
type: execute
wave: 3
depends_on: ["18-05"]
files_modified:
  - cmd/ass-guard/main.go
  - cmd/ass-guard/acp_serve.go
  - cmd/ass-guard/picker.go
  - cmd/ass-guard/resume_flags_test.go
  - internal/acpserve/options.go
  - internal/acpserve/acp_serve.go
  - internal/acpserve/resume_test.go
autonomous: true
requirements: [ACP-06]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full CC trio works from the CLI (D-10): `ass-guard --resume` opens the interactive picker; `--resume <id|name>` (space form, `=` form, or positional after a bare flag) resumes directly; `--continue`/`-c` resumes the most recent session in the CURRENT directory with zero ceremony; combining any resume flag with --prompt is a clean typed error"
    - "`--resume`/`--continue` are registered as persistent flags on the ROOT command so both `ass-guard --resume` and `ass-guard acp serve --resume <id>` work (cobra persistent-flag inheritance; cmd.Flags() resolves inherited flags in the child RunE) — the flag is available in any launch context, not only via editor RPC (ROADMAP criterion 3)"
    - "The picker is pipe-safe terminal I/O (D-11): numbered rows on stderr (`N) <title-60-runes>  (<relative time>)`, top 10 by recency), a number read line-wise from stdin, one re-prompt on an invalid entry, EOF or a second invalid entry exits non-zero with a clear error — no raw-mode/termios dependency anywhere in the path"
    - "The selected or resolved target is injected into the serve composition BEFORE acpserve.Run's Serve step via Options.ResumeTarget, and acpserve.Run loads it through the SAME load engine as session/load (Server.LoadSession) — one engine, two entrypoints (research OQ3 resolved)"
    - "--continue resolution is cwd-scoped (matching CC's documented 'most recent interactive session in the current directory'); --resume <id|name> resolves against the cwd store with a clear not-found error naming the directory searched — cross-project registry search is a documented deviation (see <deviation_note>)"
    - "A target id is validated against sessIDPattern before any path construction; name resolution matches the title prefix case-insensitively, errors with the candidate list when ambiguous, and errors cleanly when nothing matches"
  artifacts:
    - path: "cmd/ass-guard/picker.go"
      provides: "func SelectSession(rows []session.SessionHeader, in io.Reader, out io.Writer) (string, error) — the numbered-list picker; plus FormatRelativeTime(t time.Time, now time.Time) string"
      contains: "SelectSession"
    - path: "cmd/ass-guard/main.go"
      provides: "root persistent flags --resume (string, NoOptDefVal picker sentinel) and --continue/-c (bool); root RunE delegation to the serve flow when a resume flag is set"
      contains: "resume"
    - path: "internal/acpserve/options.go"
      provides: "Options.ResumeTarget string (empty = no initial load)"
      contains: "ResumeTarget"
    - path: "internal/acpserve/acp_serve.go"
      provides: "Run consumes Options.ResumeTarget: after NewServer, before Serve, calls Server.LoadSession — failure is a loud fatal, never a silent no-load serve"
      contains: "ResumeTarget"
  key_links:
    - from: "cmd/ass-guard/acp_serve.go"
      to: "internal/acpserve/options.go"
      via: "the serve RunE resolves the resume target (direct id, name match, picker, or --continue first-row) and sets Options.ResumeTarget before acpserve.Run"
      pattern: "ResumeTarget"
    - from: "internal/acpserve/acp_serve.go"
      to: "internal/acp/handlers.go"
      via: "acpserve.Run calls the exported Server.LoadSession core (18-01) — the identical engine session/load uses"
      pattern: "LoadSession"
    - from: "cmd/ass-guard/picker.go"
      to: "internal/session/list.go"
      via: "picker rows come from session.ListSessions (already tombstone-filtered and recency-ordered — first row is the --continue target)"
      pattern: "ListSessions\\("
---

<deviation_note>
Planner decision (within CONTEXT "Claude's Discretion" + research OQ1/A2): CC's `--resume <session-id>` resolves across ALL projects via a central registry (`~/.claude/projects/`). ass-guard's store is per-directory with no registry, and ROADMAP criterion 3 glosses "works anywhere" as CLI-flag availability ("not only from the editor"), which this plan delivers. Resolution is therefore cwd-scoped with a clear not-found error naming the directory searched. Cross-project unique-match search would need a new project-directory registry — a store design decision outside this phase's sources; it is surfaced here as a documented deviation for the operator, not silently descoped. The Deferred Ideas list is untouched (un-delete surface remains the only deferral).
</deviation_note>

<objective>
The CC-parity CLI resume surface: the full flag trio on the root command, the pipe-safe numbered picker, name/id resolution against the cwd store, and serve-side target injection through the same load engine session/load uses — so `ass-guard --resume`, `ass-guard --resume <id>`, and `ass-guard -c` all land the user in a fully replayed, reconciled session.

Purpose: ROADMAP criterion 3 ("`ass-guard --resume` works anywhere (CLI flag), not only from the editor") and D-10/D-11 verbatim — the operator wants the daily-driver convenience at parity, not just the resumability primitive.
Output: root + serve flag wiring, picker.go, Options.ResumeTarget consumption, tests across cmd and acpserve.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-session-family/18-CONTEXT.md
@.planning/phases/18-session-family/18-RESEARCH.md
@.planning/phases/18-session-family/18-PATTERNS.md
@.planning/phases/18-session-family/18-01-PLAN.md
@cmd/ass-guard/main.go
@cmd/ass-guard/acp_serve.go
@internal/acpserve/options.go
@internal/acpserve/acp_serve.go
@internal/session/list.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Root + serve flag trio with resolution semantics</name>
  <files>cmd/ass-guard/main.go, cmd/ass-guard/acp_serve.go, cmd/ass-guard/resume_flags_test.go</files>
  <behavior>
    - TestResumeFlagParsing: `--resume` bare sets the picker sentinel; `--resume=<id>` sets the id; `--resume <id>` (space form) leaves the id in args for the RunE to consume as the target; `--continue` and `-c` both set the bool; `--resume x --prompt y` produces the typed combination error.
    - TestContinueResolvesMostRecentCwd: a temp store with three sessions of distinct mtimes — the --continue resolution returns the newest non-tombstoned id for THAT directory; an empty store returns a clean not-found error naming the directory.
    - TestResumeTargetResolution: an exact sessIDPattern-valid id resolves to itself; a name matching one title prefix (case-insensitive) resolves to that session; a prefix matching two titles errors listing both candidates; a prefix matching nothing errors naming the directory searched; a traversal-shaped id is rejected before any file access.
    - Root RunE with a resume flag set and no --prompt delegates to the serve flow (assert via the testable seam — see action) with the resolved target in Options; with --prompt also set, errors.
  </behavior>
  <read_first>cmd/ass-guard/main.go (root command construction, the --prompt one-shot contract), cmd/ass-guard/acp_serve.go (newACPServeCmd :44-77 flag-registration pattern, runACPServeCmd extraction precedent, stderr-only diagnostics discipline), internal/acpserve/options.go (Options struct field style + doc comments), internal/session/list.go (ListSessions ordering — first row = most recent; SessionHeader.Title for name matching), internal/coreexec/messaging.go (sessIDPattern — copy or reference), cmd/ass-guard existing tests (the cobra-tree test conventions that stayed in cmd after 15-03)</read_first>
  <action>
RED: write resume_flags_test.go with the four test functions; use the cmd test conventions already in the package (cobra-tree tests stayed here by the 15-03 decision). For delegation assertions, extract the root resume branch into a testable function (e.g. resolveResumeTarget(args, flags) and a runServeWithTarget(target string) indirection the test can intercept — follow how runACPServeCmd was extracted for testability). Commit failing.
GREEN: main.go — register persistent flags on root: resume as a string flag with NoOptDefVal set to a sentinel constant (e.g. pickSentinel), continue as a bool with shorthand c. Root RunE: if either is set, error on --prompt combination, resolve the target (bare sentinel + args[0] present = direct target; sentinel alone = picker mode; --continue = ListSessions first row in the cwd store), then delegate to the serve flow with Options.ResumeTarget set. acp_serve.go — the serve RunE reads the inherited flags via cmd.Flags() (cobra resolves parent persistent flags there) and performs the SAME resolution, setting Options.ResumeTarget before acpserve.Run; bare `acp serve --resume` opens the picker too (same code path, shared helper in one place — put resolveResumeTarget in acp_serve.go or a small shared file in cmd, not duplicated). Name matching: ListSessions over the cwd store, case-insensitive prefix match on Title, ambiguity = error listing candidates (id + title), miss = error naming the searched directory. All diagnostics to stderr. Commit green.
  </action>
  <verify>
    <automated>go test -race -count=1 ./cmd/ass-guard/ -run 'TestResumeFlag|TestContinueResolves|TestResumeTarget'</automated>
  </verify>
  <acceptance_criteria>
- All four test functions pass under -race; the three flag forms (bare, =, space) and both continue spellings behave identically to the behavior block.
- The combination error names both flags; the not-found and ambiguous errors name the directory/candidates.
- cmd/ass-guard/main.go registers both flags as persistent on root (grep PersistentFlags with the two names); the serve path resolves them via cmd.Flags().
- No termios/raw-mode import appears anywhere in cmd (grep for termios, raw-mode constructs returns nothing).
  </acceptance_criteria>
  <done>The trio parses and resolves everywhere it is typed, with cwd-scoped semantics and clean errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The numbered picker (D-11)</name>
  <files>cmd/ass-guard/picker.go, cmd/ass-guard/resume_flags_test.go</files>
  <behavior>
    - TestPickerSelectsByNumber: three header rows; stdin "2" selects the second id; rows render on the writer as `1) <title-60-runes>  (<rel time>)` with one prompt line.
    - TestPickerRetriesOnceThenFails: stdin "abc" then "9" (out of range) with three rows — after the second invalid entry the picker errors; stdin EOF immediately errors; empty stdin line counts as invalid.
    - TestPickerRelativeTime: 90 seconds -> "2m ago"-class output (minutes/hours/days buckets — assert the bucket word, not the exact locale formatting); a 0-age session shows the just-now bucket.
    - TestPickerPipeSafe: driving with an io.Pipe reader and a bytes.Buffer writer (no *os.File anywhere in the signature) — the exact surfaces `ass-guard --resume` uses over ssh/pipes.
    - TestPickerTruncatesTitles: a 200-rune title renders as exactly 60 runes followed by the time bucket.
  </behavior>
  <read_first>cmd/ass-guard/acp_serve.go (stderr-only diagnostics discipline + the log.New stderr precedent at internal/acp/server.go:114), internal/session/list.go (SessionHeader — the row source), .planning/phases/18-session-family/18-RESEARCH.md (CC picker row content calibration; Pitfall 10)</read_first>
  <action>
RED: add the five picker tests to resume_flags_test.go. Commit failing.
GREEN: create picker.go with SelectSession(headers []session.SessionHeader, in io.Reader, out io.Writer) (string, error): print at most 10 rows (ListSessions already ordered — take the first 10), each `N) title  (rel)`, title truncated unicode-safely to 60 runes, rel time via FormatRelativeTime with buckets (seconds -> "just now"; minutes; hours; days; weeks). Print the selection prompt, read one line via bufio.Reader (trim whitespace), parse an int in 1..len(rows); invalid -> reprint the prompt once; second invalid or EOF -> typed error. Return the chosen SessionID. The caller (flag resolution from Task 1) passes os.Stdin/os.Stderr at the composition edge ONLY — the function itself stays io.Reader/io.Writer for pipe safety. Commit green.
  </action>
  <verify>
    <automated>go test -race -count=1 ./cmd/ass-guard/ -run 'TestPicker'</automated>
  </verify>
  <acceptance_criteria>
- All five picker tests pass; SelectSession's signature takes io.Reader/io.Writer (no os.File, no termios).
- Row format is exactly one line per row plus one prompt line; retry-once-then-fail and EOF behaviors asserted.
- Title truncation is rune-safe at 60; relative time uses the minute/hour/day/week buckets.
  </acceptance_criteria>
  <done>The picker works over pipes, ssh, and plain TTYs with one retry and clean EOF failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Serve-side target injection through the one load engine</name>
  <files>internal/acpserve/options.go, internal/acpserve/acp_serve.go, internal/acpserve/resume_test.go, cmd/ass-guard/acp_serve.go</files>
  <behavior>
    - TestResumeTargetLoadsBeforeServe: an Options with ResumeTarget set against a temp store holding a clean fixture transcript — the serve composition loads the session through Server.LoadSession BEFORE Serve begins; the client pipe observes the replay frames; a follow-up prompt continues the turn sequence. (Drive via the same serve-test harness pattern internal/acpserve already uses — read the 16-06 simulator test.)
    - TestResumeTargetAbsentIsNoop: empty ResumeTarget performs no load and serve starts normally (zero load-side frames).
    - TestResumeTargetFailureIsLoud: ResumeTarget pointing at a nonexistent transcript makes Run return an error (never a silently fresh serve).
  </behavior>
  <read_first>internal/acpserve/options.go (Options struct + doc-comment style), internal/acpserve/acp_serve.go (Run's statement order — NewServer is where the injection lands, before Serve; 15-06 pinned the order), internal/acp/handlers.go (Server.LoadSession — the 18-01 exported core), internal/acpserve existing serve tests (the runServeForTest/simulator harness conventions)</read_first>
  <action>
RED: write internal/acpserve/resume_test.go with the three tests using the package's existing serve-driving harness. Commit failing.
GREEN: options.go gains the ResumeTarget string field with a doc comment stating the contract (resolved pre-serve by cmd; empty = none). acp_serve.go Run: after NewServer and before Serve, if Options.ResumeTarget is non-empty call srv.LoadSession(ctx, target) (ctx = the serve ctx); on error return it wrapped with the target id (loud fatal — the CLI user asked for THAT session). cmd/ass-guard/acp_serve.go sets the field from the Task 1 resolution. Commit green with message feat(18-06): CLI resume trio wired through the load engine.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acpserve/ -run 'TestResumeTarget' && go test -race -count=1 ./cmd/ass-guard/ && mise build</automated>
  </verify>
  <acceptance_criteria>
- All three resume-target tests pass; the load happens strictly before Serve (frame ordering on the pipe proves it).
- Options.ResumeTarget exists with the documented contract; the failure path returns an error naming the target.
- The full cmd test suite stays green and the binary builds (`mise build` or the project's build task).
  </acceptance_criteria>
  <done>`ass-guard --resume`, `--resume <id|name>`, and `--continue`/`-c` all land in a replayed, reconciled session through the same engine as the editor's session/load.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal stdin → picker | user-typed (or piped) selection input |
| CLI args → resolution | the target id/name string is user input reaching path construction and the load engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-13 | Tampering | traversal-shaped resume target reaching path joins | high | mitigate | sessIDPattern validation before resolution file access; name-form targets never touch paths directly (matched against ListSessions results, the matched id then validated) |
| T-18-14 | DoS | picker fed an endless stdin of garbage | low | mitigate | retry-once-then-error bound; line-wise bufio reading with the existing buffer caps |
| T-18-15 | Spoofing | a name-prefix match selecting the wrong session silently | low | mitigate | ambiguity errors list all candidates; the picker shows ids implicitly through ordered rows and the resolution error messages name the directory searched |
| T-18-SC | Tampering | package installs | low | accept | No new packages (stdlib + existing cobra) |
</threat_model>

<verification>
- `go test -race -count=1 ./cmd/ass-guard/ ./internal/acpserve/` green.
- `mise ci` (full gate) before phase verification.
- Manual (phase verification): `ass-guard --resume` in a terminal with past sessions; `ass-guard -c` in a project dir with a recent session; both over `ssh`/pipe to prove D-11.
</verification>

<success_criteria>
- The trio works from any launch context with cwd-scoped resolution and clean errors.
- One load engine serves both entrypoints (RPC and CLI) — no second resume implementation.
- Picker pipe-safe with bounded retries; serve injection ordered before Serve.
</success_criteria>

<output>
Create `.planning/phases/18-session-family/18-06-SUMMARY.md` when done
</output>

## Artifacts this phase-plan produces

- `cmd/ass-guard/picker.go` — `SelectSession`, `FormatRelativeTime`
- `cmd/ass-guard/main.go` — root persistent flags `--resume` (picker-sentinel NoOptDefVal) and `--continue`/`-c`
- `cmd/ass-guard/acp_serve.go` — shared `resolveResumeTarget` + Options.ResumeTarget population
- `internal/acpserve/options.go` — `Options.ResumeTarget`
- `internal/acpserve/acp_serve.go` — pre-Serve load injection
- New tests: `cmd/ass-guard/resume_flags_test.go`, `internal/acpserve/resume_test.go`
