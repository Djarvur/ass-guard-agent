---
phase: 18-session-family
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/reconcile.go
  - internal/session/reconcile_test.go
  - internal/session/manager.go
  - internal/session/testdata/reconcile/class01_dangling_tool_call.jsonl
  - internal/session/testdata/reconcile/class02_turn_without_terminal.jsonl
  - internal/session/testdata/reconcile/class03_ask_suspended_unresolved.jsonl
  - internal/session/testdata/reconcile/class04_subagent_unresolved.jsonl
  - internal/session/testdata/reconcile/class05_open_chunk_stream.jsonl
  - internal/session/testdata/reconcile/class06_plan_mode_state.jsonl
  - internal/session/testdata/reconcile/class07_turn_sequence.jsonl
  - internal/session/testdata/reconcile/class08_request_without_usage.jsonl
  - internal/session/testdata/reconcile/class09_missing_session_end.jsonl
autonomous: true
requirements: [ACP-06]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every dangling-expectation class in the ten-row live-state inventory gets exactly its defined synthetic closure: dangling tool_call -> failed tool_result, turn without terminal -> canceled line, unresolved ask_suspended -> failed tool_result for its toolCallID (cancelled-normal), dangling subagent_dispatch -> failed subagent_result, open agent_message_chunk stream -> terminal assistant_message, missing session_end -> synthetic session_end (18-RESEARCH Live-State Inventory rows 1-5, 9; D-02)"
    - "Every synthetic closure carries provenance: Line.Cause set to the interrupt marker, subtle in replay, unambiguous on disk (D-02); classes 6/7/8 append NOTHING (they are state seeds or audit-only pairs — rows 6, 7, 8)"
    - "The engine reads the transcript ALONE: Reconcile takes []Line and returns closures + seed, performs no I/O, and no session-state sidecar is created anywhere (D-01)"
    - "Seed carries MaxTurns (max %03d turn suffix) and the persisted plan-mode target from the LAST plan_mode line's Cause (rows 6-7; Pitfall 3)"
    - "Reconciliation is idempotent: a transcript that already contains the closures classifies as clean — a second pass returns zero closures and the same seed (18-RESEARCH kill-during-replay row)"
    - "A cleanly-closed transcript returns zero closures; a transcript with a torn/garbage final line is classified over its conforming prefix without error (readers skip non-conforming lines)"
    - "Synthetic closures are APPENDED on disk at load time (18-RESEARCH A6 resolved: on-disk provenance per D-02 'unambiguous on disk'), never rewritten into existing lines"
  artifacts:
    - path: "internal/session/reconcile.go"
      provides: "const InterruptedCause; type Seed struct {MaxTurns int64; PlanMode string; PlanModePresent bool}; func Reconcile(sessionID string, lines []Line) (closures []Line, seed Seed) — pure classification over the ten-row inventory"
      contains: "func Reconcile("
    - path: "internal/session/manager.go"
      provides: "AppendSynthetic(line *Line) error — exported append for reconciliation closures using the same marshal→redact→mutex→single-write discipline as appendLine"
      contains: "AppendSynthetic"
    - path: "internal/session/testdata/reconcile/"
      provides: "one hand-written fixture per inventory row, named classNN_<slug>.jsonl (fixture discipline per row number)"
  key_links:
    - from: "internal/runtime/runtime.go (18-05 integration)"
      to: "internal/session/reconcile.go"
      via: "Runner.ResumeSession will call Reconcile then Manager.AppendSynthetic per closure then SeedResume with the full seed — this plan delivers the engine and the append seam, 18-05 wires them"
      pattern: "Reconcile\\("
    - from: "internal/session/manager.go"
      to: "internal/session/reconcile.go"
      via: "closures returned by Reconcile flow through AppendSynthetic so redaction discipline covers synthetic content too"
      pattern: "AppendSynthetic"
---

<objective>
The reconciliation engine as a pure transcript pass: classify every dangling expectation a kill -9 mid-turn can leave on disk (the ten-row live-state inventory from 18-RESEARCH), produce provenance-marked synthetic closure lines for each, and derive the state seeds (turn counter, plan mode) — all from the transcript alone, with zero sidecar state.

Purpose: ROADMAP criterion 2's "no ghost state" after kill -9 is exactly this classification; D-01 makes the transcript the single source of truth, so the engine must be a read-only classifier plus append-only closures. Building it pure (no I/O) makes every inventory row table-testable before the load path consumes it (18-05).
Output: internal/session/reconcile.go with Reconcile + Seed, Manager.AppendSynthetic, one fixture per inventory class, all green under -race.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-session-family/18-CONTEXT.md
@.planning/phases/18-session-family/18-RESEARCH.md
@internal/session/transcript.go
@internal/session/manager.go
@internal/session/session.go
@internal/session/reconstruction_test.go
</context>

<assumptions>
- FLAGGED ASSUMPTION (edge-probe row, ACP-06, category unclassified — deliberately NOT auto-resolved): the ten-row inventory is assumed COMPLETE for kill -9 ghost-state coverage. The classification is total by construction — line kinds outside the inventory are state/bookkeeping kinds that cannot dangle (readers skip unknown kinds per 16-D-20, so an unknown kind is never classified as dangling). If a future line kind introduces a new pair-or-state semantic, its row must be added here; the reconcile test's table is the checklist. Surfaced, not silently dropped.
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — inventory fixture table + failing Reconcile tests</name>
  <files>internal/session/reconcile_test.go, internal/session/testdata/reconcile/class01_dangling_tool_call.jsonl, internal/session/testdata/reconcile/class02_turn_without_terminal.jsonl, internal/session/testdata/reconcile/class03_ask_suspended_unresolved.jsonl, internal/session/testdata/reconcile/class04_subagent_unresolved.jsonl, internal/session/testdata/reconcile/class05_open_chunk_stream.jsonl, internal/session/testdata/reconcile/class06_plan_mode_state.jsonl, internal/session/testdata/reconcile/class07_turn_sequence.jsonl, internal/session/testdata/reconcile/class08_request_without_usage.jsonl, internal/session/testdata/reconcile/class09_missing_session_end.jsonl</files>
  <behavior>
    - Table-driven TestReconcile with one subtest per fixture class01..class09 asserting the exact closure set (kinds, TurnID/ToolCallID/SubagentTurnID keying, IsError flags, Cause == InterruptedCause) and the seed values.
    - TestReconcileIdempotent: feeding a fixture's lines plus its expected closures back through Reconcile yields zero closures and an unchanged seed.
    - TestReconcileClean: a cleanly-closed multi-turn fixture yields zero closures, MaxTurns equal to its highest turn number, and PlanModePresent only when a plan_mode line exists.
    - TestReconcileSkipsGarbageTail: a fixture ending in a non-JSON garbage line classifies over the conforming prefix (the engine receives the reader-filtered slice; the test documents the reader contract).
  </behavior>
  <read_first>internal/session/transcript.go (Line envelope :61-115 — every field the closures need: ToolCallID, IsError, Cause, ParentTurnID/SubagentTurnID, MessageID; kind consts :18-49), internal/session/manager.go (AppendCanceled :195-197 as the synthetic-terminal precedent, AppendToolResult :163, AppendSubagentResult :225, AppendAskSuspended :245 — signature shapes the closures mirror), internal/session/session.go (nextTurnID :139-143 — the %03d suffix format MaxTurns scans), internal/session/planmode.go (TypePlanMode :67, enter/exit cause values), internal/session/reconstruction_test.go (fixture discipline and the fake provider harness), .planning/phases/18-session-family/18-RESEARCH.md (Live-State Inventory table — the row-by-row contract)</read_first>
  <action>
Write the ten fixtures by hand (class10 needs no fixture — it is class-3 machinery applied to post-17 permission asks; a comment in the test file records this). Each fixture is a minimal realistic transcript exercising ONLY its row: class01 has session_start, user_message, tool_call with no tool_result; class02 has turn lines with no canceled/error terminal; class03 has ask_suspended with no resolution; class04 subagent_dispatch with no subagent_result; class05 agent_message_chunk stream with no assistant_message; class06 ends in a plan_mode enter line; class07 has turns 001..004 with a gap (003 absent) to prove max-scan not count-scan; class08 request_shaped with no usage; class09 a full clean conversation with NO session_end. Use the exact on-disk field spellings from the Line envelope (camelCase json tags). Write reconcile_test.go with the four test functions above; reference InterruptedCause, Seed{MaxTurns, PlanMode, PlanModePresent}, and Reconcile(sessionID, lines) which do not exist yet. Run to confirm compile failure counts as RED. Commit with message test(18-02): add failing reconciliation inventory tests.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run TestReconcile 2>&1 | tail -5; test ${PIPESTATUS[0]} -ne 0 && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
- internal/session/testdata/reconcile/ contains exactly the nine classNN fixtures with valid JSON lines (verify: every line of every fixture parses as JSON in the test setup).
- reconcile_test.go declares TestReconcile (table over nine subtests), TestReconcileIdempotent, TestReconcileClean, TestReconcileSkipsGarbageTail.
- The test run fails to compile or fails red — confirming the engine does not exist — and the RED commit is present in git log.
  </acceptance_criteria>
  <done>The full inventory contract is encoded as failing tests with one fixture per row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Reconcile engine + AppendSynthetic append seam</name>
  <reversibility rating="one-way">D-01 transcript-as-truth: the no-sidecar resume contract becomes load-bearing for every later session feature — the operator chose this exact door in CONTEXT D-01, so this records the rating without a new checkpoint.</reversibility>
  <files>internal/session/reconcile.go, internal/session/manager.go</files>
  <behavior>
    - Reconcile implements rows 1-9 of the inventory with the exact closures the RED tests assert; rows 6/7 populate the seed and append nothing; row 8 appends nothing and only documents the audit-only pair.
    - Pair matching is keyed (tool_result matches by ToolCallID, subagent_result by SubagentTurnID, ask resolution by the toolCallID the ask_suspended carries) — never by line adjacency.
    - Turn-terminal detection treats canceled, error, and session_end lines as terminals for their TurnID.
    - Manager.AppendSynthetic writes through the same marshal→redact→mutex→single-write path as appendLine (closures containing user-shaped text still cross the redactor — only raw-thinking bytes are redaction-exempt per 16-D-23).
  </behavior>
  <read_first>internal/session/reconcile_test.go (the contract), internal/session/manager.go (appendLine :76-103 — copy its discipline), internal/session/transcript.go (Line fields), the RED fixtures</read_first>
  <action>
GREEN: create internal/session/reconcile.go with InterruptedCause = "interrupted" (doc comment citing D-02), the Seed struct, and Reconcile as a single ordered pass: collect open tool_call ids, open ask_suspended toolCall ids, open subagent dispatch ids, per-turn terminal presence, open chunk-stream turn/message ids, last plan_mode cause, max turn suffix (reuse the suffix parsing MaxTurnCounter will share — either call seed.go's MaxTurnCounter from 18-01 if landed, or keep the scan local; prefer calling seed.go to avoid two scanners). Emit closures in transcript order of their dangling opener. Create Manager.AppendSynthetic in manager.go beside the other Append* one-liners, delegating to the existing appendLine with the line's Cause already set. Run the tests; iterate to green. REFACTOR if obvious duplication emerges (e.g. shared pair-key helpers) — tests must stay green. Commit with message feat(18-02): implement reconciliation engine. If a refactor commit lands, use refactor(18-02).
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ && go vet ./internal/session/</automated>
  </verify>
  <acceptance_criteria>
- All four reconcile test functions pass under -race, including idempotency and the garbage-tail case.
- internal/session/reconcile.go contains InterruptedCause, the Seed type with exactly MaxTurns/PlanMode/PlanModePresent, and Reconcile performing no file or network I/O (grep the file for os. and http. returns nothing outside comments).
- internal/session/manager.go contains AppendSynthetic whose body routes through the same appendLine the other Append* methods use (no second write path, no O_TRUNC anywhere).
- go vet clean; the whole internal/session package suite stays green (no regression to the pre-existing tests).
  </acceptance_criteria>
  <done>The engine classifies all nine fixture classes correctly, idempotently, purely; closures append through the redaction-disciplined seam; GREEN (and any REFACTOR) commits present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript file → engine | reconciliation consumes on-disk lines an attacker with disk write access could have crafted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering | replay poisoning via crafted transcript (fake dangling pairs, colliding ids) | medium | mitigate | Reconcile is read-only classification; closures are append-only; pair matching by key (never adjacency) means crafted collisions resolve deterministically; readers already skip non-conforming lines |
| T-18-05 | Tampering | closure content injection (a crafted transcript tricking closures into carrying attacker text) | low | mitigate | Closure payloads are engine-authored constants (e.g. "interrupted by process death"), never copied from transcript content; AppendSynthetic crosses the redactor like every other append |
| T-18-SC | Tampering | package installs | low | accept | No new packages (stdlib only) |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/session/ -run TestReconcile` green.
- Full `mise ci` at wave merge (the engine must not disturb the standing gate).
</verification>

<success_criteria>
- Ten-row inventory fully covered by table tests (nine fixtures + the class-10 comment).
- Idempotency and clean-transcript cases green.
- Zero I/O in the classifier; append-only closure writes.
</success_criteria>

<output>
Create `.planning/phases/18-session-family/18-02-SUMMARY.md` when done (RED/GREEN/REFACTOR commits listed)
</output>

## Artifacts this phase-plan produces

- `internal/session/reconcile.go` — `InterruptedCause`, `Seed`, `Reconcile`
- `internal/session/manager.go` — `AppendSynthetic`
- `internal/session/reconcile_test.go` — TestReconcile/TestReconcileIdempotent/TestReconcileClean/TestReconcileSkipsGarbageTail
- `internal/session/testdata/reconcile/class01..class09` fixtures
