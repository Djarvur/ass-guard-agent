---
phase: 18-session-family
plan: 05
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - internal/runtime/runtime.go
  - internal/acp/replay.go
  - internal/acp/replay_test.go
  - internal/session/session.go
  - internal/session/reconcile.go
  - internal/acpserve/kill9_test.go
autonomous: true
requirements: [ACP-06]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "session/load on a transcript with dangling expectations performs the full D-03 ordering: reconcile (classify + APPEND provenance-marked closures to the transcript) happens INSIDE Runner.ResumeSession before the runner's session is usable; the acp side then reads the post-closure transcript and replays it (closures included, rendered as ordinary terminal frames — subtle in the UI, unambiguous on disk per D-02); state seeds (turn counter + plan mode from the last plan_mode line) land before any prompt is accepted"
    - "The LoadSessionResponse modes field carries the seeded plan-mode state (v1 SessionModeState shape) instead of null, and available_commands_update re-fires after replay so the resumed session's command set is re-advertised (ACP-06 'commands re-advertised')"
    - "Loading the same session id twice while it is already registered returns a typed already-active error; loading after a process died mid-load succeeds — the second ResumeSession finds the first load's closures already on disk, classifies clean, and completes (kill-during-replay idempotency row)"
    - "The kill -9 matrix passes end-to-end against a real serve process: for each inventory class the harness spawns serve with a stubbed provider pausing at that suspension point, SIGKILLs the child, resumes via session/load, and asserts (a) replay completes before prompt acceptance, (b) the synthetic closure carries the interrupted provenance on disk and in replay, (c) the next turn id continues the sequence, (d) no ghost state — no orphaned tool_call or parked ask is visible as live, and a follow-up prompt works"
    - "The ask-interplay row holds: a kill during a suspended ask (with queued asks) leaves a post-resume state where NO parked ask fires, and a late client response to the dead ask's request id resolves via the 16-D-19 synthetic-cancel path rather than wedging (17-D-13 spirit at session scale)"
    - "Zero provider/LLM invocations occur during replay and reconciliation — the resume path is transcript-local (asserted by the harness's counting stub provider)"
  artifacts:
    - path: "internal/acpserve/kill9_test.go"
      provides: "TestKill9Resume — the scripted process-kill harness: re-exec child mode via an env var, stub provider with a suspension-point knob, SIGKILL, restart, session/load, matrix assertions per class"
      contains: "TestKill9Resume"
    - path: "internal/runtime/runtime.go"
      provides: "Runner.ResumeSession extended: ReadAll -> session.Reconcile -> Manager.AppendSynthetic per closure -> SeedResume(maxTurns, planMode) -> return; the full transcript-side resume contract in one place (D-01)"
      contains: "ResumeSession"
    - path: "internal/acp/replay.go"
      provides: "the load-session core updated to run AFTER runner-side reconciliation (read post-closure transcript), emit available_commands_update post-replay, populate modes from the seeded plan mode, and reject double-registration with a typed error"
  key_links:
    - from: "internal/acp/replay.go (load core)"
      to: "internal/runtime/runtime.go"
      via: "the load core calls SessionLoader.ResumeSession FIRST (which appends closures + seeds), then reads the transcript for replay — ordering is the D-03 gate's foundation"
      pattern: "ResumeSession"
    - from: "internal/runtime/runtime.go"
      to: "internal/session/reconcile.go"
      via: "ResumeSession consumes Reconcile + AppendSynthetic + SeedResume (18-02's engine wired at the runner seam)"
      pattern: "Reconcile\\("
    - from: "internal/acpserve/kill9_test.go"
      to: "internal/acp/handlers.go"
      via: "the harness drives the real Server over stdio pipes (initialize/new/prompt/load) exactly as the 16-06 Zed-client simulator does — no test-only server shortcuts"
      pattern: "session/load"
---

<objective>
Wire the reconciliation engine into the live load path and prove the acceptance spine: a kill -9 mid-turn followed by resume leaves NO ghost state. Runner.ResumeSession becomes the full transcript-side resume (reconcile + closures + seeds); the acp load core replays the post-closure transcript, re-advertises commands, and carries the seeded mode; the kill -9 harness executes the ten-row matrix plus the ask-interplay and kill-during-replay rows against a real serve process.

Purpose: ROADMAP criterion 2 verbatim — "a kill -9 mid-turn followed by resume leaves NO ghost state — dangling tool_calls close as failed, parked asks and pending permissions resolve synthetically (kill -9 E2E test green)". This is the phase's hardest correctness surface (Research Flag: densest surface; the matrix design was enumerated during research).
Output: integrated load path, the kill -9 matrix harness, all green in the standing gate.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-session-family/18-CONTEXT.md
@.planning/phases/18-session-family/18-RESEARCH.md
@.planning/phases/18-session-family/18-01-PLAN.md
@.planning/phases/18-session-family/18-02-PLAN.md
@.planning/phases/17-permissions-elicitation/17-CONTEXT.md
@internal/runtime/runtime.go
@internal/session/reconcile.go
@internal/session/manager.go
@internal/acp/replay.go
@internal/acp/handlers.go
@internal/acpserve/acp_serve.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconciliation wired into the load path (resume contract completion)</name>
  <precondition>Phase 17 is executed — its landed plans/SUMMARYs state whether permission asks write ask_suspended transcript lines (class-10 shape). Read the 17 PLAN/SUMMARY set first; either way the class-3 machinery covers it: ask_suspended-without-resolution closes as cancelled-normal, and in-memory-only ask registries are empty after process death by construction — no parked ask can fire post-resume. Record which shape 17 shipped in the SUMMARY.</precondition>
  <files>internal/runtime/runtime.go, internal/acp/replay.go, internal/acp/replay_test.go, internal/session/session.go, internal/session/reconcile.go</files>
  <behavior>
    - TestResumeSessionReconcilesAndSeeds: a temp store with the class01 fixture transcript — Runner.ResumeSession returns no error, the transcript on disk now contains the appended failed tool_result with the interrupted cause, Reconcile over the re-read file yields zero closures, and the runner's next turn id continues the max suffix.
    - TestLoadFullOrdering: a dangling-class transcript driven through the Server load core — the client pipe receives the replay INCLUDING the closure frames (the failed tool result renders as a terminal tool_call_update), the response's modes field carries the seeded plan-mode state when the fixture ends in plan_mode enter (and null otherwise), an available_commands_update frame arrives after the last replay frame and before the response, and a prompt issued after load is accepted while a concurrent prompt during replay is still typed-rejected.
    - TestLoadDoubleRegistration: loading an already-registered live session id returns the typed already-active error; after a simulated mid-load interruption (closures appended, registration not reached — construct by calling ResumeSession directly without the handler), the handler-driven load completes successfully.
    - TestResumeNoProviderCalls: the load path over a full dangling fixture invokes the counting stub provider zero times.
  </behavior>
  <read_first>internal/runtime/runtime.go (ResumeSession as 18-01 built it, sessionFor :~860-905), internal/session/reconcile.go (Reconcile/Seed/InterruptedCause from 18-02), internal/session/manager.go (AppendSynthetic from 18-02, ReadAll :287), internal/session/planmode.go (TypePlanMode :67, enter/exit cause values, the state constructor), internal/acp/replay.go (the load core + mapping from 18-01), internal/acp/types.go (SessionModeState shape from Phase 16 — grep for the modes type the initialize path uses), internal/acp/handlers.go (available_commands_update emission — the Phase 16/20 frame builder; reuse the same command-registry source the session/new path advertises from), 17-CONTEXT.md (D-13 ask drain), 17 PLAN/SUMMARY set (the class-10 shape)</read_first>
  <action>
RED: extend replay_test.go with the four test functions above (the ResumeSession one may live in internal/runtime as resume_session_test.go if package boundaries demand — keep it beside the runtime tests; the rest stay in replay_test.go). Commit failing.
GREEN: extend Runner.ResumeSession to the full contract: ReadAll (degrade loudly on error), session.Reconcile(sess.SessionID, lines), for each closure Manager.AppendSynthetic, then SeedResume with the full seed (extend Session.SeedResume to take the plan-mode target — 18-01 created SeedResume(maxTurns); widen it to SeedResume(maxTurns int64, planMode string) applying the plan-mode state when non-empty, keeping the existing call sites compiling). Update the acp load core in replay.go: call ResumeSession (already first) — now guaranteed to have appended closures — then read the transcript (the post-closure file), replay it, emit available_commands_update from the same registry source the new-session path uses, build the response modes from the runner's seeded plan-mode state (add a getter if the state is unexported — e.g. Session.PlanModeState() returning the v1-shape data the handler can serialize), and guard double registration with a typed already-active RPCError checked under the sessions-map mutex. Also unify the turn-suffix scanners: 18-02 kept its max-turn scan local to reconcile.go (same-wave independence from 18-01's seed.go); now that both are landed, make Reconcile delegate that scan to seed.go's MaxTurnCounter so exactly one suffix-scanner implementation survives — internal/session/reconcile.go is in scope for this refactor and the 18-02 suite must stay green through it. Commit green with message feat(18-05): wire reconciliation into resume.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acp/ -run 'TestLoad|TestResume|TestSessionLoad' && go test -race -count=1 ./internal/runtime/ -run 'TestResumeSession'</automated>
  </verify>
  <acceptance_criteria>
- All four test functions pass under -race; the on-disk closure append is asserted by re-reading the transcript file.
- The full-ordering test observes replay frames (including closures), then available_commands_update, then the response — in that order on the client pipe.
- modes is non-null exactly when the fixture ends in a plan-mode line; the already-active error fires for double registration; the interrupted-then-completed load succeeds.
- The counting stub provider records zero calls across the whole load path.
- The widened SeedResume keeps every existing caller compiling (grep SeedResume call sites).
- Exactly one turn-suffix scanner implementation remains: reconcile.go's seed computation routes through seed.go's MaxTurnCounter (the 18-02 local scan is gone), and the 18-02 reconcile suite is green after the change.
  </acceptance_criteria>
  <done>The load path is the complete resume contract: reconcile-append-seed before replay, closures visible in replay, mode + commands re-advertised, double-load typed-rejected, interruption-idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: kill -9 matrix harness (real process, SIGKILL, resume, assert)</name>
  <precondition>16-06-PLAN.md is executed — the Zed-client simulator harness (spawn a real serve over stdio pipes with a stub provider, drive initialize/new/prompt) exists in internal/acpserve; reuse its spawn/drive helpers rather than rebuilding them</precondition>
  <files>internal/acpserve/kill9_test.go</files>
  <behavior>
    - TestKill9Resume is a table-driven harness: rows = inventory classes 1-5 and 8-9 (each row spawns serve, drives a prompt whose stub provider emits exactly that class's partial state — e.g. writes tool_call then blocks awaiting a result that never comes — SIGKILLs the child at the pause, restarts serve over the same workdir, drives session/load, and asserts the four matrix checks a-d) plus two extra rows: the ask-interplay row (kill during a suspended ask with one queued ask; post-resume no parked ask fires and a late response to the dead ask id resolves synthetic-cancel) and the kill-during-replay row (SIGKILL the RESUMING child mid-replay; a second resume completes — idempotent).
    - Classes 6/7 (state seeds) are asserted inside every row's check (c): the next turn id continues and, for the plan-mode row, the resumed mode is the persisted one.
    - The harness runs under the standard `go test` invocation (no env-flag gating); total runtime target <= 60s (SIGKILL of a child is portable on macOS+Linux per the platform scope).
    - The stub provider counts provider invocations; every row asserts the count is zero after resume until the harness deliberately prompts again.
  </behavior>
  <read_first>internal/acpserve/acp_serve.go (Run composition — where a test-driven serve construction lands; the 16-06 simulator test file for the spawn/drive/stub-provider helpers — read whatever 16-06 created in internal/acpserve), internal/acp/handlers.go (the wire sequences the harness drives), internal/session/reconcile.go (expected closures per class — the assertion source), .planning/phases/18-session-family/18-RESEARCH.md (Kill -9 test matrix paragraph — the design contract for this harness)</read_first>
  <action>
Build kill9_test.go on the re-exec pattern: the test spawns its own test binary (os.Args[0]) with an env var (e.g. ASS_GUARD_KILL9_CHILD=<row>) whose TestMain/early branch runs a child-mode serve against a temp workdir with the row's stub provider installed via the same injection seam the 16-06 simulator uses; the parent drives the child over stdio pipes, waits for the row's suspension signal (a recognizable frame or a file marker the stub writes), sends syscall.Kill(pid, syscall.SIGKILL), waits for the child to confirm dead (Process.Wait), spawns a fresh child (or drives an in-process server) for resume, and asserts a-d: (a) no prompt is accepted before the load response is readable, (b) the transcript on disk contains the row's expected closure lines with the interrupted cause and the replay contains their terminal frames, (c) the first post-resume prompt's turn id continues the sequence, (d) a follow-up prompt completes a full turn with no orphaned tool_call in the live window (assert via the projector window or the replayed frame set). Keep per-row runtime tight: the stub's pause is a channel the parent releases, never a sleep; the only deliberate wait is the SIGKILL round-trip. If 17's ask machinery is not drivable in the child mode, gate the ask-interplay row behind the presence of 17's ask surface (skip with a loud message naming what is missing — never a silent skip). Commit with message test(18-05): kill -9 resume matrix harness.
  </action>
  <verify>
    <automated>go test -race -count=1 -timeout 180s ./internal/acpserve/ -run TestKill9Resume && go test -race -count=1 ./internal/acpserve/</automated>
  </verify>
  <acceptance_criteria>
- TestKill9Resume passes all rows under -race within the timeout; each row performs a REAL SIGKILL of a REAL serve child (no simulated process death).
- Every row's assertions cover the four matrix checks; the ask-interplay and kill-during-replay rows are present and green (or loudly skipped only if 17's surface is absent, with the skip reason in the output).
- The counting stub shows zero provider calls between resume and the deliberate follow-up prompt in every row.
- The harness adds no production-code files; any seam it needs lands as test-file-local helpers or existing exported surfaces.
  </acceptance_criteria>
  <done>The kill -9 E2E matrix is green in the standing suite — the phase's acceptance spine (ROADMAP criterion 2) is machine-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dead process's transcript → live resume | the reconciliation input is whatever a killed process happened to append — attacker-craftable in principle |
| harness parent → child process | SIGKILL and pipe driving are test-local, but the child runs the real serve composition |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering | replay poisoning via crafted dangling state | medium | mitigate | Reconcile remains read-only + append-only; closures carry engine-authored constant payloads; the harness proves the closed-system behavior end-to-end |
| T-18-11 | Denial of Service | a load that never completes wedging the connection | medium | mitigate | The resume path is transcript-local with bounded work (single pass + bounded replay); the 16-D-03 stall detector covers the emitter side; the double-registration guard prevents load storms |
| T-18-12 | Elevation | harness child escaping its temp workdir | low | accept | Test-only surface; the child runs with the same privileges as the test binary; the store paths are temp-dir-confined by construction |
| T-18-SC | Tampering | package installs | low | accept | No new packages (stdlib + existing deps) |
</threat_model>

<verification>
- `go test -race -count=1 -timeout 180s ./internal/acpserve/ -run TestKill9Resume` green.
- `go test -race -count=1 ./internal/acp/ ./internal/runtime/ ./internal/session/` green.
- `mise ci` at wave merge (the matrix joins the standing gate).
</verification>

<success_criteria>
- Resume contract complete per D-01/D-02/D-03: reconcile-append-seed before replay; closures visible with provenance; modes + commands re-advertised.
- Kill -9 matrix green per row with all four checks; ask-interplay and kill-during-replay rows covered.
- Zero provider calls on the resume path.
</success_criteria>

<output>
Create `.planning/phases/18-session-family/18-05-SUMMARY.md` when done
</output>

## Artifacts this phase-plan produces

- `internal/acpserve/kill9_test.go` — `TestKill9Resume` matrix harness + child-mode env convention
- `internal/runtime/runtime.go` — full `Runner.ResumeSession` (reconcile + append + seed)
- `internal/session/session.go` (via 18-01's file set) — widened `SeedResume(maxTurns, planMode)`; plan-mode getter if added
- `internal/acp/replay.go` — completed load core (post-closure read, commands re-advertise, modes, double-registration guard)
