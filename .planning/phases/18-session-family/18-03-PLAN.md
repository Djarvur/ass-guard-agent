---
phase: 18-session-family
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/session/list.go
  - internal/session/list_test.go
autonomous: true
requirements: [ACP-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "session/list's backing scan reads ONLY the first line (createdAt from the session_start line's Timestamp) plus a bounded prefix (<= 64 KiB, stopping at the first user_message line) for the title, plus os.Stat for mtime/tombstone/checkpoints — never a full transcript read (D-04)"
    - "Ordering is a stable total order: lastActivity descending, sessionId ascending as the deterministic tie-break; the opaque cursor encodes (lastActivity unix-nano, sessionId) of the last emitted row so the next page returns sessions strictly after that tuple and never re-emits a row across pages (D-05 — covers the adjacency and ordering edge probes)"
    - "Sessions with identical lastActivity never merge, collide, or flip order across identical calls — two equal-mtime sessions appear exactly once each, ascending id, on every call (adjacency edge, resolved per D-05's composite cursor)"
    - "Empty and single-element inputs behave exactly: an empty store returns a nil/empty header slice with an empty nextCursor; a single-session store returns one row and an empty nextCursor; a nil/empty request cursor means first page (empty-input edge)"
    - "Tombstoned sessions (an <id>.deleted sibling exists) are filtered by os.Stat existence alone — zero transcript bytes read, zero marker bytes read (D-07 read side)"
    - "The header is lean (D-06): SessionID, Title (first user prompt truncated to 80 runes, fallback literal when no user_message exists in the bounded prefix), CreatedAt, LastActivity (from mtime), HasCheckpoints — no token/cost fields"
    - "Page size is bounded: limit <= 0 selects the default 50, limit > 100 clamps to 100; a malformed cursor string is rejected with a typed error before any scan (DoS guard)"
  artifacts:
    - path: "internal/session/list.go"
      provides: "type SessionHeader struct {SessionID, Title string; CreatedAt, LastActivity time.Time; HasCheckpoints bool}; func ListSessions(dir string, cursor string, limit int) ([]SessionHeader, string, error) with unexported encodeCursor/decodeCursor"
      contains: "func ListSessions("
    - path: "internal/session/list_test.go"
      provides: "TestSessionList covering ordering, tie-break, cursor pagination no-re-emission, tombstone filter, empty/single store, title truncation, malformed cursor, corrupt first line"
  key_links:
    - from: "internal/acp/handlers.go (18-04)"
      to: "internal/session/list.go"
      via: "the session/list handler calls session.ListSessions(workDir-store, request cursor, page size) and maps headers to v1 SessionInfo (updatedAt = LastActivity) — this plan delivers the engine, 18-04 the wire surface"
      pattern: "ListSessions\\("
    - from: "cmd/ass-guard (18-06)"
      to: "internal/session/list.go"
      via: "the --continue resolution and the picker both consume ListSessions' ordering (first row = most recent in the store)"
      pattern: "ListSessions\\("
---

<objective>
The session enumeration engine: an on-demand header scan over .ass-guard/ transcripts — mtime-ordered, first-line + bounded-prefix reads only, composite (lastActivity, id) cursor pagination, tombstone stat-filter, lean headers. No index file, nothing to drift (D-04).

Purpose: ACP-05's session/list and D-11's picker and D-10's --continue all enumerate sessions through this ONE engine; building it pure-filesystem with a stable total order makes the pagination contract table-testable before any RPC surface consumes it.
Output: internal/session/list.go with ListSessions + SessionHeader + opaque cursor codec, list_test.go green under -race.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-session-family/18-CONTEXT.md
@.planning/phases/18-session-family/18-RESEARCH.md
@.planning/phases/18-session-family/18-PATTERNS.md
@internal/coreexec/messaging.go
@internal/session/transcript.go
@internal/session/manager.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — list engine contract tests</name>
  <files>internal/session/list_test.go</files>
  <behavior>
    - TestSessionListOrderingAndTiebreak: five sessions in a temp store with Chtimes-forced mtimes, two of them identical — output is lastActivity descending with the two equal ones ascending by id; identical call twice yields the identical slice.
    - TestSessionListCursorPagination: seven sessions, page size 3 — three pages return all seven exactly once, each page's rows are strictly after the previous cursor tuple, final nextCursor is empty; re-fetching with a stale cursor that points at a since-deleted session still returns a consistent page (best-effort shift documented in D-05).
    - TestSessionListTombstoneFilter: a session with a zero-byte <id>.deleted sibling never appears; its transcript bytes are never opened (assert via a chmod-000 transcript that would fail if read).
    - TestSessionListEmptyAndSingle: empty dir → empty result, empty nextCursor; one session → one row, empty nextCursor; empty-string cursor means first page.
    - TestSessionListHeaderShape: title is the first user_message text truncated to 80 runes (fixture with a 200-rune prompt); a transcript whose bounded prefix has no user_message gets the fallback literal; createdAt equals the session_start line's Timestamp; HasCheckpoints is true only when the session's checkpoint storage is non-empty.
    - TestSessionListMalformedCursor: a cursor that is not valid base64url of the tuple shape returns a typed error and performs no directory scan of transcripts (the ReadDir may happen, but no per-file reads).
    - TestSessionListCorruptFirstLine: a transcript whose first line is garbage is skipped without error (its id never appears).
    - TestSessionListBounds: limit 0 → 50-row default honored (fixture of 60 rows returns 50 + nextCursor); limit 500 → clamped to 100.
  </behavior>
  <read_first>internal/coreexec/messaging.go (Sessions :241-257 enumeration pattern, sessIDPattern :185-186, bounded reader :271-278 with readerBufInit/readerBufMax), internal/session/transcript.go (Line envelope for the header fields — Timestamp, Content/Text on user_message; filePermOwner/dirPerm), internal/session/manager.go (NewManager :39 — how transcripts are named/opened, AppendUserMessage content shape), internal/checkpoint (the store layout — Open(dir), SnapshotTurn(ctx, sessionID, turnID) keying — to derive the HasCheckpoints probe), .planning/phases/18-session-family/18-RESEARCH.md (Pattern 2 header scan, Pattern 3 composite cursor, Pitfall 5)</read_first>
  <action>
RED: write list_test.go with the eight test functions above. Build stores in t.TempDir() with hand-written transcript_<uuid>.jsonl files (session_start first line carrying a Timestamp, then a user_message line with a long Text, then filler); force mtimes with os.Chtimes for determinism. For HasCheckpoints, create checkpoint objects through the real checkpoint store API discovered in read_first (or, if the store layout makes session-scoped probing impractical without writes, probe by the on-disk layout convention the store uses and document it in the test). Reference SessionHeader/ListSessions/typed cursor error which do not exist yet. Confirm red (compile failure). Commit with message test(18-03): add failing session list engine tests.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run TestSessionList 2>&1 | tail -3; test ${PIPESTATUS[0]} -ne 0 && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
- list_test.go declares all eight TestSessionList* functions with the behaviors above.
- The run fails to compile or fails red; the RED commit is in git log.
- No test performs a full-transcript read of another test's store (fixtures stay minimal).
  </acceptance_criteria>
  <done>The engine's full contract — ordering, pagination, filtering, header shape, bounds — is encoded as failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — ListSessions engine</name>
  <files>internal/session/list.go</files>
  <behavior>
    - ListSessions implements the eight contracts; the scan is ReadDir(.ass-guard) + per-entry Stat + first-line/bounded-prefix reads only.
    - The cursor is opaque: base64 RawURLEncoding of "unixNano|sessionId", decoded and validated before use, rejected with a typed error on malformed input.
    - Sessions whose transcript name does not parse to a sessIDPattern-valid id are skipped.
  </behavior>
  <read_first>internal/session/list_test.go (the contract), internal/coreexec/messaging.go (the patterns to mirror), internal/session/transcript.go</read_first>
  <action>
GREEN: create internal/session/list.go: package-local copy of sessIDPattern with its provenance comment (per PATTERNS — the project convention is per-package copies, T-12-04-03); SessionHeader exactly as the must-have truth lists; ListSessions(dir, cursor, limit) — resolve store path filepath.Join(dir, ".ass-guard"), os.ReadDir, for each transcript_*.jsonl entry derive the id, stat the <id>.deleted sibling (skip if present), stat the transcript for mtime + regular-file check, open O_RDONLY and read ONLY the first line for createdAt + the bounded prefix (a 64 KiB-capped bufio reader stopping at the first user_message line) for the title, probe checkpoints via the layout discovered in Task 1. Sort with sort.Slice by (LastActivity desc, SessionID asc). Apply cursor: decode, keep rows strictly after the tuple (lastActivity < cursorTime, or equal time and id > cursorID — careful with the descending/ascending mixed order; encode the comparison once and test it via the pagination test). Apply limit (default 50, clamp 100). nextCursor = encode(last row) when more rows remain, else empty. Title extraction: unmarshal the user_message line's Content blocks, take the first text block's Text, truncate to 80 runes (unicode-safe via []rune), fallback literal "(no prompt)". Commit with message feat(18-03): implement session list engine. REFACTOR only if duplication with reconcile/seed scanning emerges — reuse, do not duplicate, the turn-suffix parsing.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ && go vet ./internal/session/</automated>
  </verify>
  <acceptance_criteria>
- All eight TestSessionList* functions pass under -race; the package suite and go vet stay green.
- internal/session/list.go contains func ListSessions( and the SessionHeader struct with exactly the five lean fields (SessionID, Title, CreatedAt, LastActivity, HasCheckpoints).
- The only file reads in the scan path are the first line + a bounded prefix capped at 64 KiB (grep the file for bufio confirms the reader; no ReadAll/ReadFile on transcripts).
- Cursor codec is unexported; a malformed cursor produces the typed error.
  </acceptance_criteria>
  <done>The engine enumerates sessions with a stable total order, opaque composite cursor, stat-only tombstone filter, and lean headers — green and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .ass-guard/ directory → engine | list enumerates files an attacker with workspace write access could plant |
| caller → cursor string | the (future) RPC surface passes an untrusted opaque cursor in |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-06 | DoS | unbounded paging or huge cursor strings | medium | mitigate | limit clamped to 100; cursor length-capped at decode (reject > 256 bytes) before any parsing; malformed input is a typed reject, never a loop |
| T-18-07 | Tampering | symlinked/planted transcript entries | medium | mitigate | entry must be a regular file (Stat mode check) with a sessIDPattern-valid id; non-conforming first lines are skipped (existing reader discipline) |
| T-18-08 | Info disclosure | title text leaking beyond the first prompt | low | mitigate | title derives only from the first user_message inside the 64 KiB prefix, truncated to 80 runes — assistant output, tool results, and later prompts are never header material |
| T-18-SC | Tampering | package installs | low | accept | No new packages (stdlib only) |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/session/ -run TestSessionList` green.
- Full `mise ci` at wave merge.
</verification>

<success_criteria>
- Stable total order with composite cursor; no row re-emission across pages; best-effort shift documented in the file header comment (D-05).
- Zero full-transcript reads; stat-only tombstone filter; lean header shape.
- Bounds and malformed-cursor rejection green.
</success_criteria>

<output>
Create `.planning/phases/18-session-family/18-03-SUMMARY.md` when done (RED/GREEN/REFACTOR commits listed)
</output>

## Artifacts this phase-plan produces

- `internal/session/list.go` — `SessionHeader`, `ListSessions`, unexported cursor codec, package-local `sessIDPattern` copy
- `internal/session/list_test.go` — the eight TestSessionList* contract tests
