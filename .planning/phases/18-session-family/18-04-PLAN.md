---
phase: 18-session-family
plan: 04
type: execute
wave: 2
depends_on: ["18-01", "18-03"]
files_modified:
  - internal/acp/handlers.go
  - internal/acp/types.go
  - internal/acp/session_family_test.go
  - internal/session/tombstone.go
  - internal/session/tombstone_test.go
  - internal/acpserve/acp_serve.go
autonomous: true
requirements: [ACP-05, ACP-07]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "session/list returns v1 ListSessionsResponse: sessions as a non-null JSON array of SessionInfo{sessionId (required), cwd (required, the server workDir), title, updatedAt (RFC3339 of LastActivity)}, nextCursor null when exhausted; an empty store serializes as an empty array, not null; the handler is reachable only as advertised (sessionCapabilities.list)"
    - "session/close cancels in-flight turns via the existing cancel contract, waits for the turn goroutine to drain (bounded 30s, loud structured log on force), then closes and removes the session; a second close of an already-closed session returns success — D-12 idempotency"
    - "session/delete runs the close sequence if the session is open, writes a zero-byte <id>.deleted marker beside the transcript (0600), removes the session's checkpoint objects, and leaves the audit trail untouched (D-07/D-08); delete is best-effort — partial failures return a typed error with detail (spec-unstable surface)"
    - "The GC sweep physically removes ONLY tombstoned transcript+marker pairs whose marker mtime is older than the configured grace (default 30 days), logs every removal loudly, never touches non-tombstoned files, never touches anything under the audit path, and is safe to run twice (the second run finds nothing)"
    - "Running the close/delete/sweep operations twice on the same input is uniformly idempotent: double close = success, double delete = success (marker write overwrites a zero-byte file with a zero-byte file), double sweep = no-op (ACP-07 idempotency edge)"
    - "Under interruption or concurrency: a close racing an in-flight turn still returns only after cancel-and-drain resolves; a delete racing a list is bounded by the stat filter (the session disappears from the next list call); a sweep interrupted mid-purge is completed by the next sweep and never removes a non-tombstoned file (ACP-07 concurrency edge)"
    - "initialize advertises sessionCapabilities {list:{}, close:{}, delete:{}} alongside the top-level loadSession flag; the v1 no-replay session/resume method is NOT advertised (18-RESEARCH A3 — reconcile-then-accept makes full load the only safe resume path)"
    - "The tombstone grace is configurable: the tombstoneGraceDays key (default 30) joins the Phase-16 configOptions menu with a real handler (apply-as-landed), invalid values typed-rejected per 16-D-09"
  artifacts:
    - path: "internal/session/tombstone.go"
      provides: "func Tombstone(dir, sessionID string) error (zero-byte marker write, 0600); func SweepTombstones(dir string, grace time.Duration, now time.Time) (removed []string, err error); const DefaultTombstoneGrace = 30 * 24 * time.Hour"
      contains: "SweepTombstones"
    - path: "internal/acp/handlers.go"
      provides: "handleSessionList (params parse + ListSessions + SessionInfo mapping), handleSessionClose (cancel-and-drain, idempotent), handleSessionDelete (close-if-open + tombstone + checkpoint removal, best-effort); sessionCapabilities advertisement in handleInitialize"
      contains: "handleSessionClose"
    - path: "internal/acp/types.go"
      provides: "ListSessionsRequest/Response, SessionInfo, CloseSessionRequest/Response, DeleteSessionRequest/Response — v1 shapes with tagliatelle markers"
  key_links:
    - from: "internal/acp/handlers.go"
      to: "internal/session/list.go"
      via: "handleSessionList calls session.ListSessions with the store under the server workDir and maps headers to SessionInfo (updatedAt = LastActivity)"
      pattern: "ListSessions\\("
    - from: "internal/acp/handlers.go"
      to: "internal/session/tombstone.go"
      via: "handleSessionDelete calls Tombstone after the close sequence; the checkpoint removal goes through the checkpoint store's session-scoped removal surface (add DeleteSession(sessionID) if the store lacks one)"
      pattern: "Tombstone\\(|SweepTombstones\\("
    - from: "internal/acpserve/acp_serve.go"
      to: "internal/session/tombstone.go"
      via: "acpserve.Run invokes SweepTombstones once at startup after WorkDir resolution, before Serve (the chosen sweep trigger point within the 30d contract)"
      pattern: "SweepTombstones"
  prohibitions:
    - requirement_id: ACP-05
      category: privacy
      status: resolved
      verification: judgment
      statement: "session/list MUST NOT return sessions whose transcripts live outside the requested working directory's .ass-guard/ store — the enumeration scope is the project; cross-project enumeration would leak other projects' session existence and titles"
    - requirement_id: ACP-05
      category: privacy
      status: resolved
      verification: judgment
      statement: "Session titles MUST derive only from the session's first user prompt (truncated) — the list header must never carry assistant output, tool results, file contents, or later-turn prompts"
    - requirement_id: ACP-07
      category: values
      status: resolved
      verification: judgment
      statement: "Delete and the GC sweep MUST NEVER remove or rewrite audit records — the audit trail survives unconditionally (D-08/D-20); no code path may couple audit retention to session deletion or grace expiry"
    - requirement_id: ACP-07
      category: safety
      status: resolved
      verification: judgment
      statement: "The purge MUST NOT physically destroy a tombstoned session before the configured grace period has elapsed — the grace window is the product's only undo for a mistaken delete (D-09 one-way door)"
    - requirement_id: ACP-07
      category: safety
      status: resolved
      verification: judgment
      statement: "The sweep's file-removal surface MUST be bounded to tombstoned transcript/marker pairs inside .ass-guard/ — it must never remove a live (non-tombstoned) transcript, a checkpoint of a non-tombstoned session, or any file outside the store directory"
---

<objective>
The remaining three RPC surfaces of the session family plus the tombstone lifecycle: session/list over the 18-03 engine, session/close as cancel-and-drain with idempotency, session/delete as tombstone-plus-artifact-sweep with the audit invariant, and the grace-expired GC sweep wired at serve startup with its configOptions key.

Purpose: ROADMAP criterion 4 (close stops work cleanly; delete tombstones so audit history survives and deleted sessions vanish from the list but stay on disk) and ACP-05's editor-side enumeration. The tombstone is the D-20 audit invariant's enforcement point — never rm.
Output: internal/session/tombstone.go (+test), three new handlers with round-trip tests, capability advertisement, startup sweep.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-session-family/18-CONTEXT.md
@.planning/phases/18-session-family/18-RESEARCH.md
@.planning/phases/18-session-family/18-PATTERNS.md
@.planning/phases/18-session-family/18-01-PLAN.md
@internal/acp/handlers.go
@internal/acp/server.go
@internal/acp/types.go
@internal/session/list.go
@internal/session/transcript.go
@internal/checkpoint
@internal/acpserve/acp_serve.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: session/list handler + sessionCapabilities advertisement</name>
  <files>internal/acp/handlers.go, internal/acp/types.go, internal/acp/session_family_test.go</files>
  <behavior>
    - TestHandleSessionListRoundTrip: a store with three sessions (one tombstoned) — session/list with a null cursor returns two SessionInfo rows in engine order with updatedAt RFC3339 and the workDir as cwd; the sessions array serializes as [] (never null) on an empty store with nextCursor null; a cursor from page one drives page two without re-emission.
    - initialize response advertises the nested sessionCapabilities object containing exactly list, close, and delete entries (empty objects), alongside the existing top-level loadSession; the string session/resume appears nowhere in the handler map or the advertisement (A3).
  </behavior>
  <read_first>internal/acp/handlers.go (registerHandlers :23-31, handleSessionNew :66-83 params-parse shape, capability advertisement :43-51 — where sessionCapabilities nests), internal/acp/types.go (existing result structs + tagliatelle marker convention), internal/session/list.go (SessionHeader/ListSessions from 18-03), internal/acp/session_family_test.go (18-01's fixture + pipe-driving helpers — extend, do not duplicate)</read_first>
  <action>
RED: add the round-trip test + an initialize-advertisement assertion to session_family_test.go. Commit failing.
GREEN: types.go gains ListSessionsRequest{_meta, Cursor *string, Cwd *string}, ListSessionsResponse{_meta, NextCursor *string, Sessions []SessionInfo (required)}, SessionInfo{Meta json.RawMessage, AdditionalDirectories []string, Cwd string (required), SessionID string (required), Title *string, UpdatedAt *string}, CloseSessionRequest{_meta, SessionID}, CloseSessionResponse{_meta}, DeleteSessionRequest{_meta, SessionID}, DeleteSessionResponse{_meta} — v1 field sets exactly as 18-RESEARCH pins them, tagliatelle markers on wire-cased fields. handlers.go: register session/list + session/close + session/delete in registerHandlers; handleSessionList parses the request, calls session.ListSessions(s.storeDir(), cursorValue, 0), maps each header to SessionInfo (Title only when non-fallback, UpdatedAt = LastActivity.Format(time.RFC3339), Cwd = the server workDir), guarantees the slice is non-nil so JSON emits [] not null, returns nextCursor as *string (nil when empty). Extend the initialize capability map with sessionCapabilities: {list:{}, close:{}, delete:{}} and update its doc comment (load stays top-level per Pitfall 8; no resume entry per A3). Commit green.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acp/ -run 'TestHandleSessionList|TestSessionLoad|TestInitialize'</automated>
  </verify>
  <acceptance_criteria>
- The round-trip test passes under -race with tombstone filtering visible through the RPC and pagination stable across pages.
- An empty store produces the JSON array literal with square brackets (test asserts the raw response bytes contain the empty-array token) and a null nextCursor.
- The advertisement contains sessionCapabilities with exactly the three entries; grep of internal/acp for a session/resume registration returns nothing.
- All response structs live in types.go with the tagliatelle marker comments on wire-cased fields.
  </acceptance_criteria>
  <done>The list RPC surfaces over the engine with v1 shapes, non-null arrays, and the correct capability split.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: session/close cancel-and-drain + session/delete tombstone</name>
  <files>internal/acp/handlers.go, internal/acp/server.go, internal/acp/session_family_test.go</files>
  <behavior>
    - TestSessionCloseCancelsAndDrains: a session with an in-flight turn (stub runner blocking in Run until ctx cancel) — session/close returns only after the stub's Run has returned; the session is gone from the map; a second session/close with the same id returns an empty-object success result (D-12).
    - TestSessionCloseAlreadyClosed: close of an unknown id returns success, not an error.
    - TestSessionDeleteTombstones: delete of an open session cancels the turn, writes the zero-byte marker, removes the session's checkpoint objects (assert via the checkpoint store surface), leaves the transcript file present with unchanged bytes, and leaves the audit mirror files present; a subsequent session/list omits the session; a second session/delete with the same id returns success.
    - Delete of a tombstoned-but-not-swept id re-writes the marker and returns success (idempotent).
  </behavior>
  <read_first>internal/acp/handlers.go (handleSessionCancel :153-182 — cancelTurn + closeSessionIfPossible precedent; handleLogout :195-213 — map-delete precedent; the SessionCloser optional interface at server.go:35-41), internal/acp/server.go (sessionState :71-81 — where the turn-done tracking lands), internal/session/transcript.go (openTranscript :123-150 — file-perm and marker-file discipline for the tombstone writer), internal/checkpoint (the store's session-keying — SnapshotTurn(ctx, sessionID, turnID) — and whether a session-scoped removal exists; add DeleteSession(sessionID) error to the store if absent), internal/acp/session_family_test.go (18-01 fixtures + the stub-runner conventions from internal/acp/server_test.go)</read_first>
  <action>
RED: add the three test functions to session_family_test.go with a blocking stub TurnRunner (server_test.go has the stub pattern; extend it to block on ctx.Done and signal completion via a channel). Commit failing.
GREEN: server.go — sessionState gains a turnDone channel (or WaitGroup) set when a prompt's Run begins and closed when it returns, so close can wait; keep it nil-safe for sessions with no turn yet. handlers.go — handleSessionClose: look up the session (absent => return the empty success result immediately — the D-12 idempotent branch); cancelTurn(); wait on turnDone with a 30s timeout, on timeout log one structured line naming the session and force-close anyway (loud, never silent); closeSessionIfPossible (the SessionCloser drain path); delete from the map; return the empty-object result. handleSessionDelete: run the same close sequence when open; validate the id against the package-local sessIDPattern; call session.Tombstone(workDir, id) (built in Task 3 — to keep this task compiling, implement session.Tombstone in Task 3 and have this task call it; if Task ordering within execution makes that awkward, implement the one-line marker write inline here and let Task 3 own Sweep only — the executor picks one home for Tombstone, not both); remove checkpoint objects via the store's session-scoped removal (adding store.DeleteSession(sessionID) if the API is absent, following the store's existing method style); collect best-effort failures into a typed RPCError carrying per-step detail. Commit green.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acp/ -run 'TestSessionClose|TestSessionDelete|TestHandleSessionList'</automated>
  </verify>
  <acceptance_criteria>
- All close/delete tests pass under -race; the drain wait is observable (close returns strictly after the stub Run returns — asserted via channel ordering).
- The double-close and double-delete branches both return success results, not errors.
- The deleted session's transcript bytes are unchanged and its audit artifacts still exist after delete (test asserts file presence).
- session/list omits the tombstoned session (cross-checked in the round-trip test's store).
  </acceptance_criteria>
  <done>Close drains cleanly and idempotently; delete tombstones and sweeps checkpoints while the transcript and audit survive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tombstone lifecycle — marker, GC sweep, grace config, startup wiring</name>
  <reversibility rating="one-way">D-09 auto-purge: the grace-expired sweep destroys transcripts permanently — the operator chose this exact door (auto-purge, 30d, audit-exempt) in CONTEXT D-09, so this records the rating without a new checkpoint.</reversibility>
  <files>internal/session/tombstone.go, internal/session/tombstone_test.go, internal/acpserve/acp_serve.go, internal/acp/handlers.go</files>
  <behavior>
    - TestTombstoneWrite: Tombstone creates a zero-byte <id>.deleted beside the transcript with 0600 perms; writing twice succeeds with the file still zero bytes.
    - TestSweepRespectsGrace: two tombstoned sessions, marker mtimes forced via Chtimes to 10d and 40d old, grace 30d — sweep removes ONLY the 40d pair (transcript + marker gone, 10d pair intact), returns the removed id, and logs a structured line per removal.
    - TestSweepNeverTouches: a live (non-tombstoned) transcript, an audit-path file, and an unrelated file all survive a sweep regardless of age; an orphan marker with no transcript is removed by the sweep (its pair is already gone).
    - TestSweepIdempotent: running the sweep twice removes nothing the second time and returns an empty slice.
    - TestTombstoneGraceConfig: setting tombstoneGraceDays through the Phase-16 set_config_option surface persists and applies on the next sweep; a non-positive or non-numeric value is typed-rejected with the violation detail (16-D-09).
  </behavior>
  <read_first>internal/session/transcript.go (filePermOwner :11, dirPerm :12, the .gitignore marker-file precedent :131-135), internal/session/manager.go (session.go:721-738-style slog loud-degradation — find the actual appendToolResultLoud precedent in session.go), internal/acp/handlers.go (the Phase-16 set_config_option handler and option-registry shape — grep internal/acp for the config-option registry 16-05 created; register the grace key following its exact pattern), internal/acpserve/acp_serve.go (Run's statement order — where WorkDir is resolved relative to NewServer/Serve)</read_first>
  <action>
RED: write tombstone_test.go with the five test functions; build stores in temp dirs with Chtimes-aged markers. If Tombstone/SweepTombstones do not exist, red is the compile failure. Commit failing.
GREEN: create internal/session/tombstone.go: Tombstone(dir, sessionID string) error validates the id against the package-local pattern, writes the zero-byte marker with filePermOwner beside the transcript path (filepath.Join(dir, ".ass-guard", sessionID+".deleted")), never touching the transcript; DefaultTombstoneGrace = 30*24h; SweepTombstones(dir string, grace time.Duration, now time.Time) ([]string, error) enumerates markers (suffix .deleted, id validates), stats the marker mtime, and for pairs past grace removes the transcript then the marker via os.Remove — the ONLY os.Remove surface in the phase, structurally confined to tombstoned pairs inside .ass-guard/ (path built by the same Join; audit lives under a different subtree and is never enumerated); logs slog.Info per removal with id, age, path; skips and reports non-removable files as errors in the returned error, never panicking. Wire acpserve.Run: after WorkDir resolution and before Serve, call SweepTombstones once with the configured grace, logging the removed count (degrade loudly on error, never refusing to serve). Register the tombstoneGraceDays config key in the Phase-16 option registry with default 30, integer validation (must be >= 1), and an apply handler that stores the value where acpserve's sweep reads it (follow the registry's existing apply-as-landed persistence pattern). Commit green with message feat(18-04): tombstone lifecycle and grace GC.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/session/ -run TestTombstone && go test -race -count=1 ./internal/session/ -run TestSweep && go vet ./internal/session/ ./internal/acp/</automated>
  </verify>
  <acceptance_criteria>
- All tombstone/sweep tests pass under -race; grace boundary exactness is asserted (30d boundary case: exactly-at-grace does NOT purge, past-grace does — pick and assert one convention).
- The audit-path fixture and live transcript survive every sweep invocation; the double sweep is a no-op.
- internal/session/tombstone.go contains os.Remove only inside the grace-expired branch operating on tombstoned pair paths (every os.Remove call site's arguments are derived from a validated marker id).
- The config key round-trips through the Phase-16 option surface (test drives set then apply) and rejects invalid input with a typed error.
- acpserve.Run contains exactly one SweepTombstones call site, before Serve.
  </acceptance_criteria>
  <done>The tombstone lifecycle is complete: reversible marker, grace-bounded purge, audit exemption, loud logging, config surface, startup wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→agent stdio RPC | sessionIds for close/delete, cursor strings for list, config values for the grace key — all untrusted |
| agent→filesystem store | tombstone writes, checkpoint removal, and the sweep's os.Remove cross into .ass-guard/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-02 | Tampering | destructive delete weaponized via session/delete | high | mitigate | delete is tombstone-only; the sweep's os.Remove surface is structurally confined to validated tombstoned pairs under .ass-guard/ past grace; audit subtree never enumerated |
| T-18-09 | Tampering | sessionId path construction in close/delete/tombstone | high | mitigate | package-local sessIDPattern applied before any path Join in every handler and in Tombstone/SweepTombstones |
| T-18-03 | Tampering | symlink-planted transcripts or markers misleading the sweep | medium | mitigate | marker and transcript must be regular files (Stat mode check) before removal; a symlinked path is skipped with a loud log |
| T-18-10 | DoS | grace key driven to extreme values | low | mitigate | integer >= 1 validation with typed reject; no upper clamp needed (large grace only delays purge) |
| T-18-SC | Tampering | package installs | low | accept | No new packages (stdlib only) |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/acp/ ./internal/session/` green.
- `mise ci` at wave merge.
- Manual (phase verification): in Zed, open the session picker (list), close a session mid-turn, delete one and confirm it vanishes from the list while the transcript + audit remain on disk.
</verification>

<success_criteria>
- List/close/delete round-trips green with v1 shapes and the capability split.
- D-12 close semantics proven (drain wait + idempotency).
- D-07/D-08/D-09 tombstone contract proven (never rm; audit exempt; grace-bounded purge; config surface).
</success_criteria>

<output>
Create `.planning/phases/18-session-family/18-04-SUMMARY.md` when done
</output>

## Artifacts this phase-plan produces

- `internal/session/tombstone.go` — `Tombstone`, `SweepTombstones`, `DefaultTombstoneGrace`
- `internal/acp/handlers.go` — `handleSessionList`, `handleSessionClose`, `handleSessionDelete`, sessionCapabilities advertisement, grace config key registration
- `internal/acp/types.go` — `ListSessionsRequest/Response`, `SessionInfo`, `CloseSessionRequest/Response`, `DeleteSessionRequest/Response`
- `internal/acp/server.go` — `sessionState` turn-done tracking (shared with close)
- `internal/checkpoint` — `DeleteSession(sessionID)` on the store (only if absent today)
- New tests: `internal/session/tombstone_test.go`, extended `internal/acp/session_family_test.go`
