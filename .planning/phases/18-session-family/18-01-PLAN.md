---
phase: 18-session-family
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/acp/handlers.go
  - internal/acp/types.go
  - internal/acp/server.go
  - internal/acp/replay.go
  - internal/acp/replay_test.go
  - internal/acp/session_family_test.go
  - internal/acpserve/acp_serve.go
  - internal/runtime/runtime.go
  - internal/session/session.go
  - internal/session/seed.go
autonomous: true
requirements: [ACP-06]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "session/load on a cleanly-closed past session replays the conversation as session/update notifications through the same ordered emitter path live turns use, and the load response is written only after the last replayed frame (updates-before-response, mirroring the 16-01 turn-end barrier contract)"
    - "initialize advertises loadSession: true and the LoadSessionResponse carries exactly the v1 fields _meta/configOptions/modes — no sessionId field (18-RESEARCH Pitfall 7); the request's sessionId persists as THE session id"
    - "A resumed session's next turn id continues from the transcript maxima: the next nextTurnID() after load is one past the largest <sessionID>-turn-%03d suffix on disk, never a restart at 001 (ACP-06 'continued id sequences from transcript maxima', D-01)"
    - "session/prompt arriving before replay completes is rejected with a typed RPC error naming the replay state — it never interleaves with replayed frames (D-03 reconcile-then-accept)"
    - "Loading a tombstoned session (an <id>.deleted marker exists) or a session with no transcript returns a typed error; load never creates a fresh session under a client-supplied id"
    - "Replay emits frames only for agent-visible activity kinds (agent_message_chunk/assistant_message, tool_call, tool_result-as-tool_call_update); state/bookkeeping lines (session_start/end, boundary, usage, request_shaped, command_provenance, user_message, plan_mode) seed agent-side state and title derivation but emit no client frame — the v1 session/update vocabulary has no user-message slot, and the client owns rendering of its own prompts"
  artifacts:
    - path: "internal/acp/replay.go"
      provides: "replayLine tolerant reader (bufio + skip-non-conforming, messaging.go pattern) + ReplayTranscript mapping transcript lines to session/update frames through the session's emitter handle, with the mapping table documented in the file header"
      contains: "ReplayTranscript"
    - path: "internal/acp/handlers.go"
      provides: "real handleSessionLoad (params parse + sessIDPattern validate + tombstone stat + tombstone-refusing, delegating to an exported Server.LoadSession core reusable by the CLI path) replacing the -32601 stub; SessionLoader optional capability interface; sessionState ready-gate consumed by handleSessionPrompt"
      contains: "LoadSession"
    - path: "internal/runtime/runtime.go"
      provides: "Runner.ResumeSession(ctx, sessionID) — sessionFor under the transcript's id + turnCounter seeding from transcript maxima"
      contains: "ResumeSession"
    - path: "internal/session/seed.go"
      provides: "MaxTurnCounter(sessionID string, lines []Line) int64 — max %03d turn-suffix scan"
      contains: "MaxTurnCounter"
    - path: "internal/session/session.go"
      provides: "Session.SeedResume(maxTurns int64) — exported seeding of the unexported turnCounter"
      contains: "SeedResume"
  key_links:
    - from: "internal/acp/handlers.go"
      to: "internal/runtime/runtime.go"
      via: "type-assert s.turnRunner to the SessionLoader optional interface (mirrors SessionCloser at server.go:35) and call ResumeSession before replay"
      pattern: "SessionLoader"
    - from: "internal/acpserve/acp_serve.go"
      to: "internal/acp/server.go"
      via: "acpserve.Run passes the resolved WorkDir through a new WithWorkDir ServerOption so the load handler can stat tombstones and read transcripts"
      pattern: "WithWorkDir"
    - from: "internal/acp/replay.go"
      to: "internal/acp/server.go"
      via: "ReplayTranscript writes frames through the per-session emitter handle s.Emitter(sessionID) — the 16-01 ordered TurnEmitter surface, foreground lane"
      pattern: "Emitter\\("
  prohibitions:
    - requirement_id: ACP-06
      category: transparency
      status: resolved
      verification: judgment
      statement: "Replay MUST NOT fabricate activity — every replayed session/update frame must correspond to an existing transcript line (the 16-01 emitter no-fabrication prohibition extended to the replay path: no synthetic progress cards, no cached stale frames presented as history; synthetic closure frames come only from reconciliation lines that exist on disk)"
    - requirement_id: ACP-06
      category: values
      status: resolved
      verification: judgment
      statement: "Resume MUST NOT invoke any provider/LLM call during replay or reconciliation — reconstruction is transcript-local; a silent model call on resume would spend tokens without any user action"
---

<objective>
Tracer slice for the phase: ONE load path end-to-end — the client sends session/load with a past session's id, the server validates it, the runner adopts the transcript's session id with a continued turn counter, the full conversation replays as ordered session/update frames through the same emitter live turns use, the response returns the v1 shape, and only then does the session accept prompts. Expansion (18-05) adds reconciliation for dangling expectations on this proven spine.

Purpose: ROADMAP criterion 1 ("the full conversation replays visibly through the same ordered frames as live turns") and criterion 2's id-continuation both stand on this spine; proving the load architecture on the best early context catches a replay-ordering dead-end after one commit instead of after the whole session family lands.
Output: internal/acp/replay.go (+test), real handleSessionLoad with the ready-gate, Runner.ResumeSession, session seeding helpers, capability flip.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-session-family/18-CONTEXT.md
@.planning/phases/18-session-family/18-RESEARCH.md
@.planning/phases/18-session-family/18-PATTERNS.md
@internal/acp/handlers.go
@internal/acp/server.go
@internal/acp/types.go
@internal/session/transcript.go
@internal/session/manager.go
@internal/session/session.go
@internal/coreexec/messaging.go
@internal/runtime/runtime.go
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: session/load clean-path end-to-end (tracer)</name>
  <precondition>Phase 16 plans are executed — internal/acp/emitter.go (TurnEmitter) exists and Server.Emitter(sessionID) returns the ordered foreground-lane handle (16-01 artifacts); handleSessionLoad is still the -32601 stub at handlers.go:184-192</precondition>
  <reversibility rating="costly">Flipping loadSession:true publishes the load contract to real clients; reverting later re-breaks every client that started depending on it — but it is a code flip, not a data migration.</reversibility>
  <files>internal/acp/handlers.go, internal/acp/types.go, internal/acp/server.go, internal/acp/replay.go, internal/acp/session_family_test.go, internal/acpserve/acp_serve.go, internal/runtime/runtime.go, internal/session/session.go, internal/session/seed.go</files>
  <behavior>
    - RED test TestSessionLoadReplaysCleanSession: temp workdir store holds a hand-written transcript_<uuid>.jsonl for a cleanly-closed session (session_start, user_message, agent_message_chunk+assistant_message, tool_call+tool_result, boundary, session_end); driving the Server over in-memory pipes: initialize advertises loadSession true; session/load with that sessionId streams session/update frames BEFORE the load response is readable; the frame sequence contains the assistant text as agent_message_chunk(s) and the tool_call plus a terminal tool_call_update; the response JSON has configOptions and modes keys and no sessionId key; a following session/prompt with the same sessionId is accepted (no error) and the turn it produces appends a transcript line whose TurnID suffix is exactly one past the fixture's maximum turn number.
    - session/load with a sessionId that has a zero-byte <id>.deleted sibling returns a typed RPC error (not a fresh session).
    - session/load with a sessionId matching no transcript_<id>.jsonl returns a typed RPC error.
    - session/load with a traversal-shaped sessionId (e.g. containing a slash or dots-only) is rejected before any file open (sessIDPattern).
  </behavior>
  <read_first>internal/acp/handlers.go (registerHandlers :23-31, handleSessionNew params-parse pattern :66-83, the load stub :184-192, handleSessionCancel/closeSessionIfPossible :153-182, capability advertisement :43-51), internal/acp/server.go (Server/sessionState structs :58-81, ChunkEmitter/TurnRunner/SessionCloser interfaces :16-41, Emitter :143), internal/acp/types.go (RPCError + code constants :46-69), internal/session/transcript.go (Line envelope :61-115, kind consts :18-49, filePermOwner/dirPerm :11-12), internal/session/manager.go (NewManager :39, ReadAll :287, AppendCanceled :195), internal/session/session.go (turnCounter :127, nextTurnID :139-143), internal/coreexec/messaging.go (sessIDPattern :185-186, Sessions enumeration :241-257, bounded reader :271-278), internal/runtime/runtime.go (sessionFor :~860-905 — NewManager at :884), internal/acpserve/acp_serve.go (NewServer construction inside acpserve.Run), .planning/phases/18-session-family/18-PATTERNS.md (handlers + messaging sections), .planning/phases/16-acp-wire-foundation/16-CONTEXT.md (D-02 ordered emitter, D-20 additive kinds)</read_first>
  <action>
RED: create internal/acp/session_family_test.go with TestSessionLoadReplaysCleanSession and the three rejection cases above, driving the Server through in-memory pipes the way internal/acp/server_test.go does (read that file for the frame-driving helper conventions). Fixtures: build the store in t.TempDir(), write the transcript with session.NewLine-shaped JSON by hand (or via session.NewManager Append* calls), give the uuid-form id that sessIDPattern accepts. Commit failing with message test(18-01): add failing session/load replay test.
GREEN, in dependency order:
1. internal/session/seed.go: MaxTurnCounter(sessionID string, lines []Line) int64 scans TurnID values matching the sessionID-turn-%03d shape (derive the shape from nextTurnID at session.go:139-143) and returns the max suffix, 0 when none. Pure function, no I/O.
2. internal/session/session.go: add exported SeedResume(maxTurns int64) that calls s.turnCounter.Store(maxTurns) — one method beside nextTurnID, same //nolint:funcorder grouping comment style.
3. internal/runtime/runtime.go: add exported func (r *Runner) ResumeSession(ctx context.Context, sessionID string) error — calls the existing sessionFor(ctx, sessionID) (the transcript id IS the session id; sessionFor already opens the Manager for that id at :884), then mgr.ReadAll(), then MaxTurnCounter + SeedResume. Degrade loudly (log) on ReadAll error but still seed what was readable.
4. internal/acp/server.go: add WithWorkDir(dir string) ServerOption setting a Server.workDir field (default "" meaning cwd — match acpserve's WorkDir resolution); declare the optional capability interface SessionLoader { ResumeSession(ctx context.Context, sessionID string) error } beside SessionCloser (:35-41) with the same doc style; add a ready bool (guarded by sessionState.mu) plus setReady so handleSessionPrompt can gate.
5. internal/acp/types.go: LoadSessionRequest { Meta, Cursor-independent fields: SessionId (required), Cwd (required), McpServers, AdditionalDirectories } and LoadSessionResponse { Meta json.RawMessage, ConfigOptions any, Modes any } — exactly these three response fields, wire-cased with the tagliatelle marker comment; NO session-id field anywhere in the response struct.
6. internal/acp/replay.go: the replayLine tolerant reader (own struct, copied field spellings from the Line envelope — follow the coreexec transcriptLine precedent of NOT importing internal/session, keeping the frontend decoupled for the 25-D-13 kit seam) + ReplayTranscript(emit <the per-session emitter handle type from 16-01>, dir string, sessionID string) error that streams frames: agent_message_chunk lines emit content chunks, assistant_message emits a final chunk, tool_call emits the tool_call frame, tool_result emits a terminal tool_call_update keyed by toolCallID (status completed or failed from the isError flag, locations/diff omitted on replay). Document the full mapping table (including which kinds emit nothing) in the file header per the sixth must-have truth. Bounded reading with the messaging.go scanner-buffer pattern; skip non-conforming lines.
7. internal/acp/handlers.go: replace the stub body of handleSessionLoad with: parse LoadSessionRequest; validate sessionId against a package-local copy of sessIDPattern (copy verbatim with its provenance comment per PATTERNS); stat filepath.Join(workDir-resolved .ass-guard, sessionID + ".deleted") and the transcript path — tombstoned or missing => RPCError with the existing code constants and a message naming which; type-assert turnRunner to SessionLoader and call ResumeSession (plain error => -32603 via the server.go scrub path); run ReplayTranscript through s.Emitter(sessionID); THEN insert sessionState{id} into s.sessions and setReady — the map insert and ready flip are the LAST steps before building the response (D-03). Response: Modes nil in this plan (plan-mode seeding lands with 18-05), ConfigOptions from the Phase-16 advertisement builder if one exists at execution time (grep internal/acp for the configOptions builder 16-05 created; if present call it, else null). Also flip the initialize advertisement at handlers.go:50 from its current false to true, updating the doc comment that says NO replay. Add the ready-gate to handleSessionPrompt: session not ready => typed RPC error message naming replay-in-progress.
8. internal/acpserve/acp_serve.go: pass WithWorkDir(resolved workdir) in the NewServer options inside acpserve.Run.
Commit green with message feat(18-01): implement session/load replay spine.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acp/ -run 'TestSessionLoad' && go vet ./internal/acp/ ./internal/runtime/ ./internal/session/</automated>
  </verify>
  <acceptance_criteria>
- TestSessionLoadReplaysCleanSession passes under -race: replay frames are readable on the client pipe before the load response, the response JSON contains the keys configOptions and modes and does not contain a session-id key, and the post-load prompt's turn id continues the fixture's sequence.
- The three rejection cases (tombstoned, unknown id, traversal-shaped id) each return a typed RPCError and create no sessionState and no transcript file.
- internal/acp/handlers.go contains func (s *Server) LoadSession( (the exported core the CLI path will reuse) and the registerHandlers map still routes session/load to the thin params-parse wrapper.
- internal/acp/handlers.go advertises loadSession true in the initialize capabilities (grep for loadSession shows the true literal, not the false one).
- internal/runtime/runtime.go contains func (r *Runner) ResumeSession( and internal/session/seed.go contains func MaxTurnCounter(.
- internal/acp/replay.go contains func ReplayTranscript( and does NOT contain an import of the internal/session package.
- go vet is clean on the three touched packages.
  </acceptance_criteria>
  <done>The clean-session load path works end-to-end (validate, adopt, seed, replay, gate, respond) and is committed with RED then GREEN commits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay vocabulary completion + reconcile-then-accept gate under concurrency</name>
  <files>internal/acp/replay.go, internal/acp/replay_test.go, internal/acp/handlers.go, internal/acp/session_family_test.go</files>
  <behavior>
    - ReplayTranscript over a transcript containing error lines, canceled lines, multiple interleaved tool_call/tool_result pairs across turns, and an agent_message_chunk stream whose assistant_message follows much later: every agent-visible line maps to exactly one frame in transcript order; error lines render as a terminal tool_call_update only when they carry a toolCallID, otherwise as an agent_message_chunk carrying the error text; canceled turns emit no extra frames beyond what their tool lines already emitted.
    - Torn trailing line (a final line of garbage bytes) is skipped without error.
    - TestLoadGateRejectsPromptDuringReplay: while ReplayTranscript is paused mid-stream (inject a slow emitter or a large fixture with a controllable writer), a concurrent session/prompt for that id returns the typed replay-in-progress error, and after the load completes the same prompt is accepted; run under -race.
    - Unknown future line kind (e.g. "quantum_teleport") is skipped by the reader without failing the replay (16-D-20 tolerance).
  </behavior>
  <read_first>internal/acp/replay.go (as built in Task 1), internal/acp/session_family_test.go (Task 1 fixtures), internal/session/transcript.go (Line envelope field groups :61-115), .planning/phases/16-acp-wire-foundation/16-CONTEXT.md (D-20 additive-only tolerance rule)</read_first>
  <action>
RED: extend session_family_test.go with TestLoadGateRejectsPromptDuringReplay (concurrent prompt during a deliberately slowed replay — control the emitter's writer with a blocking pipe or a counting slow writer, mirroring the fake slow writer pattern from 16-01's stress tests) and create internal/acp/replay_test.go with the mapping/torn-line/unknown-kind cases as table-driven subtests over fixture transcripts built in temp dirs. Commit failing.
GREEN: complete the mapping in replay.go for the remaining agent-visible kinds (error-line handling per the behavior note; ensure ordering is strictly transcript order and each tool_result maps to the tool_call_update keyed by its toolCallID so the client can pair them). Tighten the ready-gate so the prompt error message names the session and the replay state. Keep all additions inside the two files plus the handler gate already landed. Commit green.
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/acp/ -run 'TestReplay|TestLoadGate|TestSessionLoad'</automated>
  </verify>
  <acceptance_criteria>
- All replay_test.go subtests pass under -race; the unknown-kind and torn-line fixtures parse without error and produce the frames of the known kinds only.
- TestLoadGateRejectsPromptDuringReplay passes under -race with the prompt rejected BEFORE load completion and accepted AFTER, and the -race detector reports no data race on the ready flag or the sessions map.
- internal/acp/replay_test.go exists and contains subtests covering error-line, canceled-line, interleaved-pairs, torn-line, and unknown-kind cases.
  </acceptance_criteria>
  <done>The replay mapping covers the full agent-visible line vocabulary, unknown kinds degrade tolerantly, and the D-03 gate is proven under concurrency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→agent stdio RPC | session/load params carry an untrusted sessionId and cwd from the editor |
| agent→filesystem store | replay reads and the tombstone stat cross into .ass-guard/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | sessionId path construction in handleSessionLoad/replay.go | high | mitigate | sessIDPattern copied verbatim from messaging.go:185-186 and applied BEFORE any file open; both regex alternatives exclude path separators so the Join cannot escape .ass-guard/ |
| T-18-02 | Info disclosure | replay of another directory's session via crafted cwd | medium | mitigate | The store root is the SERVER's workDir (acpserve-resolved), never the client-supplied cwd; client cwd is recorded but not used for path resolution |
| T-18-03 | Tampering | symlinked transcript read during replay | medium | mitigate | open with O_RDONLY after stat; a non-regular file (mode&ModeSymlink resolved via os.Stat not Lstat) is rejected with the typed unknown-session error — follow the research symlink note |
| T-18-SC | Tampering | package installs | low | accept | No new packages this plan (stdlib + existing cobra); no package-manager install tasks exist |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/acp/ ./internal/runtime/ ./internal/session/` green (target < 60s).
- `mise ci` at wave merge.
- Manual surface for the phase checkpoint later: in Zed, open a past session from the picker and watch the replay render (deferred to phase verification — this plan proves the path with in-memory pipe tests).
</verification>

<success_criteria>
- Clean-session session/load replays through the ordered emitter with updates-before-response ordering.
- Turn ids continue from transcript maxima; prompts before replay-complete are typed-rejected.
- v1 LoadSessionResponse shape exact (no sessionId field); loadSession advertised true.
- Tombstoned/unknown/traversal ids rejected without side effects.
</success_criteria>

<output>
Create `.planning/phases/18-session-family/18-01-SUMMARY.md` when done
</output>

## Artifacts this phase-plan produces

- `internal/acp/replay.go` — `ReplayTranscript`, `replayLine` reader, replay mapping table
- `internal/acp/handlers.go` — real `handleSessionLoad`, exported `Server.LoadSession` core, `SessionLoader` interface (in server.go), prompt ready-gate
- `internal/acp/server.go` — `WithWorkDir` ServerOption, `Server.workDir`, `sessionState.ready/setReady`
- `internal/acp/types.go` — `LoadSessionRequest`, `LoadSessionResponse` (v1 shape)
- `internal/runtime/runtime.go` — `Runner.ResumeSession`
- `internal/session/seed.go` — `MaxTurnCounter`
- `internal/session/session.go` — `Session.SeedResume`
- New tests: `internal/acp/session_family_test.go`, `internal/acp/replay_test.go`
