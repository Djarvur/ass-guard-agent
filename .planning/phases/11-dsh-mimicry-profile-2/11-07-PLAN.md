---
phase: 11-dsh-mimicry-profile-2
plan: 07
type: execute
wave: 4
depends_on: ["11-06"]
files_modified:
  - cmd/ass-guard/profile_check.go
  - cmd/ass-guard/profile_check_test.go
  - internal/parity/dsh_suite.go
  - internal/parity/dsh_suite_test.go
  - cmd/ass-guard/parity.go
  - profiles/dsh/drift-reports
requirements: [DSH-04, DSH-05]
autonomous: true
must_haves:
  truths:
    - "`profile check dsh` detects drift against the per-profile capture: the capture-line loader is profile-keyed (zcode → rollout freshest line + ModelIO counter; dsh → recording-proxy capture line + chat-completions counter) with `drift.Detect` unchanged — running it against the 11-06 capture reports ZERO drift (the bundle is self-consistent) (DSH-04's drift leg)"
    - "The A/B parity harness runs green for dsh: the suite extracts CapturedTurns from the dsh capture, the ass-guard arm runs the dsh profile through the factory-built OpenAI provider, tool-call sequences are compared with FRESHLY baselined thresholds (zcode numbers never carried over) (DSH-05, D-03's full behavioral bar)"
    - "One live DeepSeek tool-calling round-trip succeeds per routed model against the D-01 gateway — env-gated with loud-skip-with-note when the credential is absent; extraction/drift legs never block on keys (DSH-05's live leg)"
    - "Every profile entry is traceable to a captured request: a traceability audit over coverage.yaml Sources passes, and the phase's gates (zcodeism gate, pin gate, leak-guard, mise ci) are all green at close (DSH-05's traceability leg + the phase gate)"
  artifacts:
    - path: "cmd/ass-guard/profile_check.go"
      provides: "per-profile capture loaders: a registry keyed by profile name (zcode: rollout-path loader + ModelIO counter — current behavior moved behind the interface; dsh: proxy-capture-file loader + chat-completions counter extracting system/tools/strict/headers/model counts) — the 11-01 census's profile_check genericization delivered"
    - path: "internal/parity/dsh_suite.go"
      provides: "ExtractTurnsFromDshCapture(captureLines) ([]CapturedTurn, error) — prompts + expected tool-call sequences from the capture's request/response pairs; the parity suite extractor's dsh sibling (the 11-01 census's parity genericization delivered)"
    - path: "profiles/dsh/drift-reports"
      provides: "the parity baseline record: fresh dsh thresholds, the run's summary numbers, and the live-probe results (or the loud pending note) — the D-03 evidence artifact"
  key_links:
    - from: "cmd/ass-guard/profile_check.go captureLoaderFor(name)"
      to: "internal/drift.Detect(manifest, captured)"
      via: "each loader produces the captured map; Detect stays generic — one detector, two capture formats"
      pattern: "drift\\.Detect"
    - from: "internal/parity/dsh_suite.go"
      to: "parity.Run + LiveArm{Profile: dsh, Provider: factory-built openai}"
      via: "the existing arm-agnostic harness — only the suite source is new"
      pattern: "CapturedTurn"
---

# Plan 11-07 — Drift check, A/B parity, the live probe, and the traceability close-out

## Phase Goal

**As an** operator routing DeepSeek-model turns, **I want** a second mimicry profile whose content is captured from deepseek-harness's real wire traffic, **so that** ass-guard's requests are structurally indistinguishable from dsh's — proving the N-profile thesis with no target-specific code paths.

**This plan's slice:** DSH-04's drift leg + all of DSH-05 — the proof plan. The bundle exists (11-06); now the thesis gets tested, not assumed (D-03): `profile check dsh` goes per-profile (the last zcode-ism genericization), the A/B parity harness runs dsh against a live DeepSeek endpoint with freshly baselined thresholds, one live tool-calling round-trip proves each routed model works through the full stack, and the traceability audit shows every profile entry pointing at a captured line. This is also where the phase gate comes together: zcodeism gate + zstd pin gate + leak-guard + mise ci all green in one close-out verification.

## Truths

- The parity harness is arm-agnostic (verified: `LiveArm{Profile, Provider}` in run.go:24; the cmd builds providers through the Phase-9 factory seam) — only the SUITE source is dsh-new. The suite comes from the capture's request/response pairs: prompts from user messages, expected tool-call sequences from the recorded responses (corroborated by the zstd events, as 11-05's reconciliation already proved consistent).
- `drift.Detect` is generic (manifest + map — drift.go:29); the zcode-ism is the capture-line SOURCE and the ModelIO-shaped counter (profile_check.go:86-118). This plan moves zcode behind a loader interface and adds the dsh loader: same detector, two formats — the exact genericize-or-parameterize move the 11-01 census scheduled here.
- Fresh baselines only: dsh parity thresholds are set from THIS run's numbers; zcode's Phase-1/re-capture numbers never cross over (Pitfall 18's rule; 09-04's supersede discipline).
- The live probe and parity legs are credential-gated (OPENCODE_GATEWAY_KEY per D-01; DeepSeek official API as the base-URL-swap fallback); absent key → loud skip + pending note in the drift report and SUMMARY — the extraction/drift/traceability legs stay unkeyed (09-04's rule, third time in this phase).
- D-03's bar is behavioral: tool-call sequences statistically indistinguishable — the harness's existing comparison + surprise check, now fed dsh data. The wire-shape equality was already proven mechanically (11-05's full-loop test + 11-06's real-data round-trip); this plan proves the BEHAVIOR on top.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dsh-mimicry-profile-2/11-CONTEXT.md
@.planning/phases/11-dsh-mimicry-profile-2/11-RESEARCH.md
@cmd/ass-guard/profile_check.go
@cmd/ass-guard/parity.go
@internal/parity/run.go

<interfaces>
From the current code (read before editing):
```go
// cmd/ass-guard/profile_check.go — the genericization site
func runProfileCheck(name, profilesDir, captureFile string) error  // :58 — loads coverage.yaml by NAME (already generic), then…
func loadCaptureLine(captureFile string) (json.RawMessage, error) // :86 — --capture-file OR the zcode-hardcoded freshest-rollout path (:103-118) — THE zcode-ism
func extractCaptureCounts(raw json.RawMessage) map[string]any     // :122 — parses zcode ModelIO (request.body.system blocks, tools, headers, model) — the second zcode-ism
func reportProfileCheck(profileName string, drifts []drift.Drift, manifestFields int)  // :175 — generic output, keep

// internal/drift — already generic
func Detect(manifest *profile.CoverageManifest, captured map[string]any) []Drift  // drift.go:29 — UNCHANGED

// internal/parity — arm-agnostic; suite sources are per-target
type CapturedTurn struct { ... }                                  // harness.go — the suite unit
func ExtractTurnsFromRollout(rollout string) ([]CapturedTurn, error)  // replay.go — the zcode suite source (pattern for dsh_suite.go)
type LiveArm struct { Profile profile.Profile; Provider provider.Provider }  // run.go:24 — takes ANY provider
func Run(ctx, *RunOptions) (RunResult, error)                     // run.go:79 — comparison + results JSON

// cmd/ass-guard/parity.go — builds the provider via the scheduler factory (setupProviderFactory + factory.Build, :88-99); --profile flag exists (default zcode)
```
</interfaces>
</context>

<tasks>

<task type="tdd" id="T1">
  <name>Task 1: Per-profile capture loaders for `profile check` (RED→GREEN)</name>
  <files>cmd/ass-guard/profile_check.go, cmd/ass-guard/profile_check_test.go</files>
  <behavior>
    - Test 1 (registry): `captureLoaderFor("zcode")` returns the rollout-path loader with the ModelIO counter (current behavior — regression-pinned by the existing tests); `captureLoaderFor("dsh")` returns the proxy-capture loader with the chat-completions counter; unknown profile → clear error naming the flag to pass --capture-file.
    - Test 2 (dsh counter): a capture request line (the 11-03 schema fixture) yields the captured map with the chat-completions keys the drift detector consumes: system-block list, tools list, strict presence, header-name set, model — mapped onto the same manifest field paths the coverage.yaml declares.
    - Test 3 (end-to-end drift): `profile check dsh --capture-file <synthetic capture line matching the shipped manifest>` reports ZERO drift; a mutated line (one tool removed) reports the TIER-appropriate drift — the detector proves it can fail (the canary discipline).
    - Test 4 (flag override): --capture-file works for BOTH profiles regardless of registry (the existing escape hatch stays).
  </behavior>
  <action>
    Refactor `cmd/ass-guard/profile_check.go`: introduce a capture-loader interface (loader + counter pair) with a small registry keyed by profile name; move the zcode path (freshest-rollout resolution + ModelIO counting) behind it unchanged; add the dsh path (proxy-capture JSONL freshest request line + a chat-completions counter extracting system/tools/strict/headers/model into the captured map on the manifest's field paths). `runProfileCheck` consults the registry, honoring --capture-file first (unchanged semantics). `internal/drift` is NOT modified — one detector, two formats. Per DSH-04's "profile check dsh detects drift against the per-profile capture" + the 11-01 census's scheduled genericization.
  </action>
  <read_first>
    - cmd/ass-guard/profile_check.go (loadCaptureLine + extractCaptureCounts — the code being moved behind the interface)
    - internal/drift/drift.go (Detect's captured-map expectations — the dsh counter must speak them)
    - internal/profile/types.go CoverageEntry (the manifest field paths both counters fill)
    - profiles/dsh/coverage.yaml (the shipped manifest the dsh loader checks against)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./cmd/ass-guard/ -race -run TestProfileCheck -v 2>&1 | tail -8</automated></verify>
  <done>Registry with two loaders + zcode regression green + dsh drift detectable (canary) + flag override intact</done>
  <acceptance_criteria>
    - `go test ./cmd/ass-guard/ -race` exits 0
    - `go run ./cmd/ass-guard profile check dsh --capture-file <11-06 capture line>` reports zero drift (executor smoke; recorded in SUMMARY)
    - `./scripts/zcodeism-gate.sh` green (the loader registry carries no target names outside data)
  </acceptance_criteria>
</task>

<task type="tdd" id="T2">
  <name>Task 2: The dsh parity suite extractor (RED→GREEN)</name>
  <files>internal/parity/dsh_suite.go, internal/parity/dsh_suite_test.go</files>
  <behavior>
    - Test 1 (extraction): a synthetic capture file (request+response pairs per the 11-03 schema) yields CapturedTurns: each user message starts a turn; the expected tool-call sequence comes from the paired response's tool_calls (id/name/arguments); multi-tool-call responses preserve order.
    - Test 2 (empty-output + subagent turns): the '(no output)' tool-result turn and the subagent-dispatch turn extract correctly (the workload's divergence classes survive into the suite).
    - Test 3 (corroboration): when the zstd events are supplied, the tool-call sequences agree with the wire responses (reuse 11-05's reconciliation); disagreement errors.
    - Test 4 (suite compat): the extracted suite runs through `parity.Run` against a stub arm — the suite is harness-compatible by construction.
  </behavior>
  <action>
    Per DSH-05 + research §8: create `internal/parity/dsh_suite.go` with `ExtractTurnsFromDshCapture` mirroring `ExtractTurnsFromRollout`'s role for the dsh capture format (read replay.go first — copy its suite-construction discipline, not its format logic). Prompts from user messages; expected sequences from response tool_calls; order-preserving; error on malformed pairs rather than skipping (a silently-shortened suite would under-test parity). Unit tests over a synthetic capture fixture built with the 11-03 schema (reuse the proxy tests' fixture builders).
  </action>
  <read_first>
    - internal/parity/replay.go (ExtractTurnsFromRollout — the sibling extractor's discipline)
    - internal/parity/harness.go (CapturedTurn's exact shape)
    - cmd/dsh-proxy/main_test.go (fixture builders for capture lines)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/parity/ -race -run TestDsh -v 2>&1 | tail -8</automated></verify>
  <done>The dsh suite extractor feeds the existing harness; divergence classes preserved; malformed input errors loudly</done>
  <acceptance_criteria>
    - `go test ./internal/parity/ -race` exits 0
    - `grep -c "ExtractTurnsFromDshCapture" internal/parity/dsh_suite.go cmd/ass-guard/parity.go` >= 2 (extractor exists and is wired)
  </acceptance_criteria>
</task>

<task type="auto" id="T3">
  <name>Task 3: The A/B parity run + live probe + traceability audit + the phase-gate close-out</name>
  <files>cmd/ass-guard/parity.go, profiles/dsh/drift-reports/2026-08-14-parity-baseline.md</files>
  <action>
    (a) Wire the dsh suite source into `cmd/ass-guard/parity.go` (a --dsh-capture flag or mode alongside --rollout; the arm stays LiveArm built via the scheduler factory with `--profile dsh` selecting the bundle — read parity.go's flag flow first). (b) THE PARITY RUN (credential-gated): with OPENCODE_GATEWAY_KEY exported, run the harness over the 11-06 capture's suite against the gateway; record the summary numbers in `profiles/dsh/drift-reports/2026-08-14-parity-baseline.md` with EXPLICITLY fresh thresholds ("dsh baseline established <date>; zcode baselines do not apply"). Absent key → append the loud pending note ("parity pending OPENCODE_GATEWAY_KEY — drift/traceability legs complete; the phase gate requires this leg before close") and report the skip in the SUMMARY — never silently drop. (c) LIVE PROBE per routed model: `go run ./cmd/ass-guard probe --provider opencode-gw` (11-04) for each dsh-declared model actually routed (deepseek-chat; deepseek-reasoner if a tier/project routes it); PASS results (or the loud skip) go into the drift report. (d) TRACEABILITY AUDIT: walk profiles/dsh/coverage.yaml — every entry's Source cites a capture line; write the audit verdict (N/N traceable) into the drift report. (e) CLOSE-OUT: run the full gate set — `mise run ci` (which now includes the zcodeism gate + zstd pin test), the leak-guard, and `go test ./... -race`; record all green in the drift report's final section. Per DSH-05 + the ROADMAP phase gate (every leg, no stub-only evidence).
  </action>
  <read_first>
    - cmd/ass-guard/parity.go (flag flow + factory wiring — the extension site)
    - internal/parity/run.go (RunOptions/RunResult — what the run records)
    - cmd/ass-guard/probe.go (11-04's probe — the live-leg command)
    - profiles/dsh/coverage.yaml (the traceability audit's subject)
    - .planning/phases/09-serve-path-audit-zcode-parity-re-capture/09-04-PLAN.md (the loud-skip + supersede discipline being replayed)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && test -f profiles/dsh/drift-reports/2026-08-14-parity-baseline.md && grep -c "thresholds\|traceable\|OPENCODE_GATEWAY_KEY\|probe" profiles/dsh/drift-reports/2026-08-14-parity-baseline.md && mise run ci 2>&1 | tail -5</automated></verify>
  <done>Parity run recorded (fresh baseline) or loudly pending; probe PASS/skip per routed model recorded; traceability N/N; the full gate set green and evidenced in the drift report</done>
  <acceptance_criteria>
    - The drift report contains EITHER fresh parity numbers with the fresh-baseline sentence OR the pending-key note (never silence)
    - The traceability verdict states N/N coverage entries cite capture lines
    - `mise run ci` exits 0 (zcodeism gate + zstd pin riding it)
    - `go test ./... -race` zero FAIL
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live gateway/DeepSeek endpoint → parity arm + probe | External service consuming the dsh profile's shaped requests; responses drive pass/fail |
| Capture file (outside repo) → suite extractor → parity expectations | The expected tool-call sequences come from captured live-dsh behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-22 | Tampering | parity thresholds copied from zcode baselines (cross-baseline comparison) | mitigate | The drift report's fresh-baseline sentence is required by acceptance; the Phase-1 numbers are marked superseded-inapplicable |
| T-11-23 | Repudiation | parity/skip outcome unrecorded — the phase closes on vibes | mitigate | The drift report artifact + SUMMARY requirement; verify-phase reads exactly this report |
| T-11-24 | Information disclosure | credential leakage through parity/probe errors | mitigate | Factory-credential machinery + internal/redact scrubbing (probe.go pattern); the drift report records host + outcome, never keys |
| T-11-25 | Tampering | a silently-shortened suite (skipped malformed pairs) under-testing parity | mitigate | The extractor errors on malformed pairs (never skips); Test 1 pins order-preservation and completeness |
</threat_model>

<verification>
- `profile check dsh` zero-drift on the capture line; canary proves detection (T1)
- Parity run recorded (fresh baseline) or loudly pending the key; probe results per routed model recorded (T3)
- Traceability N/N; `mise run ci` + `go test ./... -race` green — every ROADMAP phase-gate leg evidenced
</verification>

<success_criteria>
- D-03's full behavioral bar is proven or explicitly pending its credential — never assumed, never silently dropped
- "N profiles, no target-specific code paths" holds mechanically: two capture formats through one drift detector, two suites through one harness, two bundles through one adapter — and the gates enforce it going forward (DSH-01/04/05 close together)
</success_criteria>

<output>
Create `.planning/phases/11-dsh-mimicry-profile-2/11-07-SUMMARY.md` when done
</output>
