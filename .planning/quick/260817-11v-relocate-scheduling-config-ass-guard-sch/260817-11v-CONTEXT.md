# Quick Task 260817-11v: Relocate scheduling config: .ass-guard/scheduling.yaml to config.yaml (global ~/.config layer + project .ass-guard layer) - Context

**Gathered:** 2026-08-17 (revised same day — see Revision below)
**Status:** Ready for planning

> **REVISION (2026-08-17, user):** The project-level config stays in `.ass-guard/` under the
> NEW name `config.yaml` (NOT `.config/ass-guard-agent/` in the project tree). The global
> layer remains `~/.config/ass-guard-agent/config.yaml`. Additionally: no migration concerns
> at all — there are no existing installs; treat any old `scheduling.yaml` as nonexistent.

<domain>
## Task Boundary

The operator-facing scheduling config file is renamed and re-layered. Two layers:
- **Global (NEW):** `~/.config/ass-guard-agent/config.yaml` (via `os.UserHomeDir()`; no XDG helper convention in this codebase).
- **Project (renamed):** `<workDir>/.ass-guard/config.yaml` — same directory as today, new filename.

Merge order: embedded default → global → project (project wins). The YAML schema is unchanged (scheduler-only: providers / models / tiers / time_windows / circuit_breaker / cost_ceiling).

In scope:
- Where the runtime READS the operator config (layered merge above; `scheduler.Load` callers).
- Where first-run SEEDS the project config (`internal/firstrun` + `internal/defaults` — the embedded seed file is renamed `scheduling.yaml` → `config.yaml` and still seeds into `.ass-guard/` via the existing WriteTree flow).
- All code references to the old filename `scheduling.yaml` (loader callers, provider-factory wiring, permission-warning check, CLI flag help text), docs (`docs/install.md`), tests, and `.gitignore` comment block.
- The rest of `.ass-guard/` (transcripts, learned.yaml, api_key, audit/, seeded profiles, openspec.toml) is OUT of scope and stays put. `.ass-guard/` remains the ignored runtime dir; the global dir gets NO project-tree `.config/` involvement.
- No legacy reads, no fallback, no migration logic anywhere — no existing installs exist (user decision).

</domain>

<decisions>
## Implementation Decisions

### Path layers — global XDG-style + project in .ass-guard/ (user decision, revised)
- Global: `~/.config/ass-guard-agent/config.yaml` (via `os.UserHomeDir()`; the codebase has no XDG helper convention — `profile_check.go:113` and `ecosys/loader.go` use `os.UserHomeDir()` directly).
- Project: `<workDir>/.ass-guard/config.yaml` — the SAME runtime directory as today, file renamed from `scheduling.yaml` to `config.yaml`. There is NO `.config/ass-guard-agent/` directory in the project tree.
- Merge order: embedded default → global → project (project wins), using the existing `scheduler.Load` overlay semantics.

### Migration — none at all (user decision, revised)
- No existing installs exist. No fallback, no auto-migration, no deprecation shim, no legacy reads of `.ass-guard/scheduling.yaml`.
- Any reference to the old filename `scheduling.yaml` in code/tests/docs/comments is renamed to `config.yaml`; nothing needs to keep working against the old name.

### File scope — same schema, renamed (user decision)
- The file keeps the exact current scheduler schema; `config.yaml` is a name change, not a generalized config container. No new top-level sections, no nesting under a `scheduling:` key.

### Claude's Discretion
- Whether the permission-hygiene warning (SC3, warnLooseConfigPerm) checks global, project, or both new-path files — cover both new-path files if cheap, else the project one.
- Seeding stays exactly as today (first-run seeds `.ass-guard/` via WriteTree; the embedded seed entry is simply renamed) — no global-dir seeding.
- Test/godoc wording updates; internal constant/helper naming.

</decisions>

<specifics>
## Specific Ideas

- Current load path construction: `cmd/ass-guard/acp_serve.go` (`filepath.Join(opts.WorkDir, ".ass-guard", "scheduling.yaml")` around lines 315–327; `warnLooseConfigPerm` at :327).
- First-run seeding: `internal/firstrun/firstrun.go` (`assGuardDir = ".ass-guard"`, seeds scheduling.yaml via `defaults.WriteTree`) — seeding flow unchanged, only the seed entry's filename changes; embedded seed at `internal/defaults/seed/scheduling.yaml`; embedded copy also at `internal/scheduler/defaults/scheduling.yaml`.
- Loader: `internal/scheduler/load.go` — `Load(configPaths...)` already deep-merges embedded default → each caller path; adding the global layer is a caller-side change (pass global then project).
- CLI: `cmd/ass-guard/scheduling.go` flag help "path to scheduling.yaml" (lines 61, 120).
- `.gitignore:55-59` — `.ass-guard/` ignore entry stays as-is; only the comment block's mention of `scheduling.yaml` needs rewording. No new ignore entry is required (the project config remains inside `.ass-guard/`).
- Known test files referencing the old name: provider_factory_test.go, zeroconfig_test.go, acp_serve_test.go, e2e_opsx_test.go, firstrun_test.go, defaults_test.go.
- Simplification vs the first draft: because the project config stays in `.ass-guard/`, `defaults.WriteTree` keeps seeding it in place (renamed) — no separate `WriteSchedulingConfig` split and no new project-tree directory creation.

</specifics>

<canonical_refs>
## Canonical References

- XDG Base Directory Specification (convention behind `~/.config/...`): https://specifications.freedesktop.org/basedir-spec/latest/

</canonical_refs>
