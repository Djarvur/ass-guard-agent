---
phase: quick-260817-11v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/ass-guard/provider_factory.go
  - cmd/ass-guard/acp_serve.go
  - cmd/ass-guard/scheduling.go
  - cmd/ass-guard/provider_factory_test.go
  - internal/defaults/defaults.go
  - internal/defaults/seed/sync.sh
  - internal/defaults/defaults_test.go
  - internal/scheduler/load.go
  - internal/scheduler/defaults/scheduling.yaml
  - internal/defaults/seed/scheduling.yaml
  - internal/firstrun/firstrun.go
  - internal/firstrun/firstrun_test.go
  - cmd/ass-guard/zeroconfig_test.go
  - cmd/ass-guard/acp_serve_test.go
  - cmd/ass-guard/e2e_opsx_test.go
  - internal/scheduler/factory.go
  - internal/scheduler/doc.go
  - internal/scheduler/init.go
  - internal/hookdag/config.go
  - internal/hookdag/seeded.yaml
  - docs/install.md
  - .gitignore
autonomous: true
requirements:
  - QUICK-260817-11v
estimate:
  tokens: 33000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Fresh `ass-guard acp serve` in an empty project seeds .ass-guard/ exactly as today (.gitignore + profiles/zcode + openspec.toml) with the config file now named config.yaml; no legacy-named file is created anywhere"
    - "A config at ~/.config/ass-guard-agent/config.yaml is honored as the global layer, and the project layer (<workDir>/.ass-guard/config.yaml) overrides it on conflict (embedded default -> global -> project, project wins)"
    - "The old filename is treated as nonexistent — a file carrying it is never read; with only that file present the runtime serves the embedded default, exactly as with no config at all"
    - "Startup warns once per loose-permission new-path file (global or project) with the chmod 0600 recommendation; 0600-tight files stay silent"
    - "`ass-guard scheduling validate --help` and `resolve --help` name config.yaml in the --config flag help"
    - "No tracked file outside .planning references the old filename — the git-grep rename gate is clean"
  artifacts:
    - "cmd/ass-guard/provider_factory.go with globalConfigPath/projectConfigPath helpers and layered loadSchedulingFactory (project = <workDir>/.ass-guard/config.yaml)"
    - "internal/scheduler/defaults/config.yaml and internal/defaults/seed/config.yaml (git mv renames, byte-identical drift pair); defaults.WriteTree still seeds it in place under .ass-guard/ — seeding flow unchanged, entry renamed"
    - "docs/install.md documenting the two-layer layout + layered resolution"
    - ".gitignore comment block reworded to the new filename (.ass-guard/ ignore entry itself unchanged)"
  key_links:
    - "firstrun.Ensure -> defaults.WriteTree -> <workDir>/.ass-guard/config.yaml (renamed seed entry, no flow change)"
    - "loadSchedulingFactory -> [globalConfigPath(), projectConfigPath(workDir)] -> scheduler.Load(global, project) — existing files only, stat-gated (Load still errors on an explicitly-passed missing path)"
    - "acp_serve.go startup -> warnLooseConfigPerm(globalPath) + warnLooseConfigPerm(projectPath)"
---

<objective>
Rename the operator scheduling config file from scheduling.yaml to config.yaml and add a NEW global layer: project config stays in the same runtime directory (<workDir>/.ass-guard/config.yaml), global config lives at ~/.config/ass-guard-agent/config.yaml (os.UserHomeDir-based). Merge order embedded default -> global -> project (project wins). Schema unchanged. No migration machinery of any kind — no existing installs exist; the old filename is treated as nonexistent everywhere.

Purpose: The scheduling config is per-machine operator config (may carry a literal api_key); it gets the config-dir convention at the global layer while the project layer remains in the established runtime dir under the new generalized name (locked decisions in the revised 260817-11v-CONTEXT.md).

Output: The 22 modified/renamed files above; all tests + the repo guard (`mise run ci`) green; a clean repo-wide rename grep gate.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260817-11v-relocate-scheduling-config-ass-guard-sch/260817-11v-CONTEXT.md
@.planning/STATE.md

Ground-truth sources (read once before starting):
- cmd/ass-guard/provider_factory.go — loadSchedulingFactory (the D-08 wiring seam shared by acp serve / tracer / parity), setupProviderFactory, warnLooseConfigPerm
- cmd/ass-guard/acp_serve.go:306-332 — runACPServe wiring: setupProviderFactory(opts.WorkDir) + warnLooseConfigPerm + the D-08 comment block
- cmd/ass-guard/scheduling.go:61,:120 — --config flag help text (validate + resolve)
- internal/scheduler/load.go — Load(paths...) stat-gated caller contract (errors on a passed-but-missing path), //go:embed defaults/scheduling.yaml, EmbeddedDefaultScheduling()
- internal/defaults/defaults.go:27 + internal/defaults/seed/sync.sh — Seed embed.FS (explicit per-path embed directive), WriteTree's generic seed/-prefix walk, the byte-for-byte drift pair, leak guard
- internal/firstrun/firstrun.go — Ensure: first-run gate on .ass-guard/ absence; seeding flow UNCHANGED (only the package doc names the renamed entry)
- Known test files: provider_factory_test.go (writeTestScheduling helper, TestStartupWarn_ConfigPermLoose), zeroconfig_test.go (assertAssGuardSeeded), acp_serve_test.go (serveAuditWorkDir:1650), e2e_opsx_test.go (findRepoRoot locator), firstrun_test.go, defaults_test.go (drift + leak guards)

Go discipline (go-ultimate skill, applies to all tasks): table-driven tests, gofmt formatting, no new dependencies, atomic commits per task.
</context>

<tasks>

<!-- planner-discipline-allow: scheduling.yaml -->

<task type="auto" tdd="true">
  <name>Task 1: Layered read path — global config.yaml + project .ass-guard/config.yaml, dual-path perm warning</name>
  <files>cmd/ass-guard/provider_factory.go, cmd/ass-guard/acp_serve.go, cmd/ass-guard/scheduling.go, cmd/ass-guard/provider_factory_test.go</files>
  <behavior>
    - Test global-only layer: HOME pinned via t.Setenv to a temp home carrying <home>/.config/ass-guard-agent/config.yaml (sentinel provider entry); loadSchedulingFactory over an empty workDir merges the sentinel over the embedded default.
    - Test project-over-global precedence: both layers present, conflicting tier binding; the project-layer value wins (Load overlay order is global then project).
    - Test old-name-is-nonexistent: a file at <workDir>/.ass-guard/scheduling.yaml carrying a sentinel provider is never read — resolved config contains no sentinel; behavior identical to no-config (embedded floor).
    - Test hermetic isolation: with HOME pinned to an empty temp dir, the existing zero-config tests still pass and stderr stays empty (no ambient global config leaks into assertions).
    - Test SC3 dual-path warning: a 0644 global config warns; a 0644 project config warns; 0600-tight files stay silent (extend TestStartupWarn_ConfigPermLoose).
  </behavior>
  <action>
    RED first: write the new table-driven tests in provider_factory_test.go before touching production code (behaviors above).
    In provider_factory.go, add two helpers: globalConfigPath() resolving os.UserHomeDir() joined with .config/ass-guard-agent/config.yaml (no XDG helper convention exists in this codebase — profile_check.go:113 and ecosys/loader.go use os.UserHomeDir() directly — per the locked path decision), and projectConfigPath(workDir) joining workDir with .ass-guard and config.yaml (same runtime directory as today, renamed filename per the revised decision).
    Rewrite loadSchedulingFactory: build an ordered candidate list [global, project]; os.Stat each; pass only the existing ones to scheduler.Load(existing...) in that order (Load errors on a passed-but-missing path — keep the existing stat-gate pattern); when none exist call scheduler.Load() as today. On os.UserHomeDir error, log one stderr line and continue without the global layer (degrade, never refuse — mirrors setupProviderFactory's degradation; parity.go:90 and main.go:128 call setupProviderFactory("", ...) and inherit the layering through this shared seam with zero changes — their empty-workDir project path stays CWD-relative by join semantics). Delete the old overlayPath join entirely — the old filename string must not survive anywhere in cmd/ass-guard production code (no legacy reads; the old name is nonexistent per the revised decision).
    In acp_serve.go, call warnLooseConfigPerm for BOTH new-path files (global then project — covering both layers is cheap, chosen per CONTEXT discretion); update the D-08 comment block (lines ~314-327) and the SC3 comment to name the new layered paths; the seedACPGuard "initialized" log wording stays (zeroconfig_test.go asserts the exact "ass-guard: initialized" substring — do not change it).
    In scheduling.go, update the --config flag help on BOTH validate (line 61) and resolve (line 120): "path to config.yaml (default: embedded zero-config floor)". Flag semantics unchanged — an explicit path still replaces layered discovery; the default is still the embedded floor (CONTEXT scopes the CLI change to help text only).
    In provider_factory_test.go, repoint writeTestScheduling to <workDir>/.ass-guard/config.yaml (same helper dir, new filename); add t.Setenv("HOME", t.TempDir()) to EVERY test that reaches loadSchedulingFactory (established hermetic pattern — acp_serve_test.go:757; note t.Setenv makes those tests non-parallel); add the new layered tests. GREEN, then gofmt.
  </action>
  <verify>
    <automated>go test ./cmd/ass-guard/ -run 'TestLoadScheduling|TestStartupWarn|TestEditorZeroEnv|TestBackwardCompat' -count=1 && go build ./... && gofmt -l cmd/ass-guard | grep -c . | grep -x 0</automated>
  </verify>
  <done>Global layer merged, project layer overriding, old filename provably never read, perm warning covering both new-path files, CLI help naming config.yaml — all pinned by tests; the old filename no longer appears in cmd/ass-guard production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename the embedded seed pair — WriteTree seeds config.yaml in place</name>
  <files>internal/defaults/defaults.go, internal/defaults/seed/sync.sh, internal/defaults/defaults_test.go, internal/scheduler/load.go, internal/scheduler/defaults/scheduling.yaml, internal/defaults/seed/scheduling.yaml, internal/firstrun/firstrun.go, internal/firstrun/firstrun_test.go, cmd/ass-guard/zeroconfig_test.go</files>
  <behavior>
    - Test WriteTree materializes config.yaml under root (renamed entry; profiles/ + openspec.toml unchanged) — TestWriteTree_MaterializesAllArtifacts expects the new filename in the tree list.
    - Test Ensure seeds .ass-guard/ with .gitignore + profiles/zcode + openspec.toml + config.yaml (renamed entry; seeding flow otherwise unchanged — TestEnsure_SeedsFreshDir / assertAssGuardSeeded updated to the new filename).
    - Drift guard continues to pass: internal/scheduler/defaults/config.yaml and internal/defaults/seed/config.yaml byte-identical via EmbeddedDefaultScheduling().
  </behavior>
  <action>
    RED first: update defaults_test.go, firstrun_test.go, and zeroconfig_test.go expectations to the new filename before the renames (tests fail against the still-old embed paths).
    Rename the embedded pair with git mv: internal/scheduler/defaults/scheduling.yaml to internal/scheduler/defaults/config.yaml, and internal/defaults/seed/scheduling.yaml to internal/defaults/seed/config.yaml (contents byte-identical — schema unchanged per CONTEXT file-scope decision).
    In internal/scheduler/load.go, update the //go:embed directive to defaults/config.yaml and reword the embeddedDefault + EmbeddedDefaultScheduling godoc to name config.yaml (exported name unchanged — internal naming is safe discretion; the drift guard depends on it).
    In internal/defaults/defaults.go, update ONLY the explicit per-path //go:embed directive (seed/profiles seed/openspec.toml seed/config.yaml) and the Seed/package doc comments — WriteTree's walk is generic (strips the seed/ prefix) so it seeds .ass-guard/config.yaml with zero walk changes; NO new function, NO skip-entry, NO directory-creation change (per the revised decision the seeding flow is unchanged).
    In internal/defaults/seed/sync.sh, update the section-(b) copy to source internal/scheduler/defaults/config.yaml and destination seed/config.yaml, and the leak-guard grep target to seed/config.yaml.
    In internal/firstrun/firstrun.go, update only the package-doc wording that names the seeded entries (scheduling.yaml to config.yaml) — no logic change.
  </action>
  <verify>
    <automated>go test ./internal/defaults/ ./internal/firstrun/ -count=1 && go test ./cmd/ass-guard/ -run 'TestZeroConfig|TestEnsure' -count=1 && go build ./...</automated>
  </verify>
  <done>First-run seeding produces .ass-guard/config.yaml via the unchanged WriteTree flow; the embedded floor + seed pair renamed byte-identically with drift + leak guards green; no new seeding machinery.</done>
</task>

<task type="auto">
  <name>Task 3: Reference sweep — serve/e2e tests, operator-facing strings, docs/install.md, .gitignore comment, rename gate</name>
  <files>cmd/ass-guard/acp_serve_test.go, cmd/ass-guard/e2e_opsx_test.go, internal/scheduler/factory.go, internal/scheduler/doc.go, internal/scheduler/init.go, internal/scheduler/load.go, internal/hookdag/config.go, internal/hookdag/seeded.yaml, docs/install.md, .gitignore</files>
  <action>
    Tests: in acp_serve_test.go, serveAuditWorkDir (~line 1650) writes the real config to <workDir>/.ass-guard/config.yaml (same dir, new filename); update the stale comments at ~line 1529 and ~line 1838 (the credential SOURCE is now the operator's config.yaml). In e2e_opsx_test.go, findRepoRoot (~line 70) and the gate/skip/fatal messages (~lines 31, 60, 94) check <dir>/.ass-guard/config.yaml — same runtime dir, new filename.
    Operator-facing strings: internal/scheduler/factory.go lines 204 and 240 — "set api_key in scheduling.yaml" becomes "set api_key in config.yaml" (credential-hint stderr output).
    Comment sweep (wording only, no behavior): internal/scheduler/doc.go line 11, internal/scheduler/init.go line 24, internal/scheduler/load.go godoc wording ("Operators override any of these via scheduling.yaml" at applyDefaults and the decode-error prefixes may stay scheduling-flavored where they describe the SCHEMA — rename only where the string names the operator FILE), internal/hookdag/config.go line 84, internal/hookdag/seeded.yaml line 4 (references to internal/scheduler's convention now name config.yaml).
    docs/install.md section 4: rename the seeded-artifact list entry to config.yaml (same .ass-guard/ location); document the two-layer resolution (embedded default, then ~/.config/ass-guard-agent/config.yaml global, then <project>/.ass-guard/config.yaml — project wins). No migration/transition commentary — no existing installs exist.
    .gitignore: comment-block rewording only (lines 55-59) — the seeded config is now named config.yaml; the .ass-guard/ ignore entry itself is unchanged and NO new entry is added (the project config stays inside the already-ignored runtime dir).
    Final sweep: run the repo-wide tracked-file grep for the old filename (excluding .planning) — it must return nothing; if a reference survives in a file not listed above, update it the same way and note the extra file in the SUMMARY.
  </action>
  <verify>
    <automated>[ -z "$(git grep -l 'scheduling.yaml' -- ':(exclude).planning')" ] && echo RENAME-CLEAN && go test ./... -count=1 && mise run ci</automated>
  </verify>
  <done>Zero tracked references to the old filename outside .planning; serve/e2e test fixtures use the new filename in the same .ass-guard/ dir; operator-facing strings, docs, and the .gitignore comment updated; the full repo guard (vet + lint + build + race) green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator filesystem (home + project) -> process config | operator-authored YAML crossing into runtime config; may carry a literal api_key |
| credentials at rest | global (~/.config/ass-guard-agent/config.yaml) and project (<workDir>/.ass-guard/config.yaml) files on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q11-01 | Information Disclosure | global + project config.yaml (literal api_key at rest) | medium | mitigate | SC3 warnLooseConfigPerm extended to BOTH new-path files (Task 1); the project layer stays inside the already-gitignored .ass-guard/ runtime dir, so accidental commit remains impossible (Task 3 keeps that entry) |
| T-q11-02 | Tampering | global layer (~/.config/ass-guard-agent) can override embedded defaults incl. provider base_url | low | accept | Same trust domain as the pre-existing project overlay — an attacker with home-dir write access already owned the old project config; the global layer adds no new privilege |
| T-q11-03 | Elevation | config-driven base_url redirect exfiltrating credentials | low | accept | Pre-existing operator-controlled surface (base_url override existed before); unchanged by the rename |

No package-manager installs in this plan — no supply-chain row required.
</threat_model>

<verification>
- Task 1: focused cmd/ass-guard test run + build + gofmt (layered merge, precedence, old-name-ignored, dual-path warning, help text).
- Task 2: internal/defaults + internal/firstrun + TestZeroConfig/TestEnsure (renamed seed pair, unchanged WriteTree flow, drift/leak guards).
- Task 3: repo-wide rename grep gate (tracked files, .planning excluded), full go test ./..., and mise run ci as the final guard per repo convention (env-gated E2E skips without their env vars).
</verification>

<success_criteria>
- A fresh project run seeds .ass-guard/{.gitignore, profiles/zcode, openspec.toml, config.yaml} via the unchanged WriteTree flow; no legacy-named file anywhere.
- Embedded default -> global (~/.config/ass-guard-agent/config.yaml) -> project (.ass-guard/config.yaml) layering proven by tests; project wins conflicts.
- The old filename is provably never read and referenced nowhere in tracked files outside .planning.
- All three tasks' automated gates green; mise run ci green; git grep gate clean.
</success_criteria>

<output>
Create .planning/quick/260817-11v-relocate-scheduling-config-ass-guard-sch/260817-11v-SUMMARY.md when done
</output>
