---
quick_id: 260817-uv3
slug: archive-the-tmp-zcode-recapture-driver-k
date: 2026-08-17
status: complete
---

# Quick Task 260817-uv3: Archive the /tmp/zcode-recapture driver kit

## Task

Copy the `/tmp/zcode-recapture/` driver kit (17 files) into the repo at
`tools/zcode-recapture/` with a provenance README, before /tmp reaping destroys it.
Ordered by the accepted Phase-9 UAT disposition and Phase-12 discuss decision D-04.

## Tasks

1. **Copy + document** — `cp -p /tmp/zcode-recapture/* tools/zcode-recapture/`; write
   `tools/zcode-recapture/README.md` (contents table, provenance, re-run pointer to
   `docs/recapture-runbook.md`, secrets discipline).
   - files: `tools/zcode-recapture/*`
   - action: copy + README
   - verify: 17 files present + README; secrets scan clean (redaction markers present,
     no literal credentials, driver reads keys from env)
   - done: files committed

## Deviations / notes

- Executed in-process by the manager session (no planner/executor agents spawned) per
  the house precedent for this runtime; mechanical copy, zero source code.
- The pinned rollout file `model-io-sess_3cee56ae….jsonl` could NOT be archived — it
  has rotated off all live locations (recorded in the README and 12-CONTEXT.md D-04).
- Committed directly on master (docs/tooling-only, matching this session's doc-commit
  pattern); the quick workflow's origin-forked branch step was skipped because
  origin/master is 380 commits behind local master.
