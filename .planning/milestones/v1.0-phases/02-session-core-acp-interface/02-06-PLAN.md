---
phase: 02-session-core-acp-interface
plan: 06
type: execute
wave: 4
depends_on: ["02", "03", "05"]
files_modified:
  - internal/session/subagent.go
  - internal/session/subagent_test.go
  - internal/session/session.go
  - internal/session/transcript.go
requirements: [PARA-01, PARA-02, PARA-03]
autonomous: true   # subagent dispatch + recovery unit-testable with fakes; no live network
---

# Plan 02-06 — Subagent dispatch: isolated goroutine turn-loops, streamed progress, panic recovery

## Truths

- Implements D-10 (RestrictedExecutor from 02-03 wired into the subagent turn loop — model sees full catalog, executor enforces subset), D-11 (streamed intermediate progress: tool calls + partial outputs flow to the bus tagged with parent-turn-id; the parent's lean window gets ONLY the final SubagentResult — RESEARCH §10.3 event-vs-window boundary), D-13 (goroutine-boundary recover → structured tool-error; process NEVER crashes), PARA-01 (Task/Agent dispatches isolated goroutine turn-loops with scoped context + restricted tools), PARA-02 (results via bus tagged parent-turn-id; only results to parent window), PARA-03 (panic recovered → tool-error, never crash).
- The subagent is a NESTED Session Core turn loop: own transcript lines (subagentTurnID + parentTurnID), own lean window (projected from its sub-transcript), SAME provider (via the 02-04 semaphore, D-12), RestrictedExecutor (02-03) wrapping the stub tool executor.
- Tool execution stays STUBBED (Phase-1 D-15) — subagent tool calls produce stub tool_results. Real execution is Phase 4. The dispatch skeleton + recover + streamed-progress are real.

<objective>
Add the Task/Agent subagent dispatch to the Session Core: when the model invokes Task (or Agent), spawn an isolated goroutine running a nested turn loop with a restricted tool subset (D-10). The subagent streams tool calls + partial outputs to the bus tagged with parent-turn-id (PARA-02 — forwarded to Zed for visibility); on completion it publishes a SubagentResult with only the final result (the parent's lean window receives only this — D-11). A panicking subagent is recovered at the goroutine boundary (D-13) → the parent gets a structured tool-error; the process NEVER crashes.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/02-session-core-acp-interface/02-CONTEXT.md
@.planning/phases/02-session-core-acp-interface/02-RESEARCH.md
@.planning/phases/02-session-core-acp-interface/02-PATTERNS.md

<interfaces>
<!-- Plan 02-02 Session Core (turn loop being extended with Task tool handling) -->
<!-- Plan 02-03 RestrictedExecutor (the wrapper the subagent uses) -->
<!-- Plan 02-05 Session.Prompt with Stream + boundary + semaphore (the parent turn loop) -->
</interfaces>
</context>

<must_haves>
  truths:
    - "Task/Agent invocation spawns an isolated goroutine turn-loop with a restricted tool subset (PARA-01)"
    - "Streamed progress (tool calls, chunks) is published with parent-turn-id (PARA-02)"
    - "The parent's lean window receives ONLY the final SubagentResult — intermediates excluded (D-11)"
    - "A panicking subagent is recovered → tool-error to parent; process NEVER crashes (PARA-03, D-13)"
    - "The subagent acquires the provider semaphore before each provider call (D-12)"
    - "Every panic produces an investigate-and-fix-ready error transcript line (PROJECT.md must-have)"
  artifacts:
    - path: "internal/session/subagent.go"
      provides: "DispatchSubagent + runSubagentLoop + recoverSubagent"
      min_lines: 100
    - path: "internal/session/subagent_test.go"
      provides: "PARA-01/02/03 coverage with -race"
      min_lines: 80
  key_links:
    - from: "internal/session/session.go turn loop (Task tool)"
      to: "internal/session/subagent.go DispatchSubagent"
      via: "when tool name is Task/Agent, spawn the subagent goroutine"
      pattern: "DispatchSubagent"
    - from: "internal/session/subagent.go goroutine"
      to: "internal/event/bus.go Publish SubagentResult"
      via: "on completion or panic, publish SubagentResult with parent-turn-id"
      pattern: "bus\\.Publish|SubagentResult"
</must_haves>

<tasks>

<task type="tdd" id="T1">
  <name>Task 1: Subagent dispatch + nested turn loop + restricted executor + streamed progress (PARA-01, PARA-02, D-11)</name>
  <files>internal/session/subagent.go, internal/session/subagent_test.go, internal/session/session.go, internal/session/transcript.go</files>
  <behavior>
    - Test 1 (dispatch PARA-01): a Task tool_call triggers DispatchSubagent; a `subagent_dispatch` transcript line (parentTurnID, subagentTurnID, restrictedTools) is appended; a goroutine runs the nested loop.
    - Test 2 (restricted tools D-10): the subagent uses a RestrictedExecutor; a disallowed tool gets a "not available" error.
    - Test 3 (streamed progress PARA-02): the subagent's tool calls + chunks are published to the bus with ParentTurnID set; a test subscriber receives them.
    - Test 4 (event-vs-window D-11): the parent's projector reads ONLY parent-turnID lines + the final subagent_result tool_result; subagent intermediates (subagentTurnID) do NOT appear in the parent's lean window.
    - Test 5 (final result PARA-02): on completion, a SubagentResult event + a `subagent_result` line + the parent's tool_result for the Task call are produced; the parent continues.
    - Test 6 (semaphore D-12): the subagent acquires the semaphore before each provider call (pre-fill test blocks until Release).
    - Test 7 (cancel propagation D-16): cancelling the parent ctx propagates to the subagent's derived ctx; the subagent's provider call aborts; a subagent_result with ctx.Err() is published.
  </behavior>
  <action>
    Build `internal/session/subagent.go` per RESEARCH §10. Define `func (s *Session) DispatchSubagent(ctx context.Context, parentTurnID, toolCallID, prompt string, restrictedTools []string)`: generate subagentTurnID; append `subagent_dispatch` line; derive child ctx (`ctx, cancel := context.WithCancel(ctx)`); launch `go func() { defer s.recoverSubagent(...); defer cancel(); s.runSubagentLoop(ctx, subagentTurnID, parentTurnID, toolCallID, prompt, restrictedTools) }()`. `runSubagentLoop`: a nested turn loop using the SAME manager (appending subagent-tagged lines), SAME provider (via semaphore — `s.semaphore.Acquire(ctx); defer s.semaphore.Release()` per call), a `RestrictedExecutor` (`toolcat.NewRestrictedExecutor(s.toolExec, restrictedTools)`), and a lean window projected from the subagent's own lines. Each chunk/tool_call published with ParentTurnID + TurnID=subagentTurnID. On completion, publish `SubagentResult{ParentTurnID, ToolCallID, Result}` + append `subagent_result` line + the parent's tool_result. Add `subagent_dispatch` + `subagent_result` line types + Append helpers to `transcript.go`. Update `session.go` turn loop: when a tool_call name is "Task" or "Agent", call `DispatchSubagent` (async — the parent's tool_result arrives via bus) and continue. Write tests FIRST (RED) with fakes, then GREEN. Run with `-race`.
  </action>
  <read_first>
    - .planning/phases/02-session-core-acp-interface/02-RESEARCH.md (§10.1-10.3 dispatch + restriction + event-vs-window; §11 semaphore)
    - .planning/phases/02-session-core-acp-interface/02-CONTEXT.md (D-10, D-11, D-13)
    - .planning/phases/02-session-core-acp-interface/02-PATTERNS.md (C5 goroutine-boundary recover)
    - internal/session/session.go (from 02-02/02-05 — being extended)
    - internal/toolcat/restricted.go (from 02-03 — being wired)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/session/ -race -v -run 'TestDispatchSubagent|TestRestrictedToolsInSubagent|TestStreamedProgress|TestEventVsWindowBoundary|TestSubagentFinalResult|TestSubagentSemaphore|TestSubagentCancel'</verify>
  <done>
    - Task/Agent triggers DispatchSubagent + subagent_dispatch transcript line
    - RestrictedExecutor wired (D-10); semaphore acquired (D-12)
    - Streamed progress published with parent-turn-id (PARA-02)
    - Parent lean window excludes intermediates (D-11)
    - Final SubagentResult + subagent_result + parent tool_result produced
    - Parent cancel propagates to subagent (D-16)
  </done>
  <acceptance_criteria>
    - `go test ./internal/session/ -race` passes
    - PARA-01/02 + D-10/D-11 verified
  </acceptance_criteria>
</task>

<task type="tdd" id="T2">
  <name>Task 2: Panic recovery at goroutine boundary (PARA-03, D-13) — investigate-and-fix-ready</name>
  <files>internal/session/subagent.go, internal/session/subagent_test.go</files>
  <behavior>
    - Test 1 (panic → tool-error PARA-03): a panicking subagent (fake provider panics) is recovered → parent receives a SubagentResult with non-nil Error (tool-error for the Task call); process does NOT crash.
    - Test 2 (error transcript line): the panic produces an `error` transcript line with component="subagent", scrubbed message (redact.ScrubError), recovered value, recoverable=false, and stack (debug.Stack()).
    - Test 3 (parent continues): after the panic, the parent turn loop continues (no hang/crash).
    - Test 4 (parent panic recovery — Pitfall 7): a parent-turn panic is recovered by Prompt's defer → `error` line + error return (no crash).
    - Test 5 (ACP reader recover — Pitfall 7): the ACP reader goroutine recovers from panics on individual frame dispatch + keeps reading (verify the deferred recover exists from 02-01/02-05; add if missing).
  </behavior>
  <action>
    Implement `recoverSubagent(parentTurnID, toolCallID, subagentTurnID string)` (deferred in the subagent goroutine) per RESEARCH §10.4: `if r := recover(); r != nil { stack := debug.Stack(); err := fmt.Errorf("subagent panic: %v", r); s.bus.Publish(event.SubagentResult{ParentTurnID: parentTurnID, ToolCallID: toolCallID, Result: "", Error: err}); s.manager.AppendError(subagentTurnID, "subagent", redact.ScrubError(err), nil, false, string(stack)); s.manager.AppendSubagentResult(parentTurnID, subagentTurnID, "", err.Error()) }`. Verify the parent's Prompt defer recovers (Pitfall 7 — should exist from 02-02/02-05; harden: append error line with parent stack on parent panic). Verify the ACP reader has a per-dispatch recover (from 02-01; add `defer func(){ if r := recover(); r != nil { ... log to stderr ... } }()` around dispatch if missing). Write tests FIRST (RED), verify GREEN.
  </action>
  <read_first>
    - .planning/phases/02-session-core-acp-interface/02-RESEARCH.md (§10.4 recoverSubagent; §13 Pitfall 7)
    - .planning/PROJECT.md (investigate-and-fix-ready logging principle)
    - .planning/phases/02-session-core-acp-interface/02-CONTEXT.md (D-13)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/session/ ./internal/acp/ -race -v -run 'TestSubagentPanicRecovery|TestSubagentErrorTranscriptLine|TestParentContinuesAfterSubagentPanic|TestParentPanicRecovery|TestACPReaderRecover'</verify>
  <done>
    - Panicking subagent recovered → tool-error to parent (PARA-03); no crash
    - Panic → error transcript line with stack + scrubbed message (investigate-and-fix-ready)
    - Parent continues after subagent panic
    - Parent + ACP reader panics recovered (Pitfall 7)
  </done>
  <acceptance_criteria>
    - `go test ./internal/session/ ./internal/acp/ -race` passes
    - PARA-03 + D-13 + investigate-and-fix-ready verified
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| parent turn ↔ subagent goroutine | Subagent runs isolated with derived ctx; panic must not crash parent |
| subagent ↔ tool executor | RestrictedExecutor enforces the subset (D-10) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | DoS | subagent panic crashes process | mitigate | goroutine-boundary recover → tool-error + error transcript line (Task T2; D-13) |
| T-02-17 | Elevation of privilege | subagent executes restricted tool | mitigate | RestrictedExecutor enforces allow-list (D-10; Task T1 Test 2) |
| T-02-18 | Information disclosure | subagent error leaks secret | mitigate | error scrubbed via redact.ScrubError before AppendError (Task T2) |
| T-02-19 | DoS | subagent fan-out explosion | mitigate | combined provider semaphore (D-12; Task T1 Test 6) |
</threat_model>

<verification>
- `go test ./internal/session/ ./internal/acp/ -race -v`
- `go build ./...` && `go vet ./...`
</verification>

<success_criteria>
- Task/Agent dispatches isolated goroutine turn-loop with restricted tools (PARA-01)
- Streamed progress + only-final-result-to-parent (PARA-02, D-11)
- Panic recovered → tool-error, never crash (PARA-03, D-13)
- Every panic → investigate-and-fix-ready error line (PROJECT.md)
</success_criteria>

<output>
Create `.planning/phases/02-session-core-acp-interface/02-06-SUMMARY.md` when done
</output>
