---
phase: 02-session-core-acp-interface
plan: 07
type: execute
wave: 5
depends_on: ["01", "02", "03", "04", "05", "06"]
files_modified:
  - internal/session/reconstruction_test.go
  - internal/session/end_to_end_test.go
  - internal/audit/transcript_writer.go
  - internal/audit/transcript_writer_test.go
  - internal/audit/audit.go
requirements: [LOG-04, LOG-02]
autonomous: true   # scripted session against fakes; no live network
---

# Plan 02-07 — Reconstruction-sufficiency test (LOG-04) + transcript-writer audit fold + end-to-end session gate

## Truths

- Implements D-20 (audit log = transcript, ONE artifact — the AuditLogger is folded into the TranscriptWriter; one writer, one schema, no two-writer drift), LOG-04 (rotation = per-session files; reconstruction-sufficiency test verifies the transcript + profile reconstruct what happened), LOG-02 (the transcript writer is an async bus consumer, verified end-to-end here).
- This is the phase GATE plan: the reconstruction test must pass before `/gsd:verify-work`. It proves the transcript is sufficient for human investigation (the transcript's primary purpose per D-03/D-09).

<objective>
Fold the Phase-1 AuditLogger into the unified TranscriptWriter (D-20 — one artifact), then add the reconstruction-sufficiency test (LOG-04) and an end-to-end session test exercising the full Phase-2 stack (ACP → Session Core → streaming → subagent → cancel → logout). The reconstruction test verifies the transcript + profile answer every investigation question. The end-to-end test is the integration gate proving the full stack works together with secrets redacted and no events dropped.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/02-session-core-acp-interface/02-CONTEXT.md
@.planning/phases/02-session-core-acp-interface/02-RESEARCH.md
@.planning/phases/02-session-core-acp-interface/02-PATTERNS.md

<interfaces>
<!-- Plan 02-02 Manager (sole transcript writer) -->
<!-- Plan 02-04 expanded bus (all 7 event kinds) -->
<!-- Plan 02-06 subagent dispatch (exercised end-to-end) -->
<!-- Phase-1 internal/audit AuditLogger (being folded) -->
</interfaces>
</context>

<must_haves>
  truths:
    - "The Phase-1 AuditLogger is folded into the TranscriptWriter (D-20 — one artifact, one writer)"
    - "A reconstruction-sufficiency test (LOG-04) proves the transcript + profile answer every investigation question for a scripted session"
    - "An end-to-end session test exercises ACP → Session Core → streaming → subagent → cancel → logout and asserts the transcript captures the full sequence redacted"
    - "The transcript writer is async (LOG-02) — never blocks the turn loop (verified end-to-end)"
  artifacts:
    - path: "internal/session/reconstruction_test.go"
      provides: "TestTranscriptReconstructsSession — 8 investigation questions (RESEARCH §9)"
      min_lines: 100
    - path: "internal/session/end_to_end_test.go"
      provides: "TestEndToEndSession — full stack round-trip + transcript assertions"
      min_lines: 80
    - path: "internal/audit/transcript_writer.go"
      provides: "TranscriptWriter — folds AuditLogger; subscribes all kinds; writes via Manager"
      min_lines: 60
  key_links:
    - from: "internal/audit/transcript_writer.go"
      to: "internal/session/manager.go Append*"
      via: "each event-kind handler calls the matching Manager.Append*"
      pattern: "manager\\.Append"
</must_haves>

<tasks>

<task type="execute" id="T1">
  <name>Task 1: Fold Phase-1 AuditLogger into TranscriptWriter (D-20 — one artifact)</name>
  <files>internal/audit/transcript_writer.go, internal/audit/transcript_writer_test.go, internal/audit/audit.go</files>
  <action>
    Build `internal/audit/transcript_writer.go` per PATTERNS.md. Define `type TranscriptWriter struct { manager *session.Manager; bus *event.Bus }` and `func NewTranscriptWriter(manager *session.Manager, bus *event.Bus) *TranscriptWriter` that subscribes to all 7 event kinds (RequestShaped, AgentMessageChunk, ToolCall, ToolCallUpdate, UsageUpdate, SubagentResult, Boundary) — each handler calls the matching `manager.Append*`. The writer runs as `Run(ctx)` goroutine select-looping on the subscribed channels (from the 02-04 expanded bus). The Manager already redacts per-line (02-02) — the writer does NOT re-redact. Remove the Phase-1 `internal/audit/audit.go` AuditLogger (D-20 — one artifact; a shim risks two-writer drift). Migrate the Phase-1 AuditLogger tests to `transcript_writer_test.go` asserting the writer forwards events to the Manager (Manager redaction tested in 02-02). The Phase-1 RequestShaped redaction test (auth value [REDACTED], header names preserved) now passes through writer → Manager. No separate audit-log file — the transcript IS the audit log.
  </action>
  <read_first>
    - .planning/phases/02-session-core-acp-interface/02-RESEARCH.md (§2.4 async contract; D-20)
    - .planning/phases/02-session-core-acp-interface/02-PATTERNS.md (Pattern Assignment for transcript_writer.go)
    - .planning/phases/01-mimicry-mvp-north-star-proof/01-05-PLAN.md (T3 the Phase-1 AuditLogger being folded)
    - internal/audit/audit.go (Phase-1 AuditLogger — being removed)
    - internal/session/manager.go (from 02-02 — Append* methods being called)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/audit/ -race -v -run TestTranscriptWriter && go build ./internal/audit/ ./internal/session/</verify>
  <done>
    - TranscriptWriter subscribes to all 7 event kinds, forwards to Manager.Append*
    - Phase-1 AuditLogger removed (no two-writer drift)
    - Writer is async (goroutine; never blocks producers)
    - No separate audit-log file — transcript is the one artifact (D-20)
  </done>
  <acceptance_criteria>
    - `go test ./internal/audit/ -race` passes
    - D-20 one-artifact verified
  </acceptance_criteria>
</task>

<task type="tdd" id="T2">
  <name>Task 2: Reconstruction-sufficiency test (LOG-04) — the phase gate</name>
  <files>internal/session/reconstruction_test.go</files>
  <behavior>
    - Test (LOG-04): run a scripted session — session/new + 3 prompts (one triggering a Bash boundary, one dispatching a Task subagent) + session/cancel + logout — then read the transcript + profile and assert ALL 8 investigation questions from RESEARCH §9:
      (a) user prompts (user_message lines in order with content)
      (b) model output (assistant_message + agent_message_chunk lines in order)
      (c) tools called with args (tool_call lines with name + input)
      (d) tool results (tool_result lines with output + isError)
      (e) boundaries (boundary lines with cause="mutating-command:Bash" + commandRef)
      (f) verbatim shaped requests (request_shaped lines present + redacted — auth value [REDACTED], header names preserved)
      (g) errors (error lines with component + recoverable, if any injected)
      (h) cancel state (canceled line with turnID + timestamp)
    - Assert every line type that occurred is well-formed (parseable JSON).
    - Assert NO secret values leak (grep file for `sk-` / `Bearer ` = 0 matches).
    - Assert subagent dispatch + result captured (subagent_dispatch + subagent_result with parentTurnID).
  </behavior>
  <action>
    Build `internal/session/reconstruction_test.go` per RESEARCH §9. Construct a real Session (fakes: mock streaming Provider, stub tool executor, fake profile) + TranscriptWriter + expanded bus; run the scripted session (multiple session.Prompt calls; inject a boundary via a Bash tool_call; inject a Task subagent via DispatchSubagent; cancel one turn; call logout); read the transcript from `t.TempDir()` `.ass-guard/`; assert each investigation question. This is the phase-gate test — must pass before `/gsd:verify-work`. Write the test FIRST (RED until the full 02-01..06 stack is in place), then verify GREEN (the stack is in place since this plan depends on 01-06).
  </action>
  <read_first>
    - .planning/phases/02-session-core-acp-interface/02-RESEARCH.md (§9 reconstruction test + 8 questions)
    - .planning/phases/02-session-core-acp-interface/02-CONTEXT.md (D-20, D-03)
    - internal/session/session.go + manager.go + subagent.go (from 02-02/05/06 — stack being verified)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/session/ -race -v -run TestTranscriptReconstructsSession</verify>
  <done>
    - TestTranscriptReconstructsSession asserts all 8 investigation questions (a-h)
    - No secret values leak (grep sk-/Bearer = 0)
    - Covers boundary + subagent + cancel + error line types
    - Test passes (LOG-04 gate green)
  </done>
  <acceptance_criteria>
    - `go test ./internal/session/ -race -run TestTranscriptReconstructsSession` passes (LOG-04 gate)
  </acceptance_criteria>
</task>

<task type="execute" id="T3">
  <name>Task 3: End-to-end session test (full stack gate)</name>
  <files>internal/session/end_to_end_test.go</files>
  <action>
    Build `internal/session/end_to_end_test.go` with `TestEndToEndSession`: an in-process mock ACP client (02-01 io.Pipe harness) drives the full server through initialize → session/new → session/prompt (mock streaming provider: 3 chunks + a Task tool_call triggering a subagent) → observe session/update notifications (agent_message_chunk + tool_call from parent AND subagent, parent-turn-id tagged) → session/cancel (mid-subagent) → logout. Assert: notifications arrived in order; session/prompt response stopReason "cancelled"; transcript (temp dir) contains the full sequence (session_start, user_message, request_shaped, agent_message_chunk, tool_call, subagent_dispatch, subagent_result, canceled, session_end); stdout clean (only ACP frames); TranscriptWriter kept up (no events dropped). Run with `-race`.
  </action>
  <read_first>
    - .planning/phases/02-session-core-acp-interface/02-RESEARCH.md (§8.3 end-to-end streaming; §9; §13 Pitfalls)
    - internal/acp/server_test.go (from 02-01 — io.Pipe harness to reuse)
    - internal/session/session.go + subagent.go (from 02-02/05/06)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/session/ -race -v -run TestEndToEndSession</verify>
  <done>
    - TestEndToEndSession exercises initialize → session/new → session/prompt → subagent → cancel → logout
    - Notifications in order; stopReason "cancelled" after cancel
    - Transcript captures full sequence; stdout clean; no events dropped
    - Full suite green with -race (phase gate)
  </done>
  <acceptance_criteria>
    - `go test ./... -race` passes (phase gate green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| full stack ↔ transcript | Reconstruction verifies the transcript captures everything that crossed every boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Information disclosure | transcript misses a secret-bearing line | mitigate | reconstruction greps for sk-/Bearer = 0 across the full session (Task T2) |
| T-02-21 | Repudiation | transcript incomplete | mitigate | reconstruction asserts all 8 questions + every line type (LOG-04 gate, Task T2) |
</threat_model>

<verification>
- `go test ./... -race` — full suite green (phase gate)
- `go build ./...` && `go vet ./...`
</verification>

<success_criteria>
- AuditLogger folded into TranscriptWriter (D-20 one artifact)
- Reconstruction test passes (LOG-04)
- End-to-end test passes (full stack gate)
- Full suite green with -race
</success_criteria>

<output>
Create `.planning/phases/02-session-core-acp-interface/02-07-SUMMARY.md` when done
</output>
