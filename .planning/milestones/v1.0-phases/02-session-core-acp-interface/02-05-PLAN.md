---
phase: 02-session-core-acp-interface
plan: 05
type: execute
wave: 3
depends_on: ["01", "02", "03", "04"]
files_modified:
  - internal/acp/server.go
  - internal/acp/server_test.go
  - internal/acp/handlers.go
  - internal/acp/handlers_test.go
  - internal/session/session.go
  - internal/session/session_test.go
  - internal/session/boundary.go
  - internal/session/boundary_test.go
requirements: [ACP-03, ACP-04, PARA-04]
autonomous: true   # in-process mock ACP client + mock streaming provider; live Zed is operator-gated
---

# Plan 02-05 — Integration: real Session Core in ACP + streaming wired + session/load no-op (D-09) + cancel end-to-end (D-16) + boundaries

## Truths

- Implements D-09 (session/load is NO-OP — `loadSession: false` advertised; method responds -32601; NO replay), D-16 (`session/cancel` = context cancellation + drain queued events + "canceled" boundary marker — end-to-end from ACP notification to turn abort), D-18 step 4 streaming wired (the turn loop calls Provider.Stream from 02-04, reads chunks, emits to the bus, ACP adapter forwards to session/update — NO full-turn buffering), SESS-02/03 boundary integration (mutating tool calls trigger boundary lines via the 02-03 `toolcat.IsBoundary` formula, resetting the lean window).
- Carries ACP-03 (DROPPED — the session/load no-op/error implementation, explicit per D-09), ACP-04 (end-to-end streaming now real, replacing the 02-01 stub), PARA-04 (the semaphore from 02-04 is wired into the turn loop).
- This is the INTEGRATION plan: swaps the 02-01 stub turn loop for the real 02-02 Session Core, wires the 02-03 mutability boundary into the turn loop, consumes the 02-04 Stream + semaphore + expanded bus. After this plan, the ACP server is feature-complete for single-agent (non-subagent) turns. Subagent dispatch is Plan 02-06.

<objective>
Wire the real Session Core (02-02), the streaming provider + semaphore + expanded bus (02-04), and the mutability boundary (02-03) into the ACP server (02-01). The stub turn loop from 02-01 is replaced; the ACP server now drives a real Session.Prompt that streams token-by-token. session/load is a no-op/error per D-09 (NO replay). session/cancel cancels the active turn end-to-end. Mutating tool calls write boundary lines and reset the lean window.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/02-session-core-acp-interface/02-CONTEXT.md
@.planning/phases/02-session-core-acp-interface/02-RESEARCH.md
@.planning/phases/02-session-core-acp-interface/02-PATTERNS.md

<interfaces>
<!-- Plan 02-01 ACP server (being extended) -->
<!-- Plan 02-02 Session Core (being wired in) -->
<!-- Plan 02-03 toolcat.IsBoundary (being called from the turn loop) -->
<!-- Plan 02-04 Provider.Stream + Semaphore + expanded Bus (being consumed) -->
</interfaces>
</context>

<must_haves>
  truths:
    - "The ACP server's session/prompt handler drives the real Session.Prompt, streaming session/update notifications token-by-token with NO full-turn buffering"
    - "session/load responds with a -32601 method-not-supported error (loadSession:false advertised); NO replay is implemented (D-09)"
    - "session/cancel cancels the active turn end-to-end: Provider.Stream aborts, queued bus events drain, a canceled boundary marker is written, the session/prompt request gets stopReason 'cancelled'"
    - "Mutating tool calls trigger boundary lines (via toolcat.IsBoundary from 02-03); the next projection resets the lean window"
    - "Config can ADD boundaries (e.g. WebFetch) but cannot flip a mutating tool to read-only (SESS-02 structural floor, verified in 02-03)"
    - "The turn loop acquires the provider semaphore before Provider.Stream (D-12)"
  artifacts:
    - path: "internal/session/boundary.go"
      provides: "MaybeAppendBoundary — calls toolcat.IsBoundary + Manager.AppendBoundary conditionally"
      min_lines: 30
    - path: "internal/acp/handlers.go"
      provides: "session/prompt drives real Session.Prompt; session/load no-op; session/cancel cancels active turn"
      min_lines: 80
  key_links:
    - from: "internal/acp/handlers.go session/prompt"
      to: "internal/session/session.go Prompt"
      via: "spawns go s.runTurn which calls session.Prompt"
      pattern: "session\\.Prompt"
    - from: "internal/session/session.go turn loop"
      to: "internal/provider/streaming.go Stream"
      via: "calls provider.Stream, reads chunks, emits to bus"
      pattern: "provider\\.Stream"
    - from: "internal/acp/handlers.go session/cancel"
      to: "active turn context.CancelFunc"
      via: "looks up + calls the session's active turn cancel func"
      pattern: "cancelFunc|Cancel\\(\\)"
</must_haves>

<tasks>

<task type="tdd" id="T1">
  <name>Task 1: Boundary integration in the turn loop (SESS-02/03/04/05) + Stream wiring</name>
  <files>internal/session/session.go, internal/session/session_test.go, internal/session/boundary.go, internal/session/boundary_test.go</files>
  <behavior>
    - Test 1 (Stream wired D-18 step 4): the turn loop calls `provider.Stream` (not Send), reads chunks from the returned channel, emits AgentMessageChunk/ToolCall/UsageUpdate to the bus per chunk, and assembles the assistant_message on stream end.
    - Test 2 (boundary on mutating tool SESS-02/03): when the model invokes Bash (catalog Mutating), after the stub tool_result the turn loop calls MaybeAppendBoundary → a `boundary` line with cause="mutating-command:Bash" + commandRef is appended; the NEXT projection's ReadLastBoundary finds it and resets the lean window (zero carry-forward).
    - Test 3 (config adds boundary SESS-02): with configAdded=["WebFetch"], a WebFetch tool_call (catalog ReadOnly) triggers a boundary (config-added); IsBoundary returns true.
    - Test 4 (config cannot downgrade SESS-02): a catalog-mutating tool (Bash) with an empty configAdded still triggers a boundary (structural floor).
    - Test 5 (read-only tool no boundary): a Read tool_call does NOT trigger a boundary (no boundary line appended).
    - Test 6 (semaphore wired D-12): the turn loop Acquires the semaphore before Stream + Releases after; a test that pre-fills the semaphore blocks the turn until Release.
    - Test 7 (cancel mid-stream D-16): cancelling ctx mid-Stream closes the chunk channel, aborts the HTTP request, a `canceled` line is appended, and the turn returns stopReason "cancelled".
  </behavior>
  <action>
    Build `internal/session/boundary.go`: `func (s *Session) MaybeAppendBoundary(toolName, toolCallID, turnID string) error` — calls `toolcat.IsBoundary(toolName, s.catalog, s.configAdded)`; if true, calls `s.manager.AppendBoundary("mutating-command:"+toolName, toolCallID, turnID)` (or "config-added:"+toolName for config-added boundaries). Update `internal/session/session.go` turn loop (from 02-02): replace the `provider.Send` call with `provider.Stream` — read chunks from the returned `<-chan StreamChunk`, emit `event.AgentMessageChunk{Content: c.Text, MessageID: ..., TurnID: turnID}` / `event.ToolCall{...}` / `event.UsageUpdate{...}` to the bus per chunk (RESEARCH §2.2 payload shapes), assemble the assistant_message. Wrap the Stream call in `s.semaphore.Acquire(ctx); defer s.semaphore.Release()`. After each tool_call's stub tool_result, call `s.MaybeAppendBoundary(toolName, toolCallID, turnID)`. On ctx cancellation mid-stream, append a `canceled` line (`s.manager.AppendCanceled(turnID, time.Now(), "context cancelled")`) and return ("cancelled", nil). Write tests FIRST (RED), then GREEN.
  </action>
  <read_first>
    - .planning/phases/02-session-core-acp-interface/02-RESEARCH.md (§6 boundary write timing — AFTER tool_result; D-18 step 4 streaming; D-16 cancel mechanism; §11 semaphore use)
    - .planning/phases/02-session-core-acp-interface/02-CONTEXT.md (D-09, D-16, D-18, SESS-02/03)
    - internal/session/session.go + manager.go (from 02-02 — being extended)
    - internal/toolcat/mutability.go (from 02-03 — IsBoundary being called)
    - internal/provider/streaming.go + semaphore.go (from 02-04 — being consumed)
    - internal/event/bus.go (from 02-04 — Publish being called)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/session/ -race -v -run 'TestStreamWired|TestBoundaryOnMutatingTool|TestConfigAddsBoundary|TestConfigCannotDowngrade|TestReadOnlyNoBoundary|TestSemaphoreWired|TestCancelMidStream'</verify>
  <done>
    - Turn loop calls provider.Stream + emits chunks to the bus (D-18 step 4)
    - Mutating tool calls trigger boundary lines; next projection resets lean window
    - Config adds boundaries; cannot downgrade (SESS-02 floor)
    - Semaphore acquired around Stream (D-12)
    - Cancel mid-stream → canceled line + stopReason "cancelled" (D-16)
  </done>
  <acceptance_criteria>
    - `go test ./internal/session/ -race` passes
    - Boundary integration verified for mutating/config-added/read-only cases
  </acceptance_criteria>
</task>

<task type="tdd" id="T2">
  <name>Task 2: ACP integration — real Session.Prompt + session/load no-op (D-09) + session/cancel end-to-end (D-16)</name>
  <files>internal/acp/server.go, internal/acp/server_test.go, internal/acp/handlers.go, internal/acp/handlers_test.go</files>
  <behavior>
    - Test 1 (real streaming ACP-04): in-process mock client sends initialize → session/new → session/prompt; server constructs a real Session (mock streaming Provider emitting 3 chunks); client observes 3 `agent_message_chunk` session/update notifications (streamed) before the response with stopReason "end_turn".
    - Test 2 (session/load no-op D-09): session/load request gets a -32601 error response; NO replay notifications; initialize advertised loadSession:false.
    - Test 3 (session/cancel end-to-end D-16): client sends session/prompt (streaming starts), then session/cancel (notification); turn aborts, a `canceled` line in transcript, session/prompt gets stopReason "cancelled", no stale session/update after cancel.
    - Test 4 (transport discipline regression): stdout is still clean (only ACP frames) after wiring the real Session Core.
    - Test 5 (active-turn cancel map): the server stores active turn cancel funcs in a map[sessionId]context.CancelFunc; session/cancel looks up + calls it; concurrent session/news don't race (`-race`).
  </behavior>
  <action>
    Update `internal/acp/handlers.go`: session/prompt handler constructs/looks-up the real `*session.Session` for the sessionId, stores the turn's `context.CancelFunc` in `s.activeCancels[sessionId]` (a `map[string]context.CancelFunc` guarded by `sync.Mutex`), calls `session.Prompt(ctx, prompt)` in a goroutine (the ACP adapter from 02-01 forwards bus events to session/update), writes the stopReason response when Prompt returns, and deletes the cancel func from the map on turn end. session/load handler: respond `RPCError{Code: -32601, Message: "session/load not supported (loadSession is false)"}` — NO replay (D-09). session/cancel handler (notification, no id): look up `s.activeCancels[sessionId]`, call the CancelFunc (the turn aborts → Session.Prompt returns "cancelled"). Update the ACP adapter (02-01) to subscribe to the EXPANDED bus (02-04) kinds: AgentMessageChunk/ToolCall/ToolCallUpdate/UsageUpdate → map to session/update notifications (RESEARCH §7.4 discriminator mapping). Update tests; keep the 02-01 io.Pipe mock-client harness. Write new-behavior tests FIRST (RED), then GREEN.
  </action>
  <read_first>
    - .planning/phases/02-session-core-acp-interface/02-RESEARCH.md (§5 session/load no-op; §7.4 session/update discriminator mapping; §8 the server skeleton; D-16 cancel)
    - .planning/phases/02-session-core-acp-interface/02-CONTEXT.md (D-09 NO REPLAY; D-16 cancel)
    - internal/acp/server.go + handlers.go + adapter.go (from 02-01 — stub being replaced)
    - internal/session/session.go (from 02-02/02-05 T1 — being wired in)
    - internal/event/bus.go (from 02-04 — expanded kinds being subscribed)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/acp/ -race -v -run 'TestRealStreaming|TestSessionLoadNoOp|TestSessionCancelEndToEnd|TestStdoutCleanRegression|TestActiveCancelMap'</verify>
  <done>
    - session/prompt drives real Session.Prompt + streams session/update (ACP-04, no buffering)
    - session/load returns -32601, NO replay (D-09 — ACP-03 DROPPED)
    - session/cancel aborts turn end-to-end, writes canceled line, returns "cancelled" (D-16)
    - Stdout clean (transport discipline regression)
    - Active-cancel map is concurrency-safe (`-race`)
  </done>
  <acceptance_criteria>
    - `go test ./internal/acp/ -race` passes
    - session/load no-op verified (ACP-03 NOT implemented per D-09)
    - End-to-end streaming + cancel work through the in-process mock client
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| session/cancel → active turn | Cancel signal crosses from ACP reader goroutine to turn goroutine via context |
| ACP adapter ↔ bus | The adapter subscribes to bus kinds and writes session/update to stdout |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | DoS | leaky turn goroutine on cancel | mitigate | every select includes ctx.Done(); Stream aborts on cancel; cancel test asserts clean exit (Task T1 Test 7, Task T2 Test 3; Pitfall 3) |
| T-02-14 | Tampering | session/load replay scope creep (D-09) | mitigate | session/load returns -32601; NO replay code path (Task T2 Test 2; Pitfall 6) |
</threat_model>

<verification>
- `go test ./internal/acp/ ./internal/session/ -race -v`
- `go build ./...` && `go vet ./...`
- session/load no-op verified (ACP-03 DROPPED per D-09)
</verification>

<success_criteria>
- Real Session Core streams token-by-token through ACP (ACP-04)
- session/load is no-op (D-09; ACP-03 not implemented)
- session/cancel works end-to-end (D-16)
- Boundaries written on mutating commands, lean window resets (SESS-02/03/04)
</success_criteria>

<output>
Create `.planning/phases/02-session-core-acp-interface/02-05-SUMMARY.md` when done
</output>
