---
phase: 07-multi-provider-config-credentials
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - cmd/ass-guard/acp_serve.go
  - cmd/ass-guard/main.go
  - cmd/ass-guard/parity.go
  - cmd/ass-guard/provider_factory.go
  - cmd/ass-guard/provider_factory_test.go
  - internal/scheduler/factory.go
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PCFG-02, PCFG-03, PCFG-04, PROV-01]
tags: [go, wiring, cli, credentials, docs]
estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # From ROADMAP SC1-SC4 + D-07/D-08 (the operator-visible integration)
    - "`ass-guard acp serve` loads `.ass-guard/scheduling.yaml` (overlaid on the embedded default), builds a scheduler.ProviderFactory once at startup, resolves the heavy tier to its (provider, model) via the scheduler resolver, and the session's provider is the factory-built credentialed instance — the hardcoded makeProvider is gone. (SC2, D-08, PCFG-03)"
    - "An operator declares >=2 providers in `.ass-guard/scheduling.yaml`, at least one carrying a literal api_key; with NO relevant env var exported, the factory builds that provider from the file credential (Source=config) so a Zed-spawned `ass-guard acp serve` authenticates with zero environment. (SC1, D-01, PCFG-02)"
    - "At startup, ass-guard writes to STDERR a one-line warning naming every provider whose credential is unresolvable (no flag, no env, no literal); it does NOT refuse to start as long as >=1 provider is credentialed. (D-07, PCFG-02)"
    - "If `.ass-guard/scheduling.yaml` exists and its mode is looser than 0600, ass-guard warns once on STDERR at startup (credential-on-disk hygiene; the dir is already gitignored). (SC3, PCFG-02)"
    - "Zero-config backward-compat: with no `.ass-guard/scheduling.yaml` changes, no flag, and `$ZAI_API_KEY` set, the factory resolves the anthropic provider's key from $ZAI_API_KEY (Source=env) — identical to today's env-only behavior; the tracer (`main.go`) and `parity` paths build the provider the same way. (SC4, D-06, PCFG-04)"
    - "No credential string reaches stdout (stdout stays ACP-only); all warnings + the 0600 check write to stderr. (SC3 transport discipline, PCFG-02)"
    - "The three former hardcoded provider-construction sites (acp_serve.go makeProvider ~line 257, main.go runTrace ~line 127, parity.go runParity ~line 87) all delegate credential/base_url resolution to the factory; no site hardcodes a bare `provider.NewAnthropicProvider(shaper.New())` without resolved credentials. acp_serve.go (the operator-facing path) keeps zero direct NewAnthropicProvider calls; main.go's LOG-01 tracer may reconstruct the AnthropicProvider to attach its RequestCapturer, but only with base_url+key resolved through the factory (via an Endpoint accessor), never from hardcoded defaults. (D-08)"
  artifacts:
    - "cmd/ass-guard/acp_serve.go — runACPServe loads scheduling.yaml + builds the factory + replaces makeProvider; startup calls factory.WarnUncredentialed(stderr) + a 0600 perm check"
    - "cmd/ass-guard/main.go — runTrace builds the factory (embedded default + optional .ass-guard overlay) and delegates provider construction to it"
    - "cmd/ass-guard/parity.go — runParity builds the factory and delegates provider construction to it"
    - "A shared, testable helper extracted from the above (e.g. loadSchedulingFactory(workDir, apiKeyFlag, stderr) (*scheduler.Config, *scheduler.ProviderFactory, error)) so the wiring is unit-testable without spawning the full ACP server"
    - ".planning/REQUIREMENTS.md — new PCFG-01..04 entries under §Providers + four traceability-matrix rows; PROV-01 status -> Complete (Phase 7)"
    - ".planning/ROADMAP.md — Phase 7 Plans list finalized (07-01, 07-02 with one-line objectives)"
  key_links:
    - "scheduler.Load(.ass-guard/scheduling.yaml) -> NewProviderFactory -> resolver.Resolve('heavy') -> factory.Build(primary.Provider, shaper.New()) -> sessionTurnRunner.makeProvider -> session.Session.Provider"
    - "Embedded default api_key_env: ZAI_API_KEY (from 07-01) -> factory reads $ZAI_API_KEY -> backward-compat preserved (the link SC4 depends on)"
---

<objective>
Wire the Plan 07-01 factory into the three live provider-construction sites (D-08), add the D-07 startup uncredentialed-provider warning + the SC3 0600-permission warning, prove zero-config backward-compat (SC4) and the editor-zero-env case (SC1), and finalize the requirements traceability (PCFG-01..04; PROV-01 -> Complete). This is the integration layer that makes the config+factory slice operator-visible.

Purpose: Turn the credentialed-instance factory into the single provider-construction seam ass-guard actually uses at runtime, so an operator's multi-provider config + file credentials take effect and today's single-provider `$ZAI_API_KEY` flow keeps working unchanged.

Output: Factory-wired `acp serve` / tracer / parity paths, the two startup warnings, the backward-compat + zero-env proofs, and the updated REQUIREMENTS.md + ROADMAP.md.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multi-provider-config-credentials/07-CONTEXT.md

# Plan 07-01 output (the symbols this plan wires in) — read 07-01-SUMMARY.md if it exists
@.planning/phases/07-multi-provider-config-credentials/07-01-PLAN.md

# Source-of-truth code (read before editing)
@cmd/ass-guard/acp_serve.go
@cmd/ass-guard/main.go
@cmd/ass-guard/parity.go
@cmd/ass-guard/scheduling.go
@internal/scheduler/factory.go
@internal/scheduler/load.go
@internal/scheduler/dispatch.go
@internal/scheduler/resolver.go
@internal/provider/anthropic.go
@internal/firstrun/firstrun.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-08 factory wiring at the three construction sites + D-07 startup uncredentialed-provider warning (stderr)</name>
  <files>cmd/ass-guard/acp_serve.go, cmd/ass-guard/main.go, cmd/ass-guard/parity.go, cmd/ass-guard/provider_factory_test.go</files>
  <read_first>
    - .planning/phases/07-multi-provider-config-credentials/07-CONTEXT.md (D-07 warn-at-startup/fail-lazy; D-08 factory replaces the 3 sites)
    - .planning/phases/07-multi-provider-config-credentials/07-01-PLAN.md (the exact factory API: NewProviderFactory, Build, WarnUncredentialed)
    - cmd/ass-guard/acp_serve.go (runACPServe ~line 243-281: where sessionTurnRunner + makeProvider are built; the graceful-degradation log.Printf pattern at ~line 210, 266 to mirror for the startup warn)
    - cmd/ass-guard/main.go (runTrace ~line 94-144: provider.NewAnthropicProvider at ~line 127)
    - cmd/ass-guard/parity.go (runParity ~line 62-98: provider.NewAnthropicProvider at ~line 87)
    - cmd/ass-guard/scheduling.go (how scheduler.Load + scheduler.NewResolver are already used in-process — the resolve subcommand is the pattern to mirror)
    - internal/scheduler/factory.go (NewProviderFactory / Build / WarnUncredentialed signatures from 07-01)
    - internal/scheduler/resolver.go (Resolve(tier, project, now, capReq) -> (primary Target, fallbacks, err); primary.Provider is the name to Build)
  </read_first>
  <behavior>
    - A new testable helper loadSchedulingFactory(workDir, apiKeyFlag, stderr) loads .ass-guard/scheduling.yaml overlaid on the embedded default, builds scheduler.NewProviderFactory, calls factory.WarnUncredentialed(stderr), and returns (cfg, factory, err). On a load error it returns err (caller degrades gracefully).
    - With a 2-provider config where one provider has a literal api_key and the other has none, loadSchedulingFactory writes exactly one stderr warning naming the uncredentialed provider; it returns a non-nil factory and a nil error (does not refuse to build).
    - acp serve's session provider is the factory-built instance for the resolved heavy-tier provider (no direct provider.NewAnthropicProvider call remains in acp_serve.go).
    - Zero-config (embedded default only, $ZAI_API_KEY set) resolves the anthropic provider's key from $ZAI_API_KEY (Source=env).
  </behavior>
  <action>
    RED first. Add a test cmd/ass-guard/provider_factory_test.go (package main) TestLoadSchedulingFactory_WarnsUncredentialed: write a temp .ass-guard/scheduling.yaml declaring two providers (one with a literal api_key, one with api_key_env pointing at an UNSET var), call the helper with a bytes.Buffer stderr, assert the buffer contains a warning line naming the uncredentialed provider AND the returned factory is non-nil AND err is nil. Add TestLoadSchedulingFactory_ZeroConfigEnv: with only the embedded default (no overlay file) and $ZAI_API_KEY set via t.Setenv, assert the factory's Build for the heavy-tier provider resolves Source=env (build the provider, confirm it is NOT the noCredentialProvider — i.e. a real *provider.AnthropicProvider). (To observe Source/credentialed-ness without exporting internals, assert on the concrete type returned: a credentialed anthropic provider is *provider.AnthropicProvider; an uncredentialed one is the scheduler.noCredentialProvider — if that type is unexported, assert the negative via a behavioral Send returning a *provider.ProviderError KindStructural.)

    Implement in cmd/ass-guard (a new small helper, place it in acp_serve.go or a new cmd/ass-guard/provider_factory.go — prefer a new file to keep acp_serve.go focused): `func loadSchedulingFactory(workDir, apiKeyFlag string, stderr io.Writer) (*scheduler.Config, *scheduler.ProviderFactory, error)` that (1) resolves the overlay path filepath.Join(workDir, ".ass-guard", "scheduling.yaml"), (2) if that file exists calls scheduler.Load(path) else scheduler.Load() (embedded default only), (3) on error returns err, (4) builds factory := scheduler.NewProviderFactory(cfg, apiKeyFlag, slog.Default()), (5) calls factory.WarnUncredentialed(stderr), (6) returns (cfg, factory, nil). Honor transport discipline: WarnUncredentialed writes to the stderr writer only.

    Wire acp_serve.go runACPServe: replace the `makeProvider: func() provider.Provider { return provider.NewAnthropicProvider(shaper.New()) }` block (~line 257) — call loadSchedulingFactory(opts.WorkDir, "", os.Stderr) early in runACPServe (after profile load); on error log.Printf a graceful-degradation message (mirror line 266) and fall back to scheduler.Load()+factory from the embedded default. Resolve the heavy-tier provider name via primary, _, err := scheduler.NewResolver(cfg).Resolve(tierHeavy, "", time.Now(), scheduler.CapabilityReq{}) (tierHeavy constant exists); on resolve error default to the first declared provider in cfg.Providers (sorted). Set makeProvider to a closure that returns factory.Build(resolvedProviderName, shaper.New())'s provider (ignore the Build error — Validate guarantees the provider exists; an uncredentialed provider returns the noCredentialProvider wrapper, which is the intended lazy-fail).

    Wire cmd/ass-guard/main.go runTrace (~line 127): replace `provider.NewAnthropicProvider(shaper.New(), provider.WithAnthropicRequestCapture(capturer))` — build the factory via loadSchedulingFactory("" or the profiles' workDir, "", os.Stderr) (reuse the same helper; for the tracer the workDir is cwd), Build the heavy-tier provider, then if the built provider is an *provider.AnthropicProvider apply WithAnthropicRequestCapture by reconstructing — simpler: have the factory expose a way to attach the capturer, OR keep the capturer by building via the factory and, because Build returns the interface, type-assert to *provider.AnthropicProvider to call a capturer setter. To avoid adding a setter, the cleanest path is: in main.go, after factory.Build returns the provider, if the LOG-01 capturer is needed, construct the provider through the factory for credential/base_url resolution but re-apply the capturer via provider.WithAnthropicRequestCapture by adding a thin factory option — PREFER adding an optional variadic to ProviderFactory.Build is out of scope; instead, since the tracer is the LOG-01 path only, keep main.go's provider construction using the resolved credential: read the resolved key+base_url from the factory (expose `(*ProviderFactory).ResolvedKey(providerName) (string, string)` returning base_url+key — add this tiny accessor in 07-01's factory.go if not present, else reconstruct here) and call provider.NewAnthropicProvider(shaper.New(), provider.WithAnthropicBaseURL(baseURL), provider.WithAnthropicAPIKey(key), provider.WithAnthropicRequestCapture(capturer)). If adding the accessor is cleaner, add `func (f *ProviderFactory) Endpoint(providerName string) (baseURL, key string, ok bool)` to factory.go (this is a 07-01 file — coordinate: this plan may Edit internal/scheduler/factory.go to add the accessor; note it in files_modified if so).

    Wire cmd/ass-guard/parity.go runParity (~line 87): replace `provider.NewAnthropicProvider(shaper.New())` with the factory-built heavy-tier provider (same loadSchedulingFactory + resolver.Resolve + factory.Build pattern; parity is operator-gated on $ZAI_API_KEY so zero-config env resolution applies).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && grep -rn 'NewAnthropicProvider(shaper.New())' cmd/ass-guard/ ; echo "--- (expect zero matches of the bare ctor) ---" && go build ./cmd/ass-guard && go test ./cmd/ass-guard/ -run 'LoadSchedulingFactory' -race -count=1 && go test ./cmd/ass-guard/ -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'provider.NewAnthropicProvider(shaper.New())' cmd/ass-guard/acp_serve.go cmd/ass-guard/main.go cmd/ass-guard/parity.go` returns no matches (the bare hardcoded ctor is gone from all three sites).
    - `grep -rn 'loadSchedulingFactory\|NewProviderFactory\|factory.Build' cmd/ass-guard/acp_serve.go cmd/ass-guard/main.go cmd/ass-guard/parity.go` shows the factory is used at all three sites.
    - `go build ./cmd/ass-guard` exits 0.
    - `go test ./cmd/ass-guard/ -run TestLoadSchedulingFactory_WarnsUncredentialed -race -count=1` exits 0 and asserts the stderr warning names the uncredentialed provider AND the factory is non-nil.
    - `go test ./cmd/ass-guard/ -run TestLoadSchedulingFactory_ZeroConfigEnv -race -count=1` exits 0 (backward-compat: $ZAI_API_KEY resolves the anthropic provider).
    - `go test ./cmd/ass-guard/ -race -count=1` exits 0 (no regression to the existing ACP/engine/MCP tests).
  </acceptance_criteria>
  <done>The three hardcoded provider-construction sites delegate to the scheduler.ProviderFactory; acp serve loads the operator config + warns at startup about uncredentialed providers without refusing to start; the tracer and parity paths use the factory too. Wiring tests green under -race. Committed.</done>
  <reversibility rating="costly">Per 07-CONTEXT D-08 the factory wiring "touches every provider construction site and the scheduler<->session boundary." Reverting reintroduces three hardcoded ctors — doable but touches the same three files + the helper. Flagged costly; the factory API (07-01) is the stable seam that keeps this reversible.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC3 0600-permission warning + SC4 zero-config backward-compat proof + SC1 editor-zero-env proof</name>
  <files>cmd/ass-guard/acp_serve.go, cmd/ass-guard/provider_factory_test.go</files>
  <read_first>
    - .planning/phases/07-multi-provider-config-credentials/07-CONTEXT.md (SC3 gitignore+0600; SC4 zero-config byte-for-byte; SC1 editor-spawned zero-env)
    - cmd/ass-guard/acp_serve.go (runACPServeCmd / seedACPGuard — where startup checks live; the filePermOwner=0o600 constant already in main.go)
    - internal/firstrun/firstrun.go (filePerm=0o644 the seed writes with — the 0600 warning is advisory, the seed stays 0644; the warning targets operator-authored literal-key files)
    - cmd/ass-guard/provider_factory_test.go (from Task 1 — extend with the perm + backward-compat + zero-env cases)
    - internal/scheduler/factory.go (ResolvedCredential.Source values: flag|env|config|"")
  </read_first>
  <behavior>
    - At startup, if `.ass-guard/scheduling.yaml` exists and its mode permits group/other read/write (i.e. not 0600-tight), ass-guard writes one STDERR warning recommending 0600 (credential-on-disk hygiene). It never refuses to start.
    - Zero-config backward-compat: embedded default + $ZAI_API_KEY + no flag + no literal -> the resolved anthropic credential Source is "env" and the key equals $ZAI_API_KEY (today's exact behavior).
    - Editor-zero-env: a 2-provider config where the active provider carries a literal api_key, with NO env vars set and NO flag -> the resolved credential Source is "config" (the file authenticates the process with zero environment).
  </behavior>
  <action>
    RED first. Extend cmd/ass-guard/provider_factory_test.go with three cases. (1) TestStartupWarn_ConfigPermLoose: create a temp .ass-guard/scheduling.yaml, chmod it 0644, run the startup perm check against a bytes.Buffer stderr, assert the buffer contains a 0600 recommendation; chmod 0600 and assert no warning. (2) TestBackwardCompat_ZAIEnvOnly: using only the embedded default (no overlay) + t.Setenv("ZAI_API_KEY","k") + no flag, assert the factory resolves the anthropic provider credentialed from env (the built provider is a real *provider.AnthropicProvider, not the noCredentialProvider) — proving today's flow. (3) TestEditorZeroEnv_LiteralInConfig: write a temp .ass-guard/scheduling.yaml whose heavy-tier provider has a literal api_key and api_key_env unset, unset every *_API_KEY via t.Setenv("ZAI_API_KEY","") etc., call the resolver+factory, and assert the resolved credential Source is "config" (use the factory.Endpoint accessor or assert the built provider is a real adapter, not the no-cred wrapper — i.e. a file credential authenticates with zero env).

    Implement the perm check in cmd/ass-guard (a helper `func warnLooseConfigPerm(path string, stderr io.Writer)`): os.Stat the path; if mode & 0o077 (!= 0) fmt.Fprintf(stderr) a one-line warning recommending chmod 0600 because the file may carry an api_key. Call it from runACPServe / runACPServeCmd right after loadSchedulingFactory succeeds, for the overlay path when it exists. Do NOT change the seed's write mode (firstrun stays 0644 — the warning is advisory for operator-authored literal-key files). Honor transport discipline: stderr only.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./cmd/ass-guard/ -run 'StartupWarn_ConfigPermLoose|BackwardCompat_ZAIEnvOnly|EditorZeroEnv_LiteralInConfig' -race -count=1 && go test ./cmd/ass-guard/ -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'warnLooseConfigPerm\|0600' cmd/ass-guard/acp_serve.go` shows the perm check is wired into the startup path.
    - `go test ./cmd/ass-guard/ -run TestStartupWarn_ConfigPermLoose -race -count=1` exits 0; warns at 0644, silent at 0600.
    - `go test ./cmd/ass-guard/ -run TestBackwardCompat_ZAIEnvOnly -race -count=1` exits 0 (SC4: $ZAI_API_KEY-only flow preserved).
    - `go test ./cmd/ass-guard/ -run TestEditorZeroEnv_LiteralInConfig -race -count=1` exits 0 (SC1: literal-in-config authenticates with zero env).
    - No test writes to stdout (transport discipline): `go test ./cmd/ass-guard/ -run 'StartupWarn|BackwardCompat|EditorZeroEnv' -v 2>&1 | grep -c '^--- PASS'` >= 3.
  </acceptance_criteria>
  <done>The 0600 perm warning fires at startup for loose config; zero-config backward-compat (SC4) and the editor-zero-env case (SC1) are proven by tests. Committed.</done>
  <reversibility rating="reversible">A startup warning + tests are local and easily changed.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Finalize traceability — add PCFG-01..04 to REQUIREMENTS.md §Providers + matrix; mark PROV-01 Complete; finalize ROADMAP Phase 7 plan list</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (§Providers lines ~37-39 for PROV-01..03; the Traceability table ~line 170-238 + coverage counts ~line 240-244)
    - .planning/ROADMAP.md (Phase 7 section ~line 187-201: the placeholder `Plans:` list + the 4 success criteria)
    - .planning/phases/07-multi-provider-config-credentials/07-CONTEXT.md (D-09 — the exact PCFG-01..04 wording to transcribe)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md (use Edit, scoped — NEVER Write the whole file). Under §"Providers (Phase 1 substrate + Phase 3)" add four unchecked REQ entries (Phase 7), transcribing D-09's wording: PCFG-01 (provider/model/credential config schema extension + loader + validation), PCFG-02 (credential precedence flag>env>config + ${VAR} expansion + no-log/redaction + missing-credential warn+lazy behavior), PCFG-03 (scheduler->provider credentialed-instance factory/registry wiring), PCFG-04 (zero-config backward-compat: embedded default seeds env-only $ZAI_API_KEY). Mark the PROV-01 checkbox `[ ]` -> `[x]` and append "(completed by Phase 7)" since Phase 7 completes it. In the Traceability table add five rows: PCFG-01..04 -> "7 — Multi-Provider Config & Credentials" / Pending (or Complete once executed), and update the PROV-01 row's Status Pending -> Complete. Update the coverage counts: v1 total becomes 71 (67 + 4 new PCFG) and Phase 7 distribution = 5 (PCFG-01..04 + PROV-01 completion); restate "Mapped to phases: 71 (100%)" / "Unmapped: 0".

    Edit .planning/ROADMAP.md (scoped Edit) the Phase 7 `Plans:` block: replace the `- [ ] TBD (run /gsd-plan-phase 7 ...)` line with two checked-plan entries: `- [ ] 07-01-PLAN.md — credential schema + precedence resolver + provider factory (PCFG-01..04 core; tracer slice)` and `- [ ] 07-02-PLAN.md — wire factory into acp serve/tracer/parity + startup warnings + zero-config/zero-env proofs + traceability (PCFG-02..04, PROV-01)`. Leave the Phase 7 Goal + 4 success criteria unchanged. Also bump the Phase Overview table row for Phase 7 if it carries a stale requirement count (it currently reads "PCFG-01.. (new...) + completes PROV-01" — that is fine, leave it).

    Do NOT touch any other phase's rows. These are scoped placeholder fills only.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && grep -c 'PCFG-0' .planning/REQUIREMENTS.md && grep -c '07-0[12]-PLAN.md' .planning/ROADMAP.md && grep -c '\[x\] \*\*PROV-01\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PCFG-01\|PCFG-02\|PCFG-03\|PCFG-04' .planning/REQUIREMENTS.md` returns >= 4 (one entry each in §Providers) and each also appears once in the Traceability table.
    - `grep -c '07-01-PLAN.md\|07-02-PLAN.md' .planning/ROADMAP.md` returns >= 2 (the finalized plan list).
    - `grep '\[x\] \*\*PROV-01\*\*' .planning/REQUIREMENTS.md` returns 1 (PROV-01 marked complete).
    - The REQUIREMENTS.md coverage block reflects 71 total v1 requirements, 0 unmapped (eyeball the edited counts).
    - Full phase gate still green (this task is docs-only, but assert no code regression): `go test ./... -race -count=1` (the single known pre-existing failure `internal/profile.TestStability_WithinSessionExtractionSource` is the documented Phase-1 data-source blocker — exclude it; everything else must pass) and `mise ci 2>/dev/null || (go vet ./... && golangci-lint run ./... && go build ./...)` exits 0.
  </acceptance_criteria>
  <done>REQUIREMENTS.md carries PCFG-01..04 (§Providers + traceability matrix) with PROV-01 marked Complete and coverage counts updated; ROADMAP.md Phase 7 plan list is finalized (07-01, 07-02). Full phase gate green. Committed.</done>
  <reversibility rating="reversible">Docs edits are trivially reversible.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.ass-guard/scheduling.yaml` (operator file) → startup | the file may carry a literal api_key; its on-disk permissions + gitignore status determine exposure. |
| startup → stderr | credential warnings + the 0600 perm warning cross to the operator-visible stderr stream; stdout must stay ACP-only. |
| factory → live session provider | the resolved credential is handed to the adapter used by every turn; a wrong resolution authenticates as the wrong principal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Information Disclosure | `.ass-guard/scheduling.yaml` literal api_key committed / world-readable | high | mitigate | `.ass-guard/` is wholesale gitignored (Phase 6, confirmed); this plan adds the 0600 perm warning (SC3). The seed itself stays 0644 with NO literal key (D-06) so the warning targets operator-authored literal-key files only. |
| T-07-06 | Information Disclosure | startup warning accidentally printing a credential | high | mitigate | WarnUncredentialed (07-01) names only the provider + the env var to set, never the key; the 0600 warning names only the path + the recommended mode. All warnings go to stderr (transport discipline). Acceptance gate: no test observes a key in any warning buffer. |
| T-07-07 | Spoofing | wrong provider authenticated due to mis-resolution | medium | mitigate | Resolution is deterministic (07-01 ResolveCredential, table-tested) + the heavy-tier provider is resolved via the validated scheduler resolver; Validate (07-01) rejects dangling provider refs so an unknown name cannot silently resolve. |
| T-07-08 | Denial of Service | acp serve fails to start on a bad overlay config | low | mitigate | loadSchedulingFactory errors degrade gracefully (fall back to the embedded default + log to stderr), mirroring the existing engine/learning-store degradation pattern (acp_serve.go ~line 266, 360). |
| T-07-SC | Supply chain (Tampering) | (none) | n/a | accept | No new external packages in this plan (stdlib + existing scheduler/provider APIs only). |
</threat_model>

<verification>
- `go build ./cmd/ass-guard` exits 0.
- `go test ./cmd/ass-guard/ ./internal/scheduler/ ./internal/defaults/ ./internal/provider/ -race -count=1` exits 0.
- `go vet ./...` and `golangci-lint run ./...` exit 0.
- `grep -rn 'NewAnthropicProvider(shaper.New())' cmd/ass-guard/` returns no bare-ctor matches (all three sites factory-wired).
- End-to-end manual (operator-gated, OPTIONAL — needs a real key in `.ass-guard/scheduling.yaml`): `ass-guard acp serve` started by Zed with zero shell env makes a real model turn using the file credential. Not an autonomous gate (the construction + zero-env tests in Task 2 are the autonomous proof).
</verification>

<success_criteria>
- SC1 (multi-provider + file credentials + editor zero-env): proven by TestEditorZeroEnv_LiteralInConfig (file credential authenticates with zero env).
- SC2 (>=2 models/provider + correct instance per resolved pair): the factory (07-01) builds the correct adapter per provider; acp serve resolves the heavy tier and builds that instance (Task 1).
- SC3 (precedence + no-log + gitignore + 0600): precedence (07-01) + redactor reliance + gitignore (Phase 6) + the 0600 startup warning (Task 2).
- SC4 (zero-config backward-compat): proven by TestBackwardCompat_ZAIEnvOnly ($ZAI_API_KEY-only flow preserved) + the D-06 embedded default (07-01).
- PROV-01 (any provider via configurable base URL): completed — factory builds per configured base_url (07-01 wire round-trip + Task 1 wiring).
</success_criteria>

<output>
Create `.planning/phases/07-multi-provider-config-credentials/07-02-SUMMARY.md` when done. Record: the factory-wiring seam (loadSchedulingFactory), the 3 replaced construction sites, the startup warnings (D-07 uncredentialed + 0600), the backward-compat + zero-env proof results, and the REQUIREMENTS/ROADMAP updates. Note any deviation from the plan (e.g. if a factory.Endpoint accessor was added to 07-01's factory.go to support the tracer's capturer).
</output>

## Artifacts this plan produces
- `cmd/ass-guard` factory wiring: `loadSchedulingFactory(workDir, apiKeyFlag, stderr)` helper + factory-driven `sessionTurnRunner.makeProvider` (acp_serve) + factory-driven tracer (main.go) + factory-driven parity (parity.go).
- `warnLooseConfigPerm(path, stderr)` — the SC3 0600-permission startup warning.
- Startup D-07 warning path (calls `factory.WarnUncredentialed(stderr)` from 07-01).
- New test file `cmd/ass-guard/provider_factory_test.go` (wiring + warn + backward-compat + zero-env + perm cases).
- REQUIREMENTS.md: PCFG-01..04 (§Providers + traceability matrix) + PROV-01 -> Complete; coverage counts updated to 71.
- ROADMAP.md: Phase 7 `Plans:` list finalized (07-01, 07-02).
- (Possibly) `(*scheduler.ProviderFactory).Endpoint(providerName)` accessor in internal/scheduler/factory.go if the tracer's LOG-01 capturer needs resolved base_url+key — record as a noted cross-plan edit.

## Surfaced assumptions (do not silently drop)
- **No live round-trip gate:** SC1's "real model turn with zero environment" requires a real key in `.ass-guard/scheduling.yaml`, which is operator setup. The autonomous proof is the construction-level TestEditorZeroEnv_LiteralInConfig (file credential, Source=config, zero env). A live end-to-end turn is listed as OPTIONAL operator verification, not an autonomous gate — consistent with the planner's allowance for "a construction assertion proving the right base_url+key+shape reach the provider ctor."
- **Seed stays 0644:** firstrun writes the seed scheduling.yaml at 0644 (internal/firstrun filePerm). The 0600 warning is advisory and targets operator-authored literal-key files; this plan does NOT change the seed's write mode (changing it would be a Phase-6 contract change and would not help — the D-06 seed carries NO literal key anyway).
- **main.go LOG-01 capturer:** the tracer attaches a RequestCapturer for audit. If the factory's Build returns the interface without a capturer seam, this plan adds a small `(*ProviderFactory).Endpoint(providerName)` accessor to 07-01's factory.go so main.go can reconstruct the AnthropicProvider with the resolved base_url+key+capturer. This is a cross-plan Edit of an 07-01 file; flagged for the executor to handle atomically.
