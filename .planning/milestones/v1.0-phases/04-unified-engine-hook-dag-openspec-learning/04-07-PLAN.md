---
phase: 04-unified-engine-hook-dag-openspec-learning
plan: 07
type: execute
wave: 4
depends_on: ["04-05", "04-06"]
files_modified:
  - internal/engine/observe.go
  - internal/engine/observe_test.go
  - internal/engine/e2e_test.go
  - cmd/ass-guard/acp_serve.go
  - cmd/ass-guard/acp_serve_test.go
  - .planning/phases/04-unified-engine-hook-dag-openspec-learning/04-VERIFICATION-PREP.md
requirements: [ENG-03]
autonomous: true   # cancel-drain logic + full e2e with fake provider/stub openspec; no live keys; verification-prep is a doc mapping success criteria → tests
---

# Plan 04-07 — Cancel-drain (ENG-03 full) + full end-to-end + phase verification prep

## Phase Goal

**As an** SSD practitioner, **I want** manual cancellation to abort the in-flight turn AND drain the engine's queued continue-injections, and the full phase goal demonstrated in one test, **so that** the structural-safety off-switch is complete and the phase's 5 success criteria are mapped to verifiable tests.

## Truths

- Completes D-03/ENG-03 (the only off-switch is manual cancellation — it cancels the in-flight turn AND drains queued injections). The 04-01 tracer implemented the engine's cancel-aware loop (ctx cancel → return "cancelled" + no further injections); THIS plan wires the ACP `session/cancel` path (acp_serve.go handleSessionCancel → st.cancelTurn) to the engine's cancel so that a cancel during a continue-injection chain stops the chain AND abandons any pending injections.
- Carries ENG-03 (drains queued injections on cancel — the full off-switch). Consolidates the 5 ROADMAP success criteria into one verification-prep document + one full e2e test that exercises them in sequence.
- Depends on 04-05 (the engine wiring in acp_serve — this plan adds the cancel-drain to that wiring) + 04-06 (the learning ask path — exercised in the full e2e). This is the phase's LAST integration + verification gate before `/gsd:verify-work`.

<objective>
Complete the structural-safety off-switch + prove the full phase goal: (1) the engine's Observe wrapper, on ctx cancellation mid-chain, returns ("cancelled", nil) AND abandons every pending continue-injection (the 04-01 cancel test was unit-level; here the cancel flows from `session/cancel` → `st.cancelTurn()` → the engine's ctx); (2) a queued-injection drain test: queue 3 continue-injections, fire `session/cancel` after the 1st → the 2nd and 3rd NEVER run (sess.Prompt call count == 1, not 4); (3) a full end-to-end test exercising all 5 ROADMAP success criteria in one scenario (zero-continue OpenSpec; seeded hooks fire; on-failure halt; provenance loop prevention; concurrent tools + swappable backend; learning ask-once); (4) `04-VERIFICATION-PREP.md` maps each of the 5 success criteria to the specific test(s) + commands that prove it, ready for `/gsd:verify-work`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-unified-engine-hook-dag-openspec-learning/04-CONTEXT.md
@.planning/phases/04-unified-engine-hook-dag-openspec-learning/04-RESEARCH.md
@cmd/ass-guard/acp_serve.go
@internal/acp/handlers.go
@internal/engine/observe.go

<interfaces>
<!-- No new public types. This plan: -->
<!-- (a) tightens the engine's cancel contract (the drain is already structural -->
<!--     in 04-01's Observe — ctx cancel ⇒ no further runner.Run; here we assert -->
<!--     it at the ACP integration level), -->
<!-- (b) ensures session/cancel's ctx reaches the engine's Observe (it does — the -->
<!--     engine runs inside the turnRunner.Run which holds the cancelled turnCtx), -->
<!-- (c) writes the full e2e + the verification-prep doc. -->
</interfaces>
</context>

<must_haves>
  truths:
    - "Cancel-drain is structural (ENG-03): the engine's Observe loop checks `ctx.Err()` before each continue-injection (04-01 T3); session/cancel cancels the turnCtx that Observe runs under (via st.cancelTurn in handleSessionCancel) — so a cancel during a chain stops it AND abandons pending injections. This plan verifies the contract end-to-end (the unit-level cancel test was 04-01 T3 Test 6)"
    - "No queued injection survives a cancel: a scenario with 3 pending continue-injections, cancelled after the 1st turn, results in exactly 1 sess.Prompt call (NOT 4) — the drain is observable"
    - "The full end-to-end test exercises all 5 ROADMAP success criteria: (1) zero-continue OpenSpec + structural safety; (2) seeded post-implement hook fires + graceful degradation; (3) hook on-failure halt + provenance loop prevention; (4) learning ask-once-remember (a candidate → 3 confirms → active); (5) concurrent read-only tools + serialized mutating + swappable backend"
    - "04-VERIFICATION-PREP.md is a 1:1 map: each of the 5 success criteria → the test name(s) + the exact `go test` command that proves it, so `/gsd:verify-work` has a ready gate"
    - "Phase gate: `go test ./... -race` green, `go build ./...` + `go vet ./...` clean, no TODO/FIXME left in the Phase-4 production files (grep-gated)"
  artifacts:
    - path: "internal/engine/observe.go"
      provides: "(if needed) a Cancel() accessor or a documented guarantee that ctx cancellation is the drain mechanism; no behavioral change if 04-01 T3's cancel test already covers the contract — this task is primarily the ACP-level verification"
      min_lines: 0
    - path: "internal/engine/e2e_test.go"
      provides: "the full 5-criteria end-to-end test (fake provider + real engine + real hookdag + real toolexec + real learning store + stub openspec)"
      min_lines: 120
    - path: "cmd/ass-guard/acp_serve_test.go"
      provides: "TestCancelDrainsInjections — session/cancel after the 1st of N continue-injections ⇒ exactly 1 sess.Prompt call"
      min_lines: 50
    - path: ".planning/phases/04-unified-engine-hook-dag-openspec-learning/04-VERIFICATION-PREP.md"
      provides: "the 5 success criteria → test + command map"
      min_lines: 60
  key_links:
    - from: "internal/acp/handlers.go handleSessionCancel"
      to: "internal/acp/server.go sessionState.cancelTurn"
      via: "session/cancel → cancelTurn() → cancels turnCtx → the engine's Observe loop observes ctx.Err() and drains"
      pattern: "cancelTurn|setCancel"
    - from: "internal/engine/observe.go Observe"
      to: "ctx.Err() check before each continue-injection"
      via: "Observe checks ctx.Err() at the top of each loop iteration (04-01 T3) — the drain"
      pattern: "ctx\\.Err\\(\\)"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → session/cancel → engine ctx | The cancel signal must reach the engine's Observe loop; a detached ctx would leave queued injections running (the off-switch broken). |

## STRIDE Threat Register (canonical Phase-4 IDs)

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-04 | Safety / Availability | session/cancel fails to drain queued continue-injections (the structural off-switch is broken — PROJECT.md's only safety mechanism) | mitigate | T1 Test 2: session/cancel after the 1st of N injections ⇒ sess.Prompt called exactly ONCE (not N); T1 also verifies Observe runs under a ctx derived from turnCtx (the load-bearing wiring check) |
| T-04-01 | Availability | graceful degradation regressed at the integration level | mitigate | T2 criterion 2 re-asserts engine-panic ⇒ turn completes at the full-wiring level |
</threat_model>

<tasks>

<task type="tdd" id="T1">
  <name>Task 1: Cancel-drain end-to-end (ENG-03) — session/cancel drains queued injections</name>
  <files>internal/engine/observe_test.go, cmd/ass-guard/acp_serve_test.go, internal/engine/observe.go</files>
  <behavior>
    - Test 1 (unit — 04-01 T3 Test 6 re-affirmed): a fake TurnRunner that always returns a matching output (would loop to the budget); cancel the ctx AFTER the 1st Run; Observe returns ("cancelled", nil); Run called exactly ONCE (not MaxContinueInjections+1).
    - Test 2 (ACP-level drain): construct the full acp_serve wiring (04-05 T2) with a fake provider scripted to ALWAYS emit the impl-complete pattern (infinite continue chain). Drive a session/prompt in a goroutine; after the FIRST session/update chunk, issue session/cancel. Assert: sess.Prompt was called exactly ONCE (the chain stopped); no further session/update chunks after the cancel; the prompt returned stopReason "cancelled".
    - Test 3 (pending injections abandoned): a recording hookdag Executor + a fake provider that emits action:hook on every turn (which would chain hooks); cancel after the 1st; assert the hook ran at most once + no further hook launches.
  </behavior>
  <action>
    First, verify the cancel path: trace `handleSessionCancel` → `st.cancelTurn()` → `cancel()` (the turnCtx's cancel func, stored per-session) → the engine's Observe loop sees `ctx.Err()` (the turnRunner.Run runs under turnCtx). If the engine's Observe is invoked under a ctx DERIVED from turnCtx (it is — Run is called with turnCtx), the drain is already structural (04-01 T3 Test 6). If NOT (e.g. the engine was given a background ctx in 04-05 T2), FIX the wiring so Observe runs under a ctx derived from turnCtx — this is the load-bearing fix for ENG-03. Then add Test 1 (re-affirm the unit contract) + Test 2 (the ACP-level drain — the real off-switch) + Test 3 (hook-chain drain). Test 2 issues session/cancel via the Server's handler dispatch (a `handleSessionCancel` call with the session's id) — the ACP test harness in acp_serve_test.go. Add `04-VERIFICATION-PREP.md` in T3. If no production change is needed (the contract already holds), this task is the test + a doc comment in observe.go stating "the drain mechanism is ctx cancellation; session/cancel reaches Observe via the turnCtx — see TestCancelDrainsInjections."
  </action>
  <read_first>
    - internal/engine/observe.go (the 04-01 Observe cancel check — confirm it runs under a ctx derived from turnCtx)
    - cmd/ass-guard/acp_serve.go (the 04-05 wiring — confirm Observe is called under turnCtx, NOT a detached ctx)
    - internal/acp/handlers.go (handleSessionCancel → st.cancelTurn — the cancel origin)
    - internal/acp/server.go (sessionState.setCancel/cancelTurn — how the cancel func is stored + invoked)
    - .planning/phases/04-unified-engine-hook-dag-openspec-learning/04-RESEARCH.md (§1.7 the cancel-drain mechanism; §2.1 engine placement)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/engine ./cmd/ass-guard -run 'TestCancel|TestDrain' -race -v</verify>
  <acceptance_criteria>
    - Test 1 (unit cancel: Observe returns ("cancelled", nil), Run called once) passes
    - Test 2 (ACP-level drain: session/cancel after the 1st turn ⇒ sess.Prompt called ONCE, no further updates, stopReason "cancelled") passes — ENG-03 proven end-to-end
    - Test 3 (hook-chain drain: cancel ⇒ at most one hook launch) passes
    - The engine's Observe is confirmed (via test + a doc comment in observe.go) to run under a ctx derived from turnCtx — the drain is structural
    - `go test ./internal/engine ./cmd/ass-guard -race` passes
  </acceptance_criteria>
</task>

<task type="execute" id="T2">
  <name>Task 2: Full end-to-end — all 5 ROADMAP success criteria in one scenario</name>
  <files>internal/engine/e2e_test.go</files>
  <action>
    Write `internal/engine/e2e_test.go` — one comprehensive test (or a small suite of 5 sub-tests, `t.Run`) exercising the 5 ROADMAP Phase-4 success criteria using the full acp_serve wiring (04-05) + a fake provider + a stub openspec + a real Manager (t.TempDir transcript) + a real learning store + a real hookdag executor + a real toolexec dispatcher (with a recording fake backend):
    - Criterion 1 (zero-continue + structural safety): the 3-stage scenario from 04-05 T3 + the unmatched-safety cell.
    - Criterion 2 (seeded hooks + graceful degradation): a stage triggers the post-implement hook; the hook's test step runs (stub exit 0); inject an engine panic + assert the turn still completes (stop == "end_turn") + the original result returned.
    - Criterion 3 (on-failure + provenance loop prevention): a hook with a run-command step that exits 1 + on_failure:halt ⇒ the chain stops, the failure is in HookProgress; launch the SAME hook twice concurrently with allow_reentrant:false ⇒ one is skipped-reentrant.
    - Criterion 4 (learning ask-once): an unmatched launch situation → the engine asks (EngineDecision{ask}); RecordCandidate; 2 more occurrences with Confirm ⇒ on the 3rd the entry is active; a 4th occurrence uses the stored answer (no ask).
    - Criterion 5 (concurrent tools + swappable backend): a turn with 4 read-only tool calls finishes in < 80ms (concurrency); a mutating call serializes; swap the WebSearch backend by config (no code change) + assert the new backend is invoked.
    The test(s) are the phase's integration proof. They MUST pass under `-race`. Read the 04-05 T3 test as the starting harness + extend it. Do NOT add production code here — if a criterion fails, the gap is in 04-01..04-06 and must be fixed there (record the gap as a finding, do not paper over it).
  </action>
  <read_first>
    - cmd/ass-guard/acp_serve_test.go (the 04-05 T3 e2e harness — extend)
    - .planning/ROADMAP.md (Phase 4 Success Criteria — the 5 criteria this test proves verbatim)
    - internal/engine/integration_test.go (the 04-01 integration patterns)
    - internal/learning/store_test.go (the learning confirm-threshold pattern — criterion 4)
    - internal/toolexec/batch_test.go (the concurrency-timing assertion — criterion 5)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/engine -run TestE2E -race -v</verify>
  <acceptance_criteria>
    - `internal/engine/e2e_test.go` exercises all 5 ROADMAP Phase-4 success criteria (5 sub-tests or one scenario covering all 5)
    - Criterion 1 (zero-continue + unmatched-safety) passes
    - Criterion 2 (seeded hooks fire + engine-panic graceful degradation) passes
    - Criterion 3 (on-failure halt + provenance re-entrant skip) passes
    - Criterion 4 (ask-once → candidate → 3 confirms → active → reused) passes
    - Criterion 5 (read-only concurrent < 80ms + mutating serializes + backend swappable) passes
    - `go test ./internal/engine -race` passes
  </acceptance_criteria>
</task>

<task type="execute" id="T3">
  <name>Task 3: 04-VERIFICATION-PREP.md + phase gate (full suite green + clean)</name>
  <files>.planning/phases/04-unified-engine-hook-dag-openspec-learning/04-VERIFICATION-PREP.md</files>
  <action>
    Write `.planning/phases/04-unified-engine-hook-dag-openspec-learning/04-VERIFICATION-PREP.md` mapping each of the 5 ROADMAP Phase-4 success criteria to the test(s) + exact command that proves it. Format: a table with columns `Criterion | REQ-IDs | Test(s) | Command | Status`. Rows:
    1. Zero-continue OpenSpec + structural safety | ENG-01/02/03/05, OPEN-01/02/03 | `internal/engine/e2e_test.go TestE2E/Criterion1`, `cmd/ass-guard/acp_serve_test.go TestEndToEndZeroContinue` | `go test ./internal/engine ./cmd/ass-guard -run 'TestE2E/Criterion1|TestEndToEnd' -race -v`
    2. Seeded hooks + graceful degradation | HOOK-01/02/05, ENG-04 | `TestE2E/Criterion2`, `internal/hookdag/executor_test.go TestExecute` | `go test ./internal/engine ./internal/hookdag -run 'TestE2E/Criterion2|TestExecute' -race -v`
    3. on-failure + provenance loop prevention | HOOK-03/04 | `TestE2E/Criterion3`, `internal/hookdag/executor_test.go TestReentrant` | `go test ./internal/engine ./internal/hookdag -run 'TestE2E/Criterion3|TestReentrant' -race -v`
    4. Learning ask-once-remember + versioned/revertible | LRN-01/02/03/04 | `TestE2E/Criterion4`, `internal/learning/store_test.go`, `cmd/ass-guard/learning_cmd_test.go TestRevert` | `go test ./internal/learning ./cmd/ass-guard -run 'TestE2E/Criterion4|TestCandidate|TestConfirm|TestRevert' -race -v`
    5. Concurrent reads + serialized mutations + swappable backend | TOOL-04/05 | `TestE2E/Criterion5`, `internal/toolexec/batch_test.go TestDispatchBatch`, `internal/toolexec/backend_test.go TestBackendsFromConfig` | `go test ./internal/toolexec -run 'TestDispatchBatch|TestBackendsFromConfig' -race -v`
    Plus a Phase Gate section: `go test ./... -race` (must be green), `go build ./...` + `go vet ./...` (clean), and a grep gate: `grep -rn "TODO\|FIXME" internal/engine internal/hookdag internal/openspec internal/learning internal/toolexec cmd/ass-guard | wc -l` must be 0 (no leftover TODOs in Phase-4 production files). Then RUN the phase gate commands + record the actual output in the doc's Status column. If any command fails, the failure is a finding to fix in the source plan (not here) — record it as a BLOCKER line at the top of the doc.
  </action>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 Success Criteria — the 5 rows)
    - .planning/REQUIREMENTS.md (the exact REQ-ID text per criterion)
    - the test file paths from 04-01..04-06 (the test names referenced in the map)
  </read_first>
  <verify>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./... -race && go build ./... && go vet ./... && grep -rn "TODO\|FIXME" internal/engine internal/hookdag internal/openspec internal/learning internal/toolexec cmd/ass-guard | wc -l</verify>
  <acceptance_criteria>
    - `04-VERIFICATION-PREP.md` exists with a 5-row criterion→test→command table covering all 19 REQ-IDs
    - The Phase Gate section records the ACTUAL output of `go test ./... -race`, `go build ./...`, `go vet ./...` (green) and the TODO/FIXME grep (0)
    - Every criterion row's command, when run, passes (or a BLOCKER line names the failing criterion + the source plan to fix)
    - `go test ./... -race` is green for all Phase-4 packages together
    - `go build ./... && go vet ./...` succeed
  </acceptance_criteria>
</task>

</tasks>
