---
phase: 00-spike-re-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spikes/go.mod
  - spikes/README.md
  - .gitignore
  - spikes/01-jsonl-capture/README.md
  - spikes/01-jsonl-capture/FINDING.md
autonomous: true
requirements: [STACK-Phase0-#1, STACK-Phase0-#4]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An isolated Go module exists at spikes/ (module github.com/djarvur/ass-guard-spikes, go 1.25) that every other spike in this phase can `go run` against"
    - "The repo root has NO go.mod (clean root preserved per D-05); spikes/go.sum and built binaries are gitignored"
    - "Spikes are THROWAWAY evidence-producers, not the start of the real codebase (D-04); Phase 1 starts the real module at the repo root with deliberate dependency decisions uncoupled from what the spikes pinned"
    - "The real zcode transcript path ~/.zcode/cli/rollout/model-io-sess_<session-id>.jsonl is located and a representative `type: \"model_io\"` line is captured and redacted per D-03, written to spikes/01-jsonl-capture/FINDING.md"
    - "STACK item #4's STRUCTURALLY-MOOT closure reasoning (STT always external → no cgo → goreleaser matrix unaffected, per D-06) is written to spikes/01-jsonl-capture/FINDING.md for Plan 05 to fold into VERIFIED-FACTS.md"
  artifacts:
    - spikes/go.mod
    - spikes/README.md
    - spikes/01-jsonl-capture/README.md
    - spikes/01-jsonl-capture/FINDING.md
  key_links:
    - "spikes/go.mod module path == github.com/djarvur/ass-guard-spikes (so Plans 02/03/04 `package main` files compile in this module)"
    - "spikes/01-jsonl-capture/FINDING.md is the evidence Plan 05 reads to author VERIFIED-FACTS.md items #1 and #4 (Plan 05 is the single writer of VERIFIED-FACTS.md)"
---

<objective>
Stand up the throwaway spikes module (D-05) and close the two items that need no provider keys or network: #1 (zcode JSONL path + schema — the Phase-1 ground truth for MIMC-02/PROF-03/PROF-05) and #4 (structurally-moot STT cross-compile per D-06). This plan produces the evidence as `spikes/01-jsonl-capture/FINDING.md`; it does NOT touch VERIFIED-FACTS.md (Plan 05 is the single writer of that file, so Wave-1 plans have zero files_modified overlap and can run in true parallel).

Purpose: This is the Wave-1 foundation. Plans 02, 03, 04 each `go run` a spike inside the module this plan creates and each writes a per-spike evidence file in its own subdir. Plan 05 (Wave 2) reads all four evidence files + the §3 finding and authors VERIFIED-FACTS.md in a single integration pass. #1 is the single most important item in the phase (Phase 1's mimicry profile is extracted from these transcripts) and carries the Tier-B path-correction finding that needs user sign-off in Plan 05.

Output: An isolated, gitignored-artifact `spikes/` module; a committed `spikes/README.md` and `spikes/01-jsonl-capture/{README,FINDING}.md` where FINDING.md holds the §3 corrected-path/schema finding (item #1) and the D-06 STRUCTURALLY-MOOT reasoning (item #4).
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-spike-re-verification/00-CONTEXT.md
@.planning/phases/00-spike-re-verification/00-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the isolated spikes module + gitignore artifacts (D-05)</name>
  <files>spikes/go.mod, spikes/README.md, .gitignore</files>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-05: isolated module, committed source + README, gitignored go.sum/binaries, clean root)
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §1 (version pins) and §7 (exact go.mod content + directory layout, including the 01-jsonl-capture/ subdir with no main.go)
    - .gitignore (current state — append, do not overwrite)
    - AGENTS.md "Constraints" + "Transport discipline" (stdout = ACP only; all logging → stderr)
  </read_first>
  <action>
    Create the throwaway spikes module per D-05. This module is explicitly NOT the ass-guard codebase — it exists only to produce evidence for VERIFIED-FACTS.md (D-04).

    1. Write `spikes/go.mod` with EXACT content: module `github.com/djarvur/ass-guard-spikes`, `go 1.25`, and a `require` block pinning `github.com/go-telegram/bot v1.23.0` and `github.com/sashabaranov/go-openai v1.42.0` (the versions confirmed in RESEARCH.md §1). Do NOT add `anthropic-sdk-go` — no spike needs it (RESEARCH.md §7). Use exact tags, never `@latest` (D-02's `Verified against` field demands a concrete version).

    2. Write `spikes/README.md` (committed per D-05) that (a) states these spikes are THROWAWAY and must not be imported in Phase 1; (b) lists each planned spike (`01-jsonl-capture` = filesystem capture, no Go; `02-openai-toolschema`; `03-acp-handshake`; `05-stdout-collision`) with a one-line description; (c) records the env vars each needs (`MINIMAX_API_KEY`/`GROQ_API_KEY` for #2, `OPENAI_BASE_URL`/`OPENAI_MODEL` for #2, a bot token for #5); (d) notes that ALL spike output goes to stderr, never stdout (transport discipline); (e) notes item #4 has no spike (STRUCTURALLY-MOOT per D-06); (f) notes that each spike subdir writes a per-spike `FINDING.md`/`RESULT.md` evidence file that plan 00-05 folds into VERIFIED-FACTS.md.

    3. Append to the repo-root `.gitignore` (use Edit, scoped append — do NOT rewrite the file): `spikes/go.sum` and `spikes/bin/` and `/spikes/**/bin/`. Do NOT ignore `spikes/*.go`, `spikes/**/main.go`, `spikes/go.mod`, or `spikes/**/*.md` — source and findings are committed (D-05).

    4. Do NOT create a root `go.mod`. Verify the repo root stays clean: `ls go.mod` must fail after this task.

    Plausible failure tier (D-07): Tier A. If `go 1.25` is not the local toolchain, the skeleton still commits; the spikes run via `go run` in later plans and a toolchain mismatch surfaces there as a Tier-A version note, not here.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; head -1 spikes/go.mod | grep -q 'github.com/djarvur/ass-guard-spikes' &amp;&amp; grep -q 'go 1.25' spikes/go.mod &amp;&amp; grep -q 'go-telegram/bot v1.23.0' spikes/go.mod &amp;&amp; grep -q 'sashabaranov/go-openai v1.42.0' spikes/go.mod &amp;&amp; ! [ -f go.mod ] &amp;&amp; grep -q 'spikes/go.sum' .gitignore &amp;&amp; echo SKELETON_OK</automated>
  </verify>
  <done>
    `spikes/go.mod` declares module `github.com/djarvur/ass-guard-spikes`, `go 1.25`, with exact pins `go-telegram/bot@v1.23.0` + `go-openai@v1.42.0`; `spikes/README.md` documents all spikes + THROWAWAY warning + stderr discipline + the per-spike evidence-file convention; repo root has no `go.mod`; `.gitignore` ignores `spikes/go.sum` and built binaries but not spike source or findings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: #1 zcode JSONL filesystem capture + path/schema documentation</name>
  <files>spikes/01-jsonl-capture/README.md, spikes/01-jsonl-capture/FINDING.md</files>
  <behavior>
    - Path assertion: the directory ~/.zcode/cli/rollout/ exists and contains at least one file matching model-io-sess_*.jsonl
    - Schema assertion: the first parseable line with type=="model_io" in a rollout file contains the keys request.body.system (an array), request.body.tools (an array), request.headers (an object), response.toolCalls (an array), model.modelId, model.providerId
    - STACK path assertion: ~/.claude/projects/*/  exists but is a different product's transcript (NOT the zcode mimicry source) — confirms the Tier-B path correction is real
  </behavior>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §3 (HEADLINE FINDING: correct path, per-line schema, request/response sub-objects, redacted sample, sanitization checklist) and §8 (D-03 sanitization checklist)
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-03 sanitization, D-07 tier semantics)
    - .planning/research/STACK.md line ~139 ("JSONL session transcripts ... ~/.claude/projects/<munged-cwd>/<session-id>.jsonl" — the WRONG premise being corrected)
    - spikes/go.mod (confirm the module exists from Task 1)
  </read_first>
  <action>
    Item #1 is a filesystem capture, NOT a Go program (CONTEXT.md per-item assignment; RESEARCH.md §7). Produce the evidence by inspecting real files on this machine, then write it to `spikes/01-jsonl-capture/FINDING.md` (NOT to VERIFIED-FACTS.md — Plan 05 owns that file).

    1. Confirm the §3 finding holds on this machine. List `~/.zcode/cli/rollout/` and pick one `model-io-sess_*.jsonl`. Using `jq` (or `python3 -m json.tool`), extract ONE line whose `.type == "model_io"` and print its top-level keys, then its `request.body` keys, `request.headers` keys, and `response.toolCalls` shape. Confirm `request.body.system` (array), `request.body.tools` (array, ~77 entries), `request.headers` (the ~12 identity header names), and `response.toolCalls[]` (`{id, name, input}`) are all present. Also confirm `~/.claude/projects/*/` exists but uses a `queue-operation`/claude-code schema (a different product) — this is the proof the STACK path is wrong for zcode.

    2. Apply the D-03 sanitization checklist (RESEARCH.md §8) to produce a redacted representative excerpt: replace all prompt/system text, ids (session/trace/turn/request/call), token counts, and tool arguments with `[REDACTED: ...]`; PRESERVE all JSON keys, all HTTP header NAMES (the header-name set IS the mimicry target per PROF-05), all enum values, and array lengths. The excerpt should be ~10-15 representative lines showing the structure (not a full dump).

    3. Write `spikes/01-jsonl-capture/README.md` documenting the discovery: the correct path (`~/.zcode/cli/rollout/model-io-sess_<session-id>.jsonl`), why the STACK `~/.claude/projects/` path is a different product, the per-line schema (lift the table from RESEARCH.md §3), the "which fields carry what" Phase-1 lookup table, and a note that `01-jsonl-capture/` has NO `main.go` (filesystem capture; `jq`/`python3 -m json.tool` suffice — RESEARCH.md §7).

    4. Write `spikes/01-jsonl-capture/FINDING.md` as the DRAFT VERIFIED-FACTS.md content for item #1 (Plan 05 will fold this in verbatim-ish). Structure it with the D-02 fields so Plan 05 can copy block-by-block: a heading `# #1. zcode JSONL transcript path + per-line schema`, then the seven fields `Fact` (quote STACK verbatim), `Source` (STACK.md item #1 + ~line 139), `Verified` (2026-08-09), `Verified against` (on-disk files at the corrected path; name the session-id file inspected, redacted), `Status` (`FAILED` — Tier-B: a load-bearing STACK premise (the path) is wrong; the D-07 resolution is recorded in `Notes`, and explicit user sign-off lands in Plan 05), `Evidence` (the D-03-redacted ~10-15 line excerpt, inline fenced, with the redaction note), and `Notes` (the corrected path, the corrected schema, the obsolete munged-cwd convention, the Anthropic-shape `request.body` + `providerId: "builtin:zai-coding-plan"`, the downstream MIMC-02 wording impact, the PROF-03 `target_capture_ref` = sessionId + corrected path, and the Tier-B resolution pointer "awaiting user sign-off in plan 00-05; research predicts (a) revise-and-continue").

    Transport discipline: there is no Go program here, so no stdout concern. Any terminal output during capture is dev-time only and is NOT committed.

    Plausible failure tier (D-07): Tier B (favorable-direction). If `~/.zcode/cli/rollout/` is absent OR a `model_io` line lacks `request.body.system`/`request.body.tools`, STOP, record FAILED in FINDING.md, and surface the three Tier-B options — the mimicry thesis depends on this data existing. (Research predicts revise-and-continue: the path is present and the schema is richer than STACK claimed.) The user sign-off on the resolution lands in Plan 05, not here; this task only records the finding.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; test -d "$HOME/.zcode/cli/rollout" &amp;&amp; ls ~/.zcode/cli/rollout/model-io-sess_*.jsonl &gt;/dev/null 2&gt;&amp;1 &amp;&amp; grep -q 'model-io-sess_&lt;session-id&gt;.jsonl' spikes/01-jsonl-capture/README.md &amp;&amp; grep -q 'model-io-sess_&lt;session-id&gt;.jsonl' spikes/01-jsonl-capture/FINDING.md &amp;&amp; grep -q 'request.body.system' spikes/01-jsonl-capture/FINDING.md &amp;&amp; grep -q 'request.headers' spikes/01-jsonl-capture/FINDING.md &amp;&amp; grep -q 'response.toolCalls' spikes/01-jsonl-capture/FINDING.md &amp;&amp; grep -q 'REDACTED' spikes/01-jsonl-capture/FINDING.md &amp;&amp; grep -q 'Status' spikes/01-jsonl-capture/FINDING.md &amp;&amp; echo CAPTURE_OK</automated>
  </verify>
  <done>
    `~/.zcode/cli/rollout/model-io-sess_*.jsonl` confirmed present with a `type: "model_io"` line carrying `request.body.{system,tools}`, `request.headers` (the 12 identity header names), and `response.toolCalls[]`; `spikes/01-jsonl-capture/README.md` records the path + schema + Phase-1 lookup table; `spikes/01-jsonl-capture/FINDING.md` holds the D-02-shaped item-#1 draft (FAILED status, corrected path, D-03-redacted evidence, Tier-B pointer) for Plan 05 to fold into VERIFIED-FACTS.md; `~/.claude/projects/` confirmed as a different product's transcript.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the item #4 STRUCTURALLY-MOOT finding (D-06)</name>
  <files>spikes/01-jsonl-capture/FINDING.md</files>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-06: STT always external; #4 STRUCTURALLY-MOOT; D-02 field schema)
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §6 (the D-06 reasoning chain for #4) and §8 (section template)
    - .planning/research/STACK.md line ~74 (the ggml-org/whisper.cpp/bindings/go LOW-confidence row — the cgo cross-compile premise being closed)
    - spikes/01-jsonl-capture/FINDING.md (current state from Task 2 — APPEND item #4 as a second section, do not overwrite the item #1 section)
  </read_first>
  <action>
    Append the item #4 section to `spikes/01-jsonl-capture/FINDING.md` (the same evidence file Plan 05 reads). This closes item #4 structurally per D-06 — no spike is produced.

    Append a heading `# #4. whisper.cpp / STT cross-compile (structurally moot)` followed by the seven D-02 fields:
    - `Fact`: quote STACK's item #4 verbatim (the whisper.cpp cgo cross-compile risk for the goreleaser macOS+Linux amd64+arm64 matrix; the `ggml-org/whisper.cpp/bindings/go` LOW-confidence row).
    - `Source`: STACK.md §"Open Verification Items" item #4 + the ggml-org/whisper.cpp bindings row (~line 74).
    - `Verified`: 2026-08-09.
    - `Verified against`: CONTEXT.md decision D-06 + RESEARCH.md §6 reasoning chain.
    - `Status`: `STRUCTURALLY-MOOT`.
    - `Evidence`: a one-paragraph reasoning chain (NOT a code sample) — D-06 mandates STT is ALWAYS an external utility: OpenAI Whisper API = HTTPS call (zero cgo); whisper.cpp local = out-of-process `whisper-cli` subprocess the user installs (never the cgo binding); Groq STT = HTTPS call. Therefore ass-guard's goreleaser matrix (pure Go, CGO_ENABLED=0) is unaffected by STT choice, and STACK item #4's premise (cgo cross-compile risk) does not arise.
    - `Notes`: this rule propagates to v2 STT work (never relitigated); no spike produced; cross-reference RESEARCH.md §6 and CONTEXT.md D-06.

    This is the complete closure of item #4 — Plan 05 copies this section into VERIFIED-FACTS.md verbatim.

    Plausible failure tier (D-07): not applicable — item #4 is closed structurally by D-06, not by empirical spike. There is no failure mode; the reasoning is sound and the architecture enforces it.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; F=spikes/01-jsonl-capture/FINDING.md &amp;&amp; grep -q '^# #4\.' "$F" &amp;&amp; grep -A3 '^# #4\.' "$F" | grep -q 'STRUCTURALLY-MOOT' &amp;&amp; grep -q 'D-06' "$F" &amp;&amp; echo FINDING4_OK</automated>
  </verify>
  <done>
    `spikes/01-jsonl-capture/FINDING.md` contains BOTH a `# #1.` section (from Task 2) and a `# #4.` section (this task) with `Status: STRUCTURALLY-MOOT` and the D-06 reasoning chain. Plan 05 folds both into VERIFIED-FACTS.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → spikes/01-jsonl-capture/FINDING.md (committed) | zcode rollout transcripts on disk cross into a committed evidence file; they contain the user's real prompts and possibly secrets/tokens/PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-01 | Information Disclosure | spikes/01-jsonl-capture/FINDING.md item #1 Evidence (committed) | high | mitigate | Apply D-03 sanitization checklist before commit: redact all prompt/system text, ids (session/trace/turn/request/call), token counts, tool arguments, and file paths; preserve only JSON keys + HTTP header NAMES + enum values + array lengths. The header-name set is the mimicry target (PROF-05) and is safe; prompt content is not. Plan 05 re-runs the sanitization grep over the final VERIFIED-FACTS.md as a second barrier. |
| T-00-02 | Information Disclosure | STACK.md (read-only reference) | low | accept | STACK.md is an existing committed doc with no secrets; this plan reads it but never writes it. No mitigation beyond not introducing secrets via edits. |
</threat_model>

<verification>
- `spikes/go.mod` declares the isolated module with exact version pins (no root go.mod).
- `.gitignore` ignores `spikes/go.sum` + binaries, not source or findings.
- `spikes/01-jsonl-capture/README.md` records the corrected path + schema + Phase-1 lookup table.
- `spikes/01-jsonl-capture/FINDING.md` has BOTH `# #1.` (FAILED + corrected + redacted evidence + Tier-B pointer) and `# #4.` (STRUCTURALLY-MOOT + D-06 reasoning) sections in D-02 shape, ready for Plan 05 to fold into VERIFIED-FACTS.md.
- No secrets in FINDING.md (D-03 sanitization applied).
- This plan does NOT create or modify `.planning/research/VERIFIED-FACTS.md` (Plan 05 is the single writer).
</verification>

<success_criteria>
- The throwaway module exists and is isolated (D-05 honored; clean root).
- STACK item #1's path correction is captured as dated evidence (closes item #1; unblocks MIMC-02/PROF-03/PROF-05 with corrected ground truth).
- STACK item #4 is closed structurally (D-06 honored; no spike).
- The evidence is in a per-spike file under this plan's own subdir (zero files_modified overlap with Plans 02/03/04 — true Wave-1 parallelism).
</success_criteria>

<output>
Create `.planning/phases/00-spike-re-verification/00-01-SUMMARY.md` when done.
</output>
