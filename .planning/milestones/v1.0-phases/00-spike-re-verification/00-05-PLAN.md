---
phase: 00-spike-re-verification
plan: 05
type: execute
wave: 3
depends_on: ["00-01", "00-02", "00-03", "00-04"]
files_modified:
  - .planning/phases/00-spike-re-verification/check-verified-facts.sh
  - .planning/research/VERIFIED-FACTS.md
autonomous: false
requirements: [STACK-Phase0-#1, STACK-Phase0-#2, STACK-Phase0-#3, STACK-Phase0-#4, STACK-Phase0-#5]
user_setup: []
note: "This plan is the SINGLE WRITER of .planning/research/VERIFIED-FACTS.md. Wave-1 plans (01-04) produced per-spike evidence files in their own subdirs (spikes/01-jsonl-capture/FINDING.md, spikes/0{2,3,5}-*/RESULT.md) precisely so VERIFIED-FACTS.md has one writer and Wave-1 plans have zero files_modified overlap. This plan reads those four evidence files and assembles VERIFIED-FACTS.md from them."

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "VERIFIED-FACTS.md has all 5 sections fully populated with the D-02 field schema, every Status is one of {VERIFIED, FAILED, PARTIAL, STRUCTURALLY-MOOT}, and no TBD/TODO/placeholder remains"
    - "Every embedded sample in VERIFIED-FACTS.md is sanitized per D-03 (no prompts, ids, tokens, keys, paths, tool arguments) — a secret/PII grep returns nothing"
    - "The §3 Tier-B finding (zcode JSONL path differs from STACK) has been surfaced to the user with the three D-07 options and the user's chosen resolution is recorded in item #1 Notes"
    - "The completeness gate script passes (exit 0): 5 section headers, 5 Status fields, 5 Evidence fields, no forbidden tokens"
    - "STACK.md is UNCHANGED (D-01 — VERIFIED-FACTS.md is the post-spike source of truth; STACK stays as the pre-spike recommendation)"
  artifacts:
    - .planning/phases/00-spike-re-verification/check-verified-facts.sh
    - .planning/research/VERIFIED-FACTS.md (final, complete, sanitized, user-resolution-recorded — authored by this plan from the four Wave-1 evidence files)
  key_links:
    - "check-verified-facts.sh encodes the §8/§9 completeness rules (5 sections, 5 Status, 5 Evidence, no TBD/TODO) and is the phase's exit gate"
    - "VERIFIED-FACTS.md is assembled from spikes/01-jsonl-capture/FINDING.md (items #1 + #4), spikes/02-openai-toolschema/RESULT.md (#2), spikes/03-acp-handshake/RESULT.md (#3), spikes/05-stdout-collision/RESULT.md (#5) — this plan is the single writer of VERIFIED-FACTS.md"
    - "Item #1 Notes records the user's Tier-B resolution choice (predicted: revise-and-continue), closing the loop the #1 spike opened"
---

<objective>
Integrate the four Wave-1 spike outputs into a complete, sanitized, user-approved VERIFIED-FACTS.md — and because this plan is the SINGLE WRITER of that file, it also builds the skeleton and folds all five per-spike evidence sections together. This plan also carries the D-07 Tier-B decision checkpoint for the §3 finding: the zcode JSONL path correction changes MIMC-02's wording, so the user must explicitly choose revise-and-continue / halt-and-replan / accept-and-document before Phase 1 planning.

Purpose: this is the integration task — VERIFIED-FACTS.md is the phase's actual product, and the §3 finding's user sign-off is the one human decision in the phase. The completeness gate is the binary "phase is done" check the VALIDATION.md document defines. Wave-1 plans deliberately wrote per-spike evidence files (FINDING.md / RESULT.md) instead of touching VERIFIED-FACTS.md so that Wave-1 plans have zero files_modified overlap and run in true parallel; this plan consumes all four and produces the single integrated doc.

Output: the completeness-gate shell script; the final VERIFIED-FACTS.md (skeleton + all 5 sections assembled from the four evidence files + sanitized + resolution recorded); the user's Tier-B resolution captured in item #1 Notes.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-spike-re-verification/00-CONTEXT.md
@.planning/phases/00-spike-re-verification/00-RESEARCH.md
@.planning/phases/00-spike-re-verification/00-VALIDATION.md
@AGENTS.md
@spikes/README.md
@spikes/01-jsonl-capture/FINDING.md
@spikes/02-openai-toolschema/RESULT.md
@spikes/03-acp-handshake/RESULT.md
@spikes/05-stdout-collision/RESULT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the VERIFIED-FACTS.md completeness gate script</name>
  <files>.planning/phases/00-spike-re-verification/check-verified-facts.sh</files>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §8 (the completeness gate rules: ≥5 section headers via `grep -c '^## #'`, ≥5 Status fields, ≥5 Evidence fields, no TBD/TODO/[fill in]/empty evidence) and §9 (Validation Architecture)
    - .planning/phases/00-spike-re-verification/00-VALIDATION.md (the full-suite command line + the completeness check contract)
    - .planning/research/VERIFIED-FACTS.md (current state — to confirm the script's grep patterns will match the file's actual format)
  </read_first>
  <action>
    Write an executable shell script at `.planning/phases/00-spike-re-verification/check-verified-facts.sh` that codifies the §8 completeness rules and exits non-zero on any failure. Concretely:

    1. `chmod +x` the script (it must be runnable as `bash check-verified-facts.sh`).
    2. Point at `VF="$PWD/.planning/research/VERIFIED-FACTS.md"`. Fail early with a clear message if the file is missing.
    3. Check section count: `$(grep -c '^## #' "$VF")` must be ≥ 5. (Grep hygiene per planner rules: this counts `## #N.` headings, which are the section markers; the comment-prose concern does not apply because markdown headings are structural.)
    4. Check Status fields: `$(grep -c '^- \*\*Status:\*\*' "$VF")` must be ≥ 5, AND each matched line must match one of `VERIFIED|FAILED|PARTIAL|STRUCTURALLY-MOOT`. (Use a second grep with `-E` and `-v` to assert NO Status line is outside the enum.)
    5. Check Evidence fields: `$(grep -c '^- \*\*Evidence:\*\*' "$VF")` must be ≥ 5, and assert no Evidence field is immediately followed by an empty line (the §8 rule "none followed by an empty line").
    6. Check forbidden tokens: `grep -nEi 'TBD|TODO|\[fill in\]|<empty>|pending spike completion' "$VF"` must return NOTHING. (Note: the placeholder text plans 01 wrote for #2/#3/#5 — "populated by plan 00-0X" — must be GONE by the time this gate runs, because plans 02/03/04 replaced those placeholders. The gate enforces that.)
    7. Check item #4 is STRUCTURALLY-MOOT: `grep -A3 '^## #4\.' "$VF" | grep -q 'STRUCTURALLY-MOOT'`.
    8. Check the §3 path correction is recorded: `grep -q 'model-io-sess_<session-id>.jsonl' "$VF"`.
    9. Print a one-line PASS/FAIL summary per check to stdout (this is a dev-time gate script, not a spike — stdout here is fine; the transport-discipline rule applies to the agent process, not to a verification script) and exit 0 only if ALL checks pass.

    Do NOT make the script depend on the spikes running — it gates the DOC, not the code. The spikes' own exit codes are their verification (per-plan).
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; test -x .planning/phases/00-spike-re-verification/check-verified-facts.sh &amp;&amp; bash -n .planning/phases/00-spike-re-verification/check-verified-facts.sh &amp;&amp; echo GATE_SCRIPT_SYNTAX_OK</automated>
  </verify>
  <done>
    `.planning/phases/00-spike-re-verification/check-verified-facts.sh` exists, is executable, passes `bash -n` syntax check, and encodes all 9 completeness/forbidden-token/structural checks. (It will be RUN against the completed file in Task 3.)
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Surface the §3 Tier-B finding for user resolution (D-07)</name>
  <decision>STACK's zcode JSONL transcript path (`~/.claude/projects/<munged-cwd>/<session-id>.jsonl`, message-level) is WRONG for zcode. The actual path is `~/.zcode/cli/rollout/model-io-sess_<session-id>.jsonl` with a `type: "model_io"` schema carrying full wire-level request/response (higher fidelity than STACK projected). This is a Tier-B architectural finding (a load-bearing STACK premise is wrong), favorable in direction. The user must choose how to resolve it per D-07, because it changes Phase 1's MIMC-02 requirement wording.</decision>
  <context>
    D-07 requires that any Tier-B finding STOP the phase and surface three options to the user. This is the only Tier-B finding in Phase 0 (research predicts option (a)). The resolution chosen here is recorded in VERIFIED-FACTS.md item #1 Notes in Task 3, and — if (a) or (c) — propagates as a wording correction to MIMC-02 (REQUIREMENTS.md) during Phase 1 planning. The finding itself is already recorded in VERIFIED-FACTS.md item #1 (Status FAILED + corrected path) by plan 01; this checkpoint resolves the DISPOSITION, not the evidence.
  </context>
  <options>
    <option id="option-a">
      <name>Revise-and-continue (research prediction)</name>
      <pros>The capability (extract zcode profile from on-disk logs) is fully intact and in fact EASIER than STACK projected — the rollout files capture the full request body (system, tools, messages) + identity headers, so no MITM proxy is needed for the body. Phase 1's MIMC-02 just needs its path wording corrected from `~/.claude/projects/...` to `~/.zcode/cli/rollout/model-io-sess_<id>.jsonl`. No roadmap change; no replan.</pros>
      <cons>Requires a one-line wording correction to MIMC-02 (and any PROF-03 `target_capture_ref` reference) during Phase 1 planning. The munged-cwd convention is obsolete for zcode (keys by session-id).</cons>
    </option>
    <option id="option-b">
      <name>Halt-and-replan</name>
      <pros>Only justified if the corrected path/schema somehow invalidates the Phase-1 mimicry approach (it does not, per research).</pros>
      <cons>Unnecessary disruption — the finding is favorable-direction. Research recommends against this.</cons>
    </option>
    <option id="option-c">
      <name>Accept-and-document</name>
      <pros>Records the finding without acting on it; MIMC-02 wording stays as-is and the Phase-1 researcher reads VERIFIED-FACTS.md for the truth.</pros>
      <cons>Leaves a stale path in REQUIREMENTS.md that could mislead a future reader who skips VERIFIED-FACTS.md.</cons>
    </option>
  </options>
  <resume-signal>Reply with: option-a (revise-and-continue — default), option-b (halt-and-replan), or option-c (accept-and-document). The chosen resolution is recorded in VERIFIED-FACTS.md item #1 Notes in the next task.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Author VERIFIED-FACTS.md from the four evidence files + record resolution + sanitize + gate</name>
  <files>.planning/research/VERIFIED-FACTS.md</files>
  <read_first>
    - spikes/01-jsonl-capture/FINDING.md (the D-02-shaped drafts for items #1 and #4 from plan 01)
    - spikes/02-openai-toolschema/RESULT.md (the D-02-shaped draft for item #2 from plan 02)
    - spikes/03-acp-handshake/RESULT.md (the D-02-shaped draft for item #3 from plan 03)
    - spikes/05-stdout-collision/RESULT.md (the D-02-shaped draft for item #5 from plan 04)
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §8 (D-02 section template + the 8-point D-03 sanitization checklist) and §9 (completeness gate)
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-01 new file + STACK unchanged; D-02 field schema; D-03 sanitization; D-07 Tier-B resolution recording)
    - the user's resolution choice from Task 2 (option-a / option-b / option-c)
    - .planning/research/STACK.md (to confirm it is UNCHANGED after this task — D-01)
    - .planning/phases/00-spike-re-verification/check-verified-facts.sh (the gate from Task 1)
  </read_first>
  <action>
    This plan is the SINGLE WRITER of `.planning/research/VERIFIED-FACTS.md` (per D-01; Wave-1 plans wrote only per-spike evidence files so this file has one author). Author it now from the four evidence files.

    1. Create `.planning/research/VERIFIED-FACTS.md` (D-01 — new file). File header: one-line title `# VERIFIED-FACTS — Phase 0 Re-verification` + a note that this is the post-spike source of truth (STACK.md is the pre-spike recommendation, left unchanged) + the verification date (2026-08-09).

    2. Assemble the five sections IN ORDER (#1 → #5), each under a `## #N. <title>` heading (the exact heading text the gate greps for). Fold each section's seven D-02 fields from the matching evidence file:
       - Item #1 and item #4 ← copy from `spikes/01-jsonl-capture/FINDING.md` (that file holds both `# #1.` and `# #4.` blocks).
       - Item #2 ← copy from `spikes/02-openai-toolschema/RESULT.md`.
       - Item #3 ← copy from `spikes/03-acp-handshake/RESULT.md`.
       - Item #5 ← copy from `spikes/05-stdout-collision/RESULT.md`.
       Re-head each copied block from `# #N.` to `## #N.` (one `#` level deeper) so the gate's `grep -c '^## #'` matches. Preserve the seven field labels and their content verbatim.

    3. Record the Tier-B resolution in item #1 Notes (per Task 2's outcome): append a line stating "Tier-B resolution (D-07): user chose option-<X> on 2026-08-09." and the consequence. For option-a: "→ Phase 1 MIMC-02 path wording to be corrected to `~/.zcode/cli/rollout/model-io-sess_<id>.jsonl` during Phase 1 planning; munged-cwd convention obsolete for zcode." For option-c: "→ MIMC-02 wording unchanged; Phase 1 researcher reads this section as ground truth." (option-b would halt the phase — do NOT author the file; surface to the orchestrator instead.)

    4. Run a full D-03 sanitization pass over the ENTIRE file (RESEARCH.md §8 checklist, all 8 points). For each embedded sample across items #1-#5, confirm: prompt/system text redacted; ids (session/trace/turn/request/call) redacted; token counts redacted (key kept); API keys/auth tokens redacted; file paths redacted; tool arguments redacted; JSON keys + HTTP header NAMES + enum values + array lengths PRESERVED. Fix any lapse. Each item's Evidence field must carry its redaction note.

    5. Run a secret/PII grep over the file and confirm zero matches: patterns for common API-key prefixes (`sk-`, `Bearer <long>`, `xai-`, `MINIMAX_API_KEY=...`, `GROQ_API_KEY=...`), session-id UUIDs in non-redacted form, and absolute home paths (`/Users/`). Any hit → redact and re-grep.

    6. Confirm STACK.md is UNCHANGED (D-01): `git diff --stat .planning/research/STACK.md` must show no changes from this plan. STACK stays as the pre-spike recommendation; VERIFIED-FACTS.md is the post-spike source of truth.

    7. Run the completeness gate: `bash .planning/phases/00-spike-re-verification/check-verified-facts.sh` and confirm exit 0. If it fails, fix the flagged check and re-run until it passes.

    Plausible failure tier (D-07): Tier A for sanitization/gate failures (fix and continue). The only Tier-B path through this task is if Task 2 returned option-b (halt-and-replan) — in that case, do NOT author the file; report to the orchestrator that the phase is halted per the user's Tier-B decision.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; bash .planning/phases/00-spike-re-verification/check-verified-facts.sh &amp;&amp; ! grep -qiE 'sk-[A-Za-z0-9]{10}|Bearer [A-Za-z0-9]{20}|MINIMAX_API_KEY=|GROQ_API_KEY=' .planning/research/VERIFIED-FACTS.md &amp;&amp; test -z "$(git diff .planning/research/STACK.md)" &amp;&amp; grep -qi 'Tier-B resolution\|option-' .planning/research/VERIFIED-FACTS.md &amp;&amp; echo PHASE0_GATE_PASS</automated>
  </verify>
  <done>
    `check-verified-facts.sh` exits 0; VERIFIED-FACTS.md has 5 complete sections assembled from the four Wave-1 evidence files, all Status in enum, no placeholders/forbidden tokens; the D-03 sanitization pass leaves zero secret/PII matches; the Tier-B resolution is recorded in item #1 Notes; STACK.md is byte-identical to its pre-phase state (D-01 honored).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| all spike outputs → VERIFIED-FACTS.md (committed) | this plan aggregates evidence from every spike into the single committed doc; it is the last chance to catch a secret that slipped through |
| user decision → committed record | the Tier-B resolution crosses from a human decision into the committed Notes field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-10 | Information Disclosure | VERIFIED-FACTS.md final sanitization pass | high | mitigate | Task 3 runs the full D-03 8-point checklist + a secret/PII grep (`sk-`, `Bearer <token>`, `*_API_KEY=`, raw UUIDs, `/Users/` paths) over the WHOLE file; any hit is redacted before the gate passes. This is the phase's final secret-leak barrier. |
| T-00-11 | Repudiation | Tier-B resolution recording (item #1 Notes) | low | mitigate | The chosen option + date + consequence is recorded verbatim in item #1 Notes, so the disposition is auditable. (No code/security impact; this is traceability.) |
</threat_model>

<verification>
- The completeness gate script runs and exits 0 against the final VERIFIED-FACTS.md.
- All 5 sections fully populated; all Status in {VERIFIED, FAILED, PARTIAL, STRUCTURALLY-MOOT}; no TBD/TODO/placeholder.
- D-03 sanitization: zero secret/PII grep matches across the file.
- The §3 Tier-B resolution is recorded in item #1 Notes with the user's chosen option.
- STACK.md unchanged (D-01).
</verification>

<success_criteria>
- VERIFIED-FACTS.md is complete, dated, sanitized, and gated — Phase 1 builds on grounded, dated evidence (PITFALLS N18 mitigation; ROADMAP Phase-0 goal met).
- The one Tier-B finding has an explicit, recorded user resolution (D-07 honored).
- STACK.md remains the untouched pre-spike record; VERIFIED-FACTS.md is the post-spike source of truth (D-01).
</success_criteria>

<output>
Create `.planning/phases/00-spike-re-verification/00-05-SUMMARY.md` when done.
</output>
