---
phase: 00-spike-re-verification
plan: 04
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - spikes/05-stdout-collision/main.go
  - spikes/05-stdout-collision/RESULT.md
autonomous: true
requirements: [STACK-Phase0-#5]
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A throwaway Go spike runs go-telegram/bot@v1.23.0 in a goroutine alongside canned ACP frames written to stdout, and asserts stdout is byte-equal to the canned frames (zero non-ACP bytes from the bot)"
    - "The bot goroutine exits within ~2s of context cancellation (confirms the context-first contract STACK picked the library for)"
    - "The spike routes the bot's WithErrorsHandler (and any debug output) to slog → stderr, never stdout"
    - "STACK item #5 is recorded in spikes/05-stdout-collision/RESULT.md (the per-spike evidence file) with Status VERIFIED — Plan 05 folds this into VERIFIED-FACTS.md"
  artifacts:
    - spikes/05-stdout-collision/main.go
    - spikes/05-stdout-collision/RESULT.md
  key_links:
    - "spikes/05-stdout-collision/main.go is package main inside module github.com/djarvur/ass-guard-spikes (compiles against the go.mod from plan 01)"
    - "spikes/05-stdout-collision/RESULT.md is the D-02-shaped item-#5 draft Plan 05 reads to author VERIFIED-FACTS.md item #5 (Plan 05 is the single writer of VERIFIED-FACTS.md)"
---

<objective>
Empirically verify STACK item #5: that `go-telegram/bot@v1.23.0` and ACP can coexist in one process without the bot corrupting stdout (stdout is reserved for ACP frames — PROJECT.md transport discipline, non-negotiable). This is the integration test the ROADMAP explicitly calls for.

Purpose: ass-guard runs Telegram as a peer goroutine inside the same process as an ACP stdio server. If the bot writes anything to stdout, it corrupts the ACP protocol contract. This spike PROVES the library is safe-by-default (silent when WithDebug is off) and stays clean when its error/debug handlers route to stderr. It also confirms the `context.Context`-first contract STACK picked the library for (clean shutdown on cancel).

Output: `spikes/05-stdout-collision/main.go` (throwaway) and `spikes/05-stdout-collision/RESULT.md` (the D-02-shaped item-#5 draft with the byte-diff evidence, 0 = clean) that Plan 05 folds into VERIFIED-FACTS.md.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-spike-re-verification/00-CONTEXT.md
@.planning/phases/00-spike-re-verification/00-RESEARCH.md
@AGENTS.md
@spikes/go.mod
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write and run the #5 stdout-collision integration spike</name>
  <files>spikes/05-stdout-collision/main.go</files>
  <behavior>
    - Clean-stdout assertion: after the bot goroutine runs concurrently with canned ACP frames written to stdout, stdout is byte-equal to the canned frames (zero extra bytes from the bot)
    - Default-silence assertion: with WithDebug NOT set, the bot produces no stdout writes even before handler routing
    - Handler-routing assertion: when WithErrorsHandler is registered to route to slog (stderr), a simulated internal error produces output on STDERR only, never stdout
    - Shutdown assertion: the bot goroutine exits within ~2 seconds of context cancellation (sync.WaitGroup or done channel confirms exit)
  </behavior>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §5 (the load-bearing API surface: bot.New + RegisterHandler + Start(ctx) blocks on the long-poll loop; WithDebug/WithDebugHandler/WithErrorsHandler route via callbacks; NO WithLogger, NO io.Writer, no unconditional stdout write; library is silent by default; the exact spike discipline — register WithErrorsHandler → slog/stderr, do NOT register a stdout WithDebugHandler; cancellation via context.WithCancel; the byte-equality assertion shape)
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-04 throwaway, D-07 tiers)
    - .planning/phases/00-spike-re-verification/00-VALIDATION.md row "00-05-0x" (assertion: Telegram logging → stderr, never stdout; byte-diff count 0 = clean)
    - spikes/go.mod (confirm the module + the go-telegram/bot@v1.23.0 pin)
    - AGENTS.md "Transport discipline" (stdout = ACP only — this is the load-bearing rule this spike proves)
  </read_first>
  <action>
    Write a throwaway Go program (package main) under `spikes/05-stdout-collision/main.go` in module `github.com/djarvur/ass-guard-spikes`. It runs go-telegram/bot in a goroutine alongside canned ACP frames and asserts stdout stays clean.

    Concrete shape (RESEARCH.md §5):
    1. Capture stdout via an `os.Pipe` or `io.TeeReader`/buffer so the spike can byte-compare after the run. (Redirect real stdout to a pipe; write the canned ACP frames to the write-end; read the canonical bytes back from the read-end.) The bot must NOT be given the write-end — it should have no stdout handle at all.
    2. Build the bot with `bot.New("DUMMY_TOKEN_FOR_SPIKE_NO_NETWORK", bot.WithErrorsHandler(func(ctx, err){ slog.Error("telegram", "err", err) }))`. Do NOT pass `WithDebug()` (default silence). Do NOT pass any handler writing to stdout. The token is a dummy — the spike must NOT make a real network call (use a token that fails fast, or stub the long-poll, so the goroutine spends the test window idle/cancelling rather than hitting Telegram's API). If the library's Start() blocks on a real GetMe call that fails without a valid token, structure the test so the goroutine is started, the canned frames are written, the assertion is taken, THEN context is cancelled — the bot's startup error (if any) is exactly what WithErrorsHandler should route to stderr, and that is part of what the assertion checks.
    3. `go b.Start(telegramCtx)` in a goroutine tracked by a `sync.WaitGroup` (or a done channel) so the spike can confirm exit.
    4. On the main goroutine, write a sequence of canned ACP frames to stdout (the captured/redirected write-end): e.g. an `initialize` response, a `session/update` notification, a `session/prompt` response — each a single JSON object terminated by `\n` (the exact framing from RESEARCH.md §2).
    5. Assert: the bytes read back from stdout's read-end are byte-equal to the canned frames the spike wrote (use `bytes.Equal` or compute a diff/extra-byte count). The assertion passes iff the extra-byte count is 0.
    6. Assert: after `cancel()` on telegramCtx, the bot goroutine exits within ~2s (`wg.Wait()` with a 2s timeout, or a select on the done channel with `time.After(2*time.Second)`).
    7. Assert: any output the bot produced (the simulated error from the dummy token) appeared on STDERR (via slog), NOT on stdout.
    8. Print to STDERR: the canned frame count, the stdout extra-byte count (must be 0), the bot exit timing, per-assertion PASS/FAIL, and the structured `=== SPIKE 05 RESULT ===` footer.

    Transport discipline (AGENTS.md, load-bearing): this spike's ENTIRE POINT is proving stdout stays clean. The canned ACP frames are the ONLY thing the spike writes to stdout. Everything else — slog, the error handler, the result footer, PASS/FAIL — goes to stderr. The verify step will assert stdout (outside the captured comparison) carries only the canned frames.

    Plausible failure tier (D-07): Tier A. If the bot DOES write to stdout (e.g. an unconditional print the research missed), the FIX is routing the responsible handler to stderr — which is exactly the mitigation this spike exists to validate; record FAILED + the fix, CONTINUE. If the bot does NOT exit on cancel (library contract bug), that is also Tier A — record FAILED + the mitigation (harder shutdown), continue. Research predicts VERIFIED: the library is silent by default and routes everything through callbacks. This is the lowest-risk Tier in the phase.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent/spikes &amp;&amp; go vet ./05-stdout-collision/ &amp;&amp; go run ./05-stdout-collision/ 2&gt;/tmp/spike05.out &amp;&amp; grep -q '=== SPIKE 05 RESULT ===' /tmp/spike05.out &amp;&amp; grep -qiE 'extra.byte.*0|stdout.*clean|byte.equal' /tmp/spike05.out &amp;&amp; echo SPIKE05_OK</automated>
  </verify>
  <done>
    `go run ./spikes/05-stdout-collision/` exits 0; its stderr footer shows the stdout extra-byte count is 0 (clean) and the bot goroutine exited within ~2s of cancel; all diagnostic output is on stderr; the only stdout bytes are the canned ACP frames the spike itself wrote.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the item #5 finding to spikes/05-stdout-collision/RESULT.md</name>
  <files>spikes/05-stdout-collision/RESULT.md</files>
  <read_first>
    - .planning/phases/00-spike-re-verification/00-RESEARCH.md §5 (the API-surface finding to cite: silent by default, callbacks for debug/errors, no WithLogger/io.Writer) and §8 (D-02 section template + D-03 checklist)
    - .planning/phases/00-spike-re-verification/00-CONTEXT.md (D-02, D-03, D-07)
    - the spike's stderr output from Task 1 (the byte-diff count + exit timing + PASS/FAIL)
  </read_first>
  <action>
    Write `spikes/05-stdout-collision/RESULT.md` as the DRAFT VERIFIED-FACTS.md content for item #5 (Plan 05 will fold this in verbatim-ish). This plan does NOT touch VERIFIED-FACTS.md — Plan 05 is the single writer of that file (keeps Wave-1 files_modified non-overlapping).

    Structure RESULT.md with the D-02 fields so Plan 05 can copy block-by-block: a heading `# #5. go-telegram/bot + ACP stdout collision`, then the seven fields:
    - `Fact`: quote STACK item #5 verbatim (the go-telegram/bot row + the "Telegram frontend must never write to stdout" Version-Compatibility line).
    - `Source`: STACK.md §"Open Verification Items" item #5 + the go-telegram/bot table rows (~lines 45, 63, 192).
    - `Verified`: 2026-08-09.
    - `Verified against`: `github.com/go-telegram/bot@v1.23.0` (released 2026-08-03), via the integration spike (bot goroutine + canned ACP frames + byte-equal assertion).
    - `Status`: `VERIFIED` (the spike proved stdout stays clean); `FAILED` + Tier-A fix in Notes only if the bot wrote to stdout (then record the handler-routing mitigation).
    - `Evidence`: inline fenced block with (a) the canned ACP frame sequence the spike wrote to stdout, (b) the stdout extra-byte count (0), (c) the bot exit timing after cancel, (d) the PASS lines. Sanitize per D-03: the dummy token must NOT appear (replace with `[REDACTED: dummy token]`); redact any real path. Preserve the assertion structure and counts. Redaction note in Evidence.
    - `Notes`: (a) the load-bearing finding — go-telegram/bot is SILENT BY DEFAULT (no WithDebug → no stdout writes), and routes all debug/errors through `WithDebugHandler`/`WithErrorsHandler` callbacks (NO `WithLogger`, NO `io.Writer`, no unconditional stdout print found); (b) the mitigation recipe — register `WithErrorsHandler` → slog → stderr; do NOT register a stdout-writing debug handler; (c) the context-first contract confirmed — the bot goroutine exits within ~2s of context cancel; (d) the downstream implication — ass-guard's Phase-? Telegram frontend follows this exact pattern; the transport discipline (stdout = ACP only) holds with go-telegram/bot in-process; (e) Tier disposition (VERIFIED, or Tier-A FAILED + fix if applicable).

    Sanitization (D-03): grep RESULT.md for the dummy token string and any real paths before commit — redact. (Plan 05 re-runs the grep over the merged VERIFIED-FACTS.md as a second barrier.)
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; F=spikes/05-stdout-collision/RESULT.md &amp;&amp; grep -q '^# #5\.' "$F" &amp;&amp; grep -q '\*\*Status:\*\*' "$F" &amp;&amp; grep -qE 'VERIFIED|FAILED' "$F" &amp;&amp; grep -q 'go-telegram/bot@v1.23.0\|v1.23.0' "$F" &amp;&amp; grep -qiE 'stderr|stdout.*clean|extra.byte.*0' "$F" &amp;&amp; echo RESULT5_OK</automated>
  </verify>
  <done>
    `spikes/05-stdout-collision/RESULT.md` has a `# #5.` heading with all seven D-02 fields; Status VERIFIED (or FAILED + Tier-A fix); `Verified against` cites go-telegram/bot@v1.23.0; Evidence shows the byte-clean assertion (0 extra bytes) + bot exit timing; Notes record the silent-by-default finding + the handler-routing recipe; no dummy token in the file. Plan 05 folds this into VERIFIED-FACTS.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot goroutine → process stdout | go-telegram/bot runs inside the same process whose stdout is the ACP channel; any bot stdout write corrupts the protocol |
| spike output (stderr) → spikes/05-stdout-collision/RESULT.md (committed) | the spike result + any captured bot error cross into a committed evidence file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-08 | Tampering / protocol violation | spikes/05-stdout-collision stdout integrity | high | mitigate | This IS the security-adjacent assertion: stdout must be byte-equal to the canned ACP frames. The spike captures stdout via os.Pipe and asserts zero extra bytes. The bot's WithErrorsHandler routes to slog→stderr; no stdout-writing debug handler is registered. |
| T-00-09 | Information Disclosure | spikes/05-stdout-collision dummy bot token + RESULT.md | low | mitigate | Use a clearly-dummy placeholder token (no network call); D-03-redact it to `[REDACTED: dummy token]` before commit so no token-like string lands in the repo. Plan 05 re-runs the grep over the merged VERIFIED-FACTS.md. |
</threat_model>

<verification>
- `go run ./spikes/05-stdout-collision/` exits 0; stderr footer reports stdout extra-byte count 0 and bot exit within ~2s.
- The only stdout bytes are the canned ACP frames the spike wrote (transport discipline proven).
- `spikes/05-stdout-collision/RESULT.md` has the complete D-02-shaped item #5 draft; Status VERIFIED; the silent-by-default + handler-routing recipe recorded.
- No dummy token in the committed file.
- This plan does NOT create or modify `.planning/research/VERIFIED-FACTS.md` (Plan 05 is the single writer).
</verification>

<success_criteria>
- STACK item #5 closed: go-telegram/bot@v1.23.0 and ACP coexist in one process without stdout collision; the transport discipline (stdout = ACP only) holds.
- The context-first shutdown contract confirmed (bot exits on cancel).
- The mitigation recipe (errors → slog → stderr) recorded for the Phase-? Telegram frontend.
</success_criteria>

<output>
Create `.planning/phases/00-spike-re-verification/00-04-SUMMARY.md` when done.
</output>
