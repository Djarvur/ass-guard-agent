---
phase: 12-product-functional-completeness
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/ass-guard/acp_serve.go
  - cmd/ass-guard/planmode_wiring_test.go
autonomous: true
gap_closure: true
gap_ids: [G-12-3]
requirements: [ACP-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - On a live acp serve session, EnterPlanMode flips plan mode ON — a plan_mode_enter marker lands in the session transcript and the ON state rides TurnOutput.PlanMode as engine-decision provenance
    - While plan mode is ON, a mutating tool call is refused WITHOUT executing, the captured refusal form landing as the model-visible tool_result
    - ExitPlanMode surfaces the approval question to the ACP client and suspends the turn; an approving reply resumes the SAME turn, ungates mutating tools, records the plan_mode_exit marker, and the approved-form tool_result reaches the model
  artifacts:
    - cmd/ass-guard/acp_serve.go — sessionFor wires the per-session plan-mode state onto the Session
    - cmd/ass-guard/planmode_wiring_test.go — server-level regression battery driving the REAL acp.Server over stdio pipes
  key_links:
    - sessionFor → SetPlanMode(planMode) → Session.planMode (today nil ⇒ the gate at session.go planModeBlocks never fires and the Enter flip at the tool-result site is skipped)
    - plan_mode marker writers (enter/exit causes) ↔ the TurnOutput.PlanMode reconstruction scan in acp_serve.go (~1854-1890)
---

<objective>
Close UAT gap G-12-3 (test 3, severity major): plan mode is dead on the production serve path. `sessionFor` builds `planMode := session.NewPlanModeState()` and hands it to `coreexec.RegisterInteractive` (so ExitPlanMode's executor SEES it), but never calls the Session setter — `Session.planMode` stays nil, so the Enter flip at the tool-result site is skipped (no plan_mode_enter marker), the runtime-level mutating-tool gate never fires, and ExitPlanMode answers "not in plan mode". Exactly the operator-reported failure: EnterPlanMode rendered its form twice, no marker, an Edit sailed through, Exit errored.

Purpose: ACP-02's captured runtime-level plan-mode enforcement (12-04/12-05, scope resolved BY CAPTURE) is a safety-relevant product behavior that silently does not exist in production. Tests pass because they wire the state themselves (internal/session/planmode_test.go:86 calls the setter); the serve path omits it.

Output: one wiring line + a server-level regression battery proving the FULL flow through the real acp.Server: Enter → marker + gate refusal → Exit approval suspension → approval resumes same turn, state OFF, exit marker.
</objective>

<execution_context>
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/workflows/execute-plan.md
@/Users/nil/DiskD/W/Djarvur/ass-guard-agent/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-product-functional-completeness/12-UAT.md (## Gaps → G-12-3)
@cmd/ass-guard/ask_wiring_test.go (TestAskWiring_ServerLevelSurface at :584 — THE model for the regression test: real acp.NewServer over io.Pipes, scripted fake provider, sendFrame/readFrames helpers, setupEngine engine-on runner)
@cmd/ass-guard/acp_serve.go (:1270 planMode construction; :1340-1349 the SetAskBroker wiring block where the fix lands; :1854-1890 the planModeOn reconstruction scan)
@internal/session/planmode.go (SetPlanMode :122, PlanModeState, captured refusal/approved forms, marker causes)
@internal/session/session.go (:466-471 the planModeBlocks refusal branch; :529-533 the Enter-flip + marker site — both keyed on s.planMode != nil)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — server-level plan-mode flow battery (fails exactly as the UAT reported)</name>
  <files>cmd/ass-guard/planmode_wiring_test.go</files>
  <behavior>
    - Test PlanModeWiring_ServerLevelFlow drives the REAL acp.Server over io.Pipes (clone the harness shape of TestAskWiring_ServerLevelSurface: engine-on sessionTurnRunner with setupEngine, a scripted fake provider serving successive model responses, initialize → session/new → session/prompt frames).
    - Leg 1 (enter): model responds with an EnterPlanMode tool call → the session transcript gains a plan_mode line with cause plan_mode_enter, and EnterPlanMode's captured guidance form (PlanModeEnteredForm) lands as the tool_result.
    - Leg 2 (gate): model responds with a mutating call (Edit on a scratch file under the runner workDir) → the file is NOT modified, the tool_result carries the captured refusal text defined in internal/session/planmode.go, and the model's next projected request contains that refusal (assert via the fake provider's captured request bodies, the established pattern).
    - Leg 3 (exit + approval): model responds with ExitPlanMode carrying a short plan → the client receives the approval-question surface chunk ("Approve this implementation plan?" prefix from planApprovalQuestion) and the turn suspends (prompt response returns at the suspension per the 13-00 park contract); the client replies with "approve" → the SAME turn resumes, the approved form from RenderPlanApprovalResolved lands as the tool_result, a plan_mode line with cause plan_mode_exit is appended, and a subsequent mutating call in the resumed turn EXECUTES (scratch file byte-check) proving the gate lifted.
    - Provenance leg: the terminal TurnOutput for the resumed turn reports PlanMode=false, and an enter-only variant reports PlanMode=true (covers the acp_serve.go reconstruction scan end-to-end).
    - RED expectation: with the current tree the battery FAILS at leg 1/2 in exactly the diagnosed shape — no plan_mode_enter marker, no refusal (mutating call executes), ExitPlanMode yielding the outside-plan-mode error.
  </behavior>
  <action>
    Create cmd/ass-guard/planmode_wiring_test.go following the TestAskWiring_ServerLevelSurface harness conventions (same helpers: sendFrame, readFrames, rawJSON, the method/key constants; a dedicated fake provider scripting the call sequence leg by leg, recording every provider request body for the refusal-visibility assertion). Drive the whole scenario over the REAL server pipes — no direct Session construction, no manual SetPlanMode anywhere in the test (that self-wiring is precisely the blind spot that hid the bug). Assert transcript lines by reading the session transcript the way existing cmd/ass-guard tests do (the manager path under the runner workDir). Commit RED separately before any production change, per house TDD discipline.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; go test ./cmd/ass-guard/ -run 'TestPlanModeWiring_ServerLevelFlow' -count=1 2&gt;&amp;1 | tee /tmp/12-09-red.txt; grep -q 'FAIL' /tmp/12-09-red.txt</automated>
  </verify>
  <done>The battery compiles and FAILS with the diagnosed signature (dead gate, absent markers) — the regression test reproduces UAT test 3 mechanically.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — wire SetPlanMode in sessionFor beside SetAskBroker</name>
  <files>cmd/ass-guard/acp_serve.go</files>
  <action>
    In sessionFor's post-literal wiring block (immediately beside the existing SetAskBroker call, ~line 1345), add the missing Session setter call passing the already-built per-session plan-mode state variable from the RegisterInteractive block above (~line 1270). One line; no other behavior change. Comment it as the G-12-3 closure (the 12-04 captured runtime-level gate, ACP-02) referencing that RegisterInteractive receiving the state alone was insufficient — the Session field is what the gate and the Enter-flip read. Then re-run the Task 1 battery to green, run the FULL existing plan-mode/ask batteries to prove nothing else moved (TestAskWiring_*, the internal/session planmode battery — all stay green UNMODIFIED), and close with the full CI gate.
  </action>
  <verify>
    <automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent &amp;&amp; go test ./cmd/ass-guard/ -run 'TestPlanModeWiring|TestAskWiring|TestAskPark|TestPlanMode' -count=1 &amp;&amp; go test ./internal/session/ -count=1 &amp;&amp; mise ci</automated>
  </verify>
  <done>The full server-level flow passes through the real acp.Server: Enter flips state ON with its marker; mutating calls are refused with the captured form while ON; ExitPlanMode suspends on the approval question; approval resumes the same turn, lifts the gate, records the exit marker, and the approved form reaches the model. Every prior ask/plan-mode test green unmodified; mise ci green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model → session state | The model toggles its own plan-mode state via EnterPlanMode/ExitPlanMode; the gate is the only thing keeping declared-read-only phases from mutating |
| editor client → session | The approval reply arrives as a client prompt and resolves the pending approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09-01 | Elevation of Privilege (tampered safety state) | cmd/ass-guard/acp_serve.go sessionFor | high | mitigate | The missing wire made the captured runtime-level mutating-tool gate inert in production — this plan's entire scope; the server-level battery is the permanent regression pin |
| T-12-09-02 | Tampering | plan-mode approval resolution | low | accept | Approval matching stays inside RenderPlanApprovalResolved's existing label set; unchanged by this closure |
| T-12-SC | Tampering | dependency installs | high | mitigate | Zero new dependencies (cross-phase v1.1 invariant; Go stdlib + existing modules only) |
</threat_model>

<verification>
- RED commit precedes the GREEN wiring commit (house TDD discipline; evidence in /tmp/12-09-red.txt preserved into the SUMMARY).
- Full flow green over the REAL acp.Server: enter marker → gate refusal → approval suspension → same-turn resume + gate lift + exit marker.
- Reconstruction scan coverage: TurnOutput.PlanMode true after enter-only, false after approved exit.
- Prior batteries green UNMODIFIED: TestAskWiring_* , TestAskPark_*, internal/session planmode battery.
- mise ci green.
</verification>

<success_criteria>
- G-12-3 closed: the UAT truth "EnterPlanMode flips session state ON (plan_mode_enter marker in transcript); mutating calls refused with captured form while ON; ExitPlanMode surfaces plan approval and suspends" holds on the PRODUCTION serve path, not only in unit-wired tests.
- The defect class (wired-in-tests-only state) is pinned by a test that cannot pass without the production wiring line.
- Zero regressions in the ask/park/plan-mode surfaces; mise ci green.
</success_criteria>

<output>
Create .planning/phases/12-product-functional-completeness/12-09-SUMMARY.md when done (list gap_ids [G-12-3], the RED evidence path, and the wiring-line commit hash).
</output>
