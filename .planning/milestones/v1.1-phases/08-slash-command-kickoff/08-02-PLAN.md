---
phase: 08-slash-command-kickoff
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/toolexec/backend.go
  - internal/toolexec/backend_test.go
  - internal/toolexec/ddg.go
  - internal/toolexec/ddg_test.go
  - internal/toolexec/real.go
  - internal/toolexec/real_test.go
requirements: [CMD-07]
autonomous: true   # offline HTML-fixture tests only; no network in CI; keyed APIs stay config-swappable and unset
must_haves:
  truths:
    - "A zero-config first run has working WebSearch: the default backend scrapes DDG's HTML endpoint with no API key and returns structured results (D-07, CMD-07)"
    - "WebFetch returns clean markdown: HTML bodies convert via JohannesKaufmann/html-to-markdown before reaching the model (D-08, CMD-07)"
    - "Operators swap backends by config only (keyed APIs remain config entries on the same seam); the default is used exactly when no config entry overrides it"
    - "All web-tool tests run offline against committed HTML fixtures — CI never depends on the network (the real-binary/live-service evidence rule is honored by the phase gate in 08-06, not by flaky network tests)"
  artifacts:
    - path: "internal/toolexec/ddg.go"
      provides: "DefaultBackend: Search = DDG HTML endpoint scrape parsed with golang.org/x/net/html into [{title,url,snippet}] JSON; Fetch = GET + content-type-aware html→markdown conversion (JohannesKaufmann) with SSRF guards"
      min_lines: 120
    - path: "internal/toolexec/backend.go"
      provides: "selectBackend gains 'ddg' name; empty/unset name resolves to the DDG default (zero-config)"
    - path: "internal/toolexec/real.go"
      provides: "RealExecutor falls back to the default backend when Backends has no entry for WebSearch/WebFetch (config override still wins)"
  key_links:
    - from: "internal/toolexec/real.go:Execute"
      to: "internal/toolexec.DefaultBackend"
      via: "fallback when Backends[tool] absent"
      pattern: "DefaultBackend\\(\\)"
    - from: "internal/toolexec/ddg.go"
      to: "html.duckduckgo.com/html/"
      via: "HTTP GET with browser-like User-Agent, query URL-encoded"
      pattern: "duckduckgo"
---

# Plan 08-02 — Real web-tool backends: DDG-HTML WebSearch + markdown WebFetch (zero-config)

## Phase Goal

**As a** developer practicing SDD, **I want to** kick off and drive an entire OpenSpec change (`/opsx:explore → propose → apply → archive`) from ass-guard by typing the toolkit's own slash-commands, **so that** the unmodified workflow runs hands-off.

**This plan's slice:** the user added CMD-07 because `/opsx:explore` workflows need working WebSearch/WebFetch — today the seam exists (`toolexec.Backend`, stub-proven in v1.0) but zero-config runs get "no implementation yet" errors. This plan ships the real default backend: DDG-HTML search (zero key, predecessor-proven) and fetch+html→markdown, both on the existing swappable seam.

## Truths

- Carries CMD-07 entirely (DDG-HTML default backend on the swappable seam; WebFetch fetch + html→markdown; keyed APIs remain config-swappable; zero-config first run unaffected — it becomes WORKING, where today it errors).
- Implements D-07 (DDG default, zero key, swappable seam) and D-08 (JohannesKaufmann/html-to-markdown — already vetted in STACK research).
- Wave-1 parallel-safe: everything lives in `internal/toolexec` + go.mod; the default lands INSIDE the executor/backend selection so `cmd/ass-guard/acp_serve.go` (Plan 08-04/08-05 territory) is untouched.
- New dependencies are exactly the STACK-vetted pair: `github.com/JohannesKaufmann/html-to-markdown` (HIGH confidence, STACK supporting libraries) and `golang.org/x/net/html` (canonical golang.org, already the STACK-recommended HTML parser for the WebSearch/WebFetch port). No [SUS]/[ASSUMED] packages — no legitimacy checkpoints required.

<objective>
Make WebSearch and WebFetch actually work in a zero-config first run: add a `DefaultBackend` to `internal/toolexec` implementing Search as a DuckDuckGo HTML-endpoint scrape (GET `https://html.duckduckgo.com/html/?q=<query>`, parse anchors with `golang.org/x/net/html`, return a JSON array of {title,url,snippet}) and Fetch as a plain GET whose HTML response converts to markdown via `JohannesKaufmann/html-to-markdown` (non-HTML content passes through bounded raw); wire it as the default in `selectBackend("")` and as RealExecutor's fallback when no configured backend exists, so config overrides keep working untouched (D-07 seam discipline). All parsing tests run against committed HTML fixtures; SSRF guards (http/https only, private/loopback ranges refused) protect the model-controlled URL path.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-slash-command-kickoff/08-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
From the current code (read these before editing):
```go
// internal/toolexec/backend.go — the swappable seam (D-22 / TOOL-05)
type Backend interface {
    Name() string
    Search(ctx context.Context, query string) (json.RawMessage, error)
    Fetch(ctx context.Context, target string) (json.RawMessage, error)
}
type HTTPBackend struct { Client *http.Client; SearchURLTemplate, FetchURLTemplate string }
func BackendsFromConfig(cfg map[string]string) (map[string]Backend, error)   // keys "websearch"/"webfetch" → "http"|"firecrawl"
func selectBackend(name, tool string) (Backend, error)                       // "" → HTTPBackend (template-less = broken) — THIS changes to the DDG default

// internal/toolexec/real.go — the dispatch under change
type RealExecutor struct { Catalog *toolcat.Catalog; Backends map[string]Backend; Log *slog.Logger }
func (r *RealExecutor) Execute(ctx, name, input) (json.RawMessage, error)
//   line 45: `if name == "WebSearch" { if be, ok := r.Backends["WebSearch"]; ok { return be.Search(...) } }`
//   — when Backends is nil (the acp_serve construction today) the lookup misses and the call
//     falls through to the catalog's nil-Execute error. The fallback fixes zero-config.
//   helpers: extractQuery(input), extractURL(input) already parse tool inputs.
```

DDG-HTML contract (predecessor-proven pattern, STACK research): GET `https://html.duckduckgo.com/html/?q=<urlencoded>` with a browser-like User-Agent; results are anchors with class `result__a` (href may be a `/l/?uddg=<encoded>` redirect — unwrap to the real URL) + sibling `result__snippet` text. Fetch: `https://lite.duckduckgo.com/lite/?q=` is the fallback endpoint if html/ changes shape (note in code comment; the parser is fixture-pinned so drift is caught by regenerating fixtures, not by CI network calls).
</interfaces>
</context>

<tasks>

<task type="tdd" id="T1">
  <name>Task 1: Add the two STACK-vetted deps + verify html-to-markdown API surface</name>
  <files>go.mod, go.sum</files>
  <action>
    `go get github.com/JohannesKaufmann/html-to-markdown@latest` and `go get golang.org/x/net/html` (capture resolved versions in go.mod; both are STACK-researched — html-to-markdown HIGH confidence, x/net canonical). Verify the conversion API surface before depending on it further: confirm package import path `github.com/JohannesKaufmann/html-to-markdown` exposes a converter usable as `mdconverter.ConvertString(ctx?, html)` (check `go doc` for the exact constructor — v1 API is `md.NewConverter(ctx, enableCommonmark, options...)` returning a `*md.Converter` with `ConvertString(string) (string, error)`; if the module has moved to a `v2`/different shape, record the ACTUAL names in a short code comment in ddg.go and use those). Run `go mod tidy && go build ./... && go vet ./...`.
  </action>
  <read_first>
    - go.mod (current deps — confirm neither dep is present; Go 1.26 toolchain per .mise.toml)
    - .planning/research/STACK.md (§Existing Dependencies Confirmed for New Use / supporting-libraries rows for both packages)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go get github.com/JohannesKaufmann/html-to-markdown@latest golang.org/x/net/html && go mod tidy && go build ./... && go vet ./... && go doc github.com/JohannesKaufmann/html-to-markdown | head -20</automated></verify>
  <done>Both deps pinned in go.mod; build+vet green; the exact converter constructor documented for T2</done>
  <acceptance_criteria>
    - `grep "JohannesKaufmann/html-to-markdown" go.mod` returns a pinned version line
    - `grep "golang.org/x/net" go.mod` returns a version line
    - `go build ./...` exits 0
  </acceptance_criteria>
</task>

<task type="tdd" id="T2">
  <name>Task 2: DefaultBackend — DDG-HTML Search + markdown Fetch with SSRF guards (RED→GREEN)</name>
  <files>internal/toolexec/ddg.go, internal/toolexec/ddg_test.go</files>
  <behavior>
    - Test 1 (Search parses, offline): given a committed fixture `testdata/ddg-results.html` (a REAL captured DDG html response — capture once via curl during implementation, commit it), `Search` (with the HTTP layer faked by injecting the fixture through a `fetchHTML func(ctx, url) ([]byte, error)` seam) returns a JSON array with >= 3 entries each having non-empty `title`, resolved absolute `url` (uddg-unwrapped), and `snippet`.
    - Test 2 (Search shape): the returned JSON is exactly `[{"title":…,"url":…,"snippet":…}, …]` — model-facing shape pinned.
    - Test 3 (Fetch markdown, offline): given fixture HTML (`<h1>Title</h1><p>Some <b>bold</b> text</p>`), `Fetch` returns markdown containing `# Title` and `**bold**`.
    - Test 4 (Fetch non-HTML passthrough): a `text/plain` body returns the raw text (no conversion), bounded to a size cap.
    - Test 5 (SSRF guard): `Fetch` on `http://127.0.0.1/x`, `http://localhost/x`, `http://169.254.169.254/latest/meta-data`, and `http://10.0.0.1/x` errors WITHOUT any network attempt (guard runs before the GET); `https://example.com/` passes the guard. Same guard applies to Search's endpoint? No — Search targets the fixed DDG host (no user URL); the guard is Fetch-only and unit-tested via an injectable resolver or pre-parsed host check.
    - Test 6 (query encoding): the URL passed to the fetch seam for Search contains `q=` + the urlencoded query.
  </behavior>
  <action>
    Create `internal/toolexec/ddg.go`: `type DefaultBackend struct { Client *http.Client; fetchHTML func(ctx context.Context, url string) ([]byte, string, error) }` (the unexported seam returns body + detected content-type; production default uses net/http with a 30s-timeout client and browser-like User-Agent — the seam keeps tests offline). `Name() "ddg"`. `Search`: build `https://html.duckduckgo.com/html/?q=` + url.QueryEscape(query), fetchHTML, parse with `golang.org/x/net/html` walking for anchors carrying class `result__a`, unwrap `href` values of the form `/l/?uddg=<encoded>` (parse the `uddg` query param, url-decode), collect the sibling snippet node text (class `result__snippet`), marshal `[]{Title, URL, Snippet string}` with json tags `title/url/snippet`. `Fetch`: validate scheme http/https AND resolve the host refusing loopback/private/link-local ranges (net.IP checks via a small `isPublicHost(host)` helper — resolve once, check all A/AAAA records) BEFORE any request; then fetchHTML; if content-type is HTML → convert via the html-to-markdown converter (T1-verified constructor) and return `{"content": "<markdown>"}` (or the plain markdown string — match the existing WebFetch result shape in coretools.json/the v1.0 stub tests; keep whatever the catalog schema promised); else return the raw body truncated at a documented cap (e.g. 512 KiB). Keep everything stderr-logged via slog, never stdout.
  </action>
  <read_first>
    - internal/toolexec/backend.go (Backend interface + HTTPBackend — the seam being joined)
    - internal/toolexec/real.go (extractQuery/extractURL input shapes the backend's callers use)
    - .planning/research/STACK.md (WebSearch/WebFetch tool rows: DDG predecessor pattern + html-to-markdown recommendation)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/toolexec/ -race -run 'TestDDG|TestDefaultBackend|TestFetchMarkdown|TestSSRF' -v 2>&1 | tail -20</automated></verify>
  <done>All six behavior tests green offline; DDG fixture committed; SSRF guard proven to fire pre-request</done>
  <acceptance_criteria>
    - `internal/toolexec/ddg.go` exists with `Name() string { return "ddg" }` (or equivalent)
    - `internal/toolexec/testdata/ddg-results.html` committed (real captured response)
    - `grep -n "uddg" internal/toolexec/ddg.go` shows redirect unwrapping
    - `grep -n "isPublicHost\|Loopback\|IsPrivate" internal/toolexec/ddg.go` shows the SSRF guard
    - `go test ./internal/toolexec/ -race` exits 0 with NO network access (test via `go test` in the normal env — fixtures only)
  </acceptance_criteria>
</task>

<task type="tdd" id="T3">
  <name>Task 3: Default selection + RealExecutor fallback (zero-config first run works) (RED→GREEN)</name>
  <files>internal/toolexec/backend.go, internal/toolexec/real.go, internal/toolexec/backend_test.go, internal/toolexec/real_test.go</files>
  <behavior>
    - Test 7 (default selection): `selectBackend("", "websearch")` and `BackendsFromConfig(map[string]string{})` yield the DDG default backend (Name() == "ddg"); an explicit `"http"` config still selects HTTPBackend (override preserved).
    - Test 8 (executor fallback): a `RealExecutor` with `Backends: nil` executing `WebSearch` with input `{"query":"mimicry"}` returns the DDG-shaped result when the default backend's fetch seam is faked (inject through a test hook or a package-level constructor `NewRealExecutorWithDefaultBackend`); with `Backends: {"WebSearch": fakeBe}` configured, the FAKE wins (config override beats default).
    - Test 9 (unknown backend name still errors): `BackendsFromConfig({"websearch": "bogus"})` returns the existing structured ConfigError (loud misconfiguration is preserved — T-04-03b mitigation intact).
  </behavior>
  <action>
    In `selectBackend`, map `""` (and the new explicit name `"ddg"`) to the DefaultBackend (keep `"http"` and `"firecrawl"` untouched). In `RealExecutor.Execute`, change the two backend branches from `if be, ok := r.Backends[name]; ok` to: use `r.Backends[name]` when present, else the package default (a lazily-built `DefaultBackend` value — keep it cheap/idempotent; an injectable seam/var keeps tests offline). This is the zero-config fix: the acp_serve construction (`RealExecutor{Catalog, Log}` with nil Backends) starts working with no wiring change and no config — and operators who configure `http`/`firecrawl`/future keyed backends keep exact current behavior (D-07: keyed APIs remain config-swappable).
  </action>
  <read_first>
    - internal/toolexec/real.go (Execute lines 44-56 — the two branches under change)
    - internal/toolexec/backend.go (selectBackend + BackendsFromConfig)
    - internal/toolexec/backend_test.go + real_test.go (existing table tests to extend, not replace)
  </read_first>
  <verify><automated>cd /Users/nil/DiskD/W/Djarvur/ass-guard-agent && go test ./internal/toolexec/ -race -v 2>&1 | tail -15 && golangci-lint run internal/toolexec/...</automated></verify>
  <done>Zero-config WebSearch/WebFetch return real backend results in tests; configured backends override; unknown names still ConfigError</done>
  <acceptance_criteria>
    - `grep -n '"ddg"' internal/toolexec/backend.go` shows the selection case
    - RealExecutor fallback test proves nil-Backends WebSearch no longer returns "no implementation yet"
    - `go test ./internal/toolexec/ -race` and `golangci-lint run internal/toolexec/...` exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-controlled query/URL → backend HTTP | The model composes WebSearch queries and WebFetch URLs; both reach the network. |
| external web content → model context | Fetched HTML/markdown and DDG results are untrusted content delivered to the model. |
| config → backend selection | Operator config names the backend; misconfiguration must fail loudly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-05 | Information disclosure / SSRF | WebFetch URL pointing at internal services (cloud metadata, loopback admin panels) | mitigate | `isPublicHost` guard refuses loopback/private/link-local/metadata ranges BEFORE any request (Test 5); scheme restricted to http/https. |
| T-8-06 | Tampering (prompt injection) | web content crafted as instructions | accept | Fetched content is untrusted model input — identical trust class as every other tool result (Read/Bash); no sanitization (fidelity over filtering, same disposition as MCP results T-5-03). |
| T-8-07 | DoS | huge responses / slow endpoints | mitigate | 30s client timeout (existing pattern) + response size cap on the passthrough path; bounded parse (x/net/html streaming, not regex-over-gigabytes). |
| T-8-08 | Spoofing | DDG HTML shape drift silently emptying results | mitigate | fixture-pinned parser tests catch shape drift when fixtures are regenerated; empty-result is a benign degradation (model adapts per D-10-style structured result, not a crash); the lite endpoint documented as fallback. |
| T-8-09 | Elevation | config-injected backend names | mitigate | unknown names already fail loudly via ConfigError (Test 9 preserves T-04-03b); backend names are operator-configured, never model-controlled. |
</threat_model>

<verification>
- `go test ./internal/toolexec/ -race -v` — DDG parse, markdown conversion, SSRF guard, default selection, executor fallback all green offline
- `go vet ./... && golangci-lint run internal/toolexec/... && CGO_ENABLED=0 go build ./...` clean (static-binary constraint intact — both new deps are pure Go)
- `grep -rn "html-to-markdown\|x/net" go.mod` shows exactly the two pinned additions
</verification>

<success_criteria>
- Zero-config first run: WebSearch returns real DDG results; WebFetch returns clean markdown (CMD-07, D-07, D-08)
- The seam stays swappable: configured backends override the default; unknown names fail loudly
- No network dependency in CI; live-service evidence is the phase gate's job (08-06), honoring the no-stub-only-evidence rule without flaky tests
</success_criteria>

<output>
Create `.planning/phases/08-slash-command-kickoff/08-02-SUMMARY.md` when done
</output>
