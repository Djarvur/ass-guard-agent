package profile

// Repeated string literals extracted to constants (goconst).
const sessionKindMain = "main"

// classSystemPrefix is the "system:" prefix of a CacheControlPlacements key
// (corpus_scan's "system:index=N" class form).
const classSystemPrefix = "system:"
