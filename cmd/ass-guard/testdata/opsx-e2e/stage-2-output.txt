# opsx E2E stage 2 closing output (captured 2026-08-15T20:20:34Z, real binary + real model)

All artifacts created! Ready for implementation.

## Change: `add-a-tiny-feature`

**Location:** `openspec/changes/add-a-tiny-feature/`

| Artifact | Description |
|----------|-------------|
| `proposal.md` | Adds `subtract(a, b int) int` alongside existing `add`, prints both results from `main()`, updates README |
| `specs/arithmetic-operations/spec.md` | New capability spec — addition, subtraction, and CLI output requirements with testable scenarios |
| `design.md` | Mirrors `add`'s style exactly; explicitly rejects over-abstraction (no interfaces, no op tables) |
| `tasks.md` | 5 tasks in 3 groups: implementation (2), docs (1), verification (2) |

**Scope fence:** Non-Goals exclude CLI parsing, new operations (multiply/divide), and file restructuring — this stays a two-function program.

Run `/opsx:apply` to start implementing.
