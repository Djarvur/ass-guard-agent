# target_capture_ref (PROF-03): provenance of the zcode profile extraction.
# D-16 data-source strategy: scan the rollout directory at extraction time;
# do NOT hardcode specific sessions or tool counts. The session below is the
# one the checked-in artifact was extracted from; the formal extractor
# (cmd/extract-profile, Plan 01-02) re-derives this live.
target_capture_ref:
  extracted_at: "2026-08-11T00:00:00Z"
  extractor_version: "manual-seed-01-01-T3"
  corrected_path: "~/.zcode/cli/rollout/model-io-sess_<session-id>.jsonl"
  sessions:
    - id: "016eee8a-f2f0-44c3-abf9-9d57a2cf04a1"
      role: "main"
      path: "~/.zcode/cli/rollout/model-io-sess_016eee8a-f2f0-44c3-abf9-9d57a2cf04a1.jsonl"
      note: "richest main session available at extraction time; 103 tools (19 built-in core + 84 MCP/plugin)"
# Parity-reference session (held-out per D-16 / RESEARCH-FLAG-01): disjoint from extraction source.
parity_reference_session:
  id: "subagent_agent_5b30d6eb-825f-4452-bf1b-e440723d87c8"
  role: "subagent"
  note: "disjoint session supplying divergence-prone multi-tool turns for the curated parity suite"
